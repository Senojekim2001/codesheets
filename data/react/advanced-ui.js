export const meta = {
  "title": "Advanced UI Patterns",
  "domain": "react",
  "sheet": "advanced-ui",
  "icon": "🎯"
}

export const sections = [

  // ── Section 1: Error Boundaries & Portals ─────────────────────────────────────────
  {
    id: "error-portals",
    title: "Error Boundaries & Portals",
    entries: [
      {
        id: "error-boundaries",
        fn: "Error Boundaries — Graceful Error Handling",
        desc: "Catch rendering errors with error boundaries: class-based boundaries, react-error-boundary library, fallback UIs, and reset patterns.",
        category: "Error Handling",
        subtitle: "ErrorBoundary, fallback, onReset, useErrorBoundary, resetKeys",
        signature: "<ErrorBoundary fallbackRender={({error, resetErrorBoundary}) => ...}>  |  useErrorBoundary()",
        descLong: "Error boundaries catch JavaScript errors in their child component tree during rendering, lifecycle methods, and constructors. They prevent the entire app from crashing. React has no built-in hook-based boundary — use the react-error-boundary library for a modern API with fallbackRender, onReset, and resetKeys. Wrap route-level or feature-level components. Error boundaries do NOT catch errors in event handlers, async code, or server-side rendering — use try/catch for those.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Error Boundaries — Graceful Error Handling — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - simplest usage — core API only\n// STRENGTHS - easy to understand, shows the basics\n// WEAKNESSES- no error handling, no edge cases\n//\nimport { ErrorBoundary, useErrorBoundary } from 'react-error-boundary';"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Error Boundaries — Graceful Error Handling — common patterns you'll see in production.\n// APPROACH  - Combine Error Boundaries — Graceful Error Handling with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - real-world usage with proper patterns\n// STRENGTHS - production-ready, handles common cases\n// WEAKNESSES- no advanced optimization\n//\nfunction ErrorFallback({ error, resetErrorBoundary }) {\n  return (\n    <div role=\"alert\" className=\"p-4 bg-red-50 rounded-lg\">\n      <h2 className=\"text-red-800 font-bold\">Something went wrong</h2>\n      <pre className=\"text-sm text-red-600 mt-2\">{error.message}</pre>\n      <button\n        onClick={resetErrorBoundary}\n        className=\"mt-4 px-4 py-2 bg-red-600 text-white rounded\"\n      >\n        Try again\n      </button>\n    </div>\n  );\n}\nfunction App() {\n  return (\n    <ErrorBoundary\n      FallbackComponent={ErrorFallback}\n      onReset={() => {\n        // Reset app state when user clicks \"Try again\"\n        queryClient.clear();\n      }}\n      resetKeys={[userId]}  // auto-reset when userId changes\n      onError={(error, info) => {\n        // Log to error tracking service\n        reportError(error, info.componentStack);\n      }}\n    >\n      <Dashboard />\n    </ErrorBoundary>\n  );\n}\nfunction Dashboard() {\n  return (\n    <div className=\"grid grid-cols-3 gap-4\">\n      <ErrorBoundary fallback={<p>Chart failed to load</p>}>\n        <RevenueChart />\n      </ErrorBoundary>\n      <ErrorBoundary fallback={<p>Table failed to load</p>}>\n        <UsersTable />\n      </ErrorBoundary>\n      <ErrorBoundary fallback={<p>Feed failed to load</p>}>\n        <ActivityFeed />\n      </ErrorBoundary>\n    </div>\n  );\n}"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Error Boundaries — Graceful Error Handling — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - advanced patterns, edge cases, and decision rules\n// STRENGTHS - comprehensive, handles edge cases\n// WEAKNESSES- N/A\n//\nfunction SaveButton({ data }) {\n  const { showBoundary } = useErrorBoundary();\n  async function handleSave() {\n    try {\n      await saveData(data);\n    } catch (error) {\n      // Propagate to nearest ErrorBoundary\n      showBoundary(error);\n    }\n  }\n  return <button onClick={handleSave}>Save</button>;\n}\nimport { createPortal } from 'react-dom';\nfunction Modal({ children, isOpen, onClose }) {\n  if (!isOpen) return null;\n  return createPortal(\n    <div className=\"fixed inset-0 z-50 flex items-center justify-center\">\n      <div className=\"absolute inset-0 bg-black/50\" onClick={onClose} />\n      <div className=\"relative bg-white rounded-xl p-6 max-w-lg w-full mx-4\">\n        {children}\n      </div>\n    </div>,\n    document.getElementById('portal-root')\n  );\n}\n// Tooltip that escapes overflow:hidden containers\nfunction Tooltip({ children, content, position }) {\n  const [coords, setCoords] = useState(null);\n  const ref = useRef(null);\n  return (\n    <>\n      <span ref={ref} onMouseEnter={() => {\n        const rect = ref.current.getBoundingClientRect();\n        setCoords({ top: rect.top - 8, left: rect.left + rect.width / 2 });\n      }} onMouseLeave={() => setCoords(null)}>\n        {children}\n      </span>\n      {coords && createPortal(\n        <div style={{ position: 'fixed', top: coords.top, left: coords.left }}\n             className=\"transform -translate-x-1/2 -translate-y-full bg-gray-900 text-white px-3 py-1 rounded text-sm\">\n          {content}\n        </div>,\n        document.body\n      )}\n    </>\n  );\n}"
                  }
        ],
        tips: [
                  "Wrap error boundaries at route/feature level, not around individual components — too granular defeats the purpose.",
                  "useErrorBoundary().showBoundary(error) bridges event handler errors to the boundary — error boundaries only catch render errors by default.",
                  "resetKeys={[userId]} auto-resets the boundary when the key changes — no manual \"try again\" needed for navigation.",
                  "Portals render into a different DOM node but still bubble React events up the component tree — onClick on a parent still catches portal clicks."
        ],
        mistake: "Wrapping the entire app in one error boundary — if anything fails, the whole app shows the error fallback. Use multiple boundaries so one failing widget does not take down the rest of the page.",
        shorthand: {
          verbose: "// Class component error boundary (verbose)\nclass EB extends React.Component {\n  componentDidCatch(e) { this.state.error = e; }\n  render() { return this.state.error ? <Err/> : this.props.children; }\n}",
          concise: "// react-error-boundary (concise)\n<ErrorBoundary fallback={<Err/>}>\n  <Component/>\n</ErrorBoundary>",
        },
      },
      {
        id: "portal-patterns",
        fn: "Portals — Render Outside DOM Hierarchy",
        desc: "Use ReactDOM.createPortal() to render components outside parent DOM — modals, tooltips, popovers, dropdowns.",
        category: "DOM Manipulation",
        subtitle: "createPortal, modals, tooltips, popovers, event bubbling",
        signature: "createPortal(children, domNode)",
        descLong: "createPortal renders a React component into a different DOM node outside the parent component tree. Useful for modals, dropdowns, and tooltips that need to escape overflow:hidden containers or z-index stacking contexts. Events still bubble up the React tree (not the DOM tree), so onClick handlers on parents still work. Portal content is rendered into the specified DOM node (usually a root-level div). Essential for layered UIs that need visual stacking above other content.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Portals — Render Outside DOM Hierarchy — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - simplest usage — core API only\n// STRENGTHS - easy to understand, shows the basics\n// WEAKNESSES- no error handling, no edge cases\n//\nimport { createPortal } from 'react-dom';\nimport { useState, useRef } from 'react';"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Portals — Render Outside DOM Hierarchy — common patterns you'll see in production.\n// APPROACH  - Combine Portals — Render Outside DOM Hierarchy with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - real-world usage with proper patterns\n// STRENGTHS - production-ready, handles common cases\n// WEAKNESSES- no advanced optimization\n//\nfunction Modal({ isOpen, onClose, children, title }) {\n  if (!isOpen) return null;\n  return createPortal(\n    <div className=\"fixed inset-0 z-50 flex items-center justify-center\">\n      {/* Backdrop */}\n      <div\n        className=\"absolute inset-0 bg-black/50\"\n        onClick={onClose}\n      />\n      {/* Modal content */}\n      <div className=\"relative bg-white rounded-xl shadow-2xl max-w-md w-full mx-4 p-6\">\n        <h2 className=\"text-xl font-bold mb-4\">{title}</h2>\n        {children}\n        <button\n          onClick={onClose}\n          className=\"mt-6 px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700\"\n        >\n          Close\n        </button>\n      </div>\n    </div>,\n    document.getElementById('modal-root')\n  );\n}\nfunction Dropdown({ children }) {\n  const [isOpen, setIsOpen] = useState(false);\n  const triggerRef = useRef(null);\n  const [position, setPosition] = useState(null);\n  function handleOpen() {\n    if (triggerRef.current) {\n      const rect = triggerRef.current.getBoundingClientRect();\n      setPosition({\n        top: rect.bottom + 8,\n        left: rect.left,\n        width: rect.width,\n      });\n      setIsOpen(true);\n    }\n  }\n  const dropdownContent = isOpen && position && createPortal(\n    <div\n      style={{\n        position: 'fixed',\n        top: position.top,\n        left: position.left,\n        width: position.width,\n        zIndex: 1000,\n      }}\n      className=\"bg-white border border-gray-200 rounded-lg shadow-lg\"\n    >\n      {children}\n    </div>,\n    document.body\n  );\n  return (\n    <>\n      <button ref={triggerRef} onClick={handleOpen}>\n        Open Menu\n      </button>\n      {dropdownContent}\n      {isOpen && (\n        <div className=\"fixed inset-0\" onClick={() => setIsOpen(false)} />\n      )}\n    </>\n  );\n}\nfunction Tooltip({ content, children, position = 'top' }) {\n  const [isVisible, setIsVisible] = useState(false);\n  const [coords, setCoords] = useState(null);\n  const triggerRef = useRef(null);\n  function handleMouseEnter() {\n    if (triggerRef.current) {\n      const rect = triggerRef.current.getBoundingClientRect();\n      const offset = 8;\n      const positions = {\n        top: { top: rect.top - offset, left: rect.left + rect.width / 2 },\n        bottom: { top: rect.bottom + offset, left: rect.left + rect.width / 2 },\n        left: { top: rect.top + rect.height / 2, left: rect.left - offset },\n        right: { top: rect.top + rect.height / 2, left: rect.right + offset },\n      };\n      setCoords(positions[position]);\n      setIsVisible(true);\n    }\n  }\n  const tooltipContent = isVisible && coords && createPortal(\n    <div\n      style={{\n        position: 'fixed',\n        top: coords.top,\n        left: coords.left,\n      }}\n      className=\"transform -translate-x-1/2 -translate-y-full bg-gray-900 text-white px-3 py-1 rounded text-sm whitespace-nowrap pointer-events-none\"\n    >\n      {content}\n    </div>,\n    document.body\n  );\n  return (\n    <>\n      <span\n        ref={triggerRef}\n        onMouseEnter={handleMouseEnter}\n        onMouseLeave={() => setIsVisible(false)}\n        className=\"cursor-help\"\n      >\n        {children}\n      </span>\n      {tooltipContent}\n    </>\n  );\n}"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Portals — Render Outside DOM Hierarchy — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - advanced patterns, edge cases, and decision rules\n// STRENGTHS - comprehensive, handles edge cases\n// WEAKNESSES- N/A\n//\nfunction Popover({ trigger, children }) {\n  const [isOpen, setIsOpen] = useState(false);\n  const triggerRef = useRef(null);\n  const [position, setPosition] = useState(null);\n  function handleClick() {\n    if (triggerRef.current && !isOpen) {\n      const rect = triggerRef.current.getBoundingClientRect();\n      setPosition({\n        top: rect.bottom + 8,\n        left: rect.left,\n      });\n      setIsOpen(true);\n    }\n  }\n  return (\n    <>\n      <button ref={triggerRef} onClick={handleClick}>\n        {trigger}\n      </button>\n      {isOpen && position && createPortal(\n        <div\n          style={{\n            position: 'fixed',\n            top: position.top,\n            left: position.left,\n            zIndex: 999,\n          }}\n          className=\"bg-white border border-gray-200 rounded-lg shadow-xl p-4 max-w-xs\"\n          onClick={e => e.stopPropagation()}\n        >\n          {children}\n        </div>,\n        document.body\n      )}\n      {isOpen && (\n        <div className=\"fixed inset-0\" onClick={() => setIsOpen(false)} />\n      )}\n    </>\n  );\n}\nfunction Toast({ message, onClose }) {\n  return createPortal(\n    <div className=\"fixed bottom-4 right-4 bg-green-600 text-white px-6 py-3 rounded-lg shadow-lg z-50\">\n      {message}\n      <button onClick={onClose} className=\"ml-4 font-bold\">×</button>\n    </div>,\n    document.getElementById('toast-root')\n  );\n}"
                  }
        ],
        tips: [
                  "createPortal renders into a different DOM node but maintains React event bubbling — onClick handlers still work.",
                  "Always use a fixed position container (modal-root, toast-root) in your index.html — create them at render time if needed.",
                  "Events in portal children still bubble to parent React components — this is powerful for capturing clicks outside modals.",
                  "Use z-index carefully with portals — portal content sits outside normal stacking context, so high z-index values may overlap unintentionally."
        ],
        mistake: "Creating portal DOM nodes dynamically inside the component — portal nodes should exist in HTML before React renders. Create them in index.html or in useEffect with error handling.",
        shorthand: {
          verbose: "// Manual DOM manipulation (verbose, error-prone)\nfunction Modal() {\n  useEffect(() => {\n    const el = document.createElement('div');\n    document.body.appendChild(el);\n    ReactDOM.render(<Content/>, el);\n    return () => el.remove();\n  }, []);\n}",
          concise: "// Portal (concise, declarative)\nfunction Modal() {\n  return createPortal(<Content/>, document.getElementById('modal-root'));\n}",
        },
      },
      {
        id: "compound-components",
        fn: "Compound Component Pattern",
        desc: "Build flexible composable components using implicit sharing via Context — `<Select><Option>`.",
        category: "Patterns",
        subtitle: "Compound components, Context-based sharing, flexible APIs, implicit data flow",
        signature: "<Select><Option value=\"a\">A</Option></Select>",
        descLong: "Compound components are a group of related components that work together implicitly through Context. Unlike props drilling, children components automatically receive data from a parent compound component via Context. Example: `<Select>` and `<Option>` form a pair where Option doesn't need props because it reads from SelectContext. This pattern creates flexible, composable APIs while hiding implementation details. Perfect for complex UI libraries (form inputs, tabs, accordions, menus).",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Compound Component Pattern — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - simplest usage — core API only\n// STRENGTHS - easy to understand, shows the basics\n// WEAKNESSES- no error handling, no edge cases\n//\nimport { createContext, useContext, useState } from 'react';"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Compound Component Pattern — common patterns you'll see in production.\n// APPROACH  - Combine Compound Component Pattern with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - real-world usage with proper patterns\n// STRENGTHS - production-ready, handles common cases\n// WEAKNESSES- no advanced optimization\n//\nconst SelectContext = createContext();\nfunction Select({ children, value, onChange }) {\n  return (\n    <SelectContext.Provider value={{ value, onChange }}>\n      <div className=\"flex flex-col\">\n        {children}\n      </div>\n    </SelectContext.Provider>\n  );\n}\nfunction Option({ value, children }) {\n  const { value: selectedValue, onChange } = useContext(SelectContext);\n  return (\n    <label className=\"flex items-center gap-2 cursor-pointer mb-2\">\n      <input\n        type=\"radio\"\n        name=\"select\"\n        value={value}\n        checked={selectedValue === value}\n        onChange={() => onChange(value)}\n      />\n      {children}\n    </label>\n  );\n}\n// Usage:\nfunction App() {\n  const [selected, setSelected] = useState('apple');\n  return (\n    <Select value={selected} onChange={setSelected}>\n      <Option value=\"apple\">Apple</Option>\n      <Option value=\"banana\">Banana</Option>\n      <Option value=\"cherry\">Cherry</Option>\n    </Select>\n  );\n}\nconst TabsContext = createContext();\nfunction Tabs({ children, defaultTab = 0 }) {\n  const [activeTab, setActiveTab] = useState(defaultTab);\n  return (\n    <TabsContext.Provider value={{ activeTab, setActiveTab }}>\n      <div>{children}</div>\n    </TabsContext.Provider>\n  );\n}\nfunction TabList({ children }) {\n  const { activeTab } = useContext(TabsContext);\n  return <div className=\"flex border-b\">{children}</div>;\n}\nfunction TabButton({ value, children }) {\n  const { activeTab, setActiveTab } = useContext(TabsContext);\n  const isActive = activeTab === value;\n  return (\n    <button\n      onClick={() => setActiveTab(value)}\n      className={isActive ? 'border-b-2 border-blue-600 px-4 py-2' : 'px-4 py-2'}\n    >\n      {children}\n    </button>\n  );\n}\nfunction TabContent({ value, children }) {\n  const { activeTab } = useContext(TabsContext);\n  return activeTab === value ? <div>{children}</div> : null;\n}\n// Usage:\nfunction TabsDemo() {\n  return (\n    <Tabs defaultTab={0}>\n      <TabList>\n        <TabButton value={0}>Home</TabButton>\n        <TabButton value={1}>About</TabButton>\n        <TabButton value={2}>Contact</TabButton>\n      </TabList>\n      <TabContent value={0}>Home content</TabContent>\n      <TabContent value={1}>About content</TabContent>\n      <TabContent value={2}>Contact content</TabContent>\n    </Tabs>\n  );\n}"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Compound Component Pattern — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - advanced patterns, edge cases, and decision rules\n// STRENGTHS - comprehensive, handles edge cases\n// WEAKNESSES- N/A\n//\nconst AccordionContext = createContext();\nfunction Accordion({ children }) {\n  const [openItems, setOpenItems] = useState(new Set());\n  function toggle(id) {\n    const newOpen = new Set(openItems);\n    if (newOpen.has(id)) {\n      newOpen.delete(id);\n    } else {\n      newOpen.add(id);\n    }\n    setOpenItems(newOpen);\n  }\n  return (\n    <AccordionContext.Provider value={{ openItems, toggle }}>\n      <div className=\"border rounded-lg divide-y\">{children}</div>\n    </AccordionContext.Provider>\n  );\n}\nfunction AccordionItem({ id, children }) {\n  const { openItems, toggle } = useContext(AccordionContext);\n  const isOpen = openItems.has(id);\n  return (\n    <div>\n      <button\n        onClick={() => toggle(id)}\n        className=\"w-full text-left px-4 py-3 font-semibold hover:bg-gray-50\"\n      >\n        {children}\n      </button>\n    </div>\n  );\n}\nfunction AccordionPanel({ id, children }) {\n  const { openItems } = useContext(AccordionContext);\n  if (!openItems.has(id)) return null;\n  return <div className=\"px-4 py-3 bg-gray-50\">{children}</div>;\n}\n// Usage:\nfunction AccordionDemo() {\n  return (\n    <Accordion>\n      <AccordionItem id=\"1\">What is React?</AccordionItem>\n      <AccordionPanel id=\"1\">React is a JavaScript library for building UIs.</AccordionPanel>\n      <AccordionItem id=\"2\">Why use React?</AccordionItem>\n      <AccordionPanel id=\"2\">React makes building interactive UIs easy and scalable.</AccordionPanel>\n    </Accordion>\n  );\n}"
                  }
        ],
        tips: [
                  "Compound components use Context to communicate — no need to pass callbacks and state through every layer.",
                  "Design the API for composability — allow children to be reordered, hidden, or conditionally rendered.",
                  "Use unique IDs or keys in AccordionItem, TabButton — Context depends on knowing which child is which.",
                  "Compound components are best for libraries and complex, related components — don't overuse for simple cases."
        ],
        mistake: "Forcing compound component structure when simple props would work — if there are only 2-3 components and minimal state, props are simpler.",
        shorthand: {
          verbose: "// Props drilling (verbose, deeply nested)\n<Select\n  value={v}\n  onChange={setV}\n  options={opts}\n  renderOption={(opt) => <Option opt={opt} value={v} onChange={setV} />}\n/>",
          concise: "// Compound components (concise, implicit)\n<Select value={v} onChange={setV}>\n  {opts.map(opt => <Option value={opt} key={opt} />)}\n</Select>",
        },
      },
      {
        id: "render-props-pattern",
        fn: "Render Props Pattern",
        desc: "Pass a function as a child or prop to share state/logic — children as function, render prop, vs hooks.",
        category: "Patterns",
        subtitle: "render prop, children as function, function components, logic sharing",
        signature: "<DataFetcher render={data => <View data={data} />} />  |  <DataFetcher>{data => <View data={data} />}</DataFetcher>",
        descLong: "Render props is a pattern where a component accepts a function as a prop (or children) that returns JSX. The component manages logic/state and calls the function with the result, allowing the parent to control rendering. Example: `<DataFetcher render={data => <h1>{data}</h1>} />`. Useful for logic extraction before hooks existed. Today, hooks are preferred for most cases (simpler code), but render props is still useful for: library components that accept custom rendering, components that wrap multiple layers of state.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Render Props Pattern — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - simplest usage — core API only\n// STRENGTHS - easy to understand, shows the basics\n// WEAKNESSES- no error handling, no edge cases\n//\nimport { useState, useEffect, ReactNode } from 'react';"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Render Props Pattern — common patterns you'll see in production.\n// APPROACH  - Combine Render Props Pattern with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - real-world usage with proper patterns\n// STRENGTHS - production-ready, handles common cases\n// WEAKNESSES- no advanced optimization\n//\nfunction DataFetcher({ url, render, children }) {\n  const [data, setData] = useState(null);\n  const [loading, setLoading] = useState(true);\n  const [error, setError] = useState(null);\n  useEffect(() => {\n    fetch(url)\n      .then(r => r.json())\n      .then(data => { setData(data); setLoading(false); })\n      .catch(err => { setError(err); setLoading(false); });\n  }, [url]);\n  // Can use either 'render' prop or 'children' as function\n  const renderFn = render || children;\n  return renderFn({ data, loading, error });\n}\nfunction App() {\n  return (\n    <DataFetcher\n      url=\"/api/users\"\n      render={({ data, loading, error }) =>\n        loading ? (\n          <p>Loading...</p>\n        ) : error ? (\n          <p>Error: {error.message}</p>\n        ) : (\n          <ul>\n            {data.map(user => (\n              <li key={user.id}>{user.name}</li>\n            ))}\n          </ul>\n        )\n      }\n    />\n  );\n}\nfunction App2() {\n  return (\n    <DataFetcher url=\"/api/posts\">\n      {({ data, loading, error }) => (\n        <>\n          {loading && <p>Loading...</p>}\n          {error && <p>Error: {error.message}</p>}\n          {data && (\n            <div>\n              {data.map(post => (\n                <article key={post.id}>\n                  <h2>{post.title}</h2>\n                  <p>{post.body}</p>\n                </article>\n              ))}\n            </div>\n          )}\n        </>\n      )}\n    </DataFetcher>\n  );\n}"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Render Props Pattern — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - advanced patterns, edge cases, and decision rules\n// STRENGTHS - comprehensive, handles edge cases\n// WEAKNESSES- N/A\n//\nfunction MouseTracker({ children }) {\n  const [position, setPosition] = useState({ x: 0, y: 0 });\n  useEffect(() => {\n    function handleMouseMove(event) {\n      setPosition({ x: event.clientX, y: event.clientY });\n    }\n    window.addEventListener('mousemove', handleMouseMove);\n    return () => window.removeEventListener('mousemove', handleMouseMove);\n  }, []);\n  return children(position);\n}\nfunction App3() {\n  return (\n    <MouseTracker>\n      {position => (\n        <div>\n          <p>Mouse position: {position.x}, {position.y}</p>\n          <div\n            style={{\n              position: 'absolute',\n              left: position.x,\n              top: position.y,\n              width: 10,\n              height: 10,\n              borderRadius: '50%',\n              backgroundColor: 'red',\n              pointerEvents: 'none',\n            }}\n          />\n        </div>\n      )}\n    </MouseTracker>\n  );\n}\nfunction FormState({ initialValues, onSubmit, children }) {\n  const [values, setValues] = useState(initialValues);\n  const [errors, setErrors] = useState({});\n  const [touched, setTouched] = useState({});\n  function handleChange(e) {\n    const { name, value } = e.target;\n    setValues(prev => ({ ...prev, [name]: value }));\n  }\n  function handleBlur(e) {\n    const { name } = e.target;\n    setTouched(prev => ({ ...prev, [name]: true }));\n  }\n  async function handleSubmit(e) {\n    e.preventDefault();\n    await onSubmit(values);\n  }\n  return children({\n    values,\n    errors,\n    touched,\n    handleChange,\n    handleBlur,\n    handleSubmit,\n  });\n}\nfunction LoginForm() {\n  return (\n    <FormState\n      initialValues={{ email: '', password: '' }}\n      onSubmit={async (values) => {\n        await fetch('/api/login', {\n          method: 'POST',\n          body: JSON.stringify(values),\n        });\n      }}\n    >\n      {({ values, handleChange, handleBlur, handleSubmit }) => (\n        <form onSubmit={handleSubmit}>\n          <input\n            name=\"email\"\n            value={values.email}\n            onChange={handleChange}\n            onBlur={handleBlur}\n            placeholder=\"Email\"\n          />\n          <input\n            name=\"password\"\n            type=\"password\"\n            value={values.password}\n            onChange={handleChange}\n            onBlur={handleBlur}\n            placeholder=\"Password\"\n          />\n          <button type=\"submit\">Login</button>\n        </form>\n      )}\n    </FormState>\n  );\n}\n// Render props version:\nfunction withDataFetch(url) {\n  return function RenderPropWrapper({ children }) {\n    const [data, setData] = useState(null);\n    // ... fetch logic\n    return children(data);\n  };\n}\n// Hooks version (cleaner):\nfunction useFetch(url) {\n  const [data, setData] = useState(null);\n  // ... fetch logic\n  return data;\n}\n// In component:\nfunction MyComponent() {\n  const data = useFetch('/api/data');  // Hooks: simpler\n  return <div>{data}</div>;\n}"
                  }
        ],
        tips: [
                  "Render props and children-as-function are similar — use whichever reads better (children is often cleaner).",
                  "Render props are useful for wrapping multiple layers of state in libraries, but hooks are usually simpler for new code.",
                  "Always make sure the function identity is stable — if render prop is defined inline, it'll recreate on every render.",
                  "You can combine render props with hooks — a hook can use a component that takes render props."
        ],
        mistake: "Defining render prop inline: `<DataFetcher render={() => <View />} />` — this creates a new function every render, breaking memoization. Define it outside or useMemo it.",
        shorthand: {
          verbose: "// Manual / verbose approach\n// Old: render props (verbose, wrapper function)\n<DataFetch url=\"/data\" render={d => <View data={d} />} />\n// More explicit but longer",
          concise: "// Modern: hooks (concise, direct)\nconst data = useFetch('/data');\nreturn <View data={data} />;",
        },
      },
      {
        id: "controlled-uncontrolled",
        fn: "Controlled vs Uncontrolled Components",
        desc: "Choose between ref-based (uncontrolled) and state-based (controlled) form inputs.",
        category: "Form Patterns",
        subtitle: "controlled components, uncontrolled components, useRef, useState, form inputs",
        signature: "<input value={state} onChange={setState} />  |  <input ref={ref} />",
        descLong: "Controlled components use React state to manage form input values — React is the single source of truth. Uncontrolled components use refs to access DOM values directly — the DOM is the source of truth. Controlled: easier to validate in real-time, better for form validation logic, required for conditional rendering. Uncontrolled: simpler for read-only forms, integrates with non-React code, better performance for large forms. Most modern forms use controlled components, but uncontrolled is useful for file inputs and integrating with external libraries.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Controlled vs Uncontrolled Components — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - simplest usage — core API only\n// STRENGTHS - easy to understand, shows the basics\n// WEAKNESSES- no error handling, no edge cases\n//\nimport { useState, useRef } from 'react';"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Controlled vs Uncontrolled Components — common patterns you'll see in production.\n// APPROACH  - Combine Controlled vs Uncontrolled Components with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - real-world usage with proper patterns\n// STRENGTHS - production-ready, handles common cases\n// WEAKNESSES- no advanced optimization\n//\nfunction ControlledForm() {\n  const [name, setName] = useState('');\n  const [email, setEmail] = useState('');\n  const [errors, setErrors] = useState({});\n  function handleChange(e) {\n    const { name, value } = e.target;\n    if (name === 'name') setName(value);\n    if (name === 'email') setEmail(value);\n    // Real-time validation\n    if (name === 'email' && !value.includes('@')) {\n      setErrors(prev => ({ ...prev, email: 'Invalid email' }));\n    } else {\n      setErrors(prev => ({ ...prev, [name]: null }));\n    }\n  }\n  function handleSubmit(e) {\n    e.preventDefault();\n    console.log({ name, email });\n  }\n  return (\n    <form onSubmit={handleSubmit}>\n      <div>\n        <input\n          name=\"name\"\n          value={name}\n          onChange={handleChange}\n          placeholder=\"Name\"\n        />\n      </div>\n      <div>\n        <input\n          name=\"email\"\n          value={email}\n          onChange={handleChange}\n          placeholder=\"Email\"\n        />\n        {errors.email && <span className=\"text-red-600\">{errors.email}</span>}\n      </div>\n      <button type=\"submit\" disabled={!!errors.email || !name || !email}>\n        Submit\n      </button>\n    </form>\n  );\n}\nfunction UncontrolledForm() {\n  const nameRef = useRef();\n  const emailRef = useRef();\n  function handleSubmit(e) {\n    e.preventDefault();\n    const formData = {\n      name: nameRef.current.value,\n      email: emailRef.current.value,\n    };\n    console.log(formData);\n  }\n  function handleReset() {\n    nameRef.current.value = '';\n    emailRef.current.value = '';\n  }\n  return (\n    <form onSubmit={handleSubmit}>\n      <input ref={nameRef} defaultValue=\"\" placeholder=\"Name\" />\n      <input ref={emailRef} defaultValue=\"\" placeholder=\"Email\" />\n      <button type=\"submit\">Submit</button>\n      <button type=\"button\" onClick={handleReset}>\n        Reset\n      </button>\n    </form>\n  );\n}\nfunction MixedForm() {\n  const [email, setEmail] = useState('');  // Controlled: need validation\n  const passwordRef = useRef();  // Uncontrolled: just get value on submit\n  function handleSubmit(e) {\n    e.preventDefault();\n    console.log({\n      email,\n      password: passwordRef.current.value,\n    });\n  }\n  return (\n    <form onSubmit={handleSubmit}>\n      <input\n        value={email}\n        onChange={e => setEmail(e.target.value)}\n        placeholder=\"Email\"\n      />\n      <input ref={passwordRef} type=\"password\" placeholder=\"Password\" />\n      <button type=\"submit\">Login</button>\n    </form>\n  );\n}"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Controlled vs Uncontrolled Components — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - advanced patterns, edge cases, and decision rules\n// STRENGTHS - comprehensive, handles edge cases\n// WEAKNESSES- N/A\n//\nfunction DynamicForm() {\n  const [fields, setFields] = useState(['', '']);\n  function handleFieldChange(index, value) {\n    setFields(prev => {\n      const newFields = [...prev];\n      newFields[index] = value;\n      return newFields;\n    });\n  }\n  function handleAddField() {\n    setFields(prev => [...prev, '']);\n  }\n  return (\n    <form>\n      {fields.map((value, index) => (\n        <input\n          key={index}\n          value={value}\n          onChange={e => handleFieldChange(index, e.target.value)}\n          placeholder={`Field ${index + 1}`}\n        />\n      ))}\n      <button type=\"button\" onClick={handleAddField}>\n        Add Field\n      </button>\n    </form>\n  );\n}\nfunction FileUpload() {\n  const fileRef = useRef();\n  function handleSubmit(e) {\n    e.preventDefault();\n    const file = fileRef.current.files[0];\n    console.log('Selected file:', file);\n    // Can't use onChange for real-time file info\n    // File inputs are always uncontrolled\n  }\n  return (\n    <form onSubmit={handleSubmit}>\n      <input ref={fileRef} type=\"file\" />\n      <button type=\"submit\">Upload</button>\n    </form>\n  );\n}"
                  }
        ],
        tips: [
                  "Use controlled for form validation, dynamic fields, and conditional rendering — React must manage state.",
                  "Use uncontrolled for read-only forms, file inputs, and integrating with jQuery/non-React libraries.",
                  "File inputs MUST be uncontrolled — you can't set the value attribute for security reasons.",
                  "Mixing both patterns in one form is fine — control what needs validation, leave the rest as uncontrolled."
        ],
        mistake: "Using uncontrolled for form validation — you can't validate or disable submit without accessing the DOM directly. Use controlled for validated forms.",
        shorthand: {
          verbose: "// Manual uncontrolled workaround (verbose, error-prone)\nconst form = document.querySelector('form');\nform.addEventListener('submit', () => {\n  console.log(form.elements.name.value);\n});",
          concise: "// Controlled (concise, React-driven)\nconst [name, setName] = useState('');\n<input value={name} onChange={e => setName(e.target.value)} />",
        },
      },
      {
        id: "forward-ref-pattern",
        fn: "forwardRef & useImperativeHandle — Ref Composition",
        desc: "Expose component API via refs: forwardRef passes refs through, useImperativeHandle customizes what's exposed.",
        category: "Advanced Refs",
        subtitle: "forwardRef, useImperativeHandle, ref composition, imperative APIs",
        signature: "forwardRef((props, ref) => ...)  |  useImperativeHandle(ref, () => ({method}))",
        descLong: "By default, components don't forward refs — forwardRef wraps a component to make it ref-aware. useImperativeHandle customizes what a ref exposes, allowing you to call imperative methods like `focus()`, `reset()`, `validate()`. Combine them to create reusable components that expose imperative APIs. Example: a custom TextInput can expose `focus()` and `select()` methods. Use sparingly — most cases should use props/state, but refs are essential for focus management, media playback, and integrating with third-party libraries.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of forwardRef & useImperativeHandle — Ref Composition — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - simplest usage — core API only\n// STRENGTHS - easy to understand, shows the basics\n// WEAKNESSES- no error handling, no edge cases\n//\nimport { forwardRef, useImperativeHandle, useRef, useState } from 'react';"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of forwardRef & useImperativeHandle — Ref Composition — common patterns you'll see in production.\n// APPROACH  - Combine forwardRef & useImperativeHandle — Ref Composition with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - real-world usage with proper patterns\n// STRENGTHS - production-ready, handles common cases\n// WEAKNESSES- no advanced optimization\n//\nconst CustomInput = forwardRef(function CustomInput(\n  { placeholder, ...props },\n  ref\n) {\n  const inputRef = useRef();\n  useImperativeHandle(ref, () => ({\n    focus: () => inputRef.current?.focus(),\n    select: () => inputRef.current?.select(),\n    clear: () => { inputRef.current.value = ''; },\n    getValue: () => inputRef.current?.value || '',\n  }));\n  return (\n    <input ref={inputRef} placeholder={placeholder} {...props} />\n  );\n});\n// Usage:\nfunction Form() {\n  const inputRef = useRef();\n  function handleFocus() {\n    inputRef.current?.focus();\n  }\n  function handleClear() {\n    inputRef.current?.clear();\n  }\n  return (\n    <div>\n      <CustomInput ref={inputRef} placeholder=\"Type something...\" />\n      <button onClick={handleFocus}>Focus</button>\n      <button onClick={handleClear}>Clear</button>\n    </div>\n  );\n}\nconst Modal = forwardRef(function Modal({ children }, ref) {\n  const [isOpen, setIsOpen] = useState(false);\n  useImperativeHandle(ref, () => ({\n    open: () => setIsOpen(true),\n    close: () => setIsOpen(false),\n  }));\n  if (!isOpen) return null;\n  return (\n    <div className=\"fixed inset-0 z-50 flex items-center justify-center\">\n      <div className=\"absolute inset-0 bg-black/50\" onClick={() => setIsOpen(false)} />\n      <div className=\"relative bg-white rounded-lg p-6\">\n        {children}\n      </div>\n    </div>\n  );\n});\nfunction App() {\n  const modalRef = useRef();\n  return (\n    <div>\n      <button onClick={() => modalRef.current?.open()}>Open Modal</button>\n      <Modal ref={modalRef}>\n        <h2>Modal Title</h2>\n        <p>Modal content</p>\n        <button onClick={() => modalRef.current?.close()}>Close</button>\n      </Modal>\n    </div>\n  );\n}"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of forwardRef & useImperativeHandle — Ref Composition — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - advanced patterns, edge cases, and decision rules\n// STRENGTHS - comprehensive, handles edge cases\n// WEAKNESSES- N/A\n//\nconst VideoPlayer = forwardRef(function VideoPlayer({ src }, ref) {\n  const videoRef = useRef();\n  useImperativeHandle(ref, () => ({\n    play: () => videoRef.current?.play(),\n    pause: () => videoRef.current?.pause(),\n    setVolume: (vol) => { videoRef.current.volume = vol; },\n    getCurrentTime: () => videoRef.current?.currentTime || 0,\n  }));\n  return <video ref={videoRef} src={src} />;\n});\nfunction VideoApp() {\n  const playerRef = useRef();\n  return (\n    <div>\n      <VideoPlayer ref={playerRef} src=\"video.mp4\" />\n      <button onClick={() => playerRef.current?.play()}>Play</button>\n      <button onClick={() => playerRef.current?.pause()}>Pause</button>\n      <input\n        type=\"range\"\n        min=\"0\"\n        max=\"1\"\n        step=\"0.01\"\n        onChange={e => playerRef.current?.setVolume(e.target.value)}\n      />\n    </div>\n  );\n}\nconst Form = forwardRef(function Form({ onSubmit }, ref) {\n  const [errors, setErrors] = useState({});\n  const fieldsRef = useRef({});\n  useImperativeHandle(ref, () => ({\n    validate: () => {\n      const newErrors = {};\n      if (!fieldsRef.current.email?.value?.includes('@')) {\n        newErrors.email = 'Invalid email';\n      }\n      setErrors(newErrors);\n      return Object.keys(newErrors).length === 0;\n    },\n    getValues: () => ({\n      email: fieldsRef.current.email?.value,\n      name: fieldsRef.current.name?.value,\n    }),\n    reset: () => {\n      fieldsRef.current.email.value = '';\n      fieldsRef.current.name.value = '';\n      setErrors({});\n    },\n  }));\n  return (\n    <form\n      onSubmit={e => {\n        e.preventDefault();\n        onSubmit?.(fieldsRef.current);\n      }}\n    >\n      <input\n        ref={el => fieldsRef.current.name = el}\n        placeholder=\"Name\"\n      />\n      <input\n        ref={el => fieldsRef.current.email = el}\n        placeholder=\"Email\"\n      />\n      {errors.email && <p className=\"text-red-600\">{errors.email}</p>}\n      <button type=\"submit\">Submit</button>\n    </form>\n  );\n});\nfunction FormContainer() {\n  const formRef = useRef();\n  function handleValidateAndSubmit() {\n    if (formRef.current?.validate()) {\n      const values = formRef.current.getValues();\n      console.log('Form is valid:', values);\n    }\n  }\n  return (\n    <div>\n      <Form ref={formRef} />\n      <button onClick={handleValidateAndSubmit}>\n        Validate & Submit\n      </button>\n      <button onClick={() => formRef.current?.reset()}>\n        Reset\n      </button>\n    </div>\n  );\n}"
                  }
        ],
        tips: [
                  "Only expose methods that are truly imperative — don't expose every DOM method, just the essential ones.",
                  "Prefer props and state over refs — refs should be last resort for focus, media, integration with non-React libraries.",
                  "useImperativeHandle must be used with forwardRef — forwardRef tells React to pass the ref through.",
                  "Always check ref is not null before calling methods: `ref.current?.focus()` — refs can be undefined."
        ],
        mistake: "Exposing too much via useImperativeHandle — only expose methods that are truly necessary. Expose state as methods, not as direct ref access.",
        shorthand: {
          verbose: "// Without forwardRef/useImperativeHandle\nconst form = document.querySelector('form');\nform.querySelectorAll('input').forEach(input => input.value = '');",
          concise: "// With useImperativeHandle\nconst formRef = useRef();\nformRef.current?.reset();",
        },
      },
      {
        id: "slot-pattern",
        fn: "Slot Pattern — Composable Layouts with Named Children",
        desc: "Build flexible layouts by passing named children (slots) for header, content, footer, etc.",
        category: "Patterns",
        subtitle: "slot pattern, named children, flexible layouts, composition",
        signature: "<Card header={<Header/>} body={<Body/>} footer={<Footer/>} />  |  <Card.Header/>",
        descLong: "The slot pattern (from Vue/Web Components) adapts to React: pass components as props (header, footer, body) or use a compound pattern with named exports. Allows flexible, composable layouts without complex children parsing. Example: `<Card header={<h1>Title</h1>} footer={<p>Footer</p>}>{body}</Card>`. Simpler than render props for layout patterns, more explicit than spread children. Perfect for layout components (Card, Modal, Panel) where you need to place content in specific areas.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Slot Pattern — Composable Layouts with Named Children — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - simplest usage — core API only\n// STRENGTHS - easy to understand, shows the basics\n// WEAKNESSES- no error handling, no edge cases\n//\nimport { ReactNode } from 'react';"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Slot Pattern — Composable Layouts with Named Children — common patterns you'll see in production.\n// APPROACH  - Combine Slot Pattern — Composable Layouts with Named Children with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - real-world usage with proper patterns\n// STRENGTHS - production-ready, handles common cases\n// WEAKNESSES- no advanced optimization\n//\nfunction Card({\n  header,\n  body,\n  footer,\n  className = '',\n}) {\n  return (\n    <div className={`border rounded-lg shadow p-6 ${className}`}>\n      {header && (\n        <div className=\"border-b pb-4 mb-4\">\n          {header}\n        </div>\n      )}\n      <div className=\"mb-4\">\n        {body}\n      </div>\n      {footer && (\n        <div className=\"border-t pt-4 mt-4\">\n          {footer}\n        </div>\n      )}\n    </div>\n  );\n}\nfunction Usage() {\n  return (\n    <Card\n      header={<h1 className=\"text-2xl font-bold\">Card Title</h1>}\n      body={<p>This is the main content of the card.</p>}\n      footer={<p className=\"text-sm text-gray-500\">Footer text</p>}\n    />\n  );\n}\nfunction CardCompound({ children }) {\n  return <div className=\"border rounded-lg shadow\">{children}</div>;\n}\nfunction CardHeader({ children }) {\n  return <div className=\"border-b pb-4 mb-4 px-6 pt-6\">{children}</div>;\n}\nfunction CardBody({ children }) {\n  return <div className=\"px-6 mb-4\">{children}</div>;\n}\nfunction CardFooter({ children }) {\n  return <div className=\"border-t pt-4 px-6 pb-6\">{children}</div>;\n}\n// Attach sub-components\nCardCompound.Header = CardHeader;\nCardCompound.Body = CardBody;\nCardCompound.Footer = CardFooter;\nfunction CompoundUsage() {\n  return (\n    <CardCompound>\n      <CardCompound.Header>\n        <h1 className=\"text-2xl font-bold\">Title</h1>\n      </CardCompound.Header>\n      <CardCompound.Body>\n        <p>Content goes here</p>\n      </CardCompound.Body>\n      <CardCompound.Footer>\n        <button>Action</button>\n      </CardCompound.Footer>\n    </CardCompound>\n  );\n}"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Slot Pattern — Composable Layouts with Named Children — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - advanced patterns, edge cases, and decision rules\n// STRENGTHS - comprehensive, handles edge cases\n// WEAKNESSES- N/A\n//\nfunction Modal({\n  header,\n  body,\n  footer,\n  isOpen,\n  onClose,\n}) {\n  if (!isOpen) return null;\n  return (\n    <div className=\"fixed inset-0 z-50 flex items-center justify-center\">\n      <div\n        className=\"absolute inset-0 bg-black/50\"\n        onClick={onClose}\n      />\n      <div className=\"relative bg-white rounded-xl shadow-2xl max-w-md w-full mx-4\">\n        {header && (\n          <div className=\"border-b px-6 py-4\">\n            {header}\n          </div>\n        )}\n        {body && (\n          <div className=\"px-6 py-4\">\n            {body}\n          </div>\n        )}\n        {footer && (\n          <div className=\"border-t px-6 py-4 flex gap-2 justify-end\">\n            {footer}\n          </div>\n        )}\n      </div>\n    </div>\n  );\n}\nfunction ModalUsage() {\n  const [isOpen, setIsOpen] = useState(false);\n  return (\n    <>\n      <button onClick={() => setIsOpen(true)}>Open Modal</button>\n      <Modal\n        isOpen={isOpen}\n        onClose={() => setIsOpen(false)}\n        header={<h2 className=\"text-xl font-bold\">Confirm Action</h2>}\n        body={<p>Are you sure you want to proceed?</p>}\n        footer={\n          <>\n            <button\n              onClick={() => setIsOpen(false)}\n              className=\"px-4 py-2 border rounded\"\n            >\n              Cancel\n            </button>\n            <button\n              onClick={() => setIsOpen(false)}\n              className=\"px-4 py-2 bg-blue-600 text-white rounded\"\n            >\n              Confirm\n            </button>\n          </>\n        }\n      />\n    </>\n  );\n}\nfunction Layout({\n  sidebar,\n  main,\n  topbar,\n  bottombar,\n}) {\n  return (\n    <div className=\"flex flex-col h-screen\">\n      {topbar && <div className=\"bg-gray-100 p-4\">{topbar}</div>}\n      <div className=\"flex flex-1 overflow-hidden\">\n        {sidebar && (\n          <aside className=\"w-64 bg-gray-50 border-r p-4 overflow-auto\">\n            {sidebar}\n          </aside>\n        )}\n        <main className=\"flex-1 overflow-auto p-6\">\n          {main}\n        </main>\n      </div>\n      {bottombar && <div className=\"bg-gray-100 p-4 border-t\">{bottombar}</div>}\n    </div>\n  );\n}\nfunction LayoutUsage() {\n  return (\n    <Layout\n      topbar={<h1>My App</h1>}\n      sidebar={<nav>Navigation</nav>}\n      main={<div>Main content</div>}\n      bottombar={<footer>Footer</footer>}\n    />\n  );\n}"
                  }
        ],
        tips: [
                  "Slot pattern is cleaner than deeply nested children parsing — explicitly name each slot.",
                  "Use compound components when slots need internal state sharing; use props-based slots when they're independent.",
                  "Optional slots: always check if they exist before rendering — use && or conditional rendering.",
                  "Combine slots with className prop for flexible styling — allow customization at each slot."
        ],
        mistake: "Mixing slot names carelessly — use consistent naming (header/body/footer or top/middle/bottom), don't use vague names like \"left\" or \"right\".",
        shorthand: {
          verbose: "// React.Children parsing (verbose, error-prone)\n{React.Children.map(children, (child) => {\n  if (child.type.name === 'Header') return <HeaderSlot>{child}</HeaderSlot>;\n  if (child.type.name === 'Body') return <BodySlot>{child}</BodySlot>;\n})}",
          concise: "// Named slots (concise, explicit)\nfunction Card({ header, body, footer }) {\n  return (\n    <>\n      <div>{header}</div>\n      <div>{body}</div>\n      <div>{footer}</div>\n    </>\n  );\n}",
        },
      },
      {
        id: "virtualization-list",
        fn: "Virtualization — Rendering Large Lists Efficiently (Extended)",
        desc: "Advanced virtualization: grids, variable heights, infinite scroll, dynamic sizing.",
        category: "Performance",
        subtitle: "@tanstack/react-virtual, VariableSizeList, grids, dynamic sizing, infinite scroll",
        signature: "useVirtualizer({ count, getScrollElement, estimateSize, measureElement })",
        descLong: "Advanced virtualization patterns beyond basic lists. TanStack Virtual supports grids (2D virtualization), variable row heights with dynamic measurement, and infinite scroll. Key techniques: measureElement for dynamic heights, calculating grid offsets for 2D layouts, measuring new items as they load. Perfect for large tables, image galleries, infinite feeds, and any data that changes dynamically.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Virtualization — Rendering Large Lists Efficiently (Extended) — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - simplest usage — core API only\n// STRENGTHS - easy to understand, shows the basics\n// WEAKNESSES- no error handling, no edge cases\n//\nimport { useVirtualizer } from '@tanstack/react-virtual';\nimport { useRef, useState } from 'react';"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Virtualization — Rendering Large Lists Efficiently (Extended) — common patterns you'll see in production.\n// APPROACH  - Combine Virtualization — Rendering Large Lists Efficiently (Extended) with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - real-world usage with proper patterns\n// STRENGTHS - production-ready, handles common cases\n// WEAKNESSES- no advanced optimization\n//\nfunction DynamicHeightList({ items }) {\n  const parentRef = useRef(null);\n  const virtualizer = useVirtualizer({\n    count: items.length,\n    getScrollElement: () => parentRef.current,\n    estimateSize: () => 100,  // Initial estimate\n    measureElement: typeof window !== 'undefined' ? element => element?.getBoundingClientRect().height : undefined,\n    overscan: 10,\n  });\n  return (\n    <div ref={parentRef} className=\"h-[600px] overflow-auto\">\n      <div style={{ height: `${virtualizer.getTotalSize()}px`, position: 'relative' }}>\n        {virtualizer.getVirtualItems().map(virtualItem => (\n          <div\n            key={virtualItem.key}\n            ref={virtualizer.measureElement}\n            style={{\n              position: 'absolute',\n              top: 0,\n              left: 0,\n              width: '100%',\n              transform: `translateY(${virtualItem.start}px)`,\n            }}\n            className=\"border-b p-4\"\n          >\n            <h3 className=\"font-bold\">{items[virtualItem.index].title}</h3>\n            <p className=\"whitespace-normal\">{items[virtualItem.index].content}</p>\n          </div>\n        ))}\n      </div>\n    </div>\n  );\n}\nfunction VirtualGrid({ items, columns = 3, itemHeight = 200 }) {\n  const parentRef = useRef(null);\n  const itemWidth = 100 / columns + '%';\n  const virtualizer = useVirtualizer({\n    count: Math.ceil(items.length / columns),\n    getScrollElement: () => parentRef.current,\n    estimateSize: () => itemHeight,\n    overscan: 2,\n  });\n  return (\n    <div ref={parentRef} className=\"h-[600px] overflow-auto\">\n      <div style={{ height: `${virtualizer.getTotalSize()}px`, position: 'relative' }}>\n        {virtualizer.getVirtualItems().map(virtualRow => {\n          const startIndex = virtualRow.index * columns;\n          const rowItems = items.slice(startIndex, startIndex + columns);\n          return (\n            <div\n              key={virtualRow.key}\n              style={{\n                position: 'absolute',\n                top: 0,\n                left: 0,\n                width: '100%',\n                transform: `translateY(${virtualRow.start}px)`,\n                display: 'flex',\n              }}\n            >\n              {rowItems.map((item, idx) => (\n                <div\n                  key={item.id}\n                  style={{ width: itemWidth }}\n                  className=\"p-2\"\n                >\n                  <img\n                    src={item.image}\n                    alt={item.title}\n                    className=\"w-full h-full object-cover rounded\"\n                  />\n                </div>\n              ))}\n            </div>\n          );\n        })}\n      </div>\n    </div>\n  );\n}"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Virtualization — Rendering Large Lists Efficiently (Extended) — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - advanced patterns, edge cases, and decision rules\n// STRENGTHS - comprehensive, handles edge cases\n// WEAKNESSES- N/A\n//\nfunction InfiniteList() {\n  const parentRef = useRef(null);\n  const [items, setItems] = useState(Array.from({ length: 50 }, (_, i) => ({ id: i, text: `Item ${i}` })));\n  const [isLoading, setIsLoading] = useState(false);\n  const [hasMore, setHasMore] = useState(true);\n  const virtualizer = useVirtualizer({\n    count: items.length + (hasMore ? 1 : 0),\n    getScrollElement: () => parentRef.current,\n    estimateSize: () => 50,\n    overscan: 5,\n  });\n  // Load more when near end\n  const virtualItems = virtualizer.getVirtualItems();\n  const lastVirtualItem = virtualItems[virtualItems.length - 1];\n  useEffect(() => {\n    if (\n      !isLoading &&\n      hasMore &&\n      lastVirtualItem &&\n      lastVirtualItem.index >= items.length - 5\n    ) {\n      setIsLoading(true);\n      // Simulate API call\n      setTimeout(() => {\n        const newItems = Array.from({ length: 50 }, (_, i) => ({\n          id: items.length + i,\n          text: `Item ${items.length + i}`,\n        }));\n        setItems(prev => [...prev, ...newItems]);\n        setIsLoading(false);\n      }, 500);\n    }\n  }, [lastVirtualItem, isLoading, hasMore, items.length]);\n  return (\n    <div ref={parentRef} className=\"h-[600px] overflow-auto border\">\n      <div style={{ height: `${virtualizer.getTotalSize()}px`, position: 'relative' }}>\n        {virtualizer.getVirtualItems().map(virtualItem => (\n          <div\n            key={virtualItem.key}\n            style={{\n              position: 'absolute',\n              top: 0,\n              left: 0,\n              width: '100%',\n              transform: `translateY(${virtualItem.start}px)`,\n            }}\n            className=\"p-4 border-b hover:bg-gray-50\"\n          >\n            {items[virtualItem.index]?.text || 'Loading...'}\n          </div>\n        ))}\n      </div>\n      {isLoading && <p className=\"p-4\">Loading more items...</p>}\n    </div>\n  );\n}"
                  }
        ],
        tips: [
                  "measureElement is essential for dynamic heights — TanStack measures actual elements and recalculates offsets.",
                  "For grids, calculate row index carefully: rowIndex * columns — round down when iterating items.",
                  "Infinite scroll: check lastVirtualItem?.index near data length to trigger loading before reaching the end.",
                  "Always provide an estimateSize — overscan prevents visual flashing during scroll."
        ],
        mistake: "Virtualizing items that shouldn't be (animations, transitions) — virtualized items are created/destroyed frequently, breaking CSS transitions. Use opacity changes instead.",
        shorthand: {
          verbose: "// Manual / verbose approach\n// No virtualization: renders 10,000 items (slow)\n{items.map(item => <div key={item.id}>{item.text}</div>)}\n// More explicit but longer",
          concise: "// Virtualized: renders only ~20 visible items (fast)\nconst virtualizer = useVirtualizer({ count: 10000 });\n{virtualizer.getVirtualItems().map(virtualItem => ...)}",
        },
      },
      {
        id: "animation-basics",
        fn: "Animation Basics — Framer Motion Essentials (Extended)",
        desc: "Advanced Framer Motion: gestures, scroll animations, drag, variants, orchestration.",
        category: "Animation",
        subtitle: "whileHover, whileTap, whileDrag, useScroll, useMotionValue, Reorder, drag",
        signature: "<motion.div whileHover={{}} whileTap={{}} drag/>  |  <Reorder.Group>",
        descLong: "Advanced Framer Motion patterns. Gesture animations (whileHover, whileTap, whileDrag) add interactivity. useScroll tracks scroll progress for parallax and reveal effects. useMotionValue creates values that update outside React's render cycle. Reorder.Group enables drag-to-reorder lists with layout animations. Orchestration: staggerChildren, delayChildren, useInView for scroll-triggered reveals.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Animation Basics — Framer Motion Essentials (Extended) — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - simplest usage — core API only\n// STRENGTHS - easy to understand, shows the basics\n// WEAKNESSES- no error handling, no edge cases\n//\nimport {\n  motion,\n  AnimatePresence,\n  useScroll,\n  useMotionValue,\n  useTransform,\n  useInView,\n  Reorder,\n} from 'framer-motion';\nimport { useRef } from 'react';"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Animation Basics — Framer Motion Essentials (Extended) — common patterns you'll see in production.\n// APPROACH  - Combine Animation Basics — Framer Motion Essentials (Extended) with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - real-world usage with proper patterns\n// STRENGTHS - production-ready, handles common cases\n// WEAKNESSES- no advanced optimization\n//\nfunction DraggableList({ initialItems }) {\n  const [items, setItems] = useState(initialItems);\n  return (\n    <Reorder.Group\n      axis=\"y\"\n      values={items}\n      onReorder={setItems}\n      className=\"space-y-2\"\n    >\n      {items.map(item => (\n        <Reorder.Item\n          key={item.id}\n          value={item}\n          className=\"p-4 bg-white border rounded-lg cursor-grab active:cursor-grabbing\"\n        >\n          {item.text}\n        </Reorder.Item>\n      ))}\n    </Reorder.Group>\n  );\n}\nfunction ScrollReveal({ children }) {\n  const ref = useRef(null);\n  const isInView = useInView(ref, { once: true, margin: '-100px' });\n  return (\n    <motion.div\n      ref={ref}\n      initial={{ opacity: 0, y: 100 }}\n      animate={isInView ? { opacity: 1, y: 0 } : { opacity: 0, y: 100 }}\n      transition={{ duration: 0.6 }}\n    >\n      {children}\n    </motion.div>\n  );\n}\nfunction InteractiveBox() {\n  return (\n    <motion.div\n      initial={{ scale: 1, rotate: 0 }}\n      whileHover={{ scale: 1.1, rotate: 5 }}\n      whileTap={{ scale: 0.95 }}\n      whileDrag={{ scale: 1.05 }}\n      drag\n      dragElastic={0.2}\n      className=\"w-32 h-32 bg-blue-500 rounded-lg cursor-grab\"\n    />\n  );\n}\nfunction Cursor() {\n  const mouseX = useMotionValue(0);\n  const mouseY = useMotionValue(0);\n  const rotateX = useTransform(mouseY, [-100, 100], [10, -10]);\n  const rotateY = useTransform(mouseX, [-100, 100], [-10, 10]);\n  return (\n    <motion.div\n      onMouseMove={e => {\n        mouseX.set(e.clientX - window.innerWidth / 2);\n        mouseY.set(e.clientY - window.innerHeight / 2);\n      }}\n      style={{ rotateX, rotateY }}\n      className=\"w-40 h-40 bg-gradient-to-br from-purple-500 to-pink-500 rounded-lg\"\n    />\n  );\n}"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Animation Basics — Framer Motion Essentials (Extended) — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - advanced patterns, edge cases, and decision rules\n// STRENGTHS - comprehensive, handles edge cases\n// WEAKNESSES- N/A\n//\nconst container = {\n  hidden: { opacity: 0 },\n  show: {\n    opacity: 1,\n    transition: {\n      staggerChildren: 0.2,\n      delayChildren: 0.3,\n    },\n  },\n};\nconst item = {\n  hidden: { opacity: 0, y: 20 },\n  show: { opacity: 1, y: 0 },\n};\nfunction StaggeredGrid({ items }) {\n  return (\n    <motion.div\n      variants={container}\n      initial=\"hidden\"\n      animate=\"show\"\n      className=\"grid grid-cols-3 gap-4\"\n    >\n      {items.map(item => (\n        <motion.div\n          key={item.id}\n          variants={item}\n          className=\"p-4 bg-white rounded-lg shadow\"\n        >\n          {item.text}\n        </motion.div>\n      ))}\n    </motion.div>\n  );\n}\nfunction ScrollParallax() {\n  const { scrollYProgress } = useScroll();\n  const scale = useTransform(scrollYProgress, [0, 1], [1, 1.5]);\n  const opacity = useTransform(scrollYProgress, [0, 0.5], [1, 0]);\n  return (\n    <motion.div\n      style={{ scale, opacity }}\n      className=\"fixed inset-0 z-0 bg-gradient-to-b from-purple-600 to-blue-600\"\n    />\n  );\n}\nfunction DragToDelete({ item, onDelete }) {\n  const x = useMotionValue(0);\n  const opacity = useTransform(x, [-100, 0, 100], [0, 1, 0]);\n  return (\n    <motion.div\n      drag=\"x\"\n      dragElastic={0.2}\n      onDragEnd={(event, info) => {\n        if (Math.abs(info.offset.x) > 100) {\n          onDelete(item.id);\n        }\n      }}\n      style={{ x, opacity }}\n      className=\"p-4 bg-white border rounded-lg\"\n    >\n      Drag to delete: {item.text}\n    </motion.div>\n  );\n}"
                  }
        ],
        tips: [
                  "Reorder.Group animates layout changes automatically — don't manually manage position state.",
                  "useInView once: true triggers reveal only once — perfect for scroll-triggered animations.",
                  "useMotionValue updates outside React render — use for tracking cursor, scroll, or frequent updates.",
                  "Drag state persists across renders — use onDragEnd to handle drop logic, onDrag for live updates."
        ],
        mistake: "Animating top/left instead of using drag + transform — transform-based animations are GPU-accelerated. Use drag and let Framer Motion handle positioning.",
        shorthand: {
          verbose: "// Manual scroll listener (verbose)\nwindow.addEventListener('scroll', () => {\n  const progress = window.scrollY / documentHeight;\n  element.style.transform = `scale(${1 + progress})`;\n});",
          concise: "// Framer Motion (concise)\nconst { scrollYProgress } = useScroll();\nconst scale = useTransform(scrollYProgress, [0, 1], [1, 1.5]);\n<motion.div style={{ scale }} />",
        },
      },
      {
        id: "drag-drop",
        fn: "Drag & Drop — react-dnd and dnd-kit Basics",
        desc: "Implement drag-and-drop: useDraggable, useDroppable, DndContext, event handlers.",
        category: "Interaction",
        subtitle: "dnd-kit, useDraggable, useDroppable, DndContext, drag-over, drop zones",
        signature: "<DndContext><Draggable/><Droppable/></DndContext>  |  useDraggable(), useDroppable()",
        descLong: "dnd-kit is the modern drag-and-drop library for React (successor to react-dnd). Built around a Context-based API with hooks: useDraggable for draggable items, useDroppable for drop zones, DndContext for setup. Supports mouse and touch, sorting, nested lists, and accessibility. react-dnd is an older pattern-based library; dnd-kit is simpler and more performant. Combine with sensoring (PointerSensor, TouchSensor) for multi-input support.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Drag & Drop — react-dnd and dnd-kit Basics — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - simplest usage — core API only\n// STRENGTHS - easy to understand, shows the basics\n// WEAKNESSES- no error handling, no edge cases\n//\nimport {\n  DndContext,\n  closestCenter,\n  KeyboardSensor,\n  PointerSensor,\n  TouchSensor,\n  useSensor,\n  useSensors,\n  useDraggable,\n  useDroppable,\n  DragEndEvent,\n} from '@dnd-kit/core';\nimport { useState } from 'react';\nimport {\n  arrayMove,\n  SortableContext,\n  sortableKeyboardCoordinates,\n  useSortable,\n  verticalListSortingStrategy,\n} from '@dnd-kit/sortable';\nimport { CSS } from '@dnd-kit/utilities';"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Drag & Drop — react-dnd and dnd-kit Basics — common patterns you'll see in production.\n// APPROACH  - Combine Drag & Drop — react-dnd and dnd-kit Basics with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - real-world usage with proper patterns\n// STRENGTHS - production-ready, handles common cases\n// WEAKNESSES- no advanced optimization\n//\nfunction DraggableItem({ id, label }) {\n  const { attributes, listeners, setNodeRef, transform, isDragging } =\n    useDraggable({ id });\n  const style = {\n    transform: CSS.Transform.toString(transform),\n    opacity: isDragging ? 0.5 : 1,\n  };\n  return (\n    <div\n      ref={setNodeRef}\n      style={style}\n      {...listeners}\n      {...attributes}\n      className=\"p-4 bg-blue-500 text-white rounded cursor-grab active:cursor-grabbing\"\n    >\n      {label}\n    </div>\n  );\n}\nfunction DropZone({ id, children }) {\n  const { setNodeRef, isOver } = useDroppable({ id });\n  return (\n    <div\n      ref={setNodeRef}\n      className={`min-h-[200px] border-2 border-dashed p-4 rounded ${\n        isOver ? 'border-green-500 bg-green-50' : 'border-gray-300'\n      }`}\n    >\n      {children}\n    </div>\n  );\n}\nfunction SimpleDragDrop() {\n  const [items, setItems] = useState([\n    { id: '1', label: 'Item 1' },\n    { id: '2', label: 'Item 2' },\n    { id: '3', label: 'Item 3' },\n  ]);\n  const sensors = useSensors(\n    useSensor(PointerSensor),\n    useSensor(TouchSensor),\n    useSensor(KeyboardSensor, { coordinateGetter: sortableKeyboardCoordinates })\n  );\n  function handleDragEnd(event) {\n    // Handle drop logic\n    console.log('Dropped:', event);\n  }\n  return (\n    <DndContext\n      sensors={sensors}\n      collisionDetection={closestCenter}\n      onDragEnd={handleDragEnd}\n    >\n      <div className=\"flex gap-8\">\n        <div className=\"flex-1\">\n          <h2>Draggable</h2>\n          <div className=\"space-y-2\">\n            {items.map(item => (\n              <DraggableItem key={item.id} id={item.id} label={item.label} />\n            ))}\n          </div>\n        </div>\n        <div className=\"flex-1\">\n          <h2>Drop Here</h2>\n          <DropZone id=\"trash\">Drop items to remove</DropZone>\n        </div>\n      </div>\n    </DndContext>\n  );\n}"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Drag & Drop — react-dnd and dnd-kit Basics — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - advanced patterns, edge cases, and decision rules\n// STRENGTHS - comprehensive, handles edge cases\n// WEAKNESSES- N/A\n//\nfunction SortableItem({ id, label }) {\n  const { attributes, listeners, setNodeRef, transform, transition, isDragging } =\n    useSortable({ id });\n  const style = {\n    transform: CSS.Transform.toString(transform),\n    transition,\n    opacity: isDragging ? 0.5 : 1,\n  };\n  return (\n    <div\n      ref={setNodeRef}\n      style={style}\n      {...listeners}\n      {...attributes}\n      className=\"p-4 bg-white border rounded cursor-grab active:cursor-grabbing\"\n    >\n      {label}\n    </div>\n  );\n}\nfunction SortableList() {\n  const [items, setItems] = useState([\n    { id: '1', label: 'Apple' },\n    { id: '2', label: 'Banana' },\n    { id: '3', label: 'Cherry' },\n  ]);\n  const sensors = useSensors(\n    useSensor(PointerSensor),\n    useSensor(TouchSensor)\n  );\n  function handleDragEnd(event) {\n    const { active, over } = event;\n    if (over && active.id !== over.id) {\n      setItems(items => {\n        const oldIndex = items.findIndex(i => i.id === active.id);\n        const newIndex = items.findIndex(i => i.id === over.id);\n        return arrayMove(items, oldIndex, newIndex);\n      });\n    }\n  }\n  return (\n    <DndContext\n      sensors={sensors}\n      collisionDetection={closestCenter}\n      onDragEnd={handleDragEnd}\n    >\n      <SortableContext\n        items={items.map(i => i.id)}\n        strategy={verticalListSortingStrategy}\n      >\n        <div className=\"space-y-2\">\n          {items.map(item => (\n            <SortableItem key={item.id} id={item.id} label={item.label} />\n          ))}\n        </div>\n      </SortableContext>\n    </DndContext>\n  );\n}\nfunction KanbanBoard() {\n  const [tasks, setTasks] = useState({\n    todo: [{ id: '1', title: 'Task 1' }],\n    inProgress: [{ id: '2', title: 'Task 2' }],\n    done: [{ id: '3', title: 'Task 3' }],\n  });\n  const sensors = useSensors(useSensor(PointerSensor), useSensor(TouchSensor));\n  function handleDragEnd(event) {\n    const { active, over } = event;\n    if (!over) return;\n    const sourceColumn = Object.keys(tasks).find(col =>\n      tasks[col].find(t => t.id === active.id)\n    );\n    const targetColumn = over.id;\n    if (sourceColumn && targetColumn) {\n      const sourceTask = tasks[sourceColumn].find(t => t.id === active.id);\n      setTasks(prev => ({\n        ...prev,\n        [sourceColumn]: prev[sourceColumn].filter(t => t.id !== active.id),\n        [targetColumn]: [...prev[targetColumn], sourceTask],\n      }));\n    }\n  }\n  return (\n    <DndContext sensors={sensors} onDragEnd={handleDragEnd}>\n      <div className=\"flex gap-4\">\n        {Object.entries(tasks).map(([columnId, columnTasks]) => (\n          <div key={columnId} className=\"flex-1 bg-gray-100 p-4 rounded-lg\">\n            <h2 className=\"font-bold mb-4 capitalize\">{columnId}</h2>\n            <DropZone id={columnId}>\n              <div className=\"space-y-2\">\n                {columnTasks.map(task => (\n                  <DraggableItem key={task.id} id={task.id} label={task.title} />\n                ))}\n              </div>\n            </DropZone>\n          </div>\n        ))}\n      </div>\n    </DndContext>\n  );\n}"
                  }
        ],
        tips: [
                  "dnd-kit is modern and composable — use useSortable for lists, manual useDraggable+useDroppable for complex layouts.",
                  "Sensors (PointerSensor, TouchSensor) detect different input types — include all sensors for accessibility.",
                  "collisionDetection strategy matters: closestCenter for items, closestCorners for precise drops.",
                  "Transform via CSS.Transform.toString() — this applies CSS transform for smooth 60fps drag."
        ],
        mistake: "Not using useSortable for sortable lists — it provides extra features like auto-scroll, handles, proper transform.",
        shorthand: {
          verbose: "// Manual drag-drop (verbose, boilerplate)\nconst [dragging, setDragging] = useState(null);\nconst [dropZone, setDropZone] = useState(null);\nfunction handleMouseDown(id) { setDragging(id); }\nfunction handleMouseUp() { handleDrop(dragging, dropZone); }",
          concise: "// dnd-kit (concise)\n<DndContext onDragEnd={handleDragEnd}>\n  <Sortable>{items}</Sortable>\n</DndContext>",
        },
      },
    ],
  },

  // ── Section 2: Animation & Virtualization ─────────────────────────────────────────
  {
    id: "animation-virtual",
    title: "Animation & Virtualization",
    entries: [
      {
        id: "framer-motion",
        fn: "Framer Motion — Declarative Animation",
        desc: "Animate React components with Framer Motion: enter/exit, layout animations, gestures, scroll-triggered, and shared layout.",
        category: "Animation",
        subtitle: "motion.div, animate, exit, AnimatePresence, layout, whileHover, useScroll",
        signature: "<motion.div animate={{ opacity: 1 }} exit={{ opacity: 0 }} />  |  <AnimatePresence>",
        descLong: "Framer Motion is the standard React animation library. motion.div replaces div with animation capabilities. initial/animate/exit control enter/exit states. AnimatePresence enables exit animations (React normally unmounts instantly). layout prop auto-animates layout changes (reordering, resizing). whileHover/whileTap add gesture-driven animations. useScroll tracks scroll progress for parallax effects. Variants define reusable animation states. All animations are GPU-accelerated and respect prefers-reduced-motion.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Framer Motion — Declarative Animation — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - simplest usage — core API only\n// STRENGTHS - easy to understand, shows the basics\n// WEAKNESSES- no error handling, no edge cases\n//\nimport { motion, AnimatePresence, useScroll, useTransform } from 'framer-motion';"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Framer Motion — Declarative Animation — common patterns you'll see in production.\n// APPROACH  - Combine Framer Motion — Declarative Animation with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - real-world usage with proper patterns\n// STRENGTHS - production-ready, handles common cases\n// WEAKNESSES- no advanced optimization\n//\nfunction FadeIn({ children }) {\n  return (\n    <motion.div\n      initial={{ opacity: 0, y: 20 }}\n      animate={{ opacity: 1, y: 0 }}\n      transition={{ duration: 0.3, ease: 'easeOut' }}\n    >\n      {children}\n    </motion.div>\n  );\n}\nfunction NotificationList({ items, onDismiss }) {\n  return (\n    <AnimatePresence>\n      {items.map(item => (\n        <motion.div\n          key={item.id}\n          initial={{ opacity: 0, height: 0 }}\n          animate={{ opacity: 1, height: 'auto' }}\n          exit={{ opacity: 0, x: 300 }}\n          transition={{ type: 'spring', damping: 20 }}\n          className=\"p-4 bg-white rounded shadow mb-2\"\n        >\n          {item.text}\n          <button onClick={() => onDismiss(item.id)}>Dismiss</button>\n        </motion.div>\n      ))}\n    </AnimatePresence>\n  );\n}\nfunction FilterableGrid({ items }) {\n  return (\n    <motion.div layout className=\"grid grid-cols-3 gap-4\">\n      {items.map(item => (\n        <motion.div\n          key={item.id}\n          layout                    // auto-animate position changes\n          initial={{ opacity: 0 }}\n          animate={{ opacity: 1 }}\n          exit={{ opacity: 0 }}\n          className=\"p-4 bg-white rounded-lg\"\n        >\n          {item.name}\n        </motion.div>\n      ))}\n    </motion.div>\n  );\n}"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Framer Motion — Declarative Animation — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - advanced patterns, edge cases, and decision rules\n// STRENGTHS - comprehensive, handles edge cases\n// WEAKNESSES- N/A\n//\nfunction InteractiveCard({ children }) {\n  return (\n    <motion.div\n      whileHover={{ scale: 1.02, boxShadow: '0 10px 30px rgba(0,0,0,0.12)' }}\n      whileTap={{ scale: 0.98 }}\n      transition={{ type: 'spring', stiffness: 400 }}\n      className=\"p-6 bg-white rounded-xl cursor-pointer\"\n    >\n      {children}\n    </motion.div>\n  );\n}\nfunction ParallaxHero() {\n  const { scrollYProgress } = useScroll();\n  const y = useTransform(scrollYProgress, [0, 1], [0, -200]);\n  const opacity = useTransform(scrollYProgress, [0, 0.5], [1, 0]);\n  return (\n    <motion.div style={{ y, opacity }} className=\"h-screen flex items-center\">\n      <h1 className=\"text-6xl font-bold\">Welcome</h1>\n    </motion.div>\n  );\n}\nconst container = {\n  hidden: {},\n  show: { transition: { staggerChildren: 0.1 } },\n};\nconst item = {\n  hidden: { opacity: 0, y: 20 },\n  show: { opacity: 1, y: 0 },\n};\nfunction StaggeredList({ items }) {\n  return (\n    <motion.ul variants={container} initial=\"hidden\" animate=\"show\">\n      {items.map(i => (\n        <motion.li key={i.id} variants={item}>{i.text}</motion.li>\n      ))}\n    </motion.ul>\n  );\n}"
                  }
        ],
        tips: [
                  "AnimatePresence is required for exit animations — without it, React unmounts components instantly before exit can run.",
                  "layout prop auto-animates any CSS layout change (position, size) — just add it and Framer figures out the transition.",
                  "Use type: \"spring\" for physical, natural motion and type: \"tween\" for precise, controlled easing.",
                  "Variants with staggerChildren create beautiful staggered list animations with minimal code."
        ],
        mistake: "Animating width/height directly instead of using layout — animating width/height causes layout thrashing. The layout prop uses transform-based FLIP animations which are GPU-accelerated and 60fps.",
        shorthand: {
          verbose: "// CSS keyframes (verbose, non-interactive)\n@keyframes slide {\n  from { x: 0; } to { x: 100px; }\n}\n<div style={{animation: \"slide 0.5s\"}}>",
          concise: "// Framer Motion (concise, interactive)\n<motion.div animate={{ x: 100 }} transition={{ duration: 0.5 }}/>",
        },
      },
      {
        id: "virtualization",
        fn: "Virtualization — Rendering Large Lists Efficiently",
        desc: "Render thousands of items without lag: TanStack Virtual, react-window, dynamic row heights, and infinite scroll.",
        category: "Performance",
        subtitle: "@tanstack/react-virtual, useVirtualizer, react-window, FixedSizeList, VariableSizeList",
        signature: "useVirtualizer({ count, getScrollElement, estimateSize })  |  <FixedSizeList>",
        descLong: "Virtualization renders only the visible items in a list, keeping DOM size constant regardless of data size. TanStack Virtual (formerly react-virtual) is the modern choice — headless, framework-agnostic, supports variable heights and grids. react-window is a lighter alternative for simpler cases. Key concept: only items in the viewport (plus overscan buffer) exist in the DOM. A 100,000-item list renders ~20 DOM nodes. Essential for tables, feeds, select dropdowns, and any large dataset.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Virtualization — Rendering Large Lists Efficiently — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - simplest usage — core API only\n// STRENGTHS - easy to understand, shows the basics\n// WEAKNESSES- no error handling, no edge cases\n//\nimport { useVirtualizer } from '@tanstack/react-virtual';\nimport { useRef } from 'react';"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Virtualization — Rendering Large Lists Efficiently — common patterns you'll see in production.\n// APPROACH  - Combine Virtualization — Rendering Large Lists Efficiently with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - real-world usage with proper patterns\n// STRENGTHS - production-ready, handles common cases\n// WEAKNESSES- no advanced optimization\n//\nfunction VirtualList({ items }) {\n  const parentRef = useRef(null);\n  const virtualizer = useVirtualizer({\n    count: items.length,\n    getScrollElement: () => parentRef.current,\n    estimateSize: () => 60,       // estimated row height\n    overscan: 5,                   // render 5 extra items above/below\n  });\n  return (\n    <div ref={parentRef} className=\"h-[600px] overflow-auto\">\n      <div style={{ height: virtualizer.getTotalSize(), position: 'relative' }}>\n        {virtualizer.getVirtualItems().map(virtualRow => (\n          <div\n            key={virtualRow.key}\n            ref={virtualizer.measureElement}    // dynamic height\n            style={{\n              position: 'absolute',\n              top: 0,\n              left: 0,\n              width: '100%',\n              transform: `translateY(${virtualRow.start}px)`,\n            }}\n          >\n            <ItemRow item={items[virtualRow.index]} />\n          </div>\n        ))}\n      </div>\n    </div>\n  );\n}"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Virtualization — Rendering Large Lists Efficiently — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - advanced patterns, edge cases, and decision rules\n// STRENGTHS - comprehensive, handles edge cases\n// WEAKNESSES- N/A\n//\nfunction VirtualTable({ columns, data }) {\n  const parentRef = useRef(null);\n  const rowVirtualizer = useVirtualizer({\n    count: data.length,\n    getScrollElement: () => parentRef.current,\n    estimateSize: () => 48,\n  });\n  return (\n    <div className=\"border rounded-lg\">\n      {/* Fixed header */}\n      <div className=\"flex bg-gray-50 border-b font-semibold\">\n        {columns.map(col => (\n          <div key={col.key} style={{ width: col.width }} className=\"px-4 py-3\">\n            {col.header}\n          </div>\n        ))}\n      </div>\n      {/* Virtualized body */}\n      <div ref={parentRef} className=\"h-[500px] overflow-auto\">\n        <div style={{ height: rowVirtualizer.getTotalSize(), position: 'relative' }}>\n          {rowVirtualizer.getVirtualItems().map(virtualRow => {\n            const row = data[virtualRow.index];\n            return (\n              <div\n                key={virtualRow.key}\n                style={{\n                  position: 'absolute',\n                  top: 0,\n                  transform: `translateY(${virtualRow.start}px)`,\n                  width: '100%',\n                  height: 48,\n                }}\n                className=\"flex border-b\"\n              >\n                {columns.map(col => (\n                  <div key={col.key} style={{ width: col.width }} className=\"px-4 py-3\">\n                    {row[col.key]}\n                  </div>\n                ))}\n              </div>\n            );\n          })}\n        </div>\n      </div>\n    </div>\n  );\n}"
                  }
        ],
        tips: [
                  "TanStack Virtual is headless — it computes positions, you render the DOM. This gives full control over styling and structure.",
                  "Use measureElement ref for dynamic row heights — TanStack measures the actual DOM element and adjusts positions automatically.",
                  "overscan: 5 renders 5 extra items above/below viewport — prevents flash of empty space during fast scrolling.",
                  "For search/filter, reset the virtualizer with virtualizer.scrollToIndex(0) — otherwise the scroll position persists from the previous list."
        ],
        mistake: "Using virtualization for lists under 100 items — the setup overhead is not worth it for small lists. Regular rendering is fine up to ~500 items; virtualize only when you notice scroll jank.",
        shorthand: {
          verbose: "// Render all items (slow with 10k+ items)\n{items.map(item => (\n  <div key={item.id}>{item.name}</div>\n))}",
          concise: "// Virtualize with TanStack Virtual (fast)\n<VirtualList height={600} itemCount={10000} itemSize={35} />\n// Only renders visible ~17 items",
        },
      },
    ],
  },
]

export default { meta, sections }

export const meta = {
  "title": "Computer Vision",
  "domain": "javascript",
  "sheet": "cv-opencv",
  "icon": "👁️"
}

export const sections = [

  // ── Section 1: OpenCV.js — browser-based computer vision ─────────────────────────────────────────
  {
    id: "opencv-js",
    title: "OpenCV.js — browser-based computer vision",
    entries: [
      {
        id: "opencv-basics",
        fn: "OpenCV.js — image processing, edge detection, contours",
        desc: "OpenCV.js is a WebAssembly port of OpenCV for browsers. Provides cv.Mat (image matrices), Gaussian blur, Canny edge detection, contour finding, color space conversion, thresholding, and more.",
        category: "Computer Vision",
        subtitle: "cv.Mat, cv.cvtColor, cv.GaussianBlur, cv.Canny, cv.findContours, cv.threshold, cv.HoughCircles",
        signature: "const src = cv.imread(canvas); cv.cvtColor(src, dst, cv.COLOR_RGBA2GRAY); cv.Canny(src, dst, 50, 150)",
        descLong: "OpenCV.js compiles OpenCV (C++) to WebAssembly, enabling computer vision in the browser. Key concepts: cv.Mat (n-dimensional array representing image data), color space conversion (RGBA to GRAY, HSV, etc.), filtering (GaussianBlur, medianBlur), edge detection (Canny), thresholding (binary, adaptive), contour finding (findContours, approxPolyDP), shape detection (HoughCircles, HoughLines), and feature detection (ORB, FAST). Load via script tag or npm. Must call cv['onRuntimeInitialized'] before use. Clean up Mat objects with .delete() to prevent WASM memory leaks.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Load an image, convert to grayscale, detect edges with Canny.\n// APPROACH  - cv.imread from canvas; cv.cvtColor; cv.Canny; cv.imshow.\n// STRENGTHS - Full OpenCV in browser; no server needed.\n// WEAKNESSES- ~8MB WASM download; must delete Mat objects manually.\n//\n// Load OpenCV.js\n// <script async src=\"https://docs.opencv.org/4.x/opencv.js\"></script>\n\ncv['onRuntimeInitialized'] = () => {\n  // Read image from canvas element\n  const canvas = document.getElementById('inputCanvas');\n  const src = cv.imread(canvas);\n\n  // Convert to grayscale\n  const gray = new cv.Mat();\n  cv.cvtColor(src, gray, cv.COLOR_RGBA2GRAY);\n\n  // Gaussian blur (reduces noise before edge detection)\n  const blurred = new cv.Mat();\n  cv.GaussianBlur(gray, blurred, new cv.Size(3, 3), 0);\n\n  // Canny edge detection\n  const edges = new cv.Mat();\n  cv.Canny(blurred, edges, 50, 150);\n\n  // Display result\n  cv.imshow(document.getElementById('outputCanvas'), edges);\n\n  // Clean up — prevent WASM memory leaks\n  src.delete(); gray.delete(); blurred.delete(); edges.delete();\n};"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Contour detection, shape recognition, and color-based\n//             object tracking (HSV thresholding).\n// APPROACH  - findContours + approxPolyDP for shapes; cvtColor to HSV +\n//             inRange for color masking; moments for centroid.\n// STRENGTHS - Real-time shape and color detection in browser.\n// WEAKNESSES- Sensitive to lighting; needs manual threshold tuning.\n//\ncv['onRuntimeInitialized'] = () => {\n  const video = document.getElementById('video');\n  const canvas = document.getElementById('canvas');\n  const ctx = canvas.getContext('2d');\n\n  function processFrame() {\n    ctx.drawImage(video, 0, 0, canvas.width, canvas.height);\n    const src = cv.imread(canvas);\n\n    // --- Shape Detection ---\n    const gray = new cv.Mat();\n    cv.cvtColor(src, gray, cv.COLOR_RGBA2GRAY);\n    cv.GaussianBlur(gray, gray, new cv.Size(5, 5), 0);\n\n    const binary = new cv.Mat();\n    cv.threshold(gray, binary, 100, 255, cv.THRESH_BINARY);\n\n    const contours = new cv.MatVector();\n    const hierarchy = new cv.Mat();\n    cv.findContours(binary, contours, hierarchy, cv.RETR_EXTERNAL, cv.CHAIN_APPROX_SIMPLE);\n\n    for (let i = 0; i < contours.size(); i++) {\n      const contour = contours.get(i);\n      const approx = new cv.Mat();\n      const epsilon = 0.04 * cv.arcLength(contour, true);\n      cv.approxPolyDP(contour, approx, epsilon, true);\n\n      const vertices = approx.rows;\n      const area = cv.contourArea(contour);\n\n      if (area > 500) {  // filter small noise\n        let shape = 'unknown';\n        if (vertices === 3) shape = 'triangle';\n        else if (vertices === 4) shape = 'rectangle';\n        else if (vertices > 10) shape = 'circle';\n\n        // Draw contour and label\n        const color = new cv.Scalar(0, 255, 0, 255);\n        cv.drawContours(src, contours, i, color, 2);\n\n        // Get centroid from moments\n        const moments = cv.moments(contour);\n        const cx = Math.round(moments.m10 / moments.m00);\n        const cy = Math.round(moments.m01 / moments.m00);\n        ctx.fillText(shape, cx, cy);\n      }\n      approx.delete(); contour.delete();\n    }\n\n    // --- Color Tracking (HSV) ---\n    const hsv = new cv.Mat();\n    cv.cvtColor(src, hsv, cv.COLOR_RGBA2RGB);\n    cv.cvtColor(hsv, hsv, cv.COLOR_RGB2HSV);\n\n    // Track blue objects: HSV range [100,50,50] to [130,255,255]\n    const mask = new cv.Mat();\n    const low = new cv.Mat(hsv.rows, hsv.cols, hsv.type(), [100, 50, 50, 0]);\n    const high = new cv.Mat(hsv.rows, hsv.cols, hsv.type(), [130, 255, 255, 0]);\n    cv.inRange(hsv, low, high, mask);\n\n    // Find largest blue blob\n    const colorContours = new cv.MatVector();\n    cv.findContours(mask, colorContours, new cv.Mat(), cv.RETR_EXTERNAL, cv.CHAIN_APPROX_SIMPLE);\n\n    let maxArea = 0, maxIdx = -1;\n    for (let i = 0; i < colorContours.size(); i++) {\n      const area = cv.contourArea(colorContours.get(i));\n      if (area > maxArea) { maxArea = area; maxIdx = i; }\n    }\n\n    if (maxIdx >= 0 && maxArea > 1000) {\n      const rect = cv.boundingRect(colorContours.get(maxIdx));\n      const point1 = new cv.Point(rect.x, rect.y);\n      const point2 = new cv.Point(rect.x + rect.width, rect.y + rect.height);\n      cv.rectangle(src, point1, point2, new cv.Scalar(255, 0, 0, 255), 2);\n    }\n\n    cv.imshow(canvas, src);\n\n    // Cleanup\n    src.delete(); gray.delete(); binary.delete(); contours.delete(); hierarchy.delete();\n    hsv.delete(); mask.delete(); low.delete(); high.delete(); colorContours.delete();\n\n    requestAnimationFrame(processFrame);\n  }\n\n  processFrame();\n};"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Feature detection (ORB), feature matching between images,\n//             and perspective transformation (homography).\n// APPROACH  - cv.ORB_create for keypoints; cv.BFMatcher for matching;\n//             cv.findHomography for perspective transform; cv.warpPerspective.\n// STRENGTHS - Object recognition; image alignment; augmented reality base.\n// WEAKNESSES- ORB is less accurate than SIFT/SURF; WASM performance limits.\n//\ncv['onRuntimeInitialized'] = () => {\n  // Load two images: template and scene\n  const templateCanvas = document.getElementById('template');\n  const sceneCanvas = document.getElementById('scene');\n  const template = cv.imread(templateCanvas);\n  const scene = cv.imread(sceneCanvas);\n\n  // Convert to grayscale\n  const templateGray = new cv.Mat();\n  const sceneGray = new cv.Mat();\n  cv.cvtColor(template, templateGray, cv.COLOR_RGBA2GRAY);\n  cv.cvtColor(scene, sceneGray, cv.COLOR_RGBA2GRAY);\n\n  // ORB feature detector\n  const orb = cv.ORB_create(1000);  // max 1000 features\n  const kp1 = new cv.KeyPointVector();\n  const kp2 = new cv.KeyPointVector();\n  const des1 = new cv.Mat();\n  const des2 = new cv.Mat();\n\n  orb.detectAndCompute(templateGray, new cv.Mat(), kp1, des1);\n  orb.detectAndCompute(sceneGray, new cv.Mat(), kp2, des2);\n\n  console.log(`Template: ${kp1.size()} keypoints, Scene: ${kp2.size()} keypoints`);\n\n  // Feature matching with BFMatcher (Hamming distance for ORB)\n  const matcher = new cv.BFMatcher(cv.NORM_HAMMING, false);\n  const matches = new cv.DMatchVector();\n\n  matcher.match(des1, des2, matches);\n\n  // Filter good matches (low distance)\n  const goodMatches = [];\n  for (let i = 0; i < matches.size(); i++) {\n    if (matches.get(i).distance < 50) {\n      goodMatches.push(matches.get(i));\n    }\n  }\n  console.log(`Good matches: ${goodMatches.length}`);\n\n  if (goodMatches.length >= 4) {\n    // Extract matched keypoints\n    const srcPts = [];\n    const dstPts = [];\n    for (const match of goodMatches) {\n      const kp1Point = kp1.get(match.queryIdx).pt;\n      const kp2Point = kp2.get(match.trainIdx).pt;\n      srcPts.push(kp1Point.x, kp1Point.y);\n      dstPts.push(kp2Point.x, kp2Point.y);\n    }\n\n    // Find homography (perspective transform)\n    const srcMat = cv.matFromArray(goodMatches.length, 1, cv.CV_32FC2, srcPts);\n    const dstMat = cv.matFromArray(goodMatches.length, 1, cv.CV_32FC2, dstPts);\n    const homography = cv.findHomography(srcMat, dstMat, cv.RANSAC, 5.0);\n\n    if (homography && !homography.empty()) {\n      // Warp template to scene perspective\n      const warped = new cv.Mat();\n      cv.warpPerspective(template, warped, homography, scene.size());\n\n      // Draw bounding box of detected object\n      const h = template.rows, w = template.cols;\n      const corners = cv.matFromArray(4, 1, cv.CV_32FC2, [0, 0, w, 0, w, h, 0, h]);\n      const transformedCorners = new cv.Mat();\n      cv.perspectiveTransform(corners, transformedCorners, homography);\n\n      // Draw polygon on scene\n      const pts = [];\n      for (let i = 0; i < 4; i++) {\n        pts.push(transformedCorners.floatAt(i * 2), transformedCorners.floatAt(i * 2 + 1));\n      }\n      const contour = cv.matFromArray(4, 1, cv.CV_32SC2, pts.map(p => Math.round(p)));\n      const contours = new cv.MatVector();\n      contours.push_back(contour);\n      cv.polylines(scene, contours, true, new cv.Scalar(0, 255, 0, 255), 3);\n\n      cv.imshow(document.getElementById('output'), scene);\n\n      // Cleanup\n      warped.delete(); corners.delete(); transformedCorners.delete();\n      contour.delete(); contours.delete();\n    }\n\n    srcMat.delete(); dstMat.delete(); homography?.delete();\n  }\n\n  // Cleanup all Mat objects\n  template.delete(); scene.delete();\n  templateGray.delete(); sceneGray.delete();\n  kp1.delete(); kp2.delete(); des1.delete(); des2.delete();\n  matches.delete(); matcher.delete(); orb.delete();\n};"
                  }
        ],
        tips: [
                  "Always call mat.delete() after use — WASM memory is not garbage collected; leaks cause crashes.",
                  "Use cv.RETR_EXTERNAL for outer contours only — cv.RETR_TREE for nested contours (holes inside shapes).",
                  "Use cv.RANSAC in findHomography — filters outlier matches that would distort the perspective transform.",
                  "Convert to HSV for color tracking — RGB is sensitive to lighting changes; HSV separates color from brightness.",
                  "Use requestAnimationFrame for video processing — syncs with browser repaint, avoids jank."
        ],
        mistake: "Not deleting cv.Mat objects — each Mat allocates WASM heap memory. Without .delete(), you get \"RuntimeError: memory access out of bounds\" after processing a few hundred frames.",
        shorthand: {
          verbose: "const src = cv.imread(canvas);\nconst gray = new cv.Mat();\ncv.cvtColor(src, gray, cv.COLOR_RGBA2GRAY);\nconst edges = new cv.Mat();\ncv.Canny(gray, edges, 50, 150);\ncv.imshow(outputCanvas, edges);\nsrc.delete(); gray.delete(); edges.delete();",
          concise: "const s=cv.imread(c),g=new cv.Mat(),e=new cv.Mat(); cv.cvtColor(s,g,cv.COLOR_RGBA2GRAY); cv.Canny(g,e,50,150); cv.imshow(o,e); s.delete();g.delete();e.delete();",
        },
      },
    ],
  },

  // ── Section 2: face-api.js — face detection, recognition, expressions ─────────────────────────────────────────
  {
    id: "face-api",
    title: "face-api.js — face detection, recognition, expressions",
    entries: [
      {
        id: "face-api-basics",
        fn: "face-api.js — detect faces, landmarks, expressions, age",
        desc: "face-api.js is a TensorFlow.js-based face detection and recognition library. Detect faces, 68-point landmarks, facial expressions, age, and gender. Face recognition with 128-descriptor embeddings.",
        category: "Face Detection",
        subtitle: "detectAllFaces, faceLandmark68Net, faceExpressionNet, ageGenderNet, faceRecognitionNet, LabeledFaceDescriptors",
        signature: "const detections = await faceapi.detectAllFaces(input).withFaceLandmarks().withFaceExpressions()",
        descLong: "face-api.js wraps TensorFlow.js models for face analysis. Models: ssdMobilenetv1 (fast face detection), tinyFaceDetector (lightweight), faceLandmark68Net (68 facial landmarks), faceExpressionNet (7 emotions: happy, sad, angry, surprised, neutral, fearful, disgusted), ageGenderNet (age + gender), faceRecognitionNet (128-d face embeddings for identity). Load models from CDN or local path. Use LabeledFaceDescriptors to store known faces and FaceMatcher for recognition. Works with images, video, and canvas elements.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Detect faces in an image with landmarks and expressions.\n// APPROACH  - faceapi.detectAllFaces with chained .withFaceLandmarks().withFaceExpressions().\n// STRENGTHS - Full face analysis pipeline; works in browser with TF.js.\n// WEAKNESSES- Model downloads (~6MB total); slower than OpenCV Haar cascades.\n//\nimport * as faceapi from 'face-api.js';\n\n// Load models from CDN\nconst MODEL_URL = 'https://cdn.jsdelivr.net/npm/@vladmandic/face-api/model';\nawait faceapi.nets.tinyFaceDetector.loadFromUri(MODEL_URL);\nawait faceapi.nets.faceLandmark68Net.loadFromUri(MODEL_URL);\nawait faceapi.nets.faceExpressionNet.loadFromUri(MODEL_URL);\n\n// Detect faces with landmarks and expressions\nconst input = document.getElementById('inputImage');\nconst detections = await faceapi\n  .detectAllFaces(input, new faceapi.TinyFaceDetectorOptions({ inputSize: 320, scoreThreshold: 0.5 }))\n  .withFaceLandmarks()\n  .withFaceExpressions();\n\n// Draw results on canvas\nconst canvas = document.getElementById('overlay');\nfaceapi.matchDimensions(canvas, input);\nconst resized = faceapi.resizeResults(detections, input);\nfaceapi.draw.drawDetections(canvas, resized);\nfaceapi.draw.drawFaceLandmarks(canvas, resized);\n\n// Log expressions for each face\ndetections.forEach((det, i) => {\n  const expressions = det.expressions;\n  const topEmotion = Object.entries(expressions)\n    .sort(([, a], [, b]) => b - a)[0];\n  console.log(`Face ${i}: ${topEmotion[0]} (${(topEmotion[1] * 100).toFixed(1)}%)`);\n});"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Face recognition: register known faces, then identify\n//             faces in new images using 128-d descriptor matching.\n// APPROACH  - faceRecognitionNet for embeddings; LabeledFaceDescriptors\n//             for known faces; FaceMatcher for identification.\n// STRENGTHS - Identity recognition; works with single reference photo per person.\n// WEAKNESSES- Sensitive to pose/lighting; needs good reference photos.\n//\nimport * as faceapi from 'face-api.js';\n\nconst MODEL_URL = 'https://cdn.jsdelivr.net/npm/@vladmandic/face-api/model';\nawait faceapi.nets.ssdMobilenetv1.loadFromUri(MODEL_URL);\nawait faceapi.nets.faceLandmark68Net.loadFromUri(MODEL_URL);\nawait faceapi.nets.faceRecognitionNet.loadFromUri(MODEL_URL);\n\n// Register known faces\nasync function registerFace(name, imageUrl) {\n  const img = await faceapi.fetchImage(imageUrl);\n  const detection = await faceapi\n    .detectSingleFace(img)\n    .withFaceLandmarks()\n    .withFaceDescriptor();\n\n  if (!detection) throw new Error(`No face found in ${name}'s photo`);\n\n  return new faceapi.LabeledFaceDescriptors(name, [detection.descriptor]);\n}\n\n// Register multiple people\nconst knownFaces = [\n  await registerFace('Alice', '/faces/alice.jpg'),\n  await registerFace('Bob', '/faces/bob.jpg'),\n  await registerFace('Carol', '/faces/carol.jpg'),\n];\n\n// Create face matcher (0.6 = max distance for a match)\nconst faceMatcher = new faceapi.FaceMatcher(knownFaces, 0.6);\n\n// Identify faces in a new image\nasync function identifyFaces(input) {\n  const detections = await faceapi\n    .detectAllFaces(input)\n    .withFaceLandmarks()\n    .withFaceDescriptors();\n\n  const results = detections.map(detection => {\n    const match = faceMatcher.findBestMatch(detection.descriptor);\n    return {\n      label: match.label,        // 'Alice', 'Bob', 'Carol', or 'unknown'\n      distance: match.distance,  // lower = better match\n      box: detection.detection.box,\n    };\n  });\n\n  return results;\n}\n\n// Usage\nconst testImage = document.getElementById('testImage');\nconst identities = await identifyFaces(testImage);\nidentities.forEach(id => {\n  console.log(`Found ${id.label} (distance: ${id.distance.toFixed(3)})`);\n});\n\n// Real-time face recognition from video\nasync function realtimeRecognition(video, canvas) {\n  const displaySize = { width: video.width, height: video.height };\n  faceapi.matchDimensions(canvas, displaySize);\n\n  setInterval(async () => {\n    const detections = await faceapi\n      .detectAllFaces(video)\n      .withFaceLandmarks()\n      .withFaceDescriptors();\n\n    const resized = faceapi.resizeResults(detections, displaySize);\n    canvas.getContext('2d').clearRect(0, 0, canvas.width, canvas.height);\n\n    resized.forEach(detection => {\n      const match = faceMatcher.findBestMatch(detection.descriptor);\n      const box = detection.detection.box;\n      const drawBox = new faceapi.draw.DrawBox(box, { label: match.label });\n      drawBox.draw(canvas);\n    });\n  }, 200);  // 5 fps\n}"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Multi-face pipeline: detection + landmarks + expressions +\n//             age/gender + recognition; batch processing; performance\n//             optimization with model quantization.\n// APPROACH  - Chain all models; use tinyFaceDetector for speed; process\n//             video frames with throttling; cache descriptors.\n// STRENGTHS - Complete face analysis; optimized for real-time; cached results.\n// WEAKNESSES- Running all models is CPU-intensive; needs throttling for video.\n//\nimport * as faceapi from 'face-api.js';\n\nclass FaceAnalysisPipeline {\n  constructor(options = {}) {\n    this.modelsLoaded = false;\n    this.detector = options.detector || 'tiny';\n    this.confidence = options.confidence || 0.5;\n    this.inputSize = options.inputSize || 320;\n    this.descriptorCache = new Map();\n  }\n\n  async loadModels(modelUrl) {\n    const url = modelUrl || 'https://cdn.jsdelivr.net/npm/@vladmandic/face-api/model';\n    await Promise.all([\n      faceapi.nets.tinyFaceDetector.loadFromUri(url),\n      faceapi.nets.faceLandmark68Net.loadFromUri(url),\n      faceapi.nets.faceExpressionNet.loadFromUri(url),\n      faceapi.nets.ageGenderNet.loadFromUri(url),\n      faceapi.nets.faceRecognitionNet.loadFromUri(url),\n    ]);\n    this.modelsLoaded = true;\n  }\n\n  async analyze(input, options = {}) {\n    if (!this.modelsLoaded) throw new Error('Models not loaded');\n\n    const detectorOptions = new faceapi.TinyFaceDetectorOptions({\n      inputSize: this.inputSize,\n      scoreThreshold: this.confidence,\n    });\n\n    // Full pipeline — chain all detections\n    let detections = await faceapi\n      .detectAllFaces(input, detectorOptions)\n      .withFaceLandmarks()\n      .withFaceExpressions()\n      .withAgeAndGender()\n      .withFaceDescriptors();\n\n    // Resize to input dimensions\n    if (options.resize) {\n      detections = faceapi.resizeResults(detections, options.resize);\n    }\n\n    // Format results\n    return detections.map((det, i) => ({\n      id: i,\n      box: {\n        x: det.detection.box.x,\n        y: det.detection.box.y,\n        width: det.detection.box.width,\n        height: det.detection.box.height,\n      },\n      confidence: det.detection.score,\n      expressions: this.getTopExpressions(det.expressions, 3),\n      age: Math.round(det.age),\n      gender: det.gender,\n      genderProbability: det.genderProbability,\n      descriptor: Array.from(det.descriptor),\n      landmarks: det.landmarks.positions.map(p => ({ x: p.x, y: p.y })),\n    }));\n  }\n\n  getTopExpressions(expressions, topN = 3) {\n    return Object.entries(expressions)\n      .sort(([, a], [, b]) => b - a)\n      .slice(0, topN)\n      .map(([emotion, score]) => ({ emotion, score: score }));\n  }\n\n  // Real-time video processing with throttling\n  startVideoAnalysis(video, canvas, onResult, fps = 5) {\n    const interval = 1000 / fps;\n    let processing = false;\n    let lastResult = null;\n\n    const timer = setInterval(async () => {\n      if (processing) return;\n      processing = true;\n\n      try {\n        const results = await this.analyze(video, {\n          resize: { width: video.videoWidth, height: video.videoHeight },\n        });\n\n        // Draw on canvas\n        const ctx = canvas.getContext('2d');\n        ctx.clearRect(0, 0, canvas.width, canvas.height);\n\n        results.forEach(face => {\n          // Bounding box\n          ctx.strokeStyle = '#00ff00';\n          ctx.lineWidth = 2;\n          ctx.strokeRect(face.box.x, face.box.y, face.box.width, face.box.height);\n\n          // Label\n          const topEmotion = face.expressions[0];\n          const label = `${face.gender}, ${face.age}, ${topEmotion.emotion}`;\n          ctx.fillStyle = '#00ff00';\n          ctx.font = '14px sans-serif';\n          ctx.fillText(label, face.box.x, face.box.y - 5);\n        });\n\n        lastResult = results;\n        onResult(results);\n      } catch (err) {\n        console.error('Face analysis error:', err);\n      } finally {\n        processing = false;\n      }\n    }, interval);\n\n    return () => clearInterval(timer);  // return stop function\n  }\n\n  // Batch process images\n  async batchAnalyze(imageUrls, concurrency = 3) {\n    const results = [];\n    for (let i = 0; i < imageUrls.length; i += concurrency) {\n      const batch = imageUrls.slice(i, i + concurrency);\n      const batchResults = await Promise.all(\n        batch.map(async (url) => {\n          try {\n            const img = await faceapi.fetchImage(url);\n            const faces = await this.analyze(img);\n            return { url, faces, error: null };\n          } catch (err) {\n            return { url, faces: [], error: err.message };\n          }\n        })\n      );\n      results.push(...batchResults);\n    }\n    return results;\n  }\n}\n\n// Usage\nconst pipeline = new FaceAnalysisPipeline({ detector: 'tiny', confidence: 0.5 });\nawait pipeline.loadModels();\n\n// Analyze single image\nconst faces = await pipeline.analyze(document.getElementById('photo'));\nconsole.log(`Found ${faces.length} faces`);\nfaces.forEach(f => console.log(`${f.gender}, ${f.age}: ${f.expressions[0].emotion}`));\n\n// Real-time video\nconst stop = pipeline.startVideoAnalysis(\n  document.getElementById('video'),\n  document.getElementById('overlay'),\n  (results) => console.log(`${results.length} faces detected`),\n  5  // 5 fps\n);\n// stop() to end analysis"
                  }
        ],
        tips: [
                  "Use tinyFaceDetector for real-time — 10x faster than ssdMobilenetv1 with slightly lower accuracy.",
                  "Use 0.6 as FaceMatcher distance threshold — lower is stricter (fewer false positives), higher is more lenient.",
                  "Register multiple photos per person — LabeledFaceDescriptors accepts an array of descriptors for robustness.",
                  "Throttle video processing to 5-10 fps — face detection is CPU-intensive, full 30fps will freeze the browser.",
                  "Cache face descriptors — computing embeddings is expensive; store them for known faces instead of recomputing."
        ],
        mistake: "Using ssdMobilenetv1 for real-time video — it's accurate but too slow for live processing. Use tinyFaceDetector (inputSize: 320) for 5-10 fps.",
        shorthand: {
          verbose: "const dets = await faceapi.detectAllFaces(img)\n  .withFaceLandmarks().withFaceExpressions();\ndets.forEach(d => console.log(d.expressions));",
          concise: "faceapi.detectAllFaces(img).withFaceLandmarks().withFaceExpressions().then(d => d.forEach(f => console.log(f.expressions)));",
        },
      },
    ],
  },
]

export default { meta, sections }

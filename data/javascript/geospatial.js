export const meta = {
  "title": "Geospatial",
  "domain": "javascript",
  "sheet": "geospatial",
  "icon": "🌍"
}

export const sections = [

  // ── Section 1: Turf.js — geospatial analysis & measurement ─────────────────────────────────────────
  {
    id: "turf",
    title: "Turf.js — geospatial analysis & measurement",
    entries: [
      {
        id: "turf-basics",
        fn: "Turf.js — distance, area, buffers, intersections, spatial queries",
        desc: "Turf.js is a modular geospatial analysis library. Compute distances, areas, buffers, intersections, bounding boxes, and perform spatial queries on GeoJSON data.",
        category: "Geospatial",
        subtitle: "turf.distance, turf.area, turf.buffer, turf.bbox, turf.intersect, turf.booleanPointInPolygon, turf.center",
        signature: "turf.distance(pointA, pointB, { units: 'kilometers' }) // 42.5",
        descLong: "Turf.js provides geospatial analysis on GeoJSON features. Key operations: measurement (distance, area, length, bbox), geometry creation (point, line, polygon, circle), spatial relationships (booleanPointInPolygon, booleanContains, booleanIntersects), transformations (buffer, union, intersect, difference), and data analysis (center, centroid, envelope, tin). All functions are pure and operate on GeoJSON Feature/FeatureCollection. Works in Node.js and browser. Use Turf for server-side geospatial computation, spatial joins, and proximity searches.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Compute distance between two points, create a buffer, check\n//             if a point is inside a polygon.\n// APPROACH  - turf.distance, turf.buffer, turf.booleanPointInPolygon.\n// STRENGTHS - Pure functions on GeoJSON; no external service needed.\n// WEAKNESSES- Uses planar geometry for some ops; not ideal for large areas.\n//\nimport * as turf from '@turf/turf';\n\n// Distance between two cities (lon, lat)\nconst nyc = turf.point([-74.006, 40.7128]);\nconst boston = turf.point([-71.0589, 42.3601]);\nconst dist = turf.distance(nyc, boston, { units: 'kilometers' });\nconsole.log('NYC to Boston:', dist.toFixed(1), 'km');  // ~306 km\n\n// Create a 5km buffer around a point\nconst store = turf.point([-74.006, 40.7128]);\nconst buffer = turf.buffer(store, 5, { units: 'kilometers' });\nconsole.log('Buffer area:', (turf.area(buffer) / 1e6).toFixed(2), 'sq km');\n\n// Check if a point is inside a polygon\nconst neighborhood = turf.polygon([[\n  [-74.02, 40.70], [-74.02, 40.72], [-74.00, 40.72],\n  [-74.00, 40.70], [-74.02, 40.70],\n]]);\nconst user = turf.point([-74.01, 40.71]);\nconst isInside = turf.booleanPointInPolygon(user, neighborhood);\nconsole.log('User in neighborhood:', isInside);  // true\n\n// Bounding box of a feature collection\nconst places = turf.featureCollection([\n  turf.point([-74.006, 40.7128]),\n  turf.point([-73.985, 40.7484]),\n  turf.point([-73.963, 40.7688]),\n]);\nconst bbox = turf.bbox(places);\nconsole.log('Bounding box:', bbox);  // [minLon, minLat, maxLon, maxLat]"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Spatial join: find all points within polygons; nearest\n//             neighbor search; great circle path.\n// APPROACH  - turf.booleanPointInPolygon for spatial join; turf.nearestPoint\n//             for NN; turf.greatCircle for flight path.\n// STRENGTHS - Spatial queries without PostGIS; works on GeoJSON.\n// WEAKNESSES- O(n*m) for point-in-polygon; use spatial index for large datasets.\n//\nimport * as turf from '@turf/turf';\n\n// Spatial join — find which neighborhood each restaurant belongs to\nconst neighborhoods = turf.featureCollection([\n  turf.polygon([[[-74.02, 40.70], [-74.02, 40.75], [-73.97, 40.75], [-73.97, 40.70], [-74.02, 40.70]]], { name: 'Lower Manhattan' }),\n  turf.polygon([[[-73.97, 40.70], [-73.97, 40.75], [-73.92, 40.75], [-73.92, 40.70], [-73.97, 40.70]]], { name: 'Midtown' }),\n]);\n\nconst restaurants = turf.featureCollection([\n  turf.point([-74.01, 40.72], { name: 'Pizza Place' }),\n  turf.point([-73.98, 40.73], { name: 'Burger Joint' }),\n  turf.point([-73.95, 40.74], { name: 'Sushi Bar' }),\n]);\n\n// Join: assign neighborhood to each restaurant\nrestaurants.features.forEach(r => {\n  for (const n of neighborhoods.features) {\n    if (turf.booleanPointInPolygon(r, n)) {\n      r.properties.neighborhood = n.properties.name;\n      break;\n    }\n  }\n  console.log(`${r.properties.name} -> ${r.properties.neighborhood || 'unknown'}`);\n});\n\n// Nearest point search\nconst userLocation = turf.point([-73.99, 40.72]);\nconst nearest = turf.nearestPoint(userLocation, restaurants);\nconsole.log('Nearest restaurant:', nearest.properties.name);\n\n// Great circle path (flight route)\nconst ny = turf.point([-74.006, 40.7128]);\nconst london = turf.point([-0.1278, 51.5074]);\nconst flightPath = turf.greatCircle(ny, london, { npoints: 100 });\nconsole.log('Path points:', flightPath.geometry.coordinates.length);\n\n// Along — find point at a specific distance along a line\nconst route = turf.lineString([[-74.0, 40.7], [-73.9, 40.75], [-73.8, 40.8]]);\nconst midpoint = turf.along(route, 5, { units: 'kilometers' });\nconsole.log('Point 5km along route:', midpoint.geometry.coordinates);"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Spatial indexing with rbush, batch spatial queries, polygon\n//             simplification, and Voronoi diagrams.\n// APPROACH  - rbush for R-tree spatial index; turf.simplify for Douglas-\n//             Peucker; turf.voronoi for tessellation.\n// STRENGTHS - O(log n) spatial queries; handles 100k+ features.\n// WEAKNESSES- rbush is a separate dependency; Voronoi is CPU-intensive.\n//\nimport * as turf from '@turf/turf';\nimport RBush from 'rbush';\n\n// Build spatial index for fast point-in-polygon queries\nclass SpatialIndex {\n  constructor() {\n    this.tree = new RBush();\n    this.polygons = [];\n  }\n\n  load(features) {\n    const items = features.features.map((f, i) => {\n      const bbox = turf.bbox(f);\n      this.polygons[i] = f;\n      return { minX: bbox[0], minY: bbox[1], maxX: bbox[2], maxY: bbox[3], index: i };\n    });\n    this.tree.load(items);\n  }\n\n  queryPoint(point) {\n    const [lon, lat] = point.geometry.coordinates;\n    const candidates = this.tree.search({ minX: lon, minY: lat, maxX: lon, maxY: lat });\n    for (const c of candidates) {\n      if (turf.booleanPointInPolygon(point, this.polygons[c.index])) {\n        return this.polygons[c.index];\n      }\n    }\n    return null;\n  }\n\n  queryBbox(bbox) {\n    return this.tree.search({\n      minX: bbox[0], minY: bbox[1], maxX: bbox[2], maxY: bbox[3],\n    }).map(c => this.polygons[c.index]);\n  }\n}\n\n// Usage — index 10,000 neighborhoods\nconst index = new SpatialIndex();\nindex.load(largePolygonCollection);\n\n// Fast point lookup (O(log n) instead of O(n))\nconst result = index.queryPoint(turf.point([-74.01, 40.72]));\nconsole.log('Found polygon:', result?.properties?.name);\n\n// Polygon simplification — reduce vertex count for rendering\nconst complexPolygon = turf.polygon([/* 1000+ vertices */]);\nconst simplified = turf.simplify(complexPolygon, { tolerance: 0.001, highQuality: true });\nconsole.log('Original vertices:', complexPolygon.geometry.coordinates[0].length);\nconsole.log('Simplified vertices:', simplified.geometry.coordinates[0].length);\n\n// Voronoi diagram — tessellate space around points\nconst points = turf.featureCollection([\n  turf.point([-74.01, 40.72]),\n  turf.point([-73.98, 40.73]),\n  turf.point([-73.95, 40.74]),\n  turf.point([-73.99, 40.71]),\n]);\nconst voronoi = turf.voronoi(points, { bbox: [-74.02, 40.70, -73.93, 40.75] });\nconsole.log('Voronoi cells:', voronoi.features.length);\n\n// Cluster points within 500m\nconst clustered = turf.clustersDbscan(restaurants, 0.5, { units: 'kilometers', minPoints: 2 });\nclustered.features.forEach(f => {\n  if (f.properties.cluster) {\n    console.log(`Cluster ${f.properties.cluster_id}`);\n  }\n});"
                  }
        ],
        tips: [
                  "GeoJSON coordinates are [longitude, latitude] — NOT [lat, lon]. Getting this wrong gives wrong results.",
                  "Use turf.buffer for proximity searches — create a buffer around a point and check if other features intersect.",
                  "Use rbush for large spatial datasets — brute-force point-in-polygon is O(n); R-tree is O(log n).",
                  "Use turf.simplify to reduce polygon vertices — improves rendering performance with minimal visual change.",
                  "Use turf.clustersDbscan for spatial clustering — groups nearby points without specifying cluster count."
        ],
        mistake: "Using [latitude, longitude] order — GeoJSON spec requires [longitude, latitude]. Turf.js follows this convention. Swapping them puts points in the wrong hemisphere.",
        shorthand: {
          verbose: "import * as turf from '@turf/turf';\n// Calculate distance between NYC and Boston (lng, lat order!)\nconst nyc = turf.point([-74.006, 40.7128]);\nconst boston = turf.point([-71.0589, 42.3601]);\nconst dist = turf.distance(nyc, boston, { units: 'kilometers' });\n// ~306 km",
          concise: "turf.distance(turf.point([-74,40.7]), turf.point([-71,42.3]), {units:'kilometers'});",
        },
      },
    ],
  },

  // ── Section 2: Mapbox GL & Leaflet — interactive web maps ─────────────────────────────────────────
  {
    id: "mapbox-leaflet",
    title: "Mapbox GL & Leaflet — interactive web maps",
    entries: [
      {
        id: "mapbox-leaflet-basics",
        fn: "Mapbox GL JS & Leaflet — rendering, layers, interactivity",
        desc: "Mapbox GL JS renders vector tiles with WebGL for smooth zooming. Leaflet is a lightweight raster tile map library. Use Mapbox for modern vector maps with 3D, Leaflet for simple embeds.",
        category: "Web Mapping",
        subtitle: "mapboxgl.Map, L.map, addLayer, GeoJSON layer, popups, flyTo, heatmap, choropleth",
        signature: "const map = new mapboxgl.Map({ container: 'map', style: 'mapbox://styles/mapbox/streets-v11', center: [-74, 40.7], zoom: 10 })",
        descLong: "Mapbox GL JS: WebGL-based vector tile rendering, supports custom styles, 3D terrain, data-driven styling, clusters, heatmaps. Free tier: 50k loads/month. Leaflet: lightweight (40KB), raster tiles, simple API, huge plugin ecosystem. Use Mapbox GL for data visualization (choropleth, heatmaps, 3D), smooth zoom, and custom styling. Use Leaflet for simple maps, markers, and when you need minimal JS. Both support GeoJSON layers, popups, and interactivity. Mapbox requires an API token; Leaflet is free with OpenStreetMap tiles.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Render a map with markers and popups using Leaflet and Mapbox GL.\n// APPROACH  - Leaflet: L.map + L.tileLayer + L.marker; Mapbox: mapboxgl.Map + Marker.\n// STRENGTHS - Easy setup; rich interactivity; large ecosystem.\n// WEAKNESSES- Mapbox requires API key; Leaflet is raster-only (no vector tiles).\n//\n// --- Leaflet (simple) ---\n// const map = L.map('map').setView([40.7128, -74.006], 12);\n// L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png').addTo(map);\n// L.marker([40.7128, -74.006]).addTo(map).bindPopup('NYC').openPopup();\n\n// --- Mapbox GL JS (vector tiles, WebGL) ---\nimport mapboxgl from 'mapbox-gl';\nmapboxgl.accessToken = 'YOUR_TOKEN';\n\nconst map = new mapboxgl.Map({\n  container: 'map',\n  style: 'mapbox://styles/mapbox/streets-v12',\n  center: [-74.006, 40.7128],\n  zoom: 11,\n});\n\n// Add markers\nnew mapboxgl.Marker()\n  .setLngLat([-74.006, 40.7128])\n  .setPopup(new mapboxgl.Popup().setHTML('<h3>NYC</h3>'))\n  .addTo(map);\n\n// Fly to location\nmap.on('load', () => {\n  map.flyTo({ center: [-73.985, 40.7484], zoom: 14, speed: 1.5 });\n});\n\n// Add GeoJSON layer\nmap.addSource('restaurants', {\n  type: 'geojson',\n  data: { type: 'FeatureCollection', features: [\n    { type: 'Feature', geometry: { type: 'Point', coordinates: [-74.01, 40.72] }, properties: { name: 'Pizza' } },\n  ]},\n});\n\nmap.addLayer({\n  id: 'restaurant-points',\n  type: 'circle',\n  source: 'restaurants',\n  paint: {\n    'circle-radius': 6,\n    'circle-color': '#ff0000',\n  },\n});\n\n// Click interaction\nmap.on('click', 'restaurant-points', (e) => {\n  new mapboxgl.Popup()\n    .setLngLat(e.features[0].geometry.coordinates)\n    .setHTML(e.features[0].properties.name)\n    .addTo(map);\n});"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Choropleth map with data-driven styling; heatmap layer;\n//             cluster points.\n// APPROACH  - Mapbox data-driven paint properties; heatmap layer type;\n//             cluster option on GeoJSON source.\n// STRENGTHS - Rich data visualization; smooth WebGL rendering.\n// WEAKNESSES- Complex API; style spec has learning curve.\n//\nmap.on('load', () => {\n  // Choropleth — color states by population\n  map.addSource('states', {\n    type: 'geojson',\n    data: '/data/us-states.geojson',\n  });\n\n  map.addLayer({\n    id: 'states-choropleth',\n    type: 'fill',\n    source: 'states',\n    paint: {\n      'fill-color': [\n        'interpolate',\n        ['linear'],\n        ['get', 'population'],\n        0, '#f0f0f0',\n        500000, '#c6dbef',\n        1000000, '#6baed6',\n        5000000, '#2171b5',\n        10000000, '#08306b',\n      ],\n      'fill-opacity': 0.7,\n    },\n  });\n\n  // Heatmap — show density of events\n  map.addSource('events', {\n    type: 'geojson',\n    data: '/data/events.geojson',\n    cluster: true,\n    clusterMaxZoom: 14,\n    clusterRadius: 50,\n  });\n\n  map.addLayer({\n    id: 'event-heatmap',\n    type: 'heatmap',\n    source: 'events',\n    paint: {\n      'heatmap-weight': ['interpolate', ['linear'], ['get', 'magnitude'], 0, 0, 6, 1],\n      'heatmap-intensity': ['interpolate', ['linear'], ['zoom'], 0, 1, 9, 3],\n      'heatmap-color': [\n        'interpolate', ['linear'], ['heatmap-density'],\n        0, 'rgba(0,0,0,0)',\n        0.2, '#3182bd',\n        0.4, '#abd9e9',\n        0.6, '#fee090',\n        0.8, '#fdae61',\n        1, '#d73027',\n      ],\n      'heatmap-radius': ['interpolate', ['linear'], ['zoom'], 0, 5, 9, 30],\n      'heatmap-opacity': 0.8,\n    },\n  });\n\n  // Clustered circles\n  map.addLayer({\n    id: 'clusters',\n    type: 'circle',\n    source: 'events',\n    filter: ['has', 'point_count'],\n    paint: {\n      'circle-color': ['step', ['get', 'point_count'], '#51bbd6', 50, '#f1f075', 200, '#f28cb1'],\n      'circle-radius': ['step', ['get', 'point_count'], 15, 50, 25, 200, 40],\n    },\n  });\n\n  // Unclustered points\n  map.addLayer({\n    id: 'unclustered-point',\n    type: 'circle',\n    source: 'events',\n    filter: ['!', ['has', 'point_count']],\n    paint: { 'circle-color': '#e74c3c', 'circle-radius': 6 },\n  });\n});"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Custom map controls, real-time data updates, 3D terrain,\n//             and spatial analysis integration with Turf.js.\n// APPROACH  - Custom GeoJSONSource with setData for live updates; map.setTerrain\n//             for 3D; Turf.js for spatial queries triggered by map events.\n// STRENGTHS - Full interactive map application; real-time; 3D terrain.\n// WEAKNESSES- Complex; requires Mapbox token; performance depends on tile size.\n//\nimport mapboxgl from 'mapbox-gl';\nimport * as turf from '@turf/turf';\nimport mapSdk from 'mapbox-gl';\n\nmapboxgl.accessToken = 'YOUR_TOKEN';\n\nconst map = new mapboxgl.Map({\n  container: 'map',\n  style: 'mapbox://styles/mapbox/streets-v12',\n  center: [-74.006, 40.7128],\n  zoom: 11,\n  pitch: 45,  // 3D tilt\n  bearing: -17,\n});\n\n// Add 3D terrain\nmap.on('load', () => {\n  map.addSource('dem', { type: 'raster-dem', url: 'mapbox://mapbox.terrain-rgb' });\n  map.setTerrain({ source: 'dem', exaggeration: 1.5 });\n  map.addLayer({ id: 'sky', type: 'sky', paint: { 'sky-color': '#87CEEB', 'sky-horizon-blend': 0.5 } });\n\n  // Real-time vehicle tracking source\n  map.addSource('vehicles', { type: 'geojson', data: { type: 'FeatureCollection', features: [] } });\n  map.addLayer({\n    id: 'vehicle-icons',\n    type: 'symbol',\n    source: 'vehicles',\n    layout: {\n      'icon-image': 'vehicle-icon',\n      'icon-rotate': ['get', 'bearing'],\n      'icon-allow-overlap': true,\n    },\n  });\n\n  // Update vehicle positions via WebSocket\n  const ws = new WebSocket('ws://api.example.com/vehicles');\n  ws.onmessage = (event) => {\n    const data = JSON.parse(event.data);\n    const features = data.vehicles.map(v => ({\n      type: 'Feature',\n      geometry: { type: 'Point', coordinates: [v.lon, v.lat] },\n      properties: { id: v.id, bearing: v.bearing, speed: v.speed },\n    }));\n    map.getSource('vehicles').setData({ type: 'FeatureCollection', features });\n  };\n\n  // Spatial analysis on click — find all vehicles within 1km\n  map.on('click', (e) => {\n    const clickPoint = turf.point([e.lngLat.lng, e.lngLat.lat]);\n    const buffer = turf.buffer(clickPoint, 1, { units: 'kilometers' });\n    const vehicleData = map.getSource('vehicles')._data;\n    const nearby = vehicleData.features.filter(f =>\n      turf.booleanPointInPolygon(f, buffer)\n    );\n    console.log(`Vehicles within 1km: ${nearby.length}`);\n\n    // Draw search buffer\n    if (map.getSource('search-buffer')) map.removeSource('search-buffer');\n    map.addSource('search-buffer', { type: 'geojson', data: buffer });\n    map.addLayer({\n      id: 'search-buffer-layer',\n      type: 'fill',\n      source: 'search-buffer',\n      paint: { 'fill-color': '#007cbf', 'fill-opacity': 0.2 },\n    });\n  });\n\n  // Custom navigation control\n  class MeasureControl {\n    onAdd(map) {\n      this.map = map;\n      this.container = document.createElement('div');\n      this.container.className = 'mapboxgl-ctrl mapboxgl-ctrl-group';\n      const btn = document.createElement('button');\n      btn.textContent = 'M';\n      btn.onclick = () => this.measure();\n      this.container.appendChild(btn);\n      return this.container;\n    }\n    measure() {\n      let points = [];\n      map.on('click', function handler(e) {\n        points.push([e.lngLat.lng, e.lngLat.lat]);\n        if (points.length === 2) {\n          const dist = turf.distance(turf.point(points[0]), turf.point(points[1]), { units: 'kilometers' });\n          new mapboxgl.Popup().setLngLat(points[1]).setHTML(`${dist.toFixed(2)} km`).addTo(map);\n          map.off('click', handler);\n          points = [];\n        }\n      });\n    }\n    onRemove() { this.container.parentNode.removeChild(this.container); }\n  }\n  map.addControl(new MeasureControl(), 'top-right');\n});"
                  }
        ],
        tips: [
                  "Use cluster: true on GeoJSON sources for large point datasets — Mapbox handles clustering client-side.",
                  "Use data-driven expressions (['get', 'property']) for dynamic styling — colors/sizes based on feature properties.",
                  "Use map.getSource(id).setData() for real-time updates — no need to remove and re-add layers.",
                  "Use map.setTerrain() with raster-dem source for 3D terrain — add sky layer for realistic atmosphere.",
                  "Use Leaflet for simple maps (40KB) and Mapbox GL for complex visualization (WebGL, 3D, vector tiles)."
        ],
        mistake: "Forgetting to set mapboxgl.accessToken — the map will render blank or show an error. Set it before creating any Map instance.",
        shorthand: {
          verbose: "// Full Mapbox GL setup with token, style, center, and zoom\nmapboxgl.accessToken = 'pk.YOUR_TOKEN';\nconst map = new mapboxgl.Map({\n  container: 'map',\n  style: 'mapbox://styles/mapbox/streets-v12',\n  center: [-74.006, 40.7128],\n  zoom: 10,\n});\nmap.addControl(new mapboxgl.NavigationControl());",
          concise: "new mapboxgl.Map({container:'map',style:'mapbox://styles/mapbox/streets-v12',center:[-74,40.7],zoom:10});",
        },
      },
    ],
  },

  // ── Section 3: proj4js — coordinate system transformations ─────────────────────────────────────────
  {
    id: "proj4",
    title: "proj4js — coordinate system transformations",
    entries: [
      {
        id: "proj4-basics",
        fn: "proj4js — transform coordinates between projections",
        desc: "proj4js converts coordinates between different map projections (WGS84, UTM, Web Mercator, State Plane, etc.). Essential when working with data in different coordinate systems.",
        category: "Coordinate Transform",
        subtitle: "proj4, proj4.defs, WGS84, EPSG:3857, UTM, Web Mercator, forward/inverse transform",
        signature: "proj4('EPSG:3857', 'EPSG:4326', [x, y]) // Web Mercator to WGS84",
        descLong: "proj4js is a JavaScript port of PROJ (the standard geospatial projection library). Define projections with proj4.defs() using EPSG codes or PROJ strings. Transform coordinates with proj4(fromProjection, toProjection, coordinates). Common projections: EPSG:4326 (WGS84 lat/lon), EPSG:3857 (Web Mercator — used by Mapbox/Leaflet), EPSG:32618 (UTM Zone 18N), NAD83 State Plane. Use proj4js when your data is in a local projection (e.g., UTM, State Plane) and you need to convert to WGS84 for web maps, or vice versa.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Convert between WGS84 (lat/lon) and Web Mercator (meters).\n// APPROACH  - proj4('EPSG:4326', 'EPSG:3857', [lon, lat]).\n// STRENGTHS - Accurate projection transforms; supports 4000+ EPSG codes.\n// WEAKNESSES- Some projections need custom PROJ string definitions.\n//\nimport proj4 from 'proj4';\n\n// WGS84 (EPSG:4326) -> Web Mercator (EPSG:3857)\nconst [lon, lat] = [-74.006, 40.7128];  // NYC\nconst [x, y] = proj4('EPSG:4326', 'EPSG:3857', [lon, lat]);\nconsole.log('Web Mercator:', x.toFixed(1), y.toFixed(1));\n// ~(-8237643.0, 4973849.0)\n\n// Web Mercator -> WGS84\nconst [backLon, backLat] = proj4('EPSG:3857', 'EPSG:4326', [x, y]);\nconsole.log('Back to WGS84:', backLon.toFixed(6), backLat.toFixed(6));\n// ~(-74.006, 40.7128)\n\n// Define a custom projection (UTM Zone 18N — covers NYC area)\nproj4.defs('EPSG:32618', '+proj=utm +zone=18 +ellps=WGS84 +datum=WGS84 +units=m +no_defs');\n\n// WGS84 -> UTM Zone 18N\nconst [easting, northing] = proj4('EPSG:4326', 'EPSG:32618', [lon, lat]);\nconsole.log('UTM 18N:', easting.toFixed(1), northing.toFixed(1));\n// ~(583,960.0, 4,511,380.0)"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Batch transform a GeoJSON FeatureCollection from one\n//             projection to another; handle custom State Plane projections.\n// APPROACH  - Iterate features; transform each coordinate pair; handle\n//             MultiPolygon and MultiLineString geometries.\n// STRENGTHS - Full GeoJSON projection transform; handles all geometry types.\n// WEAKNESSES- Recursive coordinate transform is slow for very large datasets.\n//\nimport proj4 from 'proj4';\n\n// Define State Plane projection (New York Long Island — EPSG:32118)\nproj4.defs('EPSG:32118', '+proj=lcc +lat_1=41.03333333333333 +lat_2=40.66666666666666 +lat_0=40.16666666666666 +lon_0=-74 +x_0=300000 +y_0=0 +ellps=GRS80 +datum=NAD83 +units=m +no_defs');\n\nfunction transformGeoJSON(geojson, fromProj, toProj) {\n  const transform = proj4(fromProj, toProj);\n\n  function transformCoords(coords) {\n    if (typeof coords[0] === 'number') {\n      return transform.forward(coords);\n    }\n    return coords.map(transformCoords);\n  }\n\n  const transformed = JSON.parse(JSON.stringify(geojson));\n  transformed.features = transformed.features.map(feature => {\n    feature.geometry.coordinates = transformCoords(feature.geometry.coordinates);\n    return feature;\n  });\n  return transformed;\n}\n\n// Transform GeoJSON from State Plane to WGS84\nconst statePlaneData = {\n  type: 'FeatureCollection',\n  features: [{\n    type: 'Feature',\n    geometry: { type: 'Polygon', coordinates: [[[300000, 0], [310000, 0], [310000, 10000], [300000, 10000], [300000, 0]]] },\n    properties: { name: 'Parcel A' },\n  }],\n};\n\nconst wgs84Data = transformGeoJSON(statePlaneData, 'EPSG:32118', 'EPSG:4326');\nconsole.log('Transformed polygon:', wgs84Data.features[0].geometry.coordinates);\n\n// Transform for Mapbox display (WGS84 -> Web Mercator)\nconst mercatorData = transformGeoJSON(wgs84Data, 'EPSG:4326', 'EPSG:3857');\nconsole.log('Mercator coords:', mercatorData.features[0].geometry.coordinates[0][0]);"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Projection validation, accuracy measurement, and integration\n//             with Turf.js for spatial analysis in projected coordinates.\n// APPROACH  - Round-trip transform to check accuracy; use UTM for distance\n//             calculations (planar is more accurate than great circle for\n//             short distances); convert back for display.\n// STRENGTHS - Accurate local measurements; proper CRS handling.\n// WEAKNESSES- Must choose appropriate UTM zone; transforms add complexity.\n//\nimport proj4 from 'proj4';\nimport * as turf from '@turf/turf';\n\n// Auto-detect UTM zone from longitude\nfunction getUTMZone(lon) {\n  return Math.floor((lon + 180) / 6) + 1;\n}\n\nfunction getUTMProjection(lon, lat) {\n  const zone = getUTMZone(lon);\n  const isNorth = lat >= 0;\n  const epsg = isNorth ? 32600 + zone : 32700 + zone;\n  proj4.defs(`EPSG:${epsg}`, `+proj=utm +zone=${zone} ${isNorth ? '+north' : '+south'} +ellps=WGS84 +datum=WGS84 +units=m +no_defs`);\n  return `EPSG:${epsg}`;\n}\n\n// Accurate distance using UTM (better than great circle for <100km)\nfunction accurateDistance(pointA, pointB) {\n  const [lonA, latA] = pointA;\n  const [lonB, latB] = pointB;\n  const avgLon = (lonA + lonB) / 2;\n  const avgLat = (latA + latB) / 2;\n  const utmProj = getUTMProjection(avgLon, avgLat);\n\n  const [xA, yA] = proj4('EPSG:4326', utmProj, [lonA, latA]);\n  const [xB, yB] = proj4('EPSG:4326', utmProj, [lonB, latB]);\n\n  return Math.sqrt((xB - xA) ** 2 + (yB - yA) ** 2);  // meters\n}\n\n// Compare accuracy: Turf (great circle) vs UTM (planar)\nconst p1 = [-74.006, 40.7128];  // NYC\nconst p2 = [-74.001, 40.7150];  // ~400m away\n\nconst turfDist = turf.distance(turf.point(p1), turf.point(p2), { units: 'meters' });\nconst utmDist = accurateDistance(p1, p2);\nconsole.log('Turf distance:', turfDist.toFixed(2), 'm');\nconsole.log('UTM distance:', utmDist.toFixed(2), 'm');\nconsole.log('Difference:', Math.abs(turfDist - utmDist).toFixed(4), 'm');\n\n// Batch transform with accuracy validation\nfunction validateTransform(original, transformed, fromProj, toProj, tolerance = 0.001) {\n  const roundTrip = transformGeoJSON(transformed, toProj, fromProj);\n  let maxError = 0;\n  for (let i = 0; i < original.features.length; i++) {\n    const origCoords = original.features[i].geometry.coordinates;\n    const rtCoords = roundTrip.features[i].geometry.coordinates;\n    const error = Math.abs(origCoords[0] - rtCoords[0]) + Math.abs(origCoords[1] - rtCoords[1]);\n    maxError = Math.max(maxError, error);\n  }\n  return { maxError, passed: maxError < tolerance };\n}"
                  }
        ],
        tips: [
                  "Use EPSG codes (e.g., EPSG:4326) instead of PROJ strings when possible — more readable and maintainable.",
                  "Use UTM for accurate local distance/area calculations — planar math is more accurate than spherical for short distances.",
                  "Auto-detect UTM zone with Math.floor((lon + 180) / 6) + 1 — ensures you use the correct zone for your data.",
                  "Round-trip transform to validate accuracy — transform from A to B and back; the error should be < 0.001 degrees.",
                  "Define custom projections with proj4.defs() at app startup — avoids \"unknown projection\" errors during runtime."
        ],
        mistake: "Using WGS84 (degrees) for area calculations — turf.area expects Web Mercator (meters). Convert to EPSG:3857 first, or use UTM for local area calculations.",
        shorthand: {
          verbose: "import proj4 from 'proj4';\n// Convert WGS84 lat/lng to Web Mercator meters\nconst [x, y] = proj4('EPSG:4326', 'EPSG:3857', [-74.006, 40.7128]);\n// x: -8237638.12, y: 4973861.68\n// Define custom projection at startup:\n// proj4.defs('EPSG:2263', '+proj=lcc +lat_1=40.666...');",
          concise: "proj4('EPSG:4326','EPSG:3857',[-74.006,40.7128]);",
        },
      },
    ],
  },
]

export default { meta, sections }

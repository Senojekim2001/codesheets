export const meta = {
  "title": "Deep Learning",
  "domain": "javascript",
  "sheet": "deeplearning",
  "icon": "🔮"
}

export const sections = [

  // ── Section 1: TF.js Layers — CNNs, RNNs, transfer learning ─────────────────────────────────────────
  {
    id: "tfjs-layers",
    title: "TF.js Layers — CNNs, RNNs, transfer learning",
    entries: [
      {
        id: "cnn-rnn",
        fn: "TF.js — CNN for images, LSTM for sequences, transfer learning",
        desc: "Build deep learning architectures in TF.js: Convolutional Neural Networks (CNNs) for image classification, LSTM/GRU for sequence data, and transfer learning with pre-trained models.",
        category: "Deep Learning",
        subtitle: "conv2d, maxPooling2d, lstm, gru, embedding, transfer learning, MobileNet, fine-tuning",
        signature: "model.add(tf.layers.conv2d({ filters: 32, kernelSize: 3, activation: 'relu', inputShape: [28,28,1] }))",
        descLong: "TF.js supports the same layer types as Keras. CNNs: conv2d (feature extraction), maxPooling2d (downsampling), flatten, dense (classification). RNNs: lstm (long-term dependencies), gru (lighter alternative), embedding (word vectors). Transfer learning: load a pre-trained model (MobileNet, COCO-SSD), freeze base layers, add new dense layers for your task, fine-tune. Use @tensorflow-models for ready-to-use pre-trained models (mobilenet, coco-ssd, body-pix, posenet, etc.).",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Build a CNN for MNIST digit classification (28x28 grayscale).\n// APPROACH  - conv2d -> maxPool -> conv2d -> maxPool -> flatten -> dense -> softmax.\n// STRENGTHS - Achieves ~99% accuracy; runs in browser with WebGL.\n// WEAKNESSES- Training is slower than Python TF on GPU.\n//\nimport * as tf from '@tensorflow/tfjs-node';\n\nconst model = tf.sequential();\n\n// Conv block 1\nmodel.add(tf.layers.conv2d({\n  inputShape: [28, 28, 1],\n  filters: 32,\n  kernelSize: 3,\n  activation: 'relu',\n}));\nmodel.add(tf.layers.maxPooling2d({ poolSize: 2 }));\n\n// Conv block 2\nmodel.add(tf.layers.conv2d({ filters: 64, kernelSize: 3, activation: 'relu' }));\nmodel.add(tf.layers.maxPooling2d({ poolSize: 2 }));\n\n// Classifier\nmodel.add(tf.layers.flatten());\nmodel.add(tf.layers.dropout({ rate: 0.25 }));\nmodel.add(tf.layers.dense({ units: 128, activation: 'relu' }));\nmodel.add(tf.layers.dense({ units: 10, activation: 'softmax' }));\n\nmodel.compile({\n  optimizer: 'adam',\n  loss: 'categoricalCrossentropy',\n  metrics: ['accuracy'],\n});\n\nmodel.summary();\n\n// Train (assuming trainXs: [N, 28, 28, 1], trainYs: one-hot [N, 10])\nawait model.fit(trainXs, trainYs, {\n  epochs: 10,\n  batchSize: 32,\n  validationSplit: 0.2,\n  callbacks: tf.callbacks.earlyStopping({ monitor: 'val_loss', patience: 3 }),\n});\n\n// Predict\nconst prediction = model.predict(tf.tensor4d(testImage, [1, 28, 28, 1]));\nconst digit = prediction.argMax(-1).dataSync()[0];\nconsole.log('Predicted digit:', digit);"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - LSTM for sentiment analysis (text classification);\n//             embedding layer + LSTM + dense classifier.\n// APPROACH  - Embedding -> LSTM -> Dense(softmax); train on tokenized text.\n// STRENGTHS - Captures word order and context; handles variable-length input.\n// WEAKNESSES- LSTMs are slower than transformers; vanishing gradient on long sequences.\n//\nimport * as tf from '@tensorflow/tfjs-node';\n\n// Hyperparameters\nconst VOCAB_SIZE = 10_000;\nconst EMBEDDING_DIM = 64;\nconst MAX_LEN = 100;\nconst NUM_CLASSES = 3;  // positive, neutral, negative\n\nconst model = tf.sequential();\n\n// Embedding layer — word indices to dense vectors\nmodel.add(tf.layers.embedding({\n  inputDim: VOCAB_SIZE,\n  outputDim: EMBEDDING_DIM,\n  inputLength: MAX_LEN,\n}));\n\n// LSTM layer — captures sequential dependencies\nmodel.add(tf.layers.lstm({\n  units: 64,\n  returnSequences: false,  // only last output\n  dropout: 0.2,\n  recurrentDropout: 0.2,\n}));\n\n// Classifier\nmodel.add(tf.layers.dense({ units: 32, activation: 'relu' }));\nmodel.add(tf.layers.dense({ units: NUM_CLASSES, activation: 'softmax' }));\n\nmodel.compile({\n  optimizer: tf.train.adam(0.001),\n  loss: 'categoricalCrossentropy',\n  metrics: ['accuracy'],\n});\n\n// Tokenize and pad text\nfunction tokenize(texts, vocab, maxLen) {\n  return texts.map(text => {\n    const tokens = text.toLowerCase().split(/\\s+/).map(w => vocab[w] || 0);\n    // Pad or truncate\n    if (tokens.length < maxLen) {\n      return [...tokens, ...Array(maxLen - tokens.length).fill(0)];\n    }\n    return tokens.slice(0, maxLen);\n  });\n}\n\n// Build vocabulary from training data\nconst vocab = {};\nlet vocabIdx = 1;\nfor (const text of trainingTexts) {\n  for (const word of text.toLowerCase().split(/\\s+/)) {\n    if (!vocab[word]) vocab[word] = vocabIdx++;\n    if (vocabIdx >= VOCAB_SIZE) break;\n  }\n}\n\n// Prepare data\nconst trainXs = tf.tensor2d(tokenize(trainingTexts, vocab, MAX_LEN));\nconst trainYs = tf.oneHot(tf.tensor1d(trainingLabels, 'int32'), NUM_CLASSES);\n\n// Train\nawait model.fit(trainXs, trainYs, {\n  epochs: 15,\n  batchSize: 64,\n  validationSplit: 0.2,\n});\n\n// Predict sentiment\nconst testText = tokenize(['This product is amazing!'], vocab, MAX_LEN);\nconst prediction = model.predict(tf.tensor2d(testText));\nconst sentiment = ['negative', 'neutral', 'positive'][prediction.argMax(-1).dataSync()[0]];\nconsole.log('Sentiment:', sentiment);  // positive"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Transfer learning with MobileNet for custom image classification;\n//             freeze base, add custom head, fine-tune top layers.\n// APPROACH  - Load pre-trained MobileNet; freeze all layers; add custom\n//             dense layers; train on new dataset; optionally unfreeze top\n//             conv layers for fine-tuning.\n// STRENGTHS - Leverages pre-trained features; trains in minutes not hours;\n//             works with small datasets.\n// WEAKNESSES- Browser inference requires downloading ~16MB model.\n//\nimport * as tf from '@tensorflow/tfjs-node';\nimport * as mobilenet from '@tensorflow-models/mobilenet';\n\n// Load pre-trained MobileNet (trained on ImageNet)\nconst baseModel = await mobilenet.load({ version: 2, alpha: 1.0 });\n\n// Get the internal TF.js model to access layers\nconst internalModel = baseModel.model;\nconsole.log('Base model layers:', internalModel.layers.length);\n\n// Freeze all base layers — don't update pre-trained weights\nfor (const layer of internalModel.layers) {\n  layer.trainable = false;\n}\n\n// Create new model: base MobileNet features -> custom classifier\nconst newModel = tf.sequential();\n\n// Add base model as first layer (feature extractor)\nnewModel.add(tf.layers.inputLayer({ inputShape: [224, 224, 3] }));\n\n// Use base model's intermediate output (before final classification)\nconst featureExtractor = tf.model({\n  inputs: internalModel.inputs,\n  outputs: internalModel.getLayer('global_average_pooling2d').output,\n});\nnewModel.add(featureExtractor);\n\n// Custom classification head\nnewModel.add(tf.layers.dense({ units: 256, activation: 'relu' }));\nnewModel.add(tf.layers.batchNormalization());\nnewModel.add(tf.layers.dropout({ rate: 0.5 }));\nnewModel.add(tf.layers.dense({ units: NUM_CLASSES, activation: 'softmax' }));\n\nnewModel.compile({\n  optimizer: tf.train.adam(0.0001),  // low LR for transfer learning\n  loss: 'categoricalCrossentropy',\n  metrics: ['accuracy'],\n});\n\n// Train on custom dataset\nawait newModel.fit(trainImages, trainLabels, {\n  epochs: 20,\n  batchSize: 16,\n  validationSplit: 0.2,\n  callbacks: [\n    tf.callbacks.earlyStopping({ monitor: 'val_loss', patience: 5 }),\n    tf.callbacks.modelCheckpoint({ filepath: 'best-model', saveBestOnly: true }),\n  ],\n});\n\n// Fine-tuning — unfreeze top conv layers\nconsole.log('Fine-tuning top layers...');\nconst topLayers = internalModel.layers.slice(-10);\nfor (const layer of topLayers) {\n  layer.trainable = true;\n}\n\n// Recompile with even lower LR\nnewModel.compile({\n  optimizer: tf.train.adam(0.00001),  // very low LR for fine-tuning\n  loss: 'categoricalCrossentropy',\n  metrics: ['accuracy'],\n});\n\nawait newModel.fit(trainImages, trainLabels, {\n  epochs: 10,\n  batchSize: 16,\n  validationSplit: 0.2,\n});\n\n// Save for browser deployment\nawait newModel.save('file://./custom-classifier');\n\n// Browser inference:\n// const model = await tf.loadLayersModel('https://cdn.example.com/custom-classifier/model.json');\n// const img = tf.browser.fromPixels(imageElement).resizeBilinear([224, 224]).expandDims(0).div(255);\n// const prediction = model.predict(img);"
                  }
        ],
        tips: [
                  "Use BatchNormalization after dense/conv layers — stabilizes training and allows higher learning rates.",
                  "Use Dropout (0.25-0.5) to prevent overfitting — especially important with small datasets.",
                  "Use earlyStopping callback — stops training when validation loss stops improving, prevents overfitting.",
                  "For transfer learning, freeze base layers first, then optionally unfreeze top layers for fine-tuning.",
                  "Use a low learning rate (0.0001) for transfer learning and even lower (0.00001) for fine-tuning."
        ],
        mistake: "Not normalizing input images — MobileNet expects inputs in [0, 1] or [-1, 1] range. Feeding raw pixel values [0, 255] causes poor convergence. Use .div(255) or .sub(127.5).div(127.5).",
        shorthand: {
          verbose: "const m = tf.sequential();\nm.add(tf.layers.conv2d({ filters: 32, kernelSize: 3, activation: 'relu', inputShape: [28,28,1] }));\nm.add(tf.layers.maxPooling2d({ poolSize: 2 }));\nm.add(tf.layers.flatten());\nm.add(tf.layers.dense({ units: 10, activation: 'softmax' }));\nm.compile({ optimizer: 'adam', loss: 'categoricalCrossentropy' });",
          concise: "const m = tf.sequential(); m.add(tf.layers.conv2d({filters:32,kernelSize:3,activation:'relu',inputShape:[28,28,1]})); m.add(tf.layers.flatten()); m.add(tf.layers.dense({units:10,activation:'softmax'})); m.compile({optimizer:'adam',loss:'cce'});",
        },
      },
    ],
  },

  // ── Section 2: WebGPU & ONNX Runtime — next-gen inference ─────────────────────────────────────────
  {
    id: "webgpu-onnx",
    title: "WebGPU & ONNX Runtime — next-gen inference",
    entries: [
      {
        id: "webgpu-onnx",
        fn: "WebGPU inference + ONNX Runtime Web",
        desc: "WebGPU enables GPU-accelerated ML inference in the browser without WebGL limitations. ONNX Runtime Web runs PyTorch/TF models exported to ONNX format with WebGPU/WASM backends.",
        category: "Inference",
        subtitle: "onnxruntime-web, WebGPU, WASM, ort.InferenceSession, tensor conversion, model export",
        signature: "const session = await ort.InferenceSession.create(modelUrl, { executionProvider: 'webgpu' })",
        descLong: "WebGPU is the successor to WebGL for GPU compute in browsers — supports compute shaders, storage buffers, and better performance for ML. ONNX Runtime Web (onnxruntime-web) loads .onnx models (exported from PyTorch, TensorFlow, scikit-learn) and runs them with WebGPU or WASM backends. Use this for running production ML models in the browser without rewriting them in TF.js. Export models with torch.onnx.export() or tf2onnx, then load with ort.InferenceSession.create().",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Run an ONNX model in the browser with ONNX Runtime Web.\n// APPROACH  - ort.InferenceSession.create(); feed tensors; run inference.\n// STRENGTHS - Runs PyTorch/TF models in browser; WebGPU acceleration.\n// WEAKNESSES- Requires ONNX export step; WebGPU not supported in all browsers.\n//\nimport * as ort from 'onnxruntime-web/web';\n\n// Load ONNX model\nconst session = await ort.InferenceSession.create('/models/resnet50.onnx', {\n  executionProviders: ['webgpu', 'wasm'],  // fallback to WASM\n});\n\n// Prepare input tensor (1x3x224x224 float32)\nconst imageData = new Float32Array(1 * 3 * 224 * 224);\n// ... fill with preprocessed image data ...\nconst inputTensor = new ort.Tensor('float32', imageData, [1, 3, 224, 224]);\n\n// Run inference\nconst feeds = { [session.inputNames[0]]: inputTensor };\nconst results = await session.run(feeds);\n\n// Get output (logits)\nconst output = results[session.outputNames[0]];\nconst predictions = output.data;\nconst topClass = Array.from(predictions)\n  .map((v, i) => ({ value: v, index: i }))\n  .sort((a, b) => b.value - a.value)\n  .slice(0, 5);\nconsole.log('Top 5 predictions:', topClass);"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Image preprocessing pipeline for ONNX inference;\n//             canvas -> tensor -> normalize -> inference.\n// APPROACH  - tf.browser.fromPixels for canvas/image; normalize with\n//             mean/std; convert to ONNX tensor; run session.\n// STRENGTHS - Full pipeline from image element to prediction.\n// WEAKNESSES- Preprocessing must match the model's training preprocessing.\n//\nimport * as ort from 'onnxruntime-web/web';\nimport * as tf from '@tensorflow/tfjs';\n\nasync function classifyImage(imageElement, modelUrl, labels) {\n  // Load model\n  const session = await ort.InferenceSession.create(modelUrl, {\n    executionProviders: ['webgpu', 'wasm'],\n  });\n\n  // Preprocess image: resize, normalize, convert to tensor\n  const tensor = tf.tidy(() => {\n    return tf.browser.fromPixels(imageElement)         // [H, W, 3] uint8\n      .resizeBilinear([224, 224])                       // [224, 224, 3]\n      .div(255.0)                                       // normalize to [0, 1]\n      .sub(tf.tensor1d([0.485, 0.456, 0.406]))          // subtract mean (ImageNet)\n      .div(tf.tensor1d([0.229, 0.224, 0.225]))          // divide by std (ImageNet)\n      .transpose([2, 0, 1])                             // [3, 224, 224] (CHW)\n      .expandDims(0);                                   // [1, 3, 224, 224]\n  });\n\n  // Convert TF.js tensor to ONNX tensor\n  const onnxTensor = new ort.Tensor(\n    'float32',\n    Float32Array.from(tensor.dataSync()),\n    [1, 3, 224, 224],\n  );\n\n  // Run inference\n  const results = await session.run({ [session.inputNames[0]]: onnxTensor });\n  const logits = results[session.outputNames[0]].data;\n\n  // Softmax to get probabilities\n  const maxLogit = Math.max(...logits);\n  const expLogits = logits.map(l => Math.exp(l - maxLogit));\n  const sumExp = expLogits.reduce((a, b) => a + b, 0);\n  const probabilities = expLogits.map(e => e / sumExp);\n\n  // Top 5 predictions\n  const top5 = probabilities\n    .map((prob, idx) => ({ label: labels[idx], probability: prob }))\n    .sort((a, b) => b.probability - a.probability)\n    .slice(0, 5);\n\n  tf.dispose(tensor);\n  return top5;\n}\n\n// Usage in browser:\n// const img = document.getElementById('image');\n// const results = await classifyImage(img, '/models/resnet50.onnx', imagenetLabels);\n// results.forEach(r => console.log(`${r.label}: ${(r.probability * 100).toFixed(1)}%`));"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - WebGPU compute shaders for custom ML ops; batch inference;\n//             model warmup; and fallback strategy for unsupported browsers.\n// APPROACH  - Check WebGPU support; warmup model; batch multiple inputs;\n//             custom WebGPU compute shader for softmax; WASM fallback.\n// STRENGTHS - GPU-accelerated; batched for throughput; graceful degradation.\n// WEAKNESSES- WebGPU API is low-level; compute shaders require WGSL knowledge.\n//\nimport * as ort from 'onnxruntime-web/web';\n\n// Check WebGPU support\nasync function checkWebGPU() {\n  if (!navigator.gpu) return false;\n  try {\n    const adapter = await navigator.gpu.requestAdapter();\n    return !!adapter;\n  } catch { return false; }\n}\n\n// Model manager — handles loading, warmup, and inference\nclass ONNXModelManager {\n  constructor(modelUrl) {\n    this.modelUrl = modelUrl;\n    this.session = null;\n    this.backend = null;\n  }\n\n  async init() {\n    const hasWebGPU = await checkWebGPU();\n    this.backend = hasWebGPU ? 'webgpu' : 'wasm';\n\n    this.session = await ort.InferenceSession.create(this.modelUrl, {\n      executionProviders: [this.backend, 'wasm'],\n      graphOptimizationLevel: 'all',\n      enableMemPattern: true,\n      enableCpuMemArena: this.backend === 'wasm',\n    });\n\n    // Warmup — first inference is always slow (kernel compilation)\n    await this.warmup();\n    console.log(`Model loaded on ${this.backend}`);\n  }\n\n  async warmup() {\n    const inputName = this.session.inputNames[0];\n    const inputShape = this.session.inputMetadata[inputName].shape\n      .map(d => typeof d === 'number' ? d : 1);\n    const dummyData = new Float32Array(inputShape.reduce((a, b) => a * b, 1));\n    const dummyTensor = new ort.Tensor('float32', dummyData, inputShape);\n    await this.session.run({ [inputName]: dummyTensor });\n  }\n\n  async infer(inputData, inputShape) {\n    const tensor = new ort.Tensor('float32', inputData, inputShape);\n    const results = await this.session.run({ [this.session.inputNames[0]]: tensor });\n    return results[this.session.outputNames[0]].data;\n  }\n\n  async batchInfer(inputs, inputShape) {\n    // Run multiple inferences in parallel\n    const promises = inputs.map(input => this.infer(input, inputShape));\n    return Promise.all(promises);\n  }\n}\n\n// Custom WebGPU compute shader — softmax\nasync function webgpuSoftmax(logits, gpuDevice) {\n  const shaderCode = `\n    @group(0) @binding(0) var<storage, read> input: array<f32>;\n    @group(0) @binding(1) var<storage, read_write> output: array<f32>;\n    @group(0) @binding(2) var<uniform> size: u32;\n\n    @compute @workgroup_size(64)\n    fn main(@builtin(global_invocation_id) gid: vec3<u32>) {\n      let idx = gid.x;\n      if (idx >= size) { return; }\n\n      // Find max for numerical stability\n      var maxVal: f32 = input[0];\n      for (var i: u32 = 1u; i < size; i++) {\n        if (input[i] > maxVal) { maxVal = input[i]; }\n      }\n\n      // Compute exp and sum\n      var sum: f32 = 0.0;\n      for (var i: u32 = 0u; i < size; i++) {\n        output[i] = exp(input[i] - maxVal);\n        sum += output[i];\n      }\n\n      // Normalize\n      if (idx < size) {\n        output[idx] = output[idx] / sum;\n      }\n    }\n  `;\n\n  // ... WebGPU buffer setup and dispatch ...\n  // This is a simplified example — real implementation needs buffer creation,\n  // pipeline setup, and command encoding.\n}\n\n// Usage\nconst manager = new ONNXModelManager('/models/efficientnet.onnx');\nawait manager.init();\n\n// Single inference\nconst result = await manager.infer(preprocessedImage, [1, 3, 224, 224]);\n\n// Batch inference\nconst batchResults = await manager.batchInfer(\n  images.map(img => preprocess(img)),\n  [1, 3, 224, 224],\n);"
                  }
        ],
        tips: [
                  "Always warmup the model with a dummy input — first inference compiles GPU kernels and is 10-100x slower.",
                  "Check navigator.gpu before using WebGPU — not all browsers support it yet; fallback to WASM.",
                  "Match preprocessing exactly to the model's training preprocessing — wrong normalization gives garbage predictions.",
                  "Use executionProviders: ['webgpu', 'wasm'] — ONNX Runtime falls back automatically if WebGPU fails.",
                  "Export models with dynamic batch dimension (batch_size=1 for inference, dynamic for training) for flexibility."
        ],
        mistake: "Forgetting to transpose image data from HWC (height, width, channels) to CHW (channels, height, width) — ONNX models typically expect CHW format, but browser images are HWC. Use .transpose([2, 0, 1]).",
        shorthand: {
          verbose: "import * as ort from 'onnxruntime-web/web';\nconst session = await ort.InferenceSession.create('model.onnx', { executionProviders: ['webgpu', 'wasm'] });\nconst input = new ort.Tensor('float32', data, [1, 3, 224, 224]);\nconst output = await session.run({ [session.inputNames[0]]: input });",
          concise: "const s = await ort.InferenceSession.create('m.onnx'); const r = await s.run({ [s.inputNames[0]]: new ort.Tensor('float32', d, [1,3,224,224]) });",
        },
      },
    ],
  },
]

export default { meta, sections }

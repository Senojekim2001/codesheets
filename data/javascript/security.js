export const meta = {
  "title": "Web Security",
  "domain": "javascript",
  "sheet": "security",
  "icon": "🔒"
}

export const sections = [

  // ── Section 1: XSS & Injection Prevention ─────────────────────────────────────────
  {
    id: "xss-prevention",
    title: "XSS & Injection Prevention",
    entries: [
      {
        id: "xss",
        fn: "Cross-Site Scripting (XSS)",
        desc: "Prevent injection of malicious scripts into web pages.",
        category: "Injection",
        subtitle: "Never trust user input — escape, sanitize, and use CSP",
        signature: "textContent | DOMPurify.sanitize() | CSP headers",
        descLong: "XSS occurs when user input is rendered as HTML without escaping, allowing script injection. Three types: stored (persisted in DB), reflected (in URL), DOM-based (client-side). Prevention: use textContent over innerHTML, sanitize HTML with DOMPurify, set Content-Security-Policy headers, and always escape user input on the server.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Cross-Site Scripting (XSS) — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - textContent vs innerHTML — the #1 XSS rule\n// STRENGTHS - Shows the most common XSS vector and fix\n// WEAKNESSES- No sanitization, no CSP\n//\n// BAD: innerHTML with user input — XSS vulnerability\nconst userInput = '<img src=x onerror=\"alert(\\'xss\\')\">';\nelement.innerHTML = userInput;  // SCRIPT EXECUTES!\n// GOOD: textContent — renders as text, not HTML\nelement.textContent = userInput;  // displays the string literally\n// GOOD: if you must use innerHTML, escape first\nfunction escapeHtml(str) {\n  const div = document.createElement('div');\n  div.textContent = str;\n  return div.innerHTML;\n}\nelement.innerHTML = escapeHtml(userInput);  // safe — escaped"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Cross-Site Scripting (XSS) — common patterns you'll see in production.\n// APPROACH  - Combine Cross-Site Scripting (XSS) with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - DOMPurify, template literals, attribute injection\n// STRENGTHS - Covers the 80% of XSS prevention\n// WEAKNESSES- No CSP, no Trusted Types\n//\n// 1) DOMPurify — sanitize HTML before inserting\n// import DOMPurify from 'dompurify';\nconst dirty = '<script>alert(\"xss\")</script><p>Hello</p>';\n// const clean = DOMPurify.sanitize(dirty);  // '<p>Hello</p>'\n// element.innerHTML = clean;  // safe — script removed\n// 2) ATTRIBUTE INJECTION — escape in attributes too\nconst userInput = '\" onmouseover=\"alert(1)';\n// BAD: element.innerHTML = '<a href=\"' + userInput + '\">link</a>';\n// GOOD: use setAttribute\nconst link = document.createElement('a');\nlink.setAttribute('href', userInput);  // browser handles escaping\nlink.textContent = 'link';\n// 3) URL INJECTION — validate protocol before redirect\nfunction safeRedirect(url) {\n  const allowed = ['http:', 'https:'];\n  try {\n    const parsed = new URL(url);\n    if (!allowed.includes(parsed.protocol)) return null;\n    return parsed.href;\n  } catch { return null; }\n}\n// safeRedirect('javascript:alert(1)')  -> null (blocked)\n// safeRedirect('https://example.com')  -> 'https://example.com' (allowed)\n// 4) TEMPLATE LITERALS — don't interpolate user input into HTML\nconst name = '<script>alert(1)</script>';\n// BAD: element.innerHTML = `<h1>Hello ${name}</h1>`;\n// GOOD: use textContent or escape\nelement.innerHTML = `<h1>Hello ${escapeHtml(name)}</h1>`;"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Cross-Site Scripting (XSS) — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - CSP, Trusted Types, framework auto-escaping, decision guide\n// STRENGTHS - Production XSS defense in depth\n// WEAKNESSES- N/A\n//\n// 1) CONTENT SECURITY POLICY — server header, prevents inline scripts\n//    Content-Security-Policy: default-src 'self'; script-src 'self' 'nonce-abc123';\n//    This blocks: inline scripts, eval(), remote scripts, data: URIs\n//    Only scripts from same origin with the correct nonce execute\n// 2) TRUSTED TYPES — browser API to enforce safe DOM sinks\n//    Requires CSP: Content-Security-Policy: require-trusted-types-for 'script';\n//    const policy = trustedTypes.createPolicy('my-policy', {\n//      createHTML: (input) => DOMPurify.sanitize(input),\n//    });\n//    element.innerHTML = policy.createHTML(userInput);  // enforced safe\n// 3) FRAMEWORK AUTO-ESCAPING — React, Vue, Angular escape by default\n// React: {userInput} is auto-escaped. dangerouslySetInnerHTML bypasses it.\n// <div>{userInput}</div>           // SAFE — auto-escaped\n// <div dangerouslySetInnerHTML={{__html: userInput}} />  // DANGEROUS\n// Always sanitize before using dangerouslySetInnerHTML:\n// <div dangerouslySetInnerHTML={{__html: DOMPurify.sanitize(userInput)}} />\n// 4) SANITIZE CONFIGURATION — customize allowed tags\n// const clean = DOMPurify.sanitize(dirty, {\n//   ALLOWED_TAGS: ['p', 'b', 'i', 'a', 'ul', 'ol', 'li'],\n//   ALLOWED_ATTR: ['href', 'title'],\n//   ALLOW_DATA_ATTR: false,\n// });\n// 5) STORED XSS — sanitize on the server too, not just client\n//    Never store raw HTML in the database\n//    Sanitize on input, escape on output (defense in depth)\n// 6) CONTENT TYPE — set correct headers to prevent MIME sniffing\n//    X-Content-Type-Options: nosniff\n//    Content-Type: text/html; charset=utf-8\n// Decision rule:\n//   display user input as text                              -> textContent (always safe)\n//   render user-provided HTML                               -> DOMPurify.sanitize() first\n//   set element attributes                                  -> setAttribute (not string concat)\n//   redirect to user-provided URL                           -> validate protocol with URL()\n//   React/Vue/Angular template                              -> auto-escaped by default\n//   must use innerHTML in React                             -> DOMPurify + dangerouslySetInnerHTML\n//   enforce safe DOM manipulation                           -> Trusted Types + CSP\n//   prevent inline scripts                                  -> Content-Security-Policy header\n//   prevent MIME sniffing                                   -> X-Content-Type-Options: nosniff\n//\n// Anti-pattern: using innerHTML with string concatenation of user input;\n//   even if the input looks safe, a crafted payload can break out of the context.\n//   Use textContent or DOMPurify.sanitize()."
                  }
        ],
        tips: [
                  "Use `textContent` instead of `innerHTML` — it renders everything as text, never executes scripts",
                  "DOMPurify is the standard library for sanitizing user-provided HTML before insertion",
                  "Always validate URL protocol before redirect: `new URL(url).protocol` must be http: or https:",
                  "Content-Security-Policy headers are the strongest defense — block inline scripts and eval()"
        ],
        mistake: "Using `innerHTML` with string concatenation of user input. Even if the input looks safe, a crafted payload can break out. Use `textContent` or `DOMPurify.sanitize()`.",
        shorthand: {
          verbose: "// Manual / verbose approach\nelement.innerHTML = userInput;\n// More explicit but longer",
          concise: "element.textContent = userInput;",
        },
      },
      {
        id: "prototype-pollution",
        fn: "Prototype Pollution",
        desc: "Prevent __proto__ and constructor pollution attacks.",
        category: "Injection",
        subtitle: "Never merge user input into objects without checking keys",
        signature: "Object.create(null) | Object.freeze(Object.prototype)",
        descLong: "Prototype pollution occurs when an attacker can modify Object.prototype (or other built-in prototypes) via __proto__, constructor, or prototype keys in user input. This affects all objects in the application. Prevention: use Object.create(null) for dictionaries, validate keys when merging, use Map instead of plain objects for user-controlled keys.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Prototype Pollution — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - Show how __proto__ pollution works and how to block it\n// STRENGTHS - Shows the attack vector clearly\n// WEAKNESSES- No deep merge fix, no Map alternative\n//\n// The attack: user input contains __proto__ key\nconst malicious = JSON.parse('{\"__proto__\": {\"isAdmin\": true}}');\n// BAD: merging into an object pollutes the prototype\nfunction badMerge(target, source) {\n  for (const key in source) {\n    target[key] = source[key];  // target.__proto__.isAdmin = true!\n  }\n}\nconst obj = {};\nbadMerge(obj, malicious);\nconsole.log({}.isAdmin);  // true — ALL objects now have isAdmin!\n// GOOD: block dangerous keys\nfunction safeMerge(target, source) {\n  for (const key in source) {\n    if (key === '__proto__' || key === 'constructor' || key === 'prototype') continue;\n    target[key] = source[key];\n  }\n}"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Prototype Pollution — common patterns you'll see in production.\n// APPROACH  - Combine Prototype Pollution with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - Object.create(null), Map for user keys, deep merge safety\n// STRENGTHS - Covers the 80% of prototype pollution prevention\n// WEAKNESSES- No Object.freeze, no library solutions\n//\n// 1) Object.create(null) — no prototype, immune to pollution\nconst safeDict = Object.create(null);\nsafeDict.__proto__ = 'hacked';  // just a regular property, no pollution\nconsole.log({}.__proto__);      // still the normal Object.prototype\n// 2) MAP for user-controlled keys — no prototype chain at all\nconst userMap = new Map();\nuserMap.set('__proto__', 'value');  // safe — Map keys are not prototype-accessible\n// 3) DEEP MERGE safety — check keys at every level\nfunction safeDeepMerge(target, source) {\n  for (const key of Object.keys(source)) {\n    if (key === '__proto__' || key === 'constructor' || key === 'prototype') continue;\n    if (typeof source[key] === 'object' && source[key] !== null) {\n      target[key] = target[key] ?? {};\n      safeDeepMerge(target[key], source[key]);\n    } else {\n      target[key] = source[key];\n    }\n  }\n  return target;\n}\n// 4) JSON.parse is safe — __proto__ becomes a regular property\nconst parsed = JSON.parse('{\"__proto__\": {\"evil\": true}}');\nconsole.log(parsed.__proto__);  // { evil: true } — own property, not prototype pollution\nconsole.log({}.evil);           // undefined — prototype not affected"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Prototype Pollution — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - Object.freeze, library vulnerabilities, decision guide\n// STRENGTHS - Production defense against prototype pollution\n// WEAKNESSES- N/A\n//\n// 1) FREEZE Object.prototype — prevents modification (dev mode)\n// Object.freeze(Object.prototype);\n// Now: ({}).__proto__.isAdmin = true;  // TypeError in strict mode\n// Note: this can break some libraries that rely on prototype extension\n// 2) KNOWN VULNERABLE PATTERNS — check your dependencies\n//    lodash < 4.17.12: _.merge and _.set were vulnerable\n//    jQuery < 3.4.0: $.extend deep copy was vulnerable\n//    Always update dependencies and check CVEs\n// 3) SAFE MERGE UTILITY — production-grade\nfunction safeMerge(target, ...sources) {\n  for (const source of sources) {\n    if (typeof source !== 'object' || source === null) continue;\n    for (const key of Object.keys(source)) {\n      if (key === '__proto__' || key === 'constructor' || key === 'prototype') continue;\n      const value = source[key];\n      if (typeof value === 'object' && value !== null && !Array.isArray(value)) {\n        target[key] = safeMerge(target[key] ?? Object.create(null), value);\n      } else {\n        target[key] = value;\n      }\n    }\n  }\n  return target;\n}\n// 4) INPUT VALIDATION — reject dangerous keys at the boundary\nfunction sanitizeKeys(obj) {\n  const dangerous = ['__proto__', 'constructor', 'prototype'];\n  const clean = Array.isArray(obj) ? [] : Object.create(null);\n  for (const key of Object.keys(obj)) {\n    if (dangerous.includes(key)) continue;\n    clean[key] = typeof obj[key] === 'object' && obj[key] !== null\n      ? sanitizeKeys(obj[key]) : obj[key];\n  }\n  return clean;\n}\n// 5) USE MODERN LIBRARIES — they handle this\n//    structuredClone() is safe — doesn't copy prototype chain\n//    Object.assign() is safe — doesn't traverse __proto__\n// Decision rule:\n//   dictionary with user-controlled keys                  -> Map or Object.create(null)\n//   merge user input into objects                         -> filter __proto__/constructor/prototype\n//   deep merge/clone                                      -> use safe merge or structuredClone\n//   freeze prototype in dev                               -> Object.freeze(Object.prototype)\n//   parse JSON from user                                  -> JSON.parse is safe (no pollution)\n//   check dependencies                                    -> update lodash, jQuery, etc.\n//   validate at boundary                                  -> sanitizeKeys before processing\n//\n// Anti-pattern: recursive merge without checking for __proto__/constructor/prototype;\n//   a crafted JSON payload can pollute all objects in the application."
                  }
        ],
        tips: [
                  "`JSON.parse()` is safe from prototype pollution — `__proto__` becomes a regular own property",
                  "Use `Map` or `Object.create(null)` for dictionaries with user-controlled keys",
                  "Always filter `__proto__`, `constructor`, and `prototype` keys when merging user input",
                  "Update lodash, jQuery, and other merge utilities — older versions are vulnerable"
        ],
        mistake: "Recursive merge without checking for `__proto__`, `constructor`, or `prototype` keys. A crafted JSON payload can pollute all objects in the application.",
        shorthand: {
          verbose: "function merge(target, source) {\n  for (const key in source) target[key] = source[key];\n}",
          concise: "function merge(target, source) {\n  for (const key of Object.keys(source)) {\n    if (key === '__proto__' || key === 'constructor') continue;\n    target[key] = source[key];\n  }\n}",
        },
      },
    ],
  },

  // ── Section 2: CSRF, CORS & Cookie Security ─────────────────────────────────────────
  {
    id: "csrf-cors",
    title: "CSRF, CORS & Cookie Security",
    entries: [
      {
        id: "csrf",
        fn: "Cross-Site Request Forgery (CSRF)",
        desc: "Prevent forged requests from other sites using tokens and SameSite cookies.",
        category: "Request Security",
        subtitle: "Anti-CSRF tokens, SameSite cookies, and Origin verification",
        signature: "SameSite=Strict | CSRF token | Origin header check",
        descLong: "CSRF tricks a user's browser into making a request to your site while authenticated. Prevention: (1) SameSite cookies (Strict or Lax) prevent cookies from being sent cross-site, (2) anti-CSRF tokens require a value the attacker can't know, (3) verify Origin/Referer headers, (4) use custom headers for state-changing requests (CORS preflight blocks simple cross-site requests with custom headers).",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Cross-Site Request Forgery (CSRF) — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - SameSite cookies and CSRF token basics\n// STRENGTHS - Shows the two primary CSRF defenses\n// WEAKNESSES- No token generation, no Origin check\n//\n// 1) SAMESITE COOKIE — server sets this header\n//    Set-Cookie: session=abc123; SameSite=Strict; Secure; HttpOnly\n//    SameSite=Strict: cookie never sent on cross-site requests\n//    SameSite=Lax: cookie sent on top-level navigation only (default in modern browsers)\n// 2) CSRF TOKEN — include a secret token in forms\n//    Server generates: <input type=\"hidden\" name=\"_csrf\" value=\"randomToken123\">\n//    Server validates: compare submitted token with session-stored token\n//    Attacker can't read the token (same-origin policy blocks cross-site reads)"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Cross-Site Request Forgery (CSRF) — common patterns you'll see in production.\n// APPROACH  - Combine Cross-Site Request Forgery (CSRF) with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - Token generation, double-submit, Origin verification\n// STRENGTHS - Covers the 80% of CSRF prevention\n// WEAKNESSES- No SignedCookies, no SameSite nuances\n//\n// 1) DOUBLE-SUBMIT PATTERN — token in cookie + form, server compares\n//    Set-Cookie: csrfToken=random123; SameSite=Strict\n//    <input type=\"hidden\" name=\"csrfToken\" value=\"random123\">\n//    Server checks: cookie csrfToken === body csrfToken\n// 2) SYNCHRONIZER TOKEN — server stores token per session\nconst crypto = require('crypto');\nfunction generateCSRFToken() {\n  return crypto.randomBytes(32).toString('hex');\n}\n// On login: req.session.csrfToken = generateCSRFToken();\n// In form:  <input type=\"hidden\" name=\"_csrf\" value=\"${csrfToken}\">\n// On submit: if (req.body._csrf !== req.session.csrfToken) reject\n// 3) ORIGIN HEADER CHECK — verify request came from your site\nfunction checkOrigin(req, res, next) {\n  const origin = req.headers.origin || req.headers.referer;\n  if (!origin || !origin.startsWith('https://yourdomain.com')) {\n    return res.status(403).json({ error: 'Invalid origin' });\n  }\n  next();\n}\n// 4) CUSTOM HEADER requirement — CORS preflight blocks simple cross-site\n//    Require X-Requested-With header on all state-changing requests\n//    fetch('/api/delete', { method: 'POST', headers: { 'X-Requested-With': 'XMLHttpRequest' } })\n//    Browsers send a preflight OPTIONS request for custom headers cross-site\n//    Your CORS policy can reject the preflight, blocking the actual request"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Cross-Site Request Forgery (CSRF) — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - Express middleware, SameSite nuances, decision guide\n// STRENGTHS - Production CSRF defense in depth\n// WEAKNESSES- N/A\n//\n// 1) EXPRESS CSRF MIDDLEWARE (using csurf or custom)\nconst crypto = require('crypto');\nclass CSRFProtection {\n  #tokens = new WeakMap();  // session -> token\n  generate(session) {\n    const token = crypto.randomBytes(32).toString('base64url');\n    this.#tokens.set(session, token);\n    return token;\n  }\n  verify(session, token) {\n    const expected = this.#tokens.get(session);\n    if (!expected || !token) return false;\n    // Use timing-safe comparison to prevent timing attacks\n    return crypto.timingSafeEqual(\n      Buffer.from(expected), Buffer.from(token)\n    );\n  }\n}\nconst csrf = new CSRFProtection();\n// Middleware:\nfunction csrfMiddleware(req, res, next) {\n  if (req.method === 'GET') {\n    req.csrfToken = csrf.generate(req.session);\n    return next();\n  }\n  // State-changing methods: verify token\n  const token = req.headers['x-csrf-token'] || req.body._csrf;\n  if (!csrf.verify(req.session, token)) {\n    return res.status(403).json({ error: 'CSRF token mismatch' });\n  }\n  next();\n}\n// 2) SAMESITE NUANCES\n//    SameSite=Strict: most secure, but breaks SSO redirect flows\n//    SameSite=Lax: default, allows top-level GET navigation (links)\n//    SameSite=None; Secure: needed for third-party cookies (iframes, widgets)\n//    For session cookies: use SameSite=Strict\n//    For SSO: use SameSite=Lax\n//    For third-party: use SameSite=None; Secure (requires HTTPS)\n// 3) CORS + CSRF — configure together\n//    CORS controls which origins can READ responses\n//    CSRF controls which origins can SEND requests with cookies\n//    Even with CORS, CSRF is needed — CORS doesn't block simple requests (GET, POST form)\n// 4) TIMING-SAFE COMPARISON — prevent timing attacks on token comparison\n//    NEVER use: token === expected  (string comparison leaks length via timing)\n//    ALWAYS use: crypto.timingSafeEqual(Buffer.from(a), Buffer.from(b))\n// Decision rule:\n//   session cookie                                         -> SameSite=Strict; Secure; HttpOnly\n//   SSO redirect flow                                      -> SameSite=Lax\n//   state-changing requests (POST/PUT/DELETE)              -> CSRF token + verify\n//   API with Authorization header (no cookies)             -> CSRF not needed (no cookies to send)\n//   compare tokens                                         -> crypto.timingSafeEqual\n//   verify request origin                                  -> check Origin/Referer header\n//   require custom header                                  -> X-Requested-With (triggers CORS preflight)\n//\n// Anti-pattern: relying only on CORS for CSRF protection;\n//   CORS doesn't block simple cross-site requests (form POSTs without custom headers).\n//   Use SameSite cookies + CSRF tokens."
                  }
        ],
        tips: [
                  "SameSite=Strict cookies are never sent on cross-site requests — strongest CSRF defense",
                  "Use `crypto.timingSafeEqual()` for token comparison — `===` leaks timing information",
                  "CORS and CSRF are different: CORS controls who can read responses, CSRF controls who can send requests",
                  "APIs using Authorization headers (not cookies) don't need CSRF protection — no cookies to send"
        ],
        mistake: "Relying only on CORS for CSRF protection. CORS doesn't block simple cross-site form POSTs. Use SameSite cookies and CSRF tokens.",
        shorthand: {
          verbose: "// Manual / verbose approach\nfetch('/api/delete', { method: 'POST', credentials: 'include' });\n// More explicit but longer",
          concise: "fetch('/api/delete', {\n  method: 'POST',\n  credentials: 'include',\n  headers: { 'X-CSRF-Token': csrfToken },\n});",
        },
      },
      {
        id: "cors",
        fn: "Cross-Origin Resource Sharing (CORS)",
        desc: "Configure which origins can access your resources from the browser.",
        category: "Request Security",
        subtitle: "Server headers control cross-origin access — configure carefully",
        signature: "Access-Control-Allow-Origin | Access-Control-Allow-Headers",
        descLong: "CORS is a browser mechanism that controls cross-origin requests. The server sends Access-Control-Allow-Origin to permit specific origins. Simple requests (GET, POST with standard content types) go through directly. Preflighted requests (custom headers, PUT/DELETE) trigger an OPTIONS preflight first. Never use Access-Control-Allow-Origin: * with credentials.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Cross-Origin Resource Sharing (CORS) — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - Basic CORS headers on the server\n// STRENGTHS - Shows the minimum CORS configuration\n// WEAKNESSES- No preflight, no credentials\n//\n// Server response headers for simple CORS\n// Access-Control-Allow-Origin: https://example.com\n// Access-Control-Allow-Methods: GET, POST\n// Access-Control-Allow-Headers: Content-Type\n// Express example\n// app.use((req, res, next) => {\n//   res.setHeader('Access-Control-Allow-Origin', 'https://example.com');\n//   res.setHeader('Access-Control-Allow-Methods', 'GET, POST');\n//   res.setHeader('Access-Control-Allow-Headers', 'Content-Type');\n//   next();\n// });"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Cross-Origin Resource Sharing (CORS) — common patterns you'll see in production.\n// APPROACH  - Combine Cross-Origin Resource Sharing (CORS) with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - Preflight handling, credentials, origin whitelist\n// STRENGTHS - Covers the 80% of CORS configuration\n// WEAKNESSES- No dynamic origin, no error handling\n//\n// 1) PREFLIGHT — handle OPTIONS request for non-simple requests\n// app.options('/api/data', (req, res) => {\n//   res.setHeader('Access-Control-Allow-Origin', 'https://example.com');\n//   res.setHeader('Access-Control-Allow-Methods', 'GET, POST, PUT, DELETE');\n//   res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');\n//   res.setHeader('Access-Control-Max-Age', '86400');  // cache preflight for 24h\n//   res.sendStatus(204);\n// });\n// 2) CREDENTIALS — allow cookies with cross-origin requests\n// Server: Access-Control-Allow-Origin: https://example.com (specific, NOT *)\n//         Access-Control-Allow-Credentials: true\n// Client: fetch(url, { credentials: 'include' })\n// 3) ORIGIN WHITELIST — allow specific origins only\nconst allowedOrigins = ['https://example.com', 'https://app.example.com'];\n// app.use((req, res, next) => {\n//   const origin = req.headers.origin;\n//   if (allowedOrigins.includes(origin)) {\n//     res.setHeader('Access-Control-Allow-Origin', origin);\n//     res.setHeader('Vary', 'Origin');  // important for caching\n//   }\n//   next();\n// });\n// 4) PREFLIGHT with credentials\n// app.options('/api/*', (req, res) => {\n//   const origin = req.headers.origin;\n//   if (allowedOrigins.includes(origin)) {\n//     res.setHeader('Access-Control-Allow-Origin', origin);\n//     res.setHeader('Access-Control-Allow-Credentials', 'true');\n//     res.setHeader('Access-Control-Allow-Methods', 'GET, POST, PUT, DELETE, PATCH');\n//     res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');\n//     res.setHeader('Access-Control-Max-Age', '86400');\n//   }\n//   res.sendStatus(204);\n// });"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Cross-Origin Resource Sharing (CORS) — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - CORS middleware, wildcard risks, decision guide\n// STRENGTHS - Production CORS configuration\n// WEAKNESSES- N/A\n//\n// 1) PRODUCTION CORS MIDDLEWARE\nclass CORSMiddleware {\n  #allowedOrigins; #allowCredentials;\n  constructor({ allowedOrigins, allowCredentials = false }) {\n    this.#allowedOrigins = new Set(allowedOrigins);\n    this.#allowCredentials = allowCredentials;\n  }\n  handle(req, res, next) {\n    const origin = req.headers.origin;\n    if (origin && this.#allowedOrigins.has(origin)) {\n      res.setHeader('Access-Control-Allow-Origin', origin);\n      res.setHeader('Vary', 'Origin');\n      if (this.#allowCredentials) {\n        res.setHeader('Access-Control-Allow-Credentials', 'true');\n      }\n    }\n    if (req.method === 'OPTIONS') {\n      res.setHeader('Access-Control-Allow-Methods', 'GET, POST, PUT, DELETE, PATCH');\n      res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization, X-Requested-With');\n      res.setHeader('Access-Control-Max-Age', '86400');\n      return res.sendStatus(204);\n    }\n    next();\n  }\n}\n// app.use(new CORSMiddleware({\n//   allowedOrigins: ['https://example.com', 'https://app.example.com'],\n//   allowCredentials: true,\n// }).handle);\n// 2) WILDCARD RISKS — never combine * with credentials\n//    Access-Control-Allow-Origin: * + Allow-Credentials: true\n//    Browsers reject this combination (for security)\n//    Always specify exact origins when credentials are involved\n// 3) EXPOSE HEADERS — let client read custom response headers\n//    res.setHeader('Access-Control-Expose-Headers', 'X-Total-Count, X-Request-Id');\n//    Without this, client JS can only read \"simple\" headers (Cache-Control, Content-Language, Content-Type, Expires, Last-Modified, Pragma)\n// 4) DYNAMIC ORIGIN — validate against patterns\nfunction isAllowedOrigin(origin, patterns) {\n  return patterns.some(pattern => {\n    if (pattern instanceof RegExp) return pattern.test(origin);\n    return pattern === origin;\n  });\n}\n// Allow all subdomains: isAllowedOrigin(origin, [/^https:\\/\\/[a-z]+\\.example\\.com$/])\n// Decision rule:\n//   public API, no credentials                            -> Access-Control-Allow-Origin: *\n//   API with cookies/auth                                 -> specific origins + Allow-Credentials: true\n//   multiple allowed origins                              -> whitelist with Set, set Vary: Origin\n//   preflight (PUT/DELETE/custom headers)                 -> handle OPTIONS with 204\n//   client needs custom response headers                  -> Access-Control-Expose-Headers\n//   allow subdomains                                      -> regex pattern matching\n//   never                                                 -> * with Allow-Credentials: true\n//\n// Anti-pattern: Access-Control-Allow-Origin: * with credentials;\n//   browsers reject this. Always specify exact origins when Allow-Credentials is true."
                  }
        ],
        tips: [
                  "Never combine `Access-Control-Allow-Origin: *` with `Allow-Credentials: true` — browsers reject it",
                  "Always set `Vary: Origin` when dynamically setting the Allow-Origin header — prevents caching issues",
                  "Preflight OPTIONS requests should return 204 with the CORS headers — no body needed",
                  "Use `Access-Control-Expose-Headers` to let client JS read custom response headers"
        ],
        mistake: "Setting `Access-Control-Allow-Origin: *` with `Allow-Credentials: true`. Browsers reject this combination. Always specify exact origins when credentials are involved.",
        shorthand: {
          verbose: "// Manual / verbose approach\nres.setHeader('Access-Control-Allow-Origin', '*');\n// More explicit but longer",
          concise: "const allowed = new Set(['https://app.com', 'https://web.app']);\nif (allowed.has(req.headers.origin)) {\n  res.setHeader('Access-Control-Allow-Origin', req.headers.origin);\n  res.setHeader('Vary', 'Origin');\n}",
        },
      },
    ],
  },

  // ── Section 3: Crypto, Hashing & Secrets ─────────────────────────────────────────
  {
    id: "crypto-hashing",
    title: "Crypto, Hashing & Secrets",
    entries: [
      {
        id: "password-hashing",
        fn: "Password Hashing",
        desc: "Hash passwords with bcrypt/argon2 — never store plain text or use MD5/SHA.",
        category: "Crypto",
        subtitle: "bcrypt, argon2, scrypt — slow hashes designed for passwords",
        signature: "bcrypt.hash(pw, rounds) | argon2.hash(pw)",
        descLong: "Passwords must be hashed with a slow, salted algorithm — never MD5 or SHA-256 (too fast, vulnerable to brute force). bcrypt (adaptive cost), argon2 (memory-hard, recommended), and scrypt are designed for passwords. Each hash includes a unique salt, preventing rainbow table attacks. Always use a library — never implement crypto yourself.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Password Hashing — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - bcrypt hash and verify — the standard for password storage\n// STRENGTHS - Shows the correct way to hash and verify passwords\n// WEAKNESSES- No argon2, no cost tuning\n//\n// npm install bcrypt\n// const bcrypt = require('bcrypt');\n// Hash a password — salt is auto-generated and embedded in the hash\nconst saltRounds = 12;\n// const hash = await bcrypt.hash('myPassword123', saltRounds);\n// hash -> '$2b$12$N9qo8uLOickgx2ZMRZoMy.MrqK3...'\n// Verify a password against the stored hash\n// const match = await bcrypt.compare('myPassword123', hash);  // true\n// const match2 = await bcrypt.compare('wrongPassword', hash); // false"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Password Hashing — common patterns you'll see in production.\n// APPROACH  - Combine Password Hashing with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - argon2 (recommended), cost tuning, timing-safe compare\n// STRENGTHS - Covers modern password hashing best practices\n// WEAKNESSES- No pepper, no migration\n//\n// 1) ARGON2 — memory-hard, recommended by OWASP\n// npm install argon2\n// const argon2 = require('argon2');\n// const hash = await argon2.hash('myPassword123', {\n//   type: argon2.argon2id,    // recommended variant\n//   memoryCost: 65536,        // 64 MB\n//   timeCost: 3,              // iterations\n//   parallelism: 1,           // threads\n// });\n// const match = await argon2.verify(hash, 'myPassword123');  // true\n// 2) COST TUNING — increase rounds as hardware gets faster\n//    bcrypt: 12 rounds ~250ms (2024). Increase to 14 when hardware improves.\n//    Target: ~250-500ms per hash on your production server\n//    Too fast = vulnerable to brute force. Too slow = DoS.\n// 3) TIMING-SAFE COMPARE — prevent timing attacks\nconst crypto = require('crypto');\nfunction safeCompare(a, b) {\n  const bufA = Buffer.from(a);\n  const bufB = Buffer.from(b);\n  if (bufA.length !== bufB.length) return false;\n  return crypto.timingSafeEqual(bufA, bufB);  // constant-time comparison\n}\n// bcrypt.compare and argon2.verify already use timing-safe comparison internally\n// 4) NEVER use fast hashes for passwords\n//    crypto.createHash('sha256').update(pw).digest('hex')  // BAD — too fast\n//    crypto.createHash('md5').update(pw).digest('hex')     // BAD — broken"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Password Hashing — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - Pepper, migration, rate limiting, decision guide\n// STRENGTHS - Production password security patterns\n// WEAKNESSES- N/A\n//\n// 1) PEPPER — server-side secret added before hashing\n//    Unlike salt (stored with hash), pepper is a server secret\n//    Even if the DB is leaked, passwords can't be cracked without the pepper\nconst pepper = process.env.PEPPER_SECRET;  // from env, not in DB\n// const hash = await bcrypt.hash(password + pepper, 12);\n// const match = await bcrypt.compare(password + pepper, storedHash);\n// 2) MIGRATION — upgrade from bcrypt to argon2 without resetting passwords\nasync function migrateHash(password, oldHash) {\n  // const oldMatch = await bcrypt.compare(password, oldHash);\n  // if (oldMatch) {\n  //   const newHash = await argon2.hash(password, { type: 'argon2id' });\n  //   await updateUserHash(userId, newHash);\n  //   return true;\n  // }\n  // return false;\n}\n// 3) RATE LIMITING — prevent brute force on login\nclass LoginRateLimiter {\n  #attempts = new Map();\n  #maxAttempts = 5;\n  #windowMs = 15 * 60 * 1000;  // 15 minutes\n  check(userId) {\n    const now = Date.now();\n    const record = this.#attempts.get(userId);\n    if (!record || now - record.windowStart > this.#windowMs) {\n      this.#attempts.set(userId, { count: 1, windowStart: now });\n      return true;\n    }\n    if (record.count >= this.#maxAttempts) return false;\n    record.count++;\n    return true;\n  }\n  reset(userId) { this.#attempts.delete(userId); }\n}\n// 4) PASSWORD REQUIREMENTS — validate before hashing\nfunction validatePassword(pw) {\n  if (pw.length < 12) return 'Password must be at least 12 characters';\n  if (!/[A-Z]/.test(pw)) return 'Must include uppercase';\n  if (!/[a-z]/.test(pw)) return 'Must include lowercase';\n  if (!/[0-9]/.test(pw)) return 'Must include a number';\n  if (!/[^A-Za-z0-9]/.test(pw)) return 'Must include a special character';\n  return null;  // valid\n}\n// Decision rule:\n//   hash passwords for storage                           -> bcrypt (12+ rounds) or argon2id\n//   highest security                                     -> argon2id with memory cost\n//   add server-side secret                               -> pepper (env variable)\n//   compare passwords                                    -> bcrypt.compare / argon2.verify (timing-safe)\n//   prevent brute force                                  -> rate limiting on login endpoint\n//   upgrade hash algorithm                               -> migrate on next successful login\n//   validate password strength                           -> length 12+, mixed case, numbers, specials\n//   never                                                -> MD5, SHA-1, SHA-256 for passwords\n//   never                                                -> store plain text or reversible encryption\n//\n// Anti-pattern: using SHA-256 or MD5 for password hashing;\n//   these are fast hashes designed for integrity, not passwords.\n//   Use bcrypt or argon2id which are slow and memory-hard."
                  }
        ],
        tips: [
                  "Use bcrypt (12+ rounds) or argon2id for password hashing — never MD5 or SHA-256",
                  "bcrypt and argon2 auto-generate and embed the salt in the hash — no separate salt storage needed",
                  "Add a pepper (server-side secret from env) for defense in depth — protects against DB-only leaks",
                  "Rate-limit login attempts to prevent brute force — 5 attempts per 15 minutes is common"
        ],
        mistake: "Using SHA-256 or MD5 for password hashing. These are fast hashes designed for data integrity, not passwords. Use bcrypt or argon2id which are slow and memory-hard.",
        shorthand: {
          verbose: "// Manual / verbose approach\nconst hash = crypto.createHash('sha256').update(pw).digest('hex');\n// More explicit but longer",
          concise: "const hash = await bcrypt.hash(pw, 12);",
        },
      },
      {
        id: "secrets-management",
        fn: "Secrets Management",
        desc: "Never hardcode secrets — use env vars, secret managers, and .env files.",
        category: "Crypto",
        subtitle: "Environment variables, secret managers, no client-side secrets",
        signature: "process.env.SECRET | vault.get(key) | .env (gitignored)",
        descLong: "Secrets (API keys, database passwords, JWT signing keys) must never be hardcoded in source code, committed to git, or sent to the client. Use environment variables for development, secret managers (AWS Secrets Manager, HashiCorp Vault) for production. Never expose secrets in frontend JS — anything in the browser is visible to the user.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Basic usage of Secrets Management — understand the core syntax and behavior.\n// APPROACH  - Simple example with minimal parameters; no edge cases.\n// STRENGTHS - Clear, readable; shows the fundamental pattern.\n// WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n// APPROACH  - Environment variables and .env files\n// STRENGTHS - Shows the basic secrets management pattern\n// WEAKNESSES- No secret manager, no rotation\n//\n// .env file (gitignored, never committed)\n// DATABASE_URL=postgresql://user:pass@localhost:5432/db\n// JWT_SECRET=your-very-long-random-secret\n// API_KEY=sk_live_12345\n// In code (Node.js)\nconst dbUrl = process.env.DATABASE_URL;\nconst jwtSecret = process.env.JWT_SECRET;\n// NEVER do this:\n// const apiKey = 'sk_live_12345';  // hardcoded in source — BAD\n// NEVER commit .env to git — add to .gitignore"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Real-world usage of Secrets Management — common patterns you'll see in production.\n// APPROACH  - Combine Secrets Management with related functions; handle common edge cases.\n// STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n// WEAKNESSES- May need optimization for large datasets or complex queries.\n// APPROACH  - Secret managers, client-side rules, validation\n// STRENGTHS - Covers the 80% of secrets management\n// WEAKNESSES- No rotation, no audit\n//\n// 1) SECRET MANAGER — production-grade secret storage\n// AWS Secrets Manager:\n// const { SecretsManagerClient, GetSecretValueCommand } = require('@aws-sdk/client-secrets-manager');\n// const client = new SecretsManagerClient();\n// const response = await client.send(new GetSecretValueCommand({ SecretId: 'prod/db/password' }));\n// const secret = JSON.parse(response.SecretString);\n// 2) CLIENT-SIDE RULE — no secrets in frontend JS\n//    Anything in browser JS is visible via DevTools\n//    BAD:  const API_KEY = 'sk_live_12345';  // in a .js file sent to browser\n//    GOOD: use a backend proxy — frontend calls your API, backend adds the secret\n// 3) VALIDATE required secrets at startup\nfunction requireSecrets() {\n  const required = ['DATABASE_URL', 'JWT_SECRET', 'API_KEY'];\n  const missing = required.filter(key => !process.env[key]);\n  if (missing.length > 0) {\n    throw new Error(`Missing required secrets: ${missing.join(', ')}`);\n  }\n}\n// requireSecrets();  // call at application startup\n// 4) GIT SAFETY — prevent accidental commits\n//    .gitignore: .env, .env.local, .env.*.local\n//    Pre-commit hook: check for common secret patterns\n//    Tools: git-secrets, truffleHog, gitleaks"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Advanced usage of Secrets Management — performance, edge cases, and expert patterns.\n// APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n// STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n// WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n// APPROACH  - Secret rotation, caching, audit, decision guide\n// STRENGTHS - Production secrets management lifecycle\n// WEAKNESSES- N/A\n//\n// 1) SECRET ROTATION — change secrets without downtime\nclass SecretManager {\n  #cache = new Map(); #refreshing = new Map();\n  async get(key) {\n    if (this.#cache.has(key)) {\n      const { value, expiresAt } = this.#cache.get(key);\n      if (Date.now() < expiresAt) return value;\n    }\n    if (this.#refreshing.has(key)) return this.#refreshing.get(key);\n    this.#refreshing.set(key, this.#fetchSecret(key));\n    try {\n      const value = await this.#refreshing.get(key);\n      this.#cache.set(key, { value, expiresAt: Date.now() + 3600000 });  // 1h TTL\n      return value;\n    } finally { this.#refreshing.delete(key); }\n  }\n  async #fetchSecret(key) {\n    // Fetch from AWS Secrets Manager / Vault / etc.\n    // Implementation depends on your secret manager\n  }\n}\n// 2) AUDIT LOGGING — track secret access\nclass AuditedSecretManager {\n  #inner; #log;\n  constructor(inner, logger) { this.#inner = inner; this.#log = logger; }\n  async get(key) {\n    const value = await this.#inner.get(key);\n    this.#log.info({ action: 'secret_access', key, timestamp: new Date().toISOString() });\n    return value;\n  }\n}\n// 3) SHORT-LIVED TOKENS — minimize secret lifetime\n//    Instead of a long-lived API key, use short-lived JWTs (15 min)\n//    Refresh token gets a new access token — limits exposure window\n// 4) SEPARATE SECRETS per environment\n//    dev:  .env file with test credentials\n//    staging: secret manager with staging credentials\n//    prod:  secret manager with production credentials, restricted access\n// 5) NEVER LOG SECRETS — redact in logging\nfunction redactSecrets(obj, secretKeys = ['password', 'token', 'secret', 'apiKey']) {\n  const redacted = { ...obj };\n  for (const key of Object.keys(redacted)) {\n    if (secretKeys.some(s => key.toLowerCase().includes(s))) {\n      redacted[key] = '[REDACTED]';\n    }\n  }\n  return redacted;\n}\n// Decision rule:\n//   development                                           -> .env file (gitignored)\n//   production                                            -> secret manager (AWS, Vault, etc.)\n//   frontend (browser)                                    -> no secrets — use backend proxy\n//   API keys                                              -> server-side only, rotate regularly\n//   database passwords                                    -> secret manager with rotation\n//   JWT signing keys                                      -> env var or secret manager, rotate\n//   third-party service credentials                       -> secret manager\n//   logging                                               -> redact all secret fields\n//   startup                                               -> validate all required secrets exist\n//\n// Anti-pattern: hardcoding secrets in source code;\n//   even if the repo is private, secrets in code are a security incident waiting to happen.\n//   Use environment variables and secret managers."
                  }
        ],
        tips: [
                  "Never hardcode secrets in source code — use environment variables or secret managers",
                  "Never put secrets in frontend JavaScript — anything in the browser is visible to users",
                  "Add `.env` to `.gitignore` and use pre-commit hooks to scan for accidental secret commits",
                  "Validate required secrets at startup — fail fast if any are missing"
        ],
        mistake: "Hardcoding secrets in source code. Even if the repo is private, secrets in code are a security incident. Use environment variables and secret managers.",
        shorthand: {
          verbose: "// Manual / verbose approach\nconst apiKey = 'sk_live_12345';\n// More explicit but longer",
          concise: "const apiKey = process.env.API_KEY;",
        },
      },
    ],
  },
]

export default { meta, sections }

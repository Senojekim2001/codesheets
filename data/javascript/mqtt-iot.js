export const meta = {
  "title": "MQTT & IoT",
  "domain": "javascript",
  "sheet": "mqtt-iot",
  "icon": "📡"
}

export const sections = [

  // ── Section 1: MQTT.js — publish/subscribe messaging for IoT ─────────────────────────────────────────
  {
    id: "mqtt",
    title: "MQTT.js — publish/subscribe messaging for IoT",
    entries: [
      {
        id: "mqtt-basics",
        fn: "MQTT.js — broker connection, topics, QoS, retained messages",
        desc: "MQTT.js is a client library for the MQTT protocol — the standard messaging protocol for IoT. Lightweight publish/subscribe over TCP/TLS/WebSocket. Supports QoS levels, retained messages, and Last Will.",
        category: "IoT Messaging",
        subtitle: "mqtt.connect, client.publish, client.subscribe, QoS 0/1/2, retained, last will, LWT",
        signature: "import mqtt from 'mqtt'; const client = mqtt.connect('mqtt://broker:1883')",
        descLong: "MQTT is a lightweight pub/sub protocol designed for IoT devices with limited bandwidth. Core concepts: Broker (server that routes messages), Topic (hierarchical channel like \"home/sensor/temp\"), Publish (send message to topic), Subscribe (receive messages from topic). QoS levels: 0 (at most once, fire-and-forget), 1 (at least once, acknowledged), 2 (exactly once, handshake). Retained messages: broker stores last message per topic for new subscribers. Last Will and Testament (LWT): broker publishes a message if client disconnects unexpectedly. MQTT.js works in Node.js and browser (via WebSocket). Use MQTT for sensor networks, home automation, and real-time IoT data.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Connect to MQTT broker, subscribe to a topic, publish a message.\n// APPROACH  - mqtt.connect; client.on('message'); client.publish.\n// STRENGTHS - Ultra-lightweight; ideal for constrained devices; pub/sub decoupling.\n// WEAKNESSES- No built-in request/response; broker is a single point of failure.\n//\nimport mqtt from 'mqtt';\n\n// Connect to public test broker\nconst client = mqtt.connect('mqtt://test.mosquitto.org:1883', {\n  clientId: 'js-client-' + Math.random().toString(16).slice(2),\n  clean: true,\n  connectTimeout: 4000,\n});\n\nclient.on('connect', () => {\n  console.log('Connected to broker');\n\n  // Subscribe to sensor data topic\n  client.subscribe('home/sensor/temperature', { qos: 1 }, (err) => {\n    if (!err) console.log('Subscribed to temperature topic');\n  });\n\n  // Publish a message\n  client.publish('home/sensor/temperature', JSON.stringify({\n    value: 22.5,\n    unit: 'C',\n    timestamp: Date.now(),\n  }), { qos: 1 });\n});\n\nclient.on('message', (topic, message) => {\n  console.log(`[${topic}]: ${message.toString()}`);\n  // [home/sensor/temperature]: {\"value\":22.5,\"unit\":\"C\",\"timestamp\":...}\n});\n\nclient.on('error', (err) => {\n  console.error('MQTT error:', err);\n});\n\nclient.on('offline', () => {\n  console.log('Client went offline');\n});\n\n// Graceful shutdown\nprocess.on('SIGINT', () => {\n  client.end();\n  process.exit(0);\n});"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - IoT device simulator: multiple sensors publishing data,\n//             retained messages, Last Will, and topic hierarchy.\n// APPROACH  - Multiple virtual devices; retained messages for state;\n//             wildcard subscriptions; LWT for disconnect detection.\n// STRENGTHS - Realistic IoT simulation; retained state; wildcard topics.\n// WEAKNESSES- No persistence; broker restart loses non-retained messages.\n//\nimport mqtt from 'mqtt';\n\nconst BROKER = 'mqtt://test.mosquitto.org:1883';\n\n// --- Device simulator ---\nclass IoTDevice {\n  constructor(id, type, broker) {\n    this.id = id;\n    this.type = type;\n    this.client = mqtt.connect(broker, {\n      clientId: `device-${type}-${id}`,\n      clean: false,  // persistent session\n      will: {\n        topic: `devices/${type}/${id}/status`,\n        payload: JSON.stringify({ online: false, reason: 'unexpected' }),\n        qos: 1,\n        retain: true,\n      },\n    });\n\n    this.client.on('connect', () => {\n      // Publish online status (retained — new subscribers get it immediately)\n      this.client.publish(\n        `devices/${this.type}/${this.id}/status`,\n        JSON.stringify({ online: true, lastSeen: Date.now() }),\n        { qos: 1, retain: true }\n      );\n      this.startReporting();\n    });\n  }\n\n  startReporting() {\n    // Report sensor data every 5 seconds\n    this.interval = setInterval(() => {\n      const data = this.readSensor();\n      this.client.publish(\n        `devices/${this.type}/${this.id}/data`,\n        JSON.stringify(data),\n        { qos: 1 }\n      );\n    }, 5000);\n  }\n\n  readSensor() {\n    if (this.type === 'temperature') {\n      return { value: 20 + Math.random() * 5, unit: 'C', ts: Date.now() };\n    } else if (this.type === 'humidity') {\n      return { value: 40 + Math.random() * 20, unit: '%', ts: Date.now() };\n    }\n    return { value: Math.random() * 100, ts: Date.now() };\n  }\n\n  disconnect() {\n    clearInterval(this.interval);\n    this.client.publish(\n      `devices/${this.type}/${this.id}/status`,\n      JSON.stringify({ online: false, reason: 'graceful' }),\n      { qos: 1, retain: true }\n    );\n    this.client.end();\n  }\n}\n\n// Create multiple devices\nconst devices = [\n  new IoTDevice(1, 'temperature', BROKER),\n  new IoTDevice(2, 'temperature', BROKER),\n  new IoTDevice(1, 'humidity', BROKER),\n];\n\n// --- Dashboard subscriber ---\nconst dashboard = mqtt.connect(BROKER, { clientId: 'dashboard-' + Date.now() });\n\ndashboard.on('connect', () => {\n  // Wildcard subscriptions\n  dashboard.subscribe('devices/+/+/data', { qos: 1 });     // all sensor data\n  dashboard.subscribe('devices/+/+/status', { qos: 1 });    // all status updates\n\n  // Single-level wildcard: devices/+/temperature/+\n  // Multi-level wildcard: devices/# (all device topics)\n});\n\ndashboard.on('message', (topic, message) => {\n  const parts = topic.split('/');\n  // topic: devices/temperature/1/data\n  const [, type, id, kind] = parts;\n  const data = JSON.parse(message.toString());\n\n  if (kind === 'data') {\n    console.log(`[${type}#${id}] ${data.value}${data.unit || ''}`);\n  } else if (kind === 'status') {\n    console.log(`[${type}#${id}] ${data.online ? 'ONLINE' : 'OFFLINE'}`);\n  }\n});\n\n// Disconnect a device after 30s (triggers LWT)\nsetTimeout(() => {\n  console.log('Disconnecting device 1...');\n  // Force disconnect without graceful message — triggers LWT\n  devices[0].client.end(true);\n}, 30000);"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - MQTT over WebSocket (browser), shared subscriptions,\n//             message queue management, and reconnection strategy.\n// APPROACH  - mqtt.connect with WebSocket URL; shared subscriptions\n//             ($share/group/topic); exponential backoff reconnect.\n// STRENGTHS - Browser-compatible; load-balanced subscriptions; resilient.\n// WEAKNESSES- WebSocket adds overhead; shared subscriptions need MQTT 5.\n//\nimport mqtt from 'mqtt';\n\n// --- Browser MQTT client (WebSocket) ---\n// In browser: const client = mqtt.connect('wss://broker.example.com:8081/mqtt')\n\nclass RobustMQTTClient {\n  constructor(url, options = {}) {\n    this.url = url;\n    this.options = {\n      clientId: options.clientId || 'client-' + Math.random().toString(16).slice(2),\n      clean: options.clean !== false,\n      reconnectPeriod: 0,  // disable auto-reconnect; we handle it\n      connectTimeout: 5000,\n      keepalive: 60,\n      ...options,\n    };\n    this.subscriptions = new Map();  // topic -> handler\n    this.connected = false;\n    this.reconnectAttempts = 0;\n    this.maxReconnectAttempts = 10;\n    this.messageQueue = [];\n    this.maxQueueSize = 1000;\n    this.connect();\n  }\n\n  connect() {\n    this.client = mqtt.connect(this.url, this.options);\n\n    this.client.on('connect', () => {\n      console.log('MQTT connected');\n      this.connected = true;\n      this.reconnectAttempts = 0;\n\n      // Resubscribe to all topics\n      this.subscriptions.forEach((handler, topic) => {\n        this.client.subscribe(topic, { qos: 1 });\n      });\n\n      // Flush message queue\n      this.flushQueue();\n    });\n\n    this.client.on('message', (topic, message) => {\n      const handler = this.subscriptions.get(topic);\n      if (handler) {\n        handler(topic, JSON.parse(message.toString()));\n      }\n    });\n\n    this.client.on('error', (err) => {\n      console.error('MQTT error:', err.message);\n    });\n\n    this.client.on('close', () => {\n      this.connected = false;\n      if (this.reconnectAttempts < this.maxReconnectAttempts) {\n        this.scheduleReconnect();\n      }\n    });\n\n    this.client.on('offline', () => {\n      this.connected = false;\n    });\n  }\n\n  scheduleReconnect() {\n    const delay = Math.min(1000 * 2 ** this.reconnectAttempts, 30000);\n    this.reconnectAttempts++;\n    console.log(`Reconnecting in ${delay}ms (attempt ${this.reconnectAttempts})`);\n    setTimeout(() => this.connect(), delay);\n  }\n\n  subscribe(topic, handler, options = {}) {\n    this.subscriptions.set(topic, handler);\n    if (this.connected) {\n      this.client.subscribe(topic, { qos: options.qos || 1 });\n    }\n  }\n\n  publish(topic, message, options = {}) {\n    const payload = typeof message === 'string' ? message : JSON.stringify(message);\n    if (this.connected) {\n      this.client.publish(topic, payload, { qos: options.qos || 1, retain: options.retain || false });\n    } else {\n      // Queue message for when connection is restored\n      if (this.messageQueue.length < this.maxQueueSize) {\n        this.messageQueue.push({ topic, payload, options });\n      } else {\n        console.warn('Message queue full, dropping message');\n      }\n    }\n  }\n\n  flushQueue() {\n    while (this.messageQueue.length > 0) {\n      const { topic, payload, options } = this.messageQueue.shift();\n      this.client.publish(topic, payload, { qos: options.qos || 1, retain: options.retain || false });\n    }\n  }\n\n  // Shared subscription — multiple clients in same group share messages\n  // Requires MQTT 5 broker\n  sharedSubscribe(group, topic, handler) {\n    const sharedTopic = `$share/${group}/${topic}`;\n    this.subscribe(sharedTopic, handler);\n  }\n\n  disconnect() {\n    this.client.end();\n    this.subscriptions.clear();\n  }\n}\n\n// Usage — IoT dashboard with robust reconnection\nconst client = new RobustMQTTClient('mqtt://test.mosquitto.org:1883', {\n  clientId: 'dashboard-001',\n});\n\n// Subscribe to sensor data\nclient.subscribe('home/+/temperature', (topic, data) => {\n  console.log(`Temperature: ${data.value}C from ${topic}`);\n});\n\n// Shared subscription — load balance across multiple dashboard instances\nclient.sharedSubscribe('dashboards', 'alerts/#', (topic, data) => {\n  console.log(`Alert: ${data.message}`);\n});\n\n// Publish (queues if offline)\nclient.publish('home/commands/thermostat', { setpoint: 21.0 });\n\n// Browser usage:\n// const wsClient = new RobustMQTTClient('wss://broker.example.com:8081/mqtt');"
                  }
        ],
        tips: [
                  "Use retained messages for device status — new subscribers immediately see the last known state without waiting for an update.",
                  "Use wildcard subscriptions: + matches one level, # matches all remaining levels. \"home/+/temp\" matches \"home/living/temp\" but not \"home/living/sensor/temp\".",
                  "Set Last Will in connect options — broker publishes it if the client disconnects unexpectedly, enabling offline detection.",
                  "Use QoS 1 for important messages — guarantees delivery at least once. QoS 0 for high-frequency sensor data where occasional loss is acceptable.",
                  "Use clean: false for persistent sessions — broker stores subscriptions and missed messages while client is offline."
        ],
        mistake: "Using QoS 2 for high-frequency sensor data — QoS 2 requires a 4-step handshake per message, which is overkill. Use QoS 1 (acknowledged) for reliability or QoS 0 (fire-and-forget) for speed.",
        shorthand: {
          verbose: "import mqtt from 'mqtt';\nconst client = mqtt.connect('mqtt://broker:1883');\nclient.on('connect', () => { client.subscribe('topic'); client.publish('topic', 'hello'); });\nclient.on('message', (t, m) => console.log(t, m.toString()));",
          concise: "const c=mqtt.connect('mqtt://broker:1883'); c.on('connect',()=>{c.subscribe('t');c.publish('t','hi')}); c.on('message',(t,m)=>console.log(t,m+''));",
        },
      },
    ],
  },

  // ── Section 2: Johnny-Five & Web Bluetooth — hardware control ─────────────────────────────────────────
  {
    id: "johnny-five",
    title: "Johnny-Five & Web Bluetooth — hardware control",
    entries: [
      {
        id: "johnny-five-basics",
        fn: "Johnny-Five (Arduino) & Web Bluetooth (BLE) — hardware I/O",
        desc: "Johnny-Five is a robotics/IoT framework for Node.js — controls Arduino, Raspberry Pi, and other boards via Firmata. Web Bluetooth API connects browsers to BLE devices. Both handle sensors, LEDs, motors, and servos.",
        category: "Hardware",
        subtitle: "new five.Board, five.Led, five.Sensor, five.Servo, navigator.bluetooth, GATT, characteristic",
        signature: "import { Board, Led } from 'johnny-five'; const board = new Board(); board.on('ready', () => new Led(13).blink(500))",
        descLong: "Johnny-Five wraps the Firmata protocol to control hardware from Node.js. Components: Board (connection to Arduino/PI), Led (digital output), Sensor (analog input), Servo (PWM output), Motor (DC motor control), Button (digital input), Joystick, IMU (accelerometer/gyro). Requires Arduino flashed with StandardFirmata sketch. Web Bluetooth API: navigator.bluetooth.requestDevice (user gesture required), connect to GATT server, read/write characteristics. Use Johnny-Five for Node.js robotics, Web Bluetooth for browser-based BLE device control.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "// === ENTRY-LEVEL EXAMPLE ===\n// TASK      - Johnny-Five: blink LED, read sensor, control servo.\n//             Web Bluetooth: connect to BLE device and read characteristic.\n// APPROACH  - Johnny-Five: Board + Led/Sensor/Servo; Web Bluetooth:\n//             navigator.bluetooth + GATT server.\n// STRENGTHS - JavaScript for hardware; no C/C++ needed; rich component API.\n// WEAKNESSES- Johnny-Five requires physical board + USB; Web Bluetooth\n//             needs HTTPS and user gesture.\n//\n// --- Johnny-Five (Node.js + Arduino) ---\nimport { Board, Led, Sensor, Servo } from 'johnny-five';\n\nconst board = new Board({ port: '/dev/ttyUSB0' });  // or auto-detect\n\nboard.on('ready', () => {\n  // Blink LED on pin 13\n  const led = new Led(13);\n  led.blink(500);\n\n  // Read analog sensor (e.g., potentiometer) on pin A0\n  const sensor = new Sensor('A0');\n  sensor.on('change', () => {\n    console.log('Sensor value:', sensor.value, '/ 1023');\n  });\n\n  // Control servo on pin 9\n  const servo = new Servo(9);\n  servo.sweep();  // sweep 0-180 continuously\n  // servo.to(90);  // move to specific angle\n\n  // Button on pin 2\n  board.pinMode(2, board.MODES.INPUT);\n  board.digitalRead(2, (value) => {\n    console.log('Button:', value ? 'pressed' : 'released');\n  });\n});\n\n// --- Web Bluetooth (Browser) ---\n// async function connectBLE() {\n//   const device = await navigator.bluetooth.requestDevice({\n//     filters: [{ services: ['heart_rate'] }],\n//   });\n//   const server = await device.gatt.connect();\n//   const service = await server.getPrimaryService('heart_rate');\n//   const characteristic = await service.getCharacteristic('heart_rate_measurement');\n//   await characteristic.startNotifications();\n//   characteristic.addEventListener('characteristicvaluechanged', (event) => {\n//     const heartRate = event.target.value.getUint8(1);\n//     console.log('Heart rate:', heartRate, 'bpm');\n//   });\n// }\n// document.getElementById('connect').addEventListener('click', connectBLE);"
                  },
                  {
                            "tier": "junior",
                            "code": "// === JUNIOR EXAMPLE ===\n// TASK      - Johnny-Five: temperature monitoring with alert threshold;\n//             Web Bluetooth: write commands to BLE device (LED control).\n// APPROACH  - Sensor with threshold events; GATT write characteristic.\n// STRENGTHS - Event-driven hardware; bidirectional BLE communication.\n// WEAKNESSES- Hardware-specific; debugging requires physical devices.\n//\nimport { Board, Sensor, Led } from 'johnny-five';\n\nconst board = new Board();\n\nboard.on('ready', () => {\n  // Temperature sensor (TMP36 on A0)\n  const tempSensor = new Sensor({ pin: 'A0', freq: 1000 });\n  const alertLed = new Led(12);\n  const FAN_PIN = 3;\n\n  let lastTemp = 0;\n  tempSensor.on('change', () => {\n    // TMP36: (voltage - 0.5) * 100 = temp in C\n    // voltage = sensor.value / 1023 * 5\n    const voltage = (tempSensor.value / 1023) * 5;\n    const tempC = (voltage - 0.5) * 100;\n    const tempF = tempC * 9/5 + 32;\n\n    if (Math.abs(tempC - lastTemp) > 0.5) {\n      console.log(`Temp: ${tempC.toFixed(1)}C / ${tempF.toFixed(1)}F`);\n      lastTemp = tempC;\n    }\n\n    // Temperature control\n    if (tempC > 30) {\n      alertLed.on();\n      board.analogWrite(FAN_PIN, 255);  // turn fan on full\n      console.log('WARNING: High temperature! Fan ON');\n    } else if (tempC < 25) {\n      alertLed.off();\n      board.analogWrite(FAN_PIN, 0);    // turn fan off\n    }\n  });\n});\n\n// --- Web Bluetooth: Control BLE LED ---\n// async function controlLed(command) {\n//   const device = await navigator.bluetooth.requestDevice({\n//     filters: [{ namePrefix: 'LED-' }],\n//     optionalServices: ['0000ffe0-0000-1000-8000-00805f9b34fb'],\n//   });\n//   const server = await device.gatt.connect();\n//   const service = await server.getPrimaryService('0000ffe0-0000-1000-8000-00805f9b34fb');\n//   const characteristic = await service.getCharacteristic('0000ffe1-0000-1000-8000-00805f9b34fb');\n//\n//   // Write command: 'ON', 'OFF', or color value\n//   const encoder = new TextEncoder();\n//   await characteristic.writeValue(encoder.encode(command));\n//   console.log('Sent:', command);\n// }\n// document.getElementById('ledOn').onclick = () => controlLed('ON');\n// document.getElementById('ledOff').onclick = () => controlLed('OFF');\n// document.getElementById('ledRed').onclick = () => controlLed('255,0,0');"
                  },
                  {
                            "tier": "senior",
                            "code": "// === SENIOR EXAMPLE ===\n// TASK      - Johnny-Five: multi-sensor IoT node with data aggregation;\n//             Web Bluetooth: continuous monitoring with reconnection.\n// APPROACH  - Multiple sensors with throttled publishing; BLE disconnect\n//             detection and auto-reconnect; data batching.\n// STRENGTHS - Production IoT node; resilient BLE; efficient data handling.\n// WEAKNESSES- Complex setup; hardware-specific pin configurations.\n//\nimport { Board, Sensor, Accelerometer, Gyro } from 'johnny-five';\n\nclass IoTNode {\n  constructor(port) {\n    this.board = new Board({ port });\n    this.sensors = {};\n    this.dataBuffer = [];\n    this.publishInterval = 5000;  // batch publish every 5s\n    this.setup();\n  }\n\n  setup() {\n    this.board.on('ready', () => {\n      // Temperature sensor\n      this.sensors.temp = new Sensor({ pin: 'A0', freq: 1000 });\n      // Light sensor\n      this.sensors.light = new Sensor({ pin: 'A1', freq: 1000 });\n      // Accelerometer (I2C)\n      this.sensors.accel = new Accelerometer({ controller: 'MPU6050' });\n      // Gyroscope (I2C)\n      this.sensors.gyro = new Gyro({ controller: 'MPU6050' });\n\n      // Collect data\n      this.sensors.temp.on('change', () => this.collectData('temperature', this.sensors.temp.value));\n      this.sensors.light.on('change', () => this.collectData('light', this.sensors.light.value));\n      this.sensors.accel.on('change', () => this.collectData('accelerometer', {\n        x: this.sensors.accel.x, y: this.sensors.accel.y, z: this.sensors.accel.z,\n      }));\n\n      // Batch publish\n      setInterval(() => this.publishBatch(), this.publishInterval);\n\n      console.log('IoT node ready with sensors:', Object.keys(this.sensors));\n    });\n\n    this.board.on('error', (err) => {\n      console.error('Board error:', err);\n    });\n  }\n\n  collectData(type, value) {\n    this.dataBuffer.push({\n      type,\n      value,\n      timestamp: Date.now(),\n    });\n    // Prevent buffer overflow\n    if (this.dataBuffer.length > 500) {\n      this.dataBuffer.shift();\n    }\n  }\n\n  publishBatch() {\n    if (this.dataBuffer.length === 0) return;\n    const batch = this.dataBuffer.splice(0);\n    console.log(`Publishing ${batch.length} data points`);\n\n    // Here you would publish to MQTT, HTTP, or database\n    // this.mqttClient.publish('iot/node/data', JSON.stringify(batch));\n  }\n}\n\n// Start IoT node\n// const node = new IoTNode('/dev/ttyUSB0');\n\n// --- Web Bluetooth: Continuous monitoring with reconnection ---\nclass BLEMonitor {\n  constructor() {\n    this.device = null;\n    this.server = null;\n    this.characteristic = null;\n    this.reconnectDelay = 1000;\n    this.maxReconnectDelay = 30000;\n    this.connected = false;\n  }\n\n  async connect(serviceUuid, characteristicUuid, onData) {\n    try {\n      this.device = await navigator.bluetooth.requestDevice({\n        filters: [{ services: [serviceUuid] }],\n        optionalServices: [serviceUuid],\n      });\n\n      this.device.addEventListener('gattserverdisconnected', () => {\n        this.connected = false;\n        console.log('BLE disconnected, attempting reconnect...');\n        this.reconnect(serviceUuid, characteristicUuid, onData);\n      });\n\n      this.server = await this.device.gatt.connect();\n      const service = await this.server.getPrimaryService(serviceUuid);\n      this.characteristic = await service.getCharacteristic(characteristicUuid);\n\n      await this.characteristic.startNotifications();\n      this.characteristic.addEventListener('characteristicvaluechanged', (event) => {\n        this.connected = true;\n        this.reconnectDelay = 1000;  // reset on success\n        const data = this.parseData(event.target.value);\n        onData(data);\n      });\n\n      this.connected = true;\n      this.reconnectDelay = 1000;\n      console.log('BLE connected');\n    } catch (err) {\n      console.error('BLE connection failed:', err);\n    }\n  }\n\n  async reconnect(serviceUuid, characteristicUuid, onData) {\n    if (!this.device || !this.device.gatt) return;\n    const delay = this.reconnectDelay;\n    this.reconnectDelay = Math.min(this.reconnectDelay * 2, this.maxReconnectDelay);\n\n    setTimeout(async () => {\n      try {\n        if (this.device.gatt.connected) return;\n        this.server = await this.device.gatt.connect();\n        const service = await this.server.getPrimaryService(serviceUuid);\n        this.characteristic = await service.getCharacteristic(characteristicUuid);\n        await this.characteristic.startNotifications();\n        this.connected = true;\n        this.reconnectDelay = 1000;\n        console.log('BLE reconnected');\n      } catch (err) {\n        console.error('Reconnect failed, retrying...');\n        this.reconnect(serviceUuid, characteristicUuid, onData);\n      }\n    }, delay);\n  }\n\n  parseData(dataView) {\n    // Parse based on your device's data format\n    const result = {};\n    for (let i = 0; i < dataView.byteLength; i++) {\n      result[`byte${i}`] = dataView.getUint8(i);\n    }\n    return result;\n  }\n\n  async write(data) {\n    if (!this.characteristic) throw new Error('Not connected');\n    await this.characteristic.writeValue(data);\n  }\n\n  disconnect() {\n    if (this.device && this.device.gatt) {\n      this.device.gatt.disconnect();\n    }\n  }\n}\n\n// Usage\n// const monitor = new BLEMonitor();\n// document.getElementById('connect').addEventListener('click', async () => {\n//   await monitor.connect(\n//     '0000ffe0-0000-1000-8000-00805f9b34fb',\n//     '0000ffe1-0000-1000-8000-00805f9b34fb',\n//     (data) => console.log('Received:', data)\n//   );\n// });"
                  }
        ],
        tips: [
                  "Flash StandardFirmata onto Arduino before using Johnny-Five — it's the firmware that lets Node.js control the board.",
                  "Web Bluetooth requires a user gesture — navigator.bluetooth.requestDevice() must be called from a click/touch handler.",
                  "Use sensor.freq to throttle readings — sensor.on(\"change\") fires on every reading; 1000ms freq prevents flooding.",
                  "Use board.analogWrite for PWM output (0-255) — controls motor speed, LED brightness, and servo position.",
                  "Web Bluetooth requires HTTPS — the API is only available in secure contexts (localhost or HTTPS)."
        ],
        mistake: "Calling navigator.bluetooth.requestDevice() without a user gesture — the API requires it to be triggered by a click or keypress. Calling it on page load throws \"User cancelled the requestDevice prompt\".",
        shorthand: {
          verbose: "import { Board, Led } from 'johnny-five';\nconst board = new Board();\nboard.on('ready', () => { const led = new Led(13); led.blink(500); });",
          concise: "new (require('johnny-five').Board)().on('ready',()=>new (require('johnny-five').Led)(13).blink(500));",
        },
      },
    ],
  },
]

export default { meta, sections }

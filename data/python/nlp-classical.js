export const meta = {
  "id": "nlp-classical",
  "label": "Classical NLP",
  "icon": "📝",
  "description": "Pre-LLM NLP that still wins on latency, cost, and interpretability: spaCy pipelines (tokenize / POS / lemma / NER / Matcher), sklearn text features (TF-IDF, ngrams, classifiers, topic modeling), and the patterns — cleaning, feature engineering, classical-vs-LLM decision — that ship in production."
}

export const sections = [

  // ── Section 1: spaCy — pipelines, NER, Matcher ─────────────────────────────────────────
  {
    id: "spacy",
    title: "spaCy — pipelines, NER, Matcher",
    entries: [
      {
        id: "spacy-pipeline",
        fn: "spaCy pipeline — load model, tokenize, POS, lemma",
        desc: "Load a model with `spacy.load`; pipe text through; iterate `Doc → Token` for tokens, lemmas, parts-of-speech, dependencies. The default toolkit when you need linguistic features without a transformer.",
        category: "spaCy",
        subtitle: "spacy.load, nlp(text), Doc / Token / Span, token.lemma_ / pos_ / dep_, nlp.pipe for batch, en_core_web_sm/md/lg/trf, disable= for speed",
        signature: "nlp = spacy.load(\"en_core_web_sm\"); doc = nlp(\"text\"); for tok in doc: tok.lemma_, tok.pos_",
        descLong: "spaCy is the production-grade NLP library when you need fast, deterministic linguistic features — tokenization, POS tagging, lemmas, dependency parses — without a transformer. Models come in sizes: `_sm` (small, ~12MB, fast), `_md` (with vectors, ~40MB), `_lg` (larger vectors, ~560MB), `_trf` (transformer-backed, ~440MB, slowest but best accuracy). The pipeline is a sequence of components (`tokenizer → tagger → parser → ner → ...`); disable any you don't need with `disable=` for speed. The three examples solve the SAME concrete task — extract lemmatized content tokens from a document, with POS info — at three depths: minimal `nlp(text)` and iterate → batched `nlp.pipe` + disabled components for speed → production with model versioning, multilingual handling, transformer-vs-CPU decision.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of spaCy pipeline — load model, tokenize, POS, lemma — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# pip install spacy\n# python -m spacy download en_core_web_sm\nimport spacy\nnlp = spacy.load(\"en_core_web_sm\")\ndef content_lemmas(text: str) -> list[str]:\n    doc = nlp(text)\n    return [tok.lemma_.lower() for tok in doc\n            if not tok.is_stop and not tok.is_punct and not tok.is_space]\nprint(content_lemmas(\"The quick brown foxes were jumping over the lazy dogs.\"))\n# ['quick', 'brown', 'fox', 'jump', 'lazy', 'dog']\n# Token attributes that matter:\n#   tok.text         - original surface form\n#   tok.lemma_       - canonical form ('jumping' -> 'jump')\n#   tok.pos_         - coarse POS ('VERB', 'NOUN', 'ADJ', ...)\n#   tok.tag_         - fine POS ('VBG', 'NN', 'JJ', ...)\n#   tok.dep_         - syntactic dep ('nsubj', 'dobj', 'amod', ...)\n#   tok.is_stop      - \"the\", \"a\", \"is\", ...\n#   tok.is_punct     - \".\", \",\", ...\n#   tok.like_num     - numeric token\n#   tok.ent_type_    - named-entity type if part of one ('PERSON', 'GPE', ...)"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of spaCy pipeline — load model, tokenize, POS, lemma — common patterns you'll see in production.\n# APPROACH  - Combine spaCy pipeline — load model, tokenize, POS, lemma with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             for speed; batch via nlp.pipe; preserve sentences.\nimport spacy\n# disable= skips pipeline components we don't need.\n# en_core_web_sm includes: tok2vec, tagger, parser, attribute_ruler, lemmatizer, ner.\n# For lemmas, we need tagger + lemmatizer; can drop parser + ner.\nnlp = spacy.load(\"en_core_web_sm\", disable=[\"parser\", \"ner\"])\ndef content_lemmas_batch(texts: list[str], *, batch_size: int = 64) -> list[list[str]]:\n    \"\"\"Process N texts in batches; ~5-10x faster than one-at-a-time.\"\"\"\n    results = []\n    for doc in nlp.pipe(texts, batch_size=batch_size):\n        lemmas = [tok.lemma_.lower() for tok in doc\n                  if not tok.is_stop and not tok.is_punct and not tok.is_space]\n        results.append(lemmas)\n    return results\n# Per-sentence iteration (for sentence-level tasks).\ndef sentences_with_lemmas(text: str) -> list[tuple[str, list[str]]]:\n    nlp_with_parser = spacy.load(\"en_core_web_sm\", disable=[\"ner\"])\n    doc = nlp_with_parser(text)\n    return [(sent.text, [t.lemma_.lower() for t in sent if not t.is_stop and not t.is_punct])\n            for sent in doc.sents]\n# Sentence segmentation needs the parser (or senter component).\n# Speed comparison (10k short docs on a laptop):\n#   nlp(text) in a loop:                ~25s\n#   nlp.pipe(texts, batch_size=64):     ~3s        (~8x speedup)\n#   nlp.pipe with disable=[...]:        ~1.5s      (~16x speedup)\n#   spacy_with_gpu (transformer):       ~5s        (best accuracy)"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of spaCy pipeline — load model, tokenize, POS, lemma — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             handling, n_process for CPU parallelism.\nimport spacy\nimport multiprocessing\n# Use case                        Model                    Notes\n# ----------------------------------------------------------------\n# Tokenize / POS / lemma           en_core_web_sm           smallest; ~12MB\n# Word vectors / similarity        en_core_web_md           ~40MB; 300d vecs\n# Best non-transformer accuracy    en_core_web_lg           ~560MB\n# State-of-the-art accuracy        en_core_web_trf          ~440MB; needs PyTorch\n# Multilingual                     xx_ent_wiki_sm           80+ langs, NER only\n# Other languages                  fr_core_news_sm, etc.\n_nlp_cache: dict[str, spacy.Language] = {}\ndef get_nlp(model: str = \"en_core_web_sm\",\n            disable: tuple[str, ...] = (\"parser\", \"ner\")) -> spacy.Language:\n    key = f\"{model}|{','.join(disable)}\"\n    if key not in _nlp_cache:\n        _nlp_cache[key] = spacy.load(model, disable=list(disable))\n    return _nlp_cache[key]\ndef lemmatize_corpus(texts: list[str]) -> list[list[str]]:\n    nlp = get_nlp(\"en_core_web_sm\", disable=(\"parser\", \"ner\"))\n    results = []\n    # n_process > 1 forks worker processes; each gets a copy of the model.\n    n = max(1, multiprocessing.cpu_count() - 1)\n    for doc in nlp.pipe(texts, batch_size=64, n_process=n):\n        results.append([t.lemma_.lower() for t in doc\n                        if not t.is_stop and not t.is_punct and not t.is_space])\n    return results\n# pip install spacy[transformers]\n# spacy.require_gpu() OR spacy.prefer_gpu()\n# Then load en_core_web_trf — auto-uses CUDA.\n# Decision rule:\n#   tokenize + POS + lemma                -> en_core_web_sm; disable parser + ner\n#   need dependency parse                  -> include parser; ~2x slower\n#   word similarity / vectors             -> en_core_web_md (300d) or _lg\n#   max accuracy                           -> en_core_web_trf with GPU\n#   millions of docs                       -> nlp.pipe + n_process\n#   small batches                          -> nlp.pipe is enough\n#   non-English                            -> per-language model OR xx_* multilingual\n#   pure tokenization (no NLP)             -> blank model: spacy.blank(\"en\")\n#   regex-only cleaning                    -> NOT spaCy; just re module\n#   serving in a web request                -> warm the model at startup; NEVER load per-request\n#\n# Anti-pattern: spacy.load() inside a request handler. Loading\n# en_core_web_sm takes ~1s; transformer models take 3-5s. Loading\n# per-request makes every request slow AND wastes memory (each\n# replica loads its own copy). Load once at startup; share the\n# loaded model."
                  }
        ],
        tips: [
                  "Pick the smallest model that does the job. `en_core_web_sm` is the right default; reach for `_md` (vectors) or `_trf` (best accuracy) only when you need them.",
                  "`disable=[\"parser\", \"ner\"]` cuts processing time ~2-3× when you don't need those components. The biggest win after batching.",
                  "`nlp.pipe(texts, batch_size=64)` is 5-10× faster than calling `nlp(text)` in a loop. Always batch.",
                  "`n_process=N` spawns N worker processes for `nlp.pipe`. Useful for CPU-bound bulk processing on multi-core machines; not faster on small batches.",
                  "For transformer models (`_trf`), call `spacy.require_gpu()` BEFORE `spacy.load()`. Without GPU, transformer models are 10× slower than `_lg`.",
                  "Load the model ONCE at process startup. Never inside a request handler — model load is 1-5 seconds."
        ],
        mistake: "Calling `spacy.load()` inside a request handler. Each load takes 1-5 seconds; users see request latency tied to model size. Load once at startup, share the cached model across requests.",
        shorthand: {
          verbose: "# Per-request load — every request waits ~1-5s\ndef handle(text):\n    nlp = spacy.load(\"en_core_web_sm\")\n    return [t.lemma_ for t in nlp(text)]",
          concise: "# Module-level load — paid once at startup\nnlp = spacy.load(\"en_core_web_sm\")\ndef handle(text):\n    return [t.lemma_ for t in nlp(text)]",
        },
      },
      {
        id: "spacy-ner",
        fn: "spaCy NER — named entities, rule-based, custom training",
        desc: "Extract entities (PERSON, ORG, GPE, DATE, MONEY, ...) from text. Built-in NER for the common cases; PhraseMatcher / EntityRuler for domain entities; train a custom NER for novel domains.",
        category: "spaCy",
        subtitle: "doc.ents, ent.label_ / ent.text, EntityRuler, PhraseMatcher, custom training via spacy train, Prodigy for annotation, score thresholding",
        signature: "doc = nlp(\"Apple bought Beats in 2014\"); [(ent.text, ent.label_) for ent in doc.ents]",
        descLong: "spaCy's built-in NER recognizes ~18 entity types out of the box (PERSON, ORG, GPE, DATE, MONEY, PRODUCT, EVENT, etc.). For domain-specific entities (drug names, ticker symbols, internal product codes), three options of increasing investment: (a) `EntityRuler` adds rule-based patterns to the pipeline; (b) `PhraseMatcher` matches a list of strings; (c) train a custom NER component with spaCy's training pipeline. The three examples solve the SAME concrete task — extract company mentions from text — at three depths: built-in NER → EntityRuler with custom company list → custom-trained NER component for domain-specific entities.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of spaCy NER — named entities, rule-based, custom training — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport spacy\nnlp = spacy.load(\"en_core_web_sm\")\ndoc = nlp(\"Apple bought Beats Electronics in 2014 for $3 billion. Tim Cook announced the deal.\")\nfor ent in doc.ents:\n    print(f\"{ent.text:25} {ent.label_:10} ({ent.start_char}-{ent.end_char})\")\n# Apple                     ORG        (0-5)\n# Beats Electronics         ORG        (12-29)\n# 2014                      DATE       (33-37)\n# $3 billion                MONEY      (42-52)\n# Tim Cook                  PERSON     (54-62)\n# Common entity labels (en_core_web_sm):\n#   PERSON   - people, including fictional\n#   ORG      - companies, agencies, institutions\n#   GPE      - countries, cities, states\n#   DATE     - absolute or relative dates\n#   MONEY    - monetary values\n#   PRODUCT  - objects, vehicles, foods (not services)\n#   EVENT    - named events\n#   LAW      - named documents made into laws\n#   PERCENT  - percentage values\n# Filter to just orgs:\norgs = [ent.text for ent in doc.ents if ent.label_ == \"ORG\"]"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of spaCy NER — named entities, rule-based, custom training — common patterns you'll see in production.\n# APPROACH  - Combine spaCy NER — named entities, rule-based, custom training with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             (e.g., \"OpenAI\", \"Anthropic\" might miss; ticker symbols\n#             definitely will).\nimport spacy\nfrom spacy.pipeline import EntityRuler\nnlp = spacy.load(\"en_core_web_sm\")\nruler = nlp.add_pipe(\"entity_ruler\", before=\"ner\")\n# Two pattern types:\npatterns = [\n    # 1. Phrase patterns — match exact strings\n    {\"label\": \"ORG\", \"pattern\": \"OpenAI\"},\n    {\"label\": \"ORG\", \"pattern\": \"Anthropic\"},\n    {\"label\": \"ORG\", \"pattern\": \"Hugging Face\"},\n    {\"label\": \"ORG\", \"pattern\": \"DeepMind\"},\n    # 2. Token patterns — match by token attributes\n    {\"label\": \"TICKER\", \"pattern\": [{\"TEXT\": {\"REGEX\": \"^\\\\$[A-Z]{1,5}$\"}}]},  # $AAPL, $MSFT\n    {\"label\": \"TICKER\", \"pattern\": [\n        {\"IS_UPPER\": True, \"LENGTH\": {\"IN\": [3, 4, 5]}},\n        {\"TEXT\": \".\"},\n    ]},                                                # NYSE-style \"AAPL.\"\n]\nruler.add_patterns(patterns)\ndoc = nlp(\"OpenAI raised $6.6B in October 2024. Compare to $MSFT or AAPL.\")\nfor ent in doc.ents:\n    print(f\"{ent.text:15} {ent.label_}\")\n# OpenAI          ORG       <- caught by EntityRuler\n# $6.6B           MONEY     <- statistical NER\n# October 2024    DATE      <- statistical NER\n# $MSFT           TICKER    <- regex pattern\n# AAPL            TICKER    <- token pattern\n# patterns.jsonl:\n#   {\"label\": \"ORG\", \"pattern\": \"OpenAI\"}\n#   {\"label\": \"ORG\", \"pattern\": \"Anthropic\"}\n# ruler.from_disk(\"patterns.jsonl\")\n# When you have 10k+ phrases, EntityRuler is slow; PhraseMatcher\n# uses a Trie internally.\nfrom spacy.matcher import PhraseMatcher\nmatcher = PhraseMatcher(nlp.vocab, attr=\"LOWER\")\ncompanies = [\"OpenAI\", \"Anthropic\", \"Google DeepMind\", \"Hugging Face\"] * 1000\nmatcher.add(\"COMPANY\", [nlp.make_doc(c) for c in companies])\ndef find_companies(text: str) -> list[tuple[str, int, int]]:\n    doc = nlp(text)\n    matches = matcher(doc)\n    return [(doc[start:end].text, start, end) for _, start, end in matches]"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of spaCy NER — named entities, rule-based, custom training — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             (e.g., DRUG, GENE, internal product codes).\nimport spacy\nfrom spacy.tokens import DocBin\nfrom spacy.training import Example\nfrom pathlib import Path\n# Format: list of (text, {\"entities\": [(start, end, label), ...]})\nTRAIN_DATA = [\n    (\"Patient was prescribed acetaminophen 500mg.\",\n     {\"entities\": [(23, 36, \"DRUG\"), (37, 41, \"DOSAGE\")]}),\n    (\"Started ibuprofen 200mg twice daily.\",\n     {\"entities\": [(8, 17, \"DRUG\"), (18, 23, \"DOSAGE\")]}),\n    # ... need 100s for usable accuracy; 1000s for production\n]\ndef make_docbin(data, output: Path) -> None:\n    nlp_blank = spacy.blank(\"en\")\n    db = DocBin()\n    for text, annotations in data:\n        doc = nlp_blank.make_doc(text)\n        ents = []\n        for start, end, label in annotations[\"entities\"]:\n            span = doc.char_span(start, end, label=label, alignment_mode=\"contract\")\n            if span is not None:\n                ents.append(span)\n        doc.ents = ents\n        db.add(doc)\n    db.to_disk(output)\n# make_docbin(TRAIN_DATA, Path(\"./train.spacy\"))\n# make_docbin(DEV_DATA, Path(\"./dev.spacy\"))\n# 1. Generate config:\n#    $ python -m spacy init config config.cfg --lang en --pipeline ner\n# 2. Edit config.cfg if needed (epochs, dropout, etc.)\n# 3. Train:\n#    $ python -m spacy train config.cfg \\\n#        --paths.train ./train.spacy \\\n#        --paths.dev ./dev.spacy \\\n#        --output ./models\n# 4. Best model lands in ./models/model-best/\nnlp_custom = spacy.load(\"./models/model-best\")\ndoc = nlp_custom(\"Patient took acetaminophen 500mg and ibuprofen 200mg.\")\nfor ent in doc.ents:\n    print(f\"{ent.text:20} {ent.label_:10}\")\n# Often you want BOTH custom DRUG/DOSAGE and built-in PERSON/DATE.\n# Two approaches:\n#  (a) Train custom on top of en_core_web_sm (--gpu-id 0 --vectors en_core_web_md)\n#  (b) Run built-in NER first; then add EntityRuler/custom on top\nnlp = spacy.load(\"en_core_web_sm\")\n# Add custom NER as a separate pipeline component:\n# (custom_ner is the trained component from above)\n# nlp.add_pipe(\"ner\", source=nlp_custom, name=\"custom_ner\", after=\"ner\")\n# Decision rule:\n#   built-in entity types (PERSON, ORG, ...)  -> spacy.load + doc.ents\n#   small custom list (10-1000 phrases)        -> EntityRuler with patterns\n#   huge phrase list (10k+)                     -> PhraseMatcher (Trie-based)\n#   novel domain (drugs, genes, codes)         -> custom-trained NER component\n#   high-recall + structured                    -> rule-based first, ML for fuzziness\n#   low-resource language                       -> xx_* multilingual model (NER-only)\n#   tag overlapping spans                        -> Span Categorizer (spancat) instead of NER\n#   confidence per entity                        -> span.score (with parser_max_length config)\n#   active learning / annotation                 -> Prodigy (paid; spaCy's annotation tool)\n#   evaluate accuracy                             -> spacy evaluate model.spacy dev.spacy\n#\n# Anti-pattern: training a custom NER from 10 examples and shipping\n# it. NER models need 100s of examples per entity type to be reliable;\n# 10 examples produces a model that overfits to those exact strings.\n# Either: (a) use rule-based EntityRuler if your entities are\n# exhaustive, OR (b) annotate 100+ examples per type before training."
                  }
        ],
        tips: [
                  "Built-in NER recognizes ~18 entity types out of the box. Check `spacy.explain(\"ORG\")` for documentation on each label.",
                  "For small custom phrase lists, `EntityRuler` (`nlp.add_pipe(\"entity_ruler\", before=\"ner\")`) is the right tool — patterns + statistical NER combined.",
                  "For huge phrase lists (10k+), `PhraseMatcher` is much faster than EntityRuler — uses a Trie internally.",
                  "Custom NER training needs 100s of examples per entity type minimum. Below that, you're overfitting.",
                  "Use `alignment_mode=\"contract\"` when converting char-span annotations — handles tokenization mismatches gracefully (rounds to nearest token boundary).",
                  "`Span Categorizer` (`spancat`) handles overlapping spans (e.g., \"United States\" can be both ORG and GPE); standard NER doesn't support overlap."
        ],
        mistake: "Training a custom NER from 10 examples and shipping it. Models need 100s of examples per entity type to be reliable; 10 examples produces a model that overfits to those exact strings and fails on slight variations. Use rule-based `EntityRuler` until you have 100+ examples per type.",
        shorthand: {
          verbose: "// Manual / verbose approach\n# Train custom NER on 10 examples → overfits, breaks in prod\nTRAIN = [(text, {\"entities\": [...]}) for text in ten_examples]\n// More explicit but longer",
          concise: "# Use EntityRuler until you have 100+ annotations per type\nruler = nlp.add_pipe(\"entity_ruler\", before=\"ner\")\nruler.add_patterns([{\"label\": \"DRUG\", \"pattern\": \"acetaminophen\"}, ...])",
        },
      },
      {
        id: "spacy-matcher-patterns",
        fn: "spaCy Matcher — token + dependency pattern matching",
        desc: "Match linguistic patterns: token-level (`Matcher`) for \"verb followed by noun\"; syntactic (`DependencyMatcher`) for \"subject of verb is a person\". The right tool for rule-based extraction from parsed text.",
        category: "spaCy",
        subtitle: "Matcher (token patterns), DependencyMatcher (syntactic), span groups, regex in patterns, OP=? / + / *, custom callbacks",
        signature: "matcher = Matcher(nlp.vocab); matcher.add(\"VERB_NOUN\", [[{\"POS\": \"VERB\"}, {\"POS\": \"NOUN\"}]])",
        descLong: "`Matcher` matches token-level patterns: \"find ADJECTIVE NOUN\", \"find a verb followed by 2+ adjectives\", \"find tokens matching this regex\". `DependencyMatcher` matches syntactic structure: \"find subject of any verb where subject is a PERSON\". These are deterministic — same input always produces the same output, useful when you need explainable extraction. The three examples solve the SAME concrete task — extract \"Person VERB Object\" relations from sentences (\"Apple acquired Beats\", \"Microsoft bought GitHub\") — at three depths: simple Matcher token pattern → quantifiers + spans → DependencyMatcher for syntactic relations.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of spaCy Matcher — token + dependency pattern matching — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport spacy\nfrom spacy.matcher import Matcher\nnlp = spacy.load(\"en_core_web_sm\")\nmatcher = Matcher(nlp.vocab)\n# Pattern: ORG/PERSON entity followed by an acquisition verb.\npattern = [\n    {\"ENT_TYPE\": {\"IN\": [\"ORG\", \"PERSON\"]}},\n    {\"LEMMA\": {\"IN\": [\"acquire\", \"buy\", \"purchase\"]}},\n]\nmatcher.add(\"ACQUISITION\", [pattern])\ndoc = nlp(\"Apple acquired Beats. Microsoft bought GitHub. Larry Page sold his shares.\")\nfor match_id, start, end in matcher(doc):\n    span = doc[start:end]\n    print(f\"Match: '{span.text}'\")\n# Match: 'Apple acquired'\n# Match: 'Microsoft bought'\n# Token attributes you can match on:\n#   TEXT, LOWER, LEMMA  - the surface / lowercase / lemma\n#   POS, TAG, DEP        - linguistic features\n#   ENT_TYPE             - entity label (after NER runs)\n#   IS_ALPHA, IS_DIGIT, IS_PUNCT, LIKE_NUM\n#   LENGTH, SHAPE        - \"AAA999\" pattern\n#   REGEX                - custom regex match"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of spaCy Matcher — token + dependency pattern matching — common patterns you'll see in production.\n# APPROACH  - Combine spaCy Matcher — token + dependency pattern matching with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             multi-word organizations, capture full ORG span.\nimport spacy\nfrom spacy.matcher import Matcher\nnlp = spacy.load(\"en_core_web_sm\")\nmatcher = Matcher(nlp.vocab)\n# Pattern: ORG (one OR more tokens) + optional adjective + acquire-verb + ORG\n# Quantifiers:\n#   \"?\"  - 0 or 1\n#   \"*\"  - 0 or more\n#   \"+\"  - 1 or more\n#   \"!\"  - never\npattern = [\n    {\"ENT_TYPE\": \"ORG\", \"OP\": \"+\"},                   # 1+ ORG tokens\n    {\"POS\": \"ADV\", \"OP\": \"?\"},                          # optional adverb (e.g., \"successfully\")\n    {\"LEMMA\": {\"IN\": [\"acquire\", \"buy\", \"purchase\"]}},\n    {\"POS\": \"DET\", \"OP\": \"?\"},                          # optional 'the'\n    {\"ENT_TYPE\": \"ORG\", \"OP\": \"+\"},                   # 1+ ORG tokens\n]\nmatcher.add(\"ACQUISITION\", [pattern])\ntext = \"Apple Inc. successfully acquired Beats Electronics. Microsoft bought the GitHub platform.\"\ndoc = nlp(text)\nfor match_id, start, end in matcher(doc):\n    span = doc[start:end]\n    # Find the boundary between subject and object.\n    verb_idx = next(i for i, tok in enumerate(span) if tok.lemma_ in (\"acquire\", \"buy\", \"purchase\"))\n    subj = span[:verb_idx]\n    obj  = span[verb_idx+1:]\n    print(f\"  {subj.text}  ->  {obj.text}\")\n# Apple Inc.  ->  Beats Electronics\n# Microsoft   ->  the GitHub platform\nfrom spacy.tokens import Doc\n# Multiple matchers can share span groups via doc.spans[\"my_group\"]\ndoc.spans[\"acquisitions\"] = [doc[start:end] for _, start, end in matcher(doc)]\nprint(doc.spans[\"acquisitions\"])"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of spaCy Matcher — token + dependency pattern matching — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             relations (subject of VERB is ORG; object of VERB is ORG)\n#             which is robust to word order and intervening modifiers.\nimport spacy\nfrom spacy.matcher import DependencyMatcher\nnlp = spacy.load(\"en_core_web_sm\")\ndep_matcher = DependencyMatcher(nlp.vocab)\n# Pattern: a verb whose nsubj is an ORG and whose dobj is an ORG.\npattern = [\n    # Anchor: an acquisition verb.\n    {\"RIGHT_ID\": \"verb\",\n     \"RIGHT_ATTRS\": {\"LEMMA\": {\"IN\": [\"acquire\", \"buy\", \"purchase\"]}, \"POS\": \"VERB\"}},\n    # Subject of that verb (nsubj) — must be ORG.\n    {\"LEFT_ID\": \"verb\",\n     \"REL_OP\": \">\",                                    # immediate child\n     \"RIGHT_ID\": \"subject\",\n     \"RIGHT_ATTRS\": {\"DEP\": \"nsubj\", \"ENT_TYPE\": \"ORG\"}},\n    # Object of that verb (dobj) — must be ORG.\n    {\"LEFT_ID\": \"verb\",\n     \"REL_OP\": \">\",\n     \"RIGHT_ID\": \"object\",\n     \"RIGHT_ATTRS\": {\"DEP\": \"dobj\", \"ENT_TYPE\": \"ORG\"}},\n]\ndep_matcher.add(\"ACQUISITION_REL\", [pattern])\ndoc = nlp(\"In 2014, Apple successfully acquired Beats Electronics for $3 billion.\")\nfor match_id, token_ids in dep_matcher(doc):\n    # token_ids gives indices in the order matching the pattern (verb, subject, object).\n    verb, subj, obj = (doc[i] for i in token_ids)\n    # Expand to full noun phrase for subject/object.\n    subj_span = doc[subj.left_edge.i:subj.right_edge.i + 1]\n    obj_span  = doc[obj.left_edge.i:obj.right_edge.i + 1]\n    print(f\"  {subj_span.text}  --[{verb.lemma_}]-->  {obj_span.text}\")\n# Apple  --[acquire]-->  Beats Electronics\n#   \"<\"   - immediate parent (head)\n#   \">\"   - immediate child\n#   \"<<\"  - any ancestor\n#   \">>\"  - any descendant\n#   \".\"   - immediately following (left to right in surface order)\n#   \".*\"  - any token following\n#   \";\"   - immediately preceding\n#   \";*\"  - any token preceding\n# Decision rule:\n#   match by surface tokens               -> Matcher; fast, simple\n#   match across word order / modifiers   -> DependencyMatcher; needs parser\n#   match overlapping spans                -> Span Categorizer (spancat)\n#   regex on text                          -> {\"TEXT\": {\"REGEX\": ...}} in pattern\n#   POS / lemma                            -> {\"POS\": \"VERB\"} / {\"LEMMA\": \"buy\"}\n#   entity type                            -> {\"ENT_TYPE\": \"PERSON\"}\n#   optional / repeating tokens            -> \"OP\" : \"?\" / \"+\" / \"*\"\n#   case-insensitive                       -> {\"LOWER\": \"...\"}\n#   token shape (e.g., \"AAA999\")           -> {\"SHAPE\": \"Xxxx9999\"}\n#   complex multi-pattern logic            -> multiple matcher.add() calls;\n#                                                callback for post-filter\n#   spans across sentence boundary         -> not supported in Matcher; iterate doc.sents\n#   needs subject-verb-object              -> DependencyMatcher (use parser)\n#\n# Anti-pattern: writing complex Matcher patterns when you really need\n# DependencyMatcher. Token patterns can't express \"the SUBJECT of\n# this verb\" — they only express linear sequences. A passive\n# construction (\"Beats was acquired by Apple\") flips subject and\n# object in linear order; DependencyMatcher with REL_OP=\">\" finds\n# them by syntactic role regardless of position."
                  }
        ],
        tips: [
                  "`Matcher` for linear token patterns; `DependencyMatcher` for syntactic relations. The latter handles passive voice (\"X was acquired by Y\") where linear order flips.",
                  "Token patterns support quantifiers (`OP: \"?\"`, `\"+\"`, `\"*\"`) — same as regex but on tokens.",
                  "Match on `LEMMA` not `TEXT` when you want morphological variants (\"buys\", \"bought\", \"buying\" all match `LEMMA: \"buy\"`).",
                  "For huge phrase lists (>1000), use `PhraseMatcher` (Trie-based, much faster than `Matcher` patterns).",
                  "For overlapping spans (one token in multiple matches), assign to `doc.spans[\"my_group\"]` — `doc.ents` doesn't allow overlap.",
                  "Use `spacy.displacy.serve(doc, style=\"dep\")` to visualize the dependency parse — invaluable when writing DependencyMatcher patterns."
        ],
        mistake: "Writing complex `Matcher` patterns when you really need `DependencyMatcher`. Token patterns are linear — they can't match \"subject of verb\" across modifiers (\"Apple, the tech giant, acquired Beats\"). Passive voice (\"Beats was acquired by Apple\") flips order entirely. Use DependencyMatcher when relations matter.",
        shorthand: {
          verbose: "# Linear pattern fails on passive voice + modifiers\npattern = [{\"ENT_TYPE\": \"ORG\"}, {\"LEMMA\": \"acquire\"}, {\"ENT_TYPE\": \"ORG\"}]\n# \"Beats was acquired by Apple\" -> miss\n# \"Apple, the tech giant, acquired Beats\" -> miss",
          concise: "# DependencyMatcher matches syntactic role\n[{\"RIGHT_ID\": \"v\", \"RIGHT_ATTRS\": {\"LEMMA\": \"acquire\"}},\n {\"LEFT_ID\": \"v\", \"REL_OP\": \">\", \"RIGHT_ID\": \"subj\", \"RIGHT_ATTRS\": {\"DEP\": \"nsubj\"}},\n {\"LEFT_ID\": \"v\", \"REL_OP\": \">\", \"RIGHT_ID\": \"obj\",  \"RIGHT_ATTRS\": {\"DEP\": \"dobj\"}}]\n# Works for passive + modifiers.",
        },
      },
    ],
  },

  // ── Section 2: sklearn-text — TF-IDF, classification, topic modeling ─────────────────────────────────────────
  {
    id: "sklearn-text",
    title: "sklearn-text — TF-IDF, classification, topic modeling",
    entries: [
      {
        id: "tfidf-vectorizer",
        fn: "TfidfVectorizer — text → numeric features",
        desc: "Convert documents to TF-IDF feature vectors. The default text-feature extractor for classical ML; still strong baseline for classification, retrieval, and clustering.",
        category: "sklearn-text",
        subtitle: "TfidfVectorizer, ngram_range, min_df / max_df, sublinear_tf, stop_words, max_features, HashingVectorizer for streaming, sparse matrices",
        signature: "vec = TfidfVectorizer(ngram_range=(1, 2), min_df=2, max_features=50_000)\nX = vec.fit_transform(docs)",
        descLong: "TF-IDF (term-frequency × inverse-document-frequency) weighs each word by how distinctive it is to a document — common words get low weight, document-specific words high. `TfidfVectorizer` is the workhorse for converting text to ML features. Knobs: `ngram_range` (unigrams alone vs unigrams+bigrams), `min_df`/`max_df` (drop rare and ubiquitous terms), `sublinear_tf=True` (log-scale frequencies; usually wins), `max_features` (cap vocabulary). The output is a sparse matrix — works with sklearn classifiers directly. The three examples solve the SAME concrete task — build TF-IDF features for a small classification dataset — at three depths: defaults → tuned ngrams + min_df → HashingVectorizer for streaming + sparse-friendly downstream.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of TfidfVectorizer — text → numeric features — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nfrom sklearn.feature_extraction.text import TfidfVectorizer\ndocs = [\n    \"The cat sat on the mat\",\n    \"The dog sat on the log\",\n    \"Cats and dogs are pets\",\n]\nvec = TfidfVectorizer()\nX = vec.fit_transform(docs)                            # sparse matrix\nprint(X.shape)                                          # (3, 9) — 9 unique terms\nprint(vec.get_feature_names_out())\n# ['and' 'are' 'cat' 'cats' 'dog' 'dogs' 'log' 'mat' 'pets' 'sat' 'the']\nprint(X.toarray().round(3))                            # dense for inspection\n# Each row is a doc; each column a term; values are TF-IDF weights."
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of TfidfVectorizer — text → numeric features — common patterns you'll see in production.\n# APPROACH  - Combine TfidfVectorizer — text → numeric features with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nfrom sklearn.feature_extraction.text import TfidfVectorizer\nvec = TfidfVectorizer(\n    lowercase=True,\n    strip_accents=\"unicode\",\n    ngram_range=(1, 2),                                # unigrams + bigrams; usually wins\n    min_df=2,                                          # drop terms in <2 docs (typos, OOV)\n    max_df=0.95,                                       # drop terms in >95% of docs (\"the\", domain-common)\n    max_features=50_000,                                # cap vocabulary size\n    sublinear_tf=True,                                  # log(1+tf); usually a small win\n    norm=\"l2\",                                          # L2-normalize each row (default; for cosine sim)\n    stop_words=\"english\",                              # built-in; or your own list; or None\n    token_pattern=r\"\\b[a-zA-Z][a-zA-Z]+\\b\",         # 2+ letter alpha words\n)\n# Fit on training set; transform both:\nX_train = vec.fit_transform(train_docs)               # learns vocabulary\nX_test  = vec.transform(test_docs)                    # uses SAME vocabulary; OOV terms ignored\nprint(f\"Vocab size: {len(vec.vocabulary_)}\")\nprint(f\"Train shape: {X_train.shape}\")\nprint(f\"Top 10 most common features: {sorted(vec.vocabulary_.items(), key=lambda kv: kv[1])[:10]}\")\n# Get all terms BEFORE filtering:\n# raw_vec = TfidfVectorizer(min_df=1, max_df=1.0)\n# raw_vec.fit(train_docs)\n# dropped = set(raw_vec.vocabulary_) - set(vec.vocabulary_)"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of TfidfVectorizer — text → numeric features — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             optional TfidfTransformer; sparse-aware downstream.\nfrom sklearn.feature_extraction.text import HashingVectorizer, TfidfTransformer\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.linear_model import SGDClassifier\nimport scipy.sparse as sp\n# Use case: too much data to fit in memory at once; or online learning.\nhv = HashingVectorizer(\n    n_features=2**18,                                   # 262k feature space\n    alternate_sign=False,                               # all positive (matches TF-IDF semantics)\n    ngram_range=(1, 2),\n    norm=None,                                          # we'll let TfidfTransformer normalize\n)\npipe = Pipeline([\n    (\"hash\", hv),\n    (\"tfidf\", TfidfTransformer(sublinear_tf=True)),\n    (\"clf\", SGDClassifier(loss=\"log_loss\", alpha=1e-5, max_iter=5)),\n])\n# Streaming partial_fit (when data > memory):\ndef stream_train(file_paths, labels):\n    \"\"\"Train incrementally over batches without loading everything.\"\"\"\n    classes = list(set(labels))                        # known up-front\n    for batch_paths, batch_y in batches(file_paths, labels, size=10_000):\n        batch_texts = [open(p).read() for p in batch_paths]\n        # Hash + idf-transform NOT compatible with partial_fit (idf needs full corpus).\n        # For true streaming, drop TfidfTransformer; use HashingVectorizer only.\n        X_batch = hv.transform(batch_texts)\n        pipe.named_steps[\"clf\"].partial_fit(X_batch, batch_y, classes=classes)\ndef batches(items, labels, size): ...\nfrom sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.pipeline import make_pipeline\np = make_pipeline(TfidfVectorizer(ngram_range=(1, 2), min_df=2),\n                   LogisticRegression(C=1.0, max_iter=1000))\n# p.fit(X_train, y_train)\n# Get most predictive features per class:\n# vec, clf = p.named_steps[\"tfidfvectorizer\"], p.named_steps[\"logisticregression\"]\n# feature_names = vec.get_feature_names_out()\n# for class_idx in range(len(clf.classes_)):\n#     top_idx = clf.coef_[class_idx].argsort()[-20:][::-1]\n#     print(f\"Class {clf.classes_[class_idx]}: {feature_names[top_idx]}\")\n# Most sklearn linear models (LogisticRegression, SGDClassifier, LinearSVC)\n# accept sparse matrices natively. AVOID:\n#   X_dense = X_sparse.toarray()                        # OOM for large corpora\n# Tree-based models (RandomForest, GradientBoosting) silently densify;\n# they're often the wrong choice for high-dim TF-IDF features.\n# Decision rule:\n#   small dataset, bounded vocab          -> TfidfVectorizer; defaults are fine\n#   need bigrams                            -> ngram_range=(1, 2)\n#   noise / typos in vocab                  -> min_df=2 or higher\n#   common words dominating                 -> max_df=0.95; or stop_words=\"english\"\n#   need to limit memory                    -> max_features=50_000\n#   data > memory / online learning         -> HashingVectorizer + SGDClassifier\n#   character n-grams                        -> analyzer=\"char_wb\", ngram_range=(3, 5)\n#   multilingual                            -> char n-grams; or per-language vectorizer\n#   need to inspect features                 -> TfidfVectorizer (HashingVectorizer is opaque)\n#   compare with embeddings                  -> sentence-transformers (different pattern)\n#   exact match retrieval                   -> BM25 (rank_bm25) usually beats TF-IDF cosine\n#   classification baseline                  -> TF-IDF + LogisticRegression; surprisingly hard to beat\n#\n# Anti-pattern: calling fit_transform on the test set (or fitting a\n# new vectorizer for each split). The vocabulary diverges; OOV\n# behavior is broken; cross-validation lies. ALWAYS fit on train,\n# transform on test. Wrap in a sklearn Pipeline so this is enforced."
                  }
        ],
        tips: [
                  "`ngram_range=(1, 2)` (unigrams + bigrams) usually wins over unigrams alone for classification. Larger ranges quickly explode vocabulary; rarely worth it without `min_df`.",
                  "`min_df=2` drops terms appearing in <2 docs — eliminates typos and one-off OOV. `min_df=5` for larger corpora.",
                  "`sublinear_tf=True` applies log(1+tf) — small win on most tasks; almost never hurts.",
                  "For huge corpora (>1M docs), use `HashingVectorizer` — no fit() needed; constant memory; supports `partial_fit` for online learning.",
                  "For multilingual text, character n-grams (`analyzer=\"char_wb\"`, `ngram_range=(3, 5)`) generalize across word forms and languages.",
                  "TF-IDF + LogisticRegression is a surprisingly hard-to-beat baseline. Try this BEFORE transformers — it's 100× faster to train and serve."
        ],
        mistake: "Calling `fit_transform` on test data (or fitting separate vectorizers for train/test). The vocabulary diverges; OOV terms in test get silently dropped; metrics are wrong. ALWAYS `fit` on train and `transform` on test — wrap in a sklearn `Pipeline` so this is enforced.",
        shorthand: {
          verbose: "# Separate fit per split — broken vocabulary + leakage\nX_train = TfidfVectorizer().fit_transform(train_docs)\nX_test  = TfidfVectorizer().fit_transform(test_docs)",
          concise: "# Fit on train, transform on test\nvec = TfidfVectorizer().fit(train_docs)\nX_train = vec.transform(train_docs)\nX_test  = vec.transform(test_docs)",
        },
      },
      {
        id: "text-classification",
        fn: "Text classification — Pipeline, baseline, hyperparameter tuning",
        desc: "Build a text classifier: TF-IDF + LogisticRegression / LinearSVC in a sklearn `Pipeline`; tune via `GridSearchCV`; handle class imbalance with `class_weight=\"balanced\"` or resampling.",
        category: "sklearn-text",
        subtitle: "Pipeline(TfidfVectorizer, LogisticRegression), GridSearchCV, class_weight, classification_report, calibration, when to consider transformers",
        signature: "pipe = Pipeline([(\"tfidf\", TfidfVectorizer()), (\"clf\", LogisticRegression())]); pipe.fit(X, y)",
        descLong: "For text classification (spam/ham, topic classification, sentiment, intent), the classical-ML baseline is hard to beat: TF-IDF features + linear classifier. `LogisticRegression` and `LinearSVC` are the standard picks; both handle high-dim sparse features natively. Wrap in a `Pipeline` so cross-validation properly fits the vectorizer per fold. The three examples solve the SAME concrete task — train a sentiment classifier (positive/negative) on review data and evaluate — at three depths: minimal Pipeline + LogisticRegression → hyperparameter tuning + calibration + class-imbalance handling → production with feature inspection, threshold tuning, comparison with transformer baseline.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Text classification — Pipeline, baseline, hyperparameter tuning — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nfrom sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.model_selection import train_test_split\nfrom sklearn.metrics import classification_report\n# Pretend dataset:\ndocs = [\"I loved this movie\", \"Terrible film, waste of time\", \"Amazing experience\",\n        \"Boring and predictable\", \"Highly recommended\", \"Awful, would not watch again\"]\nlabels = [\"pos\", \"neg\", \"pos\", \"neg\", \"pos\", \"neg\"]\nX_train, X_test, y_train, y_test = train_test_split(docs, labels, test_size=0.3, random_state=42)\npipe = Pipeline([\n    (\"tfidf\", TfidfVectorizer(ngram_range=(1, 2), min_df=1)),\n    (\"clf\",   LogisticRegression(max_iter=1000)),\n])\npipe.fit(X_train, y_train)\ny_pred = pipe.predict(X_test)\nprint(classification_report(y_test, y_pred))\n# Predict on new text:\nprint(pipe.predict([\"Best movie ever!\"]))             # ['pos']\nprint(pipe.predict_proba([\"Best movie ever!\"]))       # array of probabilities"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Text classification — Pipeline, baseline, hyperparameter tuning — common patterns you'll see in production.\n# APPROACH  - Combine Text classification — Pipeline, baseline, hyperparameter tuning with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             search; handle class imbalance.\nfrom sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.model_selection import GridSearchCV, train_test_split\nfrom sklearn.metrics import classification_report\npipe = Pipeline([\n    (\"tfidf\", TfidfVectorizer()),\n    (\"clf\",   LogisticRegression(max_iter=2000, class_weight=\"balanced\")),\n    # class_weight=\"balanced\" weighs minority class higher → useful when\n    # one class is ~10x more common than another.\n])\n# Grid: try multiple ngram ranges, min_df, regularization strength.\nparam_grid = {\n    \"tfidf__ngram_range\": [(1, 1), (1, 2)],\n    \"tfidf__min_df\": [1, 2, 5],\n    \"tfidf__max_df\": [0.95, 1.0],\n    \"tfidf__sublinear_tf\": [True, False],\n    \"clf__C\": [0.1, 1.0, 10.0],\n    \"clf__solver\": [\"liblinear\", \"lbfgs\"],\n}\n# F1-macro is robust to class imbalance.\ngrid = GridSearchCV(pipe, param_grid, cv=5, scoring=\"f1_macro\", n_jobs=-1, verbose=1)\ngrid.fit(X_train, y_train)\nprint(f\"Best params: {grid.best_params_}\")\nprint(f\"Best F1: {grid.best_score_:.3f}\")\nbest = grid.best_estimator_\nprint(classification_report(y_test, best.predict(X_test)))\n# Default: predict_proba > 0.5 -> positive\n# Sometimes you want higher precision (raise threshold) or recall (lower).\nimport numpy as np\nproba = best.predict_proba(X_test)[:, list(best.classes_).index(\"pos\")]\nfor threshold in [0.3, 0.5, 0.7]:\n    y_pred_thresh = np.where(proba >= threshold, \"pos\", \"neg\")\n    print(f\"\\nThreshold {threshold}:\")\n    print(classification_report(y_test, y_pred_thresh))\n# from imblearn.over_sampling import RandomOverSampler\n# ros = RandomOverSampler(random_state=42)\n# X_resampled, y_resampled = ros.fit_resample(X_train_tfidf, y_train)\n# Note: imblearn requires features (X_train_tfidf) not raw text."
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Text classification — Pipeline, baseline, hyperparameter tuning — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             save/load model, when to switch to transformers.\nimport joblib\nfrom sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.calibration import CalibratedClassifierCV\nimport numpy as np\n# LogisticRegression's probabilities are well-calibrated by default;\n# LinearSVC's \"decision_function\" output is NOT a probability — wrap.\n# from sklearn.svm import LinearSVC\n# clf = CalibratedClassifierCV(LinearSVC(), cv=5)        # adds calibrated predict_proba\npipe = Pipeline([\n    (\"tfidf\", TfidfVectorizer(ngram_range=(1, 2), min_df=2, sublinear_tf=True)),\n    (\"clf\",   LogisticRegression(max_iter=2000, class_weight=\"balanced\", C=1.0)),\n])\npipe.fit(X_train, y_train)\nvec = pipe.named_steps[\"tfidf\"]\nclf = pipe.named_steps[\"clf\"]\nfeature_names = vec.get_feature_names_out()\nfor class_idx, class_name in enumerate(clf.classes_):\n    coefs = clf.coef_[class_idx if len(clf.classes_) > 2 else 0]\n    if class_name == clf.classes_[0] and len(clf.classes_) == 2:\n        coefs = -coefs                                  # flip for binary\n    top_idx = coefs.argsort()[-20:][::-1]\n    print(f\"Top features for {class_name}:\")\n    for i in top_idx:\n        print(f\"  {feature_names[i]:30}  {coefs[i]:+.3f}\")\njoblib.dump(pipe, \"sentiment.joblib\")\nloaded = joblib.load(\"sentiment.joblib\")\n# from fastapi import FastAPI\n# app = FastAPI()\n# model = joblib.load(\"sentiment.joblib\")\n# @app.post(\"/classify\")\n# async def classify(text: str):\n#     pred = model.predict([text])[0]\n#     proba = model.predict_proba([text])[0]\n#     return {\"label\": pred, \"scores\": dict(zip(model.classes_, proba.tolist()))}\n# Decision rule:\n#   binary classification                 -> LogisticRegression (well-calibrated probs)\n#   multi-class, large vocab              -> LinearSVC (faster, no probas without wrapper)\n#   class imbalance                        -> class_weight=\"balanced\" first; then resampling\n#   need calibrated probs from SVC        -> CalibratedClassifierCV wrapper\n#   feature interpretation                 -> coef_ on linear models\n#   want >baseline accuracy                -> try transformers (sentence-transformers + classifier\n#                                                head, or fine-tune a HF model)\n#   want fast inference                    -> linear classifier; sub-ms per text; transformer ~50ms\n#   too few examples (<100/class)         -> few-shot LLM prompting probably wins\n#   thousands of classes                   -> hierarchical classification or transformer\n#   model file size matters                -> sklearn model is KB; transformer is GB\n#   threshold tuning                       -> use predict_proba; pick threshold on validation set\n#   class imbalance >100x                  -> resampling (imblearn) + threshold tuning\n#\n# Anti-pattern: shipping a transformer for a binary classification\n# task that TF-IDF + LR solves at 95% F1. Transformer adds ~50ms\n# latency per request, ~100MB+ model size, GPU dependency, and\n# fine-tuning complexity. For most classification tasks, the linear\n# baseline is the right answer; reach for transformers only when the\n# baseline isn't good enough (or when you need cross-domain transfer)."
                  }
        ],
        tips: [
                  "`Pipeline([(\"tfidf\", ...), (\"clf\", ...)])` is essential — `GridSearchCV` then refits the vectorizer PER FOLD, which is the only correct way to cross-validate text classifiers.",
                  "`class_weight=\"balanced\"` adjusts the loss to weight minority class higher. Try this BEFORE resampling — usually enough for 1:5 imbalances.",
                  "`f1_macro` is the right metric for imbalanced classes — averages F1 across classes equally. `accuracy` lies on imbalanced data.",
                  "Threshold tuning (`predict_proba > T` instead of default `0.5`) is free precision/recall trade. Pick T on validation set, not test.",
                  "For LinearSVC, wrap in `CalibratedClassifierCV` to get probability outputs (its raw `decision_function` isn't a probability).",
                  "`joblib.dump(pipe, \"model.joblib\")` is the right way to persist sklearn models — handles numpy arrays efficiently. Use `compress=3` to reduce file size."
        ],
        mistake: "Shipping a transformer for a binary classification task that TF-IDF + LogisticRegression solves at 95% F1. Transformer adds ~50ms latency per request, ~100MB+ model size, GPU dependency, fine-tuning complexity. Try the linear baseline first; reach for transformers only when it's not good enough.",
        shorthand: {
          verbose: "# Transformer for binary classifier — overkill\nfrom transformers import pipeline\nclf = pipeline(\"text-classification\", model=\"bert-base-uncased\")",
          concise: "# TF-IDF + LR — usually 95%+ F1, ~1ms inference\npipe = Pipeline([(\"tfidf\", TfidfVectorizer()),\n                  (\"clf\", LogisticRegression(class_weight=\"balanced\"))])\npipe.fit(X, y)",
        },
      },
      {
        id: "topic-modeling",
        fn: "Topic modeling — LDA, NMF, embeddings + clustering",
        desc: "Discover topics in unlabeled documents: LatentDirichletAllocation (probabilistic), NMF (faster, often cleaner), or sentence-transformers + clustering (modern, captures semantics). The \"what is this corpus about?\" tool.",
        category: "sklearn-text",
        subtitle: "LatentDirichletAllocation, NMF, BERTopic, coherence scoring, n_components, top words per topic, doc-topic distribution",
        signature: "lda = LatentDirichletAllocation(n_components=10).fit(X_tfidf); print_top_words(lda, vec)",
        descLong: "Topic modeling: given a corpus, find K latent topics each described by a distribution over words; describe each document as a mixture of topics. Two classical algorithms: **LDA** (probabilistic; works on TF/Count features), **NMF** (faster, often produces cleaner topics, works on TF-IDF). Modern alternative: **BERTopic** (sentence embeddings + clustering + class-based TF-IDF) — usually beats classical when topics overlap or are subtle. The three examples solve the SAME concrete task — discover ~10 topics in a news corpus and label them — at three depths: NMF on TF-IDF → LDA + coherence scoring + topic visualization → BERTopic with embeddings.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Topic modeling — LDA, NMF, embeddings + clustering — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nfrom sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.decomposition import NMF\n# Pretend corpus:\ndocs = [\n    \"the team won the championship game last night\",\n    \"stock prices fell after the earnings report\",\n    \"new species of frog discovered in the rainforest\",\n    # ... thousands of docs ...\n]\n# NMF works on TF-IDF (LDA wants raw counts).\nvec = TfidfVectorizer(max_features=5000, stop_words=\"english\", ngram_range=(1, 2))\nX = vec.fit_transform(docs)\nnmf = NMF(n_components=10, random_state=42, max_iter=400)\nW = nmf.fit_transform(X)                               # doc → topic distribution (n_docs, 10)\nH = nmf.components_                                     # topic → word distribution (10, vocab)\nfeature_names = vec.get_feature_names_out()\ndef print_top_words(model, feature_names, n_top: int = 10):\n    for topic_idx, topic in enumerate(model.components_):\n        top_idx = topic.argsort()[-n_top:][::-1]\n        print(f\"Topic {topic_idx}: {' '.join(feature_names[top_idx])}\")\nprint_top_words(nmf, feature_names)\n# Topic 0: game team season player win league sports coach\n# Topic 1: stock market price share earnings investor company report\n# Topic 2: species animal forest tree wildlife rainforest researcher discovered\n# ... (manually label each cluster)\n# Per-document dominant topic:\nimport numpy as np\ndoc_topics = W.argmax(axis=1)                          # which topic dominates each doc"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Topic modeling — LDA, NMF, embeddings + clustering — common patterns you'll see in production.\n# APPROACH  - Combine Topic modeling — LDA, NMF, embeddings + clustering with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             NMF vs LDA.\nfrom sklearn.feature_extraction.text import CountVectorizer, TfidfVectorizer\nfrom sklearn.decomposition import LatentDirichletAllocation, NMF\nimport numpy as np\ncount_vec = CountVectorizer(max_features=5000, stop_words=\"english\")\nX_count = count_vec.fit_transform(docs)\n# LDA: probabilistic; topics are probability distributions.\nlda = LatentDirichletAllocation(\n    n_components=10,\n    learning_method=\"online\",                          # batch is slower for large corpora\n    learning_decay=0.7,\n    n_jobs=-1,\n    random_state=42,\n)\nlda.fit(X_count)\n# A coherent topic has top words that frequently co-occur. Compute a\n# simple coherence proxy (UMass): for each pair of top-N words in a\n# topic, count co-occurrences; higher = more coherent.\ndef umass_coherence(model, feature_names, X, n_top: int = 10) -> float:\n    X_bin = (X > 0).astype(int)                        # binarize: doc has term y/n\n    doc_freq = np.asarray(X_bin.sum(axis=0)).flatten()  # term -> doc count\n    n_docs = X.shape[0]\n    coherences = []\n    for topic in model.components_:\n        top_idx = topic.argsort()[-n_top:][::-1]\n        topic_coh = 0.0\n        pairs = 0\n        for i in range(len(top_idx)):\n            for j in range(i):\n                co = (X_bin[:, top_idx[i]].multiply(X_bin[:, top_idx[j]])).sum()\n                topic_coh += np.log((co + 1) / doc_freq[top_idx[j]])\n                pairs += 1\n        coherences.append(topic_coh / max(pairs, 1))\n    return float(np.mean(coherences))\nresults = {}\nfor n in [5, 10, 15, 20, 30]:\n    lda_n = LatentDirichletAllocation(n_components=n, random_state=42).fit(X_count)\n    coh = umass_coherence(lda_n, count_vec.get_feature_names_out(), X_count)\n    results[n] = coh\n    print(f\"n_components={n}: coherence={coh:.3f}\")\nbest_n = max(results, key=results.get)\nprint(f\"\\nBest: n_components={best_n}\")\ntfidf_vec = TfidfVectorizer(max_features=5000, stop_words=\"english\")\nX_tfidf = tfidf_vec.fit_transform(docs)\nnmf = NMF(n_components=best_n, random_state=42, max_iter=400)\nnmf.fit(X_tfidf)\n# Inspect topics from each; pick the one whose topics are more interpretable."
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Topic modeling — LDA, NMF, embeddings + clustering — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             which captures semantic topics that BoW can't (synonyms,\n#             paraphrases).\n# pip install sentence-transformers umap-learn hdbscan scikit-learn\nfrom sentence_transformers import SentenceTransformer\nimport umap\nimport hdbscan\nfrom sklearn.feature_extraction.text import CountVectorizer\nimport numpy as np\nmodel = SentenceTransformer(\"all-MiniLM-L6-v2\")        # small + fast; 384-dim\nembeddings = model.encode(docs, show_progress_bar=True, batch_size=32)\n# embeddings.shape = (n_docs, 384)\nreducer = umap.UMAP(n_components=5, min_dist=0.0, metric=\"cosine\", random_state=42)\nembeddings_reduced = reducer.fit_transform(embeddings)\nclusterer = hdbscan.HDBSCAN(min_cluster_size=15, metric=\"euclidean\", cluster_selection_method=\"eom\")\ncluster_labels = clusterer.fit_predict(embeddings_reduced)\n# cluster_labels of -1 means \"noise\" (didn't fit any cluster)\nprint(f\"Found {len(set(cluster_labels)) - 1} clusters; {(cluster_labels == -1).sum()} noise docs\")\n# For each cluster, treat all its docs as one super-doc; compute TF-IDF\n# across the super-docs; top terms = topic words.\ndocs_per_cluster = {}\nfor doc, label in zip(docs, cluster_labels):\n    docs_per_cluster.setdefault(label, []).append(doc)\n# Concatenate each cluster into one document.\ncluster_docs = [\" \".join(docs_per_cluster[k]) for k in sorted(docs_per_cluster) if k != -1]\ncount_vec = CountVectorizer(stop_words=\"english\", min_df=2)\nX_clusters = count_vec.fit_transform(cluster_docs)\n# c-TF-IDF: term frequency across clusters.\nimport numpy as np\nn_clusters = X_clusters.shape[0]\ntf = X_clusters.toarray()\nn_per_cluster = tf.sum(axis=1, keepdims=True)\nlog_avg_freq = np.log(1 + (tf.sum(axis=0) / max(n_clusters, 1)))\nctf_idf = (tf / np.maximum(n_per_cluster, 1)) * log_avg_freq\nfeature_names = count_vec.get_feature_names_out()\nfor cluster_id, scores in enumerate(ctf_idf):\n    top_idx = scores.argsort()[-10:][::-1]\n    print(f\"Cluster {cluster_id}: {' '.join(feature_names[top_idx])}\")\n# from bertopic import BERTopic\n# topic_model = BERTopic(min_topic_size=15, calculate_probabilities=True)\n# topics, probs = topic_model.fit_transform(docs)\n# topic_model.get_topic_info()\n# topic_model.visualize_topics()                       # interactive plotly viz\n# topic_model.visualize_barchart()\n# Decision rule:\n#   small labeled dataset, KNOWN topics      -> classification, not topic modeling\n#   need probabilistic topic mixtures         -> LDA on CountVectorizer\n#   need cleaner / more interpretable topics  -> NMF on TfidfVectorizer\n#   modern, semantic topics                   -> BERTopic (sentence-transformers + clustering)\n#   millions of docs                           -> NMF (faster) OR online LDA\n#   topics overlap / are subtle               -> embeddings beat BoW\n#   need to know \"doc X is 70% topic 1, 30% topic 2\"  -> LDA (gives mixtures);\n#                                                          BERTopic gives single topic + probabilities\n#   pick optimal n_topics                     -> coherence (UMass / cv); range 5-50 is typical\n#   short text (tweets, queries)              -> BERTopic; LDA struggles with short docs\n#   multilingual                               -> multilingual sentence-transformer + BERTopic\n#   trends over time                          -> BERTopic.topics_over_time\n#\n# Anti-pattern: training LDA with n_components=2 on a corpus of 1M\n# docs and expecting meaningful topics. LDA topic count interacts\n# strongly with corpus size; rule of thumb: n_topics ≈ sqrt(n_docs)/4\n# for an initial guess, then tune via coherence. n=2 is essentially\n# binary clustering — useless for \"what topics are in this corpus?\"."
                  }
        ],
        tips: [
                  "LDA wants COUNT features; NMF wants TF-IDF. Wrong combination produces noisy topics.",
                  "Pick `n_components` (number of topics) via coherence on a held-out range. Start with 10-30; coherence usually peaks then plateaus.",
                  "NMF is often more interpretable than LDA at the same n_topics — try both and pick the one whose topics make sense to a human.",
                  "BERTopic (sentence-transformers + UMAP + HDBSCAN + c-TF-IDF) is the modern answer. Captures semantic topics LDA/NMF miss; library handles all the plumbing.",
                  "For short documents (tweets, queries), classical LDA struggles (sparse counts). BERTopic or sentence-embedding clustering is much better.",
                  "Use `topic_model.visualize_topics()` (BERTopic) or pyLDAvis for interactive topic exploration — much faster than reading top-words tables."
        ],
        mistake: "Training LDA with `n_components=2` (or any tiny number) on a large corpus and expecting meaningful topics. LDA topic count interacts with corpus size; n=2 is essentially binary clustering. Rule of thumb: start with `n_topics ≈ sqrt(n_docs)/4`, then tune via coherence.",
        shorthand: {
          verbose: "// Manual / verbose approach\n# n=2 on a large corpus — useless\nLatentDirichletAllocation(n_components=2).fit(X_count)\n// More explicit but longer",
          concise: "# Sweep + pick by coherence\nfor n in [10, 15, 20, 30, 50]:\n    coh = umass_coherence(LDA(n).fit(X), ...)\n# Pick n where coherence peaks.",
        },
      },
    ],
  },

  // ── Section 3: NLP Patterns — cleaning, ngrams, classical-vs-LLM ─────────────────────────────────────────
  {
    id: "patterns",
    title: "NLP Patterns — cleaning, ngrams, classical-vs-LLM",
    entries: [
      {
        id: "text-cleaning-pipeline",
        fn: "Text cleaning — normalization, stop words, lemmatization",
        desc: "Reproducible text-cleaning pipeline: lowercase + Unicode normalize + tokenize + remove stop words + lemmatize. The pre-feature-extraction step that everyone implements differently.",
        category: "Patterns",
        subtitle: "unicodedata.normalize NFKC, lowercase, regex cleanup, spaCy tokenize / lemmatize, stop word lists, when to skip cleaning (with transformers)",
        signature: "def clean(text): text = unicodedata.normalize(\"NFKC\", text); text = text.lower(); ...",
        descLong: "Text cleaning is the unglamorous-but-critical step before feature extraction. Decisions: lowercase or preserve case? remove stop words or keep? lemmatize, stem, or neither? remove punctuation or treat as features? handle Unicode normalization (NFC/NFKC)? remove URLs/emails/numbers or keep? Each choice depends on downstream task. The three examples solve the SAME concrete task — clean a corpus of user reviews for a sentiment classifier — at three depths: minimal regex + lowercase → spaCy lemmatization + stop words → production pipeline with Unicode handling, configurable steps, transformer-vs-classical decision.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Text cleaning — normalization, stop words, lemmatization — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport re\ndef clean_text(text: str) -> str:\n    text = text.lower()\n    text = re.sub(r\"http\\S+\", \" \", text)               # remove URLs\n    text = re.sub(r\"[^\\w\\s]\", \" \", text)              # punctuation -> space\n    text = re.sub(r\"\\s+\", \" \", text).strip()           # collapse whitespace\n    return text\nprint(clean_text(\"Loved it!! See https://example.com 5/5 stars.\"))\n# 'loved it see 5 5 stars'\n# What this does + doesn't do:\n# + lowercases (hello/Hello/HELLO are the same)\n# + removes URLs (irrelevant noise)\n# + removes punctuation (depends on task: punctuation matters for some)\n# - doesn't tokenize (TfidfVectorizer does its own)\n# - doesn't lemmatize ('loving'/'loved'/'love' stay distinct)\n# - doesn't handle Unicode quirks (curly quotes, accented chars)"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Text cleaning — normalization, stop words, lemmatization — common patterns you'll see in production.\n# APPROACH  - Combine Text cleaning — normalization, stop words, lemmatization with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             Unicode normalization.\nimport re\nimport unicodedata\nimport spacy\n# Lazy-load to avoid heavy startup if not used.\n_nlp = None\ndef get_nlp():\n    global _nlp\n    if _nlp is None:\n        _nlp = spacy.load(\"en_core_web_sm\", disable=[\"parser\", \"ner\"])\n    return _nlp\ndef clean_for_classifier(text: str, *, drop_stop: bool = True) -> str:\n    \"\"\"Clean for downstream TF-IDF + classifier.\"\"\"\n    # Unicode: NFKC merges look-alike chars (e.g., \"ﬁ\" -> \"fi\")\n    text = unicodedata.normalize(\"NFKC\", text)\n    text = text.lower()\n    text = re.sub(r\"http\\S+|www\\.\\S+\", \" \", text)     # URLs\n    text = re.sub(r\"\\S+@\\S+\", \" \", text)              # emails\n    text = re.sub(r\"\\d+\", \"NUM\", text)                  # collapse numbers (or remove)\n    nlp = get_nlp()\n    doc = nlp(text)\n    tokens = [\n        tok.lemma_\n        for tok in doc\n        if not tok.is_punct\n        and not tok.is_space\n        and not (drop_stop and tok.is_stop)\n        and len(tok.lemma_) > 1                         # drop single-char artifacts\n    ]\n    return \" \".join(tokens)\n# Custom stop-word adjustments (some \"stop words\" matter for sentiment).\nSENTIMENT_KEEP = {\"not\", \"no\", \"never\", \"but\", \"however\"}\nget_nlp().Defaults.stop_words -= SENTIMENT_KEEP        # don't drop these\nprint(clean_for_classifier(\"Loved it!! NOT a waste of time. See https://x.com\"))\n# 'love not waste time'"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Text cleaning — normalization, stop words, lemmatization — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             pattern (some steps need stats), batch processing.\nimport re, unicodedata\nfrom dataclasses import dataclass, field\nfrom typing import Iterable\nimport spacy\n@dataclass\nclass TextCleaner:\n    lowercase: bool = True\n    nfkc: bool = True\n    remove_urls: bool = True\n    remove_emails: bool = True\n    number_handling: str = \"collapse\"                  # 'collapse' / 'remove' / 'keep'\n    drop_stop_words: bool = True\n    drop_punct: bool = True\n    lemmatize: bool = True\n    min_token_length: int = 2\n    custom_keep_stop: set[str] = field(default_factory=set)\n    spacy_model: str = \"en_core_web_sm\"\n    def __post_init__(self):\n        self._nlp = None\n    def _ensure_nlp(self):\n        if self._nlp is None:\n            self._nlp = spacy.load(self.spacy_model, disable=[\"parser\", \"ner\"])\n            for w in self.custom_keep_stop:\n                self._nlp.vocab[w].is_stop = False\n    def _pre_clean(self, text: str) -> str:\n        if self.nfkc: text = unicodedata.normalize(\"NFKC\", text)\n        if self.lowercase: text = text.lower()\n        if self.remove_urls: text = re.sub(r\"http\\S+|www\\.\\S+\", \" \", text)\n        if self.remove_emails: text = re.sub(r\"\\S+@\\S+\", \" \", text)\n        if self.number_handling == \"remove\": text = re.sub(r\"\\d+\", \" \", text)\n        elif self.number_handling == \"collapse\": text = re.sub(r\"\\d+\", \"NUM\", text)\n        return re.sub(r\"\\s+\", \" \", text).strip()\n    def clean_one(self, text: str) -> str:\n        self._ensure_nlp()\n        text = self._pre_clean(text)\n        if not (self.lemmatize or self.drop_stop_words or self.drop_punct):\n            return text                                # skip spaCy if not needed\n        doc = self._nlp(text)\n        tokens = []\n        for tok in doc:\n            if self.drop_punct and tok.is_punct: continue\n            if tok.is_space: continue\n            if self.drop_stop_words and tok.is_stop: continue\n            tok_text = tok.lemma_ if self.lemmatize else tok.text\n            if len(tok_text) < self.min_token_length: continue\n            tokens.append(tok_text)\n        return \" \".join(tokens)\n    def clean_batch(self, texts: Iterable[str], *, batch_size: int = 64) -> list[str]:\n        \"\"\"Faster than clean_one in a loop via nlp.pipe.\"\"\"\n        self._ensure_nlp()\n        pre_cleaned = [self._pre_clean(t) for t in texts]\n        if not (self.lemmatize or self.drop_stop_words):\n            return pre_cleaned\n        results = []\n        for doc in self._nlp.pipe(pre_cleaned, batch_size=batch_size):\n            tokens = []\n            for tok in doc:\n                if self.drop_punct and tok.is_punct: continue\n                if tok.is_space: continue\n                if self.drop_stop_words and tok.is_stop: continue\n                tok_text = tok.lemma_ if self.lemmatize else tok.text\n                if len(tok_text) < self.min_token_length: continue\n                tokens.append(tok_text)\n            results.append(\" \".join(tokens))\n        return results\n# Use:\ncleaner = TextCleaner(custom_keep_stop={\"not\", \"no\", \"never\"})\ncleaned = cleaner.clean_batch(reviews)\n# Modern transformer models (BERT, RoBERTa, sentence-transformers)\n# have their OWN tokenizer. Pre-cleaning often HURTS because:\n#  - Lowercase loses case features (\"Apple\" vs \"apple\")\n#  - Removing punctuation loses sentence structure\n#  - Lemmatization conflicts with subword tokenization\n#  - Stop-word removal removes context the transformer uses\n# For transformer pipelines: do MINIMAL cleaning (NFKC, URL removal\n# if irrelevant). Let the model see most of the original text.\n# Decision rule:\n#   downstream = TF-IDF / BoW             -> aggressive cleaning helps\n#   downstream = transformer / embedding   -> minimal cleaning (NFKC + url-strip only)\n#   sentiment task                          -> preserve negations (\"not\", \"no\", \"never\")\n#   topic modeling                          -> aggressive stop-word removal\n#   informal text (tweets)                  -> preserve emojis (or replace with placeholder)\n#   multilingual                            -> NFKC normalization is critical\n#   case matters (NER, code)                -> don't lowercase\n#   numbers carry signal (financial)        -> keep numbers (don't replace with NUM)\n#   need reproducibility                    -> dataclass config; pin spaCy version\n#   speed matters                           -> use clean_batch with nlp.pipe\n#\n# Anti-pattern: aggressive cleaning before a transformer. Lowercasing,\n# removing punctuation, removing stop words, lemmatizing — then\n# feeding into BERT. The transformer's pretraining was on natural\n# text; aggressive cleaning makes input out-of-distribution. Apply\n# minimal cleaning (NFKC, optional URL strip) and let the model see\n# the original."
                  }
        ],
        tips: [
                  "For TF-IDF / BoW classifiers, aggressive cleaning (lowercase, lemmatize, drop stop words) usually helps. For transformers, MINIMAL cleaning is right.",
                  "Unicode normalization (`unicodedata.normalize(\"NFKC\", text)`) merges look-alike characters (\"ﬁ\" → \"fi\", curly → straight quotes). Run this BEFORE lowercase.",
                  "For sentiment tasks, keep negation words (\"not\", \"no\", \"never\", \"but\") — they're technically stop words but flip meaning. Customize the stop list.",
                  "Number handling depends on task: replace with `NUM` for sentiment (numbers don't matter), KEEP for financial / scientific (numbers carry signal).",
                  "Use `nlp.pipe(texts, batch_size=64)` for batch cleaning — 5-10× faster than calling spaCy per doc.",
                  "For multilingual corpora, char n-grams + NFKC is often more robust than language-specific tokenization."
        ],
        mistake: "Aggressive cleaning (lowercase + lemmatize + remove stop words) before feeding into a transformer (BERT, sentence-transformers). The model's pretraining was on natural text; aggressive cleaning makes input out-of-distribution and HURTS performance. For transformers: NFKC + optional URL strip, leave the rest.",
        shorthand: {
          verbose: "# Aggressive cleaning before BERT — hurts the model\ncleaned = clean_for_classifier(text, drop_stop=True, lemmatize=True)\nembedding = bert_model.encode(cleaned)",
          concise: "# Minimal cleaning for transformers\ntext = unicodedata.normalize(\"NFKC\", raw_text)\ntext = re.sub(r\"http\\S+\", \"\", text)\nembedding = bert_model.encode(text)   # let the model see most of the original",
        },
      },
      {
        id: "ngrams-features",
        fn: "N-grams — character vs word, when each helps",
        desc: "Word n-grams capture phrase context (\"New York\" as one feature); character n-grams handle morphology and OOV (\"running\" → \"run\", \"runs\", \"runner\" share substrings). Often combined for best results.",
        category: "Patterns",
        subtitle: "word ngrams (TfidfVectorizer ngram_range), char_wb analyzer, FeatureUnion to combine, OOV handling, multilingual char n-grams",
        signature: "word_vec = TfidfVectorizer(ngram_range=(1,2)); char_vec = TfidfVectorizer(analyzer=\"char_wb\", ngram_range=(3,5))",
        descLong: "Word n-grams treat phrases as features (\"not good\" is one feature, separate from \"not\" + \"good\"). Character n-grams use letter sequences (\"running\" → \"run\", \"unn\", \"nni\", \"nin\", \"ing\") which generalize across morphological variants AND handle out-of-vocabulary words. Best results often combine both via `FeatureUnion`. The three examples solve the SAME concrete task — extract features for a multilingual product review classifier — at three depths: word unigrams + bigrams → char n-grams (4-6) → FeatureUnion combining both.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of N-grams — character vs word, when each helps — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nfrom sklearn.feature_extraction.text import TfidfVectorizer\ndocs = [\"the cat sat\", \"a dog ran\", \"cats and dogs\"]\n# Word unigrams (default).\nvec1 = TfidfVectorizer(ngram_range=(1, 1))\nprint(vec1.fit(docs).get_feature_names_out())\n# ['and' 'cat' 'cats' 'dog' 'dogs' 'ran' 'sat' 'the']\n# Word unigrams + bigrams.\nvec2 = TfidfVectorizer(ngram_range=(1, 2))\nprint(vec2.fit(docs).get_feature_names_out())\n# ['and' 'and dogs' 'cat' 'cat sat' 'cats' 'cats and' 'dog' 'dog ran' ...]\n# When word bigrams help:\n#   Sentiment: \"not good\" carries different meaning than \"not\" + \"good\"\n#   Topic: \"machine learning\" is a topic; \"machine\" + \"learning\" alone are not\n# When they don't:\n#   Already-strong unigrams + small dataset (sparsity hurts)"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of N-grams — character vs word, when each helps — common patterns you'll see in production.\n# APPROACH  - Combine N-grams — character vs word, when each helps with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nfrom sklearn.feature_extraction.text import TfidfVectorizer\n# analyzer=\"char_wb\" = char n-grams that respect word boundaries\n# (won't span across spaces). \"char\" alone spans words.\nchar_vec = TfidfVectorizer(\n    analyzer=\"char_wb\",\n    ngram_range=(3, 5),                                # 3-5 char sequences\n    min_df=2,\n)\ndocs = [\"I love running\", \"She runs quickly\", \"Runners are fast\"]\nchar_vec.fit(docs)\n# Notice: \"run\" appears in all three (running/runs/Runners) as a feature\n# even though the surface words differ. Word n-grams would see them as\n# separate.\nprint(char_vec.get_feature_names_out()[:30])\n# ' ar' ' fa' ' lo' ' ov' ' qu' ' ru' 're ' 're a' 'are' 'are ' 'ast'\n# 'cki' ... 'nin' 'ning' 'ning ' 'nn' ...\n# When char n-grams help:\n#   Multilingual / typos / rare words / morphology\n#   Short text where word vocab is sparse\n#   Names / product codes / DNA sequences\n# When they hurt:\n#   Vocabulary explosion (millions of features); use min_df=N\n#   Word semantics matter more than morphology\nfrom sklearn.pipeline import FeatureUnion\ncombined = FeatureUnion([\n    (\"word\", TfidfVectorizer(ngram_range=(1, 2), min_df=2)),\n    (\"char\", TfidfVectorizer(analyzer=\"char_wb\", ngram_range=(3, 5), min_df=5)),\n])\nX = combined.fit_transform(docs)\nprint(f\"Combined features: {X.shape[1]}\")            # word features + char features\n# Often gets +1-3% F1 over either alone for sentiment / classification"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of N-grams — character vs word, when each helps — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             combination, tuning via GridSearchCV.\nfrom sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.pipeline import Pipeline, FeatureUnion\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.model_selection import GridSearchCV\npipe = Pipeline([\n    (\"features\", FeatureUnion([\n        (\"word\", TfidfVectorizer(\n            ngram_range=(1, 2), min_df=2, sublinear_tf=True,\n            analyzer=\"word\",\n        )),\n        (\"char\", TfidfVectorizer(\n            ngram_range=(3, 5), min_df=5, sublinear_tf=True,\n            analyzer=\"char_wb\",\n            max_features=50_000,                       # cap; char features explode otherwise\n        )),\n    ])),\n    (\"clf\", LogisticRegression(max_iter=2000, class_weight=\"balanced\")),\n])\nparam_grid = {\n    \"features__word__ngram_range\": [(1, 1), (1, 2), (1, 3)],\n    \"features__char__ngram_range\": [(3, 4), (3, 5), (4, 6)],\n    \"features__char__max_features\": [20_000, 50_000, 100_000],\n    \"clf__C\": [0.1, 1.0, 10.0],\n}\ngrid = GridSearchCV(pipe, param_grid, cv=3, scoring=\"f1_macro\", n_jobs=-1)\n# When word features dominate but char features add signal, weight them:\nweighted = FeatureUnion([\n    (\"word\", TfidfVectorizer(ngram_range=(1, 2))),\n    (\"char\", TfidfVectorizer(analyzer=\"char_wb\", ngram_range=(3, 5))),\n], transformer_weights={\"word\": 1.0, \"char\": 0.3})    # downweight char\n# Decision rule:\n#   English, well-formed text             -> word n-grams (1, 2) — usually enough\n#   sentiment, negation matters            -> word n-grams (1, 2) keeping \"not\"\n#   typos / informal text (tweets)         -> char n-grams (3, 5)\n#   short text, sparse vocab               -> char n-grams cover OOV better\n#   multilingual                            -> char n-grams; less sensitive to language\n#   names / product codes / DNA            -> char n-grams (3, 5)\n#   need both                               -> FeatureUnion(word + char)\n#   feature explosion                       -> max_features=N AND min_df=K\n#   topic modeling                          -> word n-grams; char too noisy\n#   transformer downstream                  -> NEITHER; transformers tokenize themselves\n#   need to interpret features              -> word n-grams only (char features are opaque)\n#   multilingual sentiment                  -> char + word combined; or pretrained multilingual model\n#\n# Anti-pattern: word n-grams with very large ngram_range (e.g., 1-5)\n# without min_df. Vocabulary explodes from ~10k unigrams to millions\n# of 5-grams; sparsity dominates; classifier overfits to specific\n# phrases that appeared once in training. Cap at (1, 2) or (1, 3)\n# and require min_df >= 2."
                  }
        ],
        tips: [
                  "Word n-grams (1, 2) is the sane default. Larger ranges explode vocabulary; (1, 3) only helps with strong `min_df`.",
                  "Character n-grams (`char_wb` analyzer, range 3-5) handle OOV, typos, and morphology. Critical for short or multilingual text.",
                  "Combine word + char features via `FeatureUnion` — often gets +1-3% F1 over either alone, especially on noisy text.",
                  "Cap char-n-gram vocabulary with `max_features=50_000` — char features explode without bounds (a million-doc corpus has 100M+ char-5-grams).",
                  "Use `transformer_weights` on FeatureUnion to downweight one feature set (`{\"word\": 1.0, \"char\": 0.3}`) when one dominates the other.",
                  "For transformer downstream models, SKIP n-gram features entirely — the model handles tokenization."
        ],
        mistake: "Word n-grams with `ngram_range=(1, 5)` and no `min_df`. Vocabulary explodes from ~10k unigrams to millions of 5-grams; sparsity dominates; classifier overfits to phrases that appeared once. Cap at (1, 2) or (1, 3) with `min_df >= 2`.",
        shorthand: {
          verbose: "// Manual / verbose approach\n# Vocabulary explosion — millions of features\nTfidfVectorizer(ngram_range=(1, 5))\n// More explicit but longer",
          concise: "# Bounded vocab\nTfidfVectorizer(ngram_range=(1, 2), min_df=2, max_features=50_000)",
        },
      },
      {
        id: "classical-vs-llm",
        fn: "Classical NLP vs LLM — when each wins",
        desc: "Frontier LLMs are stunning at zero-shot tasks but slow, expensive, and opaque. Classical NLP (spaCy + sklearn) is fast, cheap, deterministic, and interpretable. The decision matrix matters more than the tools.",
        category: "Patterns",
        subtitle: "classification baseline (TF-IDF + LR), latency / cost / interpretability tradeoffs, hybrid: classical filter + LLM, embedding + classifier",
        signature: "when classical wins: latency < 10ms, cost < $0.0001/req, deterministic, interpretable, < 100k examples",
        descLong: "The temptation: \"use an LLM for everything\". The reality: for a binary classification task with 10k labeled examples, TF-IDF + LogisticRegression beats GPT-4 in latency (1ms vs 500ms), cost ($0 vs $0.001/request), interpretability (top features visible), and often accuracy. LLMs win on zero-shot tasks, complex reasoning, generative work, and few-shot adaptation. The right architecture is often hybrid: classical model handles the 99% common case; LLM handles the 1% edge cases. The three examples solve the SAME concrete task — classify support tickets into 50 categories with 100k labeled examples — at three depths: TF-IDF + LR baseline → fine-tuned transformer → hybrid (classical filter + LLM fallback).",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Classical NLP vs LLM — when each wins — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nfrom sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.pipeline import Pipeline\nfrom sklearn.metrics import classification_report\npipe = Pipeline([\n    (\"tfidf\", TfidfVectorizer(ngram_range=(1, 2), min_df=2, sublinear_tf=True)),\n    (\"clf\",   LogisticRegression(max_iter=2000, class_weight=\"balanced\")),\n])\npipe.fit(X_train, y_train)\nprint(classification_report(y_test, pipe.predict(X_test)))\n# Profile:\n#   Train time:    ~30 seconds on 100k examples\n#   Inference:     ~1ms per request\n#   Cost:          $0 (just CPU)\n#   Model size:    ~50MB (joblib)\n#   F1-macro:      typically 0.75-0.85 on well-defined categories\n# Compare to LLM:\n#   GPT-4 inference: ~500-1500ms per request\n#   Cost: ~$0.001 per classification (input tokens)\n#   F1-macro: maybe 0.80-0.90 (zero-shot or few-shot)\n#   ... but no fine-tuning needed, no labeled data needed\n# When classical baseline is the right answer:\n#   - You have >5k labeled examples\n#   - Latency matters (real-time)\n#   - High volume (>100 req/sec) makes LLM cost prohibitive\n#   - Categories are well-defined, not subjective"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Classical NLP vs LLM — when each wins — common patterns you'll see in production.\n# APPROACH  - Combine Classical NLP vs LLM — when each wins with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             classical baseline plateaus.\nfrom sentence_transformers import SentenceTransformer\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.metrics import classification_report\nimport numpy as np\nencoder = SentenceTransformer(\"all-MiniLM-L6-v2\")     # 22MB, fast, 384-dim\nX_train_emb = encoder.encode(X_train, show_progress_bar=True, batch_size=64)\nX_test_emb  = encoder.encode(X_test,  show_progress_bar=True, batch_size=64)\nclf = LogisticRegression(max_iter=2000, class_weight=\"balanced\", C=1.0)\nclf.fit(X_train_emb, y_train)\nprint(classification_report(y_test, clf.predict(X_test_emb)))\n# Profile:\n#   Embed training set: ~30 seconds for 100k docs (CPU); ~5s on GPU\n#   Embed inference:    ~10-50ms per request (CPU); ~1ms (GPU)\n#   Train classifier:   ~10s\n#   Cost:                $0 if self-hosted; ~$0.0001/req for hosted embeddings\n#   F1-macro:            typically +5-10% over TF-IDF baseline\n# When embeddings beat TF-IDF:\n#   - Subtle semantic distinctions (paraphrases, synonyms)\n#   - Cross-domain / multi-domain data\n#   - Need cross-lingual transfer (use multilingual encoder)\n#   - Few labels (embeddings + LR works with 100s of examples)\n# Method                Latency    Cost/req    F1     Setup\n# TF-IDF + LR           1ms        $0          0.78   joblib + sklearn\n# Embedding + LR        20ms       $0.0001     0.84   sentence-transformers\n# Fine-tuned BERT       50ms (GPU) $0.0005     0.87   training pipeline\n# GPT-4 zero-shot       1000ms     $0.002      0.83   prompt design\n# GPT-4 few-shot        1200ms     $0.005      0.88   examples + prompt"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Classical NLP vs LLM — when each wins — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             tickets; LLM handles low-confidence / novel categories.\nfrom sklearn.feature_extraction.text import TfidfVectorizer\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.pipeline import Pipeline\nimport joblib\nimport numpy as np\ndef train_classical(X_train, y_train):\n    pipe = Pipeline([\n        (\"tfidf\", TfidfVectorizer(ngram_range=(1, 2), min_df=2, sublinear_tf=True)),\n        (\"clf\",   LogisticRegression(max_iter=2000, class_weight=\"balanced\", C=1.0)),\n    ])\n    pipe.fit(X_train, y_train)\n    return pipe\ndef llm_classify(text: str, allowed_labels: list[str]) -> tuple[str, float]:\n    \"\"\"Use LLM when classical model is uncertain. Returns (label, confidence).\"\"\"\n    # In production: call OpenAI / Anthropic with prompt template.\n    # Here, sketch the structure:\n    prompt = f\"Classify this ticket into one of: {allowed_labels}\\n\\n{text}\\n\\nLabel:\"\n    # response = openai.chat.completions.create(...)\n    # return parse_label_and_confidence(response)\n    return (\"billing\", 0.95)                           # placeholder\nclass HybridClassifier:\n    def __init__(self, classical_model, *, confidence_threshold: float = 0.7):\n        self.classical = classical_model\n        self.threshold = confidence_threshold\n        self.labels = list(classical_model.classes_)\n    def predict(self, text: str) -> dict:\n        # Classical first.\n        proba = self.classical.predict_proba([text])[0]\n        top_idx = np.argmax(proba)\n        confidence = proba[top_idx]\n        classical_label = self.labels[top_idx]\n        if confidence >= self.threshold:\n            return {\n                \"label\": classical_label,\n                \"confidence\": float(confidence),\n                \"tier\": \"classical\",\n                \"cost_units\": 0,\n            }\n        # Fallback to LLM for uncertain cases.\n        llm_label, llm_conf = llm_classify(text, self.labels)\n        return {\n            \"label\": llm_label,\n            \"confidence\": llm_conf,\n            \"tier\": \"llm\",\n            \"cost_units\": 1,\n            \"classical_top\": classical_label,\n            \"classical_confidence\": float(confidence),\n        }\n# In production, log tier per request; alert if LLM tier > 20%\n# (means classical is degrading; retrain).\n# Decision rule:\n# Question                              Classical wins        LLM wins\n# -------------------------------------------------------------------\n# Latency requirement < 50ms             ✓                    ✗\n# Cost per request > $0.0001 OK?         ✗                    ✓\n# Have >5k labeled examples?             ✓                    ✗ (LLM zero-shot)\n# Categories change frequently?          ✗ (retrain)          ✓\n# Need explainable decisions?            ✓ (top features)     ✗\n# Need few-shot adaptation?              ✗                    ✓\n# Reasoning / extraction / summarization ✗                    ✓\n# Multilingual without retraining        ✗ (per-lang)         ✓\n# Adversarial inputs                     ✓ (deterministic)    ✗\n# > 1000 req/sec                          ✓                    ✗ (cost)\n# Subjective tasks (creativity, taste)   ✗                    ✓\n#\n# Hybrid wins when:\n#   - High volume (cost matters) AND some hard cases (LLM helps)\n#   - Need fast common path (classical) but want LLM safety net\n#   - Categories are 80% well-defined, 20% ambiguous\n#\n# Anti-pattern: replacing a working TF-IDF + LR baseline with GPT-4\n# because \"GPT-4 is better\". For binary or small-multiclass tasks\n# with 5k+ labels, the classical baseline often matches or beats\n# zero-shot LLM accuracy at 1/1000 the cost and 1/100 the latency.\n# Measure both; pick by accuracy * cost * latency, not by hype."
                  }
        ],
        tips: [
                  "Always run a classical baseline (TF-IDF + LogisticRegression) BEFORE reaching for transformers or LLMs. ~30 minutes of work; sets the bar.",
                  "For tasks with >5k labels, classical or fine-tuned models almost always beat zero-shot LLM in accuracy AND cost.",
                  "Sentence embeddings + linear classifier is the middle path: 5-10% F1 over TF-IDF, 50-100× faster than full LLM, runs on CPU.",
                  "Use LLMs when you have <100 labeled examples (few-shot wins), when categories change frequently (no retraining), or for generative/reasoning tasks.",
                  "Hybrid architectures (classical baseline + LLM fallback for low-confidence) capture both worlds — fast common path, LLM for hard cases.",
                  "Track tier-handling rate in production. If LLM-tier > 20% over time, the classical model is drifting — retrain."
        ],
        mistake: "Replacing a working classical baseline with GPT-4 because \"LLMs are better\". For binary or small-multiclass classification with 5k+ labeled examples, TF-IDF + LR often matches or beats zero-shot LLM at 1/1000 the cost and 1/100 the latency. Measure both; pick by accuracy × cost × latency, not by hype.",
        shorthand: {
          verbose: "# Replace working classical pipeline with GPT-4\ndef classify(text):\n    response = openai.chat.completions.create(...)\n    return parse_response(response)\n# 500ms, $0.002/call, opaque",
          concise: "# Classical baseline first; measure; switch only if it loses\npipe = Pipeline([(\"tfidf\", TfidfVectorizer()),\n                  (\"clf\", LogisticRegression())])\npipe.fit(X, y)\n# 1ms, $0, interpretable, 0.85 F1",
        },
      },
    ],
  },
]

export default { meta, sections }

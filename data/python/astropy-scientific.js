export const meta = {
  "id": "astropy-scientific",
  "label": "Astropy & Scientific",
  "icon": "🔭",
  "description": "Astropy (units + Quantity, SkyCoord coordinate frames, FITS I/O, Time scales) for astronomy; NetworkX for graph algorithms; SymPy for exact symbolic math. Three things you keep getting wrong: mixing degrees and radians (let `u.deg` / `u.rad` carry the unit), assuming naive UTC datetimes when you needed TAI/TDB for ephemeris work, and reading FITS files without a `with` block (memory-mapped data goes stale)."
}

export const sections = [

  // ── Section 1: Astropy — units, time, coords, FITS ─────────────────────────────────────────
  {
    id: "astropy",
    title: "Astropy — units, time, coords, FITS",
    entries: [
      {
        id: "astropy-units-quantity",
        fn: "astropy.units / Quantity — values that carry units",
        desc: "A `Quantity` is a NumPy array tagged with a unit (`5 * u.m`, `np.array([1,2,3]) * u.km/u.s`). Arithmetic propagates and converts units; `.to(target)` reprojects (`(3 * u.km).to(u.m)` → `3000 m`). Catches unit-mismatch bugs at *runtime* before they ruin a calculation.",
        category: "astropy",
        subtitle: "astropy.units (u.m, u.s, u.km/u.s, u.deg, u.arcsec, u.Jy), Quantity = number * unit, .to(target_unit), .to_value() to strip units, equivalencies (spectral, parallax, dimensionless_angles), Constants module (const.c, const.G, const.M_sun)",
        signature: "q = 5 * u.km; q.to(u.m); q.value; (1 * u.eV).to(u.J, equivalencies=u.spectral())",
        descLong: "Quantity wraps NumPy: arithmetic propagates units, `.to(target)` converts, `.to_value(target)` returns a plain number in those units. Some conversions need an \"equivalency\" — energy ↔ wavelength needs `u.spectral()`; angular size ↔ physical size needs `u.parallax()`. Constants like `const.c` and `const.G` are pre-typed Quantities. Three depths solve the SAME task — compute kinetic energy of a 70 kg human walking 1.4 m/s — at depths: bare numbers (no unit safety) → Quantity arithmetic → mixed-unit input (km/h) with `.to()` to canonical SI plus a sanity check via `dimensionless_angles`-style equivalency.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Compute kinetic energy of a 70 kg person walking 1.4 m/s.\n# APPROACH  - Plain Python.\n# STRENGTHS - Direct.\n# WEAKNESSES- Nothing catches \"I gave 1.4 km/h instead of 1.4 m/s\".\nm = 70.0                                              # kg\nv = 1.4                                               # m/s\nke = 0.5 * m * v**2\nprint(ke, \"J\")                                        # 68.6 J -- correct only if you trusted the units\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — KE of a 70 kg person — using astropy units.\n# APPROACH  - Multiply by units; let arithmetic propagate.\n# STRENGTHS - Result CARRIES the unit; a unit-mismatch bug raises.\n# WEAKNESSES- More verbose for trivial cases.\nimport astropy.units as u\n\nm = 70 * u.kg\nv = 1.4 * u.m / u.s\n\nke = 0.5 * m * v**2\nprint(ke)                                             # 68.6 J  (Quantity)\nprint(ke.to(u.J))                                     # explicit convert\nprint(ke.to_value(u.J))                               # plain float in joules\n\n# Demonstrate the safety net:\ntry:\n    bad = 0.5 * m * (5 * u.km / u.h)                  # mixing units? It works...\n    print(\"ok:\", bad.to(u.J))                         # ...because km/h is convertible.\n    very_bad = m + (5 * u.kg / u.s)                   # incompatible units\nexcept u.UnitConversionError as e:\n    print(\"caught:\", e)\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — KE — production: function that accepts ANY mass / speed\n#             unit, validates inputs, returns SI joules; physical constants\n#             from astropy.constants.\n# APPROACH  - Type-check via Quantity; .to(canonical) up front; const-driven.\n# STRENGTHS - Reusable; rejects unitless inputs explicitly; SI-canonical.\n# WEAKNESSES- Slight overhead vs raw floats; pays off in any pipeline\n#             that mixes units across modules.\nfrom __future__ import annotations\nimport astropy.units as u\nfrom astropy import constants as const\n\n\ndef kinetic_energy(mass: u.Quantity, speed: u.Quantity) -> u.Quantity:\n    \"\"\"Return KE in SI joules for any mass/speed units. Raises on missing unit.\"\"\"\n    if not isinstance(mass, u.Quantity):\n        raise TypeError(\"mass must be a Quantity (e.g. 70 * u.kg)\")\n    if not isinstance(speed, u.Quantity):\n        raise TypeError(\"speed must be a Quantity (e.g. 1.4 * u.m / u.s)\")\n\n    m = mass.to(u.kg)\n    v = speed.to(u.m / u.s)\n    return (0.5 * m * v**2).to(u.J)\n\n\ndef relativistic_factor(speed: u.Quantity) -> float:\n    \"\"\"gamma = 1 / sqrt(1 - v^2/c^2). Returns dimensionless float.\"\"\"\n    v = speed.to(u.m / u.s)\n    beta_sq = (v / const.c) ** 2                      # already dimensionless\n    return float((1 / (1 - beta_sq).value ** 0.5))\n\n\n# Use it - any unit input works.\nprint(kinetic_energy(70 * u.kg,  1.4 * u.m / u.s))     # 68.6 J\nprint(kinetic_energy(70 * u.kg,  5   * u.km / u.h))    # auto-converted\nprint(kinetic_energy(0.5 * const.M_sun.to(u.kg), 200 * u.km / u.s))\n\n# Equivalencies for non-trivial conversions:\nenergy = (532 * u.nm).to(u.eV, equivalencies=u.spectral())\nprint(f\"532 nm photon = {energy:.3f}\")                 # 2.331 eV\n\n# Decision rule:\n#   Want compile-/runtime safety on units      -> Quantity everywhere; ban raw floats.\n#   Performance-critical loops                  -> .to_value(canonical) early; raw NumPy inside.\n#   Need physical constants                     -> astropy.constants (typed Quantities).\n#   Need spectral / parallax conversions        -> equivalencies argument to .to().\n#   Want to print with formatting                -> f\"{q:.3f}\" or q.to_string(format=\"latex\").\n#   Need to serialize across systems             -> store value + unit string;\n#                                                   reconstruct with u.Unit(str).\n#   Want pandas-aware unit DataFrames            -> astropy.table.Table or pint-pandas\n#                                                   (NOT pandas + Quantity directly).\n\n# Anti-pattern:\n#   if mass > 50:                               # mass is a Quantity\n#       ...\n# Comparing a Quantity to a scalar uses the Quantity's value with its\n# CURRENT unit - if mass got passed as grams, 50 means 50 grams. Always\n# compare to a Quantity: if mass > 50 * u.kg.\n"
                  }
        ],
        tips: [
                  "`Quantity` arithmetic propagates units; `.to(target)` converts; `.to_value(target)` returns a bare number.",
                  "Use `astropy.constants` (`const.c`, `const.G`, `const.M_sun`) for typed physical constants.",
                  "For non-trivial conversions (wavelength↔energy, angular size↔distance), pass `equivalencies=u.spectral()` / `u.parallax()`.",
                  "Compare quantities to other quantities — `if mass > 50 * u.kg`, not `if mass > 50`.",
                  "For perf-critical inner loops, strip with `.to_value(canonical)` and operate on plain NumPy."
        ],
        mistake: "Comparing a Quantity to a bare scalar (`if mass > 50:`). The comparison uses the Quantity's current unit — if `mass` is in grams, 50 means 50 grams. Always wrap the threshold: `if mass > 50 * u.kg`.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
      {
        id: "astropy-skycoord",
        fn: "astropy.coordinates.SkyCoord — celestial coordinates",
        desc: "`SkyCoord(ra, dec, frame=, unit=, obstime=)` is the celestial-coordinate workhorse. Transform between frames (`.transform_to(\"galactic\")`), compute angular separations (`.separation(other)`), match catalogs (`.match_to_catalog_sky(other)`). Always pass units explicitly — bare floats are ambiguous between deg and hourangle.",
        category: "astropy",
        subtitle: "SkyCoord (ra, dec, unit=, frame= \"icrs\"/\"fk5\"/\"galactic\"/\"altaz\"), .transform_to(frame_or_string), .separation (returns Quantity in degrees), .match_to_catalog_sky (KD-tree sky matching), Galactic l/b, AltAz needs obstime + EarthLocation, parsing \"10h21m00s +41d12m00s\" strings",
        signature: "c = SkyCoord(ra=10.625*u.deg, dec=41.2*u.deg, frame=\"icrs\"); c.galactic.l, c.galactic.b; c.separation(other)",
        descLong: "A `SkyCoord` knows its frame (`icrs` is the modern default — equivalent to FK5 J2000.0 to <0.1″). For horizon coordinates (alt/az), you must supply an `obstime` and an `EarthLocation`. Catalog cross-matching uses an internal KD-tree — `match_to_catalog_sky` returns indices + separations in O(N log N). Three depths solve the SAME task — find which Hipparcos catalog star is closest to a given RA/Dec — at depths: linear-search `separation` over 100 candidates → `match_to_catalog_sky` against the full catalog → multi-frame transform with `obstime` + galactic-coordinate filter + Hipparcos-vs-Gaia-DR3 epoch correction.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Find which of 5 candidate stars is closest to M31 (Andromeda).\n# APPROACH  - SkyCoord for M31, loop over candidates, .separation.\n# STRENGTHS - Demonstrates ICRS + separation.\n# WEAKNESSES- O(N) linear search; will be slow on real catalogs.\nimport astropy.units as u\nfrom astropy.coordinates import SkyCoord\n\nm31 = SkyCoord(ra=10.6847 * u.deg, dec=41.269 * u.deg, frame=\"icrs\")\n\ncandidates = SkyCoord(\n    ra=[10.0, 10.5, 10.7, 11.0, 12.0] * u.deg,\n    dec=[41.0, 41.2, 41.3, 41.0, 40.0] * u.deg,\n    frame=\"icrs\",\n)\nseps = candidates.separation(m31)                     # Angle (Quantity in deg)\ni = int(seps.argmin())\nprint(f\"closest is index {i}, sep = {seps[i].to(u.arcmin):.2f}\")\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — closest match — using match_to_catalog_sky on a\n#             larger pretend catalog.\n# APPROACH  - SkyCoord arrays + KD-tree match.\n# STRENGTHS - O(N log N) for matching; scales to millions.\n# WEAKNESSES- Returns ONE match per query - for k-NN use search_around_sky.\nimport astropy.units as u\nimport numpy as np\nfrom astropy.coordinates import SkyCoord\n\nrng = np.random.default_rng(42)\nn = 100_000\ncatalog = SkyCoord(\n    ra=rng.uniform(0, 360, size=n) * u.deg,\n    dec=rng.uniform(-90, 90, size=n) * u.deg,\n    frame=\"icrs\",\n)\n\nqueries = SkyCoord(\n    ra=[10.6847, 83.633, 187.706] * u.deg,            # M31, M1, M87\n    dec=[41.269, 22.014, 12.391]   * u.deg,\n    frame=\"icrs\",\n)\n\nidx, sep2d, _ = queries.match_to_catalog_sky(catalog)\nfor q, i, s in zip(queries, idx, sep2d):\n    print(f\"query=({q.ra:.3f}, {q.dec:.3f})  match={i}  sep={s.to(u.arcmin):.2f}\")\n\n# For all matches within X arcsec, use search_around_sky:\n# i_q, i_c, sep_2d, sep_3d = queries.search_around_sky(catalog, 30 * u.arcsec)\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — sky matching — production: ICRS + obstime, galactic\n#             filter (avoid the plane), Gaia-DR3-style proper motion epoch\n#             correction so old-catalog positions land on the new epoch.\n# APPROACH  - apply_space_motion(new_obstime); galactic latitude cut;\n#             match in target epoch.\n# STRENGTHS - Catches the \"the star moved\" bug that ruins old-catalog joins.\n# WEAKNESSES- Needs proper motions (pmra, pmdec) and obstime to do PM\n#             correction; falls back to no-correction when missing.\nfrom __future__ import annotations\nimport astropy.units as u\nimport numpy as np\nfrom astropy.coordinates import SkyCoord\nfrom astropy.time import Time\n\n\ndef cross_match(\n    queries: SkyCoord,\n    catalog: SkyCoord,\n    *,\n    target_obstime: Time | None = None,\n    galactic_latitude_cut_deg: float | None = 10.0,    # avoid Milky Way plane\n    max_sep: u.Quantity = 5 * u.arcsec,\n) -> tuple[np.ndarray, u.Quantity, np.ndarray]:\n    \"\"\"Return (catalog_idx, sep, mask) for queries in catalog.\n\n    catalog_idx: shape (N,), -1 where no match within max_sep.\n    sep:         Quantity, separation to the matched object.\n    mask:        boolean, True where a valid match exists.\n    \"\"\"\n    cat = catalog\n    # 1) Epoch correction: if catalog has space motion + obstime, propagate.\n    if target_obstime is not None and getattr(cat, \"obstime\", None) is not None:\n        try:\n            cat = cat.apply_space_motion(new_obstime=target_obstime)\n        except (ValueError, AttributeError):\n            pass                                        # missing PM/parallax - skip\n\n    # 2) Galactic-plane filter (optional).\n    if galactic_latitude_cut_deg is not None:\n        keep = np.abs(cat.galactic.b.to(u.deg).value) > galactic_latitude_cut_deg\n        cat = cat[keep]\n        keep_indices = np.where(keep)[0]\n    else:\n        keep_indices = np.arange(len(cat))\n\n    idx_local, sep, _ = queries.match_to_catalog_sky(cat)\n    mask = sep <= max_sep\n    catalog_idx = np.where(mask, keep_indices[idx_local], -1)\n    return catalog_idx, sep, mask\n\n\n# Demo\nrng = np.random.default_rng(7)\nn = 50_000\ncatalog = SkyCoord(\n    ra=rng.uniform(0, 360, n) * u.deg,\n    dec=rng.uniform(-30, 30, n) * u.deg,\n    frame=\"icrs\",\n)\nqueries = SkyCoord(\n    ra=[10.7, 83.6, 187.7] * u.deg,\n    dec=[41.27, 22.0, 12.4] * u.deg,\n    frame=\"icrs\",\n)\nidx, sep, mask = cross_match(queries, catalog,\n                             max_sep=5 * u.arcmin, galactic_latitude_cut_deg=10.0)\nfor q, i, s, ok in zip(queries, idx, sep, mask):\n    print(f\"q=({q.ra:.2f},{q.dec:.2f})  match={i}  sep={s.to(u.arcmin):.2f}  matched={ok}\")\n\n# Decision rule:\n#   Single coordinate                            -> SkyCoord(ra, dec, unit=, frame=).\n#   ICRS modern catalog (Gaia, etc.)             -> frame=\"icrs\" (default).\n#   Old FK4 / B1950 data                         -> frame=\"fk4\", equinox=\"B1950\".\n#   Galactic l/b                                 -> .transform_to(\"galactic\") or .galactic.\n#   Horizon (alt/az)                             -> AltAz frame + obstime + EarthLocation.\n#   Pairwise distance                            -> a.separation(b) -> Angle Quantity.\n#   N x M matching                               -> match_to_catalog_sky (1-NN).\n#   All within radius                            -> search_around_sky.\n#   Old catalog vs modern epoch                  -> apply_space_motion(new_obstime=...) when\n#                                                   PM and parallax are available.\n\n# Anti-pattern:\n#   SkyCoord(ra=10.685, dec=41.27)              # bare floats\n# Astropy raises if unit= is missing - the units are too important to\n# guess. Always pass unit=(u.deg, u.deg) (or u.hourangle for RA).\n"
                  }
        ],
        tips: [
                  "Always pass units to `SkyCoord` — bare floats raise; mixing degrees and hourangles is too easy without them.",
                  "`.transform_to(\"galactic\")` / `.galactic` gives Galactic `(l, b)`; `AltAz` needs `obstime` + `EarthLocation`.",
                  "`match_to_catalog_sky` is 1-NN; for all neighbors within a radius, use `search_around_sky`.",
                  "For epoch differences, call `apply_space_motion(new_obstime=...)` — Gaia DR3 catalog joined to legacy data is the canonical pitfall.",
                  "`SkyCoord` arrays support standard NumPy slicing — `coords[mask]`, `coords[i:j]` work."
        ],
        mistake: "Constructing a `SkyCoord` without explicit units — astropy raises (correctly) because `(10.685, 41.27)` could be degrees, hours, or radians. Always pass `unit=(u.deg, u.deg)` or `u.hourangle`.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
      {
        id: "astropy-fits-io",
        fn: "astropy.io.fits — read and write FITS files",
        desc: "FITS is the astronomy standard for images, spectra, and tables. Open with `fits.open(path)`; access HDUs by index or name (`hdul[0]`, `hdul[\"SCI\"]`); read header/data with `.header` / `.data`. **Always use a `with` block** — FITS uses memory-mapped data that goes stale when the file closes.",
        category: "astropy",
        subtitle: "fits.open as context manager, HDUList indexing (0-based; PrimaryHDU often empty for tables), .header (FITS keyword dict), .data (numpy ndarray for images, FITS_rec for tables), fits.getdata / fits.getheader (one-shot, no list management), BinTableHDU vs ImageHDU, WCS from header, lazy-loaded data via memmap",
        signature: "with fits.open(path) as hdul: hdr = hdul[0].header; img = hdul[0].data; tbl = hdul[\"EVENTS\"].data",
        descLong: "`fits.open(path)` returns an `HDUList` — a list of HDU objects, each with `.header` and `.data`. The primary HDU is index 0; named extensions can be accessed by index or by `EXTNAME`. By default `.data` is memory-mapped — fast, but the array becomes invalid the moment the file closes. Always use `with` blocks. For one-shot reads, `fits.getdata` and `fits.getheader` skip the HDUList overhead. Three depths solve the SAME task — read a FITS image and its WCS — at depths: bare `fits.getdata` → context-managed `fits.open` with header inspection → `WCS` reconstruction with sky-pixel conversions and a memmap-safe `.copy()`.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Read a FITS image; print shape and a header keyword.\n# APPROACH  - fits.getdata + fits.getheader (one-shot helpers).\n# STRENGTHS - Two lines.\n# WEAKNESSES- No HDU inspection; no WCS; no memmap-safe copy if you keep\n#             the array around for long.\nfrom astropy.io import fits\n\ndata = fits.getdata(\"image.fits\")                     # ndarray (H, W) or (H, W, ..)\nheader = fits.getheader(\"image.fits\")\nprint(\"shape:\", data.shape, \"dtype:\", data.dtype)\nprint(\"OBJECT:\", header.get(\"OBJECT\"))\nprint(\"EXPTIME:\", header.get(\"EXPTIME\"))\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — read FITS image — but inspect HDU layout and pull the\n#             WCS from the header.\n# APPROACH  - fits.open with a context manager; iterate HDUs.\n# STRENGTHS - Reveals multi-extension files; WCS-aware.\n# WEAKNESSES- Data is still memory-mapped; goes stale outside the with-block.\nfrom astropy.io import fits\nfrom astropy.wcs import WCS\n\nwith fits.open(\"image.fits\") as hdul:\n    print(hdul.info())                                # human-readable HDU table\n\n    primary = hdul[0]\n    sci = hdul[\"SCI\"] if \"SCI\" in [h.name for h in hdul] else primary\n\n    print(f\"OBJECT={sci.header.get('OBJECT')}  shape={sci.data.shape}\")\n\n    # Sky <-> pixel via WCS.\n    wcs = WCS(sci.header)\n    sky = wcs.pixel_to_world(100.5, 100.5)            # SkyCoord at pixel (100.5, 100.5)\n    print(\"sky at pixel (100.5, 100.5):\", sky)\n\n    # IMPORTANT: copy data BEFORE leaving the with-block if you want to\n    # use it later - memmap is invalidated on close.\n    img = sci.data.copy()\n\nprint(\"img shape (after close):\", img.shape)\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — FITS image + WCS — production: helper that returns a\n#             dict with image, header dict, WCS, and BinTable contents\n#             from any extension; safe across context-manager close.\n# APPROACH  - One pass, copy data eagerly, return typed dict.\n# STRENGTHS - Caller never has to know about memmap; WCS reconstructed\n#             once; tables come back as numpy structured arrays.\n# WEAKNESSES- Eager copy means peak RAM = file size briefly.\nfrom __future__ import annotations\nfrom astropy.io import fits\nfrom astropy.wcs import WCS\nfrom pathlib import Path\nimport numpy as np\n\n\ndef read_fits(path: Path, *, image_hdu: str | int = 0) -> dict:\n    \"\"\"Return image + WCS + header + tables, all detached from memmap.\"\"\"\n    out: dict = {\"path\": str(path), \"tables\": {}}\n    with fits.open(path, memmap=True) as hdul:\n        out[\"info\"] = [\n            {\"index\": i, \"name\": h.name, \"kind\": type(h).__name__,\n             \"shape\": getattr(h.data, \"shape\", None) if h.data is not None else None}\n            for i, h in enumerate(hdul)\n        ]\n\n        # Image extension - copy out of memmap immediately.\n        sci = hdul[image_hdu]\n        if sci.data is None:\n            raise RuntimeError(f\"HDU {image_hdu!r} has no data\")\n        out[\"image\"]  = sci.data.copy()\n        out[\"header\"] = dict(sci.header)\n        try:\n            out[\"wcs\"] = WCS(sci.header)\n        except Exception:\n            out[\"wcs\"] = None\n\n        # Pull all BinTableHDUs into structured arrays.\n        for h in hdul:\n            if isinstance(h, fits.BinTableHDU) and h.data is not None:\n                out[\"tables\"][h.name] = np.array(h.data, copy=True)\n    return out\n\n\ndef write_fits(path: Path, image: np.ndarray, *,\n               header: dict | None = None, overwrite: bool = False) -> None:\n    hdu = fits.PrimaryHDU(data=image)\n    if header:\n        for k, v in header.items():\n            hdu.header[k] = v\n    fits.HDUList([hdu]).writeto(path, overwrite=overwrite)\n\n\n# Use it\nresult = read_fits(Path(\"image.fits\"))\nimg = result[\"image\"]\nwcs: WCS = result[\"wcs\"]\nprint(\"image:\", img.shape, img.dtype)\nprint(\"WCS naxis:\", wcs.naxis if wcs else None)\n\n# Sky -> pixel for a known coordinate.\nif wcs is not None:\n    import astropy.units as u\n    from astropy.coordinates import SkyCoord\n    target = SkyCoord(ra=10.6847 * u.deg, dec=41.269 * u.deg, frame=\"icrs\")\n    px, py = wcs.world_to_pixel(target)\n    print(f\"M31 falls at pixel ({px:.1f}, {py:.1f})\")\n\nwrite_fits(Path(\"out.fits\"), img, header={\"OBJECT\": \"demo\"}, overwrite=True)\n\n# Decision rule:\n#   One-shot read of one HDU                  -> fits.getdata / fits.getheader.\n#   Multi-extension or unknown layout          -> fits.open + hdul.info() + with-block.\n#   Need WCS                                    -> astropy.wcs.WCS(header).\n#   FITS table                                  -> BinTableHDU.data is a record array;\n#                                                  np.array(...) copies it out of memmap.\n#   Need to keep data after close              -> .data.copy() or memmap=False.\n#   Streaming write                              -> fits.append (one HDU at a time).\n#   Compressed FITS (.fits.fz)                   -> handled transparently by fits.open.\n#   Header-only inspection                       -> fits.getheader (no data load).\n\n# Anti-pattern:\n#   data = fits.open(\"img.fits\")[0].data       # no with-block; memmap dangling\n# Outside a with-block the FITS file may close, and accessing .data later\n# raises a confusing OSError. Always use 'with' and .copy() if you need\n# the array to outlive the block.\n"
                  }
        ],
        tips: [
                  "Always use `with fits.open(path) as hdul:` — `.data` is memory-mapped and goes stale on close.",
                  "For one-shot reads, `fits.getdata(path, ext=N)` and `fits.getheader(path, ext=N)` skip the HDUList ceremony.",
                  "Tables are `BinTableHDU.data` — a NumPy record array. `np.array(tbl, copy=True)` decouples it from the memmap.",
                  "`astropy.wcs.WCS(header)` reconstructs world-pixel transforms; `wcs.pixel_to_world(x, y)` returns a `SkyCoord`.",
                  "`fits.append(path, hdu)` streams one HDU at a time — useful for incremental writes during a long pipeline."
        ],
        mistake: "Reading FITS data without a `with` block (`data = fits.open(path)[0].data`). The file may close before you use the array, and the memmap raises a cryptic `OSError`. Use `with fits.open(...) as hdul:` and `.copy()` if you need the data to outlive the block.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },

  // ── Section 2: NetworkX — graph algorithms ─────────────────────────────────────────
  {
    id: "networkx",
    title: "NetworkX — graph algorithms",
    entries: [
      {
        id: "networkx-graphs-paths",
        fn: "nx.Graph / DiGraph / shortest_path / centrality",
        desc: "NetworkX is pure-Python graphs: `Graph()` (undirected), `DiGraph()` (directed), `MultiGraph()`. Add nodes/edges (`add_edge(u, v, weight=)`), then run algorithms (`shortest_path`, `pagerank`, `betweenness_centrality`, `connected_components`). Beautiful API; **slow on >100k nodes** — switch to igraph or graph-tool for scale.",
        category: "networkx",
        subtitle: "nx.Graph (undirected) / DiGraph (directed) / MultiGraph (parallel edges), add_node / add_edge (weight=), shortest_path (Dijkstra default; weight=) and shortest_path_length, connected_components / strongly_connected_components, pagerank, betweenness_centrality, scale ceiling ~50-100k nodes (then igraph or graph-tool)",
        signature: "g = nx.Graph(); g.add_edge(\"a\",\"b\", weight=2); nx.shortest_path(g, \"a\", \"b\", weight=\"weight\"); nx.pagerank(g)",
        descLong: "Build a graph by adding edges; nodes are inferred. Edge attributes are kwargs (`weight=`, `color=`, anything). Algorithms accept a `weight=attribute_name` argument — Dijkstra over weighted edges, PageRank with edge weights, etc. Three depths solve the SAME task — find the shortest path between two cities through a road network — at depths: unweighted graph + `shortest_path` → weighted graph (km) + Dijkstra → directed graph with travel time, multi-source `single_source_dijkstra_path_length` for \"everywhere reachable in 30 min\".",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Find the shortest (fewest hops) path from A to D.\n# APPROACH  - Undirected nx.Graph + nx.shortest_path (BFS by default).\n# STRENGTHS - Three lines.\n# WEAKNESSES- Hop-count, not distance; \"shortest\" rarely means \"fewest hops\".\nimport networkx as nx\n\ng = nx.Graph()\ng.add_edges_from([(\"A\", \"B\"), (\"B\", \"C\"), (\"C\", \"D\"), (\"A\", \"D\")])\nprint(nx.shortest_path(g, \"A\", \"D\"))                  # ['A', 'D'] - direct edge wins\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — A to D — but with weighted edges (km between cities).\n# APPROACH  - add_edge(weight=...); shortest_path(weight=\"weight\").\n# STRENGTHS - Real distance-based shortest path (Dijkstra).\n# WEAKNESSES- Undirected; doesn't model one-way streets.\nimport networkx as nx\n\ng = nx.Graph()\nedges = [\n    (\"A\", \"B\", 5),\n    (\"B\", \"C\", 4),\n    (\"C\", \"D\", 2),\n    (\"A\", \"D\", 12),                                   # direct but longer\n    (\"B\", \"D\", 7),\n]\nfor u, v, w in edges:\n    g.add_edge(u, v, weight=w)\n\npath = nx.shortest_path(g, \"A\", \"D\", weight=\"weight\")\ndist = nx.shortest_path_length(g, \"A\", \"D\", weight=\"weight\")\nprint(path, \"total km:\", dist)                        # ['A', 'B', 'C', 'D']  total: 11\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — shortest path on a road network — production: directed\n#             graph (one-way streets), travel-time weights, multi-source\n#             \"isochrone\" (everywhere reachable in <= 30 min), centrality\n#             on the travel-time graph for hub identification.\n# APPROACH  - DiGraph with travel_time per edge; single_source_dijkstra\n#             gives the isochrone; betweenness_centrality finds bottlenecks.\n# STRENGTHS - Real-world directed/weighted modeling; reusable analyses.\n# WEAKNESSES- > ~50k nodes -> igraph or graph-tool (10-100x faster).\nfrom __future__ import annotations\nimport networkx as nx\nfrom typing import Iterable\n\n\ndef build_road_network(edges: Iterable[tuple[str, str, float]]) -> nx.DiGraph:\n    \"\"\"edges: (origin, destination, travel_time_minutes). One-way edges only.\"\"\"\n    g = nx.DiGraph()\n    for u, v, t in edges:\n        g.add_edge(u, v, travel_time=t)\n    return g\n\n\ndef isochrone(g: nx.DiGraph, origin: str, max_minutes: float) -> dict[str, float]:\n    \"\"\"All nodes reachable from origin within max_minutes -> {node: time}.\"\"\"\n    return nx.single_source_dijkstra_path_length(\n        g, origin, cutoff=max_minutes, weight=\"travel_time\",\n    )\n\n\ndef hubs(g: nx.DiGraph, k: int = 5) -> list[tuple[str, float]]:\n    \"\"\"Top-k nodes by travel-time-weighted betweenness centrality.\"\"\"\n    bc = nx.betweenness_centrality(g, weight=\"travel_time\", normalized=True)\n    return sorted(bc.items(), key=lambda kv: kv[1], reverse=True)[:k]\n\n\n# --- Demo ---\nedges = [\n    (\"A\", \"B\", 5),  (\"B\", \"A\", 5),\n    (\"B\", \"C\", 8),  (\"C\", \"B\", 8),\n    (\"C\", \"D\", 3),                                    # one-way!\n    (\"A\", \"D\", 25),\n    (\"B\", \"D\", 12), (\"D\", \"B\", 12),\n    (\"D\", \"E\", 4),  (\"E\", \"D\", 4),\n]\ng = build_road_network(edges)\n\nprint(\"shortest A -> D:\", nx.shortest_path(g, \"A\", \"D\", weight=\"travel_time\"),\n      \"time:\", nx.shortest_path_length(g, \"A\", \"D\", weight=\"travel_time\"))\n\n# Everywhere reachable from A within 20 minutes:\niso = isochrone(g, \"A\", 20.0)\nprint(\"20-min isochrone from A:\", iso)\n\n# Top-3 hub nodes by betweenness on travel time:\nprint(\"top hubs:\", hubs(g, k=3))\n\n# Decision rule:\n#   < 50k nodes, productivity over speed              -> NetworkX (Pythonic API).\n#   100k-10M nodes                                     -> igraph (C core, Python bindings).\n#   10M+ nodes / billion edges                         -> graph-tool (C++ + OpenMP) or\n#                                                       cuGraph (GPU).\n#   Need shortest path                                 -> nx.shortest_path(weight=).\n#   All-pairs shortest paths                            -> nx.floyd_warshall_numpy\n#                                                       (dense; O(n^3 * 8 bytes)).\n#   Weighted PageRank / centrality                     -> nx.pagerank / betweenness_centrality\n#                                                       with weight=.\n#   Communities                                         -> nx.community.louvain_communities (3.x)\n#                                                       or python-louvain.\n#   Persist a graph                                     -> nx.write_gpickle / write_gml /\n#                                                       write_graphml; for parquet, use NetworkX\n#                                                       to_pandas_edgelist.\n\n# Anti-pattern:\n#   for i in range(1_000_000):\n#       g.add_edge(...)                              # NetworkX nodes are dicts of dicts\n# 1M edges in NetworkX easily takes 10 GB and 5 minutes. For that scale,\n# build with igraph (g = ig.Graph(edges=edges)) or graph-tool.\n"
                  }
        ],
        tips: [
                  "Use `nx.Graph` for undirected, `nx.DiGraph` for directed, `nx.MultiGraph` if parallel edges have different attributes.",
                  "Edge attributes are arbitrary kwargs; `nx.shortest_path(..., weight=\"travel_time\")` references the attribute name.",
                  "`single_source_dijkstra_path_length(g, src, cutoff=X)` is the right tool for \"everywhere reachable in ≤ X\" (isochrones, broadcast radius).",
                  "Betweenness centrality is O(V·E) — slow on big graphs; use approximate `betweenness_centrality(k=samples)` past ~10k nodes.",
                  "NetworkX caps out around 50-100k nodes for productivity; switch to **igraph** (10-100× faster) or **graph-tool** (C++) for scale."
        ],
        mistake: "Building a million-edge graph in NetworkX. Each node is a Python dict-of-dicts — memory and runtime explode. Use **igraph** for >50k nodes.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },

  // ── Section 3: SymPy — exact symbolic math ─────────────────────────────────────────
  {
    id: "sympy",
    title: "SymPy — exact symbolic math",
    entries: [
      {
        id: "sympy-symbolic-math",
        fn: "symbols / solve / diff / integrate / lambdify",
        desc: "SymPy is a CAS in pure Python: declare `symbols(\"x y\")`, build expressions, simplify, differentiate, integrate, solve. Convert symbolic expressions into fast NumPy callables with `lambdify(args, expr, \"numpy\")`. The right tool for **exact** answers; for numeric work at scale, hand off to NumPy/JAX.",
        category: "sympy",
        subtitle: "sp.symbols (real=True, positive=True for tighter assumptions), sp.Eq (symbolic equality), sp.solve / nsolve / linsolve, sp.diff (partial derivatives via tuples), sp.integrate (definite via tuple bounds), sp.simplify / expand / factor / cancel, sp.lambdify (compile to numpy / jax / cython), sp.latex for typesetting",
        signature: "x, y = sp.symbols(\"x y\", real=True); sol = sp.solve([eq1, eq2], [x, y]); f = sp.lambdify((x, y), expr, \"numpy\")",
        descLong: "SymPy treats symbols as algebraic variables — operations build expression trees rather than evaluating. `solve` handles polynomial systems analytically (use `nsolve` for numerical root-finding when there's no closed form). `diff(expr, x)` differentiates; `diff(expr, x, 2)` is the second derivative; `diff(expr, x, y)` is mixed partial. `integrate(expr, x)` is indefinite, `integrate(expr, (x, a, b))` is definite. `lambdify` compiles a SymPy expression to fast NumPy/JAX/cython — bridge to numerical pipelines. Three depths solve the SAME task — solve a quadratic, differentiate, evaluate at points — at depths: bare solve + diff (printed) → assumptions (real, positive) + simplify + lambdify → full optimization workflow with Newton's method on the symbolic gradient.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Solve x^2 - 5x + 6 = 0; differentiate the LHS; evaluate at x=4.\n# APPROACH  - sp.symbols + sp.solve + sp.diff.\n# STRENGTHS - Symbolic; exact roots.\n# WEAKNESSES- No assumptions => may return complex roots in general.\nimport sympy as sp\n\nx = sp.symbols(\"x\")\nexpr = x**2 - 5*x + 6\n\nroots = sp.solve(expr, x)\nderiv = sp.diff(expr, x)\nprint(\"roots:\", roots)                                # [2, 3]\nprint(\"derivative:\", deriv)                           # 2*x - 5\nprint(\"derivative at 4:\", deriv.subs(x, 4))           # 3\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — solve, differentiate, evaluate — but use assumptions\n#             (real) and lambdify the derivative for fast NumPy evaluation.\n# APPROACH  - symbols(real=True) + lambdify(numpy).\n# STRENGTHS - Tighter assumption set; symbolic -> compiled NumPy callable.\n# WEAKNESSES- Wider expressions can be slow to lambdify; cache the result.\nimport sympy as sp\nimport numpy as np\n\nx = sp.symbols(\"x\", real=True)\nexpr = x**2 - 5*x + 6\n\nroots = sp.solve(expr, x)\nderiv = sp.diff(expr, x)\nprint(\"roots:\", roots)\nprint(\"derivative:\", deriv)\n\n# Compile the derivative to a NumPy-vectorized function.\nf = sp.lambdify(x, deriv, \"numpy\")\nxs = np.linspace(0, 10, 5)\nprint(\"derivative at xs:\", f(xs))                     # vectorized: [-5, -2.5, 0, 2.5, 5]\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — work with f(x) = x^2 - 5x + 6 — production: solve\n#             AND optimize via Newton's method on the symbolic gradient,\n#             with a SymPy-derived Jacobian and lambdified callables.\n# APPROACH  - Symbolic gradient + Hessian via sp.diff; lambdify both;\n#             scipy.optimize.newton or root_scalar for numeric solve.\n# STRENGTHS - Exact derivatives (no finite-difference noise); fast eval\n#             via NumPy backend.\n# WEAKNESSES- For many-variable problems you still want JAX autodiff -\n#             SymPy is for moderate symbolic expressions, not deep nets.\nfrom __future__ import annotations\nimport sympy as sp\nimport numpy as np\nfrom scipy.optimize import newton\n\n\n# Symbolic problem: minimize f(x, y) = (x - 3)^2 + (y + 1)^2 + sin(x) cos(y)\nx, y = sp.symbols(\"x y\", real=True)\nf = (x - 3)**2 + (y + 1)**2 + sp.sin(x) * sp.cos(y)\n\ngrad = sp.Matrix([sp.diff(f, x), sp.diff(f, y)])      # 2-vector\nhess = sp.hessian(f, (x, y))                          # 2x2 matrix\nprint(\"grad:\", grad.T)\nprint(\"hess:\", hess)\n\n# Compile to NumPy.\nf_np    = sp.lambdify((x, y), f,    \"numpy\")\ngrad_np = sp.lambdify((x, y), grad, \"numpy\")\nhess_np = sp.lambdify((x, y), hess, \"numpy\")\n\n\ndef newton_step(p: np.ndarray) -> np.ndarray:\n    g = np.array(grad_np(*p), dtype=float).flatten()\n    H = np.array(hess_np(*p), dtype=float)\n    return p - np.linalg.solve(H, g)\n\n\np = np.array([0.0, 0.0])\nfor i in range(15):\n    p_new = newton_step(p)\n    if np.linalg.norm(p_new - p) < 1e-10:\n        break\n    p = p_new\nprint(f\"converged at p = {p}, f(p) = {f_np(*p):.6f}\")\n\n# 1D case via scipy.optimize.newton on the lambdified derivative:\ng1d = sp.diff((x - 3)**2 + sp.sin(x), x)\ng1d_np = sp.lambdify(x, g1d, \"numpy\")\nroot = newton(g1d_np, x0=0.0, tol=1e-10)\nprint(f\"1D minimizer ~ {root:.6f}\")\n\n# Decision rule:\n#   Need EXACT roots / closed form              -> sp.solve.\n#   Numeric root only                            -> sp.nsolve(eq, var, x0) or scipy.optimize.\n#   Need exact derivatives                       -> sp.diff (single var) or sp.Matrix +\n#                                                   sp.diff for Jacobians.\n#   Need to USE the result in a hot loop         -> sp.lambdify(args, expr, \"numpy\").\n#   Symbolic expression too big for lambdify     -> common cause of slowness; sp.simplify\n#                                                   or sp.cse (common sub-expression elim) first.\n#   Need autodiff with deep nets                  -> JAX (autograd) or PyTorch.\n#   Need integrals you can also evaluate          -> sp.integrate + lambdify;\n#                                                   for purely numeric, scipy.integrate.quad.\n#   Need typeset output                            -> sp.latex(expr) for LaTeX strings.\n\n# Anti-pattern:\n#   for i in range(1_000_000):\n#       y = expr.subs(x, i).evalf()                  # symbolic in a hot loop\n# .subs + .evalf is slow (Python overhead per call). Always lambdify ONCE\n# and call the compiled function on a NumPy array.\n"
                  }
        ],
        tips: [
                  "Declare assumptions on symbols (`real=True`, `positive=True`, `integer=True`) — solve and simplify give tighter results.",
                  "Use `sp.simplify`, `sp.expand`, `sp.factor`, `sp.cancel`, `sp.together` to reshape expressions (and avoid each other when possible).",
                  "`sp.lambdify((x, y), expr, \"numpy\")` compiles a symbolic expression into a vectorized NumPy callable — the bridge to fast numeric work.",
                  "For exact rational arithmetic, use `sp.Rational(1, 3)`, not `1/3` (which is a Python float).",
                  "For systems of equations, `sp.linsolve` (linear) is much faster than `sp.solve` and returns parametric solutions cleanly."
        ],
        mistake: "Calling `expr.subs(x, val).evalf()` inside a hot loop. Each call rebuilds the expression tree. **Lambdify once**, then call the compiled function on NumPy arrays.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },

  // ── Section 4: When to reach for which scientific stack ─────────────────────────────────────────
  {
    id: "patterns",
    title: "When to reach for which scientific stack",
    entries: [
      {
        id: "scientific-stack-decision",
        fn: "astropy vs scipy vs sympy vs JAX vs igraph vs graph-tool",
        desc: "astropy = astronomy domain (units, time, coords, FITS, WCS). scipy = numerical generalist (linalg, ODEs, optimization, statistics). sympy = exact symbolic. JAX = autodiff + GPU/TPU NumPy. igraph / graph-tool = graphs at scale. The Python role is glue across these — no single library does everything.",
        category: "patterns",
        subtitle: "astropy (units + Quantity, SkyCoord, FITS, Time, WCS - astronomy-domain), scipy (linalg, integrate, optimize, signal, stats, sparse - generalist), sympy (exact symbolic CAS), JAX (autodiff + jit + vmap, GPU/TPU; lax_numpy interop), igraph (C-core, 10-100x NetworkX), graph-tool (C++ + OpenMP, fastest), specialized (sunpy for solar, pyteomics for mass-spec, biopython for sequences)",
        signature: "# astronomy units/time/coords  -> astropy\\n# numerical methods            -> scipy\\n# exact algebra                -> sympy\\n# autodiff + GPU               -> JAX\\n# graphs at scale              -> igraph or graph-tool",
        descLong: "Each library solves a different problem. astropy is the astronomy-domain Swiss army knife — units, time scales, sky coordinates, FITS, WCS. scipy is the workhorse for numeric methods (linalg, optimize, integrate, signal, stats). sympy gives exact symbolic answers. JAX is NumPy-on-XLA with autodiff and GPU/TPU — the modern replacement for autograd. For graphs, igraph (C core) is 10-100× faster than NetworkX; graph-tool (C++ + OpenMP) is the fastest. Three depths solve the SAME task — fit a 2D Gaussian to an image — at depths: scipy.optimize.curve_fit (CPU, finite-diff Jacobian) → JAX with jit + autodiff (fast on big images, GPU-able) → astropy.modeling LevMarLSQFitter for FITS + units integration.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Fit a 2D Gaussian to a small image with scipy.\n# APPROACH  - Define model + scipy.optimize.curve_fit.\n# STRENGTHS - Single library; works for any model.\n# WEAKNESSES- Finite-difference Jacobian; slow for big images;\n#             no autodiff; no GPU.\nimport numpy as np\nfrom scipy.optimize import curve_fit\n\n\ndef gauss2d(xy, amp, x0, y0, sx, sy, offset):\n    x, y = xy\n    return offset + amp * np.exp(-(((x - x0) / sx)**2 + ((y - y0) / sy)**2) / 2)\n\n\n# Synthetic image with a Gaussian + noise.\nrng = np.random.default_rng(0)\nH = W = 64\ny, x = np.mgrid[:H, :W]\ntrue_params = (10.0, 31.5, 31.5, 5.0, 7.0, 1.0)\nimg = gauss2d((x, y), *true_params) + rng.normal(0, 0.5, (H, W))\n\nxy = np.vstack([x.ravel(), y.ravel()])\npopt, _ = curve_fit(gauss2d, xy, img.ravel(), p0=(8, 30, 30, 4, 4, 0))\nprint(\"fitted:\", popt)\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — Gaussian fit — but using JAX (autodiff + jit).\n# APPROACH  - jax.jit + jax.grad; minimize with optax or scipy on the\n#             jit'd cost.\n# STRENGTHS - Exact gradient; jit gives near-C speed; transparently moves\n#             to GPU with jax.devices.\n# WEAKNESSES- JAX overhead for tiny problems; learning curve for vmap/jit.\nimport jax\nimport jax.numpy as jnp\nfrom jax import jit, grad\nimport numpy as np\nfrom scipy.optimize import minimize\n\n\ndef gauss2d_jax(params, x, y):\n    amp, x0, y0, sx, sy, offset = params\n    return offset + amp * jnp.exp(-(((x - x0)/sx)**2 + ((y - y0)/sy)**2) / 2)\n\n\ndef cost(params, x, y, img):\n    pred = gauss2d_jax(params, x, y)\n    return jnp.mean((pred - img)**2)\n\n\n# JIT-compile cost + its gradient.\ncost_jit = jit(cost)\ngrad_jit = jit(grad(cost, argnums=0))\n\nH = W = 64\ny_arr, x_arr = jnp.mgrid[:H, :W].astype(jnp.float32)\ntrue_params = jnp.array([10.0, 31.5, 31.5, 5.0, 7.0, 1.0])\nrng = np.random.default_rng(0)\nimg = np.array(gauss2d_jax(true_params, x_arr, y_arr)) + rng.normal(0, 0.5, (H, W))\nimg_j = jnp.asarray(img)\n\np0 = np.array([8., 30., 30., 4., 4., 0.])\nres = minimize(\n    lambda p: float(cost_jit(jnp.asarray(p), x_arr, y_arr, img_j)),\n    p0,\n    jac=lambda p: np.asarray(grad_jit(jnp.asarray(p), x_arr, y_arr, img_j)),\n    method=\"BFGS\",\n)\nprint(\"fitted:\", res.x)\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — 2D Gaussian fit — production: astropy modeling for\n#             FITS-aware fits with units, plus a JAX vmap-batched solver\n#             for fitting MANY sources in one go.\n# APPROACH  - astropy.modeling for unit-aware single fits; JAX vmap for\n#             batched fits across image stacks.\n# STRENGTHS - Two patterns side by side; pick the right tool per use case.\n# WEAKNESSES- Two libraries to know; JAX install (CPU/GPU) is heavier\n#             than scipy.\nimport astropy.units as u\nfrom astropy.modeling import models, fitting\nimport numpy as np\n\n# --- Astropy path: unit-aware, FITS-friendly ---\ny, x = np.mgrid[:64, :64]\ntrue = models.Gaussian2D(amplitude=10, x_mean=31.5, y_mean=31.5,\n                         x_stddev=5, y_stddev=7)\nrng = np.random.default_rng(0)\nimg = true(x, y) + rng.normal(0, 0.5, (64, 64))\n\ng_init = models.Gaussian2D(amplitude=8, x_mean=30, y_mean=30,\n                           x_stddev=4, y_stddev=4)\nfitter = fitting.LevMarLSQFitter()\ng_fit  = fitter(g_init, x, y, img)\nprint(\"astropy fit:\", g_fit)\n\n\n# --- JAX path: batched fits across many small images ---\nimport jax\nimport jax.numpy as jnp\nfrom jax import jit, vmap\nimport optax\n\n\ndef make_grid(H, W):\n    yy, xx = jnp.mgrid[:H, :W].astype(jnp.float32)\n    return xx, yy\n\n\ndef gauss2d(params, xx, yy):\n    amp, x0, y0, sx, sy, off = params\n    return off + amp * jnp.exp(-(((xx - x0)/sx)**2 + ((yy - y0)/sy)**2) / 2)\n\n\ndef loss(params, xx, yy, img):\n    return jnp.mean((gauss2d(params, xx, yy) - img) ** 2)\n\n\n# Single-image fit step (Adam).\nloss_grad = jax.value_and_grad(loss)\n\n\n@jit\ndef step(params, opt_state, xx, yy, img, opt):\n    val, g = loss_grad(params, xx, yy, img)\n    updates, opt_state = opt.update(g, opt_state, params)\n    return optax.apply_updates(params, updates), opt_state, val\n\n\ndef fit_one(img, n_iters: int = 200):\n    H, W = img.shape\n    xx, yy = make_grid(H, W)\n    params = jnp.array([img.max(), W / 2, H / 2, W / 8, H / 8, img.min()])\n    opt = optax.adam(0.1)\n    opt_state = opt.init(params)\n    for _ in range(n_iters):\n        params, opt_state, _ = step(params, opt_state, xx, yy, img, opt)\n    return params\n\n\n# Fit a STACK of N images in parallel via vmap.\nfit_batch = jit(vmap(fit_one))\n\n\n# stack: (N, H, W)\ntrue_params = jnp.array([\n    [10, 31.5, 31.5, 5, 7, 1],\n    [ 8, 20.0, 40.0, 4, 4, 0],\n    [12, 45.0, 15.0, 3, 6, 0],\n])\nxx, yy = make_grid(64, 64)\nstack = jnp.stack([gauss2d(p, xx, yy) for p in true_params])\nstack = stack + jax.random.normal(jax.random.PRNGKey(0), stack.shape) * 0.5\n\nfits = fit_batch(stack)\nprint(\"jax batched fits:\\n\", np.array(fits))\n\n# Decision rule:\n#   Domain-specific astronomy ops               -> astropy (units / coords / FITS / time).\n#   Generic numerical methods                    -> scipy (linalg / optimize / integrate / stats).\n#   Exact symbolic                               -> sympy.\n#   Autodiff + GPU                               -> JAX (jit / grad / vmap).\n#   Train deep nets                              -> PyTorch.\n#   Graph algorithms                             -> NetworkX (small) -> igraph -> graph-tool.\n#   Spark-scale arrays                            -> Dask or Ray.\n#   Out-of-core arrays                            -> Zarr / xarray + dask.\n#   Solar physics                                -> sunpy (built on astropy).\n#   Mass spec                                    -> pyteomics.\n#   Time series tabular                          -> pandas / polars.\n\n# Anti-pattern:\n#   re-implementing scipy.optimize.minimize in pure Python \"for clarity\"\n# scipy.optimize is decades of optimization research. Either USE it or\n# move to JAX/jaxopt for autodiff. Don't roll your own gradient descent\n# unless the topic IS gradient descent.\n"
                  }
        ],
        tips: [
                  "**astropy** owns the astronomy domain: units, time scales, coordinate frames, FITS, WCS — the right tool the moment your data has a sky position.",
                  "**scipy** is the numerical generalist: linalg, optimize, integrate, signal, sparse, stats. Reach for it before you reach for \"I'll just write the algorithm myself\".",
                  "**SymPy** for exact symbolic answers; **JAX** for autodiff + GPU on numerical pipelines; **PyTorch** for deep nets.",
                  "For graphs: NetworkX (productivity, small) → igraph (10-100× faster) → graph-tool (C++ + OpenMP, fastest).",
                  "Domain-specific siblings: **sunpy** (solar, built on astropy), **pyteomics** (mass spec), **biopython** (sequences) — let them carry the domain knowledge."
        ],
        mistake: "Re-implementing `scipy.optimize.minimize` \"for clarity\". scipy carries decades of optimization research. Either use it or move to JAX/jaxopt for autodiff — don't roll your own gradient descent unless that's the topic.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },
]

export default { meta, sections }

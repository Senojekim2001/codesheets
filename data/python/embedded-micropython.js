export const meta = {
  "id": "embedded-micropython",
  "label": "MicroPython / Embedded",
  "icon": "🔌",
  "description": "MicroPython on microcontrollers (ESP32, RP2040 / Pico, STM32) and CircuitPython (Adafruit's fork, friendlier UX). 256 KB of RAM and a few MB of flash. The four things you keep getting wrong: importing the wrong stdlib (`time` exists but most modules are `u<name>` or stripped down), allocating in interrupt handlers (forbidden), using `print()` for binary data over the REPL (corrupts the serial line), and forgetting that floats are 32-bit (single precision) on most ports."
}

export const sections = [

  // ── Section 1: MicroPython vs CPython — what's different ─────────────────────────────────────────
  {
    id: "core",
    title: "MicroPython vs CPython — what's different",
    entries: [
      {
        id: "micropython-vs-cpython",
        fn: "gc / memory / async — what to do differently",
        desc: "MicroPython is ~95% Python-the-language with a stripped-down stdlib. Floats are 32-bit; integers can be small-ints (no allocation) or arbitrary-precision (heap). RAM is the bottleneck — preallocate buffers, watch `gc.mem_free()`, and use `const()` for compile-time constants. `uasyncio` exists and is lighter than CPython's `asyncio`.",
        category: "core",
        subtitle: "gc.mem_free / mem_alloc / collect, micropython.const for compile-time constants, bytearray pre-allocation, uasyncio (smaller event loop), float = 32-bit (most ports), no Decimal / fractions / multiprocessing, time vs utime (alias on most ports), print() over USB/UART vs binary",
        signature: "import gc; gc.collect(); gc.mem_free(); from micropython import const; PIN = const(2); buf = bytearray(64)",
        descLong: "The big differences: small RAM (typically 256 KB on ESP32, 264 KB on RP2040), 32-bit floats (no `float64`), no `multiprocessing` / `threading.Thread` on most boards (RP2040 has two cores via `_thread`), `uasyncio` instead of `asyncio` (same API, smaller). Use `micropython.const()` so the value gets inlined at compile time (no global-table lookup). Preallocate `bytearray(N)` instead of growing strings. Three depths solve the SAME task — read a sensor every 100 ms and average over 1 s — at depths: naive list-append + sum (heap churn) → preallocated bytearray + manual ring index → uasyncio task with bytearray + const + explicit gc.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Read an ADC value every 100 ms and average over the last 10 reads.\n# APPROACH  - Append to a list; sum / len.\n# STRENGTHS - Reads like CPython.\n# WEAKNESSES- list.append + slicing churns the heap; gc runs constantly,\n#             pauses your loop unpredictably.\nimport time\nfrom machine import ADC, Pin\n\nadc = ADC(Pin(34))                                    # ESP32 ADC pin\nwindow = []\n\nwhile True:\n    window.append(adc.read())                          # 0..4095\n    if len(window) > 10:\n        window = window[-10:]                          # !! reallocates list\n    print(\"avg =\", sum(window) / len(window))\n    time.sleep_ms(100)\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — averaged ADC — but with a preallocated buffer + ring\n#             index so no heap allocation in the hot loop.\n# APPROACH  - bytearray-style fixed buffer + manual index; const for size.\n# STRENGTHS - Steady RAM use; no GC pauses inside the loop.\n# WEAKNESSES- Synchronous; blocks on time.sleep.\nimport time\nimport gc\nfrom machine import ADC, Pin\nfrom micropython import const\n\nWIN = const(10)                                        # inlined at compile\n\nadc = ADC(Pin(34))\nbuf = [0] * WIN                                        # preallocated\ni   = 0\nfilled = 0\n\nwhile True:\n    buf[i] = adc.read()\n    i = (i + 1) % WIN\n    if filled < WIN:\n        filled += 1\n    avg = sum(buf[:filled]) // filled                  # avoid float division if you can\n    print(\"avg =\", avg, \"free =\", gc.mem_free())\n    time.sleep_ms(100)\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — averaged ADC — production: uasyncio task, periodic\n#             gc.collect, exposes the latest average via a shared module\n#             attribute that other tasks can read without copying.\n# APPROACH  - uasyncio.create_task; explicit gc; bytearray with manual sum\n#             across the ring; no float division.\n# STRENGTHS - Multiple async tasks (sensor + WiFi + display) in 256 KB.\n# WEAKNESSES- Need to internalize uasyncio idioms; no preemption.\nimport uasyncio as asyncio\nimport gc\nfrom machine import ADC, Pin\nfrom micropython import const\n\nWIN          = const(20)\nPERIOD_MS    = const(100)\nGC_EVERY_MS  = const(2000)\n\n\nclass RollingMean:\n    __slots__ = (\"buf\", \"i\", \"filled\", \"_sum\")\n\n    def __init__(self, n: int) -> None:\n        self.buf    = [0] * n\n        self.i      = 0\n        self.filled = 0\n        self._sum   = 0\n\n    def push(self, v: int) -> None:\n        # Keep a running sum -> O(1) update.\n        old = self.buf[self.i]\n        self._sum += v - old\n        self.buf[self.i] = v\n        self.i = (self.i + 1) % len(self.buf)\n        if self.filled < len(self.buf):\n            self.filled += 1\n            self._sum += old                          # we subtracted a \"fake\" zero\n\n    @property\n    def value(self) -> int:\n        return self._sum // max(1, self.filled)\n\n\nSENSOR_AVG = RollingMean(WIN)\n\n\nasync def sensor_task(pin: int) -> None:\n    adc = ADC(Pin(pin))\n    while True:\n        SENSOR_AVG.push(adc.read())\n        await asyncio.sleep_ms(PERIOD_MS)\n\n\nasync def gc_task() -> None:\n    while True:\n        await asyncio.sleep_ms(GC_EVERY_MS)\n        gc.collect()                                  # bound peak RAM\n\n\nasync def reporter_task() -> None:\n    while True:\n        print(\"avg =\", SENSOR_AVG.value, \"free =\", gc.mem_free())\n        await asyncio.sleep_ms(1000)\n\n\nasync def main() -> None:\n    asyncio.create_task(sensor_task(34))\n    asyncio.create_task(gc_task())\n    await reporter_task()\n\n\nasyncio.run(main())\n\n# Decision rule:\n#   Stable hot loop                     -> preallocate (bytearray / list of fixed size).\n#   Constants the compiler can inline    -> from micropython import const.\n#   Multiple I/O tasks                   -> uasyncio (NOT threading on most boards).\n#   Need pause-free hot loop             -> avoid heap allocation inside it; gc.collect()\n#                                            on a separate periodic task.\n#   Need <1ms response                   -> machine.Timer.PERIODIC (hardware timer)\n#                                            with a tiny ISR; do NOT allocate in ISR.\n#   Need to dump telemetry                -> use struct.pack into a bytearray; print()\n#                                            of a string of digits is slow + GC-heavy.\n#   Two cores (RP2040)                    -> _thread.start_new_thread for the second core;\n#                                            communicate via _thread.allocate_lock.\n\n# Anti-pattern:\n#   def isr(pin):\n#       data.append(adc.read())            # heap allocation INSIDE an interrupt\n# Heap allocation in an ISR is FORBIDDEN in MicroPython - you'll get\n# MemoryError or a board reset. Use micropython.schedule(callback, arg)\n# inside the ISR; the callback runs on the main thread where allocation\n# is safe.\n"
                  }
        ],
        tips: [
                  "RAM is the constraint — preallocate `bytearray(N)` / `list_of_n_zeros` and reuse them in hot loops.",
                  "`from micropython import const` makes `const(VALUE)` a compile-time constant — saves a global-table lookup per access.",
                  "Use `uasyncio` (the MicroPython port of asyncio) for cooperative multitasking — `_thread` is only on RP2040.",
                  "No allocation inside interrupt handlers — schedule a Python callback with `micropython.schedule(cb, arg)`.",
                  "Most ports use 32-bit floats — precision past ~7 decimal digits is gone. Use integers when possible."
        ],
        mistake: "Allocating inside an interrupt handler (`buf.append(...)`) — MicroPython forbids it and you'll see `MemoryError` or a hard reset. Defer work via `micropython.schedule(cb, arg)`.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },

  // ── Section 2: Hardware peripherals — GPIO, PWM, ADC, I2C, SPI ─────────────────────────────────────────
  {
    id: "hardware",
    title: "Hardware peripherals — GPIO, PWM, ADC, I2C, SPI",
    entries: [
      {
        id: "micropython-gpio-pwm-adc",
        fn: "machine.Pin / PWM / ADC — digital + analog I/O",
        desc: "The `machine` module is the hardware abstraction: `Pin(num, Pin.IN/OUT, Pin.PULL_UP)` for digital I/O, `PWM(Pin)` for analog-out, `ADC(Pin)` for analog-in. Pin numbering is **board-specific** — ESP32 GPIO numbers, Pico GPxx numbers, STM32 letter+number combinations.",
        category: "hardware",
        subtitle: "machine.Pin (Pin.IN, Pin.OUT, Pin.PULL_UP, Pin.PULL_DOWN), .value() / .on() / .off() / .toggle(), Pin.irq(trigger=Pin.IRQ_FALLING, handler=) for input edges, machine.PWM(pin, freq=, duty_u16=), machine.ADC(pin) returns 0..65535 (16-bit normalized) on most modern ports, board pin labels (ESP32 GPIO34 = ADC1_6)",
        signature: "p = Pin(2, Pin.OUT); p.on(); p = Pin(0, Pin.IN, Pin.PULL_UP); p.irq(handler=cb, trigger=Pin.IRQ_FALLING); pwm = PWM(Pin(15), freq=1000, duty_u16=32768)",
        descLong: "Digital I/O is `Pin(num, mode, pull)` — `IN`, `OUT`, optional pull-up/down. Read with `.value()` (0/1), write with `.on()` / `.off()` / `.value(0|1)`. Inputs can attach an interrupt handler with `.irq(handler=cb, trigger=Pin.IRQ_RISING|Pin.IRQ_FALLING)`. PWM ramps a digital pin into an analog-looking signal — use `freq=` (Hz) and `duty_u16=` (0..65535). ADC reads analog voltage; modern ports normalize to 0..65535 via `.read_u16()`. Three depths solve the SAME task — read a button (debounced) and dim an LED with a potentiometer — at depths: bare poll loop → IRQ-driven button + EMA-smoothed ADC → debounced IRQ + uasyncio brightness ramp.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Button toggles LED; potentiometer sets LED brightness.\n# APPROACH  - Polling loop reads pin + ADC every 50 ms.\n# STRENGTHS - 10 lines.\n# WEAKNESSES- Button bounces (multiple reads per press); no smoothing on ADC;\n#             can miss short presses if loop is busy.\nimport time\nfrom machine import Pin, PWM, ADC\n\nled_pwm = PWM(Pin(2), freq=1000, duty_u16=0)         # ESP32: GPIO2 onboard LED\nbutton  = Pin(0, Pin.IN, Pin.PULL_UP)                # active-low\npot     = ADC(Pin(34))                                # ADC1 channel 6 on ESP32\n\nled_on = False\nprev_btn = 1                                          # PULL_UP idle = 1\n\nwhile True:\n    btn = button.value()\n    if prev_btn == 1 and btn == 0:                    # falling edge\n        led_on = not led_on\n    prev_btn = btn\n\n    raw = pot.read_u16()                              # 0..65535\n    led_pwm.duty_u16(raw if led_on else 0)\n\n    time.sleep_ms(50)\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — button + potentiometer — but IRQ-driven button (no\n#             missed presses) + exponential-moving-average smoothing on ADC.\n# APPROACH  - Pin.irq(trigger=FALLING) sets a flag; main loop services it.\n# STRENGTHS - Catches every press; smoother brightness; cheap CPU.\n# WEAKNESSES- Bounce still bites if you press fast (use senior debounce).\nimport time\nfrom machine import Pin, PWM, ADC\n\n\nclass App:\n    def __init__(self):\n        self.led_pwm = PWM(Pin(2), freq=1000, duty_u16=0)\n        self.button  = Pin(0, Pin.IN, Pin.PULL_UP)\n        self.pot     = ADC(Pin(34))\n        self.led_on  = False\n        self.flag    = False\n        self.smoothed = 0\n        self.button.irq(handler=self._on_press, trigger=Pin.IRQ_FALLING)\n\n    def _on_press(self, pin):                          # runs in IRQ context\n        # Just set a flag - no allocation, no slow work.\n        self.flag = True\n\n    def loop(self):\n        ALPHA = 0.2                                    # EMA factor\n        while True:\n            if self.flag:\n                self.flag = False\n                self.led_on = not self.led_on\n\n            raw = self.pot.read_u16()\n            self.smoothed = int((1 - ALPHA) * self.smoothed + ALPHA * raw)\n            self.led_pwm.duty_u16(self.smoothed if self.led_on else 0)\n            time.sleep_ms(20)\n\n\nApp().loop()\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — button + potentiometer — production: hardware-timer\n#             debounced button via IRQ + 30 ms guard, uasyncio task that\n#             ramps brightness toward target rather than stepping.\n# APPROACH  - IRQ sets pending; uasyncio task validates after debounce\n#             window and ramps led_pwm smoothly.\n# STRENGTHS - Bounce-proof; smooth brightness; multiple tasks possible.\n# WEAKNESSES- Most boards have ~256 KB RAM - keep imports lean.\nimport uasyncio as asyncio\nimport time\nfrom machine import Pin, PWM, ADC\nfrom micropython import const\n\nDEBOUNCE_MS  = const(30)\nRAMP_STEP    = const(800)                              # duty_u16 per tick\nTICK_MS      = const(20)\n\n\nclass App:\n    def __init__(self):\n        self.led_pwm = PWM(Pin(2), freq=1000, duty_u16=0)\n        self.button  = Pin(0, Pin.IN, Pin.PULL_UP)\n        self.pot     = ADC(Pin(34))\n        self.led_on  = False\n        self._press_pending_ms = 0\n        self.button.irq(handler=self._on_irq, trigger=Pin.IRQ_FALLING)\n        self._target = 0\n        self._current = 0\n\n    def _on_irq(self, pin):\n        # Capture timestamp; debounce verifies in the async task.\n        self._press_pending_ms = time.ticks_ms()\n\n    async def debounce_task(self):\n        last = 0\n        while True:\n            t = self._press_pending_ms\n            if t and time.ticks_diff(time.ticks_ms(), t) >= DEBOUNCE_MS:\n                self._press_pending_ms = 0\n                if self.button.value() == 0:           # still pressed -> real press\n                    if time.ticks_diff(time.ticks_ms(), last) >= DEBOUNCE_MS:\n                        self.led_on = not self.led_on\n                        last = time.ticks_ms()\n            await asyncio.sleep_ms(5)\n\n    async def adc_task(self):\n        while True:\n            raw = self.pot.read_u16()\n            self._target = raw if self.led_on else 0\n            await asyncio.sleep_ms(20)\n\n    async def ramp_task(self):\n        while True:\n            if self._current < self._target:\n                self._current = min(self._target, self._current + RAMP_STEP)\n            elif self._current > self._target:\n                self._current = max(self._target, self._current - RAMP_STEP)\n            self.led_pwm.duty_u16(self._current)\n            await asyncio.sleep_ms(TICK_MS)\n\n    async def main(self):\n        asyncio.create_task(self.debounce_task())\n        asyncio.create_task(self.adc_task())\n        await self.ramp_task()\n\n\nasyncio.run(App().main())\n\n# Decision rule:\n#   Read a stable input                       -> Pin.value() poll.\n#   Need every edge                           -> Pin.irq(trigger=...).\n#   Bouncy switch                              -> hardware RC + IRQ + software debounce window.\n#   Drive an LED brightness                    -> PWM.duty_u16 (0..65535).\n#   Drive a servo                              -> PWM(freq=50, duty_u16 around 5%-10%).\n#   Read analog voltage                        -> ADC(pin).read_u16().\n#   Multiple sensors / timed loops             -> uasyncio tasks; one task per sensor.\n#   Sub-millisecond response                   -> machine.Timer (hardware) with a tiny ISR.\n#   Need to ALLOCATE in an interrupt           -> you can't; micropython.schedule(cb, arg).\n\n# Anti-pattern:\n#   pin.irq(handler=lambda p: print(\"pressed\"))\n# print() inside an ISR allocates and writes to UART - both slow and\n# allocation-prone. Set a flag in the ISR; print from the main thread.\n"
                  }
        ],
        tips: [
                  "Pin numbers are board-specific — `Pin(2)` is the onboard LED on ESP32 dev boards but a different pin on other boards.",
                  "`Pin.irq(trigger=Pin.IRQ_FALLING|Pin.IRQ_RISING, handler=cb)` runs `cb` on edges — keep `cb` tiny (no allocation, no print).",
                  "`PWM(pin, freq=, duty_u16=)` — `duty_u16` is 0..65535; for servos use `freq=50` and ~5%-10% duty.",
                  "ADC: modern ports return 0..65535 via `.read_u16()`; older ESP32 firmware uses `.read()` returning 0..4095.",
                  "Need <1 ms response? Use `machine.Timer.PERIODIC` — hardware timer fires an ISR independent of the main loop."
        ],
        mistake: "`print()` inside an interrupt handler — allocates and blocks on UART. Always set a flag in the ISR; do work on the main thread.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
      {
        id: "micropython-i2c-spi",
        fn: "machine.I2C / SPI — talk to sensors and displays",
        desc: "I2C is the right bus for low-speed sensors (BME280, MPU6050, OLED screens) — two wires (SDA/SCL), addresses up to 127 devices. SPI is faster (MHz) — used for SD cards, big displays, ADCs. Both are exposed via `machine.I2C` / `machine.SPI` with `readfrom_mem`/`write` helpers.",
        category: "hardware",
        subtitle: "machine.I2C (id=, scl=, sda=, freq=400000), i2c.scan() to enumerate addresses, i2c.readfrom_mem(addr, reg, n) / writeto_mem(addr, reg, bytes), machine.SPI (baudrate=, polarity=, phase=, sck=, mosi=, miso=), CS pin manual, struct for binary unpacking, ssd1306 / bme280 driver libs",
        signature: "i2c = I2C(0, scl=Pin(22), sda=Pin(21), freq=400_000); i2c.scan(); i2c.readfrom_mem(0x76, 0xD0, 1); spi = SPI(1, baudrate=10_000_000, polarity=0, phase=0)",
        descLong: "I2C: pick a peripheral id (0 or 1 typically), set `scl` and `sda` pins, optionally `freq=400_000` for fast-mode. `i2c.scan()` returns a list of responding 7-bit addresses. `readfrom_mem(addr, reg, nbytes)` reads `nbytes` from a register (the standard sensor protocol). SPI is more setup: `baudrate=`, `polarity=`/`phase=` (SPI mode 0-3), explicit `sck`/`mosi`/`miso` pins, and you toggle the chip-select pin yourself. Three depths solve the SAME task — read a BME280 temperature sensor — at depths: raw I2C registers + struct → drop in a community driver (`bme280.py`) → robust wrapper that auto-detects the address, retries on bus errors, exposes `read_async()`.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Read raw temperature register from a BME280 over I2C.\n# APPROACH  - i2c.scan() to find it; read 3 bytes at the temp register.\n# STRENGTHS - No external library needed.\n# WEAKNESSES- Doesn't apply calibration; raw value isn't degrees C.\nfrom machine import I2C, Pin\n\ni2c = I2C(0, scl=Pin(22), sda=Pin(21), freq=400_000)  # ESP32: I2C0 default pins\nprint(\"devices:\", [hex(a) for a in i2c.scan()])       # expect 0x76 or 0x77\n\naddr = 0x76                                           # BME280 default I2C address\nchip_id = i2c.readfrom_mem(addr, 0xD0, 1)[0]\nprint(\"chip id =\", hex(chip_id))                      # 0x60 = BME280\n\n# Wake up the sensor: write 0x27 to ctrl_meas (temp/press oversampling x1, normal mode).\ni2c.writeto_mem(addr, 0xF4, b\"\\x27\")\n\n# Read 3-byte raw temperature (registers 0xFA-0xFC, big-endian 20-bit value).\ndata = i2c.readfrom_mem(addr, 0xFA, 3)\nraw_temp = (data[0] << 12) | (data[1] << 4) | (data[2] >> 4)\nprint(\"raw temp:\", raw_temp)                          # NOT in degrees yet\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — BME280 temperature in degrees C — using a community\n#             driver instead of hand-coding the calibration math.\n# APPROACH  - upip / mip install bme280; driver handles registers + cal.\n# STRENGTHS - Real degrees C; one line per reading.\n# WEAKNESSES- Bus errors (sensor unplugged, EMI) raise OSError; need\n#             handling in production.\n# Install on board:  $ mpremote mip install github:robert-hh/BME280\nimport time\nfrom machine import I2C, Pin\nimport bme280\n\ni2c = I2C(0, scl=Pin(22), sda=Pin(21), freq=400_000)\nsensor = bme280.BME280(i2c=i2c, address=0x76)\n\nwhile True:\n    t, p, h = sensor.read_compensated_data()\n    print(f\"T={t/100:.2f} C  P={p/256/100:.1f} hPa  H={h/1024:.1f} %\")\n    time.sleep(2)\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — BME280 readings — production: address auto-detect,\n#             OSError retry with backoff, uasyncio task that publishes\n#             the latest reading for other tasks (display, MQTT).\n# APPROACH  - Wrapper class; address sniff via i2c.scan(); exponential\n#             backoff on bus errors; shared snapshot updated atomically.\n# STRENGTHS - Survives flaky wiring; multi-task friendly; no alloc in\n#             the hot loop.\n# WEAKNESSES- Driver lib must already be on the board (mpremote mip).\nimport uasyncio as asyncio\nimport time\nimport bme280\nfrom machine import I2C, Pin\nfrom micropython import const\n\n\nPERIOD_MS    = const(2000)\nMAX_BACKOFF  = const(30_000)\nADDRESSES    = (0x76, 0x77)\n\n\nclass BMEReader:\n    __slots__ = (\"i2c\", \"sensor\", \"snapshot\", \"last_error\")\n\n    def __init__(self, i2c: I2C):\n        self.i2c    = i2c\n        self.sensor = self._connect()\n        self.snapshot  = (None, None, None, 0)         # (T, P, H, ts_ms)\n        self.last_error = None\n\n    def _connect(self):\n        for addr in ADDRESSES:\n            if addr in self.i2c.scan():\n                return bme280.BME280(i2c=self.i2c, address=addr)\n        raise OSError(\"BME280 not found on bus\")\n\n    async def task(self):\n        backoff = 500\n        while True:\n            try:\n                t, p, h = self.sensor.read_compensated_data()\n                self.snapshot = (t / 100, p / 25600, h / 1024, time.ticks_ms())\n                self.last_error = None\n                backoff = 500\n                await asyncio.sleep_ms(PERIOD_MS)\n            except OSError as e:\n                self.last_error = str(e)\n                # Try to reconnect; back off so we don't spin.\n                await asyncio.sleep_ms(backoff)\n                backoff = min(backoff * 2, MAX_BACKOFF)\n                try:\n                    self.sensor = self._connect()\n                except OSError:\n                    pass\n\n\nasync def reporter(reader: BMEReader):\n    while True:\n        t, p, h, ts = reader.snapshot\n        if t is not None:\n            print(f\"[{ts}] T={t:.2f}  P={p:.2f} hPa  H={h:.1f} %\")\n        else:\n            print(\"waiting for sensor:\", reader.last_error)\n        await asyncio.sleep_ms(2000)\n\n\nasync def main():\n    i2c = I2C(0, scl=Pin(22), sda=Pin(21), freq=400_000)\n    reader = BMEReader(i2c)\n    asyncio.create_task(reader.task())\n    await reporter(reader)\n\n\nasyncio.run(main())\n\n# Decision rule:\n#   Sensor with kHz updates                  -> I2C is fine (up to 1 MHz fast-mode-plus).\n#   Big display / SD card / fast ADC          -> SPI (MHz baud rate).\n#   New sensor model                          -> i2c.scan() first; check it shows up.\n#   Multiple devices same bus                 -> different addresses (I2C) or\n#                                                 separate CS pins (SPI).\n#   3.3V vs 5V mismatch                        -> level shifter; many sensors are 3.3V only.\n#   Sporadic OSError                          -> bus contention or pull-up missing\n#                                                 (4.7k typical on SDA/SCL).\n#   Need to swap pins on ESP32                  -> ESP32 I2C is software-routable; just pass\n#                                                 different scl=/sda=.\n#   Need lots of I/O                            -> I2C/SPI multiplexers (TCA9548A) instead\n#                                                 of more peripheral instances.\n\n# Anti-pattern:\n#   i2c = I2C(0)  # default pins on this board\n#   i2c.readfrom_mem(0x76, 0xD0, 1)   # without 4.7k pull-ups on SDA/SCL\n# Most dev boards lack onboard pull-ups for the I2C bus. Without them,\n# you'll see intermittent OSError(ETIMEDOUT). Add 4.7k from each line\n# to 3.3V or use a board with pull-ups already wired.\n"
                  }
        ],
        tips: [
                  "`i2c.scan()` returns the list of responding addresses — your first sanity check after wiring up a new sensor.",
                  "I2C bus needs **4.7 kΩ pull-ups** from SDA and SCL to 3.3 V — most dev boards don't include them, hence intermittent `OSError`.",
                  "Use community drivers (`bme280.py`, `ssd1306.py`, `mpu9250.py`) — install via `mpremote mip install <pkg>` rather than reading raw registers.",
                  "For SPI, you toggle CS yourself before/after each transaction — peripherals don't do it for you.",
                  "For binary register parsing, `struct.unpack` works on MicroPython — `struct.unpack(\">h\", buf)` for big-endian int16, etc."
        ],
        mistake: "Wiring up an I2C sensor without external 4.7 kΩ pull-ups on SDA/SCL — intermittent `OSError(ETIMEDOUT)` that looks like a sensor bug. Add pull-ups (or use a board that has them).",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },

  // ── Section 3: Platforms — ESP32, RP2040 (Pico), CircuitPython ─────────────────────────────────────────
  {
    id: "platforms",
    title: "Platforms — ESP32, RP2040 (Pico), CircuitPython",
    entries: [
      {
        id: "micropython-esp32-wifi",
        fn: "esp32 / network.WLAN — WiFi + HTTPS + MQTT",
        desc: "ESP32 has built-in WiFi + Bluetooth. `network.WLAN(network.STA_IF)` is the station-mode interface; connect with `.connect(ssid, pwd)`, poll `.isconnected()`. Once online, `urequests` for HTTP(S), `umqtt.simple` for MQTT, sockets via `usocket`. Memory: an HTTPS request is ~30 KB of RAM peak — trim other allocations first.",
        category: "platforms",
        subtitle: "network.WLAN(STA_IF) (station) vs AP_IF (access point), .active(True) -> .connect(ssid, pwd) -> .isconnected, .ifconfig() returns (ip, mask, gw, dns), urequests.get/post (TLS works on ESP32 if firmware has SSL), umqtt.simple MQTTClient (publish/subscribe), socket reuse to avoid TLS handshake cost, deepsleep for battery operation",
        signature: "wlan = network.WLAN(network.STA_IF); wlan.active(True); wlan.connect(SSID, PWD); while not wlan.isconnected(): time.sleep_ms(100)",
        descLong: "WiFi setup: enable station mode, connect, wait for `isconnected()`. Once up, `urequests.get(url)` does HTTP/HTTPS (with TLS if your firmware ships SSL). MQTT via `umqtt.simple.MQTTClient(client_id, broker, ...)` — `connect()`, `publish(topic, msg)`, `subscribe(topic)`, `check_msg()` to poll. For battery-powered devices, `machine.deepsleep(ms)` cuts power to ~10 µA; the chip reboots on wake, so your code starts from `boot.py` again. Three depths solve the SAME task — POST a sensor reading to a server every minute — at depths: blocking connect + urequests.post → reuse one MQTT connection across many publishes → deepsleep cycle: connect, post, deepsleep 60 s.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Connect to WiFi; POST a sensor reading once.\n# APPROACH  - WLAN.connect + urequests.post.\n# STRENGTHS - One file; demonstrates the basics.\n# WEAKNESSES- Blocks while connecting; no retry; no auth.\nimport network\nimport time\nimport urequests as requests\n\nSSID, PWD = \"my-net\", \"secret\"\n\nwlan = network.WLAN(network.STA_IF)\nwlan.active(True)\nwlan.connect(SSID, PWD)\nwhile not wlan.isconnected():\n    time.sleep_ms(200)\nprint(\"ip:\", wlan.ifconfig()[0])\n\nr = requests.post(\"https://example.com/api/sensor\",\n                  json={\"temp\": 23.5, \"humidity\": 41.0})\nprint(\"status:\", r.status_code, r.text)\nr.close()                                              # IMPORTANT: free socket\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — periodic sensor POST — but reuse one MQTT connection\n#             across many publishes (much cheaper than HTTPS each time).\n# APPROACH  - umqtt.simple; reconnect on broker errors.\n# STRENGTHS - ~5x lower power than HTTPS-per-publish; persistent connection.\n# WEAKNESSES- MQTT broker assumed to exist; no QoS 2 in umqtt.simple.\nimport network, time, gc, json\nfrom umqtt.simple import MQTTClient\n\nSSID, PWD = \"my-net\", \"secret\"\nBROKER, TOPIC = \"broker.example.com\", b\"sensors/lab1/temp\"\n\ndef wifi_up():\n    w = network.WLAN(network.STA_IF); w.active(True)\n    if not w.isconnected():\n        w.connect(SSID, PWD)\n        for _ in range(50):\n            if w.isconnected(): break\n            time.sleep_ms(200)\n    return w\n\ndef mqtt_connect():\n    c = MQTTClient(client_id=b\"esp32-lab1\", server=BROKER, keepalive=60)\n    c.connect()\n    return c\n\n\nwifi_up()\nclient = mqtt_connect()\n\nwhile True:\n    payload = json.dumps({\"t\": 22.7, \"h\": 39.4, \"ts\": time.time()})\n    try:\n        client.publish(TOPIC, payload.encode(), retain=False, qos=0)\n    except OSError:\n        try:\n            client.disconnect()\n        except OSError:\n            pass\n        client = mqtt_connect()\n        client.publish(TOPIC, payload.encode())\n    gc.collect()\n    time.sleep(60)\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — periodic POST — production: deepsleep cycle for\n#             battery operation (target <100 uA between reads), retries\n#             with bounded attempts, persisted state across resets.\n# APPROACH  - WiFi connect with bounded retry; MQTT publish; machine.RTC\n#             memory to count failed cycles; deepsleep for the period.\n# STRENGTHS - Real long-life battery design; survives transient outages.\n# WEAKNESSES- deepsleep loses Python state; everything resumes from boot.py;\n#             RTC memory is small (8 bytes on ESP32 for fast-RTC).\nimport gc\nimport json\nimport time\nimport machine\nimport network\nfrom umqtt.simple import MQTTClient\nfrom machine import RTC\n\nSSID, PWD = \"my-net\", \"secret\"\nBROKER, TOPIC = \"broker.example.com\", b\"sensors/lab1/temp\"\n\nPERIOD_S        = 60\nMAX_WIFI_TRIES  = 60          # ~12 s at 200 ms\nMAX_MQTT_TRIES  = 3\nRTC_MEM_KEY     = 0           # byte index of \"consecutive failures\" counter\n\n\ndef read_sensor() -> dict:\n    # Replace with a real sensor read.\n    return {\"t\": 22.7, \"h\": 39.4}\n\n\ndef wifi_up() -> network.WLAN | None:\n    w = network.WLAN(network.STA_IF); w.active(True)\n    if not w.isconnected():\n        w.connect(SSID, PWD)\n        for _ in range(MAX_WIFI_TRIES):\n            if w.isconnected(): return w\n            time.sleep_ms(200)\n        return None\n    return w\n\n\ndef publish_once(payload: bytes) -> bool:\n    for attempt in range(MAX_MQTT_TRIES):\n        try:\n            c = MQTTClient(client_id=b\"esp32-lab1\", server=BROKER,\n                           keepalive=30, ssl=False)\n            c.connect()\n            c.publish(TOPIC, payload, qos=0)\n            c.disconnect()\n            return True\n        except OSError:\n            time.sleep_ms(500 * (attempt + 1))\n    return False\n\n\ndef get_failure_counter() -> int:\n    rtc = RTC()\n    mem = rtc.memory()\n    return mem[RTC_MEM_KEY] if mem else 0\n\n\ndef set_failure_counter(n: int) -> None:\n    rtc = RTC()\n    mem = bytearray(rtc.memory() or b\"\\x00\" * 8)\n    mem[RTC_MEM_KEY] = n\n    rtc.memory(bytes(mem))\n\n\ndef main_cycle() -> None:\n    failures = get_failure_counter()\n\n    wlan = wifi_up()\n    if wlan is None:\n        set_failure_counter(min(failures + 1, 255))\n        machine.deepsleep(PERIOD_S * 1000)              # try again later\n\n    payload = json.dumps(read_sensor()).encode()\n    ok = publish_once(payload)\n    set_failure_counter(0 if ok else min(failures + 1, 255))\n\n    # Always sleep, even on failure - prevents power-eating spin.\n    wlan.disconnect(); wlan.active(False)\n    gc.collect()\n    machine.deepsleep(PERIOD_S * 1000)\n\n\nmain_cycle()                                            # runs once per wake; never returns\n\n# Decision rule:\n#   Always-on, mains powered                 -> persistent MQTT (junior tier).\n#   Battery, occasional updates              -> deepsleep cycle (senior tier).\n#   Need to reach 8+ months on AAA batteries -> deepsleep + LoRa instead of WiFi.\n#   Need OTA firmware updates                 -> esp32.Partition + ota_set_boot_partition.\n#   Need TLS                                  -> standard MicroPython firmware on ESP32 has SSL;\n#                                                certs go in flash via mpremote.\n#   Need NTP time after boot                  -> ntptime.settime() once after WiFi up.\n#   Many devices behind one MQTT topic        -> client_id includes machine.unique_id().hex().\n\n# Anti-pattern:\n#   for _ in range(N):\n#       requests.post(...)\n#       time.sleep(60)\n# Sleeping while WiFi is up wastes ~80 mA continuously - kills batteries.\n# Either keep MQTT alive (small constant draw) OR deepsleep between\n# publishes (huge savings).\n"
                  }
        ],
        tips: [
                  "WiFi connect: `WLAN(STA_IF).active(True).connect(ssid, pwd)`, then poll `isconnected()` with a short sleep.",
                  "Always `r.close()` after `urequests.get/post` — sockets leak otherwise and you'll OOM after ~10 requests.",
                  "For periodic publishes, MQTT-keepalive is ~5× cheaper power than HTTPS-per-publish.",
                  "`machine.deepsleep(ms)` brings ESP32 to ~10 µA — the chip reboots on wake, so persist state in `RTC().memory()`.",
                  "For TLS, your firmware needs SSL compiled in — the standard MicroPython ESP32 build does, but minimal builds may not."
        ],
        mistake: "Polling-loop sleep with WiFi active for low-power use cases — WiFi at idle still draws ~80 mA. Either keep MQTT for steady ops or deepsleep between events.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
      {
        id: "micropython-rp2040-pico",
        fn: "rp2 / Pico — second core, PIO, asyncio",
        desc: "The RP2040 (Raspberry Pi Pico) has two ARM Cortex-M0+ cores and **PIO** — a Programmable I/O subsystem that runs tiny state-machine programs at clock speed. Use the second core via `_thread`; use PIO when bit-banging would be too slow (WS2812 LEDs, custom UART/SPI variants).",
        category: "platforms",
        subtitle: "_thread.start_new_thread (Pico has 2 cores; ESP32 doesn't support _thread well), rp2.PIO + @asm_pio decorator (state-machine assembler, 32 instructions max), StateMachine.put / get for FIFO, common PIO uses (WS2812 RGB strip, software UART, quadrature decoder), Pico W: + WiFi via cyw43439, Pico 2 (RP2350): RISC-V or ARM cores",
        signature: "_thread.start_new_thread(worker, ()); @rp2.asm_pio(...) def prog(): nop(); sm = rp2.StateMachine(0, prog, freq=1_000_000, set_base=Pin(15)); sm.active(1)",
        descLong: "The Pico's two killer features: dual-core and PIO. `_thread.start_new_thread(target, args)` launches `target` on core 1 — communicate via shared globals + `_thread.allocate_lock()`. PIO is a tiny 32-instruction assembler that runs independently of the CPU at up to 133 MHz — perfect for WS2812 timing, software UART, quadrature encoders. Three depths solve the SAME task — drive a strip of WS2812 RGB LEDs — at depths: bit-banging in Python (works for 8 LEDs at low refresh) → PIO state machine driving the LED strip at full clock → uasyncio task on core 0 doing animation while PIO on core 1 handles the data line.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Light up 8 WS2812 RGB LEDs in a rainbow.\n# APPROACH  - Bit-bang the data line manually.\n# STRENGTHS - Works without any extra setup.\n# WEAKNESSES- WS2812 needs sub-microsecond timing; pure-Python bit-bang\n#             on Pico can do it for 8 LEDs at ~10 Hz refresh, but breaks\n#             at higher counts.\nimport time\nfrom machine import Pin\n\n# WS2812 protocol: each bit is a pulse; bit timing is on the order of 400 ns.\n# Pure Python is too slow for >~8 LEDs - use PIO (junior tier).\n\nPIN = 15\nNUM = 8\npin = Pin(PIN, Pin.OUT)\n\n\ndef send_color(r, g, b):\n    # WS2812 wants GRB order, MSB first.\n    grb = (g << 16) | (r << 8) | b\n    for i in range(23, -1, -1):\n        bit = (grb >> i) & 1\n        # Crude: not actually fast enough for real WS2812 timing.\n        pin.value(1)\n        if bit: pin.value(1)\n        else:   pin.value(0)\n        pin.value(0)\n\n\nwhile True:\n    for hue in range(NUM):\n        r = (hue * 30) % 256\n        send_color(r, 32, 255 - r)\n    time.sleep_ms(100)\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — drive a WS2812 strip — but with a PIO state machine\n#             so timing is deterministic at any LED count.\n# APPROACH  - rp2.asm_pio program implementing the 800 kHz protocol; feed\n#             RGB colors via the StateMachine FIFO.\n# STRENGTHS - Real-world correct timing for 100s of LEDs.\n# WEAKNESSES- PIO assembler has a learning curve; 32-instruction limit.\nimport array, time\nimport rp2\nfrom machine import Pin\n\nNUM = 30\nPIN = 15\n\n# Standard WS2812 PIO program: shifts 24 bits MSB-first; tuned for 8 MHz\n# clock per bit-time-third (so total bit window = 1.25 us).\n@rp2.asm_pio(sideset_init=rp2.PIO.OUT_LOW,\n             out_shiftdir=rp2.PIO.SHIFT_LEFT,\n             autopull=True, pull_thresh=24)\ndef ws2812():\n    T1 = 2; T2 = 5; T3 = 3\n    wrap_target()\n    label(\"bitloop\")\n    out(x, 1)               .side(0)    [T3 - 1]\n    jmp(not_x, \"do_zero\")   .side(1)    [T1 - 1]\n    jmp(\"bitloop\")          .side(1)    [T2 - 1]\n    label(\"do_zero\")\n    nop()                   .side(0)    [T2 - 1]\n    wrap()\n\n\nsm = rp2.StateMachine(0, ws2812, freq=8_000_000, sideset_base=Pin(PIN))\nsm.active(1)\n\nbuf = array.array(\"I\", [0] * NUM)                     # 32-bit per LED, top 8 bits unused\n\ndef show():\n    sm.put(buf, 8)                                    # left-shift by 8 to align top 24 bits\n\n\nwhile True:\n    for hue in range(NUM):\n        r = (hue * 8) & 0xFF\n        # WS2812 wants GRB order; pack as 0x00GGRRBB shifted left 8.\n        buf[hue] = (r << 16) | (32 << 24) | ((255 - r) << 8)\n    show()\n    time.sleep_ms(50)\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — WS2812 strip — production: PIO drives data, uasyncio\n#             on core 0 runs the animation, core 1 reads buttons over\n#             _thread for input concurrency.\n# APPROACH  - PIO + uasyncio + _thread; lock guards the shared color buffer.\n# STRENGTHS - Smooth animation; UI button input doesn't drop frames;\n#             scales to 200+ LEDs.\n# WEAKNESSES- _thread on RP2040 is preemptive between cores - need locks\n#             around shared state.\nimport _thread, time, array, rp2, uasyncio as asyncio\nfrom machine import Pin\n\n\nNUM = 60\nDATA_PIN = 15\nBUTTON_PIN = 14\n\n\n@rp2.asm_pio(sideset_init=rp2.PIO.OUT_LOW,\n             out_shiftdir=rp2.PIO.SHIFT_LEFT,\n             autopull=True, pull_thresh=24)\ndef ws2812():\n    T1 = 2; T2 = 5; T3 = 3\n    wrap_target()\n    label(\"bitloop\")\n    out(x, 1)               .side(0)    [T3 - 1]\n    jmp(not_x, \"do_zero\")   .side(1)    [T1 - 1]\n    jmp(\"bitloop\")          .side(1)    [T2 - 1]\n    label(\"do_zero\")\n    nop()                   .side(0)    [T2 - 1]\n    wrap()\n\n\nsm = rp2.StateMachine(0, ws2812, freq=8_000_000, sideset_base=Pin(DATA_PIN))\nsm.active(1)\n\nbuf = array.array(\"I\", [0] * NUM)\nbuf_lock = _thread.allocate_lock()\nmode = 0                                               # 0=rainbow, 1=fire\n\n\ndef grb(r, g, b):\n    return (g << 24) | (r << 16) | (b << 8)\n\n\ndef core1_buttons():\n    global mode\n    btn = Pin(BUTTON_PIN, Pin.IN, Pin.PULL_UP)\n    last = 1\n    while True:\n        v = btn.value()\n        if last == 1 and v == 0:                       # falling edge\n            mode = (mode + 1) % 2\n        last = v\n        time.sleep_ms(10)\n\n\n_thread.start_new_thread(core1_buttons, ())            # core 1\n\n\nasync def animator():\n    t = 0\n    while True:\n        with buf_lock:\n            for i in range(NUM):\n                if mode == 0:\n                    h = (t + i * 8) & 0xFF\n                    buf[i] = grb(h, 32, 255 - h)\n                else:\n                    flicker = ((t * 7 + i * 23) & 0x3F)\n                    buf[i] = grb(180 + flicker, 40 + (flicker >> 1), 0)\n        sm.put(buf, 8)\n        t = (t + 4) & 0xFF\n        await asyncio.sleep_ms(33)                     # ~30 FPS\n\n\nasync def main():\n    await animator()\n\n\nasyncio.run(main())\n\n# Decision rule:\n#   Need 2 cores (RP2040 / RP2350)             -> _thread.start_new_thread + lock.\n#   Need sub-microsecond timing (WS2812, etc.)  -> rp2.PIO program.\n#   Need 5+ peripherals on one MCU              -> PIO can implement extra UARTs/SPIs.\n#   Need WiFi on Pico                           -> Pico W (cyw43439) - same _thread/PIO,\n#                                                 + network.WLAN.\n#   Need >2 cores or NEON                       -> not Pico; reach for ESP32-S3 or\n#                                                 a Cortex-A SBC (Linux land).\n#   Need C-level perf in MicroPython            -> @micropython.viper / @micropython.native\n#                                                 decorators on small functions.\n\n# Anti-pattern:\n#   shared = []\n#   _thread.start_new_thread(producer, ())\n#   _thread.start_new_thread(consumer, ())   # both touching shared without lock\n# Two cores writing to the same Python list is a recipe for corruption\n# or hangs. Always _thread.allocate_lock() and 'with lock:'.\n"
                  }
        ],
        tips: [
                  "RP2040 / RP2350 have two cores — `_thread.start_new_thread(target, args)` runs `target` on core 1.",
                  "PIO state machines can do sub-microsecond timing the CPU can't — WS2812, software UART, quadrature decoder.",
                  "For WiFi on Pico, use **Pico W** (cyw43439 chip) — same MicroPython API as ESP32 (`network.WLAN`).",
                  "`@micropython.viper` and `@micropython.native` decorators give near-C performance on tight functions — use sparingly.",
                  "Locks are mandatory between cores — `_thread.allocate_lock()`, then `with lock:` around shared-state mutations."
        ],
        mistake: "Two cores writing to a shared Python list without a lock — corruption or hangs. Always `_thread.allocate_lock()` and `with lock:` around mutations.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },

  // ── Section 4: Tooling and dev workflow ─────────────────────────────────────────
  {
    id: "tooling",
    title: "Tooling and dev workflow",
    entries: [
      {
        id: "micropython-mpremote",
        fn: "mpremote / mip / WebREPL — flash, deploy, debug",
        desc: "The modern MicroPython workflow: `mpremote` (one tool replaces ampy/rshell). `mpremote connect /dev/ttyACM0 fs cp main.py :main.py` deploys a file. `mpremote mip install <pkg>` installs a community package. WebREPL is the in-browser fallback for ESP32 over WiFi.",
        category: "tooling",
        subtitle: "mpremote (USB serial control: connect, repl, fs cp / ls / rm, run, mount, soft-reset, exec), mpremote mip install (package manager replacing upip), Thonny IDE for beginners, WebREPL for over-the-air, esptool.py for firmware flash, frozen modules (compile .py into firmware to save RAM)",
        signature: "mpremote connect /dev/ttyACM0 fs cp main.py :main.py; mpremote mip install bme280; mpremote run script.py; mpremote mount .",
        descLong: "`mpremote` is the one tool to learn. `connect /dev/ttyACM0` selects the board (or just `mpremote` to autodetect a single board). `repl` opens an interactive REPL. `fs cp local.py :remote.py` copies a file (`:` prefix means the board's filesystem). `mip install <pkg>` installs from PyPI/GitHub via the on-device `mip` package manager. `run script.py` executes a local script on the board (transient, not persisted). `mount .` mounts the current host directory as `/remote` on the board — perfect for live development. Three depths solve the SAME task — deploy and run a `main.py` on a board — at depths: copy `main.py` and reboot → `mount .` for live edit-test → CI script that flashes firmware, deploys files, runs a smoke test on the connected board.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Copy main.py to the board and watch it run.\n# APPROACH  - mpremote fs cp + mpremote reset + mpremote repl.\n# STRENGTHS - Three commands.\n# WEAKNESSES- Manual; no live edit; have to remember the device path.\n\"\"\"\n$ mpremote connect /dev/ttyACM0 fs cp main.py :main.py\n$ mpremote connect /dev/ttyACM0 reset\n$ mpremote connect /dev/ttyACM0 repl\n# Ctrl-X to exit the REPL.\n\n# Even shorter when only one board is plugged in:\n$ mpremote fs cp main.py :main.py\n$ mpremote reset\n$ mpremote repl\n\"\"\"\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — develop main.py against the board — but with mount\n#             so the local file IS the remote file (live edit).\n# APPROACH  - mpremote mount . then run main from the mounted dir.\n# STRENGTHS - Edit on host, run on board, no copy step.\n# WEAKNESSES- Only one mount per session; production deploy still needs cp.\n\"\"\"\n# In one terminal:\n$ cd ~/code/sensor\n$ mpremote mount .\n# Now /remote on the board mirrors ~/code/sensor on the host.\n\n# In another terminal (or after Ctrl-D in the mounted REPL):\n$ mpremote exec \"import sys; sys.path.insert(0, '/remote'); exec(open('/remote/main.py').read())\"\n\n# Install community drivers on the board:\n$ mpremote mip install github:robert-hh/BME280\n$ mpremote mip install github:miguelgrinberg/microdot\n\n# List files on the board:\n$ mpremote fs ls\n\"\"\"\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — deploy a project — production: shell script that\n#             flashes firmware (esptool), deploys app + dependencies,\n#             runs a smoke test, returns nonzero on failure.\n# APPROACH  - esptool.py for firmware; mpremote for files + smoke test;\n#             smoke test prints SMOKE_OK on success.\n# STRENGTHS - Reproducible deploy; CI-friendly; catches regressions.\n# WEAKNESSES- USB device paths differ across OSes (/dev/ttyACM0 on Linux,\n#             /dev/cu.usbmodem* on macOS, COM3 on Windows).\n\"\"\"\n#!/usr/bin/env bash\nset -euo pipefail\n\nPORT=\"${1:-/dev/ttyACM0}\"\nFW_BIN=\"firmware/esp32-20260101-v1.23.0.bin\"\n\necho \"==> erase flash\"\nesptool.py --port \"$PORT\" erase_flash\n\necho \"==> flash MicroPython firmware\"\nesptool.py --port \"$PORT\" --baud 460800 write_flash -z 0x1000 \"$FW_BIN\"\n\n# Wait for device to come back as a serial port.\nsleep 2\n\necho \"==> install on-device packages\"\nmpremote connect \"$PORT\" mip install github:robert-hh/BME280\nmpremote connect \"$PORT\" mip install umqtt.simple\n\necho \"==> deploy app\"\nmpremote connect \"$PORT\" fs cp -r src/* :\nmpremote connect \"$PORT\" fs cp config/secrets.py :secrets.py\n\necho \"==> smoke test\"\noutput=$(mpremote connect \"$PORT\" run tests/smoke.py)\necho \"$output\"\nif ! grep -q \"SMOKE_OK\" <<< \"$output\"; then\n    echo \"smoke test FAILED\"\n    exit 1\nfi\n\necho \"==> reset into main.py\"\nmpremote connect \"$PORT\" reset\necho \"OK\"\n\"\"\"\n\n# tests/smoke.py example:\n\"\"\"\nimport gc, machine\n\nprint('mem_free:', gc.mem_free())\nprint('freq:', machine.freq())\nprint('unique_id:', machine.unique_id().hex())\n\ntry:\n    import bme280, network, umqtt.simple\n    print('imports ok')\nexcept ImportError as e:\n    print('IMPORT FAIL:', e)\n    raise SystemExit(1)\n\nprint('SMOKE_OK')\n\"\"\"\n\n# Decision rule:\n#   Single board, dev workflow              -> mpremote (fs cp + reset + repl).\n#   Live editing on the host                 -> mpremote mount.\n#   Install community drivers                 -> mpremote mip install <pkg>.\n#   Beginner-friendly IDE                     -> Thonny (built-in MicroPython support).\n#   Over-the-air on ESP32 (no USB)           -> WebREPL.\n#   Save RAM on a tight memory budget         -> freeze .py modules into the firmware\n#                                                 (manifest.py + custom build).\n#   Update firmware                            -> esptool.py for ESP32; uf2 drag-and-drop\n#                                                 for Pico.\n#   CI / automated deploys                    -> shell script wrapping the above.\n\n# Anti-pattern:\n#   ampy --port ... put main.py\n# ampy is dead. mpremote is the official tool now and supports everything\n# ampy did plus mip, mount, exec, run, etc. Just use mpremote.\n\"\"\"\n"
                  }
        ],
        tips: [
                  "`mpremote` replaces ampy/rshell — one tool with `connect`, `fs`, `repl`, `run`, `exec`, `mount`, `mip`, `reset`.",
                  "`mpremote mip install <pkg>` installs from PyPI or `github:user/repo` directly on the board — modern replacement for `upip`.",
                  "`mpremote mount .` mirrors the host directory onto the board's filesystem — fastest dev loop, no `cp` step.",
                  "For ESP32 firmware updates use `esptool.py write_flash`; for Pico just drag the `.uf2` file onto the BOOTSEL drive.",
                  "Save RAM by **freezing** modules into the firmware (compile `.py` → `.mpy` → bake into a custom build) — only matters when you're memory-bound."
        ],
        mistake: "Reaching for `ampy` in 2026 — it's deprecated. Use `mpremote` (official) — same operations, more features, actively maintained.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },

  // ── Section 5: Pick the right embedded Python flavor ─────────────────────────────────────────
  {
    id: "patterns",
    title: "Pick the right embedded Python flavor",
    entries: [
      {
        id: "micropython-vs-circuitpython",
        fn: "MicroPython vs CircuitPython vs C/Rust — pick the toolchain",
        desc: "MicroPython for serious embedded — performance, async, every chip, open community. CircuitPython (Adafruit fork) for beginners and quick projects — friendlier API, USB drive shows up, hardware learning ramp. C / Rust when you need every microsecond, or for sleep currents <10 µA, or for safety-critical work.",
        category: "patterns",
        subtitle: "MicroPython (community-driven, every major MCU, uasyncio, _thread on RP2040, viper/native decorators) vs CircuitPython (Adafruit; CIRCUITPY USB drive, simpler stdlib mapping, no asyncio in older releases) vs Arduino C++ (universal but verbose) vs Rust embedded (memory-safe, async via embassy, no GC) vs ESP-IDF (C, official Espressif framework with FreeRTOS), Zephyr RTOS (cross-MCU C)",
        signature: "# MicroPython: import machine; machine.Pin(2, machine.Pin.OUT)\\n# CircuitPython: import board, digitalio; led = digitalio.DigitalInOut(board.D13)\\n# Rust embassy: let mut led = Output::new(p.PIN_25, Level::Low, Speed::Low);",
        descLong: "MicroPython covers ESP32 / RP2040 / STM32 / nRF / many others; same code runs across them with light pin-name changes. CircuitPython is Adafruit's fork: friendlier API (`board.D13` instead of pin numbers), the board mounts as a USB drive (`CIRCUITPY/`), and it ships drivers for hundreds of Adafruit sensors. Trade-off: slower, slightly less RAM-efficient, smaller chip support. Reach for **C / C++** (Arduino or ESP-IDF) when you need 100+ µs precision, peripheral access MicroPython doesn't expose, or sleep currents below 10 µA. Reach for **Rust + embassy** for memory-safety + async on bare metal — modern, fast, growing fast in 2026. Three depths solve the SAME task — blink an LED at 1 Hz — at depths: MicroPython 5-line script → CircuitPython equivalent showing the API differences → Rust embassy version showing what \"no GC, real-time\" looks like.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Blink the onboard LED at 1 Hz. (MicroPython)\n# APPROACH  - machine.Pin + sleep.\n# STRENGTHS - Five lines.\n# WEAKNESSES- Synchronous; blocks the whole interpreter.\nimport time\nfrom machine import Pin\n\nled = Pin(2, Pin.OUT)\nwhile True:\n    led.toggle()\n    time.sleep(0.5)\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — blink LED at 1 Hz — in CircuitPython on the same board.\n# APPROACH  - import board + digitalio.\n# STRENGTHS - Friendlier names (board.D13 instead of pin numbers); board\n#             auto-mounts as CIRCUITPY drive - drag-and-drop main.py.\n# WEAKNESSES- Older releases lacked asyncio; smaller chip family.\n\"\"\"\n# code.py on a CIRCUITPY drive (Adafruit's main script entry point):\nimport board\nimport digitalio\nimport time\n\nled = digitalio.DigitalInOut(board.LED)              # board.LED is portable\nled.direction = digitalio.Direction.OUTPUT\n\nwhile True:\n    led.value = not led.value\n    time.sleep(0.5)\n\"\"\"\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — blink LED at 1 Hz — Rust + embassy on RP2040 (no GC,\n#             real-time async, fits in 32 KB flash).\n# APPROACH  - embassy executor; Output pin; Timer::after for sleep.\n# STRENGTHS - Memory-safe, deterministic timing, microamp sleep currents,\n#             zero-cost async.\n# WEAKNESSES- Rust learning curve; longer build; cross-compile setup.\n\"\"\"\n# Cargo.toml dependencies:\n#   embassy-executor = { version = \"0.6\", features = [\"arch-cortex-m\", \"executor-thread\"] }\n#   embassy-rp       = { version = \"0.2\",  features = [\"rp2040\", \"time-driver\"] }\n#   embassy-time     = { version = \"0.3\" }\n\n#![no_std]\n#![no_main]\n\nuse embassy_executor::Spawner;\nuse embassy_rp::gpio::{Level, Output};\nuse embassy_time::{Duration, Timer};\nuse {defmt_rtt as _, panic_probe as _};\n\n#[embassy_executor::main]\nasync fn main(_spawner: Spawner) {\n    let p = embassy_rp::init(Default::default());\n    let mut led = Output::new(p.PIN_25, Level::Low);\n\n    loop {\n        led.toggle();\n        Timer::after(Duration::from_millis(500)).await;\n    }\n}\n\n# Build:\n#   $ cargo build --release --target thumbv6m-none-eabi\n# Flash (RP2040 in BOOTSEL):\n#   $ probe-rs run --chip RP2040 target/thumbv6m-none-eabi/release/blink\n\"\"\"\n\n# Decision rule:\n#   Quick prototype, beginner                  -> CircuitPython.\n#   Production embedded, Python everywhere     -> MicroPython.\n#   Need uasyncio / _thread / viper             -> MicroPython.\n#   Need every microsecond of timing            -> C / C++ (ESP-IDF / Arduino) or Rust.\n#   Need <10 uA deepsleep                        -> C with FreeRTOS / ESP-IDF; MicroPython\n#                                                 does ~10 uA on ESP32 deepsleep but app\n#                                                 wake-up is heavier.\n#   Memory safety + async on bare metal         -> Rust + embassy.\n#   Safety-critical (medical, avionics)         -> not Python; certified C (MISRA), Ada,\n#                                                 or Rust with formal subsets.\n#   Audio DSP at MHz rates                       -> C/C++ with SIMD or Rust + nostd.\n#   Cross-vendor portability                    -> Zephyr RTOS (C) for the maximum reach.\n\n# Anti-pattern:\n#   \"I'll just rewrite the firmware in Python because I know Python\"\n# When the existing C firmware works, RAM is tight, real-time is critical,\n# and the team is C-only - keep C. MicroPython is for greenfield embedded\n# where productivity > the last 20% of perf, and team is Python-fluent.\n\"\"\"\n"
                  }
        ],
        tips: [
                  "MicroPython for serious embedded (perf + ecosystem); CircuitPython for friendly first projects.",
                  "CircuitPython auto-mounts as `CIRCUITPY` USB drive — drag-and-drop `code.py` to deploy. No `mpremote` needed.",
                  "For sub-µs timing or sleep currents <10 µA, drop to **C** (ESP-IDF / Arduino) or **Rust embassy**.",
                  "Rust + embassy is the modern bare-metal-async path — memory-safe, zero-cost async, growing fast in 2026.",
                  "For cross-vendor portability across MCU brands, **Zephyr RTOS** (C) reaches the most chips — but it's a C codebase."
        ],
        mistake: "Rewriting working C firmware in MicroPython because \"I know Python\" — when RAM is tight and real-time is critical, the rewrite usually loses. MicroPython shines on greenfield projects where productivity > the last 20% of perf.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },
]

export default { meta, sections }

export const meta = {
  "id": "gamedev-pygame",
  "label": "Game Dev (pygame)",
  "icon": "🎮",
  "description": "Game development in Python with pygame (the SDL2 wrapper) and pymunk (Chipmunk2D physics). pygame is a software renderer — fine up to ~60 FPS at 1080p with sprites; reach for arcade or moderngl-pygame when you need GPU-accelerated batching. The four things you keep getting wrong: a fixed-step game loop vs frame-rate-coupled motion, blitting without converting surfaces (`.convert_alpha()`), forgetting to track a separate physics-rect from the draw-rect, and mixing pixels with physics-engine units."
}

export const sections = [

  // ── Section 1: Display, event loop, the game frame ─────────────────────────────────────────
  {
    id: "basics",
    title: "Display, event loop, the game frame",
    entries: [
      {
        id: "pygame-event-loop",
        fn: "pygame.init / display / event loop / clock",
        desc: "Every pygame app is the same shape: `pygame.init()` → create a display surface → loop {handle events → update state → draw → flip → tick the clock}. Use `pygame.time.Clock()` to cap framerate; never `time.sleep()` inside the loop.",
        category: "basics",
        subtitle: "pygame.init / display.set_mode (size, flags=DOUBLEBUF | SCALED), event loop with pygame.event.get (QUIT, KEYDOWN, MOUSEBUTTONDOWN), Clock.tick(60) (returns elapsed ms), display.flip vs display.update (rect list), surface.fill for clear, pygame.quit() on exit",
        signature: "pygame.init(); screen = pygame.display.set_mode((w, h)); clock = pygame.time.Clock(); while running: ... clock.tick(60)",
        descLong: "The standard skeleton: init pygame; create the display surface (`set_mode`); enter `while running`. Each iteration: drain `pygame.event.get()` for input, update game state, redraw to the screen surface, `display.flip()` to swap buffers, `clock.tick(fps)` to cap frame rate (and return milliseconds since the last call — useful as `dt`). `display.flip()` redraws the whole window; `display.update(rect_list)` is faster on slow GPUs but needs you to track dirty rectangles. Three depths solve the SAME task — show a bouncing ball — at depths: minimal frame-rate-coupled loop → fixed-timestep update with `dt`-scaled physics → fixed-timestep simulation with frame-time accumulator and decoupled render.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Bouncing ball - press ESC to quit.\n# APPROACH  - Standard event loop; velocity in pixels per FRAME.\n# STRENGTHS - Smallest possible pygame example.\n# WEAKNESSES- Speed depends on frame rate (60 FPS vs 144 FPS = 2.4x faster).\nimport pygame, sys\n\npygame.init()\nW, H = 640, 360\nscreen = pygame.display.set_mode((W, H))\npygame.display.set_caption(\"bouncer\")\nclock  = pygame.time.Clock()\n\nx, y     = 100, 50\nvx, vy   = 4, 3\nradius   = 20\n\nwhile True:\n    for e in pygame.event.get():\n        if e.type == pygame.QUIT or (e.type == pygame.KEYDOWN and e.key == pygame.K_ESCAPE):\n            pygame.quit(); sys.exit()\n\n    # update\n    x += vx; y += vy\n    if x < radius or x > W - radius: vx = -vx\n    if y < radius or y > H - radius: vy = -vy\n\n    # draw\n    screen.fill((20, 20, 30))\n    pygame.draw.circle(screen, (240, 220, 80), (x, y), radius)\n    pygame.display.flip()\n\n    clock.tick(60)                                   # cap at 60 FPS\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — bouncing ball — but motion is in pixels per SECOND\n#             so it's framerate-independent.\n# APPROACH  - dt = clock.tick(60) / 1000.0; velocity in px/s.\n# STRENGTHS - Same speed at 30, 60, 144 Hz.\n# WEAKNESSES- dt-scaled motion can still pass through walls at very low\n#             frame rates; senior tier fixes with a fixed-step accumulator.\nimport pygame, sys\n\npygame.init()\nW, H = 640, 360\nscreen = pygame.display.set_mode((W, H))\nclock  = pygame.time.Clock()\n\nx, y   = 100.0, 50.0\nvx, vy = 240.0, 180.0                                # pixels per SECOND\nradius = 20\n\nwhile True:\n    dt = clock.tick(60) / 1000.0                     # seconds since last frame\n    for e in pygame.event.get():\n        if e.type == pygame.QUIT or \\\n           (e.type == pygame.KEYDOWN and e.key == pygame.K_ESCAPE):\n            pygame.quit(); sys.exit()\n\n    x += vx * dt; y += vy * dt\n    if x < radius or x > W - radius:\n        vx = -vx; x = max(radius, min(W - radius, x))\n    if y < radius or y > H - radius:\n        vy = -vy; y = max(radius, min(H - radius, y))\n\n    screen.fill((20, 20, 30))\n    pygame.draw.circle(screen, (240, 220, 80), (int(x), int(y)), radius)\n    pygame.display.flip()\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — bouncing ball — production: fixed-step physics with a\n#             time accumulator so simulation is deterministic regardless of\n#             render rate; render-only interpolation between physics steps.\n# APPROACH  - Glenn Fiedler's \"Fix Your Timestep\" pattern: accumulate real\n#             time, run N physics steps of constant dt, render at the\n#             leftover alpha between steps.\n# STRENGTHS - Deterministic; reproducible saves/replays; smooth render.\n# WEAKNESSES- Slightly more complex; mostly invisible until you do\n#             multiplayer / replay or hit very slow frames.\nfrom __future__ import annotations\nimport pygame\nimport sys\nfrom dataclasses import dataclass\n\n\n@dataclass\nclass State:\n    x: float; y: float\n    vx: float; vy: float\n\n\ndef lerp(a: float, b: float, t: float) -> float:\n    return a + (b - a) * t\n\n\ndef step_physics(s: State, dt: float, w: int, h: int, r: int) -> State:\n    nx = s.x + s.vx * dt\n    ny = s.y + s.vy * dt\n    nvx, nvy = s.vx, s.vy\n    if nx < r or nx > w - r:\n        nvx = -nvx\n        nx = max(r, min(w - r, nx))\n    if ny < r or ny > h - r:\n        nvy = -nvy\n        ny = max(r, min(h - r, ny))\n    return State(nx, ny, nvx, nvy)\n\n\ndef main() -> None:\n    pygame.init()\n    W, H = 800, 450\n    screen = pygame.display.set_mode((W, H))\n    clock  = pygame.time.Clock()\n\n    R = 20\n    cur = State(100.0, 50.0, 240.0, 180.0)\n    prev = cur                                        # for interpolation\n\n    PHYSICS_HZ = 120\n    FIXED_DT = 1.0 / PHYSICS_HZ\n    accumulator = 0.0\n    MAX_FRAME_DT = 0.25                                # avoid spiral-of-death\n\n    while True:\n        frame_dt = min(clock.tick(0) / 1000.0, MAX_FRAME_DT)   # uncapped render\n        for e in pygame.event.get():\n            if e.type == pygame.QUIT or \\\n               (e.type == pygame.KEYDOWN and e.key == pygame.K_ESCAPE):\n                pygame.quit(); sys.exit()\n\n        accumulator += frame_dt\n        # Run fixed-step physics until we've consumed real time.\n        while accumulator >= FIXED_DT:\n            prev = cur\n            cur  = step_physics(cur, FIXED_DT, W, H, R)\n            accumulator -= FIXED_DT\n\n        # Render at interpolated position between prev (t-FIXED_DT) and cur.\n        alpha = accumulator / FIXED_DT\n        rx = lerp(prev.x, cur.x, alpha)\n        ry = lerp(prev.y, cur.y, alpha)\n\n        screen.fill((20, 20, 30))\n        pygame.draw.circle(screen, (240, 220, 80), (int(rx), int(ry)), R)\n        pygame.display.flip()\n\n\nif __name__ == \"__main__\":\n    main()\n\n# Decision rule:\n#   Toy / jam / hours-old prototype          -> frame-coupled (intro tier).\n#   Anything you'll polish                    -> dt-scaled (junior tier).\n#   Multiplayer / determinism / replay        -> fixed-step + accumulator\n#                                                + render interpolation (senior tier).\n#   Need exact framerate cap                  -> clock.tick(60).\n#   Need uncapped render with vsync            -> set_mode(..., vsync=1) and\n#                                                clock.tick(0).\n#   Need lower CPU when minimized              -> check display.get_active(); sleep\n#                                                more aggressively when not active.\n#   Need to skip render but keep simulation    -> headless mode set DISPLAYSURF=\n#                                                pygame.Surface (no flip).\n\n# Anti-pattern:\n#   pygame.time.delay(16)                    # inside the loop instead of clock.tick\n# delay() blocks; clock.tick() targets a frame rate AND returns elapsed\n# time you can use as dt. Always use Clock for the loop pacing.\n"
                  }
        ],
        tips: [
                  "Always pace with `pygame.time.Clock().tick(fps)` — never `time.sleep()` or `pygame.time.delay()` in the loop.",
                  "For framerate-independent motion use seconds: `dt = clock.tick(60) / 1000.0`, then `x += vx * dt`.",
                  "For deterministic physics (multiplayer, replays), use a fixed-step + accumulator pattern.",
                  "`display.flip()` swaps the whole buffer; `display.update(rects)` updates only dirty rects (faster on slow GPUs).",
                  "On startup, set `set_mode((w, h), pygame.SCALED | pygame.RESIZABLE)` for letterboxed scaling on HiDPI displays."
        ],
        mistake: "Hard-coded movement like `x += 4` per frame — the ball moves twice as fast on a 144 Hz monitor. Always scale by `dt`.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
      {
        id: "pygame-surface-rect",
        fn: "Surface / Rect / blit / convert — the drawing primitives",
        desc: "`Surface` is a 2D pixel buffer; `screen` is the special one tied to the window. `Rect` is `(x, y, w, h)` with collision/positioning helpers. **Always call `.convert()` (opaque) or `.convert_alpha()` (with transparency) right after loading an image** — un-converted surfaces blit 5-10× slower.",
        category: "basics",
        subtitle: "pygame.Surface (off-screen surfaces for compositing), .convert() (opaque) vs .convert_alpha() (transparent PNG), screen.blit(surface, (x, y)), Rect (x, y, w, h) + .move_ip / .colliderect / .collidepoint, get_rect() helper, SRCALPHA flag for new transparent surfaces, dirty-rect rendering with display.update(rect_list)",
        signature: "img = pygame.image.load(\"p.png\").convert_alpha(); rect = img.get_rect(center=(x, y)); screen.blit(img, rect)",
        descLong: "A `Surface` is `(width, height, depth, flags)`; the `screen` you get from `set_mode` is one. Loaded images come in a generic format — `.convert()` matches them to the screen format (5-10× faster blit), `.convert_alpha()` does the same and preserves a per-pixel alpha channel. `Rect` is the geometric workhorse: positioning (`get_rect(center=, topleft=)`), in-place motion (`move_ip(dx, dy)`), collision tests (`colliderect`, `collidelist`, `collidepoint`). Three depths solve the SAME task — load a sprite, move it with arrow keys, draw it bouncing off the edges — at depths: blit at integer pos → Rect-driven position with `clamp_ip` → group of sprites with sprite class + dirty-rect render for performance.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Load a sprite, move it with arrow keys.\n# APPROACH  - Track an (x, y); blit each frame.\n# STRENGTHS - Three lines of game logic.\n# WEAKNESSES- Forgets .convert_alpha(); slow blit; no Rect; no boundary check.\nimport pygame, sys\n\npygame.init()\nscreen = pygame.display.set_mode((640, 360))\nclock = pygame.time.Clock()\n\nimg = pygame.image.load(\"ship.png\")                  # !! no .convert_alpha()\nx, y = 100, 100\n\nwhile True:\n    for e in pygame.event.get():\n        if e.type == pygame.QUIT: pygame.quit(); sys.exit()\n\n    keys = pygame.key.get_pressed()\n    if keys[pygame.K_LEFT]:  x -= 4\n    if keys[pygame.K_RIGHT]: x += 4\n    if keys[pygame.K_UP]:    y -= 4\n    if keys[pygame.K_DOWN]:  y += 4\n\n    screen.fill((20, 20, 30))\n    screen.blit(img, (x, y))\n    pygame.display.flip()\n    clock.tick(60)\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — sprite + arrow keys — using Rect for position and\n#             clamp_ip for boundaries; .convert_alpha() for fast blit.\n# APPROACH  - rect = img.get_rect(center=(x, y)); rect.move_ip + clamp_ip.\n# STRENGTHS - 5-10x faster blit; clean boundary clamp; collision-ready.\n# WEAKNESSES- Single sprite; no group / batched rendering.\nimport pygame, sys\n\npygame.init()\nW, H = 640, 360\nscreen = pygame.display.set_mode((W, H))\nclock  = pygame.time.Clock()\n\nimg  = pygame.image.load(\"ship.png\").convert_alpha()  # match screen format + alpha\nrect = img.get_rect(center=(W // 2, H // 2))\nbounds = pygame.Rect(0, 0, W, H)\n\nSPEED = 240   # px/s\n\nwhile True:\n    dt = clock.tick(60) / 1000.0\n    for e in pygame.event.get():\n        if e.type == pygame.QUIT: pygame.quit(); sys.exit()\n\n    keys = pygame.key.get_pressed()\n    dx = (keys[pygame.K_RIGHT] - keys[pygame.K_LEFT]) * SPEED * dt\n    dy = (keys[pygame.K_DOWN]  - keys[pygame.K_UP])   * SPEED * dt\n    rect.move_ip(int(dx), int(dy))\n    rect.clamp_ip(bounds)                             # keep inside the window\n\n    screen.fill((20, 20, 30))\n    screen.blit(img, rect)\n    pygame.display.flip()\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — controllable sprite — production: pygame.sprite.Sprite\n#             subclass, sprite group, dirty-rect rendering, normalized\n#             diagonal movement.\n# APPROACH  - Group.draw + Group.update + display.update(dirty rects).\n# STRENGTHS - Scales to many sprites; only dirty rects redraw each frame;\n#             diagonal speed not 1.41x faster.\n# WEAKNESSES- Dirty-rect tracking has a small CPU cost; on modern GPUs\n#             plain display.flip() is often fine.\nfrom __future__ import annotations\nimport pygame\nimport sys\nimport math\n\n\nclass Player(pygame.sprite.Sprite):\n    SPEED = 240.0  # px/s\n\n    def __init__(self, image: pygame.Surface, pos: tuple[int, int]):\n        super().__init__()\n        self.image = image\n        self.rect  = image.get_rect(center=pos)\n        # Float position so motion isn't quantized to int every frame.\n        self._fx, self._fy = float(self.rect.x), float(self.rect.y)\n\n    def update(self, dt: float, bounds: pygame.Rect) -> None:\n        keys = pygame.key.get_pressed()\n        dx = keys[pygame.K_RIGHT] - keys[pygame.K_LEFT]\n        dy = keys[pygame.K_DOWN]  - keys[pygame.K_UP]\n        if dx or dy:\n            mag = math.hypot(dx, dy)\n            dx /= mag; dy /= mag                      # normalize diagonal\n        self._fx += dx * self.SPEED * dt\n        self._fy += dy * self.SPEED * dt\n        self.rect.x = int(self._fx); self.rect.y = int(self._fy)\n        self.rect.clamp_ip(bounds)\n        self._fx, self._fy = self.rect.x, self.rect.y\n\n\ndef main() -> None:\n    pygame.init()\n    W, H = 800, 450\n    screen = pygame.display.set_mode((W, H))\n    bg = pygame.Surface(screen.get_size()).convert()\n    bg.fill((20, 20, 30))\n    screen.blit(bg, (0, 0))\n    pygame.display.flip()\n\n    clock = pygame.time.Clock()\n    img   = pygame.image.load(\"ship.png\").convert_alpha()\n    player = Player(img, (W // 2, H // 2))\n    sprites = pygame.sprite.Group(player)\n    bounds = pygame.Rect(0, 0, W, H)\n\n    while True:\n        dt = clock.tick(60) / 1000.0\n        for e in pygame.event.get():\n            if e.type == pygame.QUIT: pygame.quit(); sys.exit()\n\n        # Erase old positions with the background.\n        sprites.clear(screen, bg)\n        sprites.update(dt, bounds)\n        # draw() returns a list of dirty rects (changed regions).\n        dirty = sprites.draw(screen)\n        pygame.display.update(dirty)\n\n\nif __name__ == \"__main__\":\n    main()\n\n# Decision rule:\n#   Always after image.load                       -> .convert() (opaque) or .convert_alpha().\n#   Need a Rect from a surface                    -> surf.get_rect(topleft=, center=, midbottom=).\n#   Need to keep a sprite inside a window          -> rect.clamp_ip(bounds).\n#   Need fast collision against many              -> rect.collidelist / collidelistall.\n#   Need pixel-perfect collision                   -> pygame.mask.from_surface + Mask.overlap.\n#   Need transparent off-screen surface            -> pygame.Surface(size, SRCALPHA).\n#   Many moving sprites                            -> pygame.sprite.Group + dirty-rect update.\n#   Single full-screen background                  -> blit + display.flip(); dirty rects waste CPU.\n\n# Anti-pattern:\n#   img = pygame.image.load(\"ship.png\")           # never .convert()ed\n# Loaded surfaces are in a generic 32-bit RGBA format; blitting against\n# the screen requires per-pixel format conversion every frame. .convert()\n# bakes that conversion ONCE at load time. Always do it.\n"
                  }
        ],
        tips: [
                  "Always `.convert()` (opaque) or `.convert_alpha()` (transparent) right after `pygame.image.load(...)` — 5-10× faster blit.",
                  "Use `surface.get_rect(center=...)` / `topleft=...` / `midbottom=...` to anchor positioning.",
                  "Float position internally, `int()` only when assigning to `rect.x/y` — int truncation drops sub-pixel motion.",
                  "Normalize diagonal movement (`math.hypot`) — otherwise diagonal speed is √2 ≈ 1.41× cardinal speed.",
                  "`pygame.sprite.Group` + `Group.draw / update / clear` + `display.update(dirty)` is the standard pattern for many sprites."
        ],
        mistake: "Calling `pygame.image.load(...)` without `.convert_alpha()`. Each blit re-runs format conversion; framerate tanks at 100+ sprites.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },

  // ── Section 2: Collisions and physics ─────────────────────────────────────────
  {
    id: "physics",
    title: "Collisions and physics",
    entries: [
      {
        id: "pygame-collisions-pymunk",
        fn: "Rect collision / Mask / pymunk physics — when to upgrade",
        desc: "For arcade games, `rect.colliderect(other)` is enough. For pixel-perfect, use `pygame.mask.from_surface(surf)` + `Mask.overlap`. For real physics (gravity, friction, joints), use `pymunk` (Chipmunk2D bindings) — pygame for rendering, pymunk for simulation.",
        category: "physics",
        subtitle: "rect.colliderect / collidelist / collidelistall, Sprite.spritecollide(group, sprite, dokill=, collided=), pygame.mask.from_surface + Mask.overlap, pymunk Space + Body + Shape + step, pymunk units (pixels by convention but conversion is on you), draw a pymunk body via its shape -> pygame.draw",
        signature: "rect.colliderect(other); pygame.mask.from_surface(s).overlap(m2, offset); space = pymunk.Space(); space.gravity = (0, 900); space.step(1/60)",
        descLong: "Three tiers of collision. **Rect** is fast and simple — `colliderect`, `collidepoint`, `collidelist`. **Mask** is pixel-precise — make a `Mask` from each Sprite's alpha, call `mask.overlap(other_mask, offset)` for non-rectangular shapes. **pymunk** wraps Chipmunk2D, the same engine many indie games ship with — define `Space`, add `Body` (mass + position) and `Shape` (geometry + friction + elasticity), call `space.step(dt)` each frame; render by reading body positions back. Three depths solve the SAME task — make a player-controlled box bounce off walls — at depths: rect-only collision, manual reflection → mask collision against an irregular obstacle → pymunk physics with gravity, friction, and walls as static segments.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Box bounces off the four walls; rect-only collision.\n# APPROACH  - rect.colliderect against four wall rects; flip velocity.\n# STRENGTHS - Trivial; no extra deps.\n# WEAKNESSES- No gravity; no friction; no rotation; no irregular shapes.\nimport pygame, sys\n\npygame.init()\nW, H = 640, 360\nscreen = pygame.display.set_mode((W, H))\nclock = pygame.time.Clock()\n\nbox = pygame.Rect(100, 100, 40, 40)\nvx, vy = 5, 4\nwalls = [\n    pygame.Rect(0, 0, W, 1), pygame.Rect(0, H-1, W, 1),\n    pygame.Rect(0, 0, 1, H), pygame.Rect(W-1, 0, 1, H),\n]\n\nwhile True:\n    for e in pygame.event.get():\n        if e.type == pygame.QUIT: pygame.quit(); sys.exit()\n\n    box.x += vx; box.y += vy\n    for w in walls:\n        if box.colliderect(w):\n            if w.width >  w.height: vy = -vy        # horizontal wall\n            else:                    vx = -vx       # vertical wall\n\n    screen.fill((20, 20, 30))\n    pygame.draw.rect(screen, (240, 220, 80), box)\n    for w in walls: pygame.draw.rect(screen, (100, 100, 100), w)\n    pygame.display.flip()\n    clock.tick(60)\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — box bounces — but against an IRREGULAR obstacle\n#             (a star-shaped sprite) using mask collision.\n# APPROACH  - Mask.from_surface for both; Mask.overlap with offset.\n# STRENGTHS - Pixel-perfect against any shape.\n# WEAKNESSES- Doesn't compute the contact normal; ad-hoc bounce.\nimport pygame, sys\n\npygame.init()\nW, H = 640, 360\nscreen = pygame.display.set_mode((W, H))\nclock  = pygame.time.Clock()\n\nbox_img = pygame.Surface((40, 40), pygame.SRCALPHA)\npygame.draw.rect(box_img, (240, 220, 80), box_img.get_rect())\nbox_rect = box_img.get_rect(topleft=(50, 50))\nbox_mask = pygame.mask.from_surface(box_img)\n\n# Star-shape obstacle (loaded from a transparent PNG in real code).\nstar_img = pygame.image.load(\"star.png\").convert_alpha()\nstar_rect = star_img.get_rect(center=(W//2, H//2))\nstar_mask = pygame.mask.from_surface(star_img)\n\nvx, vy = 4, 3\n\nwhile True:\n    for e in pygame.event.get():\n        if e.type == pygame.QUIT: pygame.quit(); sys.exit()\n\n    box_rect.x += vx; box_rect.y += vy\n\n    # Window edges (rect collision is fine for axis-aligned boundaries).\n    if box_rect.left < 0 or box_rect.right > W:  vx = -vx\n    if box_rect.top  < 0 or box_rect.bottom > H: vy = -vy\n\n    # Pixel-perfect against the star.\n    offset = (star_rect.x - box_rect.x, star_rect.y - box_rect.y)\n    if box_mask.overlap(star_mask, offset):\n        # Crude reflection: flip both axes (real engine = compute normal).\n        vx = -vx; vy = -vy\n\n    screen.fill((20, 20, 30))\n    screen.blit(star_img, star_rect)\n    screen.blit(box_img,  box_rect)\n    pygame.display.flip()\n    clock.tick(60)\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — bouncing box — production: pymunk physics with\n#             gravity, friction, elasticity; pygame just renders.\n# APPROACH  - pymunk.Space + dynamic Body + static walls; sync rect from\n#             body.position each frame.\n# STRENGTHS - Real physics: bounces feel right, falls under gravity,\n#             friction with walls, joints/springs/constraints available.\n# WEAKNESSES- pymunk is a separate dep; coordinate frames need care\n#             (pymunk Y is up by default; flip when drawing).\nfrom __future__ import annotations\nimport pygame\nimport sys\nimport pymunk\n\n\ndef to_pygame(p: pymunk.Vec2d, h: int) -> tuple[int, int]:\n    \"\"\"Convert pymunk world coords (Y up) to pygame screen coords (Y down).\"\"\"\n    return int(p.x), int(h - p.y)\n\n\ndef main() -> None:\n    pygame.init()\n    W, H = 800, 450\n    screen = pygame.display.set_mode((W, H))\n    clock  = pygame.time.Clock()\n    font   = pygame.font.SysFont(\"monospace\", 14)\n\n    # --- pymunk world ---\n    space = pymunk.Space()\n    space.gravity = (0, -900)                         # gravity points -Y in pymunk\n\n    # Walls (static): bottom, left, right.\n    static_lines = [\n        pymunk.Segment(space.static_body, (0, 0),     (W, 0),     2),\n        pymunk.Segment(space.static_body, (0, 0),     (0, H),     2),\n        pymunk.Segment(space.static_body, (W, 0),     (W, H),     2),\n    ]\n    for s in static_lines:\n        s.elasticity = 0.95\n        s.friction   = 0.5\n        space.add(s)\n\n    # Dynamic box.\n    mass, size = 1.0, 40\n    moment = pymunk.moment_for_box(mass, (size, size))\n    body = pymunk.Body(mass, moment)\n    body.position = (100, H - 100)                    # pymunk world coords\n    box_shape = pymunk.Poly.create_box(body, (size, size))\n    box_shape.elasticity = 0.85\n    box_shape.friction   = 0.4\n    space.add(body, box_shape)\n\n    PHYSICS_HZ = 240\n    DT = 1.0 / PHYSICS_HZ\n\n    while True:\n        for e in pygame.event.get():\n            if e.type == pygame.QUIT: pygame.quit(); sys.exit()\n            elif e.type == pygame.MOUSEBUTTONDOWN:\n                # Apply an impulse on click.\n                mx, my = e.pos\n                target = pymunk.Vec2d(mx, H - my) - body.position\n                body.apply_impulse_at_local_point(target * 5)\n\n        # Substep for stability.\n        for _ in range(4):\n            space.step(DT)\n\n        screen.fill((20, 20, 30))\n\n        # Walls.\n        for s in static_lines:\n            a = to_pygame(s.a, H); b = to_pygame(s.b, H)\n            pygame.draw.line(screen, (180, 180, 180), a, b, 2)\n\n        # Box: read 4 corners from the polygon shape.\n        verts = [body.local_to_world(v) for v in box_shape.get_vertices()]\n        verts_screen = [to_pygame(v, H) for v in verts]\n        pygame.draw.polygon(screen, (240, 220, 80), verts_screen)\n\n        screen.blit(font.render(\"click to apply impulse\", True, (200, 200, 200)),\n                    (10, 10))\n        pygame.display.flip()\n        clock.tick(60)\n\n\nif __name__ == \"__main__\":\n    main()\n\n# Decision rule:\n#   Axis-aligned arcade game                 -> rect.colliderect / collidelist.\n#   Pixel-perfect against irregular shapes   -> pygame.mask.from_surface + Mask.overlap.\n#   Need many vs many collision              -> sprite.groupcollide(g1, g2, dokill_a, dokill_b).\n#   Need real physics (gravity, friction)    -> pymunk (Chipmunk2D); pygame just renders.\n#   Need tile-based collision                 -> rect against grid cells; many engines do\n#                                                 this faster than pymunk for tile maps.\n#   Need 2D rigid body + joints + springs    -> pymunk.\n#   Need 3D physics                            -> not pygame; reach for Panda3D + Bullet,\n#                                                 or Godot/Unity with a Python script binding.\n#   Need GPU sprite batching                   -> arcade or moderngl-pygame\n#                                                 (pygame is software-rendered).\n\n# Anti-pattern:\n#   for s in obstacles:\n#       if rect.colliderect(s.rect): handle(s)\n# Linear scan over thousands of sprites kills the frame budget. Use\n# pygame.sprite.Group + groupcollide, or a spatial hash for very\n# large worlds.\n"
                  }
        ],
        tips: [
                  "Use `rect.colliderect` first — switch to masks only when you need pixel-perfect against irregular shapes.",
                  "For many-vs-many, `pygame.sprite.groupcollide(group_a, group_b, dokill_a, dokill_b)` is much faster than nested loops.",
                  "For real physics (gravity, friction, joints), reach for **pymunk** — pygame for render, pymunk for simulation.",
                  "pymunk's default Y axis is **up** (world coords) — convert to pygame's Y-down screen coords explicitly.",
                  "Substep `space.step(dt)` 2-8 times per frame for stability — fast bodies tunneling through walls is a sign you need more substeps."
        ],
        mistake: "Treating pymunk world coordinates as pygame screen coordinates — your gravity falls UP onscreen until you flip Y when rendering.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },

  // ── Section 3: Game-loop patterns and engine choice ─────────────────────────────────────────
  {
    id: "patterns",
    title: "Game-loop patterns and engine choice",
    entries: [
      {
        id: "gamedev-patterns-engines",
        fn: "Game-state stack / scene manager / engine choice",
        desc: "Most games are state machines: title → menu → play → pause → game-over. Implement as a stack of `Scene` objects (each with `handle_event`, `update(dt)`, `draw(screen)`); push/pop transitions. For polished 2D, **arcade** is faster than pygame; for full engines reach for **Godot** or **Unity** — Python's role becomes integration, not the runtime.",
        category: "patterns",
        subtitle: "Scene base class with handle_event / update / draw, stack-based scene manager (push, pop, swap), pause via push, modal overlays, transition fades, pygame vs arcade (GPU-batched, faster) vs pyglet vs Godot (GDScript / Python via godot-python) vs Unity (C# only)",
        signature: "class Scene: def handle_event(e): ...; def update(dt): ...; def draw(screen): ...\\nstack = [TitleScene()]; while ...: stack[-1].update(dt); stack[-1].draw(screen)",
        descLong: "A scene stack lets you push the pause menu without losing the gameplay scene underneath, transition with a fade, and pop back. For pure pygame, you implement the stack yourself; **arcade** ships a `View` class with the same idea but GPU-batched rendering. Beyond pygame: **arcade** for 2D with sprites at scale (10k+ sprites without dropping frames); **pyglet** for OpenGL-y stuff; **Godot** for a real engine (use GDScript — its native language — and embed Python via `godot-python` if you must). Unity is C#-only. Three depths solve the SAME task — title screen → play → game-over flow — at depths: ad-hoc state variable + if-chain → Scene stack with push/pop transitions → arcade.View skeleton with the same flow, demonstrating engine swap.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Title -> Play -> Game Over flow (press SPACE to advance).\n# APPROACH  - One state variable + if/elif inside the loop.\n# STRENGTHS - Smallest possible state machine.\n# WEAKNESSES- Gets ugly past 2-3 states; no pause / overlay support.\nimport pygame, sys\n\npygame.init()\nW, H = 640, 360\nscreen = pygame.display.set_mode((W, H))\nclock  = pygame.time.Clock()\nfont   = pygame.font.SysFont(\"sans\", 36)\n\nstate = \"title\"\n\nwhile True:\n    for e in pygame.event.get():\n        if e.type == pygame.QUIT: pygame.quit(); sys.exit()\n        if e.type == pygame.KEYDOWN and e.key == pygame.K_SPACE:\n            state = {\"title\": \"play\", \"play\": \"over\", \"over\": \"title\"}[state]\n\n    screen.fill((20, 20, 30))\n    if state == \"title\":\n        screen.blit(font.render(\"TITLE - press SPACE\", True, (240, 240, 240)), (W//2 - 200, H//2))\n    elif state == \"play\":\n        screen.blit(font.render(\"PLAYING\", True, (120, 220, 120)), (W//2 - 80, H//2))\n    elif state == \"over\":\n        screen.blit(font.render(\"GAME OVER\", True, (220, 80, 80)),   (W//2 - 110, H//2))\n\n    pygame.display.flip()\n    clock.tick(60)\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — title/play/over — using a Scene stack.\n# APPROACH  - Scene base class; manager.push / pop / swap.\n# STRENGTHS - Pause = push the pause scene; resume = pop. Clean overlays.\n# WEAKNESSES- Pygame still software-rendered; no GPU sprite batching.\nimport pygame, sys\n\n\nclass Scene:\n    def handle_event(self, event, manager): ...\n    def update(self, dt, manager): ...\n    def draw(self, screen): ...\n\n\nclass SceneManager:\n    def __init__(self, initial: Scene): self.stack: list[Scene] = [initial]\n    def push(self, s: Scene):  self.stack.append(s)\n    def pop(self):             self.stack.pop()\n    def swap(self, s: Scene):  self.stack[-1] = s\n    @property\n    def top(self) -> Scene:    return self.stack[-1]\n\n\nclass TitleScene(Scene):\n    def handle_event(self, e, mgr):\n        if e.type == pygame.KEYDOWN and e.key == pygame.K_SPACE:\n            mgr.swap(PlayScene())\n\n    def draw(self, screen):\n        screen.fill((20, 20, 30))\n        font = pygame.font.SysFont(\"sans\", 36)\n        screen.blit(font.render(\"TITLE - SPACE to play\", True, (240, 240, 240)), (60, 140))\n\n\nclass PlayScene(Scene):\n    def handle_event(self, e, mgr):\n        if e.type == pygame.KEYDOWN:\n            if e.key == pygame.K_p:     mgr.push(PauseScene())\n            elif e.key == pygame.K_RETURN: mgr.swap(GameOverScene())\n\n    def draw(self, screen):\n        screen.fill((30, 60, 30))\n        font = pygame.font.SysFont(\"sans\", 28)\n        screen.blit(font.render(\"PLAYING - P to pause / RETURN to die\", True, (220, 220, 220)),\n                    (40, 140))\n\n\nclass PauseScene(Scene):\n    def handle_event(self, e, mgr):\n        if e.type == pygame.KEYDOWN and e.key == pygame.K_p: mgr.pop()\n\n    def draw(self, screen):\n        # Translucent overlay: scene below still drew its frame.\n        overlay = pygame.Surface(screen.get_size(), pygame.SRCALPHA)\n        overlay.fill((0, 0, 0, 160))\n        screen.blit(overlay, (0, 0))\n        font = pygame.font.SysFont(\"sans\", 36)\n        screen.blit(font.render(\"PAUSED\", True, (255, 255, 255)), (260, 150))\n\n\nclass GameOverScene(Scene):\n    def handle_event(self, e, mgr):\n        if e.type == pygame.KEYDOWN and e.key == pygame.K_SPACE: mgr.swap(TitleScene())\n\n    def draw(self, screen):\n        screen.fill((40, 20, 20))\n        font = pygame.font.SysFont(\"sans\", 36)\n        screen.blit(font.render(\"GAME OVER - SPACE\", True, (240, 80, 80)), (140, 150))\n\n\ndef main():\n    pygame.init()\n    screen = pygame.display.set_mode((640, 360))\n    clock  = pygame.time.Clock()\n    mgr    = SceneManager(TitleScene())\n\n    while True:\n        dt = clock.tick(60) / 1000.0\n        for e in pygame.event.get():\n            if e.type == pygame.QUIT: pygame.quit(); sys.exit()\n            mgr.top.handle_event(e, mgr)\n\n        # PauseScene wants the gameplay frame underneath - draw the\n        # scene below first, then the top scene on top.\n        for s in mgr.stack:\n            s.draw(screen)\n\n        # Only the top scene updates each frame (paused scenes are frozen).\n        mgr.top.update(dt, mgr)\n\n        pygame.display.flip()\n\n\nmain()\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — title/play/over — but in arcade (GPU-batched), showing\n#             the migration target when you outgrow pygame.\n# APPROACH  - arcade.View with on_show / on_update / on_draw; window.show_view.\n# STRENGTHS - GPU sprite batching (10k+ sprites at 60 FPS); same logical\n#             structure as pygame Scene stack; modern arcade==3.x.\n# WEAKNESSES- Different API to learn; arcade itself is opinionated about\n#             coordinate origin (bottom-left).\nimport arcade\n\n\nW, H = 640, 360\n\n\nclass TitleView(arcade.View):\n    def on_draw(self):\n        self.clear()\n        arcade.draw_text(\n            \"TITLE - SPACE to play\", W//2, H//2,\n            arcade.color.WHITE, font_size=24, anchor_x=\"center\",\n        )\n\n    def on_key_press(self, symbol, modifiers):\n        if symbol == arcade.key.SPACE:\n            self.window.show_view(PlayView())\n\n\nclass PlayView(arcade.View):\n    def on_show_view(self):\n        arcade.set_background_color(arcade.color.DARK_GREEN)\n\n    def on_draw(self):\n        self.clear()\n        arcade.draw_text(\n            \"PLAYING - RETURN to die\", W//2, H//2,\n            arcade.color.WHITE, font_size=24, anchor_x=\"center\",\n        )\n\n    def on_key_press(self, symbol, modifiers):\n        if symbol == arcade.key.ENTER:\n            self.window.show_view(GameOverView())\n\n\nclass GameOverView(arcade.View):\n    def on_show_view(self):\n        arcade.set_background_color(arcade.color.DARK_RED)\n\n    def on_draw(self):\n        self.clear()\n        arcade.draw_text(\n            \"GAME OVER - SPACE\", W//2, H//2,\n            arcade.color.WHITE, font_size=24, anchor_x=\"center\",\n        )\n\n    def on_key_press(self, symbol, modifiers):\n        if symbol == arcade.key.SPACE:\n            self.window.show_view(TitleView())\n\n\ndef main():\n    window = arcade.Window(W, H, \"scene demo\")\n    window.show_view(TitleView())\n    arcade.run()\n\n\n# main()\n\n# Decision rule:\n#   Tiny prototype                                -> single state variable.\n#   Real game with menu / pause / overlays        -> Scene stack (junior tier).\n#   2D, want GPU sprite batching + perf            -> arcade (3.x).\n#   2D + retro effects + lower-level OpenGL        -> pyglet or moderngl-pygame.\n#   2D/3D engine with editor + GDScript            -> Godot 4 (native; godot-python optional).\n#   Want Python in Unity                            -> not happening; Unity is C#.\n#   Mobile target                                   -> Pygbag (pygame -> WASM) or arcade -> WASM\n#                                                     for web; Kivy / BeeWare for native.\n#   Multiplayer netcode                              -> deterministic fixed-step (basics tier 3)\n#                                                       + bsdiff snapshots; consider Mirror (Unity)\n#                                                       or Godot HLAPI long-term.\n\n# Anti-pattern:\n#   global state = \"play\"\n#   if state == \"menu\": ...elif state == \"play\": ...elif state == \"shop\": ...\n# Past 3 states this becomes unmaintainable and impossible to add overlay\n# behavior (pause-over-play). Switch to a Scene/View stack early.\n"
                  }
        ],
        tips: [
                  "A Scene/View stack handles menus, pauses, and overlays cleanly — push for overlays, pop to dismiss, swap to transition.",
                  "Only the **top** scene receives `update`/`handle_event`; scenes below stay frozen — that's how pause works.",
                  "**arcade** (3.x) is the GPU-accelerated next step from pygame — the same `View` idea, with sprite batching that holds 60 FPS at 10k+ sprites.",
                  "For 2D/3D engines with editors, **Godot 4** is the open-source winner; GDScript is Python-like, and `godot-python` lets you write game logic in Python.",
                  "For web-deployable pygame games, **Pygbag** ships pygame to WASM — `pygbag main.py` and you've got an itch.io build."
        ],
        mistake: "Long if/elif chains over a state variable. Past 3 states it becomes unmaintainable, and adding an overlay (pause-over-play) requires rewriting it. Switch to a Scene stack early.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },
]

export default { meta, sections }

export const meta = {
  "id": "concurrency",
  "label": "Concurrency & Parallelism",
  "icon": "🐍",
  "description": "Python concurrency: asyncio, threading, multiprocessing, subprocess, concurrent.futures, and synchronization."
}

export const sections = [

  // ── Section 1: asyncio — Asynchronous I/O ─────────────────────────────────────────
  {
    id: "asyncio",
    title: "asyncio — Asynchronous I/O",
    entries: [
      {
        id: "asyncio-basics",
        fn: "asyncio Fundamentals — async/await, Tasks & Gathering",
        desc: "Write concurrent I/O-bound code with async/await — HTTP requests, file I/O, database queries running concurrently.",
        category: "asyncio",
        subtitle: "async def, await, asyncio.gather, asyncio.create_task",
        signature: "async def main(): ...  |  await asyncio.gather(*coros)  |  asyncio.run(main())",
        descLong: "asyncio is Python's built-in async framework for concurrent I/O. async def defines a coroutine. await pauses until a result is ready, letting other coroutines run. asyncio.gather() runs multiple coroutines concurrently and collects results. asyncio.create_task() schedules a coroutine to run in the background. asyncio.run() starts the event loop. Use for: HTTP clients, web servers, database queries, file I/O — anything that waits on external systems.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of asyncio Fundamentals — async/await, Tasks & Gathering — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nimport asyncio\nasync def hello(name: str) -> str:\n    await asyncio.sleep(0.1)                    # pretend I/O\n    return f\"hello, {name}\"\nprint(asyncio.run(hello(\"Alice\")))               # one entry point starts the loop"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of asyncio Fundamentals — async/await, Tasks & Gathering — common patterns you'll see in production.\n# APPROACH  - Combine asyncio Fundamentals — async/await, Tasks & Gathering with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nimport asyncio\nimport httpx\nasync def fetch(client, url):\n    r = await client.get(url, timeout=10)\n    r.raise_for_status()\n    return r.json()\nasync def fetch_all(urls):\n    async with httpx.AsyncClient() as client:\n        # 1) gather — N requests run AT THE SAME TIME (slowest one wins, not sum)\n        return await asyncio.gather(\n            *(fetch(client, u) for u in urls),\n            return_exceptions=True,             # one failure doesn't kill the rest\n        )\nasync def background():\n    # 2) create_task — start work NOW, await later\n    bg = asyncio.create_task(slow_compute())\n    quick = await quick_thing()\n    heavy = await bg                            # already in progress while quick ran\n    return quick, heavy\nprint(asyncio.run(fetch_all([\"https://httpbin.org/get\"] * 5)))\nasync def slow_compute(): await asyncio.sleep(1); return 42\nasync def quick_thing(): return 1"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of asyncio Fundamentals — async/await, Tasks & Gathering — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nimport asyncio\nimport time\n# 1) TaskGroup — STRUCTURED CONCURRENCY: if one task fails, siblings are\n#    cancelled and ALL exceptions surface together as ExceptionGroup.\nasync def parallel_fetch(urls):\n    async with asyncio.TaskGroup() as tg:\n        tasks = [tg.create_task(fetch(u)) for u in urls]\n    return [t.result() for t in tasks]\n# Catch the bundle of errors with except*\nasync def safe(urls):\n    try:\n        return await parallel_fetch(urls)\n    except* httpx.HTTPError as eg:\n        for e in eg.exceptions: log(e)\n        return []\n# 2) Timeouts via async with — applies to the whole block, not just one await\nasync def fetch_with_budget(url, seconds=2.0):\n    async with asyncio.timeout(seconds):        # 3.11+\n        return await fetch(url)\n    # asyncio.TimeoutError on overrun\n# 3) Bridge to BLOCKING code — never call time.sleep / requests.get inside async\ndef blocking_io():\n    time.sleep(1)                                # would freeze the event loop\n    return \"done\"\nasync def use_blocking():\n    return await asyncio.to_thread(blocking_io)  # runs in a worker thread\n# 4) Cancellation discipline — clean up in finally; re-raise CancelledError\nasync def worker(q):\n    try:\n        while True:\n            item = await q.get()\n            await process(item)\n    except asyncio.CancelledError:\n        await close_resource()                   # cleanup\n        raise                                    # MUST re-raise to confirm\n# Decision rule:\n#   N concurrent calls, fail-fast              -> async with TaskGroup() (3.11+)\n#   N concurrent calls, tolerate failures       -> asyncio.gather(..., return_exceptions=True)\n#   single deadline                              -> async with asyncio.timeout(s)\n#   blocking library inside async code           -> asyncio.to_thread / loop.run_in_executor\n#   long-lived background worker                 -> create_task; handle CancelledError\n#   inside Jupyter / FastAPI                     -> NEVER asyncio.run; just await\n#   CPU-bound work                                -> NOT async; ProcessPoolExecutor\n#\n# Anti-pattern: try/except Exception that swallows CancelledError\n#   The task ignores cancel; shutdown hangs. Catch CancelledError separately\n#   (or re-raise after cleanup) so the runtime can stop the task.\nimport httpx\ndef log(_): pass\nasync def fetch(_): await asyncio.sleep(0); return {}\nasync def process(_): pass\nasync def close_resource(): pass"
                  }
        ],
        tips: [
                  "asyncio.gather() is the workhorse — run 100 HTTP requests concurrently instead of sequentially. For fail-fast structured concurrency in 3.11+, prefer `async with asyncio.TaskGroup()` — it cancels siblings on first failure and bundles errors into an ExceptionGroup (catch with `except*`).",
                  "Use asyncio.create_task() to start work in the background while you do other things — await it when you need the result.",
                  "return_exceptions=True in gather prevents one failure from canceling everything — handle errors per-task.",
                  "asyncio is for I/O-bound concurrency (network, files), NOT CPU-bound work — use multiprocessing for CPU. For BLOCKING libraries inside async code, wrap with `await asyncio.to_thread(fn)` (3.9+) so the event loop stays responsive. Apply deadlines with `async with asyncio.timeout(s)` (3.11+) over a whole block; `wait_for` is the single-coroutine version."
        ],
        mistake: "try/except Exception that catches and swallows CancelledError — the task ignores cancel; shutdown hangs; the runtime can't reclaim resources. Catch CancelledError separately, clean up in finally, then RE-RAISE so the runtime can stop the task. Also: using asyncio for CPU-bound work blocks the event loop; reach for ProcessPoolExecutor instead.",
        shorthand: {
          verbose: "async def fetch_all(urls):\n    results = []\n    for url in urls:\n        result = await fetch_url(url)\n        results.append(result)\n    return results",
          concise: "async def fetch_all(urls):\n    return await asyncio.gather(*[fetch_url(url) for url in urls])",
        },
      },
      {
        id: "asyncio-patterns",
        fn: "asyncio Patterns — Semaphores, Queues & Streaming",
        desc: "Rate limiting with semaphores, producer-consumer with queues, and async generators for streaming data.",
        category: "asyncio",
        subtitle: "asyncio.Semaphore, asyncio.Queue, async generators",
        signature: "async with semaphore: ...  |  await queue.put(item)  |  async for item in stream():",
        descLong: "Advanced asyncio patterns for production use. Semaphores limit concurrent operations (e.g., max 10 simultaneous HTTP connections). Queues implement producer-consumer patterns (crawlers, pipelines). Async generators (async def + yield) produce streaming data. TaskGroups (Python 3.11+) provide structured concurrency with proper error handling and cancellation.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of asyncio Patterns — Semaphores, Queues & Streaming — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nimport asyncio\nsem = asyncio.Semaphore(3)                        # at most 3 in flight\nasync def bounded(i):\n    async with sem:                               # waits until a slot is free\n        await asyncio.sleep(0.1)\n        return i\nasync def main():\n    return await asyncio.gather(*(bounded(i) for i in range(10)))\nprint(asyncio.run(main()))"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of asyncio Patterns — Semaphores, Queues & Streaming — common patterns you'll see in production.\n# APPROACH  - Combine asyncio Patterns — Semaphores, Queues & Streaming with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nimport asyncio\nN = 5\nSENTINEL = None\nasync def producer(q, items):\n    for it in items:\n        await q.put(it)                           # blocks if queue full -> backpressure\n    for _ in range(N):\n        await q.put(SENTINEL)                     # one sentinel per consumer\nasync def consumer(name, q):\n    while True:\n        it = await q.get()\n        if it is SENTINEL:\n            q.task_done()\n            return\n        try:\n            await process(it)\n        finally:\n            q.task_done()\nasync def main():\n    q = asyncio.Queue(maxsize=10)\n    consumers = [asyncio.create_task(consumer(f\"w{i}\", q)) for i in range(N)]\n    await producer(q, range(50))\n    await q.join()\n    await asyncio.gather(*consumers)\nasync def process(_): await asyncio.sleep(0.05)\nasyncio.run(main())"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of asyncio Patterns — Semaphores, Queues & Streaming — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nimport asyncio\n# 1) Async generator — stream items as they arrive (no list materialization)\nasync def paginate(base: str):\n    page = 1\n    while True:\n        items = await fetch(f\"{base}?page={page}\")\n        if not items:\n            return\n        for it in items:\n            yield it                              # consumer can break early\n        page += 1\n# Use it\nasync def consume_pages():\n    async for item in paginate(\"https://api.example.com/items\"):\n        await handle(item)\n# 2) Bounded fan-out — Semaphore + TaskGroup for STRUCTURED concurrency\nasync def crawl(urls, *, concurrency: int = 10):\n    sem = asyncio.Semaphore(concurrency)\n    async def one(u):\n        async with sem:\n            return await fetch(u)\n    async with asyncio.TaskGroup() as tg:        # 3.11+\n        tasks = [tg.create_task(one(u)) for u in urls]\n    return [t.result() for t in tasks]\n# 3) Long-running job queue — workers retire on cancel, NOT on sentinels\nasync def run_workers(producer, n_workers=8):\n    q = asyncio.Queue(maxsize=2 * n_workers)     # backpressure tied to worker count\n    workers = [asyncio.create_task(_worker(q)) for _ in range(n_workers)]\n    await producer(q)\n    await q.join()                                # all task_done() received\n    for w in workers: w.cancel()                  # tell idle workers to stop\n    await asyncio.gather(*workers, return_exceptions=True)\nasync def _worker(q):\n    try:\n        while True:\n            item = await q.get()\n            try:\n                await process(item)\n            except Exception as e:\n                log(e)                            # swallow per-item; one bad item != crash\n            finally:\n                q.task_done()\n    except asyncio.CancelledError:\n        return                                    # clean exit at shutdown\n# Decision rule:\n#   bound concurrency to N                  -> Semaphore(N) inside the async fn\n#   N producers, M consumers, queue        -> Queue(maxsize=) for backpressure\n#   results need order                       -> gather (results in submit order)\n#   N items, all-or-nothing semantics       -> TaskGroup + create_task\n#   stream of pages / events                  -> async generator, async for\n#   shutdown a long-lived pool                 -> q.join() + cancel workers\n#\n# Anti-pattern: starting 10_000 tasks with no Semaphore\n#   Hits OS file-descriptor limits, gets rate-limited, blows up the upstream.\n#   Always cap with Semaphore (or TaskGroup with N parallel workers).\nasync def fetch(_): await asyncio.sleep(0); return []\nasync def handle(_): pass\nasync def process(_): pass\ndef log(_): pass"
                  }
        ],
        tips: [
                  "Semaphore(10) prevents overloading APIs — 1000 URLs, but only 10 concurrent requests at any time.",
                  "TaskGroup (3.11+) is safer than gather — if one task fails, all others are cancelled and the error propagates. Combine with a Semaphore for bounded fan-out.",
                  "asyncio.to_thread() wraps blocking code (old libraries, file I/O) for use in async code without blocking the loop.",
                  "Queue with maxsize creates backpressure — producers slow down when consumers can't keep up. For long-lived worker pools, prefer `await q.join()` followed by `worker.cancel()` over per-worker sentinels — workers retire on cancel and shutdown is cleaner. For paginated/streaming sources, write an async generator and `async for` it so the consumer can break early."
        ],
        mistake: "Starting thousands of tasks with no Semaphore — hits OS file-descriptor limits, gets rate-limited, blows up the upstream. Always cap with Semaphore (or a TaskGroup with N parallel workers).",
        shorthand: {
          verbose: "semaphore = Semaphore(10)\nasync def limited_fetch(url):\n    semaphore.acquire()\n    try:\n        return await fetch_url(url)\n    finally:\n        semaphore.release()",
          concise: "semaphore = Semaphore(10)\nasync def limited_fetch(url):\n    async with semaphore:\n        return await fetch_url(url)",
        },
      },
    ],
  },

  // ── Section 2: Threading & Multiprocessing ─────────────────────────────────────────
  {
    id: "threading",
    title: "Threading & Multiprocessing",
    entries: [
      {
        id: "threading-basics",
        fn: "threading — Concurrent I/O with Threads",
        desc: "Run I/O-bound tasks concurrently using threads — limited by the GIL for CPU work but excellent for I/O.",
        category: "Threading",
        subtitle: "Thread, Lock, Event, concurrent.futures.ThreadPoolExecutor",
        signature: "ThreadPoolExecutor(max_workers=10)  |  executor.map(fn, items)",
        descLong: "Python threads run concurrently for I/O operations but are limited by the GIL (Global Interpreter Lock) for CPU work — only one thread executes Python bytecode at a time. ThreadPoolExecutor is the modern high-level API. Use threads for: HTTP requests, file I/O, database queries, subprocess calls. Use Lock/RLock for shared state. Event for signaling between threads. Threading is simpler than asyncio but uses more memory (one OS thread per task).",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of threading — Concurrent I/O with Threads — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nfrom concurrent.futures import ThreadPoolExecutor\nimport requests\ndef fetch(url):\n    return requests.get(url, timeout=10).status_code\nurls = [f\"https://httpbin.org/status/200?i={i}\" for i in range(20)]\nwith ThreadPoolExecutor(max_workers=10) as ex:\n    print(list(ex.map(fetch, urls)))            # results in input order"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of threading — Concurrent I/O with Threads — common patterns you'll see in production.\n# APPROACH  - Combine threading — Concurrent I/O with Threads with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nimport threading\nfrom concurrent.futures import ThreadPoolExecutor, as_completed\n# 1) submit + as_completed — results as they finish (fastest first)\ndef fetch(url):\n    import requests\n    r = requests.get(url, timeout=10)\n    r.raise_for_status()\n    return r.json()\nwith ThreadPoolExecutor(max_workers=20) as ex:\n    futs = {ex.submit(fetch, u): u for u in urls}\n    for f in as_completed(futs):\n        url = futs[f]\n        try:\n            data = f.result()\n        except Exception as e:\n            print(url, \"failed:\", e)\n# 2) Lock — protect shared mutable state from concurrent writes\ncounter = 0\nlock = threading.Lock()\ndef bump():\n    global counter\n    for _ in range(100_000):\n        with lock:                              # auto-release on exception\n            counter += 1                        # without lock: races, lost updates\n# 3) Event — signal between threads (e.g. shutdown)\nstop = threading.Event()\ndef worker():\n    while not stop.is_set():\n        do_work()\n        stop.wait(timeout=1)                    # check every second\n# stop.set()  # main thread later — workers see it on next loop"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of threading — Concurrent I/O with Threads — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nimport threading\n# 1) GIL: threads RELEASE the GIL during I/O and inside C-extensions (numpy,\n#    sklearn, lxml). Pure-Python CPU loops stay serial — use ProcessPoolExecutor.\n#    Test the assumption: profile, don't guess.\n# 2) RLock — needed when the SAME thread must re-acquire (recursive code paths)\nclass Cache:\n    def __init__(self):\n        self._lock = threading.RLock()          # NOT Lock, or self.refresh()->self.refresh() deadlocks\n        self._data = {}\n    def get(self, k):\n        with self._lock:\n            if k not in self._data:\n                self.refresh(k)                  # RE-enters the lock; RLock allows it\n            return self._data[k]\n    def refresh(self, k):\n        with self._lock:\n            self._data[k] = load(k)\n# 3) Thread-local state — per-thread \"globals\" without locks\nctx = threading.local()\ndef handle(req_id):\n    ctx.request_id = req_id                     # invisible to other threads\n    log_with_context()\ndef log_with_context():\n    print(\"rid:\", getattr(ctx, \"request_id\", \"-\"))\n# 4) Daemon vs non-daemon threads\n#    daemon=True   -> killed when the program exits (good for background workers)\n#    daemon=False  -> blocks shutdown until .join() (good for must-complete work)\nt = threading.Thread(target=worker, daemon=True)\n# 5) Sizing rules of thumb\n#    I/O bound, light                -> 10-50 (start at 2 * cpu_count)\n#    DB pool                          -> match max_workers to DB max_connections\n#    HTTP per host                    -> respect upstream concurrency limit\n#    CPU bound (pure Python)          -> NOT threads; ProcessPoolExecutor\n# 6) Cancellation truth — Future.cancel() ONLY works BEFORE a worker picks it up.\n#    Once running, you need cooperative cancellation (an Event the worker checks).\n# Decision rule:\n#   I/O concurrency, simple                  -> ThreadPoolExecutor.map\n#   need progress / error per task            -> submit + as_completed\n#   shared mutable state                       -> Lock; RLock for re-entrant code\n#   per-thread context (request id, db conn)   -> threading.local()\n#   coordinate shutdown across threads          -> threading.Event\n#   pure-Python CPU work                        -> ProcessPoolExecutor instead\n#   tens of thousands of small tasks            -> async I/O instead of threads\n#\n# Anti-pattern: locking everywhere \"to be safe\"\n#   Each lock is a serialization point. Wrong one acquires order across two\n#   locks -> deadlock. Minimize lock scope; consider queue-based hand-off so\n#   only one thread owns mutable state at a time.\ndef do_work(): pass\ndef load(_): return None\nurls = []\ndef worker(): pass"
                  }
        ],
        tips: [
                  "ThreadPoolExecutor is almost always better than raw Thread — it handles pool sizing, error handling, and cleanup.",
                  "as_completed() processes results in finish order — faster responses come first, no waiting for slow ones.",
                  "Use `with lock:` instead of lock.acquire()/release(); reach for `RLock` when the SAME thread re-acquires (recursive code paths) or you'll deadlock against yourself. Use `threading.local()` for per-thread context (request_id, db connection) — invisible to other threads, no locking needed.",
                  "Threads are best for I/O-bound work; for numpy/sklearn (which release the GIL during C calls) threads often beat processes too. For pure-Python CPU loops, ProcessPoolExecutor. Daemon threads (`daemon=True`) die when the program exits — good for background workers; non-daemon must `.join()` before shutdown."
        ],
        mistake: "Locking everywhere \"to be safe\" — each lock is a serialization point, and the wrong acquisition order across two locks gives you a deadlock. Minimize lock scope; consider queue-based hand-off so only one thread owns mutable state at a time. Also: threads for pure-Python CPU work hit the GIL — use ProcessPoolExecutor.",
        shorthand: {
          verbose: "results = []\nfor url in urls:\n    response = requests.get(url)\n    results.append(response.json())",
          concise: "with ThreadPoolExecutor(max_workers=20) as executor:\n    results = list(executor.map(fetch, urls))",
        },
      },
      {
        id: "multiprocessing-basics",
        fn: "multiprocessing — True Parallelism for CPU Work",
        desc: "Bypass the GIL with separate processes — actual parallel execution on multiple CPU cores.",
        category: "Multiprocessing",
        subtitle: "ProcessPoolExecutor, Pool, shared memory, Manager",
        signature: "ProcessPoolExecutor(max_workers=4)  |  Pool(4).map(fn, items)",
        descLong: "multiprocessing creates separate Python processes, each with its own GIL — true parallelism on multi-core CPUs. ProcessPoolExecutor is the modern high-level API (same interface as ThreadPoolExecutor). Pool.map() distributes work across cores. Data is serialized (pickled) between processes — keep payloads small. shared_memory (3.8+) allows zero-copy sharing of large arrays. Use for: data processing, image manipulation, scientific computing, ML inference.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of multiprocessing — True Parallelism for CPU Work — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nimport os\nfrom concurrent.futures import ProcessPoolExecutor\ndef heavy(n: int) -> int:                       # MUST be top-level (picklable)\n    return sum(i * i for i in range(n))\nwith ProcessPoolExecutor(max_workers=os.cpu_count()) as ex:\n    print(list(ex.map(heavy, [10**6] * 8)))"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of multiprocessing — True Parallelism for CPU Work — common patterns you'll see in production.\n# APPROACH  - Combine multiprocessing — True Parallelism for CPU Work with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nimport os\nfrom concurrent.futures import ProcessPoolExecutor\nfrom multiprocessing import Pool\ndef heavy(n): return sum(i * i for i in range(n))\n# 1) chunksize — amortize IPC cost over many tasks (huge win for short jobs)\nwith ProcessPoolExecutor() as ex:\n    out = list(ex.map(heavy, [50_000] * 1000, chunksize=50))\n# 2) Pool initializer — load big read-only state ONCE per worker (model, lookup)\n_model = None\ndef _init(model_path):\n    global _model\n    _model = load_model(model_path)             # paid once per process\ndef _predict(x):                                 # uses module-global _model\n    return _model.predict(x)\nwith Pool(initializer=_init, initargs=(\"models/v1.pkl\",)) as pool:\n    preds = pool.map(_predict, batches)\n# 3) imap_unordered — process results AS THEY ARRIVE (no waiting for slow ones)\nwith Pool() as pool:\n    for result in pool.imap_unordered(heavy, [10**5] * 100, chunksize=10):\n        save(result)"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of multiprocessing — True Parallelism for CPU Work — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nimport os\nfrom concurrent.futures import ProcessPoolExecutor\nfrom multiprocessing import shared_memory\nimport numpy as np\n# 1) shared_memory — avoid pickling huge arrays per task (copy ONCE)\ndef make_shm(arr: np.ndarray):\n    shm = shared_memory.SharedMemory(create=True, size=arr.nbytes)\n    np.ndarray(arr.shape, dtype=arr.dtype, buffer=shm.buf)[:] = arr\n    return shm\ndef worker(shm_name, shape, dtype, sl):\n    shm = shared_memory.SharedMemory(name=shm_name)\n    a = np.ndarray(shape, dtype=dtype, buffer=shm.buf)\n    return a[sl].sum()                          # zero-copy READ\nbig = np.random.rand(10_000_000)\nshm = make_shm(big)\ntry:\n    chunks = [(shm.name, big.shape, big.dtype, slice(i, i + 1_000_000))\n              for i in range(0, big.size, 1_000_000)]\n    with ProcessPoolExecutor() as ex:\n        partials = list(ex.map(_call, chunks))\n    print(sum(partials))\nfinally:\n    shm.close()\n    shm.unlink()                                 # MUST unlink in the creator\ndef _call(args): return worker(*args)\n# 2) Picklability — the #1 source of \"Can't pickle\" errors\n#    Top-level functions / classes:  picklable\n#    lambdas, closures, nested defs:  NOT picklable -> use cloudpickle (joblib/dask)\n#    Open file handles, locks:        NOT picklable -> create them inside the worker\n# 3) When to AVOID processes\n#    - I/O-bound work                  -> use threads or async\n#    - tasks < ~1 ms                    -> sequential beats both; or batch them\n#    - NumPy vector ops on small arrays -> released GIL means threads suffice\n#    - cross-machine                     -> Dask / Ray, not local processes\n# Decision rule:\n#   pure-Python CPU, > ~100 ms per task     -> ProcessPoolExecutor\n#   numpy / sklearn (release GIL)            -> threads OFTEN beat processes\n#   I/O-bound                                 -> threads or async\n#   need shared state                          -> initializer + module global, OR shared_memory\n#   need cross-host parallelism                -> Dask / Ray\n#   thousands of tiny tasks                    -> chunksize=N to amortize IPC\n#\n# Anti-pattern: passing a 5 GB numpy array to ex.submit(fn, arr)\n#   It's pickled and copied to the worker on every call. Use shared_memory or\n#   memory-map the file inside the worker.\ndef load_model(_): return None\nbatches = []\ndef save(_): pass"
                  }
        ],
        tips: [
                  "ProcessPoolExecutor has the same API as ThreadPoolExecutor — swap them to switch between I/O and CPU concurrency.",
                  "Use Pool initializer for expensive per-worker setup (model loading, DB connections) — called once per process.",
                  "imap_unordered is faster than map when order doesn't matter — results come as they finish.",
                  "shared_memory avoids pickling large arrays — processes read the same memory directly (zero copy). Workers MUST be top-level functions: lambdas, closures, and locally-defined classes are NOT picklable (use cloudpickle via joblib/dask if you can't refactor). On Windows / macOS spawn, always guard the entry point with `if __name__ == \"__main__\":` — without it the children re-import the module and re-launch themselves recursively."
        ],
        mistake: "Passing a 5GB numpy array to ex.submit(fn, arr) — it's pickled and copied to the worker on every call. Use `shared_memory` or memory-map the file inside the worker. Workers must be top-level functions (lambdas/closures aren't picklable). On Windows/macOS the script must be guarded by `if __name__ == \"__main__\":`.",
        shorthand: {
          verbose: "results = []\nfor n in numbers:\n    result = cpu_intensive(n)\n    results.append(result)",
          concise: "with ProcessPoolExecutor(max_workers=cpu_count()) as executor:\n    results = list(executor.map(cpu_intensive, numbers))",
        },
      },
    ],
  },

  // ── Section 3: Subprocess & Process Management ─────────────────────────────────────────
  {
    id: "subprocess",
    title: "Subprocess & Process Management",
    entries: [
      {
        id: "subprocess-run",
        fn: "subprocess — Running External Commands",
        desc: "Run shell commands, capture output, handle errors, and pipe between processes safely.",
        category: "Subprocess",
        subtitle: "subprocess.run, check_output, Popen, pipes",
        signature: "subprocess.run([\"cmd\", \"arg\"], capture_output=True, text=True, check=True)",
        descLong: "subprocess.run() is the recommended way to run external commands. It captures stdout/stderr, checks return codes, and handles timeouts. Always pass commands as lists (not strings) to avoid shell injection. Use check=True to raise on non-zero exit codes. Popen provides low-level control for streaming output, piping between processes, and long-running subprocesses.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of subprocess — Running External Commands — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nimport subprocess\nresult = subprocess.run(\n    [\"git\", \"rev-parse\", \"HEAD\"],\n    capture_output=True,                          # collect stdout + stderr\n    text=True,                                    # decode bytes -> str\n    check=True,                                   # raise on non-zero exit\n)\nprint(result.stdout.strip())"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of subprocess — Running External Commands — common patterns you'll see in production.\n# APPROACH  - Combine subprocess — Running External Commands with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nimport subprocess\nfrom pathlib import Path\ndef git_log(repo: Path, n: int = 10) -> str:\n    try:\n        r = subprocess.run(\n            [\"git\", \"log\", \"--oneline\", f\"-n{n}\"],\n            cwd=repo,                              # change directory for THIS call\n            capture_output=True, text=True,\n            check=True,\n            timeout=30,                            # mandatory; otherwise hangs forever\n        )\n        return r.stdout\n    except subprocess.CalledProcessError as e:    # non-zero exit\n        raise RuntimeError(f\"git failed ({e.returncode}): {e.stderr.strip()}\")\n    except subprocess.TimeoutExpired:\n        raise RuntimeError(\"git log timed out after 30s\")\n# Pipe data IN through stdin\nout = subprocess.run(\n    [\"wc\", \"-l\"],\n    input=\"line1\\nline2\\nline3\\n\",\n    capture_output=True, text=True, check=True,\n).stdout.strip()\nprint(out)                                         # \"3\""
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of subprocess — Running External Commands — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nimport os\nimport signal\nimport subprocess\n# 1) STREAM stdout line-by-line — for long-running commands\ndef stream(cmd: list[str]):\n    proc = subprocess.Popen(\n        cmd,\n        stdout=subprocess.PIPE,\n        stderr=subprocess.STDOUT,                  # interleave stderr with stdout\n        text=True, bufsize=1,                      # line-buffered\n    )\n    try:\n        for line in proc.stdout:                   # yields as the process prints\n            yield line.rstrip()\n    finally:\n        proc.stdout.close()\n        rc = proc.wait()\n        if rc != 0:\n            raise RuntimeError(f\"{cmd[0]} exited {rc}\")\n# 2) PIPELINE without shell=True — Python wires stdin/stdout between processes\ndef pipeline_grep_count(path: str, pattern: str) -> int:\n    p1 = subprocess.Popen([\"cat\", path], stdout=subprocess.PIPE)\n    p2 = subprocess.Popen([\"grep\", pattern], stdin=p1.stdout, stdout=subprocess.PIPE)\n    p1.stdout.close()                              # let p1 get SIGPIPE on grep exit\n    p3 = subprocess.Popen([\"wc\", \"-l\"], stdin=p2.stdout, stdout=subprocess.PIPE, text=True)\n    p2.stdout.close()\n    out, _ = p3.communicate()\n    if any(p.wait() != 0 for p in (p1, p2, p3)):\n        raise RuntimeError(\"pipeline failed\")\n    return int(out.strip())\n# 3) ENV scoping — pass a CLEAN env, don't inherit secrets you don't need\nclean_env = {\"PATH\": os.environ[\"PATH\"], \"HOME\": os.environ[\"HOME\"], \"LANG\": \"C\"}\nsubprocess.run([\"my_tool\"], env=clean_env, check=True, timeout=60)\n# 4) Cancellation — kill ENTIRE process group, not just the leader\ndef run_killable(cmd, deadline=10.0):\n    proc = subprocess.Popen(cmd, start_new_session=True)   # gives it its own group\n    try:\n        return proc.wait(timeout=deadline)\n    except subprocess.TimeoutExpired:\n        os.killpg(proc.pid, signal.SIGTERM)        # children die too\n        proc.wait(timeout=5)\n        if proc.returncode is None:\n            os.killpg(proc.pid, signal.SIGKILL)\n        raise\n# Decision rule:\n#   one-shot command, small output       -> subprocess.run(..., capture_output=True)\n#   long-running, watch progress          -> Popen + iterate stdout\n#   chain of unix commands                 -> Popen pipeline, NEVER shell=True\n#   user-supplied input                    -> list form ALWAYS (no string interpolation)\n#   needs deadline                          -> timeout= (run) or wait(timeout=) (Popen)\n#   spawns child processes                  -> start_new_session=True + os.killpg on cleanup\n#   need exit code only                      -> subprocess.call (don't capture if you don't need it)\n#\n# Anti-pattern: shell=True with f-string user input\n#   subprocess.run(f\"ls {user_input}\", shell=True)\n#   user_input = \"; rm -rf /\" -> game over. Use list form; the OS does NOT split\n#   spaces or interpret ; & | in list args."
                  }
        ],
        tips: [
                  "Always use list form [\"cmd\", \"arg1\"] — never shell=True with user input (shell injection vulnerability).",
                  "capture_output=True + text=True + check=True + timeout= is the standard recipe; without timeout, a hung child hangs your process forever.",
                  "cwd parameter changes the working directory — essential for git commands and relative paths. Pass an explicit minimal `env=` dict so the child doesn't inherit secrets it doesn't need.",
                  "For streaming output, use Popen with line-buffered stdout — subprocess.run() waits until the command finishes. For pipelines, wire stdin/stdout between Popens (NEVER `shell=True`). For cancellation, launch with `start_new_session=True` and call `os.killpg(proc.pid, SIGTERM)` so children die too."
        ],
        mistake: "`shell=True` with f-string user input — `subprocess.run(f\"ls {user_input}\", shell=True)` is a remote-code-execution vector. Use list form: the OS does NOT split spaces or interpret `; & |` in list args. Also: forgetting `start_new_session=True` means timeouts kill only the leader and orphaned children keep running.",
        shorthand: {
          verbose: "import os\nos.system(\"ls -la /tmp\")\noutput = os.popen(\"git log\").read()",
          concise: "result = subprocess.run([\"git\", \"log\", \"-1\"], capture_output=True, text=True, check=True)",
        },
      },
    ],
  },

  // ── Section 4: concurrent.futures — Unified Executor API ─────────────────────────────────────────
  {
    id: "concurrent-futures",
    title: "concurrent.futures — Unified Executor API",
    entries: [
      {
        id: "threadpool-executor",
        fn: "ThreadPoolExecutor — High-Level Thread Pool",
        desc: "Run I/O-bound tasks in a thread pool with .submit() and .map() — modern alternative to raw threading.",
        category: "concurrent.futures",
        subtitle: "ThreadPoolExecutor, .submit(), .map(), futures, as_completed()",
        signature: "ThreadPoolExecutor(max_workers=10)  |  executor.submit(fn, *args)  |  executor.map(fn, items)",
        descLong: "ThreadPoolExecutor provides a high-level interface for running tasks in a pool of threads. .submit() submits a single task and returns a Future immediately (non-blocking). .map() is like Python's map but runs in parallel. as_completed() yields futures in completion order. Use for: network I/O, file operations, database queries, API calls. Automatically manages worker threads and cleanup with context manager.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of ThreadPoolExecutor — High-Level Thread Pool — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nfrom concurrent.futures import ThreadPoolExecutor\nimport requests\ndef fetch(url): return requests.get(url, timeout=10).status_code\nurls = [f\"https://httpbin.org/status/200?i={i}\" for i in range(20)]\nwith ThreadPoolExecutor(max_workers=10) as ex:    # context manager auto-shutdown\n    print(list(ex.map(fetch, urls)))"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of ThreadPoolExecutor — High-Level Thread Pool — common patterns you'll see in production.\n# APPROACH  - Combine ThreadPoolExecutor — High-Level Thread Pool with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nfrom concurrent.futures import ThreadPoolExecutor, as_completed\ndef fetch(url):\n    import requests\n    r = requests.get(url, timeout=10); r.raise_for_status()\n    return r.json()\nwith ThreadPoolExecutor(max_workers=20) as ex:\n    # submit returns Futures immediately; map is a convenience wrapper\n    futs = {ex.submit(fetch, u): u for u in urls}\n    for fut in as_completed(futs):                 # yields fastest first\n        url = futs[fut]\n        try:\n            data = fut.result(timeout=5)           # raises if > 5s OR worker raised\n            print(url, \"->\", len(data))\n        except Exception as e:\n            print(url, \"failed:\", e)"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of ThreadPoolExecutor — High-Level Thread Pool — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nimport asyncio\nfrom concurrent.futures import ThreadPoolExecutor, Future\n# 1) SIZING — there's no universal answer; profile and size by bottleneck\n#    HTTP per host       -> respect upstream concurrency limit (often 10-20)\n#    DB pool              -> match DB server max_connections\n#    file I/O on SSD      -> 8-32 is often a sweet spot\n#    pure-Python CPU      -> threads don't help; use ProcessPoolExecutor\n# 2) ASYNC bridging — call BLOCKING code from async without freezing the loop\nio_pool = ThreadPoolExecutor(max_workers=64, thread_name_prefix=\"io\")\nasync def fetch_in_pool(url):\n    loop = asyncio.get_running_loop()\n    return await loop.run_in_executor(io_pool, fetch, url)\nasync def call_blocking(x):                        # 3.9+: simplest form\n    return await asyncio.to_thread(blocking_step, x)\n# 3) CANCELLATION truth — Future.cancel() ONLY works BEFORE a worker picks it up.\n#    Once a thread is running the body, it CANNOT be killed from outside.\n#    Implement cooperative cancellation: an Event the worker checks.\nimport threading\nstop = threading.Event()\ndef cooperative():\n    while not stop.is_set():\n        do_chunk()\n# 4) SHUTDOWN semantics — wait= and cancel_futures= (3.9+) control behavior\nex = ThreadPoolExecutor(max_workers=4)\ntry:\n    f = ex.submit(slow); f.add_done_callback(handle_done)\nfinally:\n    ex.shutdown(wait=True, cancel_futures=True)    # cancel pending, wait for running\n# 5) Future.add_done_callback — non-blocking pipelines\ndef handle_done(f: Future):\n    try:    save(f.result())\n    except Exception as e: log(e)\n# Decision rule:\n#   blocking I/O                                  -> ThreadPoolExecutor.map / submit\n#   numpy / sklearn (release GIL)                 -> ThreadPoolExecutor is FINE\n#   pure-Python CPU                                -> ProcessPoolExecutor\n#   ASYNC code path needs blocking call           -> asyncio.to_thread(fn, *args)\n#   long-lived service                              -> create ONE executor at startup\n#   need progress per task                          -> as_completed\n#   need to enforce a deadline                      -> future.result(timeout=...)\n#\n# Anti-pattern: creating a new ThreadPoolExecutor inside a hot loop\n#   Each pool spins up workers and tears them down — kills throughput. Create\n#   ONE pool at startup and reuse it for the lifetime of the service.\ndef blocking_step(x): return x\ndef fetch(_): pass\ndef do_chunk(): pass\ndef slow(): pass\ndef save(_): pass\ndef log(_): pass\nurls = []"
                  }
        ],
        tips: [
                  ".submit() returns immediately — use .result() or as_completed() to wait for results.",
                  "as_completed() processes results in finish order — faster responses first, perfect for timeouts.",
                  ".cancel() only works on PENDING futures — running tasks can't be killed from outside. For interruptible workers, design cooperative cancellation: an `Event` the worker checks between chunks.",
                  "max_workers should match your bottleneck: ~20 for network I/O, ~2× CPU count for moderate I/O. Create ONE pool at startup and reuse it for the lifetime of the service. From async code, bridge with `await asyncio.to_thread(fn)` (3.9+) or `loop.run_in_executor(pool, fn, *args)`. Shutdown with `ex.shutdown(wait=True, cancel_futures=True)` (3.9+) to drop never-started tasks."
        ],
        mistake: "Creating a new ThreadPoolExecutor inside a hot loop — each pool spins workers up and tears them down, killing throughput. Create ONE pool at startup. Also: `.cancel()` on a running task does nothing — wire cooperative cancellation through an Event the worker checks.",
        shorthand: {
          verbose: "results = []\nfor url in urls:\n    result = fetch(url)\n    results.append(result)",
          concise: "with ThreadPoolExecutor(max_workers=10) as executor:\n    results = list(executor.map(fetch, urls))",
        },
      },
      {
        id: "processpool-executor",
        fn: "ProcessPoolExecutor — High-Level Process Pool for CPU Work",
        desc: "True parallelism for CPU-intensive tasks — same executor API as threads but bypasses the GIL.",
        category: "concurrent.futures",
        subtitle: "ProcessPoolExecutor, CPU-bound work, max_workers, chunksize",
        signature: "ProcessPoolExecutor(max_workers=4)  |  executor.map(fn, items, chunksize=10)",
        descLong: "ProcessPoolExecutor is identical to ThreadPoolExecutor but uses separate processes instead of threads. Each process has its own Python interpreter and GIL — true parallel execution on multi-core CPUs. Data is serialized between processes (pickled), so keep payloads small. chunksize batches items for better performance on large datasets. Use for: data processing, ML inference, image manipulation, scientific computing. max_workers defaults to min(32, cpu_count() + 4).",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of ProcessPoolExecutor — High-Level Process Pool for CPU Work — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nimport os\nfrom concurrent.futures import ProcessPoolExecutor\ndef heavy(n: int) -> int:                       # MUST be top-level (picklable)\n    return sum(i * i for i in range(n))\nwith ProcessPoolExecutor(max_workers=os.cpu_count()) as ex:\n    print(list(ex.map(heavy, [10**6] * 8)))"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of ProcessPoolExecutor — High-Level Process Pool for CPU Work — common patterns you'll see in production.\n# APPROACH  - Combine ProcessPoolExecutor — High-Level Process Pool for CPU Work with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nimport os\nfrom concurrent.futures import ProcessPoolExecutor\ndef heavy(n): return sum(i * i for i in range(n))\n# 1) chunksize — amortize IPC cost over many short tasks (huge win)\nwith ProcessPoolExecutor() as ex:\n    out = list(ex.map(heavy, [50_000] * 1000, chunksize=50))\n# 2) initializer — load big read-only state ONCE per worker\n_model = None\ndef _init(model_path: str):\n    global _model\n    _model = load_model(model_path)\ndef _predict(x):                                 # uses module-global _model\n    return _model.predict(x)\nwith ProcessPoolExecutor(\n    max_workers=4, initializer=_init, initargs=(\"models/v1.pkl\",),\n) as ex:\n    preds = list(ex.map(_predict, batches, chunksize=8))"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of ProcessPoolExecutor — High-Level Process Pool for CPU Work — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nimport os\nfrom concurrent.futures import ProcessPoolExecutor, as_completed\n# 1) Picklability — the #1 source of \"Can't pickle\" errors\n#    OK: top-level functions, dataclasses, primitives, numpy arrays\n#    NOT OK: lambdas, closures, locally-defined classes, open file handles\n#    Workaround for closures: cloudpickle (via joblib / dask)\n# 2) Error contract — workers crash; don't let the main process hang\ndef heavy(n):\n    if n == 0: raise ValueError(\"bad input\")\n    return sum(i * i for i in range(n))\nwith ProcessPoolExecutor(max_workers=4) as ex:\n    futs = {ex.submit(heavy, n): n for n in (10**5, 0, 10**6)}\n    for f in as_completed(futs):\n        n = futs[f]\n        try:\n            print(n, \"->\", f.result(timeout=30))\n        except Exception as e:\n            print(n, \"failed:\", type(e).__name__, e)\n# 3) When to AVOID processes\n#    - I/O-bound work                            -> threads or async\n#    - tasks < ~1 ms                              -> sequential beats both; or batch them\n#    - numpy / sklearn (release GIL)              -> threads OFTEN match processes\n#    - cross-machine                               -> Dask / Ray, not local processes\n#    - Windows + Jupyter                          -> spawn-only; \"if __name__ == '__main__':\" required\n# 4) Process startup cost is REAL\n#    First task in each worker: ~50-200 ms overhead.\n#    Don't use processes for thousands of <1 ms tasks; batch into chunks.\n# 5) max_workers ≈ os.cpu_count() for CPU-bound; oversubscription rarely helps.\n# Decision rule:\n#   pure-Python CPU work, > ~100 ms per task   -> ProcessPoolExecutor + chunksize\n#   I/O-bound                                    -> ThreadPoolExecutor or asyncio\n#   numpy / scientific libs                      -> ThreadPoolExecutor (releases GIL)\n#   ML inference per request                      -> initializer to load model once per worker\n#   need shared state                              -> initializer + module global; or shared_memory\n#   distributed compute                            -> Dask / Ray\n#\n# Anti-pattern: ProcessPoolExecutor inside a request handler\n#   Process startup blows the request budget. Pre-create one pool at startup\n#   and submit work into it; don't construct a fresh pool per request.\ndef load_model(_): return None\nbatches = []"
                  }
        ],
        tips: [
                  "ProcessPoolExecutor has identical API to ThreadPoolExecutor — swap them to test I/O vs CPU bottleneck.",
                  "chunksize matters on large datasets — larger chunks reduce pickling overhead but use more memory. Process startup is ~50-200ms per worker; don't use processes for thousands of <1ms tasks (batch them).",
                  "max_workers should equal cpu_count() for CPU-bound work — more workers = more context switching overhead. For numpy/sklearn (which release the GIL), threads OFTEN match processes with no IPC cost.",
                  "Keep task payloads small — serialization to/from processes is expensive. Pass indices, not full data. Pre-create ONE pool at startup; ProcessPoolExecutor inside a request handler eats the per-request budget on worker startup. Workers must be top-level (no lambdas/closures); Windows + Jupyter use spawn-only and require `if __name__ == \"__main__\":`."
        ],
        mistake: "ProcessPoolExecutor inside a request handler — process startup blows the request budget. Pre-create one pool at startup and submit work into it; never construct a fresh pool per request. Also: I/O-bound work doesn't benefit from processes — threads or asyncio are lighter.",
        shorthand: {
          verbose: "results = []\nfor n in numbers:\n    result = cpu_task(n)\n    results.append(result)",
          concise: "with ProcessPoolExecutor() as executor:\n    results = list(executor.map(cpu_task, numbers))",
        },
      },
      {
        id: "futures-patterns",
        fn: "Futures Patterns — Wait, Timeout & Cancellation",
        desc: "Advanced patterns: wait() for custom logic, timeout handling, cancellation, and exception propagation.",
        category: "concurrent.futures",
        subtitle: "wait(), as_completed(), timeout, cancellation, exception handling",
        signature: "concurrent.futures.wait(futures, timeout=None, return_when=FIRST_COMPLETED)",
        descLong: "concurrent.futures.wait() waits for futures with fine-grained control — return_when={FIRST_COMPLETED, FIRST_EXCEPTION, ALL_COMPLETED}. Perfect for: fail-fast on first error, process results as they arrive, implement custom timeout logic. Always check future.exception() to catch worker errors. Combine .cancel() for graceful shutdown with timeout.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Futures Patterns — Wait, Timeout & Cancellation — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nfrom concurrent.futures import ThreadPoolExecutor\ndef slow():\n    import time; time.sleep(5); return \"ok\"\nwith ThreadPoolExecutor() as ex:\n    f = ex.submit(slow)\n    try:\n        print(f.result(timeout=2))               # raises TimeoutError after 2s\n    except TimeoutError:\n        print(\"too slow\")"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Futures Patterns — Wait, Timeout & Cancellation — common patterns you'll see in production.\n# APPROACH  - Combine Futures Patterns — Wait, Timeout & Cancellation with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nfrom concurrent.futures import (\n    ThreadPoolExecutor, wait, FIRST_COMPLETED, FIRST_EXCEPTION,\n)\ndef task(n, delay):\n    import time; time.sleep(delay)\n    if n == 3: raise ValueError(\"intentional\")\n    return n\nwith ThreadPoolExecutor(max_workers=4) as ex:\n    futures = [ex.submit(task, i, 0.5 * i) for i in range(5)]\n    # 1) FIRST_COMPLETED — react to whichever finishes first\n    done, pending = wait(futures, return_when=FIRST_COMPLETED, timeout=1)\n    print(\"first done:\", len(done), \"still pending:\", len(pending))\n    # 2) FIRST_EXCEPTION — stop on the first failure, cancel the rest\n    done, pending = wait(futures, return_when=FIRST_EXCEPTION)\n    for f in pending:\n        f.cancel()                                # only effective on PENDING futures\n    for f in done:\n        if (e := f.exception()) is not None:\n            print(\"error:\", e)\n        else:\n            print(\"ok:\", f.result())"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Futures Patterns — Wait, Timeout & Cancellation — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nimport time\nfrom concurrent.futures import ThreadPoolExecutor, Future, wait\n# 1) add_done_callback — fire downstream work as soon as a future completes\nex = ThreadPoolExecutor(max_workers=8)\nresults, errors = [], []\ndef on_done(f: Future):\n    try:    results.append(f.result())\n    except Exception as e: errors.append(e)\nfor i in range(20):\n    f = ex.submit(task, i, 0.1)\n    f.add_done_callback(on_done)                  # NO blocking; pipelined\n# 2) Graceful shutdown — let in-flight tasks finish, cancel the queue\nex.shutdown(wait=True, cancel_futures=True)       # 3.9+: drops never-started tasks\n# 3) Retries — futures don't retry; you do\ndef submit_with_retry(ex, fn, *args, attempts=3, base_delay=0.5):\n    last = None\n    for i in range(attempts):\n        f = ex.submit(fn, *args)\n        try:\n            return f.result()\n        except Exception as e:\n            last = e\n            time.sleep(base_delay * 2**i)         # exponential backoff\n    raise last\n# 4) Bounded global timeout for a BATCH — finish what you can, drop the rest\ndef run_with_budget(fn, items, budget_s, workers=8):\n    out = []\n    with ThreadPoolExecutor(max_workers=workers) as ex:\n        futs = {ex.submit(fn, x): x for x in items}\n        done, pending = wait(futs, timeout=budget_s)\n        for f in pending: f.cancel()\n        for f in done:\n            try:    out.append(f.result())\n            except Exception as e: print(\"failed:\", e)\n    return out\n# 5) Cancellation truth — Future.cancel() ONLY works BEFORE pickup.\n#    For RUNNING tasks, the body has to check a flag. Wrap your worker:\nimport threading\nstop = threading.Event()\ndef cancellable_work(items):\n    for it in items:\n        if stop.is_set(): return\n        process(it)\n# Decision rule:\n#   one task with a deadline                 -> future.result(timeout=N)\n#   N tasks, react to FIRST done              -> wait(..., return_when=FIRST_COMPLETED)\n#   N tasks, fail fast on any error            -> wait(..., return_when=FIRST_EXCEPTION) + cancel pending\n#   stream results to downstream consumer      -> add_done_callback (no thread blocked waiting)\n#   service shutdown                            -> ex.shutdown(wait=True, cancel_futures=True)\n#   transient failures expected                 -> retry with exponential backoff in caller\n#   need to interrupt a RUNNING task            -> cooperative Event flag, not future.cancel()\n#\n# Anti-pattern: NOT calling future.exception() / .result() on background tasks\n#   Exceptions vanish silently. The task \"succeeded\" from the pool's view.\n#   Always: call .result(), use as_completed, or attach a done callback that\n#   inspects exception().\ndef task(n, delay): import time; time.sleep(delay); return n\ndef process(_): pass"
                  }
        ],
        tips: [
                  "wait() with FIRST_EXCEPTION enables fail-fast — stop processing as soon as an error occurs.",
                  ".cancel() only works on pending futures — running ones can't be interrupted. For interrupting RUNNING tasks, the body must check a cooperative `threading.Event` between chunks.",
                  "Always use timeout on .result() for defensive programming — prevents hanging forever.",
                  "FIRST_COMPLETED is useful for real-time response — process results as they arrive, not in order. For non-blocking pipelines, attach `future.add_done_callback(fn)` so the next stage fires the moment a task completes (no thread waits idle). Futures don't retry; wrap submission in your own exponential-backoff loop."
        ],
        mistake: "NOT calling future.exception() / .result() on background tasks — exceptions vanish silently and the task \"succeeded\" from the pool's view. Always call .result(), use as_completed, or attach a done callback that inspects exception(). For graceful shutdown use `ex.shutdown(wait=True, cancel_futures=True)` (3.9+).",
        shorthand: {
          verbose: "done, pending = wait(futures, timeout=5)\nfor f in done:\n    try:\n        print(f.result())\n    except Exception as e:\n        print(f\"Error: {e}\")",
          concise: "for f in as_completed(futures, timeout=5):\n    print(f.result())",
        },
      },
    ],
  },

  // ── Section 5: asyncio Deep Dive — Advanced Patterns ─────────────────────────────────────────
  {
    id: "asyncio-deep",
    title: "asyncio Deep Dive — Advanced Patterns",
    entries: [
      {
        id: "asyncio-event-loop",
        fn: "Event Loop — Core of asyncio",
        desc: "Control the event loop directly: run_until_complete(), create_task(), and advanced scheduling.",
        category: "asyncio",
        subtitle: "asyncio.run(), asyncio.get_running_loop(), create_task(), to_thread() — get_event_loop() is legacy",
        signature: "asyncio.run(coro)  |  asyncio.get_running_loop()  |  loop.create_task(coro)  (asyncio.get_event_loop() is legacy outside a running loop)",
        descLong: "The event loop is asyncio's heart — it schedules and executes coroutines. asyncio.run() is the modern entry point (3.7+). get_event_loop() accesses the loop directly for advanced control. run_until_complete() runs a coroutine to completion. create_task() schedules a coroutine as a background task. Low-level: loop.call_soon(), loop.call_later() schedule callbacks. Use raw event loop for: testing, REPL scripting, custom scheduling.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Event Loop — Core of asyncio — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nimport asyncio\nasync def main():\n    await asyncio.sleep(0.1)\n    return \"done\"\nprint(asyncio.run(main()))                       # use this 99% of the time"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Event Loop — Core of asyncio — common patterns you'll see in production.\n# APPROACH  - Combine Event Loop — Core of asyncio with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nimport asyncio\nasync def background():\n    for i in range(3):\n        print(\"bg\", i); await asyncio.sleep(0.2)\nasync def main():\n    # Schedules the coro on the loop AND starts it immediately\n    bg = asyncio.create_task(background())\n    # Foreground keeps running concurrently\n    for i in range(2):\n        print(\"fg\", i); await asyncio.sleep(0.1)\n    await bg                                     # MUST await; orphan tasks log warnings\nasyncio.run(main())\n# Bridge to a blocking function inside async (3.9+):\ndef blocking(): import time; time.sleep(1); return 42\nasync def use_blocking():\n    return await asyncio.to_thread(blocking)    # offloads to default thread pool"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Event Loop — Core of asyncio — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nimport asyncio\nimport concurrent.futures\n# 1) ONE loop per thread. asyncio.run() creates and DESTROYS one each call.\n#    Inside FastAPI / Jupyter / pytest-asyncio, a loop is ALREADY running:\n#       - script:    asyncio.run(main())\n#       - notebook:  await main()\n#       - fastapi:   await ... inside an endpoint\n# 2) Get the RUNNING loop (3.10+ deprecates get_event_loop in non-running ctx)\nasync def get_loop_safely():\n    return asyncio.get_running_loop()           # ALWAYS prefer in async code\n# 3) uvloop — drop-in faster loop on Linux/macOS (~2-4x perf for network-heavy work)\ntry:\n    import uvloop\n    uvloop.install()                             # call BEFORE asyncio.run()\nexcept ImportError:\n    pass\n# 4) Custom executor for offloading — rarely needed, but useful for sizing\nio_pool = concurrent.futures.ThreadPoolExecutor(max_workers=64, thread_name_prefix=\"io\")\nasync def call_blocking(fn, *args):\n    loop = asyncio.get_running_loop()\n    return await loop.run_in_executor(io_pool, fn, *args)\n# 5) Loop debug mode — catches slow callbacks, hot tasks, leaked coroutines\n#    Either: asyncio.run(main(), debug=True)\n#    Or set env: PYTHONASYNCIODEBUG=1\n# 6) call_soon / call_later are LOW-LEVEL — for integrating non-async libraries\nasync def schedule_demo():\n    loop = asyncio.get_running_loop()\n    loop.call_later(0.5, print, \"delayed callback\")    # runs after 0.5s on the loop\n    loop.call_soon(print, \"next tick\")                 # runs ASAP, on next iteration\n    await asyncio.sleep(1)\n# Decision rule:\n#   script entry point                          -> asyncio.run(main())\n#   already inside a loop (FastAPI / Jupyter)    -> await directly; NEVER asyncio.run\n#   need the loop object inside async            -> asyncio.get_running_loop()\n#   need to await a blocking function             -> asyncio.to_thread(fn) (3.9+)\n#   network-heavy server, want speed              -> uvloop.install() at startup\n#   integrating a callback-style C library        -> loop.call_soon / loop.call_later\n#   debugging \"task was never awaited\"            -> asyncio.run(main(), debug=True)\n#\n# Anti-pattern: storing the loop in a global at import time\n#   The loop doesn't exist until asyncio.run() starts. Globals captured early\n#   point at a CLOSED loop after the first run. Always fetch via\n#   get_running_loop() inside an async function."
                  }
        ],
        tips: [
                  "asyncio.run() is the standard entry point — it creates/closes the loop automatically. NEVER call it inside FastAPI / Jupyter / pytest-asyncio (a loop is already running there); just `await` directly.",
                  "create_task() schedules a coroutine immediately — it runs concurrently, not sequentially.",
                  "gather() is for collecting multiple coroutine results — cleaner than managing tasks manually.",
                  "call_soon/call_later schedule callbacks — useful for integrating non-async code. For network-heavy servers on Linux/macOS, install uvloop (~2-4× perf): `import uvloop; uvloop.install()` BEFORE `asyncio.run()`. Set `debug=True` (or `PYTHONASYNCIODEBUG=1`) to catch slow callbacks, hot tasks, and \"task was never awaited\" warnings."
        ],
        mistake: "Storing the loop in a global at import time — the loop doesn't exist until asyncio.run() starts, and globals captured early point at a CLOSED loop after the first run. Always fetch via `asyncio.get_running_loop()` inside an async function.",
        shorthand: {
          verbose: "loop = asyncio.get_event_loop()\ntry:\n    result = loop.run_until_complete(main())\nfinally:\n    loop.close()",
          concise: "result = asyncio.run(main())",
        },
      },
      {
        id: "asyncio-streams",
        fn: "asyncio Streams — Async TCP/IP",
        desc: "Async network I/O with StreamReader and StreamWriter — TCP clients and servers.",
        category: "asyncio",
        subtitle: "asyncio.open_connection(), StreamReader/StreamWriter, async TCP",
        signature: "reader, writer = await asyncio.open_connection(host, port)  |  await reader.readline()",
        descLong: "asyncio streams provide async wrappers around sockets. open_connection() connects to a server, returning (reader, writer) tuple. StreamReader has .read(), .readline(), .readuntil(). StreamWriter has .write(), .writelines(), .close(). start_server() creates an async server that accepts connections. Use for: async HTTP clients (when aiohttp isn't available), custom protocols, real-time bidirectional communication.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of asyncio Streams — Async TCP/IP — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nimport asyncio\nasync def echo_once(host: str, port: int, msg: str) -> str:\n    reader, writer = await asyncio.open_connection(host, port)\n    writer.write(msg.encode() + b\"\\n\")\n    await writer.drain()                        # actually flush the buffer\n    line = await reader.readline()\n    writer.close(); await writer.wait_closed()\n    return line.decode().rstrip()"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of asyncio Streams — Async TCP/IP — common patterns you'll see in production.\n# APPROACH  - Combine asyncio Streams — Async TCP/IP with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nimport asyncio\nasync def handle(reader, writer):\n    try:\n        while True:\n            line = await reader.readline()      # b\"\" on EOF\n            if not line:\n                return\n            writer.write(line)                  # echo back\n            await writer.drain()\n    finally:\n        writer.close()\n        await writer.wait_closed()\nasync def main():\n    server = await asyncio.start_server(handle, \"127.0.0.1\", 8888)\n    async with server:\n        await server.serve_forever()\n# asyncio.run(main())                            # Ctrl-C to stop"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of asyncio Streams — Async TCP/IP — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nimport asyncio\nimport struct\n# 1) FRAMING — TCP is a byte stream, not a message stream. Length-prefix every msg.\nasync def read_msg(reader: asyncio.StreamReader) -> bytes:\n    hdr = await reader.readexactly(4)            # raises IncompleteReadError on EOF\n    (n,) = struct.unpack(\"!I\", hdr)              # network-order uint32\n    return await reader.readexactly(n)\nasync def write_msg(writer: asyncio.StreamWriter, data: bytes):\n    writer.write(struct.pack(\"!I\", len(data)) + data)\n    await writer.drain()\n# 2) Per-connection deadline + clean teardown\nasync def handle(reader, writer):\n    addr = writer.get_extra_info(\"peername\")\n    try:\n        async with asyncio.timeout(30):          # whole-conversation budget\n            while True:\n                try:\n                    msg = await read_msg(reader)\n                except asyncio.IncompleteReadError:\n                    return                       # peer closed\n                await write_msg(writer, msg.upper())\n    except (TimeoutError, ConnectionError) as e:\n        print(\"conn closed:\", addr, e)\n    finally:\n        writer.close()\n        try: await writer.wait_closed()\n        except Exception: pass\n# 3) Graceful shutdown — drain pending connections, then close\nclass Server:\n    def __init__(self):\n        self._server = None\n    async def start(self, host=\"127.0.0.1\", port=8888):\n        self._server = await asyncio.start_server(handle, host, port)\n        return self\n    async def serve(self):\n        async with self._server:\n            await self._server.serve_forever()\n    async def shutdown(self):\n        self._server.close()                      # stop accepting NEW connections\n        await self._server.wait_closed()          # waits for in-flight to finish\n# 4) Cancellation discipline — propagate, don't swallow\nasync def safe_handle(reader, writer):\n    try:\n        await handle(reader, writer)\n    except asyncio.CancelledError:\n        # finally above already closed writer; just re-raise\n        raise\n# Decision rule:\n#   simple text protocol                   -> readline() with newline framing\n#   binary or variable-length messages      -> length-prefix (struct + readexactly)\n#   per-connection deadline                  -> async with asyncio.timeout(s) (3.11+)\n#   service shutdown                          -> server.close() + wait_closed()\n#   keep-alive / many connections             -> set tcp_keepalive on the underlying socket\n#   need TLS                                   -> pass ssl=ssl_context to start_server\n#\n# Anti-pattern: write() without await drain()\n#   Data sits in the buffer; under back-pressure it never gets flushed and the\n#   writer reports \"full\". Always: writer.write(...); await writer.drain()."
                  }
        ],
        tips: [
                  "Always await writer.drain() after write() — flushes buffer and allows other tasks to run.",
                  "Always close and wait_closed() in a finally block — prevents hanging connections. Apply per-connection budgets with `async with asyncio.timeout(s):` (3.11+) and shut down gracefully via `server.close()` + `await server.wait_closed()` so in-flight clients finish.",
                  "readline() reads until \\n, readexactly() reads exact bytes, readuntil() reads until custom delimiter. TCP is a BYTE STREAM, not a message stream — for binary or variable-length messages, length-prefix every message (`struct.pack(\"!I\", len(data)) + data`) and read with `readexactly(4)` then `readexactly(n)`.",
                  "start_server() handles multiple clients concurrently — each gets its own handle_client coroutine. Pass `ssl=ssl_context` to start_server for TLS termination."
        ],
        mistake: "write() without await drain() — data sits in the buffer; under back-pressure it never gets flushed and the writer reports \"full\". Also: framing TCP as if it were a message protocol — length-prefix or use a delimiter, otherwise readers see partial / merged messages on the wire.",
        shorthand: {
          verbose: "reader, writer = await asyncio.open_connection(host, port)\nwriter.write(msg.encode())\nawait writer.drain()\nresponse = await reader.readline()\nwriter.close()\nawait writer.wait_closed()",
          concise: "reader, writer = await asyncio.open_connection(host, port)\nwriter.write(msg.encode())\nawait writer.drain()\nresponse = await reader.readline()",
        },
      },
      {
        id: "asyncio-locks",
        fn: "asyncio Locks & Synchronization",
        desc: "Thread-safe primitives for async: Lock, Semaphore, Event, Condition.",
        category: "asyncio",
        subtitle: "asyncio.Lock(), asyncio.Semaphore(), asyncio.Event(), asyncio.Condition()",
        signature: "async with asyncio.Lock(): ...  |  async with semaphore:",
        descLong: "asyncio provides async-safe synchronization primitives. Lock ensures mutual exclusion (only one coroutine at a time). Semaphore limits concurrency (N coroutines at a time). Event signals all waiting coroutines. Condition combines mutex + signaling. Unlike threading locks, these never block the event loop — they yield control when waiting.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of asyncio Locks & Synchronization — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nimport asyncio\ncounter = 0\nlock = asyncio.Lock()\nasync def bump():\n    global counter\n    for _ in range(1000):\n        async with lock:                        # one coroutine in here at a time\n            counter += 1\nasync def main():\n    await asyncio.gather(bump(), bump())\n    print(counter)                              # exactly 2000 (no races)\nasyncio.run(main())"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of asyncio Locks & Synchronization — common patterns you'll see in production.\n# APPROACH  - Combine asyncio Locks & Synchronization with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nimport asyncio\n# 1) Semaphore — bound concurrency to N\nsem = asyncio.Semaphore(3)\nasync def limited(i):\n    async with sem:                             # at most 3 inflight\n        await asyncio.sleep(0.5)\n        return i\nasync def fan_out():\n    return await asyncio.gather(*(limited(i) for i in range(10)))\n# 2) Event — many-to-many signal: \"ready, all of you can go\"\nready = asyncio.Event()\nasync def waiter(name):\n    await ready.wait()                          # blocks until ready.set()\n    print(name, \"go\")\nasync def starter():\n    await asyncio.sleep(0.2)\n    ready.set()                                 # wakes ALL waiters at once\nasync def main():\n    async with asyncio.TaskGroup() as tg:\n        for n in (\"a\", \"b\", \"c\"):\n            tg.create_task(waiter(n))\n        tg.create_task(starter())\nasyncio.run(main())"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of asyncio Locks & Synchronization — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nimport asyncio\nfrom collections import deque\n# 1) You often DON'T NEED a lock in asyncio. Coroutines run cooperatively;\n#    code BETWEEN awaits is atomic from the loop's perspective.\ncounter = 0\nasync def bump_no_lock():\n    global counter\n    counter += 1                                 # SAFE: no await between read and write\nasync def bump_needs_lock():\n    global counter\n    val = counter                                # await -> any other coro can run\n    await asyncio.sleep(0)\n    counter = val + 1                            # NOW we need a lock; race exists\n# 2) Condition — lock + \"wait until predicate is true\"\nclass WorkQueue:\n    def __init__(self):\n        self._items = deque()\n        self._cond  = asyncio.Condition()\n    async def put(self, x):\n        async with self._cond:\n            self._items.append(x)\n            self._cond.notify(1)                 # wake ONE waiter\n    async def get(self):\n        async with self._cond:\n            await self._cond.wait_for(lambda: bool(self._items))   # ALWAYS use wait_for\n            return self._items.popleft()\n# 3) BoundedSemaphore — defends against release() bugs (raises if over-released)\nsem = asyncio.BoundedSemaphore(10)\n# 4) ANTI-PATTERN: using threading.Lock in async code\n#    threading.Lock blocks the entire event loop while waiting.\n#    Always use asyncio.Lock for async code; threading.Lock only across threads.\n# 5) Per-key locks — common for caches / API rate limits\nclass KeyedLocks:\n    def __init__(self):\n        self._locks: dict[str, asyncio.Lock] = {}\n    def for_key(self, k: str) -> asyncio.Lock:\n        return self._locks.setdefault(k, asyncio.Lock())\nKEY_LOCKS = KeyedLocks()\nasync def get_or_compute(key, expensive):\n    async with KEY_LOCKS.for_key(key):\n        return await expensive(key)\n# Decision rule:\n#   no awaits in critical section          -> NO lock needed (coroutines are cooperative)\n#   read-modify-write across awaits         -> asyncio.Lock\n#   bound concurrency to N                  -> Semaphore(N) (or BoundedSemaphore for safety)\n#   broadcast \"ready\"                        -> Event (set/clear)\n#   wait until predicate                     -> Condition + wait_for(predicate)\n#   per-resource exclusion (cache key, etc.) -> dict of asyncio.Locks\n#   sync code that calls async               -> NEVER use asyncio.Lock there; use threading.Lock\n#\n# Anti-pattern: while not items: await condition.wait()\n#   Without wait_for(), spurious wakeups (rare but real) can let the loop slip\n#   past the predicate. Use cond.wait_for(predicate) — it loops for you."
                  }
        ],
        tips: [
                  "You often DON'T need a Lock in asyncio: code BETWEEN awaits is atomic from the loop's perspective. Reach for asyncio.Lock only when you have a read-modify-write that spans an `await`. Prefer `BoundedSemaphore` over `Semaphore` — it raises on over-release instead of silently breaking your bound.",
                  "Semaphore(N) allows N concurrent operations — perfect for rate-limiting API calls. For per-resource exclusion (cache key, user id), keep a `dict[key, asyncio.Lock]` so unrelated keys don't block each other.",
                  "asyncio.Event uses set() / clear() / wait() — set() releases ALL waiters; there is no notify(). asyncio.Condition is the one with notify() / notify_all() — and ALWAYS use `cond.wait_for(predicate)` (not bare `wait()`) so spurious wakeups don't slip past the condition.",
                  "Condition is overkill if you only need mutual exclusion — use Lock for that."
        ],
        mistake: "Using `threading.Lock` in async code — it blocks the entire event loop while waiting (the loop can't schedule any other coroutine). Always use `asyncio.Lock` for async code; reserve `threading.Lock` for cross-thread coordination only. Also: bare `await cond.wait()` without `wait_for(predicate)` lets spurious wakeups pass the predicate.",
        shorthand: {
          verbose: "lock = asyncio.Lock()\nasync with lock:\n    counter += 1",
          concise: "async with asyncio.Lock():\n    counter += 1",
        },
      },
      {
        id: "asyncio-timeout",
        fn: "asyncio Timeouts & Cancellation",
        desc: "Handle timeouts, cancellation, and shielding with wait_for(), timeout(), and shield().",
        category: "asyncio",
        subtitle: "asyncio.wait_for(), asyncio.timeout(), asyncio.shield()",
        signature: "await asyncio.wait_for(coro, timeout=5.0)  |  async with asyncio.timeout(5):",
        descLong: "wait_for() runs a coroutine with a timeout — raises TimeoutError if exceeded. timeout() (3.11+) is a context manager version. shield() protects a task from cancellation. Use for: implement deadlines, cancel slow operations, isolate critical tasks from parent cancellation.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of asyncio Timeouts & Cancellation — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nimport asyncio\nasync def slow():\n    await asyncio.sleep(10)\n    return \"done\"\nasync def main():\n    try:\n        return await asyncio.wait_for(slow(), timeout=2.0)\n    except TimeoutError:\n        return \"too slow\"\nprint(asyncio.run(main()))"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of asyncio Timeouts & Cancellation — common patterns you'll see in production.\n# APPROACH  - Combine asyncio Timeouts & Cancellation with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nimport asyncio\nasync def fetch(url):\n    await asyncio.sleep(5)\n    return url\nasync def with_deadline():\n    try:\n        # Applies to the WHOLE block — multiple awaits share one budget\n        async with asyncio.timeout(2.0):              # 3.11+\n            return await fetch(\"api.example.com\")\n    except TimeoutError:\n        return None\n# Cancellation cleanup — clean up in finally OR catch + RE-RAISE\nasync def worker(q):\n    try:\n        while True:\n            item = await q.get()\n            await process(item)\n    except asyncio.CancelledError:\n        await close_resource()                        # close handles / save partial progress\n        raise                                          # MUST re-raise to confirm cancellation\nasync def process(_): pass\nasync def close_resource(): pass\nprint(asyncio.run(with_deadline()))"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of asyncio Timeouts & Cancellation — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nimport asyncio\n# 1) shield — protect a CRITICAL operation from outer cancellation\nasync def must_finish():\n    await asyncio.sleep(3)\n    return \"saved\"\nasync def outer():\n    work = asyncio.shield(must_finish())              # outer cancel won't kill 'work'\n    try:\n        return await asyncio.wait_for(work, timeout=1.0)\n    except TimeoutError:\n        return await work                              # still running; just keep waiting\n# 2) Nested timeouts — INNER deadline can be tighter than OUTER\nasync def nested():\n    async with asyncio.timeout(5.0):                   # outer budget: 5s\n        async with asyncio.timeout(1.0):               # inner: 1s only\n            await flaky_call()                         # raises TimeoutError after 1s\n        await cleanup()                                 # uses what's left of the outer 5s\n# 3) TaskGroup + timeout — apply ONE deadline to a fan-out\nasync def fan_out_deadline(urls):\n    async with asyncio.timeout(10.0):                  # whole batch budget\n        async with asyncio.TaskGroup() as tg:\n            tasks = [tg.create_task(fetch(u)) for u in urls]\n    return [t.result() for t in tasks]\n# 4) Cancellation CONTRACT — never silently swallow CancelledError\nasync def good_worker():\n    try:\n        while True:\n            await one_step()\n    except asyncio.CancelledError:\n        await flush_partial()                          # cleanup\n        raise                                          # CONFIRM cancellation\n    except Exception as e:\n        log(e)                                          # other errors are NOT cancellation\n# 5) wait_for vs timeout\n#    wait_for(coro, timeout)  -> wraps a single coroutine; raises TimeoutError\n#    asyncio.timeout(s)        -> wraps a BLOCK with multiple awaits; cleaner for chains\n#    Both ultimately CANCEL the running task to enforce the deadline.\n# Decision rule:\n#   single coroutine deadline                  -> asyncio.wait_for(coro, t)\n#   multi-step block, one budget                -> async with asyncio.timeout(t) (3.11+)\n#   protect a critical op from outer cancel     -> asyncio.shield(coro)\n#   batch fan-out with deadline                  -> TaskGroup nested in timeout()\n#   cleanup on cancel                            -> try/except CancelledError, then RAISE\n#   need to NEVER cancel a step                  -> shield + run it to completion\n#\n# Anti-pattern: swallowing CancelledError without re-raising\n#   The task ignores cancellation; shutdown hangs; the runtime can't reclaim\n#   resources. ALWAYS re-raise after your finally/cleanup runs.\nasync def fetch(_): await asyncio.sleep(0); return None\nasync def flaky_call(): await asyncio.sleep(2)\nasync def cleanup(): await asyncio.sleep(0)\nasync def one_step(): await asyncio.sleep(0)\nasync def flush_partial(): pass\ndef log(_): pass"
                  }
        ],
        tips: [
                  "wait_for(coro, t) wraps a SINGLE coroutine; `async with asyncio.timeout(s)` (3.11+) wraps a BLOCK with multiple awaits sharing one budget — cleaner for chains. Both ultimately CANCEL the running task to enforce the deadline.",
                  "Always propagate CancelledError with raise — don't catch it without re-raising.",
                  "shield() prevents cancellation but timeout can still fire — useful for critical work that MUST complete (save / commit). Pair with `await work` after the timeout to keep waiting on the shielded coroutine.",
                  "Timeout from parent propagates to children — nested operations inherit deadline. Combine `async with asyncio.timeout(N)` around an `async with asyncio.TaskGroup()` to put one budget on a fan-out."
        ],
        mistake: "Catching CancelledError without re-raising — the task ignores cancellation, shutdown hangs, the runtime can't reclaim resources. Catch CancelledError, run cleanup in `finally`, then RE-RAISE so the runtime can stop the task.",
        shorthand: {
          verbose: "try:\n    result = await asyncio.wait_for(operation(), timeout=5)\nexcept asyncio.TimeoutError:\n    print(\"Timeout\")",
          concise: "async with asyncio.timeout(5):\n    result = await operation()",
        },
      },
    ],
  },

  // ── Section 6: Multiprocessing Deep Dive — Advanced Patterns ─────────────────────────────────────────
  {
    id: "multiprocessing-deep",
    title: "Multiprocessing Deep Dive — Advanced Patterns",
    entries: [
      {
        id: "mp-pool",
        fn: "multiprocessing.Pool — Process Pool API",
        desc: "High-level process pool: .map(), .apply_async(), context manager, and chunking.",
        category: "Multiprocessing",
        subtitle: "multiprocessing.Pool, .map(), .starmap(), .apply_async(), context manager",
        signature: "with Pool(4) as pool: pool.map(fn, items)  |  pool.apply_async(fn, (args,))",
        descLong: "multiprocessing.Pool provides a high-level API for distributing work across processes. .map() is like Python's map but parallel. .apply_async() submits a single task and returns ApplyResult (non-blocking). .starmap() unpacks arguments. .imap_unordered() yields results as they finish (unordered). Pool initializes workers once, reducing startup cost. Always use context manager for proper cleanup.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of multiprocessing.Pool — Process Pool API — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nfrom multiprocessing import Pool, cpu_count\ndef heavy(n): return sum(i * i for i in range(n))\nif __name__ == \"__main__\":                          # required on Windows / spawn-style\n    with Pool(cpu_count()) as pool:\n        print(pool.map(heavy, [10**6] * 8))"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of multiprocessing.Pool — Process Pool API — common patterns you'll see in production.\n# APPROACH  - Combine multiprocessing.Pool — Process Pool API with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nfrom multiprocessing import Pool, cpu_count\ndef heavy(n): return sum(i * i for i in range(n))\ndef add(a, b): return a + b\n# 1) starmap — unpack each tuple as args (no need for lambda x: f(*x))\nwith Pool() as p:\n    print(p.starmap(add, [(1, 2), (3, 4), (5, 6)]))   # [3, 7, 11]\n# 2) apply_async — submit ONE task, get a result object back\nwith Pool(4) as p:\n    r = p.apply_async(heavy, (10**6,))\n    print(r.get(timeout=10))\n# 3) imap_unordered + chunksize — stream results as they finish\nwith Pool() as p:\n    for v in p.imap_unordered(heavy, [10**5] * 100, chunksize=10):\n        save(v)\n# 4) initializer — per-process setup paid ONCE per worker\n_model = None\ndef _init(path):\n    global _model; _model = load_model(path)\ndef _predict(x):\n    return _model.predict(x)\nwith Pool(initializer=_init, initargs=(\"models/v1.pkl\",)) as p:\n    preds = p.map(_predict, batches, chunksize=8)\ndef save(_): pass\ndef load_model(_): return None\nbatches = []"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of multiprocessing.Pool — Process Pool API — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nfrom multiprocessing import Pool, TimeoutError\ndef heavy(n):\n    if n == 0:\n        raise ValueError(\"n cannot be 0\")\n    return sum(i * i for i in range(n))\n# 1) Error CONTRACT — workers raise; the parent must check\nwith Pool() as pool:\n    async_r = pool.map_async(heavy, [10**5, 0, 10**6])\n    try:\n        values = async_r.get(timeout=10)             # re-raises the worker exception\n    except ValueError as e:\n        print(\"worker raised:\", e)\n    except TimeoutError:\n        print(\"a task exceeded 10s\")\n# 2) Shutdown semantics\n#    pool.close() -> stop accepting new tasks; existing finish\n#    pool.join()  -> wait for in-flight to drain\n#    pool.terminate() -> KILL workers (SIGTERM); pending results lost\n#    Context manager calls close()+join() automatically — prefer it.\n# 3) Picklability — top-level functions / dataclasses only\n#    NOT picklable: lambdas, closures, locally-defined classes\n#    Workaround: cloudpickle (via joblib / dask) when refactoring isn't an option\n# 4) Pool vs ProcessPoolExecutor — pick by ergonomics, not capability\n#    Pool                       -> .map / .imap_unordered / .apply_async, mature\n#    ProcessPoolExecutor        -> Future-based, same shape as ThreadPoolExecutor\n#    Both spawn the SAME workers; switch freely.\n# 5) Start methods (multiprocessing.set_start_method)\n#    fork  (Linux default)      -> fast; can deadlock with threads + locks held at fork\n#    spawn (Windows/macOS def)  -> safer; slower; REQUIRES if __name__ == \"__main__\":\n#    forkserver                 -> compromise: fast spawning of clean child processes\n#    Pin one explicitly in production startup:\n#      from multiprocessing import set_start_method\n#      set_start_method(\"spawn\", force=True)\n# Decision rule:\n#   pure-Python CPU, > 100 ms per task        -> Pool.map / Pool.imap_unordered\n#   numeric heavy lifting (numpy/sklearn)      -> threads often beat processes\n#   need a Future-style API                     -> ProcessPoolExecutor\n#   want streaming results (any order)          -> imap_unordered + chunksize\n#   per-worker heavy resource                   -> initializer + module global\n#   long-running service                          -> set_start_method(\"spawn\") for stability\n#\n# Anti-pattern: pool.terminate() in normal flow\n#   Workers die mid-task; partial state on disk; resources leak. Reserve\n#   terminate() for \"the orchestrator is dying anyway\"; otherwise close + join."
                  }
        ],
        tips: [
                  "context manager (with Pool) handles cleanup automatically — always use it. `pool.close()` + `pool.join()` is the graceful path; reserve `pool.terminate()` for \"the orchestrator is dying anyway\" (workers die mid-task and partial state leaks).",
                  "initializer runs once per worker — expensive setup (model loading) happens once, not per task.",
                  "imap_unordered is faster than map when order doesn't matter — results as they arrive.",
                  "chunksize affects performance — larger chunks reduce pickling overhead but use more memory. For long-running services pin the start method explicitly: `multiprocessing.set_start_method(\"spawn\", force=True)` — fork is fast but can deadlock when forked while a thread holds locks (e.g. logging)."
        ],
        mistake: "pool.terminate() in normal flow — workers die mid-task, partial state hits disk, resources leak. Use the context manager (close+join). Also: relying on the platform default start method — explicitly `set_start_method(\"spawn\")` in production to avoid the fork+threads+locks deadlock footgun.",
        shorthand: {
          verbose: "pool = Pool(4)\nresults = pool.map(fn, items)\npool.close()\npool.join()",
          concise: "with Pool(4) as pool:\n    results = pool.map(fn, items)",
        },
      },
      {
        id: "mp-queue-pipe",
        fn: "multiprocessing Queue & Pipe — Inter-Process Communication",
        desc: "Pass data between processes: Queue (multiple consumers), Pipe (two endpoints).",
        category: "Multiprocessing",
        subtitle: "multiprocessing.Queue, Pipe, inter-process communication",
        signature: "queue = Queue()  |  queue.put(item)  |  item = queue.get()  |  conn1, conn2 = Pipe()",
        descLong: "Queue is thread-safe and process-safe FIFO. Multiple producers/consumers. Pipe is bidirectional communication between exactly 2 processes. Queue is easier for many-to-many; Pipe for 1-to-1. Both handle pickling automatically. Use for: work distribution (queue), request-response patterns (pipe), progress reporting.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of multiprocessing Queue & Pipe — Inter-Process Communication — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nfrom multiprocessing import Queue, Process\ndef producer(q):\n    for i in range(5):\n        q.put(i)\nif __name__ == \"__main__\":\n    q = Queue()\n    Process(target=producer, args=(q,)).start()\n    for _ in range(5):\n        print(q.get())"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of multiprocessing Queue & Pipe — Inter-Process Communication — common patterns you'll see in production.\n# APPROACH  - Combine multiprocessing Queue & Pipe — Inter-Process Communication with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nfrom multiprocessing import JoinableQueue, Pipe, Process\nSENTINEL = None\ndef worker(q, wid):\n    while True:\n        item = q.get()\n        if item is SENTINEL:\n            q.task_done(); return\n        try:\n            process(item)\n        finally:\n            q.task_done()                            # ALWAYS, even on failure\ndef producer(q, n_workers, items):\n    for it in items:\n        q.put(it)\n    for _ in range(n_workers):\n        q.put(SENTINEL)\nif __name__ == \"__main__\":\n    q = JoinableQueue()\n    workers = [Process(target=worker, args=(q, i)) for i in range(3)]\n    for w in workers: w.start()\n    producer(q, 3, range(20))\n    q.join()                                         # wait for ALL task_done()\n    for w in workers: w.join()\n# Pipe — strictly bidirectional, exactly two endpoints\nparent, child = Pipe()\ndef echo(c):\n    while True:\n        try:\n            msg = c.recv()\n        except EOFError:                             # peer closed\n            return\n        c.send(msg.upper())\ndef process(_): pass"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of multiprocessing Queue & Pipe — Inter-Process Communication — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nfrom multiprocessing import Queue, Pipe, Process\nfrom queue import Empty, Full\nimport time\n# 1) Object pickled cost is REAL — the per-message overhead bounds throughput.\n#    Pass tiny refs (path, key) and have the worker open / load on its side.\n#    For huge numpy arrays: shared_memory, NOT Queue.put.\n# 2) qsize() is NOT reliable — only use it as a HINT\n#    Use Empty / Full exceptions with timeouts for correctness.\ndef consume(q):\n    while True:\n        try:\n            item = q.get(timeout=1.0)                # blocks then raises Empty\n        except Empty:\n            if shutting_down(): return\n            continue\n        process(item)\n# 3) Bounded queue — backpressure on the producer\nq = Queue(maxsize=100)\ndef slow_producer(q):\n    for x in stream_source():\n        try:\n            q.put(x, timeout=5)                      # raises Full after 5s\n        except Full:\n            log(\"downstream stalled\")\n# 4) Pipe EOF discipline — peer .close() raises EOFError on .recv()\ndef reader(conn):\n    try:\n        while True:\n            msg = conn.recv()\n            handle(msg)\n    except EOFError:\n        return                                       # clean exit; do NOT log as error\n    finally:\n        conn.close()\n# 5) Polling multiple connections — connection.wait() handles N at once\nfrom multiprocessing.connection import wait\ndef multiplex(conns, deadline=10.0):\n    end = time.monotonic() + deadline\n    while conns:\n        ready = wait(conns, timeout=end - time.monotonic())\n        if not ready: break                          # timeout\n        for c in ready:\n            try:    handle(c.recv())\n            except EOFError: conns.remove(c)\n# Decision rule:\n#   one-to-many work distribution         -> Queue (or JoinableQueue if waiting on completion)\n#   strictly two endpoints, request/reply  -> Pipe\n#   need bounded backpressure              -> Queue(maxsize=N)\n#   wait for ALL items processed           -> JoinableQueue + q.join()\n#   poll multiple connections               -> multiprocessing.connection.wait\n#   shipping huge arrays                     -> shared_memory, NOT Queue.put\n#\n# Anti-pattern: forgetting q.task_done() in the failure path\n#   q.join() blocks forever; the orchestrator hangs on shutdown. Always\n#   try/finally so task_done() is called even when process(item) raises.\ndef shutting_down(): return False\ndef stream_source(): return []\ndef handle(_): pass\ndef log(_): pass"
                  }
        ],
        tips: [
                  "Queue for many-to-many communication, Pipe for 1-to-1 request-response. For polling MULTIPLE Pipe connections, use `multiprocessing.connection.wait(conns, timeout=)`.",
                  "Put None to signal workers to stop — common pattern in producer-consumer.",
                  "queue.qsize() is not reliable — use it only as a hint, not for correctness. Use `Empty` / `Full` exceptions with timeouts instead. Pickled cost is real: pass tiny refs (path, key) and have the worker open / load on its side; for huge numpy arrays use `shared_memory`, NOT `Queue.put`.",
                  "JoinableQueue tracks task_done() — use when you need to wait for batch completion. ALWAYS call `task_done()` in a `finally` so a failure in the worker body doesn't leave `q.join()` hanging forever."
        ],
        mistake: "Forgetting `q.task_done()` in the failure path — `q.join()` blocks forever and the orchestrator hangs on shutdown. Always wrap the consumer body in `try/finally: q.task_done()`. For Pipes, the peer's `.close()` raises `EOFError` on `.recv()` — handle it as a clean exit, not an error.",
        shorthand: {
          verbose: "queue = Queue()\nqueue.put(item)\nreceived = queue.get()",
          concise: "conn1, conn2 = Pipe()\nconn1.send(item)\nreceived = conn2.recv()",
        },
      },
      {
        id: "mp-shared-memory",
        fn: "multiprocessing Shared State — Value, Array, Manager",
        desc: "Share state between processes: Value (single), Array (bulk), Manager (complex).",
        category: "Multiprocessing",
        subtitle: "multiprocessing.Value, Array, Manager, shared state patterns",
        signature: "val = Value(\"i\", 0)  |  arr = Array(\"d\", [1, 2, 3])  |  manager.list(), manager.dict()",
        descLong: "Value wraps a single atomic value (int, float, char). Array is a shared numpy-like array. Manager creates a proxy server for complex types (list, dict, object). Avoid shared state when possible — it's error-prone. Prefer Queue/Pipe. If needed, always use locks to prevent race conditions.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of multiprocessing Shared State — Value, Array, Manager — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nfrom multiprocessing import Value, Lock, Process\ndef bump(counter, lock):\n    for _ in range(1000):\n        with lock:\n            counter.value += 1                       # NOT atomic; needs the lock\nif __name__ == \"__main__\":\n    counter, lock = Value(\"i\", 0), Lock()\n    ps = [Process(target=bump, args=(counter, lock)) for _ in range(4)]\n    for p in ps: p.start()\n    for p in ps: p.join()\n    print(counter.value)                              # exactly 4000"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of multiprocessing Shared State — Value, Array, Manager — common patterns you'll see in production.\n# APPROACH  - Combine multiprocessing Shared State — Value, Array, Manager with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nfrom multiprocessing import Value, Array, Manager, Process, Lock\n# 1) Value / Array — fast, ctypes-backed; lock built in (.get_lock())\ncounter = Value(\"i\", 0)\nfloats  = Array(\"d\", [1.0, 2.0, 3.0])\n# 2) Manager — proxy server for COMPLEX types; slower but supports dict/list/Namespace\ndef fill(d, k, v): d[k] = v\nif __name__ == \"__main__\":\n    with Manager() as mgr:\n        shared_dict = mgr.dict()\n        shared_list = mgr.list()\n        ns          = mgr.Namespace()\n        ns.status   = \"init\"\n        ps = [Process(target=fill, args=(shared_dict, k, v))\n              for k, v in [(\"a\", 1), (\"b\", 2), (\"c\", 3)]]\n        for p in ps: p.start()\n        for p in ps: p.join()\n        print(dict(shared_dict))                      # {'a':1, 'b':2, 'c':3}\n        print(ns.status)"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of multiprocessing Shared State — Value, Array, Manager — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nfrom multiprocessing import Value, Array, Manager, shared_memory, Process, Lock\nimport numpy as np\n# 1) shared_memory (3.8+) — zero-copy, no proxy server, no pickling per access\narr = np.arange(10_000_000, dtype=np.float64)\nshm = shared_memory.SharedMemory(create=True, size=arr.nbytes)\nnp.ndarray(arr.shape, dtype=arr.dtype, buffer=shm.buf)[:] = arr\ndef worker(shm_name, shape, dtype, sl):\n    s = shared_memory.SharedMemory(name=shm_name)\n    a = np.ndarray(shape, dtype=dtype, buffer=s.buf)\n    return a[sl].sum()                               # zero-copy READ\n# 2) Manager has REAL cost — every attribute access is an IPC call to the server\n#    For hot loops over a managed dict / list, performance falls off a cliff.\n#    Use Manager for: low-frequency coordination, status, config, results dict.\n# 3) Value's built-in lock is convenient but slower than an external Lock when\n#    you do many tight increments — group with one Lock + a regular int instead.\n# 4) Heuristic: PASS MESSAGES, don't share state.\n#    Cleaner pattern: workers compute, Queue carries results, ONE process aggregates.\n#    Reach for Value/Array/shared_memory only for hot, large, numeric data.\n# 5) Cleanup is YOUR job — shared_memory leaks memory across runs if you forget\ntry:\n    pass                                              # ... use shm ...\nfinally:\n    shm.close()                                       # this process detaches\n    shm.unlink()                                      # creator REMOVES the segment\n# Decision rule:\n#   one int counter / latch                  -> Value (with explicit Lock if hot)\n#   small fixed-size numeric array            -> Array\n#   shared dict / list / Namespace             -> Manager (low-frequency only)\n#   huge numpy / image data, hot loop          -> shared_memory + np.ndarray\n#   coordinating completion / status            -> Queue or Event, not shared dict\n#   anything you can model as messages          -> Queue / Pipe; avoid shared state\n#\n# Anti-pattern: hot-path Manager().dict updates\n#   Each m.dict[key] = value is a round-trip to the manager process; throughput\n#   collapses. Aggregate locally in each worker, Queue.put() once at the end."
                  }
        ],
        tips: [
                  "Avoid shared state — use Queue/Pipe instead. The default heuristic is PASS MESSAGES, don't share state: workers compute, Queue carries results, ONE process aggregates.",
                  "Always protect shared state with Lock — `Value.value += 1` is NOT atomic. Race conditions are silent and subtle.",
                  "Value is for single variables, Array for bulk data, Manager for complex types — but Manager has REAL cost: every attribute access is an IPC round-trip to the manager process. Reserve it for low-frequency coordination (status, config, results dict).",
                  "For huge hot numeric data, use `shared_memory` (3.8+) + `np.ndarray(..., buffer=shm.buf)` for zero-copy access — and remember `shm.close()` (this process detaches) AND `shm.unlink()` (creator removes the segment) or you leak across runs."
        ],
        mistake: "Hot-path Manager().dict updates — each `m_dict[k] = v` is a round-trip to the manager process and throughput collapses. Aggregate locally in each worker, then `Queue.put()` once at the end. Also: shared state without a Lock corrupts data silently (Value.value += 1 is not atomic).",
        shorthand: {
          verbose: "counter = Value('i', 0)\nlock = Lock()\nwith lock:\n    counter.value += 1",
          concise: "queue.put(1)\n# Aggregate results via Queue",
        },
      },
      {
        id: "mp-lock",
        fn: "multiprocessing Synchronization Primitives",
        desc: "Process-safe locks: Lock, RLock, Semaphore for process coordination.",
        category: "Multiprocessing",
        subtitle: "multiprocessing.Lock, RLock, Semaphore, process synchronization",
        signature: "lock = Lock()  |  with lock: ...  |  semaphore = Semaphore(3)",
        descLong: "Lock ensures mutual exclusion across processes. RLock (reentrant) allows same process to acquire multiple times. Semaphore limits concurrent processes. Condition combines lock + signaling. Always use context manager (with lock) to prevent deadlock.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of multiprocessing Synchronization Primitives — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nfrom multiprocessing import Lock, Process\ndef write_line(lock, msg):\n    with lock:                                       # only one process at a time\n        with open(\"log.txt\", \"a\") as f:\n            f.write(msg + \"\\n\")\nif __name__ == \"__main__\":\n    lock = Lock()\n    ps = [Process(target=write_line, args=(lock, f\"hi {i}\")) for i in range(4)]\n    for p in ps: p.start()\n    for p in ps: p.join()"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of multiprocessing Synchronization Primitives — common patterns you'll see in production.\n# APPROACH  - Combine multiprocessing Synchronization Primitives with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nfrom multiprocessing import Lock, RLock, Semaphore, Process\n# 1) Lock.acquire(timeout=) — never block forever in production\ndef safe_section(lock):\n    if lock.acquire(timeout=1.0):\n        try:\n            do_work()\n        finally:\n            lock.release()\n    else:\n        log(\"could not acquire lock in 1s — skipping\")\n# 2) Semaphore — N concurrent processes (e.g. only K downloads in flight)\nsem = Semaphore(3)\ndef bounded(i):\n    with sem:                                        # at most 3 inside this block\n        do_io(i)\n# 3) RLock — same process re-acquires; needed when method calls other locked method\nrlock = RLock()\ndef outer():\n    with rlock:\n        inner()                                      # would deadlock with plain Lock\ndef inner():\n    with rlock:\n        do_step()\ndef do_work(): pass\ndef do_io(_): pass\ndef do_step(): pass\ndef log(_): pass"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of multiprocessing Synchronization Primitives — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nfrom multiprocessing import Lock, Barrier, Process, Value\n# 1) Lock IS NEEDED for shared Value updates — Value.value += 1 is not atomic\ncounter = Value(\"i\", 0)\ncounter_lock = Lock()\ndef safe_bump():\n    with counter_lock:\n        counter.value += 1                           # read-modify-write inside the lock\n# 2) Barrier — synchronize a known number of workers before proceeding\nN = 4\nbarrier = Barrier(N)\ndef phase_worker(i):\n    setup(i)                                          # phase 1 (parallel)\n    barrier.wait()                                    # all N must arrive\n    process_phase2(i)                                 # phase 2 starts together\nif __name__ == \"__main__\":\n    for i in range(N):\n        Process(target=phase_worker, args=(i,)).start()\n# 3) Deadlock prevention — the cardinal rules\n#    a) Lock ORDER: if you ever take A then B, NEVER take B then A elsewhere.\n#    b) Always use `with lock:`; manual acquire/release leaks on exception.\n#    c) Use timeouts; design for \"couldn't acquire\" as a normal outcome.\n#    d) Hold locks for the SHORTEST window possible; do I/O outside.\n# 4) Fork + locks footgun (Linux default start method)\n#    If a child is forked while a thread in the parent HOLDS a lock, the child\n#    inherits a LOCKED lock that nobody will release -> deadlock at first use.\n#    Mitigations:\n#      - set_start_method(\"spawn\") in production\n#      - never fork from inside a thread that holds locks (e.g. logging handler)\n# 5) Process-safe counters via the Manager are SLOW (IPC per increment)\n#    For hot counters, use Value + Lock (or batch increments locally + Queue).\n# Decision rule:\n#   guard a critical section across procs    -> Lock (with timeout in production)\n#   re-entrant from same process              -> RLock\n#   bound concurrent processes to N           -> Semaphore(N)\n#   stage a parallel pipeline                 -> Barrier(N)\n#   need wait-for-condition                    -> Condition (lock + signal)\n#   one-shot start signal                      -> Event\n#   lock + Manager dict in hot loop            -> redesign — use Queue messages\n#\n# Anti-pattern: lock.acquire() with no timeout, no try/finally\n#   First exception leaks the lock; every other process hangs. ALWAYS use\n#   \"with lock:\" — it releases even if your code raises.\ndef setup(_): pass\ndef process_phase2(_): pass"
                  }
        ],
        tips: [
                  "Lock.acquire(timeout=T) prevents indefinite blocking — use in production.",
                  "Condition is for process coordination (signaling), Lock for exclusive access; `Barrier(N)` synchronizes a known number of workers before they all proceed to the next phase together.",
                  "Semaphore(N) limits N concurrent processes — useful for resource pooling.",
                  "Always use context manager (with lock) — prevents forgetting to release. Cardinal deadlock-prevention rules: (a) acquire multiple locks in a CONSISTENT order across the codebase, (b) hold locks for the SHORTEST window possible, never do I/O under a lock, (c) use timeouts and treat \"couldn't acquire\" as a normal outcome. On Linux the fork start method can deadlock if a child is forked while a thread holds a lock — pin `set_start_method(\"spawn\")` in production."
        ],
        mistake: "`lock.acquire()` with no timeout and no try/finally — the first exception leaks the lock and every other process hangs. ALWAYS use `with lock:` (auto-releases on exception). Also: forking from inside a thread that holds a lock (e.g. logging handler) deadlocks the child at first use — `set_start_method(\"spawn\")` avoids it.",
        shorthand: {
          verbose: "lock.acquire()\ntry:\n    # critical section\nfinally:\n    lock.release()",
          concise: "with lock:\n    # critical section",
        },
      },
    ],
  },

  // ── Section 7: Threading Patterns & Synchronization ─────────────────────────────────────────
  {
    id: "threading-patterns",
    title: "Threading Patterns & Synchronization",
    entries: [
      {
        id: "threading-lock",
        fn: "threading Synchronization — Lock, Condition, Event, Semaphore",
        desc: "Thread-safe primitives: Lock (mutual exclusion), Condition (signaling), Event (one-shot signal), Semaphore (counting).",
        category: "Threading",
        subtitle: "threading.Lock(), RLock, Condition, Event, Semaphore",
        signature: "lock = threading.Lock()  |  with lock: ...  |  event = threading.Event()",
        descLong: "threading.Lock ensures only one thread accesses critical section. RLock allows same thread to reacquire. Condition combines Lock + signaling (wait/notify). Event allows one thread to signal all others. Semaphore limits N concurrent threads. Use for: protecting shared state, coordinating threads, implementing producer-consumer.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of threading Synchronization — Lock, Condition, Event, Semaphore — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#\nimport threading\ncounter = 0\nlock = threading.Lock()\ndef bump():\n    global counter\n    for _ in range(100_000):\n        with lock:                                   # ALWAYS use 'with'; auto-release\n            counter += 1\nthreads = [threading.Thread(target=bump) for _ in range(4)]\nfor t in threads: t.start()\nfor t in threads: t.join()\nprint(counter)                                       # exactly 400_000"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of threading Synchronization — Lock, Condition, Event, Semaphore — common patterns you'll see in production.\n# APPROACH  - Combine threading Synchronization — Lock, Condition, Event, Semaphore with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\nimport threading\nfrom collections import deque\n# 1) Event — broadcast 'go' or 'stop' to many threads\nstop = threading.Event()\ndef worker():\n    while not stop.is_set():\n        do_step()\n        stop.wait(timeout=1.0)                       # wakes early if stop.set()\n# 2) Semaphore — bound concurrent threads\nsem = threading.Semaphore(3)\ndef bounded():\n    with sem:                                        # at most 3 inside at a time\n        do_io()\n# 3) Condition + wait_for — producer/consumer with predicate\ncond = threading.Condition()\nitems: deque[int] = deque()\ndef producer():\n    with cond:\n        items.append(42)\n        cond.notify(1)\ndef consumer():\n    with cond:\n        cond.wait_for(lambda: items)                 # wait UNTIL predicate is true\n        return items.popleft()\ndef do_step(): pass\ndef do_io(): pass"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of threading Synchronization — Lock, Condition, Event, Semaphore — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\nimport threading\nimport queue\n# 1) threading.local — per-thread \"globals\" without locks\nctx = threading.local()\ndef handle(req_id):\n    ctx.request_id = req_id                          # invisible to other threads\n    log_with_context()\ndef log_with_context():\n    print(\"rid:\", getattr(ctx, \"request_id\", \"-\"))\n# 2) Barrier — synchronize phases across N threads\nN = 4\nbarrier = threading.Barrier(N)\ndef phase_worker():\n    setup()                                          # phase 1 in parallel\n    barrier.wait()                                   # all N must arrive\n    phase_two()                                      # phase 2 starts together\n# 3) PREFER queue.Queue over hand-rolled Lock + list — it's Lock + Condition + size\nq: queue.Queue[int] = queue.Queue(maxsize=100)\ndef producer():\n    for x in range(1000):\n        q.put(x)                                     # blocks if full -> backpressure\n    for _ in range(N):\n        q.put(None)                                  # one sentinel per consumer\ndef consumer():\n    while True:\n        item = q.get()\n        try:\n            if item is None: return\n            handle_item(item)\n        finally:\n            q.task_done()\n# 4) Lock-free designs — when you can structure work as message hand-off, only\n#    ONE thread owns mutable state and you need NO locks at all.\n# 5) Deadlock prevention rules — same as multiprocessing\n#    - Always use 'with lock:' (never bare acquire/release)\n#    - Acquire multiple locks in a CONSISTENT order across the codebase\n#    - Use timeouts in production: lock.acquire(timeout=N)\n#    - Hold locks for the SHORTEST possible window; never do I/O under a lock\n# Decision rule:\n#   shared mutable state                        -> Lock (RLock if recursive)\n#   per-thread context (request id, db conn)    -> threading.local()\n#   broadcast a one-shot signal                  -> Event\n#   bound concurrency to N                        -> Semaphore(N)\n#   wait until predicate                          -> Condition + wait_for(...)\n#   producer / consumer queue                     -> queue.Queue (don't roll your own)\n#   stage parallel pipeline                       -> Barrier(N)\n#\n# Anti-pattern: try/except that swallows exceptions inside a lock\n#   The lock is released, the bug is invisible, and the next thread sees corrupt\n#   state. Either fix the bug, or re-raise after cleanup; never silently eat it.\ndef setup(): pass\ndef phase_two(): pass\ndef handle_item(_): pass"
                  }
        ],
        tips: [
                  "Lock for protecting shared state, Condition for signaling, Event for one-shot signals. PREFER `queue.Queue` over hand-rolled Lock + list — it bundles Lock + Condition + maxsize backpressure for free.",
                  "Always use context manager (with lock) — prevents forgetting to release and handles exceptions.",
                  "Condition requires `cond.wait_for(predicate)` (not bare `wait()`) — `wait_for` loops internally so spurious wakeups can't slip past your predicate. Use `threading.local()` for per-thread context (request_id, db connection) — invisible to other threads, no locking needed.",
                  "Semaphore(N) limits concurrent access — useful for rate limiting (max N requests at once). `Barrier(N)` synchronizes phases: all N threads must arrive before any proceeds."
        ],
        mistake: "Hand-rolling a Lock + list + Condition for a producer/consumer queue — `queue.Queue(maxsize=N)` gives you all of that, tested. Also: try/except that swallows exceptions inside a lock — the lock releases, the bug is invisible, and the next thread sees corrupt state. Re-raise after cleanup.",
        shorthand: {
          verbose: "lock.acquire()\ntry:\n    counter += 1\nfinally:\n    lock.release()",
          concise: "with lock:\n    counter += 1",
        },
      },
    ],
  },
]

export default { meta, sections }

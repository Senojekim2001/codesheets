export const meta = {
  "id": "mqtt-iot",
  "label": "MQTT / IoT",
  "icon": "📡",
  "description": "MQTT for IoT in Python: paho-mqtt (the workhorse, sync + threaded callback API), aiomqtt (modern asyncio wrapper around paho). MQTT is publish/subscribe over TCP with three QoS levels (0/1/2), retained messages, last-will-and-testament, and a topic tree. The four things you keep getting wrong: assuming QoS 0 delivers (it doesn't guarantee), unique client_id under reconnect (broker disconnects the duplicate), persistent vs clean session (default `clean_session=True` loses subscriptions on reconnect), and topic wildcards (`+` matches one level, `#` matches the rest, only at the end)."
}

export const sections = [

  // ── Section 1: paho-mqtt — the workhorse client ─────────────────────────────────────────
  {
    id: "paho",
    title: "paho-mqtt — the workhorse client",
    entries: [
      {
        id: "paho-publish-subscribe",
        fn: "paho.Client / connect / publish / subscribe / loop",
        desc: "`mqtt.Client(client_id=...)` is the paho client. Set `on_connect` / `on_message` callbacks, `connect(host, port)`, then either `loop_start()` (background thread) or `loop_forever()` (blocking). Publish with `client.publish(topic, payload, qos=, retain=)`; subscribe with `client.subscribe(topic, qos=)`.",
        category: "paho",
        subtitle: "mqtt.Client(client_id=, clean_session=, protocol=mqtt.MQTTv5), on_connect / on_message / on_disconnect callbacks, loop_start (thread) vs loop_forever (block) vs loop() (manual), publish (qos=0/1/2, retain=), subscribe (single topic or list of (topic, qos)), MQTTMessage (.topic, .payload, .qos, .retain), reason_code on connect",
        signature: "c = mqtt.Client(client_id=\"dev1\"); c.on_message = on_msg; c.connect(\"broker\", 1883); c.subscribe(\"sensors/#\", qos=1); c.loop_start(); c.publish(\"sensors/temp\", b\"22.5\", qos=1, retain=False)",
        descLong: "paho is the reference MQTT client. The threaded model: `loop_start()` spins a background reader/writer thread; callbacks fire on it. `loop_forever()` blocks the calling thread instead — fine for single-purpose scripts. **Always set a unique `client_id`** (broker disconnects duplicates). Default `clean_session=True` (or `clean_start=True` in MQTTv5) means subscriptions are lost on reconnect — set it to `False` for persistent sessions. Three depths solve the SAME task — subscribe to `sensors/#` and publish a temperature reading every 5 s — at depths: minimal callback + loop_start + sleep loop → reconnect handler with exponential backoff + on_connect re-subscribe → MQTTv5 client with persistent session, properties, will-message, TLS.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Subscribe to sensors/#; publish a fake temp every 5 s.\n# APPROACH  - paho.Client + on_message + loop_start + time.sleep loop.\n# STRENGTHS - Smallest pub/sub demo.\n# WEAKNESSES- No reconnect handling; client_id auto-generated (collisions);\n#             QoS 0 (fire and forget; messages can be lost).\nimport paho.mqtt.client as mqtt\nimport time\n\ndef on_connect(client, userdata, flags, rc, properties=None):\n    print(\"connected, rc =\", rc)\n    client.subscribe(\"sensors/#\")                      # QoS 0 by default\n\ndef on_message(client, userdata, msg):\n    print(f\"<- {msg.topic} ({len(msg.payload)} B): {msg.payload!r}\")\n\nc = mqtt.Client(callback_api_version=mqtt.CallbackAPIVersion.VERSION2)\nc.on_connect = on_connect\nc.on_message = on_message\n\nc.connect(\"broker.hivemq.com\", 1883, keepalive=60)\nc.loop_start()                                         # background thread\n\ntry:\n    while True:\n        c.publish(\"sensors/lab1/temp\", b\"22.5\", qos=0)\n        time.sleep(5)\nexcept KeyboardInterrupt:\n    c.loop_stop(); c.disconnect()\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — sub + periodic publish — with stable client_id,\n#             reconnect handling, and QoS 1 (at-least-once).\n# APPROACH  - Set client_id; reconnect_delay_set; resubscribe in on_connect\n#             (subscriptions are lost on every reconnect with default\n#             clean_session=True).\n# STRENGTHS - Survives broker restarts and short network drops.\n# WEAKNESSES- QoS 1 = at-least-once -> handlers must be idempotent (a\n#             message can arrive twice).\nimport paho.mqtt.client as mqtt\nimport time, uuid\n\nCLIENT_ID = f\"lab1-publisher-{uuid.uuid4().hex[:6]}\"\n\n\ndef on_connect(client, userdata, flags, rc, properties=None):\n    if rc == 0:\n        print(\"connected\")\n        # Resubscribe EVERY time we reconnect.\n        client.subscribe([(\"sensors/#\", 1), (\"control/+/cmd\", 1)])\n    else:\n        print(f\"connect failed rc={rc}\")\n\n\ndef on_message(client, userdata, msg):\n    print(f\"<- {msg.topic} qos={msg.qos} retain={msg.retain}: {msg.payload!r}\")\n\n\ndef on_disconnect(client, userdata, rc, properties=None, reason=None):\n    print(\"disconnected rc =\", rc)\n\n\nc = mqtt.Client(client_id=CLIENT_ID, clean_session=True,\n                callback_api_version=mqtt.CallbackAPIVersion.VERSION2)\nc.on_connect    = on_connect\nc.on_message    = on_message\nc.on_disconnect = on_disconnect\n\n# Auto-reconnect with exponential backoff (paho built-in).\nc.reconnect_delay_set(min_delay=1, max_delay=60)\n\nc.connect_async(\"broker.hivemq.com\", 1883, keepalive=60)\nc.loop_start()\n\ntry:\n    while True:\n        info = c.publish(\"sensors/lab1/temp\", b\"22.5\", qos=1, retain=False)\n        info.wait_for_publish(timeout=2)               # block until broker ack\n        time.sleep(5)\nexcept KeyboardInterrupt:\n    c.loop_stop(); c.disconnect()\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — telemetry publisher — production: persistent session\n#             (subscriptions survive reconnect), MQTTv5, last-will,\n#             retained \"online\" status, TLS.\n# APPROACH  - protocol=MQTTv5 + clean_start=False; will_set; tls_set;\n#             retained status published on connect, will fires on crash.\n# STRENGTHS - Survives broker restarts; downstream sees online/offline;\n#             encrypted in flight; per-message properties (MQTTv5).\n# WEAKNESSES- Persistent sessions can pile up undelivered messages on the\n#             broker if the device is offline for hours - choose carefully.\nfrom __future__ import annotations\nimport paho.mqtt.client as mqtt\nfrom paho.mqtt.properties import Properties\nfrom paho.mqtt.packettypes import PacketTypes\nimport time, json, ssl, uuid\n\n\nCLIENT_ID  = f\"lab1-{uuid.uuid4().hex[:6]}\"\nHOST, PORT = \"broker.example.com\", 8883                 # 8883 = MQTT over TLS\n\nSTATUS_TOPIC = f\"devices/{CLIENT_ID}/status\"\nTELEMETRY    = f\"devices/{CLIENT_ID}/telemetry\"\n\n\ndef on_connect(client, userdata, flags, rc, properties=None):\n    if rc == 0:\n        print(\"connected, session_present =\", flags.session_present if hasattr(flags, 'session_present') else flags.get(\"session present\"))\n        # Mark online (retained so subscribers see it without a publish race).\n        client.publish(STATUS_TOPIC, json.dumps({\"online\": True}).encode(),\n                       qos=1, retain=True)\n        # Resubscribe ONLY if we don't have a session.\n        client.subscribe(f\"control/{CLIENT_ID}/+\", qos=1)\n    else:\n        print(\"connect failed:\", rc)\n\n\ndef on_message(client, userdata, msg):\n    print(f\"cmd <- {msg.topic}: {msg.payload!r}\")\n\n\ndef make_client() -> mqtt.Client:\n    c = mqtt.Client(\n        client_id=CLIENT_ID,\n        callback_api_version=mqtt.CallbackAPIVersion.VERSION2,\n        protocol=mqtt.MQTTv5,\n    )\n    c.on_connect    = on_connect\n    c.on_message    = on_message\n    c.reconnect_delay_set(min_delay=1, max_delay=120)\n\n    # Last-will: broker publishes this if the client drops without a clean\n    # disconnect (network loss, kill -9, etc.).\n    c.will_set(STATUS_TOPIC,\n               payload=json.dumps({\"online\": False}).encode(),\n               qos=1, retain=True)\n\n    # TLS - load CA + client cert.\n    c.tls_set(\n        ca_certs=\"/etc/ssl/certs/ca-certificates.crt\",\n        # certfile=\"/etc/mqtt/dev-cert.pem\",\n        # keyfile=\"/etc/mqtt/dev-key.pem\",\n        tls_version=ssl.PROTOCOL_TLSv1_2,\n    )\n    # c.tls_insecure_set(True)                          # only for self-signed dev brokers\n\n    # Optional credentials.\n    # c.username_pw_set(\"dev-user\", \"secret\")\n\n    return c\n\n\ndef publish_telemetry(c: mqtt.Client, payload: dict) -> None:\n    # MQTTv5 properties: e.g. message expiry to drop stale telemetry.\n    props = Properties(PacketTypes.PUBLISH)\n    props.MessageExpiryInterval = 60                   # seconds\n    info = c.publish(\n        TELEMETRY, json.dumps(payload).encode(),\n        qos=1, retain=False, properties=props,\n    )\n    info.wait_for_publish(timeout=2)\n\n\ndef main() -> None:\n    c = make_client()\n    # MQTTv5 persistent session: clean_start=False AND session_expiry > 0.\n    connect_props = Properties(PacketTypes.CONNECT)\n    connect_props.SessionExpiryInterval = 24 * 3600\n    c.connect(HOST, PORT, keepalive=30,\n              clean_start=False, properties=connect_props)\n    c.loop_start()\n\n    try:\n        while True:\n            publish_telemetry(c, {\"temp\": 22.5, \"humidity\": 41.0,\n                                  \"ts\": int(time.time())})\n            time.sleep(5)\n    finally:\n        # Clean disconnect = the will message will NOT fire (we said \"online: False\"\n        # ourselves through the same retained topic).\n        c.publish(STATUS_TOPIC, json.dumps({\"online\": False}).encode(),\n                  qos=1, retain=True).wait_for_publish(2)\n        c.loop_stop(); c.disconnect()\n\n\nif __name__ == \"__main__\":\n    main()\n\n# Decision rule:\n#   Quick prototype                          -> default Client + loop_start.\n#   Need reconnect resilience                 -> reconnect_delay_set + resubscribe in on_connect.\n#   Need at-least-once delivery               -> qos=1; handlers must be idempotent.\n#   Need exactly-once delivery                -> qos=2 (slow; rarely needed in IoT).\n#   Need broker to remember subscriptions     -> clean_session=False (MQTTv3) or\n#                                                clean_start=False + SessionExpiryInterval (v5).\n#   Need device \"is online\" status            -> last-will + retained STATUS_TOPIC.\n#   Need encrypted transport                  -> tls_set + port 8883.\n#   Multi-tenant brokers                      -> ACLs + per-device certs (mutual TLS).\n#   Need to detect offline downstream         -> retained status + monitor it.\n#   Want the subscriber to immediately see the latest reading -> retain=True on publish.\n\n# Anti-pattern:\n#   c = mqtt.Client()                       # auto-generated client_id\n#   c.connect(...); c.disconnect(); c.connect(...)\n# Each connect picks a new random client_id, so the broker thinks each\n# is a different device. Subscriptions / sessions don't persist.\n# ALWAYS set a stable client_id (e.g. mac address + role).\n"
                  }
        ],
        tips: [
                  "Always pass a stable `client_id` — broker disconnects duplicates and per-device sessions are keyed on it.",
                  "Resubscribe inside `on_connect` — by default, subscriptions are lost on reconnect (`clean_session=True`).",
                  "QoS 0 = at-most-once (fire-and-forget); QoS 1 = at-least-once (handlers must be idempotent); QoS 2 = exactly-once (slow, rarely needed).",
                  "Use `client.will_set(topic, payload, qos, retain=True)` so subscribers see when the device goes offline ungracefully.",
                  "For MQTTv5 + persistent sessions, `clean_start=False` AND set `SessionExpiryInterval` properties on `CONNECT`."
        ],
        mistake: "Letting paho auto-generate `client_id`. Each reconnect looks like a new device — sessions, subscriptions, and last-will state never persist. Always set a stable `client_id`.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
      {
        id: "paho-retained-lwt-topic-design",
        fn: "retain / LWT / topic structure — patterns that scale",
        desc: "Three under-used MQTT features that change everything: **retained messages** (broker stores the last on the topic; new subscribers get it immediately), **last-will-and-testament** (LWT — broker publishes on your behalf when you drop ungracefully), and **topic-tree design** (hierarchical, no slashes-as-data; wildcards `+` and `#` only work this way).",
        category: "paho",
        subtitle: "retain=True publishes the LAST message persistently; new subscribers get it immediately, will_set(topic, payload, qos, retain) declared at CONNECT time, topic wildcards (+ = one level, # = rest, only at end), topic naming conventions (org/site/device/measurement), avoid leading \"/\", avoid encoding values in topic names",
        signature: "c.publish(\"status/dev1\", b\"online\", retain=True); c.will_set(\"status/dev1\", b\"offline\", qos=1, retain=True); c.subscribe(\"sensors/+/temp\")",
        descLong: "Retained messages: a *single* retained message per topic — `retain=True` overwrites; publishing an empty payload with `retain=True` deletes it. New subscribers get the retained message immediately on subscribe. LWT: the broker publishes a message on your behalf when it detects you dropped (no PINGRESP within keepalive × 1.5). Topic design: hierarchical paths (`org/site/device/measurement`), wildcards `+` (single level, anywhere) and `#` (multi-level, only at end). Three depths solve the SAME task — show the device's online/offline state to subscribers without polling — at depths: poll-based \"is my device alive\" → retained status + LWT (the right way) → multi-tenant topic tree with retained presence and per-device shared subscriptions for load balancing.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Show subscribers whether device dev1 is online.\n# APPROACH  - Publish \"online\" / \"offline\" yourself; subscribers poll.\n# STRENGTHS - No special MQTT features.\n# WEAKNESSES- Subscribers that connect AFTER your \"online\" message have\n#             no idea you exist. If you crash, no \"offline\" is sent.\nimport paho.mqtt.client as mqtt\nimport time, atexit\n\nc = mqtt.Client(\"dev1\", callback_api_version=mqtt.CallbackAPIVersion.VERSION2)\nc.connect(\"broker.hivemq.com\", 1883)\nc.loop_start()\n\nc.publish(\"status/dev1\", b\"online\")                   # not retained!\n\ndef shutdown():\n    c.publish(\"status/dev1\", b\"offline\").wait_for_publish(2)\n    c.disconnect()\n\natexit.register(shutdown)\n\nwhile True:\n    time.sleep(60)\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — device online/offline visibility — using RETAINED\n#             status + LWT (broker publishes \"offline\" on crash).\n# APPROACH  - retain=True publishes; will_set BEFORE connect.\n# STRENGTHS - Subscribers see status immediately; broker handles crash case.\n# WEAKNESSES- Single device; no topic-tree discipline.\nimport paho.mqtt.client as mqtt\nimport time\n\nCLIENT = \"dev1\"\nSTATUS = f\"status/{CLIENT}\"\n\nc = mqtt.Client(CLIENT, callback_api_version=mqtt.CallbackAPIVersion.VERSION2)\n\n# LWT MUST be set BEFORE connect.\nc.will_set(STATUS, b\"offline\", qos=1, retain=True)\n\nc.connect(\"broker.hivemq.com\", 1883, keepalive=30)\nc.loop_start()\n\n# Mark online with retain=True so any future subscriber sees it instantly.\nc.publish(STATUS, b\"online\", qos=1, retain=True).wait_for_publish(2)\n\ntry:\n    while True:\n        time.sleep(60)\nfinally:\n    # Clean disconnect: publish \"offline\" ourselves so the LWT doesn't.\n    c.publish(STATUS, b\"offline\", qos=1, retain=True).wait_for_publish(2)\n    c.loop_stop(); c.disconnect()\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — fleet visibility — production: hierarchical topic tree\n#             (org/site/device), retained presence + LWT per device, shared\n#             subscriptions on the consumer side for horizontal scale.\n# APPROACH  - acme/site-a/dev1/{status,telemetry}; consumers use $share/g1/.\n# STRENGTHS - One subscription pattern matches every device; consumers\n#             load-balance via shared subscriptions (MQTTv5 / EMQX / Mosquitto 2.x).\n# WEAKNESSES- Topic naming is forever - migrating breaks everyone.\nfrom __future__ import annotations\nimport paho.mqtt.client as mqtt\nimport time, json, uuid\n\n\nORG  = \"acme\"\nSITE = \"site-a\"\n\n\nclass FleetDevice:\n    \"\"\"One device's MQTT presence + telemetry publisher.\"\"\"\n\n    def __init__(self, device_id: str, host: str, port: int = 1883):\n        self.device_id = device_id\n        self.host, self.port = host, port\n\n        self.STATUS    = f\"{ORG}/{SITE}/{device_id}/status\"\n        self.TELEMETRY = f\"{ORG}/{SITE}/{device_id}/telemetry\"\n        self.CONTROL   = f\"{ORG}/{SITE}/{device_id}/control\"\n\n        self.c = mqtt.Client(client_id=device_id, clean_session=False,\n                             callback_api_version=mqtt.CallbackAPIVersion.VERSION2)\n        self.c.on_connect = self._on_connect\n        self.c.on_message = self._on_message\n\n        # LWT BEFORE connect.\n        self.c.will_set(\n            self.STATUS,\n            json.dumps({\"online\": False, \"ts\": int(time.time())}).encode(),\n            qos=1, retain=True,\n        )\n        self.c.reconnect_delay_set(min_delay=1, max_delay=120)\n\n    def _on_connect(self, client, userdata, flags, rc, properties=None):\n        if rc != 0:\n            print(\"connect failed:\", rc); return\n        # Mark online (retained for new subscribers).\n        client.publish(\n            self.STATUS,\n            json.dumps({\"online\": True, \"ts\": int(time.time())}).encode(),\n            qos=1, retain=True,\n        )\n        client.subscribe(self.CONTROL, qos=1)\n\n    def _on_message(self, client, userdata, msg):\n        print(f\"control <- {msg.topic}: {msg.payload!r}\")\n\n    def start(self):\n        self.c.connect(self.host, self.port, keepalive=30)\n        self.c.loop_start()\n\n    def publish_telemetry(self, data: dict):\n        self.c.publish(\n            self.TELEMETRY, json.dumps(data).encode(),\n            qos=1, retain=False,\n        )\n\n    def stop(self):\n        self.c.publish(\n            self.STATUS,\n            json.dumps({\"online\": False, \"ts\": int(time.time())}).encode(),\n            qos=1, retain=True,\n        ).wait_for_publish(2)\n        self.c.loop_stop(); self.c.disconnect()\n\n\n# --- Consumer side: shared subscription for horizontal scale ---\ndef consumer():\n    c = mqtt.Client(client_id=f\"consumer-{uuid.uuid4().hex[:6]}\",\n                    callback_api_version=mqtt.CallbackAPIVersion.VERSION2)\n\n    def on_connect(client, userdata, flags, rc, properties=None):\n        # $share/<group>/<topic> = MQTT 5 shared subscription.\n        # Multiple consumers in the same group split the messages between them.\n        client.subscribe(f\"$share/telemetry/{ORG}/+/+/telemetry\", qos=1)\n        client.subscribe(f\"{ORG}/+/+/status\", qos=1)               # presence: every consumer sees\n\n    def on_message(client, userdata, msg):\n        # msg.topic example: 'acme/site-a/dev1/telemetry'\n        parts = msg.topic.split(\"/\")\n        if parts[-1] == \"telemetry\":\n            print(f\"telem from {parts[2]}: {msg.payload[:60]!r}\")\n        elif parts[-1] == \"status\":\n            print(f\"status of {parts[2]}: {msg.payload!r} retain={msg.retain}\")\n\n    c.on_connect = on_connect\n    c.on_message = on_message\n    c.connect(\"broker.example.com\", 1883, keepalive=60)\n    c.loop_forever()\n\n\n# --- Use it ---\n# d = FleetDevice(\"dev1\", \"broker.example.com\")\n# d.start()\n# while True: d.publish_telemetry({\"t\": 22.7, \"h\": 39}); time.sleep(5)\n\n# Decision rule:\n#   Want subscribers to see \"current value\" on subscribe   -> publish with retain=True.\n#   Want broker to announce a crash                         -> client.will_set(...).\n#   Want presence (online/offline) for a fleet              -> retained STATUS topic + LWT\n#                                                              (above).\n#   Multi-tenant                                             -> include org / tenant in\n#                                                              the topic prefix.\n#   Need to load-balance consumers                           -> $share/group/topic\n#                                                              (MQTT 5 shared subscriptions).\n#   Need fan-out                                              -> standard subscribe (every\n#                                                              subscriber gets every msg).\n#   Need to delete a retained message                         -> publish empty payload with\n#                                                              retain=True to that topic.\n#   Topic includes a value that changes                       -> NO; values go in the payload,\n#                                                              not the topic.\n\n# Anti-pattern:\n#   c.subscribe(\"sensors/#/temp\")          # # only allowed at the END\n# # is multi-level wildcard but ONLY at the tail of the topic. For\n# \"any device, temp metric\" use 'sensors/+/temp' (single-level wildcard).\n"
                  }
        ],
        tips: [
                  "`retain=True` makes the broker keep ONE message per topic; new subscribers receive it immediately.",
                  "`will_set(...)` MUST be called before `connect()` — the broker stores the will at CONNECT time.",
                  "Topic wildcards: `+` matches **one** level; `#` matches the rest and **only** at the tail.",
                  "Topic structure: hierarchical, slash-separated, never start with `/`. Don't encode values in topic names — use the payload.",
                  "For consumer scale-out, MQTT 5 shared subscriptions (`$share/<group>/<topic>`) round-robin between subscribers in the group."
        ],
        mistake: "Putting `#` mid-topic (`sensors/#/temp`). `#` is allowed **only** at the end. For \"any device, temp metric\" use `sensors/+/temp`.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },

  // ── Section 2: aiomqtt — async client ─────────────────────────────────────────
  {
    id: "aiomqtt",
    title: "aiomqtt — async client",
    entries: [
      {
        id: "aiomqtt-async-client",
        fn: "aiomqtt.Client — async/await around paho",
        desc: "`aiomqtt` (formerly asyncio-mqtt) wraps paho behind a clean asyncio API: `async with Client(host) as client` connects, `await client.publish(...)`, `async for message in client.messages` consumes. Best when your app is already asyncio (FastAPI, websockets, async DB drivers).",
        category: "aiomqtt",
        subtitle: "aiomqtt.Client (async context manager), await client.subscribe / publish, async for message in client.messages, client.will, identifier= for client_id, asyncio.TaskGroup for concurrent publish + consume, MqttError exceptions for reconnect handling",
        signature: "async with aiomqtt.Client(\"broker\") as c: await c.subscribe(\"sensors/#\"); async for m in c.messages: ...",
        descLong: "`aiomqtt` is the modern async MQTT client. Connection is via `async with`; subscribe with `await client.subscribe(topic, qos=)`; iterate inbound messages with `async for message in client.messages`. To publish + consume concurrently, use `asyncio.TaskGroup` (Python 3.11+) so a failure in either task cancels the other. Reconnect is on you — wrap the connect/iterate cycle in a `try/except aiomqtt.MqttError` with backoff. Three depths solve the SAME task — async telemetry pipeline that consumes commands and publishes responses — at depths: bare client + sequential consume → TaskGroup with concurrent publish + consume → reconnect-loop wrapper with TLS, will-message, and bounded backoff.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Subscribe to control/+; print every message that arrives.\n# APPROACH  - aiomqtt.Client + async for.\n# STRENGTHS - Six lines.\n# WEAKNESSES- No publish; if the broker drops, the program ends.\nimport asyncio\nimport aiomqtt\n\n\nasync def main():\n    async with aiomqtt.Client(\"broker.hivemq.com\") as client:\n        await client.subscribe(\"control/+\")\n        async for message in client.messages:\n            print(f\"{message.topic}: {message.payload!r}\")\n\n\nasyncio.run(main())\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — control consumer — but ALSO publish telemetry every 5s\n#             from a concurrent task.\n# APPROACH  - asyncio.TaskGroup with consume + publish coroutines.\n# STRENGTHS - True concurrency (consume and publish at once); structured\n#             error propagation - if either task fails, the other is\n#             cancelled.\n# WEAKNESSES- Still no reconnect; one network blip ends the program.\nimport asyncio\nimport aiomqtt\n\n\nasync def consume(client: aiomqtt.Client):\n    await client.subscribe(\"control/+\", qos=1)\n    async for message in client.messages:\n        print(f\"<- {message.topic}: {message.payload!r}\")\n\n\nasync def publish(client: aiomqtt.Client):\n    n = 0\n    while True:\n        await client.publish(\"sensors/lab1/temp\", f\"22.{n}\".encode(), qos=1)\n        n = (n + 1) % 10\n        await asyncio.sleep(5)\n\n\nasync def main():\n    async with aiomqtt.Client(\n        hostname=\"broker.hivemq.com\",\n        identifier=\"lab1-async\",\n        keepalive=30,\n    ) as client:\n        async with asyncio.TaskGroup() as tg:\n            tg.create_task(consume(client))\n            tg.create_task(publish(client))\n\n\nasyncio.run(main())\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — async telemetry + control — production: reconnect loop\n#             with exponential backoff, retained presence + LWT, TLS.\n# APPROACH  - while True wrapping connect-and-stream, catching MqttError;\n#             aiomqtt.Will for last-will; aiomqtt.TLSParameters for TLS.\n# STRENGTHS - Survives broker restarts; presence visible to subscribers;\n#             encrypted in transit.\n# WEAKNESSES- Reconnect resets in-flight QoS 1 inflight queue - design\n#             handlers idempotent.\nfrom __future__ import annotations\nimport asyncio\nimport json\nimport ssl\nimport time\nimport aiomqtt\n\n\nCLIENT_ID = \"lab1-async\"\nSTATUS    = f\"devices/{CLIENT_ID}/status\"\nTELEMETRY = f\"devices/{CLIENT_ID}/telemetry\"\nCONTROL   = f\"control/{CLIENT_ID}/+\"\n\n\nwill = aiomqtt.Will(\n    topic=STATUS,\n    payload=json.dumps({\"online\": False, \"ts\": int(time.time())}).encode(),\n    qos=1, retain=True,\n)\n\n\nasync def consume(client: aiomqtt.Client):\n    await client.subscribe(CONTROL, qos=1)\n    async for message in client.messages:\n        print(f\"control <- {message.topic}: {message.payload!r}\")\n\n\nasync def publish_loop(client: aiomqtt.Client):\n    # Mark online (retained).\n    await client.publish(\n        STATUS,\n        json.dumps({\"online\": True, \"ts\": int(time.time())}).encode(),\n        qos=1, retain=True,\n    )\n    while True:\n        payload = json.dumps({\"t\": 22.7, \"h\": 39.4, \"ts\": int(time.time())}).encode()\n        await client.publish(TELEMETRY, payload, qos=1)\n        await asyncio.sleep(5)\n\n\nasync def session():\n    tls = aiomqtt.TLSParameters(\n        ca_certs=\"/etc/ssl/certs/ca-certificates.crt\",\n        tls_version=ssl.PROTOCOL_TLSv1_2,\n    )\n    async with aiomqtt.Client(\n        hostname=\"broker.example.com\",\n        port=8883,\n        identifier=CLIENT_ID,\n        keepalive=30,\n        will=will,\n        tls_params=tls,\n        # username=\"dev\", password=\"secret\",\n    ) as client:\n        async with asyncio.TaskGroup() as tg:\n            tg.create_task(consume(client))\n            tg.create_task(publish_loop(client))\n\n\nasync def main():\n    backoff = 1\n    while True:\n        try:\n            await session()\n            backoff = 1                                # reset on success\n        except aiomqtt.MqttError as e:\n            print(f\"mqtt error: {e!r}; reconnecting in {backoff}s\")\n            await asyncio.sleep(backoff)\n            backoff = min(backoff * 2, 60)\n        except* asyncio.CancelledError:\n            raise\n\n\nasyncio.run(main())\n\n# Decision rule:\n#   App is asyncio (FastAPI / websockets / asyncpg)   -> aiomqtt.\n#   App is sync                                        -> paho directly.\n#   Need cross-thread bridging                         -> paho.loop_start in thread; queue\n#                                                         to async via run_in_executor.\n#   Need to consume + publish concurrently              -> asyncio.TaskGroup (Python 3.11+).\n#   Need reconnect resilience                           -> outer while True + MqttError catch\n#                                                         with exponential backoff (above).\n#   Need TLS                                            -> aiomqtt.TLSParameters.\n#   Need MQTT 5 properties                               -> aiomqtt 2.x supports MQTT 5\n#                                                         via paho's MQTTv5 protocol.\n#   Need shared subscriptions ($share)                   -> just subscribe to the\n#                                                         \"$share/<group>/<topic>\" string.\n\n# Anti-pattern:\n#   async for message in client.messages:\n#       await slow_db_write(message)        # blocks the iteration\n# A slow handler causes back-pressure on the broker (eventually drops you).\n# Either fan messages out to a worker pool (asyncio.Queue + worker tasks)\n# or process in a TaskGroup so one slow message doesn't block the next.\n"
                  }
        ],
        tips: [
                  "`aiomqtt.Client` is an async context manager — `async with Client(host) as c:` connects/disconnects.",
                  "Iterate inbound messages with `async for message in client.messages` — handles flow control internally.",
                  "For concurrent publish + consume, use `asyncio.TaskGroup` (Python 3.11+) — structured cancellation if either side fails.",
                  "`aiomqtt.MqttError` is the catch-all for connection failures — wrap your session in a `while True` with backoff.",
                  "For shared subscriptions, just subscribe to `\"$share/<group>/<topic>\"` — aiomqtt passes the string straight through."
        ],
        mistake: "Doing slow synchronous work inside `async for message in client.messages` — blocks the iteration, causes broker back-pressure, eventual disconnect. Fan messages to a worker pool via `asyncio.Queue`.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },

  // ── Section 3: When to reach for which messaging tool ─────────────────────────────────────────
  {
    id: "patterns",
    title: "When to reach for which messaging tool",
    entries: [
      {
        id: "mqtt-vs-kafka-amqp",
        fn: "MQTT vs Kafka vs AMQP vs HTTP webhooks — pick the protocol",
        desc: "MQTT is the right protocol for **fleet IoT**: small clients, lossy networks, presence semantics, low overhead. Kafka for **high-volume event streams** with replay. AMQP (RabbitMQ) for **enterprise message routing**. HTTP webhooks for **simple cloud-to-cloud** notifications. Pick by who the producers are, what guarantees you need, and how big the messages are.",
        category: "patterns",
        subtitle: "MQTT (small payloads, mass clients, retained, LWT, QoS 0/1/2) vs Kafka (high-throughput, partitioned log, replay, larger ops cost) vs AMQP/RabbitMQ (rich routing: exchanges + queues + bindings) vs HTTP webhooks (simple, request-response, no broker), MQTT broker choices: Mosquitto (simple), EMQX (cluster + ACL), HiveMQ (commercial), AWS IoT Core (managed), Azure IoT Hub",
        signature: "# MQTT: paho/aiomqtt -> broker (Mosquitto / EMQX)\\n# Kafka: confluent-kafka -> Kafka cluster\\n# AMQP: aio-pika -> RabbitMQ\\n# HTTP: httpx.post(webhook_url, json=...)",
        descLong: "Four protocols with different sweet spots. **MQTT**: small messages (~bytes-KB), lots of clients (millions), unreliable networks, presence semantics (LWT + retained), low broker overhead — built for IoT. **Kafka**: huge throughput (millions msg/s), partitioned log with replay, larger ops footprint — built for event streaming. **AMQP / RabbitMQ**: complex routing (exchanges + queues + bindings), per-message routing keys, dead-letter queues — enterprise messaging. **HTTP webhooks**: cloud-to-cloud, simplest possible, no broker at all — but no presence, no fan-out without an explicit registry. Three depths solve the SAME task — collect telemetry from 10k devices and stream into analytics — at depths: pure MQTT (right for the device side) → MQTT → Kafka bridge (right for the analytics side) → MQTT (devices) + Kafka (analytics) + cloud IoT broker (managed presence + auth + bridging).",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - 10k IoT devices send telemetry; you read it.\n# APPROACH  - MQTT only.\n# STRENGTHS - Right protocol for the device side; one Mosquitto handles\n#             10k clients fine on a single VM.\n# WEAKNESSES- No replay; consumers must be online to receive (unless\n#             persistent session); analytics joins / time-windows are\n#             not MQTT's job.\n# devices: paho.mqtt.client.Client.publish(...)\n# consumer:\nimport paho.mqtt.client as mqtt\n\ndef on_message(client, userdata, msg):\n    print(f\"{msg.topic}: {msg.payload!r}\")\n\nc = mqtt.Client(\"analytics-consumer\",\n                callback_api_version=mqtt.CallbackAPIVersion.VERSION2)\nc.on_message = on_message\nc.connect(\"broker.example.com\", 1883)\nc.subscribe(\"acme/+/+/telemetry\")\nc.loop_forever()\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — 10k devices -> analytics — but bridge MQTT into\n#             Kafka so analytics get replay + windowed aggregations.\n# APPROACH  - paho subscribes; producer pushes to Kafka.\n# STRENGTHS - Devices stay on lightweight MQTT; analytics get Kafka's\n#             replay, partitioning, and late-arriving handling.\n# WEAKNESSES- Two systems to operate; bridge is a SPOF unless redundant.\nimport paho.mqtt.client as mqtt\nfrom confluent_kafka import Producer\nimport json\n\nproducer = Producer({\"bootstrap.servers\": \"kafka:9092\",\n                     \"client.id\":         \"mqtt-bridge\"})\n\n\ndef on_message(client, userdata, msg):\n    # MQTT topic: acme/site-a/dev1/telemetry\n    parts = msg.topic.split(\"/\")\n    org, site, dev = parts[0], parts[1], parts[2]\n    producer.produce(\n        topic=f\"telemetry.{org}\",                      # Kafka topic\n        key=dev.encode(),                              # partition key\n        value=msg.payload,                              # bytes through\n        headers=[(\"mqtt.topic\", msg.topic.encode()),\n                 (\"site\", site.encode())],\n    )\n    producer.poll(0)\n\n\nc = mqtt.Client(\"mqtt-kafka-bridge\",\n                callback_api_version=mqtt.CallbackAPIVersion.VERSION2)\nc.on_message = on_message\nc.connect(\"broker.example.com\", 1883, keepalive=30)\nc.subscribe(\"acme/+/+/telemetry\", qos=1)\nc.loop_forever()\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — fleet IoT to analytics — production: managed broker\n#             (AWS IoT Core), MQTT 5 with shared subs, Kafka downstream.\n# APPROACH  - Devices auth with X.509 to AWS IoT Core; rule engine bridges\n#             MQTT topics to Kinesis / MSK; consumers read Kafka.\n# STRENGTHS - Managed scale (millions of devices), per-device cert auth,\n#             zero broker ops; Kafka downstream has full replay/windowing.\n# WEAKNESSES- Vendor lock; cost scales with messages.\n\"\"\"\n# 1) Device side - paho with mutual TLS:\nimport paho.mqtt.client as mqtt, ssl, json, time\n\nc = mqtt.Client(client_id=\"dev-001\",\n                callback_api_version=mqtt.CallbackAPIVersion.VERSION2,\n                protocol=mqtt.MQTTv5)\nc.tls_set(\n    ca_certs=\"AmazonRootCA1.pem\",\n    certfile=\"dev-001.cert.pem\",\n    keyfile=\"dev-001.private.key\",\n    tls_version=ssl.PROTOCOL_TLSv1_2,\n)\nc.connect(\"a3xxx-ats.iot.us-east-1.amazonaws.com\", 8883, keepalive=30)\nc.loop_start()\n\nwhile True:\n    c.publish(\"acme/site-a/dev-001/telemetry\",\n              json.dumps({\"t\": 22.7}).encode(), qos=1)\n    time.sleep(5)\n\"\"\"\n\n# 2) AWS IoT Rule (Kinesis sink, defined in CDK / Terraform):\n#    SQL: SELECT *, topic() AS mqtt_topic FROM 'acme/+/+/telemetry'\n#    Action: kinesis put-record into stream \"iot-telemetry\"\n#\n# 3) Analytics consumer reads from Kinesis (or MSK / Kafka):\nfrom confluent_kafka import Consumer\n\nc = Consumer({\n    \"bootstrap.servers\": \"kafka:9092\",\n    \"group.id\":          \"telemetry-aggregator\",\n    \"auto.offset.reset\": \"earliest\",\n})\nc.subscribe([\"telemetry.acme\"])\nwhile True:\n    msg = c.poll(1.0)\n    if msg is None or msg.error():\n        continue\n    print(msg.topic(), msg.key(), msg.value()[:80])\n\n# Decision rule:\n#   Mass IoT clients, small messages, lossy net          -> MQTT.\n#   High-volume analytics, replay, partitioned log        -> Kafka.\n#   Enterprise routing (exchanges, dead letters)          -> AMQP / RabbitMQ.\n#   Cloud-to-cloud notification, no broker                 -> HTTP webhook.\n#   Need both device fleet + analytics replay              -> MQTT -> Kafka bridge.\n#   Need managed everything                                 -> AWS IoT Core / Azure IoT Hub /\n#                                                              Google Cloud IoT (sunset 2023 -\n#                                                              switch to Pub/Sub).\n#   Need on-premise + open-source                          -> Mosquitto (simple) or\n#                                                              EMQX (clustered, ACL, bridges).\n#   Need durable IoT queue                                  -> EMQX / VerneMQ persistent sessions.\n\n# Anti-pattern:\n#   \"Use MQTT for our analytics pipeline because it's fast\"\n# MQTT brokers are not partitioned logs - no replay, no compaction, weak\n# ordering across consumers. Use MQTT at the edge, Kafka at the core.\n"
                  }
        ],
        tips: [
                  "**MQTT**: tiny payloads, millions of clients, presence semantics, lossy networks — built for IoT.",
                  "**Kafka**: high-throughput partitioned log with replay — analytics, event sourcing.",
                  "**AMQP / RabbitMQ**: rich routing (exchanges + bindings + DLQ) — enterprise messaging.",
                  "**HTTP webhooks**: cloud-to-cloud, no broker, simplest — but no presence, no fan-out without registry.",
                  "Common production pattern: MQTT at the edge, bridged to Kafka or a managed cloud bus for analytics."
        ],
        mistake: "Trying to use MQTT as an analytics backplane (no replay, no partitioned ordering) or Kafka on tiny IoT devices (clients are heavy, broker ops cost is high). Use MQTT at the edge, Kafka at the core.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },
]

export default { meta, sections }

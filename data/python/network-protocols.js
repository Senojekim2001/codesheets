export const meta = {
  "id": "network-protocols",
  "label": "Network Protocols",
  "icon": "🌐",
  "description": "Network protocols in Python: stdlib `socket` for TCP/UDP/IPv6, `asyncio` streams for async TCP, `websockets` for WS, `grpcio` for gRPC, `scapy` for packet capture/crafting, `ipaddress` for safe address arithmetic. Four things you keep getting wrong: TCP `recv()` is not message-framed (you have to delimit yourself), `SO_REUSEADDR` vs `SO_REUSEPORT` (different things), forgetting to set `Nodelay`/buffer sizes for low-latency apps, and assuming UTF-8 inside binary protocol bytes."
}

export const sections = [

  // ── Section 1: Sockets — TCP, UDP, asyncio streams ─────────────────────────────────────────
  {
    id: "sockets",
    title: "Sockets — TCP, UDP, asyncio streams",
    entries: [
      {
        id: "sockets-tcp-server",
        fn: "socket / asyncio.start_server — TCP servers",
        desc: "For one client at a time, a `socket.socket(AF_INET, SOCK_STREAM)` + `bind/listen/accept` loop is fine. For real concurrency use `asyncio.start_server(handler, host, port)` — handler receives a `(reader, writer)` pair and you await reads/writes. **TCP is a byte stream, not a message stream** — frame your messages with length prefixes or delimiters.",
        category: "sockets",
        subtitle: "socket.socket (AF_INET / AF_INET6, SOCK_STREAM = TCP, SOCK_DGRAM = UDP), bind / listen / accept loop, SO_REUSEADDR (allow re-bind right after kill) vs SO_REUSEPORT (multiple processes share the port; OS load-balances), TCP_NODELAY (disable Nagle), asyncio.start_server (handler(reader, writer)), readuntil / readexactly, message framing (4-byte big-endian length prefix, or newline-delimited JSON)",
        signature: "srv = socket.socket(AF_INET, SOCK_STREAM); srv.setsockopt(SOL_SOCKET, SO_REUSEADDR, 1); srv.bind((host, port)); srv.listen(); conn, addr = srv.accept(); asyncio.start_server(handle, host, port)",
        descLong: "Two paths. **Sync sockets**: `accept()` returns a connected socket; you `recv(n)` and `send(bytes)`. Each `recv` returns up to N bytes, possibly a partial message — read until you have the full frame. **Asyncio streams**: `asyncio.start_server(handle, host, port)` spawns one coroutine per connection; `await reader.readuntil(b\"\\n\")` and `writer.write(...)` + `await writer.drain()`. Always frame your messages — the canonical pattern is a 4-byte big-endian length prefix followed by the payload. Three depths solve the SAME task — line-based echo server — at depths: blocking single-client socket → asyncio echo server, one task per connection → asyncio length-prefixed binary protocol with graceful shutdown and request/response framing.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Echo server: accept one connection, read lines, echo them back.\n# APPROACH  - Single-threaded blocking socket.\n# STRENGTHS - Demonstrates the lifecycle.\n# WEAKNESSES- One client at a time; recv() may return partial reads.\nimport socket\n\nHOST, PORT = \"0.0.0.0\", 9001\n\nwith socket.socket(socket.AF_INET, socket.SOCK_STREAM) as srv:\n    # SO_REUSEADDR lets you re-bind right after killing the server in dev.\n    srv.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)\n    srv.bind((HOST, PORT))\n    srv.listen(5)\n    print(f\"listening on {HOST}:{PORT}\")\n\n    conn, addr = srv.accept()\n    with conn:\n        print(f\"connected: {addr}\")\n        buf = b\"\"\n        while True:\n            chunk = conn.recv(4096)\n            if not chunk:\n                break                                  # client closed\n            buf += chunk\n            while b\"\\n\" in buf:\n                line, buf = buf.split(b\"\\n\", 1)\n                conn.sendall(line + b\"\\n\")\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — echo server — but multi-client via asyncio streams.\n# APPROACH  - asyncio.start_server with one coroutine per connection;\n#             await reader.readline() handles framing.\n# STRENGTHS - Thousands of concurrent clients on one thread.\n# WEAKNESSES- readline() is line-delimited; binary protocols need\n#             different framing.\nimport asyncio\n\n\nasync def handle(reader: asyncio.StreamReader, writer: asyncio.StreamWriter):\n    addr = writer.get_extra_info(\"peername\")\n    print(f\"+ {addr}\")\n    try:\n        while not reader.at_eof():\n            try:\n                line = await reader.readline()\n            except asyncio.IncompleteReadError:\n                break\n            if not line:\n                break\n            writer.write(line)\n            await writer.drain()                       # flush; back-pressure aware\n    finally:\n        print(f\"- {addr}\")\n        writer.close()\n        await writer.wait_closed()\n\n\nasync def main():\n    server = await asyncio.start_server(handle, \"0.0.0.0\", 9001)\n    addrs = \", \".join(str(s.getsockname()) for s in server.sockets)\n    print(f\"listening on {addrs}\")\n    async with server:\n        await server.serve_forever()\n\n\nasyncio.run(main())\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — TCP server — production: 4-byte length-prefixed binary\n#             protocol, graceful shutdown on SIGTERM, structured logging,\n#             SO_REUSEPORT so multiple processes can share the port.\n# APPROACH  - readexactly(4) + readexactly(n); writer.drain() back-pressure;\n#             signal handlers cancel serve_forever.\n# STRENGTHS - Real binary protocol; multi-process scale; clean shutdown.\n# WEAKNESSES- More machinery; binary framing has zero forgiveness for\n#             desynchronization (always close on partial read).\nfrom __future__ import annotations\nimport asyncio\nimport socket\nimport signal\nimport struct\nimport logging\n\n\nlogging.basicConfig(level=logging.INFO,\n                    format=\"%(asctime)s %(levelname)s %(message)s\")\nlog = logging.getLogger(\"net\")\n\n\nasync def read_frame(reader: asyncio.StreamReader) -> bytes | None:\n    \"\"\"Read a length-prefixed frame: <uint32 big-endian length><payload>.\"\"\"\n    try:\n        header = await reader.readexactly(4)\n    except asyncio.IncompleteReadError:\n        return None\n    (n,) = struct.unpack(\">I\", header)\n    if n > 16 * 1024 * 1024:                          # 16 MB sanity cap\n        raise ValueError(f\"frame too large: {n}\")\n    return await reader.readexactly(n)\n\n\ndef write_frame(writer: asyncio.StreamWriter, payload: bytes) -> None:\n    writer.write(struct.pack(\">I\", len(payload)) + payload)\n\n\nasync def handle(reader, writer):\n    peer = writer.get_extra_info(\"peername\")\n    log.info(\"connect %s\", peer)\n    try:\n        while True:\n            frame = await read_frame(reader)\n            if frame is None:\n                break\n            write_frame(writer, b\"echo:\" + frame)\n            await writer.drain()\n    except (ValueError, asyncio.IncompleteReadError) as e:\n        log.warning(\"framing error from %s: %s\", peer, e)\n    finally:\n        writer.close()\n        await writer.wait_closed()\n        log.info(\"disconnect %s\", peer)\n\n\ndef make_reuseport_socket(host: str, port: int) -> socket.socket:\n    \"\"\"SO_REUSEPORT lets multiple processes share the same listening port.\"\"\"\n    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n    s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)\n    if hasattr(socket, \"SO_REUSEPORT\"):\n        s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEPORT, 1)\n    s.bind((host, port))\n    s.listen(128)\n    s.setblocking(False)\n    return s\n\n\nasync def main():\n    sock = make_reuseport_socket(\"0.0.0.0\", 9001)\n    server = await asyncio.start_server(handle, sock=sock)\n\n    loop = asyncio.get_running_loop()\n    stop = asyncio.Event()\n    for sig in (signal.SIGTERM, signal.SIGINT):\n        loop.add_signal_handler(sig, stop.set)\n\n    log.info(\"listening on %s\", sock.getsockname())\n    try:\n        async with server:\n            serve_task = asyncio.create_task(server.serve_forever())\n            await stop.wait()                         # wait for SIGTERM/SIGINT\n            log.info(\"shutting down\")\n            server.close()\n            await server.wait_closed()\n            serve_task.cancel()\n    except asyncio.CancelledError:\n        pass\n\n\nif __name__ == \"__main__\":\n    asyncio.run(main())\n\n# Decision rule:\n#   One-client tool                              -> blocking socket.\n#   Many concurrent clients                       -> asyncio.start_server (above).\n#   Need horizontal scale on one host             -> SO_REUSEPORT + N processes.\n#   Line-delimited text                           -> reader.readline / readuntil(b\"\\n\").\n#   Binary protocol                               -> length-prefixed (uint32 big-endian).\n#   Need backpressure                              -> writer.drain() after every write.\n#   Latency-sensitive (game / trading)             -> setsockopt(IPPROTO_TCP, TCP_NODELAY, 1).\n#   Need TLS                                       -> ssl.SSLContext + start_server(ssl=ctx).\n#   IPv6                                           -> AF_INET6 + bind to \"::\"; dual-stack via\n#                                                     IPV6_V6ONLY=0.\n\n# Anti-pattern:\n#   data = reader.read(4096)\n#   message = data.decode(\"utf-8\")\n# read returns up to 4096 bytes - might be 200, might be split mid-UTF-8\n# character. Always frame messages and only decode complete ones.\n"
                  }
        ],
        tips: [
                  "TCP is a byte stream, not a message stream — frame messages with a length prefix (uint32 BE) or delimiter.",
                  "`SO_REUSEADDR` allows re-binding right after a kill; `SO_REUSEPORT` lets multiple processes share the port (OS load-balances).",
                  "For low-latency apps, set `TCP_NODELAY=1` to disable Nagle's algorithm — otherwise small writes get coalesced for ~40 ms.",
                  "Always `await writer.drain()` after `writer.write()` — that's how asyncio applies back-pressure.",
                  "For TLS, build an `ssl.SSLContext` and pass it to `asyncio.start_server(ssl=ctx)`."
        ],
        mistake: "Calling `socket.recv(n)` and assuming you got a full message. `recv` returns up to N bytes — the application protocol must frame messages explicitly (length prefix or delimiter).",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
      {
        id: "sockets-udp-multicast",
        fn: "UDP / multicast / asyncio.DatagramTransport",
        desc: "UDP is fire-and-forget datagrams: no connection, no reliability, no ordering. Ideal for service discovery (mDNS), real-time media (video, voice, games), and metrics fan-out (StatsD, multicast SNMP). Use `socket.SOCK_DGRAM`; for asyncio, `asyncio.DatagramProtocol` + `loop.create_datagram_endpoint`.",
        category: "sockets",
        subtitle: "socket.SOCK_DGRAM (UDP), no listen/accept; sendto / recvfrom, multicast (IP_ADD_MEMBERSHIP, MULTICAST_TTL), broadcast (SO_BROADCAST), asyncio DatagramProtocol (datagram_received, error_received), no built-in ordering or reliability, MTU ~1500 (don't exceed unless you accept fragmentation), use cases (mDNS, games, video, StatsD)",
        signature: "sock = socket.socket(AF_INET, SOCK_DGRAM); sock.bind((host, port)); data, addr = sock.recvfrom(65536); sock.sendto(payload, (host, port))",
        descLong: "No accept loop — bind a UDP socket and `recvfrom(buffer_size)` returns `(data, sender_addr)`. To send, `sendto(payload, (host, port))`. **Multicast**: pick an address in `224.0.0.0/4`, set `IP_MULTICAST_TTL`, optionally `IP_ADD_MEMBERSHIP` to receive. **Broadcast**: set `SO_BROADCAST` and send to `255.255.255.255`. Three depths solve the SAME task — discover devices on a LAN — at depths: send broadcast → receive replies (sync) → asyncio multicast subscriber + responder → mDNS-style hierarchy with retransmission timing and TTL-aware caching.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Discover devices: broadcast a \"WHO?\" packet, collect replies.\n# APPROACH  - SO_BROADCAST + recvfrom with a timeout.\n# STRENGTHS - Demonstrates fire-and-forget UDP.\n# WEAKNESSES- Broadcasts spam every host on the LAN; multicast (junior tier)\n#             is more polite.\nimport socket\n\nPORT = 9999\n\ns = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)\ns.setsockopt(socket.SOL_SOCKET, socket.SO_BROADCAST, 1)\ns.settimeout(2.0)\n\ns.sendto(b\"WHO?\", (\"255.255.255.255\", PORT))           # LAN-wide broadcast\n\n# Collect replies for ~2 seconds.\nprint(\"replies:\")\nwhile True:\n    try:\n        data, addr = s.recvfrom(4096)\n    except socket.timeout:\n        break\n    print(f\"  {addr}: {data!r}\")\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — LAN device discovery — but on a multicast group\n#             (only members receive, no LAN-wide spam).\n# APPROACH  - 224.0.0.251 + IP_ADD_MEMBERSHIP + IP_MULTICAST_TTL.\n# STRENGTHS - Polite to non-members; routers can drop multicast at the\n#             subnet boundary if you want.\n# WEAKNESSES- Some hostile networks (cafe wifi) block multicast entirely.\nimport socket\nimport struct\n\nGROUP = \"224.0.0.251\"                                  # link-local multicast\nPORT  = 9999\n\n\ndef make_multicast_listener(group: str, port: int) -> socket.socket:\n    s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM, socket.IPPROTO_UDP)\n    s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)\n    if hasattr(socket, \"SO_REUSEPORT\"):\n        s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEPORT, 1)\n    s.bind((\"\", port))                                # wildcard - receive on all NICs\n    # Join the group on the default interface (INADDR_ANY).\n    mreq = struct.pack(\"4s4s\", socket.inet_aton(group), socket.inet_aton(\"0.0.0.0\"))\n    s.setsockopt(socket.IPPROTO_IP, socket.IP_ADD_MEMBERSHIP, mreq)\n    return s\n\n\ndef make_multicast_sender() -> socket.socket:\n    s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM, socket.IPPROTO_UDP)\n    s.setsockopt(socket.IPPROTO_IP, socket.IP_MULTICAST_TTL, 1)   # link-local only\n    return s\n\n\n# Sender side - announce.\nsender = make_multicast_sender()\nsender.sendto(b\"WHO?\", (GROUP, PORT))\n\n# Receiver side - listen for replies.\nlistener = make_multicast_listener(GROUP, PORT)\nlistener.settimeout(2.0)\nwhile True:\n    try:\n        data, addr = listener.recvfrom(65536)\n        print(f\"{addr}: {data!r}\")\n    except socket.timeout:\n        break\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — LAN device discovery — production: asyncio\n#             DatagramProtocol that responds to discovery queries AND\n#             listens for replies; cache discovered peers with TTL.\n# APPROACH  - loop.create_datagram_endpoint with a Protocol class;\n#             schedule periodic re-queries; per-peer TTL eviction.\n# STRENGTHS - Single asyncio loop handles both directions; passes through\n#             retransmission deadlines; works on any thread/event-loop app.\n# WEAKNESSES- Multicast routing is fragile across complex networks - test\n#             through your actual environment before relying on it.\nfrom __future__ import annotations\nimport asyncio\nimport socket\nimport struct\nimport time\nimport json\n\n\nGROUP = \"224.0.0.251\"\nPORT  = 9999\nTTL_S = 30\nANNOUNCE_INTERVAL_S = 5\n\n\ndef setup_multicast_socket(group: str, port: int) -> socket.socket:\n    s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)\n    s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)\n    if hasattr(socket, \"SO_REUSEPORT\"):\n        s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEPORT, 1)\n    s.bind((\"\", port))\n    mreq = struct.pack(\"4s4s\", socket.inet_aton(group), socket.inet_aton(\"0.0.0.0\"))\n    s.setsockopt(socket.IPPROTO_IP, socket.IP_ADD_MEMBERSHIP, mreq)\n    s.setsockopt(socket.IPPROTO_IP, socket.IP_MULTICAST_TTL, 1)\n    return s\n\n\nclass Discovery(asyncio.DatagramProtocol):\n    def __init__(self, my_id: str):\n        self.my_id   = my_id\n        self.peers: dict[str, tuple[float, str]] = {}    # peer_id -> (last_seen, addr)\n        self.transport: asyncio.DatagramTransport | None = None\n\n    def connection_made(self, transport):\n        self.transport = transport\n\n    def datagram_received(self, data: bytes, addr: tuple[str, int]):\n        try:\n            msg = json.loads(data)\n        except Exception:\n            return\n        kind = msg.get(\"kind\")\n        peer = msg.get(\"id\")\n        if not peer or peer == self.my_id:\n            return\n        if kind == \"WHO\":\n            self.transport.sendto(\n                json.dumps({\"kind\": \"I-AM\", \"id\": self.my_id}).encode(),\n                addr,\n            )\n        if kind in (\"WHO\", \"I-AM\"):\n            self.peers[peer] = (time.time(), f\"{addr[0]}:{addr[1]}\")\n\n    def error_received(self, exc):\n        print(\"err:\", exc)\n\n    def announce(self):\n        self.transport.sendto(\n            json.dumps({\"kind\": \"WHO\", \"id\": self.my_id}).encode(),\n            (GROUP, PORT),\n        )\n\n    def evict_old(self):\n        cutoff = time.time() - TTL_S\n        self.peers = {p: (t, a) for p, (t, a) in self.peers.items() if t >= cutoff}\n\n\nasync def main(my_id: str):\n    sock = setup_multicast_socket(GROUP, PORT)\n    loop = asyncio.get_running_loop()\n\n    transport, proto = await loop.create_datagram_endpoint(\n        lambda: Discovery(my_id), sock=sock,\n    )\n\n    try:\n        while True:\n            proto.announce()\n            await asyncio.sleep(ANNOUNCE_INTERVAL_S)\n            proto.evict_old()\n            print(f\"peers: {dict(proto.peers)}\")\n    finally:\n        transport.close()\n\n\nasyncio.run(main(my_id=\"lab1-mac\"))\n\n# Decision rule:\n#   Service discovery on LAN                        -> multicast (224.0.0.0/4) + JSON\n#                                                       (or reuse mDNS via zeroconf lib).\n#   Real-time media (video, voice, games)            -> raw UDP + your own\n#                                                       sequence numbers.\n#   Telemetry fan-out                                -> StatsD (UDP) for fire-and-forget;\n#                                                       OTLP (TCP) when you need delivery.\n#   Need reliability over UDP                        -> QUIC (HTTP/3) or aioquic; or rely on\n#                                                       app-level ack/retransmit.\n#   Need very large UDP messages                     -> reduce to <1500 bytes (MTU) or\n#                                                       fragment yourself.\n#   Need to receive on a specific NIC                -> bind to that NIC's IP; mreq with\n#                                                       interface address.\n#   Need to test multicast in CI                     -> docker network host-mode + lo\n#                                                       multicast often broken; use a TCP\n#                                                       loopback fallback for tests.\n\n# Anti-pattern:\n#   sock.sendto(huge_blob, addr)            # > MTU\n# IP layer fragments. ANY dropped fragment loses the whole datagram. Keep\n# UDP messages under 1450 bytes for reliable cross-network delivery.\n"
                  }
        ],
        tips: [
                  "UDP has no ordering, no reliability, no flow control — every guarantee is your job.",
                  "Multicast addresses live in `224.0.0.0/4`; `224.0.0.0/24` is link-local (TTL 1, never routed).",
                  "Set `IP_MULTICAST_TTL=1` for link-local only; bigger TTL = more network hops.",
                  "Keep UDP payloads under ~1450 bytes (LAN MTU minus headers) to avoid IP fragmentation.",
                  "For service discovery, prefer the **zeroconf** library (mDNS / Bonjour) over rolling your own — it handles edge cases."
        ],
        mistake: "Sending a UDP datagram larger than the path MTU (~1500 bytes). The IP layer fragments; any dropped fragment loses the whole datagram. Keep payloads ≤ 1450 bytes.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },

  // ── Section 2: WebSockets and gRPC ─────────────────────────────────────────
  {
    id: "protocols",
    title: "WebSockets and gRPC",
    entries: [
      {
        id: "websockets-server-client",
        fn: "websockets — async WS server + client",
        desc: "`websockets` is the async-only Python WebSocket library. `websockets.serve(handler, host, port)` for the server side; `websockets.connect(url)` for the client. Each handler receives a `WebSocketServerProtocol`; `async for message in ws` consumes incoming messages. Built on asyncio; no Flask/Django integration needed.",
        category: "protocols",
        subtitle: "websockets.serve(handler, host, port) (handler is async fn taking the connection), websockets.connect(url) for clients, async for message in ws (text or bytes), ws.send / ws.recv, ConnectionClosed exception, ping_interval / ping_timeout (keepalive), max_size, websockets.broadcast for fan-out",
        signature: "async def handle(ws): async for msg in ws: await ws.send(msg.upper())\\nasync with websockets.serve(handle, \"0.0.0.0\", 8765) as srv: await asyncio.Future()",
        descLong: "**Server**: `websockets.serve(handler, host, port)` returns a context manager; the handler runs once per connection and lives until the client disconnects. **Client**: `websockets.connect(url)` is also an async context manager. Messages are `str` or `bytes`; the library frames them for you (no length-prefix needed). Use `websockets.broadcast(connections, message)` for one-to-many fan-out (chat rooms, live dashboards). Three depths solve the SAME task — uppercase echo server — at depths: minimal echo → broadcast chat (track all connections, fan out) → authenticated chat with per-client subscriptions, structured JSON messages, graceful shutdown.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - WebSocket echo server: receive a message, send it back uppercased.\n# APPROACH  - websockets.serve with an async handler.\n# STRENGTHS - 10 lines.\n# WEAKNESSES- Single client logic; no broadcast; no auth.\nimport asyncio\nimport websockets\n\n\nasync def echo(websocket):\n    async for message in websocket:\n        await websocket.send(message.upper())\n\n\nasync def main():\n    async with websockets.serve(echo, \"0.0.0.0\", 8765):\n        print(\"ws://localhost:8765\")\n        await asyncio.Future()                         # run forever\n\n\nasyncio.run(main())\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — but a chat room: every client's message goes to all\n#             other clients.\n# APPROACH  - Track connections in a set; websockets.broadcast for fan-out.\n# STRENGTHS - One-to-many in two lines; back-pressure handled per client.\n# WEAKNESSES- No auth; no message history for late-joiners; no rooms.\nimport asyncio\nimport websockets\n\n\nCLIENTS: set[websockets.WebSocketServerProtocol] = set()\n\n\nasync def chat(ws):\n    CLIENTS.add(ws)\n    try:\n        await ws.send(f\"welcome - {len(CLIENTS)} online\")\n        async for message in ws:\n            # broadcast skips closed connections automatically.\n            websockets.broadcast(CLIENTS, f\"<{ws.remote_address[0]}> {message}\")\n    finally:\n        CLIENTS.discard(ws)\n\n\nasync def main():\n    async with websockets.serve(chat, \"0.0.0.0\", 8765,\n                                ping_interval=20, ping_timeout=10):\n        print(\"chat on ws://localhost:8765\")\n        await asyncio.Future()\n\n\nasyncio.run(main())\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — chat — production: rooms via subscribe message,\n#             per-client auth via token query param, structured JSON\n#             messages, graceful shutdown.\n# APPROACH  - dict[room, set[ws]]; first message must be {auth: ...};\n#             SIGTERM closes all connections cleanly.\n# STRENGTHS - Real chat semantics; auth at handshake; clean shutdown.\n# WEAKNESSES- In-process state -> needs Redis / NATS to scale to N processes.\nfrom __future__ import annotations\nimport asyncio\nimport json\nimport signal\nimport websockets\nfrom collections import defaultdict\n\n\nVALID_TOKENS = {\"alice\": \"tok_a\", \"bob\": \"tok_b\"}     # demo only - real auth is more\nROOMS: dict[str, set[websockets.WebSocketServerProtocol]] = defaultdict(set)\n\n\nasync def authenticate(ws) -> str | None:\n    \"\"\"Expect first message {kind: 'auth', user, token}. Return user or None.\"\"\"\n    try:\n        first = await asyncio.wait_for(ws.recv(), timeout=5)\n        msg = json.loads(first)\n    except (asyncio.TimeoutError, json.JSONDecodeError):\n        return None\n    if msg.get(\"kind\") != \"auth\": return None\n    user = msg.get(\"user\"); token = msg.get(\"token\")\n    if VALID_TOKENS.get(user) != token: return None\n    return user\n\n\nasync def handler(ws):\n    user = await authenticate(ws)\n    if not user:\n        await ws.close(code=4001, reason=\"auth required\")\n        return\n\n    user_rooms: set[str] = set()\n\n    async def join(room: str):\n        if room not in user_rooms:\n            user_rooms.add(room)\n            ROOMS[room].add(ws)\n            websockets.broadcast(ROOMS[room],\n                                 json.dumps({\"kind\": \"joined\", \"user\": user, \"room\": room}))\n\n    async def leave(room: str):\n        if room in user_rooms:\n            user_rooms.remove(room)\n            ROOMS[room].discard(ws)\n\n    try:\n        await ws.send(json.dumps({\"kind\": \"auth_ok\", \"user\": user}))\n        async for raw in ws:\n            try:\n                msg = json.loads(raw)\n            except json.JSONDecodeError:\n                continue\n            kind = msg.get(\"kind\")\n            if kind == \"join\":\n                await join(msg[\"room\"])\n            elif kind == \"leave\":\n                await leave(msg[\"room\"])\n            elif kind == \"say\":\n                room = msg.get(\"room\")\n                if room in user_rooms:\n                    websockets.broadcast(\n                        ROOMS[room],\n                        json.dumps({\"kind\": \"say\", \"user\": user,\n                                    \"room\": room, \"text\": msg.get(\"text\", \"\")})\n                    )\n    finally:\n        for r in list(user_rooms):\n            await leave(r)\n\n\nasync def main():\n    stop = asyncio.Event()\n    loop = asyncio.get_running_loop()\n    for sig in (signal.SIGTERM, signal.SIGINT):\n        loop.add_signal_handler(sig, stop.set)\n\n    async with websockets.serve(\n        handler, \"0.0.0.0\", 8765,\n        ping_interval=20, ping_timeout=10, max_size=64 * 1024,\n    ):\n        print(\"chat on ws://localhost:8765\")\n        await stop.wait()\n        print(\"draining...\")\n        await asyncio.gather(*[ws.close(code=1001, reason=\"server shutdown\")\n                               for room in ROOMS.values() for ws in room],\n                              return_exceptions=True)\n\n\nasyncio.run(main())\n\n# Decision rule:\n#   Asyncio app, native websockets             -> websockets library.\n#   Sync app, want to add WS                   -> use websockets.sync.client / .server\n#                                                  (built-in sync wrappers since v12).\n#   FastAPI / Starlette app                     -> Starlette's @app.websocket(\"/ws\") wraps\n#                                                  websockets internally.\n#   Need scale across processes / nodes         -> Redis pub/sub or NATS for cross-node fan-out.\n#   Need binary frames                           -> websockets handles bytes natively;\n#                                                  send/recv accepts both str + bytes.\n#   Need keepalive over flaky networks           -> ping_interval=20 + ping_timeout=10.\n#   Browser client                               -> standard JS WebSocket() in the browser;\n#                                                  no Python on the client side.\n#   Need lower-level WS                          -> wsproto for protocol-only without IO.\n\n# Anti-pattern:\n#   while True: msg = await ws.recv()\n#                 await slow_db_write(msg)\n# Sequential await blocks the next recv. For high-throughput per-connection\n# work, fan messages out to a worker pool via asyncio.Queue.\n"
                  }
        ],
        tips: [
                  "`websockets.serve(handler, host, port)` is async-only — `handler(ws)` runs once per connection and lives until close.",
                  "`websockets.broadcast(connections, message)` is the right primitive for fan-out — handles back-pressure and closed connections.",
                  "Keep alives via `ping_interval=20, ping_timeout=10` — drops dead connections quickly across NATs/proxies.",
                  "Cap message size with `max_size=` — default 1 MB; lower it for hostile clients.",
                  "For multi-process scale, fan messages through Redis pub/sub or NATS — process-local state doesn't share."
        ],
        mistake: "Doing slow synchronous-looking work after `await ws.recv()` — blocks the next `recv` and the connection stalls. Fan messages to a worker pool via `asyncio.Queue`.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
      {
        id: "grpc-python",
        fn: "grpcio + protobuf — typed RPC services",
        desc: "gRPC = HTTP/2 + Protocol Buffers + a code generator. Define a service in `.proto`, run `python -m grpc_tools.protoc` to generate stubs, implement the server, call from clients. Streams (server-streaming, client-streaming, bidi) come for free. Use when you have known service boundaries and care about typed schemas; skip it for public-internet APIs (REST is friendlier).",
        category: "protocols",
        subtitle: "protobuf .proto schema (message + service definitions), python -m grpc_tools.protoc generates *_pb2.py / *_pb2_grpc.py, grpc.server (sync) or grpc.aio.server (async), insecure_channel vs secure_channel + ssl_channel_credentials, streaming RPCs (4 kinds), grpc.aio for asyncio integration, status codes via context.set_code, deadlines via metadata",
        signature: "protoc --python_out=. --grpc_python_out=. svc.proto; class SvcServicer(svc_pb2_grpc.SvcServicer): def Method(self, request, ctx): ...",
        descLong: "gRPC is HTTP/2 with binary protobuf payloads and code-gen stubs. Workflow: write `.proto`, generate `*_pb2.py` (messages) and `*_pb2_grpc.py` (stubs + servicers), implement the servicer, run a `grpc.server` (sync, thread pool) or `grpc.aio.server` (asyncio). Four RPC types: unary-unary (RPC), server-streaming (subscribe), client-streaming (upload), bidi-streaming (chat). Three depths solve the SAME task — Greeter service with `SayHello` and a server-streaming `SubscribeNews` — at depths: sync unary `SayHello` → sync server-streaming → async server with deadlines, status codes, and a bidi-streaming chat method.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Greeter service with SayHello(name) -> \"Hello, name\".\n# APPROACH  - Define .proto; generate stubs; sync server.\n# STRENGTHS - The canonical \"hello world\" gRPC.\n# WEAKNESSES- Sync only; no streaming; thread pool.\n# greeter.proto:\n\"\"\"\nsyntax = \"proto3\";\npackage greet;\n\nservice Greeter {\n    rpc SayHello (HelloRequest) returns (HelloReply);\n}\nmessage HelloRequest { string name = 1; }\nmessage HelloReply   { string message = 1; }\n\"\"\"\n\n# Generate:\n# python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. greeter.proto\n\n# server.py:\nimport grpc\nfrom concurrent import futures\nimport greeter_pb2\nimport greeter_pb2_grpc\n\n\nclass Greeter(greeter_pb2_grpc.GreeterServicer):\n    def SayHello(self, request, context):\n        return greeter_pb2.HelloReply(message=f\"Hello, {request.name}\")\n\n\ndef serve():\n    server = grpc.server(futures.ThreadPoolExecutor(max_workers=10))\n    greeter_pb2_grpc.add_GreeterServicer_to_server(Greeter(), server)\n    server.add_insecure_port(\"[::]:50051\")\n    server.start()\n    server.wait_for_termination()\n\n\n# client.py:\ndef call():\n    channel = grpc.insecure_channel(\"localhost:50051\")\n    stub = greeter_pb2_grpc.GreeterStub(channel)\n    resp = stub.SayHello(greeter_pb2.HelloRequest(name=\"World\"))\n    print(resp.message)\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — Greeter service — but add a server-streaming\n#             SubscribeNews method that pushes updates to the client.\n# APPROACH  - Server-side rpc returns an iterator; client iterates.\n# STRENGTHS - Real-time pushes without WebSockets; typed schema.\n# WEAKNESSES- Still sync server (thread per stream).\n# greeter.proto (extended):\n\"\"\"\nservice Greeter {\n    rpc SayHello (HelloRequest) returns (HelloReply);\n    rpc SubscribeNews (NewsRequest) returns (stream NewsItem);\n}\nmessage HelloRequest  { string name = 1; }\nmessage HelloReply    { string message = 1; }\nmessage NewsRequest   { string topic = 1; }\nmessage NewsItem      { string headline = 1; int64 ts = 2; }\n\"\"\"\n\n# server.py:\nimport time\nimport grpc\nfrom concurrent import futures\nimport greeter_pb2 as pb\nimport greeter_pb2_grpc as pb_grpc\n\n\nclass Greeter(pb_grpc.GreeterServicer):\n    def SayHello(self, request, context):\n        return pb.HelloReply(message=f\"Hello, {request.name}\")\n\n    def SubscribeNews(self, request, context):\n        # Stream 5 items with 1s spacing, then EOF.\n        for i in range(5):\n            if context.is_active() is False:           # client cancelled\n                return\n            yield pb.NewsItem(headline=f\"{request.topic}: item {i}\",\n                              ts=int(time.time()))\n            time.sleep(1)\n\n\ndef serve():\n    server = grpc.server(futures.ThreadPoolExecutor(max_workers=10))\n    pb_grpc.add_GreeterServicer_to_server(Greeter(), server)\n    server.add_insecure_port(\"[::]:50051\")\n    server.start(); server.wait_for_termination()\n\n\n# client.py:\ndef subscribe():\n    channel = grpc.insecure_channel(\"localhost:50051\")\n    stub = pb_grpc.GreeterStub(channel)\n    for item in stub.SubscribeNews(pb.NewsRequest(topic=\"ai\")):\n        print(f\"{item.ts}: {item.headline}\")\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — gRPC service — production: async server (grpc.aio),\n#             deadlines, status codes for errors, bidi-streaming Chat,\n#             TLS via grpc.ssl_server_credentials.\n# APPROACH  - grpc.aio.server; context.set_code/set_details for errors;\n#             async streams in/out for Chat.\n# STRENGTHS - Asyncio-native; deadline + cancellation aware; TLS;\n#             bidi-streaming over the same connection.\n# WEAKNESSES- aio API is similar but not identical to sync - older\n#             tutorials don't apply.\n# greeter.proto (final):\n\"\"\"\nsyntax = \"proto3\";\npackage greet;\n\nservice Greeter {\n    rpc SayHello (HelloRequest) returns (HelloReply);\n    rpc SubscribeNews (NewsRequest) returns (stream NewsItem);\n    rpc Chat (stream ChatMsg) returns (stream ChatMsg);\n}\nmessage HelloRequest { string name = 1; }\nmessage HelloReply   { string message = 1; }\nmessage NewsRequest  { string topic = 1; }\nmessage NewsItem     { string headline = 1; int64 ts = 2; }\nmessage ChatMsg      { string user = 1; string text = 2; int64 ts = 3; }\n\"\"\"\n\n# server.py:\nimport asyncio\nimport time\nimport grpc\nfrom grpc import aio\nimport greeter_pb2 as pb\nimport greeter_pb2_grpc as pb_grpc\n\n\nCONNECTED: set[asyncio.Queue] = set()                  # in-process broadcast\n\n\nclass Greeter(pb_grpc.GreeterServicer):\n    async def SayHello(self, request, context: aio.ServicerContext):\n        if not request.name:\n            await context.abort(grpc.StatusCode.INVALID_ARGUMENT, \"name required\")\n        return pb.HelloReply(message=f\"Hello, {request.name}\")\n\n    async def SubscribeNews(self, request, context: aio.ServicerContext):\n        deadline = context.time_remaining()\n        end_at = time.time() + (deadline if deadline else 30.0)\n        i = 0\n        while time.time() < end_at:\n            if context.cancelled():\n                return\n            yield pb.NewsItem(headline=f\"{request.topic}: item {i}\",\n                              ts=int(time.time()))\n            i += 1\n            await asyncio.sleep(1)\n\n    async def Chat(self, request_iterator, context: aio.ServicerContext):\n        outbound: asyncio.Queue = asyncio.Queue(maxsize=64)\n        CONNECTED.add(outbound)\n        consumer_task = asyncio.create_task(self._consume(request_iterator))\n        try:\n            while not context.cancelled():\n                try:\n                    msg = await asyncio.wait_for(outbound.get(), timeout=1.0)\n                except asyncio.TimeoutError:\n                    continue\n                yield msg\n        finally:\n            consumer_task.cancel()\n            CONNECTED.discard(outbound)\n\n    async def _consume(self, request_iterator):\n        async for msg in request_iterator:\n            stamped = pb.ChatMsg(user=msg.user, text=msg.text, ts=int(time.time()))\n            for q in list(CONNECTED):\n                try:\n                    q.put_nowait(stamped)\n                except asyncio.QueueFull:\n                    pass\n\n\nasync def serve():\n    server = aio.server()\n    pb_grpc.add_GreeterServicer_to_server(Greeter(), server)\n\n    # TLS path - self-signed for dev:\n    # creds = grpc.ssl_server_credentials([(open(\"server.key\",\"rb\").read(),\n    #                                       open(\"server.crt\",\"rb\").read())])\n    # server.add_secure_port(\"[::]:50051\", creds)\n    server.add_insecure_port(\"[::]:50051\")\n    await server.start()\n    await server.wait_for_termination()\n\n\nasyncio.run(serve())\n\n# Decision rule:\n#   Internal microservice with typed schema   -> gRPC.\n#   Public-internet API for any client        -> REST/HTTP-JSON or GraphQL.\n#   Real-time push from server                 -> gRPC server-streaming or WebSockets.\n#   Bidirectional streaming                    -> gRPC bidi (above) or WebSockets.\n#   Async Python service                        -> grpc.aio.server.\n#   Sync Python service                         -> grpc.server + ThreadPoolExecutor.\n#   Need browser support                         -> grpc-web (proxy via Envoy or grpc-web shim).\n#   Need API gateway / mesh                      -> Envoy + grpc -> grpc-json transcoding.\n#   Need REST + gRPC from one schema             -> grpc-gateway (Go) or buf.\n\n# Anti-pattern:\n#   def Method(self, request, context):\n#       result = blocking_db_call()             # in grpc.aio servicer\n# grpc.aio servicers MUST be async. A sync blocking call in an async\n# servicer freezes the entire event loop. Use async DB drivers or\n# loop.run_in_executor for legacy sync code.\n\"\"\"\n"
                  }
        ],
        tips: [
                  "Generate stubs once: `python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. svc.proto` — commit the generated files.",
                  "Four RPC types: unary-unary, server-streaming, client-streaming, bidi-streaming — they cover most needs.",
                  "`grpc.aio.server()` is the asyncio path; servicers must be `async def`. `grpc.server()` is sync with a thread pool.",
                  "Use `context.set_code(grpc.StatusCode.NOT_FOUND)` + `set_details(...)` to return typed errors.",
                  "For browser clients, you need **grpc-web** + a proxy (Envoy or grpc-web standalone) — raw gRPC isn't browser-supported."
        ],
        mistake: "Calling a blocking function inside a `grpc.aio` async servicer. Freezes the entire event loop. Use async DB drivers or `loop.run_in_executor` for legacy sync code.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },

  // ── Section 3: Packet capture and crafting ─────────────────────────────────────────
  {
    id: "low-level",
    title: "Packet capture and crafting",
    entries: [
      {
        id: "scapy-sniff-craft",
        fn: "scapy.sniff / send / packet crafting",
        desc: "scapy is a packet manipulation library — capture, dissect, modify, replay. `sniff(iface=, filter=BPF, prn=)` for capture; `IP()/TCP()/Raw()` for crafting; `send()` / `sendp()` for layer-3 / layer-2 transmission. **Requires root / `CAP_NET_RAW`** to open raw sockets.",
        category: "low-level",
        subtitle: "scapy.all (sniff, send, sendp, sr1 for one request/reply), packet construction via /-operator (Ether()/IP()/TCP()/Raw()), BPF filters (\"tcp port 80 and host 1.2.3.4\"), packet.show() / .summary() / .haslayer / [Layer], wrpcap / rdpcap for pcap I/O, requires CAP_NET_RAW or root",
        signature: "sniff(iface=\"eth0\", filter=\"tcp port 80\", prn=lambda p: p.summary(), count=10); pkt = IP(dst=\"1.2.3.4\")/TCP(dport=80)/Raw(b\"GET / HTTP/1.0\\r\\n\\r\\n\"); send(pkt)",
        descLong: "Construct packets by `/`-stacking layers: `IP(dst=...)/TCP(dport=80, flags=\"S\")/Raw(b\"...\")`. `sniff(filter=BPF, prn=cb, count=N, iface=)` runs the kernel BPF filter and calls `cb(pkt)` per match. `send(pkt)` transmits at layer 3 (IP, the kernel handles routing + ARP); `sendp(pkt)` transmits at layer 2 (you supply the Ethernet frame). For request/response in one call, `sr1(pkt, timeout=)` is the right tool. Three depths solve the SAME task — count incoming HTTP-port SYNs over 60 s — at depths: simple sniff + counter → BPF filter + summary printer → multi-iface sniff with per-source rate counter, pcap snapshot, and graceful Ctrl-C.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Print every packet seen for 10 seconds.\n# APPROACH  - sniff with a Python callback.\n# STRENGTHS - Three lines.\n# WEAKNESSES- No filter (huge volume); needs root; not portable\n#             to Windows without Npcap.\nfrom scapy.all import sniff\n\ndef show(p):\n    print(p.summary())\n\n# count=0 + timeout=10 means \"capture for 10 seconds\".\nsniff(prn=show, count=0, timeout=10)\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — count SYN packets to TCP port 80 for 60 seconds.\n# APPROACH  - BPF filter offloads the work to the kernel; closure counter.\n# STRENGTHS - Massive perf win - kernel discards non-matching packets.\n# WEAKNESSES- BPF filter syntax is its own thing (tcpdump syntax).\nfrom scapy.all import sniff, TCP\n\n\ncount = {\"syn\": 0, \"total\": 0}\n\n\ndef on_packet(p):\n    count[\"total\"] += 1\n    if p.haslayer(TCP) and p[TCP].flags & 0x02:        # SYN bit\n        count[\"syn\"] += 1\n\n\nsniff(\n    iface=\"eth0\",\n    filter=\"tcp port 80\",                              # BPF / pcap-filter syntax\n    prn=on_packet,\n    timeout=60,\n    store=False,                                        # don't keep packets in RAM\n)\n\nprint(\"totals:\", count)\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — port-80 SYN counter — production: per-source rate\n#             tracker, pcap snapshot of suspicious sources, signal-aware\n#             stop, structured summary.\n# APPROACH  - Track per-IP SYN counts; if a source exceeds threshold,\n#             save its packets to a pcap; AsyncSniffer lets us stop\n#             cleanly on SIGINT.\n# STRENGTHS - Detects SYN floods; preserves evidence; clean shutdown.\n# WEAKNESSES- Single-process; for high-rate captures you want\n#             tcpdump | scapy or a kernel-bypass library.\nfrom __future__ import annotations\nimport signal\nimport time\nfrom collections import defaultdict, deque\nfrom scapy.all import AsyncSniffer, TCP, IP, wrpcap\n\n\nTHRESHOLD_PER_MIN = 100\nWINDOW_S = 60\n\n\nclass SynWatcher:\n    def __init__(self):\n        self.events: dict[str, deque] = defaultdict(deque)\n        self.captured: dict[str, list] = defaultdict(list)\n        self.flagged: set[str] = set()\n\n    def feed(self, pkt) -> None:\n        if not (pkt.haslayer(TCP) and pkt[TCP].flags & 0x02):\n            return\n        src = pkt[IP].src if pkt.haslayer(IP) else \"?\"\n        now = time.time()\n        dq = self.events[src]\n        dq.append(now)\n        # Evict events older than the window.\n        cutoff = now - WINDOW_S\n        while dq and dq[0] < cutoff:\n            dq.popleft()\n        # Cap captured packet count per source so we don't OOM.\n        if len(self.captured[src]) < 200:\n            self.captured[src].append(pkt)\n        if len(dq) > THRESHOLD_PER_MIN and src not in self.flagged:\n            self.flagged.add(src)\n            print(f\"FLAG {src}: {len(dq)} SYN/min\")\n\n    def report(self) -> None:\n        if not self.flagged:\n            print(\"no offenders this run.\")\n            return\n        for src in sorted(self.flagged):\n            wrpcap(f\"flood-{src}.pcap\", self.captured[src])\n            print(f\"  {src}: {len(self.events[src])} hits, \"\n                  f\"saved {len(self.captured[src])} packets to flood-{src}.pcap\")\n\n\ndef main():\n    watcher = SynWatcher()\n    sniffer = AsyncSniffer(\n        iface=\"eth0\", filter=\"tcp port 80\",\n        prn=watcher.feed, store=False,\n    )\n    sniffer.start()\n    print(f\"watching tcp/80 for SYN floods (>{THRESHOLD_PER_MIN}/min)\")\n\n    def stop(_sig, _frame):\n        sniffer.stop()\n    signal.signal(signal.SIGINT, stop)\n\n    try:\n        while sniffer.running:\n            time.sleep(1)\n    finally:\n        sniffer.stop()\n        watcher.report()\n\n\nmain()\n\n# Decision rule:\n#   Need to read packets in Python              -> scapy.sniff / AsyncSniffer.\n#   Need to craft / inject                      -> IP()/TCP()/Raw() + send / sr1 / sendp.\n#   Need to read existing pcap                  -> rdpcap(\"file.pcap\"); each packet is\n#                                                  a normal scapy object.\n#   Need to write pcap                          -> wrpcap(\"out.pcap\", packets).\n#   Need high packet rates                       -> tcpdump pipe + scapy -> too slow;\n#                                                  use libpcap directly or AF_PACKET in C.\n#   Need ARP / DHCP / DNS spoofing               -> scapy has helpers (arp_send, etc.) but\n#                                                  also handle the ethical/legal review.\n#   Need fuzzing                                 -> scapy.fuzz(IP()/TCP()) auto-fuzzes fields.\n#   Need TLS visibility                          -> not scapy; mitmproxy, or scapy-ssl_tls.\n\n# Anti-pattern:\n#   sniff(prn=callback)              # without store=False\n# scapy buffers EVERY packet in RAM until the sniff ends. Long-running\n# captures OOM. Always pass store=False unless you actually need the\n# packet list.\n"
                  }
        ],
        tips: [
                  "Always pass `store=False` to long-running `sniff` — otherwise scapy buffers every packet in RAM.",
                  "Use **BPF filters** (`filter=\"tcp port 80\"`) — the kernel discards non-matching packets and saves you 99% of the work.",
                  "`AsyncSniffer().start()` lets the main thread continue and stop the sniff cleanly on SIGINT.",
                  "`sr1(pkt, timeout=)` sends a packet and returns the first reply — the right tool for ping / traceroute / port-scan-style probes.",
                  "Scapy needs **root** or `CAP_NET_RAW` capability — for unprivileged packet inspection, use Wireshark + `tshark` instead."
        ],
        mistake: "Running `sniff(prn=cb)` without `store=False` for long captures. Scapy keeps every packet in memory; you OOM in minutes on a busy interface.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },

  // ── Section 4: Choosing a network stack ─────────────────────────────────────────
  {
    id: "patterns",
    title: "Choosing a network stack",
    entries: [
      {
        id: "network-stack-decision",
        fn: "sockets vs WebSockets vs gRPC vs HTTP — pick the right tool",
        desc: "Raw sockets for custom or legacy protocols. WebSockets for browser-friendly bidirectional streams. gRPC for typed internal microservices. HTTP/JSON or HTTP/2 for public APIs and most cloud-to-cloud. QUIC / HTTP/3 emerging for lossy networks and head-of-line-blocking-sensitive workloads.",
        category: "patterns",
        subtitle: "sockets (custom protocol, lowest overhead), WebSockets (browser-friendly bidi), gRPC (typed schema, codegen, HTTP/2), REST/HTTP-JSON (universal, debuggable), HTTP/3 + QUIC (UDP-based, lossy networks, multiple streams without HoL blocking), tools: httpx for clients, FastAPI for servers, Starlette for raw ASGI",
        signature: "# raw socket: socket.socket + frame yourself\\n# ws:        websockets.serve(handler, host, port)\\n# grpc:      grpc.aio.server() + protobuf service\\n# REST:      FastAPI app w/ Pydantic models",
        descLong: "Five tiers. **Raw sockets**: custom binary protocols, embedded devices, legacy systems. **WebSockets**: any time a browser needs bidi (chat, dashboards, multiplayer). **gRPC**: internal services with known boundaries; typed schemas; streaming free. **REST / HTTP-JSON**: public APIs, anything that needs to be debuggable from `curl`. **HTTP/3 + QUIC**: emerging; helps for many parallel streams (CDNs, mobile networks, gaming). Three depths solve the SAME task — telemetry from devices to a server, reads from browser dashboards — at depths: pure sockets (devices) + WebSockets (browser) → gRPC for devices + WebSockets for browser → MQTT (devices) + REST/HTTP for queries + WebSockets for live UI updates, all behind FastAPI.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Devices send telemetry; browser sees it live.\n# APPROACH  - Raw TCP socket from device; WebSocket from browser.\n# STRENGTHS - Both ends in pure Python; no schema lock-in.\n# WEAKNESSES- Custom framing on the socket side; no auth; no replay.\n\n# device.py - line-delimited JSON over TCP\nimport socket, json, time\ns = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\ns.connect((\"server\", 9001))\nwhile True:\n    s.sendall((json.dumps({\"t\": 22.7}) + \"\\n\").encode())\n    time.sleep(5)\n\n# server.py - bridge socket to WebSocket\nimport asyncio, json, websockets\n\nCLIENTS = set()\n\n\nasync def feed_browser(reader, _writer):\n    while True:\n        line = await reader.readline()\n        if not line: break\n        websockets.broadcast(CLIENTS, line.decode().rstrip())\n\n\nasync def browser_handler(ws):\n    CLIENTS.add(ws)\n    try: await ws.wait_closed()\n    finally: CLIENTS.discard(ws)\n\n\nasync def main():\n    await asyncio.gather(\n        asyncio.start_server(feed_browser, \"0.0.0.0\", 9001).__aenter__(),\n        websockets.serve(browser_handler, \"0.0.0.0\", 8765).__aenter__(),\n        asyncio.Future(),\n    )\n\n# asyncio.run(main())\n"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - SAME — devices -> server -> browser — but devices use gRPC\n#             (typed schema) and browsers use WebSockets.\n# APPROACH  - Define telemetry.proto; gRPC server pushes into a broadcast;\n#             FastAPI/WebSocket endpoint streams to browsers.\n# STRENGTHS - Typed device API; codegen across languages; browser-friendly.\n# WEAKNESSES- Two transports to operate; gRPC isn't trivially debuggable\n#             from curl.\n\n# telemetry.proto:\n\"\"\"\nservice Telemetry {\n    rpc Stream (stream Sample) returns (Ack);\n}\nmessage Sample { string device = 1; double t = 2; int64 ts = 3; }\nmessage Ack    { int32 received = 1; }\n\"\"\"\n\n# server.py\nimport asyncio, json\nimport grpc\nfrom grpc import aio\nfrom fastapi import FastAPI, WebSocket\nimport telemetry_pb2 as pb\nimport telemetry_pb2_grpc as pb_grpc\n\n\nCLIENTS: set[WebSocket] = set()\n\n\nclass Telemetry(pb_grpc.TelemetryServicer):\n    async def Stream(self, request_iterator, context):\n        n = 0\n        async for s in request_iterator:\n            payload = json.dumps({\"device\": s.device, \"t\": s.t, \"ts\": s.ts})\n            for ws in list(CLIENTS):\n                try:\n                    await ws.send_text(payload)\n                except Exception:\n                    CLIENTS.discard(ws)\n            n += 1\n        return pb.Ack(received=n)\n\n\napp = FastAPI()\n\n\n@app.websocket(\"/ws\")\nasync def ws_endpoint(ws: WebSocket):\n    await ws.accept()\n    CLIENTS.add(ws)\n    try:\n        while True:\n            await ws.receive_text()                    # keep alive\n    finally:\n        CLIENTS.discard(ws)\n\n\nasync def serve_grpc():\n    server = aio.server()\n    pb_grpc.add_TelemetryServicer_to_server(Telemetry(), server)\n    server.add_insecure_port(\"[::]:50051\")\n    await server.start()\n    await server.wait_for_termination()\n"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — devices + browser — production-shaped: MQTT for devices\n#             (right protocol for fleet IoT), REST for queries, WebSockets\n#             for live UI; FastAPI + aiomqtt + Redis fan-out.\n# APPROACH  - Devices publish via MQTT; aiomqtt subscriber writes to\n#             Postgres + fans to Redis pub/sub; FastAPI exposes REST + WS.\n# STRENGTHS - Right tool per layer; horizontal scale via Redis; managed\n#             persistence; debuggable via REST.\n# WEAKNESSES- More moving parts; cost of three subsystems (broker, DB,\n#             cache) is real.\n# bridge.py\nimport asyncio, json\nimport aiomqtt\nimport asyncpg\nimport redis.asyncio as redis\n\n\nasync def main():\n    pg = await asyncpg.create_pool(\"postgresql://...\")\n    r  = redis.from_url(\"redis://...\")\n\n    async def handle(client, msg):\n        try:\n            data = json.loads(msg.payload)\n        except Exception:\n            return\n        # Persist\n        async with pg.acquire() as conn:\n            await conn.execute(\n                \"INSERT INTO telemetry(device, t, ts) VALUES($1, $2, $3)\",\n                data[\"device\"], data[\"t\"], data[\"ts\"],\n            )\n        # Fan out to live websockets in any process subscribed to this channel.\n        await r.publish(\"telemetry\", msg.payload)\n\n    async with aiomqtt.Client(\"broker.example.com\") as client:\n        await client.subscribe(\"acme/+/+/telemetry\", qos=1)\n        async for message in client.messages:\n            await handle(client, message)\n\n\n# api.py - FastAPI exposes both REST + WS, fed from Redis pub/sub.\nfrom fastapi import FastAPI, WebSocket\nimport redis.asyncio as redis\nimport asyncpg\n\napp = FastAPI()\n\n\n@app.on_event(\"startup\")\nasync def startup():\n    app.state.pg = await asyncpg.create_pool(\"postgresql://...\")\n    app.state.redis = redis.from_url(\"redis://...\")\n\n\n@app.get(\"/devices/{device}/recent\")\nasync def recent(device: str, limit: int = 100):\n    async with app.state.pg.acquire() as conn:\n        rows = await conn.fetch(\n            \"SELECT t, ts FROM telemetry WHERE device=$1 \"\n            \"ORDER BY ts DESC LIMIT $2\",\n            device, limit,\n        )\n    return [dict(r) for r in rows]\n\n\n@app.websocket(\"/ws/telemetry\")\nasync def stream(ws: WebSocket):\n    await ws.accept()\n    pubsub = app.state.redis.pubsub()\n    await pubsub.subscribe(\"telemetry\")\n    try:\n        async for msg in pubsub.listen():\n            if msg[\"type\"] == \"message\":\n                await ws.send_bytes(msg[\"data\"])\n    finally:\n        await pubsub.unsubscribe(\"telemetry\")\n        await pubsub.close()\n\n# Decision rule:\n#   Browser client                                  -> WebSockets (or fetch + SSE).\n#   Internal microservice with typed contract        -> gRPC.\n#   Public-internet API                              -> REST/HTTP-JSON.\n#   IoT fleet                                        -> MQTT.\n#   Custom binary protocol on embedded device        -> raw TCP/UDP.\n#   Want server -> client push without WS            -> Server-Sent Events (text/event-stream).\n#   Want low latency over lossy networks             -> QUIC / HTTP/3 (aioquic).\n#   Want async pipelines + persistence + live UI      -> MQTT in, Postgres + Redis pub/sub\n#                                                       backing FastAPI for queries + WS.\n#   Can't decide                                      -> REST + WS is the minimum-regret default;\n#                                                       optimize later.\n\n# Anti-pattern:\n#   \"We'll use raw sockets - it's faster\"\n# It's not faster - it's lower-level. You'll spend weeks reimplementing\n# framing, retries, auth, schema. Pick gRPC / MQTT / REST first; only go\n# to raw sockets when an existing protocol literally can't fit.\n\"\"\"\n"
                  }
        ],
        tips: [
                  "Browser bidi → **WebSockets**. Browser one-way push → **Server-Sent Events** (text/event-stream).",
                  "Internal typed services → **gRPC**. Public/debuggable APIs → **REST/HTTP-JSON**.",
                  "IoT fleet → **MQTT**. Custom embedded protocols → **raw sockets**, but justify it before you reach for them.",
                  "For low latency over lossy networks (mobile, gaming, CDN edge), **HTTP/3 + QUIC** (aioquic) avoids head-of-line blocking.",
                  "Common production shape: MQTT (devices) → bridge (Python) → Postgres (history) + Redis pub/sub (live) → FastAPI (REST + WS) — covers most IoT/dashboard apps."
        ],
        mistake: "Reaching for raw sockets \"for performance\". You'll re-implement framing, retries, auth, and schemas. Use gRPC or MQTT or REST first; raw sockets only when nothing else fits.",
        shorthand: {
          verbose: "// Verbose: step-by-step approach\n\n// Each step done separately",
          concise: "// Concise: idiomatic one-liner\n",
        },
      },
    ],
  },
]

export default { meta, sections }

export const meta = {
  "id": "regex",
  "label": "Regular Expressions",
  "icon": "🔍",
  "description": "Python regex: re.match, re.search, re.findall, re.sub, groups, flags, and practical patterns."
}

export const sections = [

  // ── Section 1: Basics & Matching ─────────────────────────────────────────
  {
    id: "regex-basics",
    title: "Basics & Matching",
    entries: [
      {
        id: "re-match",
        fn: "re.match() vs re.search() vs re.fullmatch()",
        desc: "Choose the right matching function based on where you need to find the pattern.",
        category: "Regex",
        subtitle: "Match at start vs anywhere vs entire string",
        signature: "re.match(pattern, string)  |  re.search(pattern, string)  |  re.fullmatch(pattern, string)",
        descLong: "re.match() checks only the beginning of the string. re.search() scans the entire string and returns the first match. re.fullmatch() requires the entire string to match the pattern. All three return a Match object (truthy) on success, None on failure. Use match() when you expect a prefix, search() for general \"find this anywhere\" queries, fullmatch() to validate entire strings.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of re.match() vs re.search() vs re.fullmatch() — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport re\ntext = \"The quick brown fox\"\n# match: anchored to the START.\nprint(re.match(r\"The\", text))         # <Match ...>\nprint(re.match(r\"quick\", text))       # None\n# search: anywhere in the string.\nprint(re.search(r\"quick\", text))      # <Match ...>\n# fullmatch: must consume the entire string.\nprint(re.fullmatch(r\"The .+ fox\", text))   # <Match ...>"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of re.match() vs re.search() vs re.fullmatch() — common patterns you'll see in production.\n# APPROACH  - Combine re.match() vs re.search() vs re.fullmatch() with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nimport re\nEMAIL = r\"[\\w.+-]+@[\\w.-]+\\.\\w{2,}\"\ndef is_valid_email(s: str) -> bool:\n    # fullmatch: validator semantics -- ENTIRE string must satisfy the pattern.\n    return re.fullmatch(EMAIL, s) is not None\n# Extract first hit + position with the walrus operator (3.8+).\ntext = \"Hi alice@example.com -- ping bob@x.io about the issue\"\nif m := re.search(EMAIL, text):\n    print(m.group(), m.span())            # 'alice@example.com' (3, 21)\n# Iterate hits with finditer (lazy) -- preferred over findall when you need positions.\nfor m in re.finditer(EMAIL, text):\n    print(m.start(), m.group())\n# match() makes sense for prefix-anchored protocols, e.g. log line keys:\nif m := re.match(r\"\\[(?P<level>\\w+)\\]\\s+\", \"[ERROR] disk full\"):\n    print(m.group(\"level\"))               # ERROR"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of re.match() vs re.search() vs re.fullmatch() — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\nfrom __future__ import annotations\nimport re\n# 'regex' is a drop-in superset of 're' with timeouts and Unicode property classes.\n# pip install regex; same API, plus regex.compile(p).search(s, timeout=0.1).\n# 1) ALWAYS compile when reused. Anchors live IN the pattern, not in the call site.\nEMAIL_RE       = re.compile(r\"\\A[\\w.+-]+@[\\w.-]+\\.\\w{2,}\\Z\", re.ASCII)\nLOG_PREFIX_RE  = re.compile(r\"\\A\\[(?P<level>DEBUG|INFO|WARN|ERROR|FATAL)\\]\\s+\")\nISO_DATE_RE    = re.compile(r\"\\A(?P<y>\\d{4})-(?P<m>\\d{2})-(?P<d>\\d{2})\\Z\")\ndef parse_log_line(line: str) -> tuple[str, str] | None:\n    if not (m := LOG_PREFIX_RE.match(line)):    # match() is fine: pattern starts with \\A\n        return None\n    return m[\"level\"], line[m.end():]\n# 2) Validators: prefer fullmatch + ASCII flag for protocol fields. Unicode \\w\n#    is huge (matches things like Khmer digits) -- almost never what you want\n#    for an email/username/hostname.\ndef is_username(s: str) -> bool:\n    return re.fullmatch(r\"[a-z][a-z0-9_-]{2,31}\", s, re.ASCII) is not None\n# 3) Untrusted patterns: NEVER feed them to stdlib 're' without bounds.\n#    Either reject them, or use 'regex' with a timeout.\ndef safe_search(pattern: str, text: str, *, timeout_s: float = 0.05) -> bool:\n    try:\n        import regex                            # external lib\n        return regex.search(pattern, text, timeout=timeout_s) is not None\n    except ImportError:                          # fall back: severely length-limit\n        if len(pattern) > 200:\n            raise ValueError(\"pattern too long without 'regex' library\")\n        return re.search(pattern, text) is not None\n# Decision rule:\n#   pattern reused                         -> re.compile at module scope, ALL CAPS name\n#   anchor at start of string              -> \\A and \\Z (NOT ^ and $) when re.MULTILINE may apply\n#   protocol fields (email/uuid/hostname)  -> fullmatch() + re.ASCII + ANCHORED pattern\n#   parsing hot path                       -> single compiled pattern with named groups; one .match\n#   pattern comes from a user / config     -> 'regex' library + timeout, or pre-vet with length+structure check\n#   need a custom early exit               -> use finditer + break, NOT findall + slice\n#\n# Anti-pattern: 'if re.match(pattern, s)' as a validator. match() is anchored\n# only at the START, not the END -- 'foo@bar.com<script>' passes. Use fullmatch\n# (or anchor with \\A...\\Z) for validation; reserve match() for explicit\n# prefix-stripping in parsers."
                  }
        ],
        tips: [
                  "match() is fastest for patterns expected at the start (e.g., file headers, config keys).",
                  "search() is the workhorse for finding patterns anywhere — use this when unsure.",
                  "fullmatch() is ideal for validation — does this entire input satisfy the pattern?",
                  "All three return a Match object with .group(), .start(), .end() methods if successful.",
                  "For protocol fields (email/uuid/hostname), use fullmatch + re.ASCII + \\A...\\Z anchors — Unicode \\w accepts surprising things (Khmer digits etc.). For untrusted patterns, stdlib re has NO timeout; use the third-party `regex` module with timeout= or reject overlong patterns."
        ],
        mistake: "`if re.match(pattern, s)` as a validator — match() is anchored only at the START, not the END, so \"foo@bar.com<script>\" passes. Use fullmatch (or anchor with \\A...\\Z); reserve match() for explicit prefix-stripping in parsers.",
        shorthand: {
          verbose: "import re\ntext = \"Contact us at help@example.com\"\nfound = False\nfor word in text.split():\n    if word.startswith(\"help\"):\n        found = True\n        break\nif found:\n    print(\"Found help email\")",
          concise: "import re\nif re.search(r\"help\\S+@\\S+\", text):\n    print(\"Found help email\")",
        },
      },
      {
        id: "re-findall",
        fn: "re.findall() and re.finditer()",
        desc: "Find all non-overlapping occurrences of a pattern.",
        category: "Regex",
        subtitle: "findall returns list, finditer returns iterator",
        signature: "re.findall(pattern, string)  |  re.finditer(pattern, string)",
        descLong: "re.findall() returns a list of all non-overlapping matches (strings or tuples if groups). re.finditer() returns an iterator of Match objects — memory-efficient for large strings. Use findall() for small result sets or when you need quick list access. Use finditer() when processing large volumes or need Match metadata (position, groups, etc.). Both are case-sensitive by default; use re.IGNORECASE flag to ignore case.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of re.findall() and re.finditer() — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport re\ntext = \"Emails: alice@x.com, bob@y.org\"\n# Plain strings:\nprint(re.findall(r\"\\w+@\\w+\\.\\w+\", text))\n# ['alice@x.com', 'bob@y.org']\n# All numbers in a string:\nprint(re.findall(r\"\\d+\", \"42 apples, 17 oranges\"))   # ['42', '17']"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of re.findall() and re.finditer() — common patterns you'll see in production.\n# APPROACH  - Combine re.findall() and re.finditer() with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nimport re\n# Pairs via groups -- list of tuples.\npairs = re.findall(r\"(\\w+):(\\d+)\", \"Alice:30, Bob:25, Charlie:35\")\n# [('Alice', '30'), ('Bob', '25'), ('Charlie', '35')]\nfor name, age in pairs:\n    print(f\"{name} is {age}\")\n# finditer for big files -- one Match at a time, with span() and groupdict().\nLOG = re.compile(r\"(?P<ts>\\d{4}-\\d{2}-\\d{2})\\s+(?P<level>\\w+)\\s+(?P<msg>.+)\")\nwith open(\"app.log\") as f:\n    for line in f:\n        if m := LOG.match(line):\n            print(m[\"ts\"], m[\"level\"], m[\"msg\"][:60])\n# Count without materializing -- sum(1 for _ in finditer(...)) beats len(findall(...)) on huge text.\ndef count_matches(pat, s):\n    return sum(1 for _ in pat.finditer(s))"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of re.findall() and re.finditer() — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\nfrom __future__ import annotations\nimport mmap\nimport re\nfrom collections.abc import Iterator\nfrom pathlib import Path\n# 1) findall vs finditer -- pick by what you'll do next.\ndef first_n(pat: re.Pattern[str], s: str, n: int) -> list[str]:\n    out: list[str] = []\n    for i, m in enumerate(pat.finditer(s)):\n        if i >= n: break\n        out.append(m.group())\n    return out\n# 2) Stream a multi-GB file -- mmap + bytes pattern + finditer.\n#    Patterns must be bytes when scanning bytes (mmap returns bytes).\nLINE_RE = re.compile(rb\"(?P<ip>\\d+\\.\\d+\\.\\d+\\.\\d+)\\s+(?P<status>\\d{3})\")\ndef scan_access_log(path: Path) -> Iterator[tuple[str, int]]:\n    with path.open(\"rb\") as f, mmap.mmap(f.fileno(), 0, access=mmap.ACCESS_READ) as mm:\n        for m in LINE_RE.finditer(mm):\n            yield m[\"ip\"].decode(), int(m[\"status\"])\n# 3) findall's two-shape return is a footgun -- normalize at the boundary.\ndef find_pairs(pat: re.Pattern[str], s: str) -> list[tuple[str, ...]]:\n    \"\"\"Always return a list of tuples, regardless of group count.\"\"\"\n    raw = pat.findall(s)\n    if not raw: return []\n    return [(x,) if isinstance(x, str) else tuple(x) for x in raw]\n# 4) Overlapping matches: findall/finditer skip overlaps. Use lookahead trick:\n#    r\"(?=(pattern))\" -- ZERO-WIDTH match captures, advances by 1, finds overlaps.\ndef overlapping(pat: str, s: str) -> list[str]:\n    return re.findall(f\"(?=({pat}))\", s)\n# overlapping(\"aa\", \"aaaa\") -> ['aa', 'aa', 'aa']      (vs findall -> ['aa', 'aa'])\n# Decision rule:\n#   small string, list at hand              -> findall (string-list or tuple-list)\n#   need positions / spans                  -> finditer + Match.span()\n#   stream a file                           -> mmap (bytes) + bytes Pattern + finditer\n#   stop after K hits                       -> finditer + break (do NOT findall + slice)\n#   overlapping matches                     -> wrap in (?=(pattern)); zero-width trick\n#   counting only                           -> sum(1 for _ in pat.finditer(s)); avoids the list\n#   grouped output that's unstable in shape -> normalize at the boundary, never trust findall's shape\n#\n# Anti-pattern: re.findall(pat, huge_text) when you only need the first match.\n# It builds the entire list before returning, paying O(N) memory for one item.\n# Use re.search(pat, huge_text) or 'next(pat.finditer(s), None)' instead."
                  }
        ],
        tips: [
                  "findall() with groups returns tuples — unpack them: for name, age in re.findall(r\"(\\w+):(\\d+)\", text):",
                  "finditer() is memory-efficient for large text — process matches one at a time instead of building a huge list.",
                  "Use \\w (word characters), \\d (digits), \\s (whitespace) for cleaner patterns.",
                  "Non-overlapping means \"aaa\" in \"aaaa\" finds only one match (position 0-3), not two — use the lookahead trick `(?=(pattern))` for overlapping. For multi-GB files, mmap + bytes pattern + finditer scans in constant memory."
        ],
        mistake: "findall on huge text when you only need the first match — it builds the entire list before returning, paying O(N) memory for one item. Use re.search or `next(pat.finditer(s), None)`. Also: findall's shape changes (1 group → list of strings, 2+ groups → list of tuples); normalize at the boundary.",
        shorthand: {
          verbose: "import re\ntext = \"apple, banana, cherry, date\"\nwords = []\nfor word in text.split(\", \"):\n    words.append(word.strip())",
          concise: "import re\nwords = re.findall(r\"\\w+\", text)",
        },
      },
      {
        id: "re-sub",
        fn: "re.sub() and re.subn()",
        desc: "Replace patterns with strings or callable functions.",
        category: "Regex",
        subtitle: "Substitution with fixed strings, backreferences, or dynamic replacements",
        signature: "re.sub(pattern, repl, string, count=0, flags=0)  |  re.subn(...)",
        descLong: "re.sub() replaces all matches (or first count matches) and returns the new string. re.subn() is identical but returns a tuple (new_string, num_replacements). The replacement can be a literal string (with backreferences \\1, \\g<name>), a function, or a lambda. Functions receive a Match object and must return a string. Use sub() for simple replacements; use a callable for complex logic.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of re.sub() and re.subn() — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport re\n# Plain replace.\nprint(re.sub(r\"Hello\", \"Hi\", \"Hello World, Hello Python\"))\n# 'Hi World, Hi Python'\n# Cap the number of replacements:\nprint(re.sub(r\"Hello\", \"Hi\", \"Hello Hello Hello\", count=1))\n# 'Hi Hello Hello'\n# Callable replacement gets the Match object:\nprint(re.sub(r\"\\d+\", lambda m: f\"[{int(m.group()):03d}]\", \"a 7 b 42\"))\n# 'a [007] b [042]'"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of re.sub() and re.subn() — common patterns you'll see in production.\n# APPROACH  - Combine re.sub() and re.subn() with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nimport re\n# Numbered backref in replacement: swap \"Last, First\" -> \"First Last\".\nprint(re.sub(r\"(\\w+),\\s+(\\w+)\", r\"\\2 \\1\", \"Smith, John; Doe, Jane\"))\n# 'John Smith; Jane Doe'\n# Named groups: clearer for non-trivial templates.\ntext = \"Name: Alice, Age: 30\"\nprint(re.sub(r\"Name: (?P<n>\\w+), Age: (?P<a>\\d+)\",\n             r\"\\g<a>-year-old \\g<n>\", text))\n# '30-year-old Alice'\n# subn returns (new_string, count) -- decide whether to write back.\nnew, n = re.subn(r\"\\bcolour\\b\", \"color\", \"favourite colour, neutral colour\")\nif n: print(f\"changed {n}: {new}\")\n# Callable: when the replacement depends on the matched text.\ndef title_acronym(m: re.Match) -> str:\n    word = m.group()\n    return word.upper() if len(word) <= 3 else word.title()\nprint(re.sub(r\"\\b\\w+\\b\", title_acronym, \"the api gw routes http requests\"))\n# 'THE API GW Routes HTTP Requests'"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of re.sub() and re.subn() — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\nfrom __future__ import annotations\nimport re\n# 1) Compile + callable -- the production form.\nWORD = re.compile(r\"\\b(\\w+)\\b\")\ndef lower_long_words(m: re.Match[str]) -> str:\n    w = m.group(1)\n    return w.lower() if len(w) > 6 else w\nprint(WORD.sub(lower_long_words, \"Architecture beats Inheritance every Time\"))\n# 'architecture beats inheritance every Time'\n# 2) re.escape is mandatory when ANY part of the pattern OR replacement comes\n#    from user data. Replacement strings interpret \\1, \\g<name>, etc.\ndef safe_replace_literal(text: str, needle: str, replacement: str) -> str:\n    return re.sub(re.escape(needle), lambda _: replacement, text)\n# Without the lambda you'd need re.sub(escaped, replacement.replace('\\\\', '\\\\\\\\'), text).\n# The callable side-steps replacement-string parsing entirely.\n# 3) Idempotence sanity: re-running the rewrite should be a no-op. Use subn:\ndef normalize_dashes(text: str) -> str:\n    out, n1 = re.subn(r\"\\s*--\\s*\", \" - \", text)\n    out, n2 = re.subn(r\"\\s+\", \" \", out)\n    if re.search(r\"\\s{2,}|\\s--\\s\", out):       # invariant\n        raise AssertionError(\"normalization not idempotent\")\n    return out\n# 4) re.sub on huge text -- prefer streaming via finditer + write to a buffer\n#    when the rewrite is expensive (e.g., calls into another service per match).\ndef rewrite_streaming(pat: re.Pattern[str], s: str, fn) -> str:\n    parts: list[str] = []\n    cur = 0\n    for m in pat.finditer(s):\n        parts.append(s[cur:m.start()])\n        parts.append(fn(m))\n        cur = m.end()\n    parts.append(s[cur:])\n    return \"\".join(parts)\n# Equivalent to pat.sub(fn, s) but you can early-stop, log, or batch.\n# Decision rule:\n#   fixed needle, fixed replacement     -> str.replace()  (faster, no regex needed)\n#   regex needle, FIXED replacement     -> re.sub(pat, \"lit\", s)  (escape if user-supplied)\n#   regex needle, DYNAMIC replacement   -> re.sub(pat, callable, s)\n#   want count of changes               -> re.subn(...)\n#   user-supplied replacement string    -> callable form, NEVER raw r\"...\" with user data\n#   huge text, side-effects per match   -> finditer + manual buffer; can early-exit\n#   need atomic 'replace OR fail'       -> subn + assert n == expected\n#\n# Anti-pattern: building the replacement string with f-strings that include\n# user input. Backslashes and \\g<name> in user data become regex directives.\n# Always pass a callable when ANY part of the replacement is user-controlled."
                  }
        ],
        tips: [
                  "Use raw strings (r\"...\") to avoid escaping backslashes — r\"\\d\" is cleaner than \"\\\\d\".",
                  "Backreferences: \\1, \\2... for positional groups; \\g<name> for named groups.",
                  "Callable replacement: def repl(m): return ... — you get the full Match object with position, groups, etc.",
                  "re.subn() is useful when you need to know how many replacements were made (and for idempotence checks).",
                  "For fixed needle + fixed replacement, str.replace() is faster — only reach for re.sub when the pattern actually varies. ALWAYS use a callable when any part of the replacement comes from user data; raw r\"...\" replacements interpret \\1, \\g<name>, etc. and can be hijacked."
        ],
        mistake: "Building the replacement string with f-strings that include user input — backslashes and \\g<name> in user data become regex directives. Pass a callable when ANY part of the replacement is user-controlled. Also: forgetting raw strings — re.sub(\"\\d\", ...) treats \\d as literal backslash-d.",
        shorthand: {
          verbose: "import re\ntext = text.replace(\"colour\", \"color\")\ntext = text.replace(\"flavour\", \"flavor\")\ntext = text.replace(\"neighbour\", \"neighbor\")",
          concise: "text = re.sub(r\"(\\w+)ou(r)\", r\"\\1o\\2\", text)",
        },
      },
      {
        id: "re-split",
        fn: "re.split()",
        desc: "Split a string using a regex pattern as the delimiter.",
        category: "Regex",
        subtitle: "Flexible string splitting with pattern-based separators",
        signature: "re.split(pattern, string, maxsplit=0, flags=0)",
        descLong: "re.split() works like str.split() but accepts regex patterns instead of fixed strings. Captures groups are included in the result. maxsplit limits the number of splits. Useful for splitting on multiple delimiters, whitespace variations, or complex boundaries. Compare: str.split(\", \") splits on exactly \", \"; re.split(r\",\\s*\") splits on comma followed by any whitespace.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of re.split() — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport re\n# Multiple delimiters at once.\nprint(re.split(r\"[,;:]\", \"apple, banana; orange: grape\"))\n# ['apple', ' banana', ' orange', ' grape']\n# Collapse runs of whitespace.\nprint(re.split(r\"\\s+\", \"one   two\\tthree\\nfour\"))\n# ['one', 'two', 'three', 'four']"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of re.split() — common patterns you'll see in production.\n# APPROACH  - Combine re.split() with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nimport re\n# Keep the rest after N splits.\nprint(re.split(r\":\", \"a:b:c:d:e\", maxsplit=2))\n# ['a', 'b', 'c:d:e']\n# Keep delimiters in the result via a capture group.\nprint(re.split(r\"(\\d+)\", \"Hello123World456Python\"))\n# ['Hello', '123', 'World', '456', 'Python']\n# Use a non-capturing group when you DON'T want delimiters back.\nprint(re.split(r\"(?:\\d+)\", \"a1b22c\"))\n# ['a', 'b', 'c']\n# Split on a position WITHOUT consuming -- camelCase via lookahead.\nprint(re.split(r\"(?=[A-Z])\", \"oneTwoThreeFour\"))\n# ['one', 'Two', 'Three', 'Four']\n# Drop empties from edges.\nparts = [p for p in re.split(r\"\\W+\", \" hello, world! \") if p]\n# ['hello', 'world']"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of re.split() — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\nfrom __future__ import annotations\nimport csv\nimport io\nimport re\n# 1) Tokenize while preserving delimiter info -- split into (token, sep) pairs.\nTOKEN = re.compile(r\"(\\s+|[,;:!?])\")\ndef tokens_with_seps(s: str) -> list[tuple[str, str]]:\n    parts = TOKEN.split(s)\n    # parts is [tok, sep, tok, sep, ...] possibly ending in tok.\n    return list(zip(parts[0::2], parts[1::2] + [\"\"]))\n# 2) Split honoring word boundaries instead of fixed punctuation.\ndef words(s: str) -> list[str]:\n    return [w for w in re.split(r\"\\W+\", s, flags=re.UNICODE) if w]\n# 3) When NOT to use re.split: CSV with quoted fields containing the delimiter.\n#    csv module handles quoting / escaping; regex split silently corrupts it.\ndef parse_csv_line(line: str) -> list[str]:\n    return next(csv.reader(io.StringIO(line)))\n# parse_csv_line('a,\"b,c\",d')   ->  ['a', 'b,c', 'd']    (correct)\n# re.split(r\",\", 'a,\"b,c\",d')    ->  ['a', '\"b', 'c\"', 'd']  (wrong)\n# 4) Multi-line records (split a log file by section header).\nSECTION = re.compile(r\"^=== (?P<title>.+) ===$\", re.MULTILINE)\ndef by_section(text: str) -> dict[str, str]:\n    parts = SECTION.split(text)              # ['', title1, body1, title2, body2, ...]\n    return dict(zip(parts[1::2], parts[2::2]))\n# 5) Keep delimiters AND know which one matched -- alternation in a capture.\nDELIMS = re.compile(r\"(--|//|;)\")\ndef split_keep_kind(s: str) -> list[tuple[str, str | None]]:\n    out: list[tuple[str, str | None]] = []\n    parts = DELIMS.split(s)\n    for tok, sep in zip(parts[0::2], parts[1::2] + [None]):\n        out.append((tok, sep))\n    return out\n# Decision rule:\n#   simple multi-delimiter                  -> re.split(r\"[,;:]\", s)\n#   variable whitespace                     -> re.split(r\"\\s+\", s)\n#   need delimiters back                    -> capture group\n#   need POSITIONS not consumed             -> lookahead/lookbehind in the split pattern\n#   quoted / nested format (CSV, JSON-ish)  -> proper parser, NOT split\n#   maxsplit \"first N tokens\"               -> str.split(maxsplit=N) is faster for fixed delim\n#   build a tokenizer                       -> finditer + scanner pattern, NOT a split chain\n#   tokenize then re-emit                   -> capture-group split + zip pairs\n#\n# Anti-pattern: re.split(r\",\", csv_line). It \"works\" until the first quoted\n# field with a comma inside, then silently breaks every consumer downstream.\n# Use the csv module for CSV. Use a real parser for anything with quoting,\n# escapes, or nesting."
                  }
        ],
        tips: [
                  "Capture groups in the pattern are included in the result — use non-capturing groups (?:...) if you want splitting without capturing.",
                  "re.split(r\"\\s+\") is a regex version of str.split() with no args — it handles multiple spaces, tabs, newlines.",
                  "Split on class boundaries: re.split(r\"(?=[A-Z])\", \"camelCase\") splits before uppercase letters without consuming them.",
                  "Empty strings in result signal consecutive delimiters — filter with [x for x in result if x]."
        ],
        mistake: "`re.split(r\",\", csv_line)` — it works until the first quoted field with a comma inside, then silently corrupts every consumer downstream. Use the csv module for CSV; use a real parser for anything with quoting, escapes, or nesting. Also: capture groups are included in the result; use (?:...) when you don't want delimiters back.",
        shorthand: {
          verbose: "import re\ntext = \"apple, banana; orange\"\nparts = text.replace(\";\", \",\").split(\",\")\nresult = [p.strip() for p in parts]",
          concise: "result = re.split(r\"[,;]\\s*\", text)",
        },
      },
      {
        id: "re-compile",
        fn: "re.compile()",
        desc: "Pre-compile regex patterns for reuse and performance.",
        category: "Regex",
        subtitle: "Pattern objects for repeated matching, flags, and debugging",
        signature: "re.compile(pattern, flags=0)",
        descLong: "re.compile() returns a Pattern object that you can call methods on multiple times: .match(), .search(), .findall(), .sub(), etc. Compiling is beneficial when you use the same pattern repeatedly — the pattern is compiled to bytecode once. Also makes code more readable and enables patterns to have names. You can inspect pattern properties: .pattern, .flags, .groups.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of re.compile() — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport re\nEMAIL = re.compile(r\"[\\w.+-]+@[\\w.-]+\\.\\w{2,}\")\nprint(EMAIL.search(\"contact alice@x.com\").group())   # 'alice@x.com'\nprint(EMAIL.findall(\"alice@x.com, bob@y.org\"))       # ['alice@x.com', 'bob@y.org']\nprint(EMAIL.sub(\"***\", \"ping bob@y.org\"))            # 'ping ***'"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of re.compile() — common patterns you'll see in production.\n# APPROACH  - Combine re.compile() with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nimport re\n# Module scope: compile once, reuse forever.\nUSER_TAG = re.compile(r\"@(?P<user>\\w+)\")\nISO_DATE = re.compile(r\"\\b(?P<y>\\d{4})-(?P<m>\\d{2})-(?P<d>\\d{2})\\b\")\nIPV4     = re.compile(r\"\\b(?:\\d{1,3}\\.){3}\\d{1,3}\\b\")\nHEADER   = re.compile(r\"^(?P<key>[\\w-]+):\\s*(?P<value>.+?)\\s*$\", re.MULTILINE)\ndef mentions(text: str) -> list[str]:\n    return [m[\"user\"] for m in USER_TAG.finditer(text)]\ndef parse_headers(blob: str) -> dict[str, str]:\n    return {m[\"key\"]: m[\"value\"] for m in HEADER.finditer(blob)}\n# Inspect for debugging:\nprint(USER_TAG.pattern)                # source string\nprint(USER_TAG.groups)                 # number of groups\nprint(USER_TAG.groupindex)             # {'user': 1}"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of re.compile() — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\nfrom __future__ import annotations\nimport re\nfrom typing import Final\n# 1) Verbose, commented patterns -- shipped with re.VERBOSE OR inline (?x).\nISO_DATETIME: Final = re.compile(\n    r\"\"\"(?x)                          # verbose mode\n    \\A\n    (?P<y>\\d{4})-(?P<m>\\d{2})-(?P<d>\\d{2})    # date\n    [T\\s]                              # separator\n    (?P<H>\\d{2}):(?P<M>\\d{2}):(?P<S>\\d{2})    # time\n    (?:\\.(?P<frac>\\d+))?               # optional fractional seconds\n    (?P<tz>Z|[+-]\\d{2}:?\\d{2})?         # optional offset\n    \\Z\n    \"\"\",\n)\n# 2) Inline flags scoped to a subpattern -- e.g., case-insensitive ONLY for a tag.\nTAG_CI: Final = re.compile(r\"<(?i:script|style)>\")\n# 3) Patterns as a typed catalog -- modules import the constant, not the string.\nclass Patterns:\n    USER:      Final = re.compile(r\"@(\\w+)\")\n    HASHTAG:   Final = re.compile(r\"#(\\w+)\")\n    URL:       Final = re.compile(r\"https?://\\S+\")\n    UUID:      Final = re.compile(\n        r\"\\A[\\dA-Fa-f]{8}-[\\dA-Fa-f]{4}-\"\n        r\"[\\dA-Fa-f]{4}-[\\dA-Fa-f]{4}-\"\n        r\"[\\dA-Fa-f]{12}\\Z\"\n    )\n# 4) Test the patterns directly -- they're values, not strings.\ndef test_iso_datetime() -> None:\n    m = ISO_DATETIME.match(\"2026-04-30T12:34:56.789Z\")\n    assert m is not None\n    assert m[\"y\"] == \"2026\" and m[\"tz\"] == \"Z\"\n    assert ISO_DATETIME.match(\"not a date\") is None\n# 5) Benchmark before assuming compile() helps:\n#    For SHORT patterns reused < ~5 times in tight code, the 're' module's\n#    LRU cache already amortizes parsing. compile() pays off when:\n#      - the pattern is long / VERBOSE (parsing cost),\n#      - you reuse it across many callers,\n#      - you reuse the Pattern object (not the source string).\n# Decision rule:\n#   pattern reused more than once         -> re.compile + UPPER_CASE module constant\n#   pattern is long / annotated           -> re.VERBOSE or (?x); ship comments inside the pattern\n#   flag should apply to a SUB-pattern    -> inline group flag (?i:...) (Python 3.6+)\n#   pattern parameterized at runtime      -> compile inside the helper, cache via functools.lru_cache\n#   patterns scattered across files       -> centralize in patterns.py with type Final\n#   regression-prone pattern              -> write tests against canonical strings; assert .groupindex\n#\n# Anti-pattern: 'p = re.compile(...)' inside a hot loop. Each iteration burns\n# the parsing cost; the cache helps only if you stayed inside re.search()\n# semantics. Compile module-globally, or memoize via lru_cache when the\n# pattern is constructed dynamically."
                  }
        ],
        tips: [
                  "Compile patterns at module level (outside functions) — compute once, reuse everywhere.",
                  "The `re` module already caches recently-used patterns, so the speed win matters most when you have many distinct patterns or a long/VERBOSE pattern (parsing cost).",
                  "Use descriptive names: DATE_PATTERN, EMAIL_PATTERN — much clearer than anonymous r\"\\d{4}...\".",
                  "Compiled patterns remember flags — case_insensitive_pattern doesn't require re.IGNORECASE on each call.",
                  "For long patterns, use re.VERBOSE (or inline (?x)) and ship comments INSIDE the pattern; scope a flag to a sub-pattern with (?i:...). Centralize patterns in a typed Final catalog and write tests against canonical positive + negative strings."
        ],
        mistake: "Compiling the same pattern inside a loop or function every time it's called — defeats the purpose. Compile once at module level.",
        shorthand: {
          verbose: "for email in emails:\n    if re.search(r\"[\\w.+-]+@[\\w.-]+\\.\\w{2,}\", email):\n        print(email)",
          concise: "pattern = re.compile(r\"[\\w.+-]+@[\\w.-]+\\.\\w{2,}\")\nfor email in emails:\n    if pattern.search(email):\n        print(email)",
        },
      },
    ],
  },

  // ── Section 2: Groups & Captures ─────────────────────────────────────────
  {
    id: "regex-groups",
    title: "Groups & Captures",
    entries: [
      {
        id: "re-groups",
        fn: "Capture groups, named groups, match.group(), match.groups()",
        desc: "Extract parts of a match using groups.",
        category: "Regex",
        subtitle: "() for groups, (?P<name>) for named groups, .group() and .groups()",
        signature: "re.search(r\"(\\w+)@(\\w+)\", text).group(1)  |  match.groups()  |  match.group(\"name\")",
        descLong: "Parentheses () create capture groups — they extract parts of a match. Numbered groups: \\1, \\2... or match.group(1), match.group(2)... Named groups (?P<name>...) are more readable: (?P<domain>\\w+). match.group(0) or match.group() returns the entire match. match.groups() returns a tuple of all groups. Nesting works: (()). Groups enable sophisticated data extraction from structured text.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Capture groups, named groups, match.group(), match.groups() — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport re\nm = re.search(r\"(\\w+)\\s+\\((\\d+)\\)\", \"Alice (30)\")\nprint(m.group(0))   # 'Alice (30)'    -- the whole match\nprint(m.group(1))   # 'Alice'          -- first group\nprint(m.group(2))   # '30'             -- second group\nprint(m.groups())   # ('Alice', '30')  -- all groups as a tuple"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Capture groups, named groups, match.group(), match.groups() — common patterns you'll see in production.\n# APPROACH  - Combine Capture groups, named groups, match.group(), match.groups() with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nimport re\nLOG = re.compile(r\"(?P<ts>\\d{4}-\\d{2}-\\d{2})\\s+(?P<level>\\w+)\\s+(?P<msg>.+)\")\nm = LOG.search(\"2026-04-30 ERROR disk full\")\nprint(m[\"ts\"], m[\"level\"])             # '2026-04-30' 'ERROR'\nprint(m.groupdict())                    # {'ts': ..., 'level': ..., 'msg': ...}\n# Optional groups -- handle None at the boundary.\nPHONE = re.compile(r\"(?P<cc>\\+\\d{1,3})?[-\\s]?(?P<num>\\d{7,12})\")\nparts = PHONE.match(\"+1-5551234567\").groupdict(default=\"\")\nprint(parts)                            # {'cc': '+1', 'num': '5551234567'}\n# Direct unpack via groupdict() into a dataclass.\nfrom dataclasses import dataclass\n@dataclass\nclass LogEvt:\n    ts: str; level: str; msg: str\nevt = LogEvt(**LOG.search(\"2026-04-30 ERROR disk full\").groupdict())\nprint(evt.level)"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Capture groups, named groups, match.group(), match.groups() — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\nfrom __future__ import annotations\nimport re\nfrom dataclasses import dataclass\nfrom typing import Self\n# 1) Typed record built from a Match -- isolates parsing in one place.\n@dataclass(frozen=True, slots=True)\nclass HTTPLog:\n    ip: str\n    user: str\n    ts: str\n    method: str\n    path: str\n    status: int\n    bytes_: int\n    _RE = re.compile(r\"\"\"\n        ^\n        (?P<ip>\\S+)\\s+-\\s+(?P<user>\\S+)\\s+\n        \\[(?P<ts>[^\\]]+)\\]\\s+\n        \"(?P<method>[A-Z]+)\\s+(?P<path>\\S+)\\s+HTTP/[\\d.]+\"\\s+\n        (?P<status>\\d{3})\\s+(?P<bytes_>\\d+|-)\n    \"\"\", re.VERBOSE)\n    @classmethod\n    def parse(cls, line: str) -> Self | None:\n        m = cls._RE.match(line)\n        if not m: return None\n        gd = m.groupdict()\n        return cls(\n            ip     = gd[\"ip\"],\n            user   = gd[\"user\"],\n            ts     = gd[\"ts\"],\n            method = gd[\"method\"],\n            path   = gd[\"path\"],\n            status = int(gd[\"status\"]),\n            bytes_ = 0 if gd[\"bytes_\"] == \"-\" else int(gd[\"bytes_\"]),\n        )\n# 2) Tag-which-alternative-fired with .lastgroup (named alternations only).\nTOKEN = re.compile(r\"\"\"\n    (?P<num>\\d+)\n    | (?P<word>[A-Za-z_]\\w*)\n    | (?P<sym>[+\\-*/=])\n\"\"\", re.VERBOSE)\ndef lex(s: str) -> list[tuple[str, str]]:\n    return [(m.lastgroup, m.group()) for m in TOKEN.finditer(s) if m.lastgroup]\n# 3) Conflict-free naming: branches with the SAME group name share that one\n#    capture across branches. mypy-style \"discriminated union\" is the lex pattern above.\n# Decision rule:\n#   <=2 groups in a one-off              -> numbered groups are fine\n#   parser / data extraction              -> named groups, period\n#   need typed output                    -> dataclass.parse(line) -> Self | None\n#   alternation, want to know which       -> Match.lastgroup with named branches\n#   optional captures                    -> groupdict(default=\"\") OR explicit None handling\n#   nested groups                        -> count by '(' that are NOT '(?:' or '(?='\n#                                          (named groups (?P<x>...) DO count)\n#   regex too complex to read             -> re.VERBOSE + comments inside the pattern\n#\n# Anti-pattern: extracting fields with positional groups, then accessing by\n# index 4 layers later. The pattern grows, you insert a group, every index\n# downstream is off by one and tests \"still pass\" because the strings happen\n# to look right. Use named groups; let renames stay local."
                  }
        ],
        tips: [
                  "Named groups are self-documenting — m.group(\"age\") is clearer than m.group(2).",
                  "m.groupdict() returns a dict of named groups — perfect for unpacking: {name, age, city} = m.groupdict()",
                  "Nested groups work: ((\\w+)@(\\w+)) has 3 groups: entire part, domain, TLD.",
                  "m.group(0) and m.group() are identical — both return the entire match.",
                  "Wrap parsing in a `@classmethod parse(cls, line) -> Self | None` on a frozen dataclass so the regex stays local and the rest of the codebase deals in typed records, not dict[str, str]. For named alternations, Match.lastgroup tells you which branch fired."
        ],
        mistake: "Extracting fields with positional groups, then accessing by index 4 layers later — the pattern grows, you insert a group, every index downstream is off by one and tests \"still pass\" because the strings happen to look right. Use named groups; let renames stay local.",
        shorthand: {
          verbose: "import re\ntext = \"Alice 30\"\nname = text.split()[0]\nage = text.split()[1]",
          concise: "import re\nm = re.search(r\"(?P<name>\\w+)\\s+(?P<age>\\d+)\", text)\nname, age = m.group(\"name\"), m.group(\"age\")",
        },
      },
      {
        id: "re-non-capturing",
        fn: "Non-capturing groups, lookahead, lookbehind",
        desc: "Match patterns without capturing, or assert conditions without consuming characters.",
        category: "Regex",
        subtitle: "(?:...) non-capturing, (?=...) positive lookahead, (?!...) negative lookahead, (?<=...) lookbehind",
        signature: "(?:...) non-capturing  |  (?=foo) lookahead  |  (?<=foo) lookbehind (fixed width)",
        descLong: "Non-capturing groups (?:...) group patterns for alternation or quantifiers without creating a numbered group. Lookahead (?=...) and (?!...) assert that what follows matches/doesn't match, without consuming it. Lookbehind (?<=...) and (?<!...) assert what precedes matches/doesn't match. Lookbehind is fixed-width in Python. These are zero-width assertions — useful for conditional matching and complex patterns.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Non-capturing groups, lookahead, lookbehind — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport re\n# Non-capturing group: alternation without polluting m.groups().\nm = re.search(r\"(?:hello|hi)\\s+(world|universe)\", \"hi universe\")\nprint(m.groups())              # ('universe',) -- only the second group counts\n# Positive lookahead: 'foo' immediately followed by 'bar', without consuming 'bar'.\nprint(re.search(r\"foo(?=bar)\", \"foobar\").group())   # 'foo'\nprint(re.search(r\"foo(?=bar)\", \"foobaz\"))            # None"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Non-capturing groups, lookahead, lookbehind — common patterns you'll see in production.\n# APPROACH  - Combine Non-capturing groups, lookahead, lookbehind with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nimport re\n# Money values without the $ sign.\nprint(re.findall(r\"(?<=\\$)\\d+(?:\\.\\d{2})?\", \"$100, $42.50, then 7 (free)\"))\n# ['100', '42.50']\n# Numbers that are NOT negative (negative lookbehind for '-').\nprint(re.findall(r\"(?<![-\\d])\\d+\", \"score: 5, change: -3, level: 12\"))\n# ['5', '12']\n# CSS class extraction -- the period isn't part of the class name.\ncss = \".btn.primary {color:red} .btn.secondary{}\"\nprint(re.findall(r\"(?<=\\.)[a-zA-Z][\\w-]*\", css))\n# ['btn', 'primary', 'btn', 'secondary']\n# Combined: words that are NOT 'the' (case-insensitive).\nprint(re.findall(r\"\\b(?!the\\b)\\w+\", \"the cat and The dog\", flags=re.IGNORECASE))\n# ['cat', 'and', 'dog']"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Non-capturing groups, lookahead, lookbehind — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\nfrom __future__ import annotations\nimport re\n# 1) ReDoS-defusing rewrite: nested quantifiers blow up.\n#    Bad:  r\"(a+)+$\"           -> exponential on \"aaaaaab\"\n#    Good: r\"(?>a+)$\"          -> atomic group; engine never re-enters the alternative\n#    Or:   r\"(a++)+$\"          -> possessive; same idea, different syntax\n#  Stdlib 're' supports NEITHER. Use the 'regex' module:\n#  import regex\n#  regex.search(r\"(?>a+)$\", text, timeout=0.05)\n# 2) Variable-width lookbehind in 'regex' (still not in stdlib):\n#  regex.search(r\"(?<=https?://)[^/]+\", \"see http://x and https://example.org\")\n# 3) Anchor-based extraction without consuming:\n#    \"URLs that come after the word 'see'\":\ndef urls_after_see(text: str) -> list[str]:\n    # \\K resets the match start; supported in 'regex' only.\n    import regex\n    return regex.findall(r\"\\bsee\\s+\\K\\bhttps?://\\S+\", text)\n# 4) \"Followed by AND not followed by\" -- chain lookarounds:\n#    'class=' followed by a value but NOT immediately by 'private'.\nNOT_PRIVATE = re.compile(r'class=\"(?=\\w)(?!private\\b)([^\"]+)\"')\n# 5) Lookbehind for delimiter trimming -- find dollar amounts but emit just the digits.\n#    Stdlib version (fixed-width):\ndef dollar_amounts(text: str) -> list[str]:\n    return re.findall(r\"(?<=\\$)\\d+(?:\\.\\d{2})?\", text)\n# 6) Word-boundary tightening: \\b doesn't see Unicode quotes.\n#    re.compile(r\"(?<!\\w)foo(?!\\w)\") replaces \\bfoo\\b when text contains\n#    custom punctuation that \\w doesn't classify as a non-word char.\n# Decision rule:\n#   group without capture                      -> (?:...)\n#   \"X right after Y\" without taking Y          -> (?<=Y)X\n#   \"X NOT preceded by Y\"                      -> (?<!Y)X\n#   \"X right before Y\"                         -> X(?=Y)\n#   \"X NOT followed by Y\"                      -> X(?!Y)\n#   variable-width lookbehind                  -> 'regex' module (stdlib forbids)\n#   exponential pattern danger                  -> atomic group (?>...) / possessive (?+, ?+, ?+) -- 'regex' module\n#   'reset' the match start (skip a prefix)    -> \\K, 'regex' module\n#   pattern compiled from user input           -> reject lookarounds, OR use 'regex' with timeout=\n#\n# Anti-pattern: 'wrap any nested quantifier in (?:...)'. Non-capturing doesn't\n# fix backtracking. r\"(?:a+)+$\" is just as exponential as r\"(a+)+$\". Atomic\n# groups, possessive quantifiers, or rewriting the pattern are the real fixes."
                  }
        ],
        tips: [
                  "Non-capturing groups (?:...) reduce group count — useful when you want alternation but don't need to extract it.",
                  "Lookahead/lookbehind don't consume characters — great for checking conditions without shifting the match position.",
                  "Lookbehind must be fixed-width in Python (no quantifiers) — (?<=[0-9]{1,3}) fails, use (?<=[0-9]{3}). The third-party `regex` module supports variable-width lookbehind plus \\K for resetting the match start.",
                  "Negative lookahead (?!...) is powerful for exclusion patterns — find words not containing a substring."
        ],
        mistake: "Wrapping a nested quantifier in (?:...) and thinking you've \"fixed\" backtracking — non-capturing doesn't change anything. r\"(?:a+)+$\" is just as exponential as r\"(a+)+$\". The real fix is atomic groups (?>...) or possessive quantifiers via the `regex` module, or rewriting the pattern flat. Never compile lookaround patterns from user input without a timeout.",
        shorthand: {
          verbose: "import re\ntext = \"The cat and the dog\"\nwords = re.findall(r\"\\w+\", text)\nresult = [w for w in words if w.lower() != \"the\"]",
          concise: "import re\nresult = re.findall(r\"\\b(?!the\\b)\\w+\", text, re.IGNORECASE)",
        },
      },
      {
        id: "re-backreferences",
        fn: "Backreferences in patterns and replacements",
        desc: "Refer to captured groups within the same pattern or in substitutions.",
        category: "Regex",
        subtitle: "\\1, \\g<name> in patterns; \\1, \\g<name> in replacements",
        signature: "pattern: r\"(\\w+)\\s+\\1\"  |  replacement: r\"\\1\\2\"  or r\"\\g<name>\"",
        descLong: "Backreferences allow you to match the same text that was previously captured. In patterns, \\1 refers to group 1, \\2 to group 2, etc. In replacements, \\1 and \\g<1> both work; for named groups use \\g<name>. This is powerful for finding repeated words, matching balanced pairs, or swapping matched groups. Python regex backreferences are numbered, not named, in the pattern itself — use \\g<name> for named group backreferences.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Backreferences in patterns and replacements — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport re\n# Find doubled words (\"the the\").\nprint(re.findall(r\"\\b(\\w+)\\s+\\1\\b\", \"The the quick fox fox jumps\"))\n# ['the', 'fox']\n# Swap two words.\nprint(re.sub(r\"(\\w+)\\s+(\\w+)\", r\"\\2 \\1\", \"Alice Bob Charlie David\"))\n# 'Bob Alice David Charlie'"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Backreferences in patterns and replacements — common patterns you'll see in production.\n# APPROACH  - Combine Backreferences in patterns and replacements with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nimport re\n# Match a quoted string with the SAME opening and closing quote.\nQUOTED = re.compile(r\"(?P<q>['\\\"])(.*?)(?P=q)\")\nprint(QUOTED.findall('he said \"hi\" and \\'bye\\''))\n# [('\"', 'hi'), (\"'\", 'bye')]\n# Collapse three-or-more repeats to two (\"aaaaa\" -> \"aa\") via backref.\nprint(re.sub(r\"(.)\\1{2,}\", r\"\\1\\1\", \"aaaaaa bbb c\"))\n# 'aa bb c'\n# Replace \"First Last\" with \"LAST, First\" using named groups.\ntext = \"Alice Smith and Bob Jones\"\nprint(re.sub(\n    r\"(?P<first>\\w+)\\s+(?P<last>\\w+)\",\n    lambda m: f\"{m['last'].upper()}, {m['first']}\",\n    text,\n))\n# 'SMITH, Alice and JONES, Bob'\n# Patterns + replacement together: dedupe consecutive whitespace AND newlines.\nprint(re.sub(r\"(\\s)\\1+\", r\"\\1\", \"a   b\\n\\n\\nc\"))\n# 'a b\\nc'"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Backreferences in patterns and replacements — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\nfrom __future__ import annotations\nimport re\n# 1) Matching tag pairs at ONE level (NOT nested). 'regex' module supports\n#    recursive patterns (?R); stdlib does not. Be explicit about the limit.\nTAG_PAIR = re.compile(r\"<(?P<t>[a-zA-Z][\\w-]*)\\b[^>]*>(?P<body>.*?)</(?P=t)>\", re.DOTALL)\n# Works for one level; doesn't support <a><a></a></a> properly. For real HTML,\n# use html.parser or BeautifulSoup -- not regex.\n# 2) Quoted-string lexer: backref keeps quotes balanced.\nSTRING = re.compile(r\"\"\"\n    (?P<q>['\"])              # opening quote\n    (?P<val>(?:\\\\.|(?!(?P=q)).)*)  # body: escapes or non-quote chars\n    (?P=q)                    # SAME quote closes\n\"\"\", re.VERBOSE)\ndef quoted_strings(s: str) -> list[str]:\n    return [m[\"val\"] for m in STRING.finditer(s)]\n# 3) Replacement-side: callables beat \\g templates when the transform is non-trivial.\n#    Backrefs in r\"...\" replacements interpret \\g<name>, \\1, \\\\, etc. -- a callable\n#    avoids that minefield AND lets you raise on invalid input.\ndef fix_phone(s: str) -> str:\n    PHONE = re.compile(r\"(?P<a>\\d{3})[.\\s-]?(?P<b>\\d{3})[.\\s-]?(?P<c>\\d{4})\")\n    def fmt(m: re.Match[str]) -> str:\n        return f\"({m['a']}) {m['b']}-{m['c']}\"\n    return PHONE.sub(fmt, s)\n# 4) Conditional pattern (?(name)yes|no) -- \"if group 'name' matched, expect X, else Y\".\n#    Stdlib supports it. Useful for optional prefixes whose presence implies a suffix.\nSCHEMED = re.compile(r\"(https?://)?(www\\.)?(?P<host>[\\w.-]+)(?(2)/|\\b)\")\n# Reads: an optional protocol, optional 'www.', a host, then if 'www.' was\n# matched (group 2) require a slash, else a word boundary.\n# Decision rule:\n#   detect duplicates / repeats              -> (\\w+)\\s+\\1   in the pattern\n#   matching delimiter pair (quotes, tags)   -> (?P<q>['\"])...(?P=q)   single-level only\n#   swap fields                              -> r\"\\2 \\1\" replacement, OR a callable\n#   transform with logic / formatting        -> CALLABLE replacement, NEVER raw r\"\\g<...>\" + f-string\n#   recursive / balanced structures          -> NOT regex; use a parser (lark, pyparsing, ast, html.parser)\n#   \"if optional prefix, then...\" rules      -> (?(group)yes|no) conditional pattern\n#   user-supplied text in the replacement    -> callable; replacement strings interpret \\1\n#\n# Anti-pattern: trying to parse JSON / HTML / SQL / nested parens with backrefs.\n# Backreferences make a regex non-regular but still cannot recognize balanced\n# nesting beyond a fixed depth. Use a real parser; you'll spend less time on\n# the regex than on debugging the cases it handles 90% of."
                  }
        ],
        tips: [
                  "Backreferences in patterns (\\1, \\2) find repeated captured text — perfect for duplicate detection.",
                  "Backreferences in replacements: \\1 or \\g<1> for numbered groups; \\g<name> for named groups.",
                  "Backreferences only work if the group actually matched — for optional groups, use \\1? or handle None.",
                  "Use (?P=name) to backreference a named group within the pattern itself; conditional pattern (?(name)yes|no) means \"if group name matched, expect X, else Y\"."
        ],
        mistake: "Trying to parse JSON / HTML / SQL / nested parens with backrefs — backreferences make a regex non-regular but still cannot recognize balanced nesting. Use a real parser (lark, pyparsing, ast, html.parser); you'll spend less time than debugging the cases regex handles 90% of.",
        shorthand: {
          verbose: "import re\ntext = \"Alice Bob\"\nparts = text.split()\nresult = f\"{parts[1]} {parts[0]}\"",
          concise: "import re\nresult = re.sub(r\"(\\w+)\\s+(\\w+)\", r\"\\2 \\1\", text)",
        },
      },
    ],
  },

  // ── Section 3: Patterns & Flags ─────────────────────────────────────────
  {
    id: "regex-patterns",
    title: "Patterns & Flags",
    entries: [
      {
        id: "re-character-classes",
        fn: "Character classes, quantifiers, escapes",
        desc: "Build patterns with character classes, quantifiers, and escape sequences.",
        category: "Regex",
        subtitle: "\\d \\w \\s [a-z] [^...] * + ? {n,m} . ^ $",
        signature: "re.search(r\"\\d+\", string)  |  re.search(r\"[a-zA-Z_]\\w*\", string)",
        descLong: "\\d matches digits 0-9; \\w matches word characters (letters, digits, underscore); \\s matches whitespace. Character classes [a-z] match ranges, [abc] match specific chars, [^abc] match NOT those chars. Quantifiers: * (0+), + (1+), ? (0-1), {n} (exactly n), {n,m} (n to m). . matches any char except newline. ^ anchors to start, $ to end. \\b word boundary. Escape special chars with backslash.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Character classes, quantifiers, escapes — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport re\nprint(re.findall(r\"\\d+\", \"42 apples, 17 oranges\"))   # ['42', '17']\nprint(re.findall(r\"\\w+\", \"hello_world-123\"))          # ['hello_world', '123']\nprint(re.split(r\"\\s+\", \"one   two\\tthree\"))         # ['one','two','three']\n# Range and negation:\nprint(re.findall(r\"[A-Z]\", \"Hello World\"))             # ['H','W']\nprint(re.findall(r\"[^0-9]+\", \"abc123def\"))             # ['abc', 'def']\n# Counts and anchors:\nprint(re.findall(r\"a{2,4}\", \"a aa aaa aaaaa\"))         # ['aa','aaa','aaaa']\nprint(re.search(r\"world$\", \"hello world\"))             # <Match>"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Character classes, quantifiers, escapes — common patterns you'll see in production.\n# APPROACH  - Combine Character classes, quantifiers, escapes with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nimport re\n# Greedy vs lazy.\nhtml = \"<a><b><c>\"\nprint(re.findall(r\"<.+>\",  html))     # ['<a><b><c>']  -- one big greedy match\nprint(re.findall(r\"<.+?>\", html))     # ['<a>', '<b>', '<c>']  -- lazy, per-tag\nprint(re.findall(r\"<[^>]+>\", html))   # ['<a>', '<b>', '<c>']  -- explicit; faster than lazy\n# Bracket-class shortcuts inside [].\nprint(re.findall(r\"[\\w.-]+\", \"a.b_c-d\"))     # ['a.b_c-d']\nprint(re.findall(r\"[^\\s,]+\",  \"a, b, c\"))     # ['a', 'b', 'c']\n# Quantifier discipline.\nprint(re.findall(r\"\\d{3,4}\", \"12 345 6789\"))           # ['345', '6789']\nprint(re.findall(r\"colou?r\",  \"color and colour\"))      # ['color', 'colour']\n# Word boundary vs anchors.\nprint(re.findall(r\"\\bcat\\b\", \"cat catalog cats scat\")) # ['cat']\n# Escape what regex steals: . * + ? ( ) [ ] { } | \\ ^ $ /\nprint(re.findall(r\"\\$\\d+\\.\\d{2}\", \"items: $9.99 and $42.00\"))\n# ['$9.99', '$42.00']"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Character classes, quantifiers, escapes — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\nfrom __future__ import annotations\nimport re\n# 1) Replace lazy '.+?' with negated class '[^X]+' wherever possible -- it\n#    eliminates a huge class of backtracking pathologies.\n#    HTML attribute value (DON'T parse HTML with regex in production):\nATTR = re.compile(r'(\\w+)=\"([^\"]*)\"')\nprint(ATTR.findall('a=\"1\" b=\"hello world\" c=\"\"'))\n# [('a', '1'), ('b', 'hello world'), ('c', '')]\n# 2) Anchor + class for \"balanced delimiter\" matching:\n#    quoted string with escapes -- linear in the number of input characters.\nQUOTED = re.compile(r'\"([^\"\\\\]*(?:\\\\.[^\"\\\\]*)*)\"')\nprint(QUOTED.findall(r'\"hi\" and \"say \\\"yes\\\"\" and \"x\"'))\n# ['hi', 'say \\\\\"yes\\\\\"', 'x']\n# 3) Unicode-aware classes via the 'regex' module (\\p{Letter}, \\p{Cf}, \\p{So}).\n#    Stdlib has \\w/\\W and re.UNICODE (default in Py3) but no Unicode categories.\ndef is_emoji_run(s: str) -> bool:\n    try:\n        import regex\n        return regex.fullmatch(r\"\\p{So}+\", s) is not None\n    except ImportError:\n        # Stdlib fallback -- ranges drift with each Unicode update.\n        return all(0x1F300 <= ord(c) <= 0x1FAFF or 0x2600 <= ord(c) <= 0x27BF\n                   for c in s)\n# 4) Build patterns from fixed strings safely with re.escape.\ndef any_of(words: list[str]) -> re.Pattern[str]:\n    return re.compile(r\"\\b(?:\" + \"|\".join(re.escape(w) for w in sorted(words, key=len, reverse=True)) + r\")\\b\",\n                      re.IGNORECASE)\n# longest-first sort prevents 'foo' shadowing 'foobar' in the alternation.\n# 5) Catastrophic-backtracking defusal.\n#    Bad:  r\"^(a+)+$\"     on \"aaaaaaaaaab\" -- exponential\n#    Good (single greedy): r\"^a+$\"\n#    Good (separate suffix): r\"^a+b?$\"\n#  If you NEED the nested structure, atomic group via 'regex': r\"^(?>a+)+$\"\n# Decision rule:\n#   \"any chars between two delims\"     -> [^delim]+   (NOT .*?)\n#   precise count                      -> {n} or {n,m}\n#   optional thing                     -> ? on the whole subpattern OR alternation\n#   word match                         -> \\b...\\b   (Unicode-aware in Py3)\n#   Unicode property classes           -> 'regex' module + \\p{...}\n#   user-supplied literal              -> re.escape() before insertion\n#   nested quantifier                  -> rewrite to flat OR atomic group ('regex')\n#   matching across newlines (HTML/JSON-ish) -> [\\s\\S]+? OR re.DOTALL; usually wrong tool, prefer a parser\n#\n# Anti-pattern: '.+?' as the universal \"match anything between\" pattern. It\n# works but leaves the engine doing maximal backtracking on every character.\n# Replace with '[^X]+' (where X is the delimiter) for linear-time matching."
                  }
        ],
        tips: [
                  "\\w is [a-zA-Z0-9_] — includes underscore, useful for identifiers.",
                  "[^...] negation matches anything NOT in the class — [^0-9] matches non-digits.",
                  "{n,m} is more precise than * or +: {2,4} matches 2-4 occurrences.",
                  ". matches any char except newline — use re.DOTALL to include newlines.",
                  "Prefer the negated class `[^X]+` over lazy `.+?` for \"any chars between two delimiters\" — eliminates a huge class of backtracking pathologies and runs in linear time. When building alternations from fixed strings, sort longest-first so \"foo\" doesn't shadow \"foobar\"."
        ],
        mistake: "`.+?` as the universal \"match anything between\" pattern — it works but leaves the engine doing maximal backtracking on every character. Replace with `[^X]+` (X = the delimiter) for linear-time matching. Also: forgetting to escape special regex chars — . matches any char, not a literal dot.",
        shorthand: {
          verbose: "import re\ntext = \"The price is $19.99\"\nfor i, char in enumerate(text):\n    if char.isdigit():\n        print(char)",
          concise: "import re\ndigits = re.findall(r\"\\d\", text)",
        },
      },
      {
        id: "re-flags",
        fn: "re.IGNORECASE, re.MULTILINE, re.DOTALL, re.VERBOSE",
        desc: "Control regex behavior with flags.",
        category: "Regex",
        subtitle: "Case-insensitive, multiline mode, dot matches newlines, verbose/comments",
        signature: "re.search(pattern, string, flags=re.IGNORECASE)  |  re.IGNORECASE | re.MULTILINE",
        descLong: "re.IGNORECASE (re.I) — case-insensitive matching. re.MULTILINE (re.M) — ^ and $ match line boundaries, not just string start/end. re.DOTALL (re.S) — . matches newlines too. re.VERBOSE (re.X) — allow whitespace and # comments in pattern for readability. Flags can be combined with |. Also specify inline: (?i) for IGNORECASE, (?m) for MULTILINE, (?s) for DOTALL, (?x) for VERBOSE.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of re.IGNORECASE, re.MULTILINE, re.DOTALL, re.VERBOSE — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport re\ntext = \"Hello HELLO hello\"\nprint(re.findall(r\"hello\", text, re.IGNORECASE))   # ['Hello','HELLO','hello']\nmulti = \"start a\\nstart b\\nend\"\nprint(re.findall(r\"^start\", multi, re.MULTILINE))  # ['start', 'start']\nprint(re.search(r\"hello.world\", \"hello\\nworld\"))             # None\nprint(re.search(r\"hello.world\", \"hello\\nworld\", re.DOTALL))  # <Match>"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of re.IGNORECASE, re.MULTILINE, re.DOTALL, re.VERBOSE — common patterns you'll see in production.\n# APPROACH  - Combine re.IGNORECASE, re.MULTILINE, re.DOTALL, re.VERBOSE with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nimport re\n# Compile with combined flags.\nEMAIL = re.compile(\n    r\"\\b[\\w.+-]+@[\\w.-]+\\.\\w{2,}\\b\",\n    re.IGNORECASE | re.ASCII,                    # ASCII keeps \\w out of full Unicode\n)\n# Per-subpattern flag with (?flag:...).\nTAG_CI = re.compile(r\"<(?i:script|style)\\b[^>]*>\")\nprint(TAG_CI.search(\"<SCRIPT>\"))                  # match\nprint(TAG_CI.search(\"<DIV>\"))                     # None\n# VERBOSE for legibility.\nDATETIME = re.compile(r\"\"\"\n    \\A\n    (?P<y>\\d{4}) - (?P<m>\\d{2}) - (?P<d>\\d{2})\n    [T\\s]\n    (?P<H>\\d{2}) : (?P<M>\\d{2}) : (?P<S>\\d{2})\n    \\Z\n\"\"\", re.VERBOSE)\n# Inline-on / inline-off (legacy syntax (?-i)) -- prefer scoped (?i:...).\nprint(re.search(r\"(?i)foo(?-i:Bar)\", \"fooBar\"))   # match\nprint(re.search(r\"(?i)foo(?-i:Bar)\", \"FOOBAR\"))   # None"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of re.IGNORECASE, re.MULTILINE, re.DOTALL, re.VERBOSE — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\nfrom __future__ import annotations\nimport re\n# 1) Protocol fields: re.ASCII pins \\w/\\d/\\b to ASCII semantics.\nUSERNAME = re.compile(r\"\\A[a-z][a-z0-9_]{2,31}\\Z\", re.ASCII)\nID_TOKEN = re.compile(r\"\\A[\\w-]{16,64}\\Z\", re.ASCII)\n# 2) Multi-line patterns: \\A and \\Z stay anchored to the WHOLE string even\n#    under re.MULTILINE; only ^ and $ change to per-line semantics.\nHEADERS = re.compile(r\"^(?P<key>[\\w-]+):\\s*(?P<value>.+?)\\s*$\", re.MULTILINE)\ndef parse_headers(blob: str) -> dict[str, str]:\n    return {m[\"key\"]: m[\"value\"] for m in HEADERS.finditer(blob)}\n# 3) DOTALL for \"blob between markers\" -- but prefer [\\s\\S]+? if mixing with others.\nSECTION = re.compile(r\"^---\\s*$(?P<body>.*?)^---\\s*$\",\n                     re.MULTILINE | re.DOTALL)\n# 4) re.DEBUG prints the parsed AST; useful when a pattern misbehaves.\ndef explain(pattern: str, flags: int = 0) -> None:\n    re.compile(pattern, flags | re.DEBUG)\n# explain(r\"(?P<y>\\d{4})-(\\d{2})\")  -- shows literal/group nodes, anchors, etc.\n# 5) Flag combos worth memorizing:\n#    Validator:        re.ASCII (and never re.IGNORECASE alone)\n#    Header parser:    re.MULTILINE\n#    Multi-line block: re.MULTILINE | re.DOTALL  (^/$ per line, '.' over newlines)\n#    Long pattern:     re.VERBOSE  (or inline (?x))\n#    Hot CI debug:     re.DEBUG    (one-shot, NEVER ship)\n#    Bytes pattern:    plain bytes literals; ASCII-only \\w by default\n# 6) Common pitfalls and their fix:\n#    \"ignorecase doesn't ignore Turkish I\"   -> re.IGNORECASE is locale-naive;\n#                                                use casefold() on input then ASCII regex\n#    \"MULTILINE breaks my anchored validator\" -> use \\A...\\Z (always whole-string)\n#                                                instead of ^...$\n#    \"DOTALL eats too much\"                   -> negated class [^X]+ is usually\n#                                                what you actually wanted\n# Decision rule:\n#   ASCII-only protocol fields              -> re.ASCII + \\A...\\Z\n#   user-visible text                       -> default Unicode; consider casefold() before matching\n#   per-line ^/$                            -> re.MULTILINE\n#   . crosses newlines                      -> re.DOTALL  OR rewrite to [\\s\\S]+?\n#   long/verbose pattern                    -> re.VERBOSE (with comments)\n#   need a flag only on a sub-pattern       -> (?flag:...) inline scoped\n#   debug a parse                           -> re.DEBUG ONCE, then remove\n#   bytes vs str                            -> the pattern type must match the input type\n#\n# Anti-pattern: 'flags=re.IGNORECASE | re.MULTILINE' on every regex out of\n# habit. Each flag changes semantics in ways that bite: \\w widens, ^/$\n# moves, '.' grows. Reach for the SPECIFIC flag the pattern needs and\n# document why."
                  }
        ],
        tips: [
                  "re.VERBOSE lets you comment and format regex patterns across multiple lines — essential for complex patterns.",
                  "Inline flags (?i) are useful when you want to compile a pattern once with mixed case handling; (?i:...) scopes the flag to a sub-pattern only.",
                  "re.MULTILINE changes ^ and $ behavior — ^ matches after \\n, $ matches before \\n. \\A and \\Z stay anchored to the WHOLE string regardless, so prefer them for validators.",
                  "re.DOTALL makes . match \\n too — useful for matching across newlines in multi-line strings.",
                  "For protocol fields (email, username, hostname, UUID), pass re.ASCII — default Unicode \\w/\\d/\\b accept things like Khmer digits as \\d, which is almost never what you want."
        ],
        mistake: "`flags=re.IGNORECASE | re.MULTILINE` on every regex out of habit — each flag changes semantics in ways that bite (\\w widens, ^/$ moves, . grows). Reach for the SPECIFIC flag the pattern needs and document why. Also: re.MULTILINE doesn't make . match newlines — that's re.DOTALL.",
        shorthand: {
          verbose: "import re\ntext = \"Hello and HELLO\"\nresult = []\nfor word in text.split():\n    if word.lower() == \"hello\":\n        result.append(word)",
          concise: "import re\nresult = re.findall(r\"hello\", text, re.IGNORECASE)",
        },
      },
      {
        id: "re-common-patterns",
        fn: "Practical regex patterns",
        desc: "Ready-to-use patterns for common data: email, URL, phone, date, IP.",
        category: "Regex",
        subtitle: "Email, URL, phone number, ISO date, IPv4 address",
        signature: "re.findall(email_pattern, text)  |  re.match(phone_pattern, phone_num)",
        descLong: "Common regex patterns for validating and extracting real-world data. Email, URL, phone numbers, dates, and IP addresses each have standard formats. These patterns are starting points — production systems often use dedicated libraries (email_validator, validators, etc.) for robustness. But regex is lightweight and useful for quick validation, parsing, and extraction tasks.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Practical regex patterns — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport re\nEMAIL = r\"[\\w.+-]+@[\\w.-]+\\.\\w{2,}\"\nURL   = r\"https?://[\\w.-]+\\.\\w{2,}(?:/\\S*)?\"\nDATE  = r\"\\d{4}-\\d{2}-\\d{2}\"\nIPV4  = r\"\\b(?:\\d{1,3}\\.){3}\\d{1,3}\\b\"\ntext = \"ping alice@x.com on 2026-04-30 from 10.0.0.5; visit https://x.com/about\"\nprint(re.findall(EMAIL, text))   # ['alice@x.com']\nprint(re.findall(URL,   text))   # ['https://x.com/about']\nprint(re.findall(DATE,  text))   # ['2026-04-30']\nprint(re.findall(IPV4,  text))   # ['10.0.0.5']"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Practical regex patterns — common patterns you'll see in production.\n# APPROACH  - Combine Practical regex patterns with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nimport re\nimport ipaddress\nfrom datetime import date\nfrom urllib.parse import urlparse\n# 1) Extract candidates with regex (loose), then validate with stdlib.\ndef find_ips(text: str) -> list[str]:\n    out = []\n    for m in re.finditer(r\"\\b(?:\\d{1,3}\\.){3}\\d{1,3}\\b\", text):\n        try:\n            ipaddress.IPv4Address(m.group())          # 256.0.0.1 raises here\n            out.append(m.group())\n        except ValueError:\n            continue\n    return out\ndef find_dates(text: str) -> list[date]:\n    out: list[date] = []\n    for m in re.finditer(r\"\\b(\\d{4})-(\\d{2})-(\\d{2})\\b\", text):\n        try:\n            out.append(date(*map(int, m.groups())))   # 2026-02-30 raises here\n        except ValueError:\n            continue\n    return out\ndef find_urls(text: str) -> list[str]:\n    out = []\n    for m in re.finditer(r\"https?://\\S+\", text):\n        u = urlparse(m.group().rstrip(\".,);\"))         # strip trailing punctuation\n        if u.scheme in (\"http\", \"https\") and u.netloc:\n            out.append(u.geturl())\n    return out\n# 2) Common micro-patterns:\nHASHTAG = re.compile(r\"(?<!\\w)#(\\w+)\")        # #python in \"#python\", NOT in \"abc#python\"\nMENTION = re.compile(r\"(?<!\\w)@(\\w+)\")\nUUID    = re.compile(r\"\\A[\\dA-Fa-f]{8}-[\\dA-Fa-f]{4}-[\\dA-Fa-f]{4}-\"\n                     r\"[\\dA-Fa-f]{4}-[\\dA-Fa-f]{12}\\Z\")"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Practical regex patterns — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\nfrom __future__ import annotations\nimport re\nimport ipaddress\nfrom datetime import datetime\nfrom email.headerregistry import Address                # stdlib email parser\nfrom urllib.parse import urlparse\n# 1) Email -- regex finds candidates; 'email_validator' validates per RFC 5322\n#    + DNS check if you want it. 'email.headerregistry.Address' is stdlib and\n#    handles most of the syntactic checks for free.\nEMAIL_LOOSE = re.compile(r\"[^\\s,;<>]+@[^\\s,;<>]+\\.[^\\s,;<>]+\")\ndef emails(text: str) -> list[str]:\n    out = []\n    for m in EMAIL_LOOSE.finditer(text):\n        try:\n            addr = Address(addr_spec=m.group())\n            out.append(f\"{addr.username}@{addr.domain}\")\n        except Exception:\n            continue\n    return out\n# 2) URL -- urlparse handles RFC 3986; check scheme allowlist + netloc.\nURL_LOOSE = re.compile(r\"\\bhttps?://\\S+\")\ndef urls(text: str, *, schemes: set[str] = {\"http\", \"https\"}) -> list[str]:\n    out = []\n    for m in URL_LOOSE.finditer(text):\n        candidate = m.group().rstrip(\".,);'\\\"\")\n        u = urlparse(candidate)\n        if u.scheme in schemes and u.netloc and \".\" in u.netloc:\n            out.append(candidate)\n    return out\n# 3) IPs -- regex catches v4/v6 hex pairs; ipaddress.ip_address validates.\nIP_LOOSE = re.compile(r\"\\b(?:[0-9a-fA-F:]{2,39}|(?:\\d{1,3}\\.){3}\\d{1,3})\\b\")\ndef ips(text: str) -> list[ipaddress.IPv4Address | ipaddress.IPv6Address]:\n    out = []\n    for m in IP_LOOSE.finditer(text):\n        try: out.append(ipaddress.ip_address(m.group()))\n        except ValueError: continue\n    return out\n# 4) Datetimes -- regex narrows; datetime.fromisoformat (3.11+ accepts most ISO 8601)\n#    handles the actual parse including timezone offsets.\nISO_LOOSE = re.compile(\n    r\"\\b\\d{4}-\\d{2}-\\d{2}[T\\s]\\d{2}:\\d{2}:\\d{2}(?:\\.\\d+)?(?:Z|[+-]\\d{2}:?\\d{2})?\\b\"\n)\ndef datetimes(text: str) -> list[datetime]:\n    out: list[datetime] = []\n    for m in ISO_LOOSE.finditer(text):\n        try: out.append(datetime.fromisoformat(m.group().replace(\"Z\", \"+00:00\")))\n        except ValueError: continue\n    return out\n# 5) Phone numbers -- DON'T hand-roll. Use 'phonenumbers' (Google's libphonenumber).\n#    import phonenumbers\n#    for m in phonenumbers.PhoneNumberMatcher(text, \"US\"):\n#        ... use m.number ...\n# Decision rule:\n#   email                 -> regex narrows; email.headerregistry.Address OR email_validator validates\n#   URL                   -> regex narrows; urlparse + scheme allowlist\n#   IPv4 / IPv6           -> regex narrows; ipaddress.ip_address validates\n#   datetime              -> regex narrows; datetime.fromisoformat / dateutil parses\n#   phone                 -> NEVER regex-only; use 'phonenumbers'\n#   credit card / IBAN    -> regex for shape; Luhn / mod-97 in code; PCI scope -- prefer tokenization\n#   HTML / JSON / SQL     -> NEVER regex; use html.parser, json, sqlglot\n#\n# Anti-pattern: shipping a \"validates email\" regex pulled off Stack Overflow.\n# RFC-correct emails permit IP-literal domains, IDN, quoted local-parts, and\n# more. The regex passes review, fails on a real customer at 2 AM. Use the\n# specialized parser; the regex's job is to find candidates fast."
                  }
        ],
        tips: [
                  "These patterns are good starting points but may not cover all edge cases. For production, use validated libraries: email_validator, validators, etc.",
                  "URL pattern (?:/\\S*)? is optional trailing path — adjust for your needs.",
                  "Phone patterns vary by region — NEVER hand-roll regex; use Google's `phonenumbers` library (libphonenumber port).",
                  "IPv4 octets should be 0-255, but regex with \\d{1,3} allows up to 999 — validate separately if needed.",
                  "Two-step pattern: regex narrows (locates candidates), specialized parser validates. Use stdlib `urlparse` (URLs), `ipaddress.ip_address` (IPv4/v6), `datetime.fromisoformat` (ISO 8601), `email.headerregistry.Address` (emails)."
        ],
        mistake: "Shipping a \"validates email\" regex pulled off Stack Overflow as the source of truth — RFC-correct emails permit IP-literal domains, IDN, quoted local-parts, and more. The regex passes review and fails on a real customer at 2 AM. Use the specialized parser; the regex's job is to find candidates fast.",
        shorthand: {
          verbose: "import re\ntext = \"Email: alice@example.com\"\nparts = text.split(\"@\")\nif len(parts) == 2 and \".\" in parts[1]:\n    print(\"Maybe valid email\")",
          concise: "import re\nif re.search(r\"[\\w.+-]+@[\\w.-]+\\.\\w{2,}\", text):\n    print(\"Possible email\")",
        },
      },
      {
        id: "re-verbose",
        fn: "re.VERBOSE for readable multi-line patterns",
        desc: "Use re.VERBOSE to write complex patterns with comments and whitespace.",
        category: "Regex",
        subtitle: "Multi-line patterns with # comments for maintainability",
        signature: "re.compile(pattern, re.VERBOSE)",
        descLong: "re.VERBOSE (re.X) allows you to write regex patterns across multiple lines with comments. Whitespace is ignored unless escaped (or in character classes). This makes complex patterns much more readable and maintainable. Comments start with # and go to end of line. Essential for production patterns that others will need to understand and modify.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of re.VERBOSE for readable multi-line patterns — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport re\nEMAIL = re.compile(r\"\"\"\n    \\b\n    [\\w.+-]+        # local part\n    @\n    [\\w.-]+         # domain\n    \\.\n    [a-z]{2,}        # TLD (case-insensitive flag below)\n    \\b\n\"\"\", re.VERBOSE | re.IGNORECASE)\nprint(EMAIL.search(\"Contact Alice@Example.COM\").group())   # 'Alice@Example.COM'"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of re.VERBOSE for readable multi-line patterns — common patterns you'll see in production.\n# APPROACH  - Combine re.VERBOSE for readable multi-line patterns with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nimport re\nfrom dataclasses import dataclass\nDATETIME = re.compile(r\"\"\"\n    (?P<y>\\d{4}) - (?P<m>\\d{2}) - (?P<d>\\d{2})        # date\n    (?:                                                # optional time\n        [T\\s]+\n        (?P<H>\\d{2}) : (?P<M>\\d{2})\n        (?: : (?P<S>\\d{2}) )?                          # optional seconds\n        (?P<tz>Z | [+-] \\d{2} :? \\d{2})?              # optional offset\n    )?\n\"\"\", re.VERBOSE)\n@dataclass(frozen=True)\nclass WhenWhere:\n    y: str; m: str; d: str\n    H: str | None; M: str | None\n    S: str | None; tz: str | None\ndef parse(s: str) -> WhenWhere | None:\n    m = DATETIME.search(s)\n    return WhenWhere(**m.groupdict()) if m else None\nprint(parse(\"Event: 2026-04-30T12:34:56+02:00\"))"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of re.VERBOSE for readable multi-line patterns — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\nfrom __future__ import annotations\nimport re\nfrom typing import Final\n# 1) HTTP request line.\nHTTP_REQUEST: Final = re.compile(r\"\"\"\n    \\A\n    (?P<method>GET | POST | PUT | DELETE | PATCH | HEAD | OPTIONS)\n    \\s+\n    (?P<path>/ \\S*)\n    \\s+\n    HTTP/ (?P<version>\\d \\. \\d)\n    \\Z\n\"\"\", re.VERBOSE)\n# 2) Apache combined log format.\nAPACHE_LOG: Final = re.compile(r\"\"\"\n    \\A\n    (?P<ip>\\S+) \\s -\\s (?P<user>\\S+) \\s\n    \\[ (?P<ts>[^\\]]+) \\] \\s\n    \" (?P<method>[A-Z]+) \\s (?P<path>\\S+) \\s HTTP/[\\d.]+ \" \\s\n    (?P<status>\\d{3}) \\s (?P<size>\\d+|-) \\s\n    \" (?P<referer>[^\"]*) \" \\s\n    \" (?P<ua>[^\"]*) \"\n\"\"\", re.VERBOSE)\n# 3) Python identifier (PEP 3131-aware via Unicode).\nPY_IDENT: Final = re.compile(r\"\"\"\n    \\A\n    [^\\W\\d]              # start: letter or underscore (NOT a digit, NOT non-word)\n    \\w*                    # rest:  word chars (Unicode)\n    \\Z\n\"\"\", re.VERBOSE | re.UNICODE)\n# 4) Tests live with the pattern -- run via pytest, fail at lint time.\ndef _test_apache_log() -> None:\n    s = '127.0.0.1 - alice [30/Apr/2026:12:00:00 +0000] \"GET /home HTTP/1.1\" 200 532 \"-\" \"curl/8\"'\n    m = APACHE_LOG.match(s); assert m is not None\n    assert m[\"status\"] == \"200\" and m[\"method\"] == \"GET\"\ndef _test_http_request() -> None:\n    assert HTTP_REQUEST.match(\"POST /api/v1 HTTP/1.1\") is not None\n    assert HTTP_REQUEST.match(\"CUSTOM /api HTTP/1.1\") is None\n# 5) Production hygiene checklist for verbose patterns:\n#    - module-level Final constant, UPPER_CASE name\n#    - \\A ... \\Z anchors when the input is \"exactly one record\"\n#    - named groups, no positional indexing in callers\n#    - inline comments per piece (verbose mode is the only place this is cheap)\n#    - one test for the positive case, at least one for a tricky negative\n#    - benchmark on a 1MB+ sample if it runs in a hot path\n# Decision rule:\n#   pattern fits 60 chars in source                  -> normal raw string is fine\n#   pattern grows beyond ~60 chars OR uses 3+ groups -> re.VERBOSE; comment each part\n#   need both verbose and case-insensitive            -> re.VERBOSE | re.IGNORECASE\n#   include literal whitespace                        -> \\s or [ ]; NEVER a bare space\n#   include a literal '#'                             -> \\# or [#]\n#   readability inside an expression                  -> inline (?x:...) for that subpattern only\n#   patterns shared across modules                    -> central patterns.py with type Final\n#   regression tests                                  -> assert positive + negative cases per pattern\n#\n# Anti-pattern: a 200-character single-line regex in a module that nobody\n# touches because \"it works\". The next bug fix takes a half-day to even read\n# the pattern. re.VERBOSE costs nothing at runtime; pay it once."
                  }
        ],
        tips: [
                  "re.VERBOSE turns whitespace into \"invisible\" — it's ignored in the pattern. Use literal spaces inside [...] or \\s to match actual spaces.",
                  "Comments with # are powerful — explain what each part of the regex does. Future you will appreciate it.",
                  "Combine re.VERBOSE with re.IGNORECASE and other flags using |: re.VERBOSE | re.IGNORECASE.",
                  "Test verbose patterns with re.compile() at module level, not inline — much more readable.",
                  "Type each pattern as `Final` in a central patterns.py and ship at least one positive + one tricky negative test per pattern; CI catches regressions before they reach production."
        ],
        mistake: "Forgetting that spaces in VERBOSE patterns are ignored unless in character classes — \"a b\" matches \"ab\", not \"a b\". Use \\s or spaces inside [...] if you need literal spaces.",
        shorthand: {
          verbose: "// Manual / verbose approach\n# Complex pattern on one line — hard to read\npattern = r\"[\\w.+-]+@[\\w.-]+\\.\\w{2,}|\\d{4}-\\d{2}-\\d{2}|https?://[\\w.-]+\"\n// More explicit but longer",
          concise: "# Same pattern with VERBOSE — self-documenting\npattern = re.compile(r\"\"\"\n    [\\w.+-]+@[\\w.-]+\\.\\w{2,}  # email\n    |                              # OR\n    \\d{4}-\\d{2}-\\d{2}           # ISO date\n    |                              # OR\n    https?://[\\w.-]+              # URL\n\"\"\", re.VERBOSE)",
        },
      },
    ],
  },
]

export default { meta, sections }

export const meta = {
  "id": "image-processing",
  "label": "Image Processing",
  "icon": "🖼️",
  "description": "Practical image work in pure Python: Pillow (the default for resize/save/draw), numpy for pixel-level ops, OpenCV when you need real CV (edges, contours, video), color spaces and thresholding, batch pipelines, format/quality tuning, and the classical-vs-ML decision."
}

export const sections = [

  // ── Section 1: Pillow — load, resize, draw ─────────────────────────────────────────
  {
    id: "pillow",
    title: "Pillow — load, resize, draw",
    entries: [
      {
        id: "pil-basics",
        fn: "Pillow basics — open, resize, save with quality",
        desc: "Load an image, resize to a max dimension, save as JPEG. The 90% case for thumbnail/upload pipelines.",
        category: "Pillow",
        subtitle: "Image.open, .save, thumbnail, resize, modes (RGB/RGBA/L), JPEG quality, format detection, EXIF orientation",
        signature: "img = Image.open(\"in.jpg\"); img.thumbnail((800, 800)); img.save(\"out.jpg\", quality=85)",
        descLong: "Pillow is the default Python imaging library — fast enough for most workloads, simple API, broad format support. Three traps to avoid: (1) JPEG cannot store alpha (RGBA) — convert to RGB first or save as PNG; (2) `Image.open` is lazy — call `.load()` or any pixel access to actually decode; (3) EXIF orientation isn't applied by default — your iPhone uploads come in sideways. The three examples solve the SAME concrete task — resize an upload to 800px max and save as JPEG quality 85 — at three depths: minimal load+thumbnail+save → mode/orientation handling + error cases → production validation pipeline (size limits, MIME sniffing, metadata stripping).",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Pillow basics — open, resize, save with quality — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nfrom PIL import Image\nimg = Image.open(\"input.jpg\")\nimg.thumbnail((800, 800))                              # in-place; preserves aspect ratio\nimg.save(\"output.jpg\", quality=85, optimize=True)\n# thumbnail() vs resize():\n#   thumbnail((W, H))  - in-place; preserves aspect; never enlarges; max dimensions\n#   resize((W, H))      - returns new Image; exact dimensions; can stretch\n#   resize uses NEAREST by default (fast/blocky); pass Image.LANCZOS for quality\n# Modes (img.mode):\n#   \"RGB\"   - 3 channels, 8 bit each (typical photos)\n#   \"RGBA\"  - 4 channels with alpha (PNG with transparency)\n#   \"L\"     - 8-bit grayscale\n#   \"P\"     - 8-bit palette (GIF, indexed PNG)\n#   \"1\"     - 1-bit black/white\n# WARNING: saving an RGBA image as JPEG raises:\n#   OSError: cannot write mode RGBA as JPEG\n# Convert first (junior tier handles this)."
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Pillow basics — open, resize, save with quality — common patterns you'll see in production.\n# APPROACH  - Combine Pillow basics — open, resize, save with quality with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nfrom PIL import Image, ImageOps, UnidentifiedImageError\nfrom pathlib import Path\ndef make_thumbnail(in_path: str, out_path: str,\n                   *, max_size: int = 800, quality: int = 85) -> None:\n    try:\n        img = Image.open(in_path)\n    except UnidentifiedImageError:\n        raise ValueError(f\"not a valid image: {in_path}\")\n    # 1) Apply EXIF rotation (iPhone, Android cameras encode orientation in EXIF)\n    img = ImageOps.exif_transpose(img)\n    # 2) Convert mode for JPEG (no alpha, no palette)\n    if img.mode in (\"RGBA\", \"LA\", \"P\"):\n        # Composite over white background; preserves visual quality.\n        bg = Image.new(\"RGB\", img.size, (255, 255, 255))\n        if img.mode == \"P\": img = img.convert(\"RGBA\")\n        bg.paste(img, mask=img.split()[-1] if img.mode == \"RGBA\" else None)\n        img = bg\n    elif img.mode != \"RGB\":\n        img = img.convert(\"RGB\")\n    # 3) Resize with high-quality resampling\n    img.thumbnail((max_size, max_size), Image.LANCZOS)\n    # 4) Save with optimization\n    img.save(out_path, \"JPEG\", quality=quality, optimize=True, progressive=True)\n# Usage:\nmake_thumbnail(\"upload.heic\", \"thumb.jpg\", max_size=800)\n# Common errors caught:\n#   - Truncated files: pass ImageFile.LOAD_TRUNCATED_IMAGES = True\n#     to allow partial files (or fail explicitly — your call)\n#   - Decompression bombs: Pillow refuses huge images by default\n#     (PIL.Image.MAX_IMAGE_PIXELS); good. Don't disable.\n#   NEAREST  - fastest; pixelated; only for icons / palette images\n#   BILINEAR - decent; ~2x NEAREST; OK for previews\n#   BICUBIC  - good quality; ~3x NEAREST\n#   LANCZOS  - best quality; ~5x NEAREST; default for downscaling"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Pillow basics — open, resize, save with quality — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             strip metadata, security limits.\nfrom PIL import Image, ImageOps, ImageFile\nfrom pathlib import Path\nfrom io import BytesIO\n# Allow truncated files? Default False (rejects partial/corrupt). Decide explicitly.\nImageFile.LOAD_TRUNCATED_IMAGES = False\n# Decompression-bomb protection (Pillow's default is 89,478,485 px ≈ 9000x9000).\n# DO NOT raise this without thinking — it's the only thing protecting against\n# 1MB JPEG that decodes to 8GB RAM. Lower for hostile uploads:\nImage.MAX_IMAGE_PIXELS = 25_000_000                    # 5000x5000 — adjust per use case\nALLOWED_FORMATS = {\"JPEG\", \"PNG\", \"WEBP\", \"GIF\"}        # whitelist, not blacklist\nMAX_INPUT_BYTES = 20 * 1024 * 1024                      # 20 MB\nclass ImageValidationError(ValueError): pass\ndef validate_and_thumbnail(\n    raw_bytes: bytes,\n    *,\n    max_size: int = 800,\n    quality: int = 85,\n    strip_metadata: bool = True,\n) -> bytes:\n    \"\"\"Validate an upload + return JPEG-encoded thumbnail bytes.\"\"\"\n    if len(raw_bytes) > MAX_INPUT_BYTES:\n        raise ImageValidationError(f\"upload exceeds {MAX_INPUT_BYTES} bytes\")\n    buf = BytesIO(raw_bytes)\n    try:\n        img = Image.open(buf)\n        img.verify()                                    # parse headers; cheap; raises on garbage\n    except Exception as e:\n        raise ImageValidationError(f\"not a valid image: {e}\")\n    # verify() consumes the file; reopen for actual processing.\n    img = Image.open(BytesIO(raw_bytes))\n    if img.format not in ALLOWED_FORMATS:\n        raise ImageValidationError(f\"format {img.format} not allowed\")\n    # EXIF rotation BEFORE we read dimensions (orientation may swap W/H semantically).\n    img = ImageOps.exif_transpose(img)\n    # Mode normalization for JPEG output.\n    if img.mode in (\"RGBA\", \"LA\", \"P\"):\n        bg = Image.new(\"RGB\", img.size, (255, 255, 255))\n        if img.mode == \"P\": img = img.convert(\"RGBA\")\n        bg.paste(img, mask=img.split()[-1] if img.mode == \"RGBA\" else None)\n        img = bg\n    elif img.mode != \"RGB\":\n        img = img.convert(\"RGB\")\n    img.thumbnail((max_size, max_size), Image.LANCZOS)\n    out = BytesIO()\n    save_kwargs = {\"format\": \"JPEG\", \"quality\": quality,\n                   \"optimize\": True, \"progressive\": True}\n    if strip_metadata:\n        # NOT passing 'exif=' or 'icc_profile=' strips them — they're not in the\n        # output unless explicitly written.\n        pass\n    else:\n        if img.info.get(\"icc_profile\"):\n            save_kwargs[\"icc_profile\"] = img.info[\"icc_profile\"]\n    img.save(out, **save_kwargs)\n    return out.getvalue()\n# Decision rule:\n#   web upload thumbnail                 -> validate_and_thumbnail above\n#   in-memory processing                  -> BytesIO; never touch disk\n#   batch on disk                          -> Path-based wrapper\n#   keep transparency                     -> output PNG/WebP, not JPEG\n#   need orientation correct               -> ImageOps.exif_transpose(img) FIRST\n#   user uploads (untrusted)               -> set MAX_IMAGE_PIXELS; whitelist formats\n#   want metadata preserved                -> save with exif= and icc_profile=\n#   need progressive JPEG                  -> progressive=True (smaller; better mobile UX)\n#   tiny images                            -> NEAREST is fine; LANCZOS overkill\n#   downscaling                            -> Image.LANCZOS\n#   upscaling                              -> Image.BICUBIC; OR don't (lossy)\n#\n# Anti-pattern: saving RGBA as JPEG without conversion. Pillow raises\n# \"cannot write mode RGBA as JPEG\"; your upload pipeline crashes on\n# every PNG with transparency. ALWAYS convert mode before save."
                  }
        ],
        tips: [
                  "Always run `ImageOps.exif_transpose(img)` before measuring dimensions or saving — phone uploads are often sideways without it.",
                  "Convert RGBA → RGB before saving as JPEG; JPEG has no alpha channel and Pillow raises if you skip the conversion.",
                  "Use `Image.LANCZOS` for quality downscaling, `Image.BICUBIC` for moderate, `Image.NEAREST` only for icons. Default is NEAREST — too blocky for photos.",
                  "`thumbnail(size)` is in-place AND preserves aspect ratio; `resize(size)` returns a new image and stretches. Almost always you want `thumbnail`.",
                  "`Image.MAX_IMAGE_PIXELS` is your only line of defense against decompression-bomb attacks. Don't raise it without a hard reason.",
                  "Use `BytesIO` for in-memory processing — avoid disk round-trips in serverless/Lambda contexts."
        ],
        mistake: "Saving an RGBA image directly as JPEG. Pillow raises `cannot write mode RGBA as JPEG`; the upload pipeline crashes on every PNG with transparency. Convert mode first: composite over a background (white or app-specific) for predictable visual output.",
        shorthand: {
          verbose: "# Crashes on PNGs with transparency\nimg = Image.open(\"upload.png\")\nimg.save(\"out.jpg\")    # OSError: cannot write mode RGBA as JPEG",
          concise: "# Convert before save\nif img.mode != \"RGB\":\n    bg = Image.new(\"RGB\", img.size, (255, 255, 255))\n    bg.paste(img, mask=img.split()[-1] if img.mode == \"RGBA\" else None)\n    img = bg\nimg.save(\"out.jpg\")",
        },
      },
      {
        id: "pil-transforms",
        fn: "Pillow transforms — rotate, crop, fit, autocontrast",
        desc: "Geometric and tonal transforms: rotate with expand, crop to aspect, ImageOps.fit for \"cover\" semantics, autocontrast for tone-stretched output. The toolkit beyond resize.",
        category: "Pillow",
        subtitle: "rotate(expand=True), crop, ImageOps.fit, ImageOps.contain, ImageOps.autocontrast, transpose, paste with alpha mask",
        signature: "img = ImageOps.fit(img, (W, H), method=Image.LANCZOS, centering=(0.5, 0.5))",
        descLong: "Beyond resize, you usually need: rotate (with `expand=True` so the canvas grows), crop-to-aspect (square thumbnails from arbitrary ratios), tone correction (autocontrast). `ImageOps` collects the high-level helpers; `Image` has the lower-level methods. The three examples solve the SAME concrete task — produce a 600×400 hero image from any input (any aspect, any orientation) — at three depths: rotate + resize → ImageOps.fit for crop-to-aspect → production with autocontrast + tone mapping + content-aware crop.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Pillow transforms — rotate, crop, fit, autocontrast — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nfrom PIL import Image\nimg = Image.open(\"input.jpg\")\nimg = img.rotate(-90, expand=True)                    # rotate 90° clockwise; canvas grows\nimg = img.resize((600, 400), Image.LANCZOS)           # exact size; STRETCHES if aspect differs\nimg.save(\"hero.jpg\", quality=85)\n# Problems:\n#   resize((600, 400)) stretches non-4:3 inputs into 4:3. A square\n#   selfie becomes squashed. Junior tier uses ImageOps.fit for\n#   crop-to-aspect.\n# rotate(angle, expand=True/False):\n#   expand=False (default): canvas size unchanged; corners may clip\n#   expand=True: canvas grows to fit rotated image (with empty fill)\n# Most rotations want expand=True."
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Pillow transforms — rotate, crop, fit, autocontrast — common patterns you'll see in production.\n# APPROACH  - Combine Pillow transforms — rotate, crop, fit, autocontrast with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nfrom PIL import Image, ImageOps\ndef make_hero(in_path: str, out_path: str, size: tuple[int, int] = (600, 400)) -> None:\n    img = Image.open(in_path)\n    img = ImageOps.exif_transpose(img)                # fix phone orientation\n    if img.mode != \"RGB\":\n        img = img.convert(\"RGB\")\n    # ImageOps.fit = \"cover\": crop to aspect, then resize to exact size.\n    # centering=(0.5, 0.5) is centered; (0.5, 0.0) crops bottom (hero often has subject at top).\n    img = ImageOps.fit(img, size, method=Image.LANCZOS, centering=(0.5, 0.5))\n    img.save(out_path, \"JPEG\", quality=85, optimize=True)\nmake_hero(\"portrait.jpg\", \"hero.jpg\")\n# Comparison:\n#   img.resize((600, 400))             - stretches (aspect ratios mismatch)\n#   ImageOps.contain(img, (600, 400))  - \"contain\": fits inside; pads or leaves whitespace\n#   ImageOps.fit(img, (600, 400))      - \"cover\": crops to fill; no whitespace\n#   ImageOps.pad(img, (600, 400))      - explicit pad; control color via color= kwarg\n# Common rotations:\n#   img.transpose(Image.FLIP_LEFT_RIGHT)\n#   img.transpose(Image.FLIP_TOP_BOTTOM)\n#   img.transpose(Image.ROTATE_90)        # 90° counterclockwise; faster than rotate()\n#   img.transpose(Image.ROTATE_180)\n#   img.transpose(Image.ROTATE_270)"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Pillow transforms — rotate, crop, fit, autocontrast — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             content-aware horizontal crop centering, batch helper.\nfrom PIL import Image, ImageOps, ImageStat\nfrom pathlib import Path\nfrom typing import Literal\ndef make_hero(\n    in_path: str,\n    out_path: str,\n    *,\n    size: tuple[int, int] = (600, 400),\n    auto_tone: bool = True,\n    centering: Literal[\"center\", \"rule_of_thirds_top\", \"salient\"] = \"center\",\n) -> None:\n    img = Image.open(in_path)\n    img = ImageOps.exif_transpose(img)\n    if img.mode != \"RGB\":\n        img = img.convert(\"RGB\")\n    # Content-aware vertical centering: heroes often have subject in upper third.\n    cy = {\"center\": 0.5, \"rule_of_thirds_top\": 0.33, \"salient\": 0.5}[centering]\n    cx = 0.5\n    if centering == \"salient\":\n        cx, cy = _saliency_center(img)                # see below\n    img = ImageOps.fit(img, size, method=Image.LANCZOS, centering=(cx, cy))\n    if auto_tone:\n        # Stretch histogram so darkest pixel is black, brightest is white.\n        # cutoff=2 ignores extreme 2% of pixels (avoids over-correcting on dust).\n        img = ImageOps.autocontrast(img, cutoff=2)\n    img.save(out_path, \"JPEG\", quality=85, optimize=True, progressive=True)\ndef _saliency_center(img: Image.Image) -> tuple[float, float]:\n    \"\"\"Cheap saliency: find brightest region; assume subject is there.\n    Better: use opencv saliency or a pretrained model (vision-modern entry).\"\"\"\n    gray = img.convert(\"L\")\n    small = gray.resize((32, 32))\n    pixels = list(small.getdata())\n    w, h = small.size\n    total = sum(pixels)\n    if total == 0:\n        return 0.5, 0.5\n    cx_acc = sum(pixels[i] * (i % w) for i in range(w * h)) / total\n    cy_acc = sum(pixels[i] * (i // w) for i in range(w * h)) / total\n    return cx_acc / w, cy_acc / h\ndef batch_heroes(input_dir: Path, output_dir: Path, *, size=(600, 400)) -> int:\n    output_dir.mkdir(parents=True, exist_ok=True)\n    n = 0\n    for p in input_dir.rglob(\"*.jpg\"):\n        try:\n            make_hero(str(p), str(output_dir / p.name), size=size)\n            n += 1\n        except Exception as e:\n            print(f\"FAIL {p.name}: {e}\")\n    return n\n# Decision rule:\n#   exact size with stretch OK            -> img.resize((W, H), Image.LANCZOS)\n#   exact size, crop to fill              -> ImageOps.fit(img, (W, H), method=LANCZOS)\n#   fit inside, allow whitespace          -> ImageOps.contain(img, (W, H))\n#   fit inside, pad to exact size         -> ImageOps.pad(img, (W, H), color=(0,0,0))\n#   rotate by angle                        -> img.rotate(deg, expand=True)\n#   90/180/270 only                        -> img.transpose(Image.ROTATE_90) — much faster\n#   tonal correction                        -> ImageOps.autocontrast(img, cutoff=2)\n#   grayscale                              -> ImageOps.grayscale(img) OR img.convert(\"L\")\n#   invert                                  -> ImageOps.invert(img.convert(\"RGB\"))\n#   high-quality content-aware crop         -> use ML saliency or face detection (next entries)\n#\n# Anti-pattern: rotate(deg) without expand=True for non-90° angles.\n# Default expand=False keeps the original canvas size, so corners\n# get clipped. The user wonders why \"the rotated image got cropped\".\n# Always pass expand=True unless you specifically want the original\n# bounding box."
                  }
        ],
        tips: [
                  "`ImageOps.fit` is \"cover\" semantics — crops to aspect, fills the box. `ImageOps.contain` is \"contain\" — fits inside with possible padding/whitespace.",
                  "`img.transpose(Image.ROTATE_90)` is 5-10× faster than `img.rotate(90)` for 90/180/270 degree rotations.",
                  "Always pass `expand=True` to `rotate()` unless you specifically want the original canvas. Default crops corners.",
                  "`ImageOps.autocontrast(img, cutoff=2)` stretches the histogram while ignoring extreme 2% — avoids overcorrecting dust spots and reflections.",
                  "For heroes/banners, `centering=(0.5, 0.33)` (rule-of-thirds top) often produces better composition than centered crops.",
                  "For real saliency-based crop, use OpenCV's `cv2.saliency.StaticSaliencyFineGrained_create()` or a vision model (see vision-modern entry)."
        ],
        mistake: "`rotate(deg)` without `expand=True` for non-orthogonal angles. Default behavior keeps the canvas size, so a 45° rotation clips all four corners. The user reports \"image got cropped after rotation\"; the fix is one keyword arg.",
        shorthand: {
          verbose: "// Manual / verbose approach\n# Default expand=False — corners clipped after rotation\nimg = img.rotate(45)\n// More explicit but longer",
          concise: "# Canvas grows to fit the rotated image\nimg = img.rotate(45, expand=True)",
        },
      },
      {
        id: "pil-drawing",
        fn: "Pillow drawing — text, shapes, watermarks",
        desc: "Draw text and shapes on images: ImageDraw + ImageFont for type, alpha-composite for transparent overlays, scale type-size to image dimensions for consistency. The watermarking + annotation toolkit.",
        category: "Pillow",
        subtitle: "ImageDraw.Draw, draw.text / rectangle / line, ImageFont.truetype, alpha_composite, ImageFilter, scaled type for consistent sizing",
        signature: "draw = ImageDraw.Draw(img); font = ImageFont.truetype(\"Arial.ttf\", 24); draw.text((10, 10), \"watermark\", fill=\"white\", font=font)",
        descLong: "For overlays, watermarks, bounding boxes on detection output, etc., `ImageDraw` provides primitives; `ImageFont` loads TrueType fonts; `Image.alpha_composite` overlays a transparent layer cleanly. The catch: default font is bitmap-only (8pt) — use `ImageFont.truetype` with a real font file for any production text. Type sizes need scaling with image dimensions, otherwise watermarks look 5pt on a 4K image. The three examples solve the SAME concrete task — add a \"Confidential\" watermark to an image — at three depths: basic draw.text → TrueType + transparent overlay → production with size-scaled type, multi-line, anti-aliasing.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Pillow drawing — text, shapes, watermarks — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nfrom PIL import Image, ImageDraw\nimg = Image.open(\"doc.jpg\").copy()\ndraw = ImageDraw.Draw(img)\ndraw.text((20, 20), \"CONFIDENTIAL\", fill=\"red\")\nimg.save(\"watermarked.jpg\")\n# Default font is a tiny built-in bitmap (8pt-ish; ugly).\n# Real apps need ImageFont.truetype — junior tier.\n# Other primitives:\ndraw.rectangle([10, 10, 100, 50], outline=\"black\", width=2)\ndraw.line([10, 10, 100, 50], fill=\"blue\", width=3)\ndraw.ellipse([10, 10, 100, 100], fill=\"red\", outline=\"black\")\ndraw.polygon([(0, 0), (100, 0), (50, 100)], fill=\"green\")\n# Bounding box on detection output:\ndraw.rectangle((x1, y1, x2, y2), outline=\"lime\", width=3)\ndraw.text((x1, y1 - 20), f\"cat 0.92\", fill=\"lime\")"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Pillow drawing — text, shapes, watermarks — common patterns you'll see in production.\n# APPROACH  - Combine Pillow drawing — text, shapes, watermarks with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             multi-line text.\nfrom PIL import Image, ImageDraw, ImageFont\ndef watermark(in_path: str, out_path: str, text: str = \"CONFIDENTIAL\") -> None:\n    img = Image.open(in_path).convert(\"RGBA\")          # need alpha for compositing\n    # Transparent overlay — same size as base.\n    overlay = Image.new(\"RGBA\", img.size, (0, 0, 0, 0))\n    draw = ImageDraw.Draw(overlay)\n    # Real font; ship one with your app or use a system path.\n    try:\n        font = ImageFont.truetype(\"DejaVuSans-Bold.ttf\", 48)\n    except OSError:\n        font = ImageFont.load_default()\n    # textbbox returns (x0, y0, x1, y1).\n    bbox = draw.textbbox((0, 0), text, font=font)\n    tw, th = bbox[2] - bbox[0], bbox[3] - bbox[1]\n    # Centered.\n    x = (img.width - tw) // 2\n    y = (img.height - th) // 2\n    # Translucent red — alpha=128 = 50%.\n    draw.text((x, y), text, fill=(255, 0, 0, 128), font=font)\n    # Composite overlay onto base.\n    out = Image.alpha_composite(img, overlay).convert(\"RGB\")\n    out.save(out_path, \"JPEG\", quality=85)\nwatermark(\"doc.jpg\", \"wm.jpg\")\n# Multi-line text:\ndraw.multiline_text((x, y), \"Line 1\\nLine 2\", fill=\"black\", font=font, spacing=4)\n# Stroke (outlined text — readable on any background):\ndraw.text((x, y), text, fill=\"white\", stroke_width=2, stroke_fill=\"black\", font=font)"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Pillow drawing — text, shapes, watermarks — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             pluggable theming, batch annotation helper.\nfrom PIL import Image, ImageDraw, ImageFont\nfrom dataclasses import dataclass\nfrom pathlib import Path\n@dataclass(frozen=True)\nclass WatermarkStyle:\n    text: str = \"CONFIDENTIAL\"\n    font_path: str = \"DejaVuSans-Bold.ttf\"\n    font_size_ratio: float = 0.05            # font height = 5% of image height\n    color: tuple[int, int, int] = (255, 0, 0)\n    opacity: int = 128                        # 0-255\n    stroke_color: tuple[int, int, int] = (0, 0, 0)\n    stroke_ratio: float = 0.05               # stroke = 5% of font height\n    rotation_deg: float = -30.0\n    diagonal: bool = True                     # tile across whole image diagonally\ndef add_watermark(img: Image.Image, style: WatermarkStyle = WatermarkStyle()) -> Image.Image:\n    base = img.convert(\"RGBA\")\n    overlay = Image.new(\"RGBA\", base.size, (0, 0, 0, 0))\n    # Scale font with image; minimum size for readability.\n    font_size = max(int(base.height * style.font_size_ratio), 14)\n    try:\n        font = ImageFont.truetype(style.font_path, font_size)\n    except OSError:\n        font = ImageFont.load_default()\n    stroke = max(1, int(font_size * style.stroke_ratio))\n    if style.diagonal:\n        # Render text on a separate canvas, rotate, then tile across overlay.\n        text_layer = Image.new(\"RGBA\", base.size, (0, 0, 0, 0))\n        td = ImageDraw.Draw(text_layer)\n        bbox = td.textbbox((0, 0), style.text, font=font, stroke_width=stroke)\n        tw, th = bbox[2] - bbox[0], bbox[3] - bbox[1]\n        # Tile diagonally — repeated text across the canvas.\n        spacing_y = int(th * 4)\n        spacing_x = int(tw * 1.5)\n        for y in range(-base.height, base.height * 2, spacing_y):\n            for x in range(-base.width, base.width * 2, spacing_x):\n                td.text((x, y), style.text,\n                        fill=(*style.color, style.opacity),\n                        font=font,\n                        stroke_width=stroke,\n                        stroke_fill=(*style.stroke_color, style.opacity))\n        text_layer = text_layer.rotate(style.rotation_deg,\n                                        resample=Image.BICUBIC,\n                                        expand=False)\n        overlay = Image.alpha_composite(overlay, text_layer)\n    else:\n        d = ImageDraw.Draw(overlay)\n        bbox = d.textbbox((0, 0), style.text, font=font, stroke_width=stroke)\n        tw, th = bbox[2] - bbox[0], bbox[3] - bbox[1]\n        x, y = (base.width - tw) // 2, (base.height - th) // 2\n        d.text((x, y), style.text,\n               fill=(*style.color, style.opacity),\n               font=font, stroke_width=stroke,\n               stroke_fill=(*style.stroke_color, style.opacity))\n    return Image.alpha_composite(base, overlay).convert(\"RGB\")\ndef batch_watermark(in_dir: Path, out_dir: Path, style: WatermarkStyle = WatermarkStyle()):\n    out_dir.mkdir(parents=True, exist_ok=True)\n    for p in in_dir.glob(\"*.jpg\"):\n        img = Image.open(p)\n        out = add_watermark(img, style)\n        out.save(out_dir / p.name, \"JPEG\", quality=85, optimize=True)\n# Decision rule:\n#   simple text label                     -> draw.text with truetype font\n#   transparent overlay                    -> separate RGBA layer + alpha_composite\n#   readable on any background            -> stroke_width + contrasting stroke_fill\n#   font scales with image                 -> font_size = ratio * img.height\n#   diagonal repeated watermark            -> tile + rotate(expand=False)\n#   bounding boxes (detection)            -> rectangle(outline=, width=) + text label\n#   anti-aliasing                          -> Pillow's text drawing is anti-aliased by default\n#   custom font                             -> ship .ttf with the app or load from /usr/share/fonts\n#   need vector output                      -> NOT Pillow; use cairosvg or matplotlib\n#   complex typography                      -> NOT Pillow; use HTML→PDF or skia-python\n#\n# Anti-pattern: hardcoding font_size=24 for all images. On a 200×200\n# thumbnail it dominates; on a 4000×4000 photo it's invisible. Always\n# scale type size with image height (or width for landscape-heavy\n# content). One ratio = consistent visual weight across sizes."
                  }
        ],
        tips: [
                  "`ImageFont.load_default()` is a tiny bitmap — only useful for tests. Always load a TrueType font for any user-visible text.",
                  "Use `stroke_width` + `stroke_fill` for text readable on any background. White text + black 2px stroke is the universal \"readable everywhere\".",
                  "Scale font size with image dimensions: `font_size = max(14, int(height * 0.04))`. Hardcoded sizes look broken at scale.",
                  "For transparent overlays, work in `RGBA` mode, draw on a separate overlay layer, then `Image.alpha_composite` onto the base.",
                  "`textbbox()` (PIL 9+) replaces the deprecated `textsize()` — returns the bounding box including font metrics for accurate centering.",
                  "For diagonal watermarks (anti-piracy), tile the text on a separate layer and rotate that layer with `expand=False`."
        ],
        mistake: "Hardcoding `font_size=24` for all images. At 200×200 it covers half the image; at 4000×4000 it's invisible. Always scale font size with image dimensions (typically `height * 0.04` for body text, `0.06+` for headlines).",
        shorthand: {
          verbose: "// Manual / verbose approach\n# Fixed size — looks wrong at every scale except one\nfont = ImageFont.truetype(\"Arial.ttf\", 24)\n// More explicit but longer",
          concise: "# Scaled font — consistent visual weight\nfont_size = max(14, int(img.height * 0.04))\nfont = ImageFont.truetype(\"Arial.ttf\", font_size)",
        },
      },
    ],
  },

  // ── Section 2: NumPy + OpenCV — pixel ops, color spaces ─────────────────────────────────────────
  {
    id: "numpy-cv",
    title: "NumPy + OpenCV — pixel ops, color spaces",
    entries: [
      {
        id: "numpy-image-ops",
        fn: "NumPy image ops — array view, channels, broadcasting",
        desc: "Convert PIL ↔ numpy; treat images as `(H, W, C)` arrays; do per-pixel arithmetic with broadcasting. The escape hatch when Pillow doesn't have a built-in.",
        category: "NumPy + OpenCV",
        subtitle: "np.array(img), Image.fromarray, dtype=uint8 vs float, channel order, slicing, broadcasting, np.clip",
        signature: "arr = np.array(img); arr[..., 0] = 0   # zero out red channel",
        descLong: "For pixel-level arithmetic — channel manipulation, blending, custom filters — Pillow images convert to/from numpy arrays with `np.array(img)` and `Image.fromarray(arr)`. Shape is `(H, W)` for grayscale or `(H, W, 3)`/`(H, W, 4)` for color. Critical: dtype is `uint8` (0-255); arithmetic that overflows wraps. Always cast to `int16` or `float32` before computing, then clip back to `uint8`. The three examples solve the SAME concrete task — blend two images at 50% opacity AND threshold a grayscale image — at three depths: basic array ops → dtype handling + per-channel ops → vectorized batch with memory-efficient streaming.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of NumPy image ops — array view, channels, broadcasting — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport numpy as np\nfrom PIL import Image\na = np.array(Image.open(\"a.jpg\"))                     # (H, W, 3) uint8\nb = np.array(Image.open(\"b.jpg\").resize((a.shape[1], a.shape[0])))\n# Blend (BUG: integer overflow if values > 255 mid-calculation)\n# blend = (a + b) // 2                                 # WRONG — uint8 wraps; cast first\nblend = ((a.astype(np.int16) + b.astype(np.int16)) // 2).astype(np.uint8)\nImage.fromarray(blend).save(\"blend.jpg\")\n# Threshold a grayscale image:\ngray = np.array(Image.open(\"photo.jpg\").convert(\"L\"))  # (H, W) uint8\nbinary = (gray > 128).astype(np.uint8) * 255           # 0 or 255\nImage.fromarray(binary).save(\"binary.jpg\")\nimg = np.array(Image.open(\"photo.jpg\"))               # (H, W, 3) RGB\n# Zero out the red channel:\nimg[..., 0] = 0\n# Swap red and blue:\nimg = img[..., [2, 1, 0]]                              # but cv2.cvtColor is faster\n# Common shape gotchas:\n#   (H, W, 3) RGB        - PIL default; matplotlib expects this\n#   (H, W, 3) BGR        - OpenCV default; cv2.cvtColor to convert\n#   (H, W)               - grayscale\n#   (H, W, 4) RGBA       - PNG with alpha\n#   (3, H, W)            - PyTorch convention; transpose with .transpose(2, 0, 1)"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of NumPy image ops — array view, channels, broadcasting — common patterns you'll see in production.\n# APPROACH  - Combine NumPy image ops — array view, channels, broadcasting with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nimport numpy as np\nfrom PIL import Image\ndef blend(a_path: str, b_path: str, alpha: float = 0.5) -> Image.Image:\n    \"\"\"Blend a + b with given alpha (0-1).\"\"\"\n    a = np.array(Image.open(a_path).convert(\"RGB\")).astype(np.float32)\n    b = np.array(Image.open(b_path).convert(\"RGB\").resize(\n        (a.shape[1], a.shape[0]))).astype(np.float32)\n    out = a * alpha + b * (1 - alpha)\n    out = np.clip(out, 0, 255).astype(np.uint8)\n    return Image.fromarray(out)\n# Per-channel saturation boost (HSV-style, in RGB space):\ndef boost_saturation(img: Image.Image, factor: float = 1.3) -> Image.Image:\n    arr = np.array(img.convert(\"RGB\")).astype(np.float32)\n    # Mean across channels = grayscale equivalent.\n    gray = arr.mean(axis=2, keepdims=True)\n    # Pull channels away from gray by 'factor'.\n    arr = gray + (arr - gray) * factor\n    return Image.fromarray(np.clip(arr, 0, 255).astype(np.uint8))\n# Channel-wise threshold (e.g., extract red regions):\ndef red_mask(img: Image.Image) -> np.ndarray:\n    arr = np.array(img.convert(\"RGB\"))\n    r, g, b = arr[..., 0], arr[..., 1], arr[..., 2]\n    # Red = high R, low G, low B.\n    return (r > 150) & (g < 100) & (b < 100)           # (H, W) bool\nmask = red_mask(Image.open(\"photo.jpg\"))\nprint(f\"Red pixels: {mask.sum()} / {mask.size}\")\ndef downscale_tiled(img: Image.Image, scale: int = 2) -> Image.Image:\n    \"\"\"For very large images, downscale one tile at a time.\"\"\"\n    w, h = img.size\n    out = Image.new(img.mode, (w // scale, h // scale))\n    tile = 512                                          # process 512x512 chunks\n    for y in range(0, h, tile * scale):\n        for x in range(0, w, tile * scale):\n            box = (x, y, min(x + tile * scale, w), min(y + tile * scale, h))\n            crop = img.crop(box)\n            crop.thumbnail((tile, tile), Image.LANCZOS)\n            out.paste(crop, (x // scale, y // scale))\n    return out"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of NumPy image ops — array view, channels, broadcasting — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             estimate, comparison with cv2 for the same op.\nimport numpy as np\nfrom PIL import Image\nfrom pathlib import Path\ndef batch_blend_vs_reference(\n    image_paths: list[Path],\n    reference_path: Path,\n    alpha: float = 0.5,\n    out_dir: Path = Path(\"blended\"),\n) -> None:\n    out_dir.mkdir(parents=True, exist_ok=True)\n    ref = np.array(Image.open(reference_path).convert(\"RGB\")).astype(np.float32)\n    H, W, _ = ref.shape\n    for p in image_paths:\n        img = Image.open(p).convert(\"RGB\").resize((W, H))\n        arr = np.array(img).astype(np.float32)\n        out = np.clip(arr * alpha + ref * (1 - alpha), 0, 255).astype(np.uint8)\n        Image.fromarray(out).save(out_dir / p.name, \"JPEG\", quality=88)\n# A (H, W, 3) uint8 array uses H * W * 3 bytes.\n#   1080p:    ~6 MB\n#   4K:       ~24 MB\n#   12 MP:    ~36 MB\n# Float32 is 4x larger:\n#   12 MP float32: ~144 MB (single image during processing)\n# Estimate before allocating; for huge images, work in tiles.\n# Operation                  numpy + PIL    cv2          Notes\n# ---------------------------------------------------------------\n# Channel arithmetic         fast            fast         numpy is fine\n# Resize                      LANCZOS          INTER_AREA    cv2 INTER_AREA is best for downscale\n# Gaussian blur               scipy.ndimage    cv2.GaussianBlur  cv2 is 5-10x faster\n# Edge detection              numpy.gradient   cv2.Canny    cv2 is the standard\n# Affine transforms           PIL              cv2.warpAffine    cv2 is faster\n# Video frames                NOT applicable   cv2.VideoCapture  PIL doesn't do video\n# Connected components        scipy            cv2.connectedComponents  cv2 is faster\n#\n# Decision rule:\n#   per-pixel arithmetic                  -> numpy on (H, W, C) arrays\n#   integer overflow risk                  -> cast to int16 or float32 before math\n#   final output                           -> clip to 0-255, cast back to uint8\n#   PyTorch / TF input                      -> transpose to (C, H, W); normalize to [0, 1]\n#   large image, OOM risk                  -> tile-based processing\n#   need a fast filter (blur, edge)         -> cv2 (next entry)\n#   need resize for ML preprocessing        -> cv2.resize INTER_AREA for downscale\n#   batch processing                         -> stack into (N, H, W, C) array; vectorize\n#   color space conversion                   -> cv2.cvtColor (next entry); faster than manual\n#   want PIL operations on a numpy array    -> Image.fromarray; add overhead but cleaner\n#\n# Anti-pattern: doing arithmetic on uint8 arrays without casting.\n#   blend = (a + b) // 2\n# uint8 + uint8 wraps modulo 256; (200 + 100) becomes 44, not 300/2.\n# Cast to int16 or float32 before any arithmetic that could exceed 255."
                  }
        ],
        tips: [
                  "`np.array(img)` returns a copy (Pillow → numpy); `Image.fromarray(arr)` does the reverse. Round-trip overhead is small (no decode/encode).",
                  "uint8 arithmetic WRAPS (modulo 256). Cast to int16 or float32 BEFORE any add/multiply, then `np.clip(arr, 0, 255).astype(np.uint8)` at the end.",
                  "PIL is RGB; OpenCV is BGR. Convert with `cv2.cvtColor(arr, cv2.COLOR_BGR2RGB)` or `arr[..., ::-1]` (slower but stdlib).",
                  "For PyTorch input, transpose `(H, W, C) → (C, H, W)` with `arr.transpose(2, 0, 1)` and normalize to `[0, 1]`.",
                  "Memory: a 4K RGB image is 24 MB as uint8, 96 MB as float32. For 12 MP photos, work in float16 or tiles to avoid OOM.",
                  "For ML preprocessing pipelines (resize, normalize, channel-shuffle), `albumentations` or `torchvision.transforms.v2` is faster than hand-rolled numpy."
        ],
        mistake: "Arithmetic on uint8 arrays without casting: `(a + b) // 2`. uint8 wraps modulo 256, so `200 + 100` becomes `44` (not `300`), and the average comes out wrong. Cast to int16 or float32 first; clip and cast back at the end.",
        shorthand: {
          verbose: "// Manual / verbose approach\n# Wraps; produces garbled output\nblend = (a + b) // 2\n// More explicit but longer",
          concise: "# Cast → math → clip → cast back\nblend = ((a.astype(np.int16) + b.astype(np.int16)) // 2).astype(np.uint8)",
        },
      },
      {
        id: "opencv-basics",
        fn: "OpenCV basics — imread, cvtColor, resize, edges",
        desc: "OpenCV (`cv2`) is the standard for real CV — fast filters, edge detection, contours, video. The catch: BGR by default, headless install for servers, type conventions differ from Pillow.",
        category: "NumPy + OpenCV",
        subtitle: "cv2.imread, cv2.imwrite, BGR ↔ RGB via cvtColor, cv2.resize INTER_AREA, cv2.GaussianBlur, cv2.Canny, headless opencv-python-headless",
        signature: "img = cv2.imread(\"in.jpg\"); edges = cv2.Canny(cv2.cvtColor(img, cv2.COLOR_BGR2GRAY), 100, 200)",
        descLong: "OpenCV is the production toolkit for \"real\" computer vision: edge detection, contours, optical flow, feature matching, video. Two install variants: `opencv-python` (with GUI/highgui — for desktop) and `opencv-python-headless` (no GUI deps — for servers/Docker; what you usually want). Default channel order is BGR (legacy), so converting to RGB before display or saving with PIL is required. The three examples solve the SAME concrete task — load, detect edges, save — at three depths: basic Canny pipeline → tuned blur+threshold → production with parameter sweep, video frames, GPU optional.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of OpenCV basics — imread, cvtColor, resize, edges — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# pip install opencv-python-headless    (NOT opencv-python on servers)\nimport cv2\nimg = cv2.imread(\"input.jpg\")                          # (H, W, 3) BGR uint8\ngray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)\nedges = cv2.Canny(gray, threshold1=100, threshold2=200)\ncv2.imwrite(\"edges.jpg\", edges)\n# imread quirks:\n#   imread(\"missing.jpg\") returns None — no exception. CHECK.\n#   if img is None: raise FileNotFoundError(\"can't read\")\n#\n#   imread(\"photo.heic\") fails on some platforms; HEIC needs extra deps.\n# Convert to RGB for matplotlib / PIL:\nimport matplotlib.pyplot as plt\nplt.imshow(cv2.cvtColor(img, cv2.COLOR_BGR2RGB))\nplt.show()\n# Common ops (one-liners):\nblurred  = cv2.GaussianBlur(img, (5, 5), 0)            # kernel (5, 5), sigma=0 (auto)\nresized  = cv2.resize(img, (640, 480), interpolation=cv2.INTER_AREA)\nflipped  = cv2.flip(img, 1)                            # 0 vertical, 1 horizontal, -1 both"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of OpenCV basics — imread, cvtColor, resize, edges — common patterns you'll see in production.\n# APPROACH  - Combine OpenCV basics — imread, cvtColor, resize, edges with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nimport cv2\nimport numpy as np\ndef detect_edges(in_path: str, out_path: str,\n                 *, low: int = 50, high: int = 150) -> None:\n    img = cv2.imread(in_path)\n    if img is None:\n        raise FileNotFoundError(in_path)\n    gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)\n    blurred = cv2.GaussianBlur(gray, (5, 5), 1.4)      # smooth before edges; reduces noise\n    edges = cv2.Canny(blurred, low, high)\n    cv2.imwrite(out_path, edges)\ndetect_edges(\"photo.jpg\", \"edges.jpg\")\ndef auto_canny(img_gray: np.ndarray, sigma: float = 0.33) -> np.ndarray:\n    \"\"\"Use median to set thresholds — works across lighting conditions.\"\"\"\n    v = np.median(img_gray)\n    low  = int(max(0, (1 - sigma) * v))\n    high = int(min(255, (1 + sigma) * v))\n    return cv2.Canny(img_gray, low, high)\ndef find_shapes(img_path: str) -> list:\n    img = cv2.imread(img_path)\n    gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)\n    blurred = cv2.GaussianBlur(gray, (5, 5), 1.4)\n    edges = cv2.Canny(blurred, 50, 150)\n    contours, _ = cv2.findContours(edges, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)\n    return contours\n# Draw contours on original:\ncontours = find_shapes(\"doc.jpg\")\nimg = cv2.imread(\"doc.jpg\")\ncv2.drawContours(img, contours, -1, (0, 255, 0), 2)\ncv2.imwrite(\"annotated.jpg\", img)\n# Downscale: cv2.INTER_AREA (best for shrinking)\n# Upscale:   cv2.INTER_CUBIC (good) or cv2.INTER_LANCZOS4 (best, slower)\n# Default:   INTER_LINEAR (fast, mediocre)\nsmall = cv2.resize(img, (640, 480), interpolation=cv2.INTER_AREA)\nbig   = cv2.resize(img, (3840, 2160), interpolation=cv2.INTER_CUBIC)"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of OpenCV basics — imread, cvtColor, resize, edges — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             when GPU helps.\nimport cv2\nfrom pathlib import Path\ndef edges_in_video(in_path: str, out_path: str) -> int:\n    cap = cv2.VideoCapture(in_path)\n    if not cap.isOpened():\n        raise FileNotFoundError(in_path)\n    fourcc = cv2.VideoWriter_fourcc(*\"mp4v\")\n    fps = cap.get(cv2.CAP_PROP_FPS)\n    w = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))\n    h = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))\n    writer = cv2.VideoWriter(out_path, fourcc, fps, (w, h), isColor=False)\n    n = 0\n    while True:\n        ok, frame = cap.read()\n        if not ok:\n            break\n        gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)\n        blurred = cv2.GaussianBlur(gray, (5, 5), 1.4)\n        edges = cv2.Canny(blurred, 50, 150)\n        writer.write(edges)\n        n += 1\n    cap.release()\n    writer.release()\n    return n\ndef has_cuda() -> bool:\n    try:\n        return cv2.cuda.getCudaEnabledDeviceCount() > 0\n    except Exception:\n        return False\ndef gpu_canny(img_path: str, out_path: str) -> None:\n    \"\"\"Faster Canny on big images / many images. Most cv2 wheels lack CUDA;\n    you have to build from source for it.\"\"\"\n    img = cv2.imread(img_path)\n    gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)\n    if has_cuda():\n        gpu_mat = cv2.cuda_GpuMat()\n        gpu_mat.upload(gray)\n        canny = cv2.cuda.createCannyEdgeDetector(50, 150)\n        edges_gpu = canny.detect(gpu_mat)\n        edges = edges_gpu.download()\n    else:\n        edges = cv2.Canny(gray, 50, 150)\n    cv2.imwrite(out_path, edges)\n# Decision rule:\n# Operation                          PIL/numpy      cv2           Best for\n# -----------------------------------------------------------------------\n# Load/save                          Pillow          cv2.imread     PIL: more formats; cv2: faster on big files\n# Resize (downscale)                  PIL.LANCZOS     INTER_AREA     cv2 ~2-3x faster\n# Gaussian blur                       scipy.ndimage   GaussianBlur   cv2 ~5-10x faster\n# Edge detection                      np.gradient     Canny          cv2 — Canny is the standard\n# Contour finding                     scipy           findContours   cv2 — better, well-tested\n# Video frames                         not supported   VideoCapture   cv2 only\n# GUI display                          not built-in    imshow         cv2 (or matplotlib)\n# Color space conversion               manual          cvtColor       cv2 — many built-in spaces\n# Affine warp                          PIL.transform   warpAffine     cv2 — much faster\n# Real-time / video                    no              yes            cv2\n# Easier API for one-off transforms   PIL             cv2            PIL — more pythonic\n#\n#   server / Docker                     -> opencv-python-headless (no Qt/GTK deps)\n#   desktop with display                -> opencv-python\n#   GPU                                  -> source-build cv2 with CUDA; or use specialized libs\n#   image-only ops                       -> PIL is often simpler\n#   video                                 -> cv2 always\n#   need contours / features              -> cv2\n#   need feature detectors (SIFT, ORB)    -> cv2.SIFT_create / cv2.ORB_create\n#\n# Anti-pattern: installing opencv-python on a headless server. Pulls\n# in Qt/GTK/X11 deps you don't need; image is 100MB+ larger; can fail\n# at import time when the display environment is missing. Use\n# opencv-python-headless on servers, Docker, Lambda, CI."
                  }
        ],
        tips: [
                  "Install `opencv-python-headless` on servers/Docker (no Qt/GTK deps; ~100MB smaller). Use `opencv-python` only on desktops where you need `cv2.imshow`.",
                  "OpenCV uses BGR by default. Convert before display/save with PIL or matplotlib: `cv2.cvtColor(img, cv2.COLOR_BGR2RGB)`.",
                  "`cv2.imread()` returns `None` (not an exception) when the file is missing or unreadable. Always check `if img is None`.",
                  "For resize: `INTER_AREA` is best for downscaling, `INTER_CUBIC` or `INTER_LANCZOS4` for upscaling, `INTER_LINEAR` is the fast default.",
                  "Canny edge thresholds depend on image content. `auto_canny` (median-based) adapts across lighting; manual tuning is for fixed-domain workflows.",
                  "For SIFT, SURF, ORB feature detection use `cv2.SIFT_create()`, `cv2.ORB_create()`. SIFT was patented but the patent expired in 2020."
        ],
        mistake: "Installing `opencv-python` on a headless server. It pulls in Qt/GTK/X11 dependencies you don't need (~100MB extra), can fail at import time when there's no display, and bloats Docker images. Use `opencv-python-headless` on servers, Docker, Lambda, CI.",
        shorthand: {
          verbose: "// Manual / verbose approach\n# Heavy install with GUI deps; can fail in headless containers\npip install opencv-python\n// More explicit but longer",
          concise: "# Headless variant — same API, no GUI deps\npip install opencv-python-headless",
        },
      },
      {
        id: "color-spaces-thresholding",
        fn: "Color spaces & thresholding — RGB / HSV / LAB, masks",
        desc: "For color-based segmentation (extract red regions, detect skin tones), HSV separates hue from brightness; LAB separates luminance from color. Pair with adaptive thresholding + morphology for clean masks.",
        category: "NumPy + OpenCV",
        subtitle: "cv2.cvtColor BGR ↔ HSV ↔ LAB, cv2.inRange, Otsu cv2.THRESH_OTSU, adaptive thresholding, morphological ops, cv2.bitwise_and",
        signature: "hsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV); mask = cv2.inRange(hsv, (0, 100, 100), (10, 255, 255))",
        descLong: "RGB is bad for color filtering — \"red\" varies wildly with lighting. HSV (Hue, Saturation, Value) separates color from brightness; you can say \"hue between 0-10 = red\" regardless of how bright. LAB (Luminance, A=green-red, B=blue-yellow) is even more perceptually uniform but less commonly needed. After thresholding, masks always have noise; morphological operations (`open` to remove specks, `close` to fill gaps) clean them up. The three examples solve the SAME concrete task — extract red regions from an image — at three depths: basic HSV inRange → wraparound handling + morphology → LAB color space + lighting-invariant thresholds + when ML segmentation wins.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Color spaces & thresholding — RGB / HSV / LAB, masks — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport cv2\nimport numpy as np\nimg = cv2.imread(\"photo.jpg\")\nhsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV)\n# HSV ranges in OpenCV:\n#   H: 0-179 (NOT 0-360! halved to fit in uint8)\n#   S: 0-255\n#   V: 0-255\n# Red wraps around at H=0 — pure red is at H=0 AND H=179.\n# This intro version misses the wrap; junior fixes it.\nlower_red = np.array([0,   100, 100])                  # H=0-10, S>100, V>100\nupper_red = np.array([10,  255, 255])\nmask = cv2.inRange(hsv, lower_red, upper_red)\n# Apply mask: keep red pixels, black out the rest.\nred_only = cv2.bitwise_and(img, img, mask=mask)\ncv2.imwrite(\"red.jpg\", red_only)\ncv2.imwrite(\"red_mask.jpg\", mask)\n# Common HSV ranges (OpenCV uint8 scale):\n#   Red:    H 0-10  AND  H 170-179 (wraps)\n#   Orange: H 11-25\n#   Yellow: H 26-34\n#   Green:  H 35-77\n#   Blue:   H 100-130\n#   Purple: H 131-155"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Color spaces & thresholding — RGB / HSV / LAB, masks — common patterns you'll see in production.\n# APPROACH  - Combine Color spaces & thresholding — RGB / HSV / LAB, masks with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nimport cv2\nimport numpy as np\ndef red_mask(img: np.ndarray) -> np.ndarray:\n    hsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV)\n    # Red wraps; need TWO ranges and union.\n    lower1 = np.array([0,   100, 80])\n    upper1 = np.array([10,  255, 255])\n    lower2 = np.array([170, 100, 80])\n    upper2 = np.array([179, 255, 255])\n    mask = cv2.inRange(hsv, lower1, upper1) | cv2.inRange(hsv, lower2, upper2)\n    # Morphology: open removes small noise, close fills small gaps.\n    kernel = cv2.getStructuringElement(cv2.MORPH_ELLIPSE, (5, 5))\n    mask = cv2.morphologyEx(mask, cv2.MORPH_OPEN,  kernel, iterations=1)\n    mask = cv2.morphologyEx(mask, cv2.MORPH_CLOSE, kernel, iterations=2)\n    return mask\n# Find connected components in the cleaned mask.\nimg = cv2.imread(\"photo.jpg\")\nmask = red_mask(img)\nn_labels, labels, stats, centroids = cv2.connectedComponentsWithStats(mask)\n# stats = [[x, y, w, h, area], ...]; label 0 is background.\nfor i in range(1, n_labels):\n    x, y, w, h, area = stats[i]\n    if area < 100: continue                            # ignore tiny specks\n    cv2.rectangle(img, (x, y), (x + w, y + h), (0, 255, 0), 2)\ncv2.imwrite(\"red_detected.jpg\", img)\ngray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)\n# threshold=0 means \"let Otsu pick\"; THRESH_OTSU activates the algorithm.\n_, otsu = cv2.threshold(gray, 0, 255, cv2.THRESH_BINARY + cv2.THRESH_OTSU)\n# Each pixel's threshold = mean of nearby pixels (block_size).\nadaptive = cv2.adaptiveThreshold(\n    gray, 255,\n    cv2.ADAPTIVE_THRESH_GAUSSIAN_C,\n    cv2.THRESH_BINARY,\n    blockSize=11,                                       # neighborhood size (odd)\n    C=2,                                                # constant subtracted from mean\n)"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Color spaces & thresholding — RGB / HSV / LAB, masks — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             when classical color thresholding fails, hybrid with ML.\nimport cv2\nimport numpy as np\n# L = Luminance (lightness), A = green-red, B = blue-yellow.\n# Threshold on AB (not L) for lighting-invariant color matching.\ndef red_mask_lab(img: np.ndarray) -> np.ndarray:\n    lab = cv2.cvtColor(img, cv2.COLOR_BGR2LAB)\n    L, A, B = cv2.split(lab)\n    # Red has high A (green-red toward red) and roughly neutral B.\n    # OpenCV LAB scales: L 0-255, A 0-255 (128 = neutral), B 0-255 (128 = neutral).\n    mask = (A > 150) & (np.abs(B.astype(int) - 128) < 30) & (L > 50)\n    mask = mask.astype(np.uint8) * 255\n    kernel = cv2.getStructuringElement(cv2.MORPH_ELLIPSE, (5, 5))\n    mask = cv2.morphologyEx(mask, cv2.MORPH_OPEN,  kernel)\n    mask = cv2.morphologyEx(mask, cv2.MORPH_CLOSE, kernel)\n    return mask\ndef dominant_color_score(img: np.ndarray, target_h: int, range_h: int = 10) -> float:\n    \"\"\"Returns fraction of high-saturation pixels with hue near target_h.\"\"\"\n    hsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV)\n    sat_mask = hsv[..., 1] > 100                       # only count vivid pixels\n    hue = hsv[..., 0]\n    target_mask = (np.abs(hue.astype(int) - target_h) < range_h) | \\\n                  (np.abs(hue.astype(int) - (180 + target_h)) < range_h)  # wrap\n    if not sat_mask.any():\n        return 0.0\n    return float((sat_mask & target_mask).sum()) / float(sat_mask.sum())\n# Decision rule:\n# Classical color thresholding wins:\n#   - Controlled lighting (studio, indoor)\n#   - Clear color separation (red apple on green grass)\n#   - Real-time / resource-constrained (no GPU)\n#   - Interpretable / debuggable\n#\n# ML segmentation (SAM, DeepLab, U-Net) wins:\n#   - Variable / unpredictable lighting\n#   - Subtle color differences (pinks vs salmons)\n#   - Need semantic understanding (what is a \"person\", not \"skin tone\")\n#   - Cross-class segmentation (multiple object types)\n#\n# Hybrid: classical first-pass to filter candidates → ML for hard cases.\n# Decision rule:\n# Operation / use case                  Tool / approach\n# -----------------------------------------------------\n# Color-based mask                       HSV inRange (consider wrap)\n# Color-based, lighting-invariant        LAB color space\n# Brightness threshold                    Otsu (auto) or fixed\n# Threshold under uneven lighting         adaptiveThreshold\n# Mask cleanup                           morphological open + close\n# Find blobs                             connectedComponentsWithStats\n# Find shapes / outlines                 findContours\n# Pixel-perfect masks                     ML segmentation (SAM, U-Net)\n# Background subtraction (video)         cv2.createBackgroundSubtractorMOG2\n# Skin tone detection                    YCbCr space + range; OR ML\n# Document scan (binary)                 adaptiveThreshold + invert\n# Salient object                          cv2.saliency or ML model\n# White balance                           cv2.xphoto.createSimpleWB\n#\n# Anti-pattern: thresholding directly on RGB channels and expecting\n# robust color filtering. Lighting changes (sun, shade, indoor)\n# move the same physical color across a huge range of RGB values.\n# Convert to HSV (or better, LAB) so hue/saturation can be filtered\n# independently of brightness."
                  }
        ],
        tips: [
                  "OpenCV HSV: H is 0-179 (halved from 360 to fit uint8), S and V are 0-255. Cheat sheet ranges in the intro example.",
                  "Red wraps at H=0/179. Always use TWO `inRange` calls and `|` (or) the masks together.",
                  "LAB color space is more perceptually uniform than HSV — better for subtle color differences and skin tones.",
                  "Always follow `inRange` thresholding with morphological `open` (remove noise) + `close` (fill gaps). Raw masks are speckled.",
                  "`Otsu` works when the image has bimodal histogram (clear foreground/background); fails on cluttered scenes.",
                  "`adaptiveThreshold` with `blockSize=11, C=2` is the document-scan recipe — local thresholding survives uneven lighting."
        ],
        mistake: "Thresholding RGB channels directly for color filtering. Lighting changes move the \"same\" color across a huge RGB range; your \"red detector\" works in the studio and fails outdoors. Convert to HSV (or LAB for lighting invariance), then threshold.",
        shorthand: {
          verbose: "// Manual / verbose approach\n# Brittle: RGB threshold breaks on any lighting change\nmask = (img[..., 2] > 150) & (img[..., 1] < 80) & (img[..., 0] < 80)\n// More explicit but longer",
          concise: "# Robust: HSV separates color from brightness\nhsv = cv2.cvtColor(img, cv2.COLOR_BGR2HSV)\nmask = cv2.inRange(hsv, (0, 100, 80), (10, 255, 255)) | cv2.inRange(hsv, (170, 100, 80), (179, 255, 255))",
        },
      },
    ],
  },

  // ── Section 3: Patterns — batch pipeline, formats, classical-vs-ML ─────────────────────────────────────────
  {
    id: "patterns",
    title: "Patterns — batch pipeline, formats, classical-vs-ML",
    entries: [
      {
        id: "batch-image-pipeline",
        fn: "Batch image pipeline — parallel, resilient, observable",
        desc: "Process N thousand images: thumbnail + watermark + write. Parallelize via `ProcessPoolExecutor`; track progress; log failures without crashing the batch.",
        category: "Patterns",
        subtitle: "concurrent.futures.ProcessPoolExecutor, tqdm progress, per-file try/except, output deduplication, before/after size telemetry",
        signature: "with ProcessPoolExecutor() as ex: list(tqdm(ex.map(process_one, paths), total=len(paths)))",
        descLong: "Image processing is CPU-bound — `ProcessPoolExecutor` (not `ThreadPoolExecutor`) gives real parallelism. The discipline: per-file try/except so one bad file doesn't crash the run; resumability via output-existence check; observability via `tqdm` and structured failure log. The three examples solve the SAME concrete task — process 10k JPEGs into thumbnails — at three depths: serial loop → ProcessPoolExecutor with progress → production with resumability, telemetry, and DLQ for failures.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Batch image pipeline — parallel, resilient, observable — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nfrom PIL import Image, ImageOps\nfrom pathlib import Path\ndef process_one(in_path: Path, out_dir: Path, size: int = 800) -> None:\n    out = out_dir / in_path.name\n    img = Image.open(in_path)\n    img = ImageOps.exif_transpose(img)\n    if img.mode != \"RGB\": img = img.convert(\"RGB\")\n    img.thumbnail((size, size), Image.LANCZOS)\n    img.save(out, \"JPEG\", quality=85, optimize=True)\nin_dir, out_dir = Path(\"input\"), Path(\"output\")\nout_dir.mkdir(exist_ok=True)\nfor p in in_dir.glob(\"*.jpg\"):\n    process_one(p, out_dir)\n# Serial loop on 10k images = ~5-30 minutes on a single core.\n# Junior tier parallelizes."
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Batch image pipeline — parallel, resilient, observable — common patterns you'll see in production.\n# APPROACH  - Combine Batch image pipeline — parallel, resilient, observable with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n# pip install tqdm\nfrom concurrent.futures import ProcessPoolExecutor, as_completed\nfrom pathlib import Path\nfrom PIL import Image, ImageOps\nfrom tqdm import tqdm\ndef process_one(in_path: Path, out_dir: Path, size: int = 800) -> tuple[Path, str | None]:\n    \"\"\"Returns (path, error_message_or_None). Caught at boundaries.\"\"\"\n    try:\n        out = out_dir / in_path.name\n        img = Image.open(in_path)\n        img = ImageOps.exif_transpose(img)\n        if img.mode != \"RGB\": img = img.convert(\"RGB\")\n        img.thumbnail((size, size), Image.LANCZOS)\n        img.save(out, \"JPEG\", quality=85, optimize=True)\n        return in_path, None\n    except Exception as e:\n        return in_path, f\"{type(e).__name__}: {e}\"\ndef batch_thumbnails(in_dir: Path, out_dir: Path,\n                     *, size: int = 800, workers: int = None) -> dict:\n    out_dir.mkdir(parents=True, exist_ok=True)\n    paths = list(in_dir.rglob(\"*.jpg\"))\n    successes, failures = 0, []\n    with ProcessPoolExecutor(max_workers=workers) as ex:\n        futures = {ex.submit(process_one, p, out_dir, size): p for p in paths}\n        for fut in tqdm(as_completed(futures), total=len(paths), desc=\"thumbnails\"):\n            path, err = fut.result()\n            if err: failures.append((path, err))\n            else: successes += 1\n    return {\"total\": len(paths), \"ok\": successes, \"failed\": len(failures),\n            \"failures\": failures}\nresult = batch_thumbnails(Path(\"input\"), Path(\"output\"), workers=8)\nprint(f\"Done: {result['ok']}/{result['total']}; {result['failed']} failed\")\nfor path, err in result[\"failures\"][:10]:\n    print(f\"  {path}: {err}\")\n# Notes:\n#   - ProcessPoolExecutor is required (Pillow releases GIL but workloads\n#     are still CPU-bound; threads don't parallelize on CPU).\n#   - workers=None defaults to os.cpu_count(); for I/O-bound workloads\n#     2-4x cores can help, but image processing rarely benefits.\n#   - Each worker imports modules from scratch — keep process_one simple."
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Batch image pipeline — parallel, resilient, observable — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             telemetry (size before/after), structured failure log.\nimport json, time\nfrom concurrent.futures import ProcessPoolExecutor, as_completed\nfrom pathlib import Path\nfrom PIL import Image, ImageOps\nfrom tqdm import tqdm\ndef process_one(in_path: Path, out_dir: Path, *,\n                size: int = 800, quality: int = 85,\n                skip_existing: bool = True) -> dict:\n    \"\"\"Returns telemetry dict; never raises.\"\"\"\n    rec = {\"in_path\": str(in_path), \"ok\": False, \"skipped\": False,\n           \"size_in_bytes\": 0, \"size_out_bytes\": 0, \"elapsed_ms\": 0,\n           \"error\": None}\n    started = time.monotonic()\n    out = out_dir / in_path.name\n    try:\n        if skip_existing and out.exists():\n            rec[\"skipped\"] = True\n            rec[\"ok\"] = True\n            return rec\n        rec[\"size_in_bytes\"] = in_path.stat().st_size\n        img = Image.open(in_path)\n        img = ImageOps.exif_transpose(img)\n        if img.mode != \"RGB\":\n            img = img.convert(\"RGB\")\n        img.thumbnail((size, size), Image.LANCZOS)\n        img.save(out, \"JPEG\", quality=quality, optimize=True, progressive=True)\n        rec[\"size_out_bytes\"] = out.stat().st_size\n        rec[\"ok\"] = True\n    except Exception as e:\n        rec[\"error\"] = f\"{type(e).__name__}: {e}\"\n    finally:\n        rec[\"elapsed_ms\"] = int((time.monotonic() - started) * 1000)\n    return rec\ndef batch(in_dir: Path, out_dir: Path, *, workers: int = None,\n          size: int = 800, quality: int = 85,\n          report_path: Path = Path(\"batch_report.jsonl\")) -> dict:\n    out_dir.mkdir(parents=True, exist_ok=True)\n    paths = sorted(in_dir.rglob(\"*.jpg\"))\n    successes = skipped = 0\n    failures: list[dict] = []\n    bytes_in = bytes_out = 0\n    with report_path.open(\"w\") as report, ProcessPoolExecutor(max_workers=workers) as ex:\n        futures = {ex.submit(process_one, p, out_dir,\n                            size=size, quality=quality): p for p in paths}\n        for fut in tqdm(as_completed(futures), total=len(paths), desc=\"batch\"):\n            rec = fut.result()\n            report.write(json.dumps(rec) + \"\\n\")\n            if rec[\"ok\"]:\n                if rec[\"skipped\"]: skipped += 1\n                else:               successes += 1\n                bytes_in  += rec[\"size_in_bytes\"]\n                bytes_out += rec[\"size_out_bytes\"]\n            else:\n                failures.append(rec)\n    return {\"total\": len(paths),\n            \"ok\": successes, \"skipped\": skipped, \"failed\": len(failures),\n            \"bytes_in\": bytes_in, \"bytes_out\": bytes_out,\n            \"compression_ratio\": (bytes_out / max(bytes_in, 1)),\n            \"first_failures\": failures[:20]}\n# Decision rule:\n#   <100 images, ad-hoc                    -> serial loop; not worth the parallelism setup\n#   1k-100k images, batch                   -> ProcessPoolExecutor + tqdm\n#   millions of images                       -> distribute (Celery/Dask) or stream from S3\n#   I/O-bound (S3 GET dominates)             -> ThreadPoolExecutor 2-4x cores\n#   CPU-bound (resize, encode)              -> ProcessPoolExecutor == cpu_count\n#   need to resume after crash               -> skip_existing=True; idempotent step\n#   need progress visible                    -> tqdm\n#   want failure DLQ                         -> append failures to JSONL; replay later\n#   want metrics                             -> compute compression ratio, p50/p99 elapsed\n#   GPU available                            -> consider torch / OpenCV CUDA for big images\n#\n# Anti-pattern: catching errors silently per-file but never reporting\n# them. The batch \"succeeds\" but 30% of images quietly failed; the\n# downstream system sees missing files and breaks. ALWAYS aggregate\n# failures into a structured log AND surface a summary at the end."
                  }
        ],
        tips: [
                  "Image processing is CPU-bound — use `ProcessPoolExecutor`, not `ThreadPoolExecutor`. Threads don't parallelize CPU work in Python.",
                  "`skip_existing` makes the batch resumable. Crashed midway? Re-run; only the unfinished files are processed.",
                  "Use `tqdm(as_completed(futures), total=N)` for live progress. Without it, large batches feel hung.",
                  "Append per-file results to JSONL — both successes (with telemetry) and failures (with error). Easy to grep, easy to compute aggregate metrics.",
                  "For S3 reads (I/O-bound), `ThreadPoolExecutor` with 2-4× cores can help; for the actual image work, fall back to processes.",
                  "Track `compression_ratio = bytes_out / bytes_in`. If it's near 1.0, your quality setting is too high; if too low, you're losing visible quality."
        ],
        mistake: "Catching errors per-file but never aggregating or surfacing them. The batch reports \"success\" but 30% of images quietly failed; the downstream pipeline sees missing files and breaks days later. Always emit a structured failure log AND print a summary at the end.",
        shorthand: {
          verbose: "# Silent failures\nfor p in paths:\n    try: process(p)\n    except: pass",
          concise: "# Aggregated failure log + summary\nresults = list(executor.map(process, paths))\nfailures = [r for r in results if r[\"error\"]]\nprint(f\"OK: {len(results) - len(failures)}; failed: {len(failures)}\")",
        },
      },
      {
        id: "image-quality-formats",
        fn: "Image formats & quality — JPEG, WebP, AVIF, content-aware choice",
        desc: "Pick the right format and quality for the smallest file at acceptable quality. JPEG for photos, PNG for diagrams, WebP / AVIF for modern browsers, perceptual quality (SSIM) for tuning.",
        category: "Patterns",
        subtitle: "JPEG quality, progressive, optimize, WebP, AVIF, PNG levels, content type → format, SSIM perceptual quality, EXIF stripping",
        signature: "img.save(\"out.webp\", \"WEBP\", quality=80, method=6)   # method 0-6, higher = slower/smaller",
        descLong: "JPEG dominated for 25 years; WebP (Google, 2010) and AVIF (AOMedia, 2019) compress better at the same perceptual quality but require modern browsers. The decision is content-aware: photos compress well as JPEG/WebP/AVIF; flat-color graphics need PNG (lossless) or WebP-lossless. Quality settings: JPEG 85 is the sweet spot, WebP 80, AVIF 70 — newer formats achieve same perceptual quality at lower numeric values. The three examples solve the SAME concrete task — pick a format and quality for smallest file at acceptable quality — at three depths: JPEG q=85 → format-by-content + WebP/AVIF → SSIM-driven quality optimization + responsive image sets.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Image formats & quality — JPEG, WebP, AVIF, content-aware choice — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nfrom PIL import Image\nimg = Image.open(\"photo.jpg\")\nimg.save(\"out.jpg\", \"JPEG\", quality=85, optimize=True, progressive=True)\n# JPEG quality cheat sheet:\n#   100  - lossless-ish; huge file; rarely needed\n#   90   - excellent; ~30% larger than 85 with negligible quality gain\n#   85   - sweet spot for most photos\n#   75   - acceptable; visible artifacts in flat regions\n#   60   - thumbnails / previews; visible artifacts\n#\n# optimize=True: ~5-10% smaller via Huffman optimization; ~2x slower\n# progressive=True: image renders top-to-bottom in chunks; better mobile UX,\n#                    same/slightly smaller file"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Image formats & quality — JPEG, WebP, AVIF, content-aware choice — common patterns you'll see in production.\n# APPROACH  - Combine Image formats & quality — JPEG, WebP, AVIF, content-aware choice with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\nfrom PIL import Image\nimport io\ndef has_alpha(img: Image.Image) -> bool:\n    return img.mode in (\"RGBA\", \"LA\") or (img.mode == \"P\" and \"transparency\" in img.info)\ndef is_photo(img: Image.Image) -> bool:\n    \"\"\"Cheap heuristic: photos have many unique colors; graphics have few.\"\"\"\n    small = img.resize((64, 64))\n    try:\n        n_colors = len(small.convert(\"RGB\").getcolors(maxcolors=64*64))\n        return n_colors > 1000\n    except TypeError:\n        return True                                     # too many colors to enumerate\ndef smart_save(img: Image.Image, base_path: str) -> tuple[str, int]:\n    \"\"\"Save with the format best suited to the content. Returns (path, bytes).\"\"\"\n    if has_alpha(img):\n        # Transparency → PNG (lossless) or WebP (better compression).\n        out_path = base_path + \".webp\"\n        img.save(out_path, \"WEBP\", lossless=False, quality=85, method=6)\n    elif is_photo(img):\n        out_path = base_path + \".jpg\"\n        if img.mode != \"RGB\":\n            img = img.convert(\"RGB\")\n        img.save(out_path, \"JPEG\", quality=85, optimize=True, progressive=True)\n    else:\n        # Flat graphic — PNG with high compression.\n        out_path = base_path + \".png\"\n        img.save(out_path, \"PNG\", optimize=True, compress_level=9)\n    return out_path, _file_size(out_path)\ndef _file_size(path: str) -> int:\n    import os\n    return os.path.getsize(path)\n# Format    Quality    Size      Notes\n# ---------------------------------------------------\n# JPEG      85         ~250KB    universal browser support\n# WebP      80         ~180KB    ~30% smaller than JPEG; modern browsers\n# AVIF      70         ~140KB    ~45% smaller than JPEG; very modern only\n# PNG       lossless   ~3MB      huge for photos; right for graphics\n# JPEG XL    lossy      ~120KB    excellent; not yet widely deployed\n#\n# The \"save in multiple formats and let the browser pick\" is the\n# <picture> + srcset pattern (senior tier).\n# method (0-6): higher = slower encoder, smaller output\n# quality (0-100): same as JPEG roughly\n# lossless=True: WebP-lossless (good for graphics; larger than lossy)"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Image formats & quality — JPEG, WebP, AVIF, content-aware choice — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             responsive sets, EXIF stripping.\nfrom PIL import Image, ImageCms\nimport io\nimport numpy as np\n# pip install scikit-image\nfrom skimage.metrics import structural_similarity as ssim\ndef jpeg_at_quality(img: Image.Image, quality: int) -> bytes:\n    if img.mode != \"RGB\":\n        img = img.convert(\"RGB\")\n    buf = io.BytesIO()\n    img.save(buf, \"JPEG\", quality=quality, optimize=True, progressive=True)\n    return buf.getvalue()\ndef find_min_quality(img: Image.Image, *, target_ssim: float = 0.95,\n                     min_q: int = 50, max_q: int = 95) -> tuple[int, bytes]:\n    \"\"\"Binary-search the lowest quality that meets target SSIM.\"\"\"\n    if img.mode != \"RGB\":\n        img = img.convert(\"RGB\")\n    orig = np.array(img)\n    lo, hi = min_q, max_q\n    best_q, best_bytes = max_q, jpeg_at_quality(img, max_q)\n    while lo <= hi:\n        mid = (lo + hi) // 2\n        encoded = jpeg_at_quality(img, mid)\n        decoded = np.array(Image.open(io.BytesIO(encoded)))\n        score = ssim(orig, decoded, channel_axis=2, data_range=255)\n        if score >= target_ssim:\n            best_q, best_bytes = mid, encoded\n            hi = mid - 1                                # try lower\n        else:\n            lo = mid + 1\n    return best_q, best_bytes\ndef generate_responsive_set(img: Image.Image, base: str,\n                            widths: list[int] = [320, 640, 1024, 1920]) -> dict:\n    \"\"\"Generate JPEG, WebP, AVIF at multiple widths.\"\"\"\n    if img.mode != \"RGB\":\n        img = img.convert(\"RGB\")\n    out = {\"jpeg\": [], \"webp\": [], \"avif\": []}\n    for w in widths:\n        if img.width <= w:\n            sized = img\n        else:\n            ratio = w / img.width\n            sized = img.resize((w, int(img.height * ratio)), Image.LANCZOS)\n        # JPEG.\n        jpg = f\"{base}-{w}.jpg\"\n        sized.save(jpg, \"JPEG\", quality=85, optimize=True, progressive=True)\n        out[\"jpeg\"].append((jpg, w))\n        # WebP.\n        webp = f\"{base}-{w}.webp\"\n        sized.save(webp, \"WEBP\", quality=80, method=6)\n        out[\"webp\"].append((webp, w))\n        # AVIF (Pillow 11+).\n        try:\n            avif = f\"{base}-{w}.avif\"\n            sized.save(avif, \"AVIF\", quality=70, speed=4)\n            out[\"avif\"].append((avif, w))\n        except KeyError:\n            pass                                         # AVIF not available\n    return out\n# HTML output:\n# <picture>\n#   <source type=\"image/avif\" srcset=\"hero-320.avif 320w, hero-640.avif 640w, ...\">\n#   <source type=\"image/webp\" srcset=\"hero-320.webp 320w, hero-640.webp 640w, ...\">\n#   <img src=\"hero-1024.jpg\" srcset=\"hero-320.jpg 320w, hero-640.jpg 640w, ...\"\n#        sizes=\"(max-width: 600px) 100vw, 50vw\" alt=\"...\">\n# </picture>\n# Decision rule:\n# Format selection by content:\n#   Photo (web)                          JPEG 85  → WebP 80 → AVIF 70\n#   Photo (lossless / archival)          PNG (huge); or AVIF lossless\n#   Graphic / diagram                    PNG (lossless)\n#   Logo with sharp edges                PNG; SVG if vector\n#   Animation                             GIF (limited) → WebP-animated → AVIF-animated\n#   Transparent overlay                   PNG → WebP\n# Quality selection:\n#   Visual evaluator says \"good\"          q=85 JPEG; 80 WebP; 70 AVIF\n#   Need exact target                      SSIM binary search; threshold 0.95\n#   Mobile / bandwidth-constrained        save multiple sizes; let CDN pick\n#   Responsive HTML                        <picture> + srcset; AVIF→WebP→JPEG fallback\n# Other:\n#   Strip EXIF / metadata                  re-save without exif= kwarg\n#   Preserve color profile                 save with icc_profile= kwarg\n#   Optimize file size                     optimize=True; method=6 for WebP\n#   Progressive load                       progressive=True for JPEG\n#\n# Anti-pattern: serving 4K JPEGs at quality 100 to a phone. The phone's\n# screen is 1080p; the user's data plan is metered. Use responsive\n# srcset so each device gets a size appropriate to its viewport.\n# A 320px-wide phone doesn't need a 4000px-wide image."
                  }
        ],
        tips: [
                  "JPEG 85 is the universal sweet spot — visible artifacts start below 75; gains above 90 are negligible.",
                  "WebP 80 ≈ JPEG 85 in perceptual quality at ~30% smaller file. AVIF 70 ≈ JPEG 85 at ~45% smaller.",
                  "Use `<picture>` + `srcset` to serve AVIF to modern browsers, WebP as fallback, JPEG as last resort. The browser picks the first format it supports.",
                  "For \"what quality is good enough?\", use SSIM (`scikit-image.metrics.structural_similarity`) — target 0.95 for \"indistinguishable to most viewers\".",
                  "Strip EXIF by default unless you have a reason to preserve it. Pillow doesn't write EXIF unless you pass `exif=` — so default behavior already strips it.",
                  "For responsive images, generate at 4-5 widths (320, 640, 1024, 1920, original). Each width × 3 formats = 12-15 files; CDN handles the rest."
        ],
        mistake: "Serving full-resolution photos to every device. A 4K JPEG sent to a phone wastes bandwidth (8× larger than needed), kills LCP (Largest Contentful Paint), and burns the user's data plan. Generate responsive sizes; use `srcset` so each device picks an appropriate width.",
        shorthand: {
          verbose: "# One huge image for everyone\nimg.save(\"hero.jpg\", \"JPEG\", quality=95)\n# 4K resolution served to phone with 320px viewport",
          concise: "# Responsive set: each device picks the right width and format\ngenerate_responsive_set(img, \"hero\", widths=[320, 640, 1024, 1920])\n# <picture> + srcset in HTML serves the right one",
        },
      },
      {
        id: "classical-vs-ml-vision",
        fn: "Classical vs ML — when each wins",
        desc: "Classical CV (cv2 + numpy) is fast, deterministic, interpretable. ML (CLIP, SAM, vision transformers) is flexible and zero-shot. The hybrid pipeline pays for itself once you have measured both.",
        category: "Patterns",
        subtitle: "classical features (Canny, contours, color hist), CLIP for zero-shot, segmentation models, latency / cost / interpretability tradeoffs",
        signature: "classical wins: < 20ms, < $0.001/img, deterministic; ML wins: zero-shot, semantic, cross-domain",
        descLong: "For \"is this image dark?\" or \"find the rectangle in the document scan\" — classical CV (cv2 + numpy) does it in milliseconds, deterministically, with code you can audit. For \"is this image safe for work?\" or \"what objects are in this image?\" — pretrained vision models (CLIP, SAM, BLIP) are zero-shot and far more accurate than rule-based pipelines. The hybrid: fast classical filter + ML for hard cases. The three examples solve the SAME concrete task — detect \"image contains text\" — at three depths: classical edge density → CLIP zero-shot → hybrid (classical fast path + CLIP fallback).",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Classical vs ML — when each wins — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport cv2\nimport numpy as np\ndef has_text_classical(img_path: str, *, edge_density_threshold: float = 0.06) -> bool:\n    \"\"\"Heuristic: text regions have high edge density.\"\"\"\n    img = cv2.imread(img_path)\n    if img is None:\n        return False\n    gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)\n    edges = cv2.Canny(gray, 100, 200)\n    density = edges.mean() / 255.0                     # fraction of edge pixels\n    return density > edge_density_threshold\n# Profile:\n#   Latency:  ~5-15ms per image (depends on size)\n#   Cost:     $0 (just CPU)\n#   Accuracy: ~75% — busy patterns also have high edge density\n#   Wins:     fast filter; zero infrastructure\n#   Loses:    busy non-text images, pure-text (handwriting low-edge)"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Classical vs ML — when each wins — common patterns you'll see in production.\n# APPROACH  - Combine Classical vs ML — when each wins with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n# pip install open_clip_torch torch\nimport torch, open_clip\nfrom PIL import Image\n# Load once at module level (~500MB, several seconds).\n_clip_model, _, _clip_preprocess = open_clip.create_model_and_transforms(\n    \"ViT-B-32\", pretrained=\"laion2b_s34b_b79k\"\n)\n_clip_tokenizer = open_clip.get_tokenizer(\"ViT-B-32\")\n_clip_model.eval()\nLABELS = [\"text on a screen\", \"handwritten text\", \"no text — pure photo\"]\ndef has_text_clip(img_path: str) -> tuple[bool, dict]:\n    \"\"\"Returns (has_text, scores).\"\"\"\n    img = Image.open(img_path)\n    img_t = _clip_preprocess(img).unsqueeze(0)\n    text_t = _clip_tokenizer(LABELS)\n    with torch.no_grad(), torch.amp.autocast(\"cuda\" if torch.cuda.is_available() else \"cpu\"):\n        img_features = _clip_model.encode_image(img_t)\n        text_features = _clip_model.encode_text(text_t)\n        img_features = img_features / img_features.norm(dim=-1, keepdim=True)\n        text_features = text_features / text_features.norm(dim=-1, keepdim=True)\n        logits = (100.0 * img_features @ text_features.T)\n        probs = logits.softmax(dim=-1).cpu().numpy()[0]\n    scores = {l: float(p) for l, p in zip(LABELS, probs)}\n    has_text = scores[\"text on a screen\"] + scores[\"handwritten text\"] > 0.5\n    return has_text, scores\n# Profile:\n#   Latency:  ~50-200ms CPU; ~10-30ms GPU\n#   Cost:     $0 self-hosted; ~$0.0005/req on managed inference\n#   Accuracy: ~92% (zero-shot)\n#   Wins:     accuracy, no labeled data needed, semantic understanding\n#   Loses:    latency, model size (~500MB), GPU helps a lot"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Classical vs ML — when each wins — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             borderline cases; cost-aware routing.\nimport cv2, torch, open_clip\nfrom PIL import Image\n_clip_model, _, _clip_preprocess = open_clip.create_model_and_transforms(\n    \"ViT-B-32\", pretrained=\"laion2b_s34b_b79k\")\n_clip_tokenizer = open_clip.get_tokenizer(\"ViT-B-32\")\n_clip_model.eval()\nLABELS = [\"text on a screen\", \"handwritten text\", \"no text — pure photo\"]\ndef edge_density(img_path: str) -> float:\n    img = cv2.imread(img_path)\n    if img is None: return 0.0\n    gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)\n    return cv2.Canny(gray, 100, 200).mean() / 255.0\ndef clip_score(img_path: str) -> float:\n    img = Image.open(img_path)\n    img_t = _clip_preprocess(img).unsqueeze(0)\n    text_t = _clip_tokenizer(LABELS)\n    with torch.no_grad():\n        f_img = _clip_model.encode_image(img_t); f_img /= f_img.norm(dim=-1, keepdim=True)\n        f_txt = _clip_model.encode_text(text_t); f_txt /= f_txt.norm(dim=-1, keepdim=True)\n        probs = (100.0 * f_img @ f_txt.T).softmax(dim=-1).cpu().numpy()[0]\n    return float(probs[0] + probs[1])                  # text labels\n# Confidence bands:\n#   density < 0.03                -> definitely no text (classical)\n#   0.03 <= density < 0.10        -> uncertain — call CLIP\n#   density >= 0.10               -> probably text (classical, but verify with CLIP if cost allows)\ndef has_text_hybrid(img_path: str, *, low: float = 0.03, high: float = 0.10) -> dict:\n    density = edge_density(img_path)\n    if density < low:\n        return {\"has_text\": False, \"tier\": \"classical\", \"density\": density}\n    if density >= high and density < 0.25:             # 0.25+ = noisy; use CLIP\n        return {\"has_text\": True, \"tier\": \"classical\", \"density\": density}\n    score = clip_score(img_path)\n    return {\"has_text\": score > 0.5, \"tier\": \"clip\",\n            \"density\": density, \"clip_score\": score}\n# tier=\"classical\" should handle ~70-85% of traffic\n# tier=\"clip\" should handle the rest\n# If tier=\"clip\" > 30%, classical thresholds need re-tuning\n# Decision rule:\n#   simple geometric query (size, color, edges)         -> classical (cv2 + numpy)\n#   semantic query (objects, text, NSFW)                  -> ML (CLIP, BLIP)\n#   need < 50ms p99                                       -> classical or hybrid\n#   need < $0.001/img cost                                -> classical first; ML for hard cases\n#   need bounding boxes                                    -> ML detection (YOLO, DETR)\n#   need pixel-perfect masks                               -> ML segmentation (SAM)\n#   labeled data available                                 -> fine-tune ResNet/EfficientNet\n#   labeled data scarce                                    -> CLIP zero-shot\n#   need explainability                                    -> classical (rules are auditable)\n#   adversarial inputs                                     -> classical (deterministic)\n#   high volume + low budget                              -> hybrid; tune the bands\n#\n# Anti-pattern: replacing a working classical pipeline with a vision\n# transformer because \"ML is more accurate\". For \"is this image\n# blurry?\" or \"find the dominant color\", classical gets 95% accuracy\n# at 1ms; CLIP gets 96% at 100ms and $0.001/req. The \"improvement\" is\n# not worth 100x cost. Measure both; pick by accuracy × latency × cost."
                  }
        ],
        tips: [
                  "Run classical first — it's 100× cheaper than ML and handles 70%+ of cases. Use ML only when classical confidence is low.",
                  "CLIP is the zero-shot workhorse for semantic image queries — \"is this an X?\" without training data.",
                  "For pixel-perfect segmentation use SAM (Segment Anything); for detection use YOLO or DETR; for classification with labels, fine-tune ResNet/EfficientNet.",
                  "Track tier mix in production. If ML-tier handles >30%, the classical thresholds drifted and need retuning.",
                  "For adversarial input handling (uploads from users), prefer classical — deterministic and auditable.",
                  "CLIP, SAM, YOLO are all CPU-runnable but far faster on GPU (~10× speedup). Budget for GPU if you serve >10 RPS."
        ],
        mistake: "Replacing a working classical pipeline with a vision transformer because \"ML is better\". For \"is this image blurry?\" or \"find the dominant color\", classical gets 95% accuracy at 1ms; CLIP gets 96% at 100ms and $0.001/req — 100× cost for 1% gain. Measure both; pick by accuracy × latency × cost.",
        shorthand: {
          verbose: "# CLIP for what cv2 already solves\ndef is_blurry(img_path):\n    return clip_zero_shot(img_path, [\"blurry\", \"sharp\"]) == \"blurry\"\n# 100ms, $0.001/req",
          concise: "# Classical: Laplacian variance — blur detector\ndef is_blurry(img_path, threshold=100.0):\n    img = cv2.imread(img_path, cv2.IMREAD_GRAYSCALE)\n    return cv2.Laplacian(img, cv2.CV_64F).var() < threshold\n# 1ms, $0",
        },
      },
    ],
  },
]

export default { meta, sections }

export const meta = {
  "id": "documentation",
  "label": "Documentation",
  "icon": "📚",
  "description": "Documenting Python projects: MkDocs (the Markdown-friendly default; mkdocs-material theme dominates), Sphinx (the RST-historical, autodoc-strong choice), docstring conventions, doctests that stay in sync with code, and the mkdocs-vs-sphinx decision."
}

export const sections = [

  // ── Section 1: MkDocs — Markdown-driven docs sites ─────────────────────────────────────────
  {
    id: "mkdocs",
    title: "MkDocs — Markdown-driven docs sites",
    entries: [
      {
        id: "mkdocs-basics",
        fn: "MkDocs basics — config, serve, mkdocs-material",
        desc: "Build a docs site from Markdown files. `mkdocs new` scaffolds; `mkdocs serve` is hot-reload; `mkdocs-material` is the de-facto theme. The fastest \"make a docs site\" setup in Python.",
        category: "MkDocs",
        subtitle: "mkdocs new, mkdocs.yml, mkdocs serve, nav structure, mkdocs-material features (search, palette, code copy)",
        signature: "pip install mkdocs mkdocs-material; mkdocs new myproject; mkdocs serve",
        descLong: "MkDocs takes Markdown files and renders a docs site — fast, simple, hot-reload during dev. The default theme is plain; almost every modern project uses `mkdocs-material` (Material for MkDocs) which adds search, dark mode, code copy buttons, admonitions, and dozens of plugins. The three examples solve the SAME concrete task — set up a docs site with home + reference pages — at three depths: minimal `mkdocs new` → mkdocs-material with search + palette + nav → production with custom domain, plugins, and CI deployment.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of MkDocs basics — config, serve, mkdocs-material — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# pip install mkdocs\n# Create scaffold:\n#   $ mkdocs new myproject\n#   $ cd myproject\n# Creates:\n#   myproject/\n#     mkdocs.yml\n#     docs/\n#       index.md         <- home page\n# Add more pages — just create .md files under docs/:\n#   docs/getting-started.md\n#   docs/api/users.md\n# site_name: My Project\n# nav:\n#   - Home: index.md\n#   - Getting Started: getting-started.md\n#   - API:\n#       - Users: api/users.md\n#       - Orders: api/orders.md\n# Run dev server with hot reload:\n#   $ mkdocs serve\n# Opens http://localhost:8000 — edits to .md auto-refresh in browser.\n# Build static site:\n#   $ mkdocs build         # output in site/\n# Serve site/ from any static host (S3, Netlify, GitHub Pages).\n# Default theme is bare-bones. Junior tier upgrades to mkdocs-material."
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of MkDocs basics — config, serve, mkdocs-material — common patterns you'll see in production.\n# APPROACH  - Combine MkDocs basics — config, serve, mkdocs-material with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             code copy, admonitions.\n# pip install mkdocs-material\n# site_name: My Project\n# site_url: https://docs.example.com\n# repo_url: https://github.com/myorg/myproject\n# repo_name: myorg/myproject\n#\n# theme:\n#   name: material\n#   logo: assets/logo.svg\n#   favicon: assets/favicon.png\n#   palette:\n#     - scheme: default\n#       primary: indigo\n#       accent: indigo\n#       toggle: { icon: material/brightness-7, name: Switch to dark }\n#     - scheme: slate\n#       primary: indigo\n#       accent: indigo\n#       toggle: { icon: material/brightness-4, name: Switch to light }\n#   features:\n#     - navigation.tabs               # top-level tabs\n#     - navigation.sections           # group nav by section\n#     - navigation.expand              # auto-expand sections\n#     - navigation.top                 # back-to-top button\n#     - search.suggest                 # search auto-suggest\n#     - search.highlight               # highlight matched terms\n#     - content.code.copy              # copy button on code blocks\n#     - content.code.annotate          # numbered annotations\n#     - content.tabs.link              # cross-tab linking\n#\n# markdown_extensions:\n#   - admonition                       # !!! note ... callouts\n#   - pymdownx.details                 # collapsible sections\n#   - pymdownx.superfences             # nested code blocks\n#   - pymdownx.tabbed:                 # tabbed content\n#       alternate_style: true\n#   - pymdownx.highlight:              # syntax highlighting\n#       anchor_linenums: true\n#   - pymdownx.inlinehilite\n#   - pymdownx.snippets                # include other files\n#   - tables\n#   - footnotes\n#   - toc:\n#       permalink: true                # add # link next to headings\n# !!! note \"Optional title\"\n#     This renders as a callout box.\n#\n# !!! warning\n#     Common pitfall here.\n#\n#     Content of tab 1\n#\n#     Content of tab 2\n#\n# # code block with line numbers + highlight specific lines:\n# ```python hl_lines=\"2 3\" linenums=\"1\"\n# def hello():\n#     print(\"highlighted\")\n#     return 42\n# ```\n# Run:\n#   $ mkdocs serve\n# Material design, search, dark mode all work."
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of MkDocs basics — config, serve, mkdocs-material — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             link checking, edit-on-github.\n# site_name: My Project\n# site_url: https://docs.example.com\n# site_description: Production-grade documentation for My Project.\n# site_author: My Team\n#\n# repo_url: https://github.com/myorg/myproject\n# edit_uri: edit/main/docs/                            # \"Edit on GitHub\" link\n#\n# theme:\n#   name: material\n#   custom_dir: overrides                              # override theme templates\n#   features:\n#     - navigation.tabs\n#     - navigation.sections\n#     - navigation.indexes                              # section index pages\n#     - navigation.tracking                             # update URL on scroll\n#     - navigation.footer                               # prev/next at footer\n#     - search.suggest\n#     - search.highlight\n#     - search.share                                    # share URL with search query\n#     - content.code.copy\n#     - content.code.annotate\n#     - content.tabs.link\n#     - content.tooltips\n#     - content.action.edit                             # edit-this-page button\n#\n# extra:\n#   social:\n#     - icon: fontawesome/brands/github\n#       link: https://github.com/myorg/myproject\n#     - icon: fontawesome/brands/python\n#       link: https://pypi.org/project/myproject/\n#   version:\n#     provider: mike                                    # multi-version dropdown\n#\n# plugins:\n#   - search\n#   - mkdocstrings:                                     # auto-generated API docs\n#       default_handler: python\n#       handlers:\n#         python:\n#           options:\n#             show_root_heading: true\n#             show_source: true\n#             docstring_style: google\n#   - git-revision-date-localized:                       # \"Last updated: ...\" per page\n#       enable_creation_date: true\n#   - minify:                                             # minify HTML output\n#       minify_html: true\n#   - redirects:                                          # 301 old URLs to new\n#       redirect_maps:\n#         \"old/path.md\": \"new/path.md\"\n# pip install mike\n# $ mike deploy --push --update-aliases 1.4 latest\n# $ mike set-default --push latest\n# Now docs.example.com/1.4/ AND /latest/ both serve; switcher in UI.\n# .github/workflows/docs.yml\n# name: docs\n# on:\n#   push:\n#     branches: [main]\n# permissions:\n#   contents: write\n# jobs:\n#   deploy:\n#     runs-on: ubuntu-latest\n#     steps:\n#       - uses: actions/checkout@v4\n#         with: { fetch-depth: 0 }\n#       - uses: actions/setup-python@v5\n#         with: { python-version: \"3.12\" }\n#       - run: pip install mkdocs-material mkdocstrings[python] mkdocs-git-revision-date-localized-plugin\n#       - run: mkdocs gh-deploy --force\n# pip install linkchecker\n# $ mkdocs build && linkchecker site/\n# overrides/main.html (extends material theme)\n# {% extends \"base.html\" %}\n# {% block extrahead %}\n#   <link rel=\"stylesheet\" href=\"{{ 'assets/extra.css' | url }}\">\n# {% endblock %}\n# Decision rule:\n#   Markdown source                       -> MkDocs\n#   need autodoc from docstrings           -> MkDocs + mkdocstrings (Python plugin)\n#   technical reference (API)               -> mkdocstrings handles it\n#   need RST                                  -> Sphinx (next entries)\n#   need versioned docs                       -> mike plugin for MkDocs\n#   need PDF export                            -> Sphinx is better for that\n#   internal team docs                         -> MkDocs; ship to internal hosting\n#   public OSS project                         -> ReadTheDocs (Sphinx) OR mkdocs gh-deploy\n#   docstring style                            -> Google (most readable; mkdocstrings supports)\n#   want full text search                      -> built-in client-side; or Algolia DocSearch for big sites\n#   custom theme                               -> overrides/ dir extends material\n#   plugins (typer, draw.io, mermaid)          -> all available; check pypi\n#\n# Anti-pattern: maintaining docs in two places (README.md + docs/).\n# README drifts; docs site shows stale info; users find both and\n# trust the wrong one. Make the docs site the source of truth; let\n# README link to it for setup-only."
                  }
        ],
        tips: [
                  "`mkdocs-material` is the de-facto theme — search, dark mode, copy buttons, admonitions all work out of the box.",
                  "Use `navigation.tabs` for top-level tabs (multi-section sites) and `navigation.indexes` to make a section's landing page act as the index.",
                  "For \"Edit on GitHub\" links, set `repo_url` and `edit_uri: edit/main/docs/`. Material adds the button automatically.",
                  "`mkdocstrings` plugin auto-generates API reference from docstrings — point at a module, get formatted docs. Works with Google / NumPy / Sphinx styles.",
                  "For multi-version docs, use the `mike` tool — deploys each version to a separate path; UI switcher; one site for all versions.",
                  "Run `linkchecker site/` in CI to catch broken links — invaluable for docs that grow over time."
        ],
        mistake: "Maintaining the same content in both README.md and the docs site. README drifts; docs site shows stale info; users find both and trust the wrong one. Pick one source of truth (usually the docs site) and have README link to it for installation/quickstart only.",
        shorthand: {
          verbose: "# Two sources, both maintained, both drift\nREADME.md: \"see installation steps below\" (out of date)\ndocs/install.md: \"see installation steps\" (newer)",
          concise: "# Single source: docs/. README points to it.\nREADME.md: \"Full docs: https://docs.example.com\"\n# docs/install.md is the only place install is documented.",
        },
      },
      {
        id: "mkdocs-mkdocstrings",
        fn: "mkdocstrings — auto-generated API reference from docstrings",
        desc: "`mkdocstrings` reads docstrings + type hints from your Python modules and generates formatted API reference pages. Pair with Google-style docstrings; the result is on par with Sphinx autodoc.",
        category: "MkDocs",
        subtitle: "mkdocstrings[python], ::: directive, options (show_source, members, filters), Google / NumPy docstring styles, type-hint integration",
        signature: "# In docs/api/users.md:\n::: myproject.users.User\n    options:\n      members: [name, save, find_by_email]",
        descLong: "`mkdocstrings` is the killer feature that makes MkDocs a real alternative to Sphinx. Pointing `::: my.module` in a Markdown file generates a styled section with classes, methods, type-hint-aware signatures, and rendered docstrings. Pair with Google-style docstrings (most human-readable) and `mkdocs-material` for nice output. The three examples solve the SAME concrete task — generate API reference for a `User` class — at three depths: basic `:::` directive → filtered options + type hints → production with cross-references, custom templates, and live-updating from Python source.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of mkdocstrings — auto-generated API reference from docstrings — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# pip install \"mkdocstrings[python]\"\n# plugins:\n#   - search\n#   - mkdocstrings:\n#       handlers:\n#         python:\n#           options:\n#             docstring_style: google     # or \"numpy\" or \"sphinx\"\n#             show_source: true\nclass User:\n    \"\"\"A user account.\n    Attributes:\n        id: Unique identifier.\n        email: Primary email.\n        is_active: Whether the user is active.\n    \"\"\"\n    def __init__(self, email: str):\n        self.email = email\n    def save(self) -> \"User\":\n        \"\"\"Save the user to the database.\n        Returns:\n            The saved user.\n        Raises:\n            ValidationError: If email is invalid.\n        \"\"\"\n        return self\n# # Users API\n#\n# ::: myproject.users.User\n#\n# That's it. mkdocstrings introspects the module, reads docstrings,\n# renders a formatted section with class signature, attributes,\n# methods, and the docstring prose.\n# Build:\n# $ mkdocs serve\n# Visit /api/users/ — see the auto-generated reference."
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of mkdocstrings — auto-generated API reference from docstrings — common patterns you'll see in production.\n# APPROACH  - Combine mkdocstrings — auto-generated API reference from docstrings with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             customize options.\n# docs/api/users.md\n# # Users API\n#\n# ## User class\n#\n# ::: myproject.users.User\n#     options:\n#       show_root_heading: false           # don't render the \"User\" heading; we did\n#       show_source: true                   # link to source on GitHub\n#       members:                            # render only these\n#         - save\n#         - find_by_email\n#         - delete\n#       inherited_members: false\n#       show_signature_annotations: true    # show type hints in signature\n#       separate_signature: true             # signature on its own line\n#       merge_init_into_class: true          # merge __init__ params into class docs\n#       docstring_section_style: table       # render Args/Returns as table\n# ::: myproject.users\n#     options:\n#       filters:\n#         - \"!^_\"                            # hide everything starting with _\n#         - \"!^test_\"                        # hide test_*\n#         - \"save\"                            # but always include 'save'\n# Standard MkDocs links work:\n#   [User class][myproject.users.User]\n#   See [find_by_email()][myproject.users.User.find_by_email]\n# These resolve to the auto-generated anchors.\n# # Authentication\n#\n# ## Sign-up flow\n#\n# A new user is created via [`User`][myproject.users.User]. After\n# validation, call [`save()`][myproject.users.User.save].\n#\n# ::: myproject.users.User\n#     options:\n#       show_bases: true\n#       show_signature: true\n# Default: searches sys.path. For src-layout projects:\n# # mkdocs.yml\n# plugins:\n#   - mkdocstrings:\n#       handlers:\n#         python:\n#           paths: [src]                    # add src/ to import path\n# Google (most human-readable):\n#   def fn(x: int) -> str:\n#       \"\"\"Short summary.\n#\n#       Longer description over multiple lines.\n#\n#       Args:\n#           x: The input value.\n#\n#       Returns:\n#           The string form of x.\n#\n#       Raises:\n#           ValueError: If x < 0.\n#       \"\"\"\n#\n# NumPy (popular in scientific Python):\n#   def fn(x):\n#       \"\"\"Short summary.\n#\n#       Parameters\n#       ----------\n#       x : int\n#           The input value.\n#\n#       Returns\n#       -------\n#       str\n#           The string form of x.\n#       \"\"\"\n#\n# RST/Sphinx (verbose; rarely chosen for new code):\n#   def fn(x):\n#       \"\"\"Short summary.\n#\n#       :param x: The input value.\n#       :type x: int\n#       :returns: The string form of x.\n#       :rtype: str\n#       \"\"\"\n# Pick Google for new projects; mkdocstrings + Google is the smoothest combo."
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of mkdocstrings — auto-generated API reference from docstrings — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             mkdocs-autorefs, gen-files for fully-automated API pages.\n# plugins:\n#   - search\n#   - autorefs                               # cross-refs by name across whole site\n#   - gen-files:                             # generate doc files dynamically\n#       scripts:\n#         - scripts/gen_api_pages.py\n#   - literate-nav:                          # use a SUMMARY.md for nav\n#       nav_file: SUMMARY.md\n#   - mkdocstrings:\n#       default_handler: python\n#       handlers:\n#         python:\n#           paths: [src]\n#           import:                          # follow imports across packages\n#             - https://docs.python.org/3/objects.inv\n#             - https://pandas.pydata.org/docs/objects.inv\n#           options:\n#             docstring_style: google\n#             docstring_options:\n#               ignore_init_summary: true\n#               returns_named_value: false\n#             show_source: true\n#             show_root_heading: true\n#             show_root_full_path: false\n#             show_object_full_path: false\n#             show_category_heading: true\n#             show_signature_annotations: true\n#             separate_signature: true\n#             merge_init_into_class: true\n#             docstring_section_style: spacy\n#             members_order: source\n#             filters: [\"!^_\"]\n#             group_by_category: true\n#             show_symbol_type_heading: true\n#             show_symbol_type_toc: true\n# \"\"\"Run by mkdocs-gen-files plugin at build time.\"\"\"\n# from pathlib import Path\n# import mkdocs_gen_files\n#\n# nav = mkdocs_gen_files.Nav()\n#\n# for path in sorted(Path(\"src\").rglob(\"*.py\")):\n#     module_path = path.relative_to(\"src\").with_suffix(\"\")\n#     doc_path = path.relative_to(\"src\").with_suffix(\".md\")\n#     full_doc_path = Path(\"api\") / doc_path\n#     parts = list(module_path.parts)\n#\n#     if parts[-1] == \"__init__\":\n#         parts = parts[:-1]\n#         doc_path = doc_path.with_name(\"index.md\")\n#         full_doc_path = full_doc_path.with_name(\"index.md\")\n#     elif parts[-1] == \"__main__\":\n#         continue\n#\n#     nav[parts] = doc_path.as_posix()\n#\n#     with mkdocs_gen_files.open(full_doc_path, \"w\") as fd:\n#         identifier = \".\".join(parts)\n#         print(f\"::: {identifier}\", file=fd)\n#\n#     mkdocs_gen_files.set_edit_path(full_doc_path, path)\n#\n# with mkdocs_gen_files.open(\"api/SUMMARY.md\", \"w\") as nav_file:\n#     nav_file.writelines(nav.build_literate_nav())\n# Now adding a new module to src/ AUTOMATICALLY appears in the docs.\n# Decision rule:\n#   simple module reference                 -> ::: module.path\n#   class with custom heading                -> ::: with options.show_root_heading\n#   filter members                            -> options.members or options.filters\n#   nested heading levels                     -> heading_level option\n#   cross-refs across pages                   -> mkdocs-autorefs plugin\n#   auto-generate page per module             -> mkdocs-gen-files + script\n#   versioned API docs                         -> mike + per-version mkdocstrings\n#   external Python objects                    -> objects.inv inventories (Sphinx-style intersphinx)\n#   docstrings include code samples            -> mkdocstrings renders fenced code blocks fine\n#   docstring tests                              -> doctest module + sybil/pytest\n#\n# Anti-pattern: writing API documentation by hand alongside docstrings.\n# Two sources, both maintained, both drift. mkdocstrings + good\n# docstrings means the docs ARE the docstrings — single source of truth,\n# always in sync with code."
                  }
        ],
        tips: [
                  "Pick Google-style docstrings for new projects — most human-readable AND well-supported by mkdocstrings.",
                  "`paths: [src]` in the mkdocstrings handler is essential for src-layout projects; without it, your modules aren't importable.",
                  "`merge_init_into_class: true` shows `__init__` parameters as part of the class signature — cleaner output for classes used as constructors.",
                  "Use `mkdocs-autorefs` for cross-page references by name — `[User][myproject.users.User]` works without manual anchor management.",
                  "For fully auto-generated API pages (one .md per module), use `mkdocs-gen-files` + a script that walks `src/` and writes `:::` directives.",
                  "`intersphinx`-style cross-refs work via `import:` URLs to `objects.inv` — link to Python stdlib, pandas, numpy etc. with auto-generated anchors."
        ],
        mistake: "Hand-writing API docs alongside docstrings. The two drift; readers find both and trust the wrong one; updating means editing two places. Use `mkdocstrings` so docstrings ARE the docs — single source of truth, always in sync with code.",
        shorthand: {
          verbose: "# Hand-write API docs separate from docstrings — drift inevitable\ndocs/api/users.md:\n  ## User\n  Args:\n    email: ...   # transcribed from docstring; updates lag",
          concise: "# mkdocstrings renders from docstrings — always in sync\ndocs/api/users.md:\n  ::: myproject.users.User",
        },
      },
      {
        id: "mkdocs-deployment",
        fn: "MkDocs deployment — gh-pages, ReadTheDocs, custom domain",
        desc: "Build + deploy a docs site: `mkdocs gh-deploy` for GitHub Pages, GitHub Actions for clean CI deploys, ReadTheDocs for fully managed hosting, custom domain via DNS.",
        category: "MkDocs",
        subtitle: "mkdocs gh-deploy, GitHub Actions deploy job, ReadTheDocs config (.readthedocs.yaml), CNAME for custom domain, mike for versioning",
        signature: "mkdocs gh-deploy --force   # builds and pushes site/ to gh-pages branch",
        descLong: "MkDocs builds a static site (`mkdocs build` → `site/`); deployment is just hosting that directory. The common paths: (a) `mkdocs gh-deploy` pushes to a `gh-pages` branch and GitHub Pages serves it; (b) GitHub Actions builds on every push to main, deploys to gh-pages; (c) ReadTheDocs builds your repo on push and hosts at `*.readthedocs.io`. Custom domain works on any of them via a CNAME. The three examples solve the SAME concrete task — deploy the docs site so readers can visit it — at three depths: `mkdocs gh-deploy` once → GitHub Actions on push → multi-version via mike + ReadTheDocs config.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of MkDocs deployment — gh-pages, ReadTheDocs, custom domain — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# Manual deploy (one-off):\n#   $ mkdocs gh-deploy --force\n# This:\n#   1. Runs 'mkdocs build' to produce site/\n#   2. Force-pushes site/ to the 'gh-pages' branch\n#   3. GitHub Pages serves it at https://USER.github.io/REPO/\n# Settings → Pages → Source = \"Deploy from a branch\", Branch = gh-pages.\n# Caveats:\n#   - Manual; nothing automatic. Junior tier adds CI.\n#   - User has to remember to run before pushing changes.\n#   - --force overwrites without merging.\n# Custom domain:\n# Create docs/CNAME (a file with one line):\n#   docs.example.com\n# After 'mkdocs gh-deploy', add CNAME record at your DNS provider:\n#   CNAME docs -> USER.github.io\n#   $ git log gh-pages --oneline -5\n#   $ curl -I https://docs.example.com/\n#   HTTP/2 200"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of MkDocs deployment — gh-pages, ReadTheDocs, custom domain — common patterns you'll see in production.\n# APPROACH  - Combine MkDocs deployment — gh-pages, ReadTheDocs, custom domain with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n# name: Deploy Docs\n# on:\n#   push:\n#     branches: [main]\n#   pull_request:\n#     branches: [main]\n# permissions:\n#   contents: write                                 # write to gh-pages\n# jobs:\n#   build:\n#     runs-on: ubuntu-latest\n#     steps:\n#       - uses: actions/checkout@v4\n#         with:\n#           fetch-depth: 0                          # mike needs full history\n#       - uses: actions/setup-python@v5\n#         with: { python-version: \"3.12\" }\n#       - name: Install\n#         run: |\n#           pip install mkdocs-material \\\n#                       mkdocstrings[python] \\\n#                       mkdocs-git-revision-date-localized-plugin \\\n#                       mkdocs-minify-plugin\n#       - name: Build\n#         run: mkdocs build --strict           # --strict fails on warnings\n#       - name: Link check\n#         run: |\n#           pip install linkchecker\n#           linkchecker site/\n#         continue-on-error: true              # report but don't fail\n#       - name: Deploy\n#         if: github.ref == 'refs/heads/main'\n#         run: mkdocs gh-deploy --force --clean\n# Each push to main builds + deploys; PRs build only (catches errors).\n# .readthedocs.yaml (in repo root):\n# version: 2\n# build:\n#   os: ubuntu-22.04\n#   tools: { python: \"3.12\" }\n# mkdocs:\n#   configuration: mkdocs.yml\n# python:\n#   install:\n#     - method: pip\n#       path: .\n#       extra_requirements: [docs]\n# # Optional: build PDFs / multi-format\n# formats: [pdf, htmlzip]\n# Connect repo at readthedocs.org → New Project → import from GitHub.\n# Free tier: builds on push, hosts at PROJECT.readthedocs.io.\n# Paid tier: custom domain, advertising-free, search analytics."
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of MkDocs deployment — gh-pages, ReadTheDocs, custom domain — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             domain, search analytics, deploy preview for PRs.\n# pip install mike\n#\n# mkdocs.yml:\n# extra:\n#   version:\n#     provider: mike\n#     default: stable\n# Deploy a version (manual):\n# $ mike deploy --push --update-aliases 1.4.2 stable\n# $ mike set-default --push stable\n#\n# Now:\n#   docs.example.com/         -> redirects to /stable/\n#   docs.example.com/stable/   -> 1.4.2\n#   docs.example.com/1.4.2/    -> 1.4.2\n#   docs.example.com/1.3/      -> previous version\n# Version dropdown appears in the Material theme.\n# .github/workflows/docs-versioned.yml\n# name: Deploy Versioned Docs\n# on:\n#   push:\n#     branches: [main]\n#     tags: ['v*']\n# permissions:\n#   contents: write\n# jobs:\n#   build:\n#     runs-on: ubuntu-latest\n#     steps:\n#       - uses: actions/checkout@v4\n#         with: { fetch-depth: 0 }\n#       - uses: actions/setup-python@v5\n#         with: { python-version: \"3.12\" }\n#       - run: pip install mkdocs-material mkdocstrings[python] mike\n#       - name: Configure git\n#         run: |\n#           git config user.name github-actions\n#           git config user.email github-actions@github.com\n#       - name: Deploy dev (main branch)\n#         if: github.ref == 'refs/heads/main'\n#         run: mike deploy --push dev\n#       - name: Deploy version (on tag)\n#         if: startsWith(github.ref, 'refs/tags/v')\n#         run: |\n#           VERSION=${GITHUB_REF_NAME#v}\n#           MAJOR_MINOR=$(echo $VERSION | cut -d. -f1-2)\n#           mike deploy --push --update-aliases $VERSION $MAJOR_MINOR stable\n#           mike set-default --push stable\n# Cloudflare Pages auto-deploys every PR to a unique URL.\n# Connect repo → builds with: pip install ... && mkdocs build → site/\n# Each PR gets pr-N.docs.example.com.pages.dev preview link.\n# Apply at docsearch.algolia.com (free for OSS).\n# Replace mkdocs-material default search:\n# extra:\n#   docsearch:\n#     api_key: YOUR_KEY\n#     index_name: YOUR_INDEX\n#     app_id: YOUR_APP_ID\n# extra:\n#   social:\n#     - icon: ...\n# Material auto-generates Open Graph + Twitter Card meta tags.\n# Decision rule:\n#   personal project / OSS                  -> GitHub Pages via mkdocs gh-deploy\n#   org / monorepo                            -> GitHub Actions deploy job\n#   need build matrix (multiple Python)       -> ReadTheDocs (free tier)\n#   need PDF / epub                            -> ReadTheDocs (formats: [pdf])\n#   need multi-version docs                    -> mike + GitHub Actions\n#   PR previews                                 -> Cloudflare Pages or Netlify\n#   custom domain                                -> docs/CNAME + DNS\n#   private docs                                  -> ReadTheDocs paid tier; or self-host (S3 + CloudFront)\n#   sub-second search at scale                   -> Algolia DocSearch (free for OSS)\n#   need analytics                                -> Material has gtag.id; or Plausible / Fathom\n#   want to track 'edit on github' clicks        -> Material has built-in tracking\n#\n# Anti-pattern: building docs locally and committing site/ to git.\n# Doubles repo size; merge conflicts on every doc edit; out-of-sync\n# with source. ALWAYS build in CI; deploy site/ to gh-pages or a\n# static host. Keep site/ in .gitignore."
                  }
        ],
        tips: [
                  "`mkdocs gh-deploy --force` is the one-command deploy — builds locally, force-pushes to `gh-pages`, GitHub Pages serves. Good for solo work.",
                  "For team workflows, GitHub Actions on push to main is the standard — every change triggers build + deploy; PRs build but don't deploy.",
                  "`mkdocs build --strict` fails on any warning (broken link, missing nav entry). Use in CI to catch errors before deploy.",
                  "`mike` for multi-version docs — deploys each version to its own path, adds a switcher dropdown. Pair with tag-triggered CI.",
                  "For PR preview deploys, Cloudflare Pages or Netlify auto-deploy each PR to a unique URL. Comment-bots post the link.",
                  "Algolia DocSearch is free for OSS projects; gives sub-second search across the site. Apply at docsearch.algolia.com."
        ],
        mistake: "Building docs locally and committing the `site/` directory to git. Doubles repo size; merge conflicts on every doc edit; gets out-of-sync with the source Markdown. Always build in CI; deploy `site/` to GitHub Pages or a static host. `.gitignore` the `site/` directory.",
        shorthand: {
          verbose: "# site/ committed to repo\n$ mkdocs build && git add site/ && git push\n# Repo doubles in size; site/ drifts from docs/",
          concise: "# CI builds + deploys; site/ never in git\n.gitignore: site/\n.github/workflows/docs.yml: mkdocs gh-deploy --force",
        },
      },
    ],
  },

  // ── Section 2: Sphinx — autodoc-strong, RST-historical ─────────────────────────────────────────
  {
    id: "sphinx",
    title: "Sphinx — autodoc-strong, RST-historical",
    entries: [
      {
        id: "sphinx-basics",
        fn: "Sphinx basics — quickstart, conf.py, RST + Markdown",
        desc: "Sphinx is the original Python docs generator. RST is the native syntax; Markdown via `myst-parser`. Modern themes (Furo, sphinx-rtd-theme, sphinx-book-theme) make Sphinx output competitive with MkDocs Material.",
        category: "Sphinx",
        subtitle: "sphinx-quickstart, conf.py, .rst vs .md (myst-parser), sphinx-build, html_theme, intersphinx, ReadTheDocs default",
        signature: "sphinx-quickstart docs && cd docs && make html",
        descLong: "Sphinx is the longest-running Python docs generator — predates MkDocs by a decade. RST (reStructuredText) is the native markup; Markdown is supported via `myst-parser`. Sphinx's strengths: world-class autodoc (`sphinx.ext.autodoc`), intersphinx for cross-project links, mature theme ecosystem (Furo, RTD, Book), PDF/epub output, multi-language support. Default for Python stdlib, NumPy, SciPy, Django. Choose Sphinx when: you need RST, multi-format output (HTML + PDF + epub), tight integration with ReadTheDocs paid features, or you're writing technical documentation that benefits from RST's structure. The three examples solve the SAME concrete task — set up Sphinx for a Python package — at three depths: quickstart with default theme → Furo + myst-parser + custom config → full ReadTheDocs setup with extensions.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Sphinx basics — quickstart, conf.py, RST + Markdown — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# pip install sphinx\n# $ sphinx-quickstart docs\n#   ... interactive prompts ...\n#   project name: My Project\n#   author: Me\n#   release: 0.1\n#   language: en\n# Creates:\n#   docs/\n#     conf.py\n#     index.rst\n#     Makefile\n#     make.bat\n#     _build/\n#     _static/\n#     _templates/\n# Welcome to My Project\n#\n# .. toctree::\n#    :maxdepth: 2\n#    :caption: Contents:\n#\n#    getting-started\n#    api/index\n#\n# Indices and tables\n#\n# * :ref:`genindex`\n# * :ref:`modindex`\n# * :ref:`search`\n# $ cd docs && make html\n# Output in docs/_build/html/\n# Open docs/_build/html/index.html in a browser.\n# Heading 1\n#\n# Heading 2\n# ---------\n#\n# Heading 3\n# ~~~~~~~~~\n#\n# **bold**\n# *italic*\n# ``code``\n#\n# - bullet 1\n# - bullet 2\n#\n# 1. numbered 1\n# 2. numbered 2\n#\n# .. note::\n#\n#    A note callout.\n#\n# .. code-block:: python\n#\n#    def hello():\n#        print(\"hi\")\n#\n# Cross-reference: :ref:`anchor-name`\n# Link: `text <https://example.com>`_\n# Default theme is alabaster (basic). Junior tier upgrades."
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Sphinx basics — quickstart, conf.py, RST + Markdown — common patterns you'll see in production.\n# APPROACH  - Combine Sphinx basics — quickstart, conf.py, RST + Markdown with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n# pip install sphinx furo myst-parser\n# # Configuration file for Sphinx documentation.\n#\n# project = \"My Project\"\n# copyright = \"2026, Me\"\n# author = \"Me\"\n# release = \"0.1.0\"\n#\n# extensions = [\n#     \"sphinx.ext.autodoc\",                      # auto-generate from docstrings\n#     \"sphinx.ext.napoleon\",                      # Google/NumPy-style docstrings\n#     \"sphinx.ext.intersphinx\",                   # cross-project links\n#     \"sphinx.ext.viewcode\",                      # link to source\n#     \"sphinx.ext.todo\",                          # .. todo:: directive\n#     \"myst_parser\",                              # Markdown support\n#     \"sphinx_copybutton\",                        # copy-code buttons\n# ]\n#\n# templates_path = [\"_templates\"]\n# exclude_patterns = [\"_build\", \"Thumbs.db\", \".DS_Store\"]\n#\n# # === Source files: both .rst and .md ===\n# source_suffix = {\n#     \".rst\": \"restructuredtext\",\n#     \".md\":  \"markdown\",\n# }\n#\n# # === Theme: Furo (modern, clean) ===\n# html_theme = \"furo\"\n# html_static_path = [\"_static\"]\n# html_title = \"My Project\"\n# html_theme_options = {\n#     \"sidebar_hide_name\": True,\n#     \"navigation_with_keys\": True,\n#     \"source_repository\": \"https://github.com/myorg/myproject/\",\n#     \"source_branch\": \"main\",\n#     \"source_directory\": \"docs/\",\n# }\n#\n# # === Autodoc settings ===\n# autodoc_default_options = {\n#     \"members\": True,\n#     \"undoc-members\": False,\n#     \"show-inheritance\": True,\n# }\n# autodoc_typehints = \"description\"               # show type hints in description\n# autodoc_typehints_format = \"short\"\n#\n# # === Napoleon: parse Google/NumPy docstrings ===\n# napoleon_google_docstring = True\n# napoleon_numpy_docstring = True\n# napoleon_include_init_with_doc = False\n# napoleon_include_private_with_doc = False\n#\n# # === Intersphinx: link to other projects' docs ===\n# intersphinx_mapping = {\n#     \"python\": (\"https://docs.python.org/3\", None),\n#     \"pandas\": (\"https://pandas.pydata.org/docs\", None),\n#     \"numpy\":  (\"https://numpy.org/doc/stable\", None),\n# }\n#\n# # === MyST: enable extensions ===\n# myst_enable_extensions = [\n#     \"colon_fence\",                              # ::: blocks\n#     \"deflist\",                                   # definition lists\n#     \"tasklist\",                                  # - [x] task lists\n#     \"linkify\",                                   # auto-link URLs\n# ]\n# # Getting Started\n#\n# Install via pip:\n#\n# ```bash\n# pip install myproject\n# ```\n#\n# :::{note}\n# Requires Python 3.10+\n# :::\n# Build:\n# $ make html"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Sphinx basics — quickstart, conf.py, RST + Markdown — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             custom theme, link check, build matrix.\n# version: 2\n# build:\n#   os: ubuntu-22.04\n#   tools:\n#     python: \"3.12\"\n#   jobs:\n#     post_create_environment:\n#       - pip install -e .[docs]\n# sphinx:\n#   configuration: docs/conf.py\n#   fail_on_warning: true\n# python:\n#   install:\n#     - method: pip\n#       path: .\n#       extra_requirements: [docs]\n# formats:\n#   - htmlzip\n#   - pdf\n#   - epub\n# [project.optional-dependencies]\n# docs = [\n#   \"sphinx>=7\",\n#   \"furo\",\n#   \"myst-parser\",\n#   \"sphinx-copybutton\",\n#   \"sphinx-design\",                              # tabs, grids, cards\n#   \"sphinx-tabs\",\n#   \"sphinxcontrib-mermaid\",                      # mermaid diagrams\n#   \"sphinx-autobuild\",                           # auto-rebuild on file changes\n#   \"sphinx-autodoc-typehints\",                   # better type-hint rendering\n# ]\n# $ pip install sphinx-autobuild\n# $ sphinx-autobuild docs docs/_build/html\n# Opens browser, rebuilds on file save (like mkdocs serve).\n# RTD admin → Versions → Activate per branch/tag.\n# Each version builds at PROJECT.readthedocs.io/en/VERSION/.\n# Default version is configurable; latest = \"stable\".\n# # Doctest integration:\n# extensions += [\"sphinx.ext.doctest\"]\n# # Run with: $ make doctest\n#\n# # Coverage check:\n# extensions += [\"sphinx.ext.coverage\"]\n# # Run with: $ make coverage; identifies undocumented members.\n#\n# # GitHub edit links:\n# html_context = {\n#     \"display_github\": True,\n#     \"github_user\": \"myorg\",\n#     \"github_repo\": \"myproject\",\n#     \"github_version\": \"main\",\n#     \"conf_py_path\": \"/docs/\",\n# }\n#\n# # Build with full path tracking (catches stale anchors):\n# nitpicky = True                                  # warn on missing references\n# nitpick_ignore = [(\"py:class\", \"T\")]              # but ignore these\n# $ make linkcheck\n# Reports broken external links.\n# .github/workflows/docs.yml\n# name: Docs\n# on:\n#   push:\n#     branches: [main]\n# jobs:\n#   build:\n#     runs-on: ubuntu-latest\n#     steps:\n#       - uses: actions/checkout@v4\n#       - uses: actions/setup-python@v5\n#         with: { python-version: \"3.12\" }\n#       - run: |\n#           pip install -e .[docs]\n#           cd docs && make html SPHINXOPTS=\"-W\"   # -W = warnings as errors\n#           make linkcheck\n# Decision rule:\n#   default Python theme                    -> Furo (modern) or sphinx-book-theme\n#   classic ReadTheDocs                       -> sphinx-rtd-theme\n#   need RST                                   -> Sphinx (always)\n#   need Markdown                              -> myst-parser (works in Sphinx)\n#   need PDF / epub                             -> Sphinx (built-in)\n#   API docs from docstrings                   -> sphinx.ext.autodoc + napoleon\n#   cross-project references                    -> intersphinx\n#   docstring style                              -> Google or NumPy via Napoleon\n#   want hot reload                              -> sphinx-autobuild\n#   complex Markdown features                    -> myst-parser + extensions\n#   diagrams                                      -> sphinxcontrib-mermaid; or rst-include images\n#   need notebooks in docs                        -> myst-nb (executes notebooks at build)\n#   tabbed content                                 -> sphinx-design or sphinx-tabs\n#\n# Anti-pattern: starting Sphinx with default conf.py + alabaster theme\n# and never updating. Default theme looks dated; users assume the\n# project itself is dated. Pick Furo (or sphinx-rtd-theme for that\n# classic Python feel) on day one."
                  }
        ],
        tips: [
                  "For modern Sphinx output, use the **Furo** theme — clean, dark mode, mobile-friendly. Default `alabaster` looks dated.",
                  "Use **myst-parser** so you can write docs in Markdown OR RST in the same project. RST for autodoc, Markdown for prose.",
                  "**Napoleon** (`sphinx.ext.napoleon`) lets you write Google/NumPy-style docstrings instead of RST-style — much more readable.",
                  "**sphinx-autobuild** gives you `mkdocs serve`-style hot-reload: `sphinx-autobuild docs docs/_build/html` watches files and rebuilds.",
                  "**ReadTheDocs** is the default Python docs host — free for OSS, builds on every push, multi-version dropdown built in.",
                  "`make html SPHINXOPTS=\"-W\"` treats warnings as errors — critical for CI to catch broken refs and missing files."
        ],
        mistake: "Starting Sphinx with the default `alabaster` theme and never upgrading. The default looks like a decade-old project; users assume the docs (and project) are abandoned. Pick a modern theme on day one — Furo for new projects, `sphinx-rtd-theme` for the familiar Python feel.",
        shorthand: {
          verbose: "# Default Sphinx — looks like 2010\n# conf.py\nhtml_theme = \"alabaster\"",
          concise: "# Modern — Furo or sphinx-rtd-theme\n# conf.py\nhtml_theme = \"furo\"\n# OR\nhtml_theme = \"sphinx_rtd_theme\"",
        },
      },
      {
        id: "sphinx-autodoc",
        fn: "Sphinx autodoc — generate API docs from docstrings",
        desc: "`sphinx.ext.autodoc` introspects modules and renders docstrings. `autosummary` generates one stub page per object. The `:autoclass:` and `:automodule:` directives are the workhorses; `sphinx-autodoc-typehints` makes type hints render properly.",
        category: "Sphinx",
        subtitle: "sphinx.ext.autodoc, automodule / autoclass / autofunction, autosummary, sphinx-autodoc-typehints, napoleon for Google/NumPy, autodoc_default_options",
        signature: ".. automodule:: myproject.users\n   :members:\n   :undoc-members:\n   :show-inheritance:",
        descLong: "Sphinx's autodoc is more mature than any alternative — `automodule`, `autoclass`, `autofunction` directives walk Python objects and render their docstrings. `autosummary` generates a table-of-contents page plus stub files for each item. Combined with `napoleon` (Google/NumPy docstring parsing) and `sphinx-autodoc-typehints` (type hints in signatures), you get rich API documentation that auto-updates when code changes. The three examples solve the SAME concrete task — generate API documentation for a Python module — at three depths: `automodule` directive → autosummary + napoleon + custom options → production with intersphinx, type-hint rendering, and gen-files for fully automated reference.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Sphinx autodoc — generate API docs from docstrings — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# Add to conf.py:\n# extensions = [\"sphinx.ext.autodoc\"]\n# Create docs/api/users.rst:\n# Users\n#\n# .. automodule:: myproject.users\n#    :members:\n#    :undoc-members:\n#    :show-inheritance:\n# Build:\n# $ make html\n# /api/users.html now has every public class/function from\n# myproject.users with their docstrings rendered.\n# .. automodule:: myproject              # whole module\n#    :members:                            # include all public\n#    :undoc-members:                       # include those without docstrings\n#    :show-inheritance:                    # show base classes\n#    :inherited-members:                   # include inherited\n#    :exclude-members: __init__            # skip these\n#\n# .. autoclass:: myproject.User            # one class\n#    :members:\n#\n# .. autofunction:: myproject.helpers.foo  # one function\n# Default: docstrings are interpreted as RST. So:\n#   def add(a, b):\n#       \"\"\"Add two numbers.\n#\n#       :param a: first number.\n#       :param b: second number.\n#       :returns: sum.\n#       \"\"\"\n# Verbose. Junior tier adds napoleon for Google/NumPy styles."
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Sphinx autodoc — generate API docs from docstrings — common patterns you'll see in production.\n# APPROACH  - Combine Sphinx autodoc — generate API docs from docstrings with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             (one page per object) + type hints.\n# pip install sphinx-autodoc-typehints\n# extensions = [\n#     \"sphinx.ext.autodoc\",\n#     \"sphinx.ext.autosummary\",\n#     \"sphinx.ext.napoleon\",\n#     \"sphinx_autodoc_typehints\",\n# ]\n#\n# autosummary_generate = True                            # generate stub .rst files\n# autodoc_default_options = {\n#     \"members\": True,\n#     \"undoc-members\": False,\n#     \"show-inheritance\": True,\n#     \"member-order\": \"bysource\",                        # source order, not alphabetical\n# }\n# autodoc_typehints = \"description\"                      # type hints in description\n# autodoc_typehints_description_target = \"documented\"\n#\n# # Napoleon: parse Google + NumPy docstrings\n# napoleon_google_docstring = True\n# napoleon_numpy_docstring = True\n# napoleon_include_init_with_doc = True\n# napoleon_use_param = True                              # use :param: style\n# napoleon_use_rtype = True\n# def fetch_user(user_id: int, *, include_orders: bool = False) -> dict:\n#     \"\"\"Fetch a user by ID.\n#\n#     Args:\n#         user_id: The user's ID.\n#         include_orders: Whether to eager-load orders.\n#\n#     Returns:\n#         A dict with the user's profile and optional orders.\n#\n#     Raises:\n#         UserNotFound: If no user has that ID.\n#\n#     Example:\n#         >>> fetch_user(42)\n#         {\"id\": 42, \"name\": \"Alice\"}\n#     \"\"\"\n# docs/api/index.rst\n# API Reference\n#\n# .. autosummary::\n#    :toctree: generated/\n#    :recursive:\n#\n#    myproject.users\n#    myproject.orders\n#    myproject.helpers\n#\n# Sphinx generates docs/api/generated/myproject.users.rst (etc.)\n# automatically with full automodule directives.\n# autodoc_default_options[\"exclude-members\"] = \"__weakref__,_internal_*\"\n# Without sphinx_autodoc_typehints: type hints stay in the signature\n# line (cluttered). With it: type hints render in the parameter\n# description (cleaner)."
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Sphinx autodoc — generate API docs from docstrings — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             skip rules, fully automated module discovery.\n# extensions = [\n#     \"sphinx.ext.autodoc\",\n#     \"sphinx.ext.autosummary\",\n#     \"sphinx.ext.napoleon\",\n#     \"sphinx.ext.intersphinx\",\n#     \"sphinx.ext.viewcode\",\n#     \"sphinx_autodoc_typehints\",\n#     \"sphinx_copybutton\",\n# ]\n#\n# # Generate stubs automatically.\n# autosummary_generate = True\n# autosummary_imported_members = True                    # include imports if their __module__ matches\n#\n# # Default options applied to every directive.\n# autodoc_default_options = {\n#     \"members\": True,\n#     \"show-inheritance\": True,\n#     \"member-order\": \"bysource\",\n#     \"exclude-members\": \"__weakref__,__init_subclass__\",\n# }\n# autodoc_typehints = \"description\"\n# autodoc_typehints_format = \"short\"\n# autodoc_class_signature = \"separated\"                  # class sig on its own line\n# autodoc_preserve_defaults = True                       # keep \"abc\" not 'abc'\n#\n# # === Custom skip: don't document _private members or test fixtures ===\n# def skip_private(app, what, name, obj, skip, options):\n#     if name.startswith(\"_\") and not name.startswith(\"__\"):\n#         return True\n#     if name.startswith(\"test_\"):\n#         return True\n#     return skip\n#\n# def setup(app):\n#     app.connect(\"autodoc-skip-member\", skip_private)\n#\n# # === Intersphinx: cross-link to other projects ===\n# intersphinx_mapping = {\n#     \"python\":  (\"https://docs.python.org/3\", None),\n#     \"pandas\":  (\"https://pandas.pydata.org/docs\", None),\n#     \"numpy\":   (\"https://numpy.org/doc/stable\", None),\n#     \"fastapi\": (\"https://fastapi.tiangolo.com\", None),\n# }\n# # Now in docstrings:\n# #   \"See :class:`pandas.DataFrame` for details.\"\n# # auto-links to the pandas docs.\n#\n# # === Type-hint rendering ===\n# typehints_defaults = \"comma\"                            # show =default in description\n# typehints_use_rtype = True\n#\n# # === Fail on warnings (catches broken refs) ===\n# nitpicky = True\n# nitpick_ignore_regex = [\n#     (r\"py:class\", r\"T|^typing\\..*\"),                  # ignore TypeVars and typing.X\n# ]\n# API Reference\n#\n# .. autosummary::\n#    :toctree: generated\n#    :template: module.rst\n#    :recursive:\n#\n#    myproject\n# {{ fullname | escape | underline }}\n#\n# .. automodule:: {{ fullname }}\n#\n#    {% block functions %}\n#    {% if functions %}\n#    .. rubric:: Functions\n#\n#    .. autosummary::\n#    {% for item in functions %}\n#       {{ item }}\n#    {%- endfor %}\n#    {% endif %}\n#    {% endblock %}\n#\n#    {% block classes %}\n#    {% if classes %}\n#    .. rubric:: Classes\n#\n#    .. autosummary::\n#       :toctree:\n#    {% for item in classes %}\n#       {{ item }}\n#    {%- endfor %}\n#    {% endif %}\n#    {% endblock %}\n# Decision rule:\n#   single module reference                  -> .. automodule:: x.y :members:\n#   one class                                  -> .. autoclass:: x.y.Class\n#   one function                               -> .. autofunction:: x.y.func\n#   recursive auto-discovery                   -> autosummary :recursive:\n#   Google docstrings                           -> napoleon extension\n#   NumPy docstrings                            -> napoleon (same extension)\n#   type hints in description                   -> sphinx_autodoc_typehints\n#   skip private                                  -> autodoc_default_options \"exclude-members\"\n#   custom skip rules                             -> connect autodoc-skip-member event\n#   cross-link to stdlib / pandas                -> intersphinx_mapping\n#   warnings as errors                            -> nitpicky = True (with regex ignores)\n#   speed up build                                  -> autodoc_typehints = \"none\" (skip type hints)\n#   document inherited methods                     -> :inherited-members:\n#   override per-page                              -> directive options\n#   want it FULLY automatic                        -> autosummary :recursive: + custom template\n#\n# Anti-pattern: hand-writing API docs while also using autodoc. Two\n# sources, both maintained, both drift. Pick autodoc OR hand-write,\n# not both. autodoc + good docstrings is the right answer for any\n# project of size — single source of truth, always in sync."
                  }
        ],
        tips: [
                  "`napoleon` extension parses Google/NumPy-style docstrings — much more readable than raw RST.",
                  "`autosummary` with `:recursive:` walks your package tree and generates one .rst stub per module — fully automated reference.",
                  "`sphinx_autodoc_typehints` with `autodoc_typehints = \"description\"` renders type hints in the parameter description, not the signature line.",
                  "`intersphinx_mapping` links your docstrings to other projects — `:class:`pandas.DataFrame`` becomes a clickable link.",
                  "Connect `autodoc-skip-member` for custom skip rules — exclude `_private`, exclude `test_*`, exclude framework noise.",
                  "`nitpicky = True` makes Sphinx fail on missing references. Use `nitpick_ignore_regex` for unavoidable false positives (TypeVars, etc.)."
        ],
        mistake: "Maintaining hand-written API docs alongside autodoc-generated ones. Two sources, both drift, readers find both, trust the wrong one. Pick ONE: hand-write (small projects, narrative docs) OR autodoc (any project of size). For Python projects, autodoc + good docstrings wins.",
        shorthand: {
          verbose: "# Hand-written reference next to autodoc — drift inevitable\ndocs/api/users.rst:\n  Users module\n  ============\n  .. automodule:: myproject.users\ndocs/manual/users.rst:\n  Users guide\n  ===========\n  Args: id, email, ...   # transcribed; updates lag",
          concise: "# autodoc only — single source of truth\n.. automodule:: myproject.users\n   :members:\n   :show-inheritance:",
        },
      },
      {
        id: "sphinx-extensions",
        fn: "Sphinx extensions — diagrams, notebooks, design components",
        desc: "The Sphinx ecosystem: `sphinx-design` for tabs/grids, `sphinxcontrib-mermaid` for diagrams, `myst-nb` to execute notebooks at build, `sphinx-copybutton` for code copy. Compose for rich docs.",
        category: "Sphinx",
        subtitle: "sphinx-design (tabs, grids, cards), sphinxcontrib-mermaid, myst-nb (executable notebooks), sphinx-copybutton, sphinx-tabs, breathe (C++)",
        signature: "extensions = [\"sphinx_design\", \"myst_nb\", \"sphinxcontrib.mermaid\"]",
        descLong: "Sphinx's extensions ecosystem covers nearly every doc-tooling need: `sphinx-design` (tabs, grids, cards), `myst-nb` (executable Jupyter notebooks AS doc pages), `sphinxcontrib-mermaid` (mermaid diagrams), `sphinx-copybutton` (one-click code copy), `sphinx-gallery` (auto-generated example pages from .py files). The three examples solve the SAME concrete task — add interactive tabbed content + a flowchart + an executable example notebook to the docs — at three depths: sphinx-tabs basic → sphinx-design (modern) + mermaid → myst-nb running notebooks at build time + auto-generated gallery.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Sphinx extensions — diagrams, notebooks, design components — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# pip install sphinx-tabs\n# extensions = [\"sphinx_tabs.tabs\"]\n# .. tabs::\n#\n#    .. tab:: Python\n#\n#       .. code-block:: python\n#\n#          import requests\n#          requests.get(\"https://example.com\")\n#\n#    .. tab:: JavaScript\n#\n#       .. code-block:: javascript\n#\n#          fetch(\"https://example.com\")\n# ```{tabs}\n# .. tab:: Python\n#\n#    ```python\n#    requests.get(...)\n#    ```\n# .. tab:: JavaScript\n#\n#    ```javascript\n#    fetch(...)\n#    ```\n# ```\n# Build:\n# $ make html"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Sphinx extensions — diagrams, notebooks, design components — common patterns you'll see in production.\n# APPROACH  - Combine Sphinx extensions — diagrams, notebooks, design components with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n# pip install sphinx-design sphinxcontrib-mermaid\n# extensions = [\n#     \"sphinx_design\",\n#     \"sphinxcontrib.mermaid\",\n#     \"myst_parser\",\n# ]\n#\n# myst_enable_extensions = [\"colon_fence\"]\n# :::{tab-set}\n# :::{tab-item} Python\n# ```python\n# import requests\n# requests.get(\"https://example.com\")\n# ```\n# :::\n# :::{tab-item} JavaScript\n# ```javascript\n# fetch(\"https://example.com\")\n# ```\n# :::\n# :::\n# :::{grid} 2\n# :::{grid-item-card} Quick Start\n# :link: getting-started\n# :link-type: doc\n# Get up and running in 5 minutes.\n# :::\n# :::{grid-item-card} API Reference\n# :link: api/index\n# :link-type: doc\n# Full API documentation.\n# :::\n# :::\n# ```{mermaid}\n# graph LR\n#   A[User] -->|1. login| B[Auth Service]\n#   B -->|2. token| A\n#   A -->|3. call API| C[API Server]\n#   C -->|4. validate| B\n# ```\n# :::{dropdown} Click to expand\n# :icon: info\n# Hidden details here.\n# :::\n#\n# :::{warning}\n# Don't do this in production.\n# :::\n# :::{button-link} https://github.com/myorg/myproject\n# :color: primary\n# View on GitHub\n# :::\n# pip install sphinx-copybutton\n# # conf.py extensions += [\"sphinx_copybutton\"]\n# # Now every code block has a copy icon."
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Sphinx extensions — diagrams, notebooks, design components — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             auto-generated gallery from .py example files.\n# pip install myst-nb sphinx-gallery\n# extensions = [\n#     \"myst_nb\",                             # replaces myst_parser; supports .md AND .ipynb\n#     \"sphinx_design\",\n#     \"sphinxcontrib.mermaid\",\n#     \"sphinx_copybutton\",\n#     \"sphinx_gallery.gen_gallery\",\n#     \"sphinx.ext.autodoc\",\n#     \"sphinx.ext.napoleon\",\n# ]\n#\n# # === myst-nb config ===\n# nb_execution_mode = \"auto\"                 # \"auto\" | \"force\" | \"off\" | \"cache\"\n# nb_execution_timeout = 60                  # seconds per cell\n# nb_execution_excludepatterns = [\"broken_*\"]\n# nb_merge_streams = True                    # combine stdout/stderr per cell\n#\n# # === sphinx-gallery: auto-generate examples gallery from .py files ===\n# sphinx_gallery_conf = {\n#     \"examples_dirs\": [\"../examples\"],      # source: examples/*.py\n#     \"gallery_dirs\": [\"gallery\"],           # output: docs/gallery/*.rst\n#     \"filename_pattern\": r\"plot_\",\n#     \"ignore_pattern\": r\"__init__\\.py\",\n#     \"show_signature\": False,\n#     \"thumbnail_size\": (400, 280),\n# }\n# docs/tutorials/quickstart.md (or .ipynb):\n#\n# # Quickstart\n#\n# Let's compute basic statistics:\n#\n# ```{code-cell} ipython3\n# import pandas as pd\n# import numpy as np\n#\n# df = pd.DataFrame({\"a\": np.random.rand(100), \"b\": np.random.rand(100)})\n# df.describe()\n# ```\n#\n# Now plot:\n#\n# ```{code-cell} ipython3\n# df.plot.scatter(\"a\", \"b\");\n# ```\n#\n# myst-nb executes these cells at build time and embeds outputs.\n# Cache by default; re-runs only on source change.\n# Project structure:\n#   examples/\n#     plot_basic.py        <- becomes docs/gallery/plot_basic.html\n#     plot_advanced.py\n#\n# examples/plot_basic.py:\n# \"\"\"\n# Basic example\n#\n# This example shows the simplest usage.\n# \"\"\"\n# import myproject\n# import matplotlib.pyplot as plt\n#\n# # %%\n# # First, do this:\n# x = myproject.compute(...)\n#\n# # %%\n# # Then plot:\n# plt.plot(x)\n# plt.show()\n#\n# Build generates a gallery page with rendered output, downloadable\n# .py and .ipynb versions.\n# Decision rule:\n#   tabbed content                          -> sphinx-design tab-set OR sphinx-tabs\n#   diagrams                                  -> sphinxcontrib-mermaid (most common)\n#                                                 sphinx-gallery (matplotlib gallery)\n#                                                 graphviz (sphinx.ext.graphviz)\n#   executable notebook                       -> myst-nb (replaces myst_parser)\n#   examples directory                          -> sphinx-gallery\n#   \"edit on GitHub\"                           -> html_context with github fields\n#   download buttons                            -> sphinx-design button-link\n#   copy code                                    -> sphinx-copybutton\n#   API stub generation                          -> sphinx.ext.autosummary :recursive:\n#   architecture decision records (ADRs)         -> myst-parser; just write Markdown\n#   blog-style                                    -> ablog extension; tags + RSS\n#   redirects                                      -> sphinx_reredirects extension\n#   versioning                                     -> readthedocs activates per-branch/tag\n#   typing-aware doc                                -> sphinx_autodoc_typehints\n#   intersphinx                                     -> sphinx.ext.intersphinx\n#\n# Anti-pattern: installing 20 sphinx extensions just because they\n# exist. Each adds build time, maintenance burden, and learning curve.\n# Start minimal (autodoc + napoleon + myst-parser); add only when you\n# have a documented need (tabs in 5+ pages = add sphinx-design)."
                  }
        ],
        tips: [
                  "**sphinx-design** is the modern replacement for sphinx-tabs — provides tabs, cards, grids, dropdowns, buttons. One extension covers most layout needs.",
                  "**myst-nb** replaces `myst-parser` and adds support for executing `.md` and `.ipynb` files at build time. Outputs are cached; only re-runs on source change.",
                  "**sphinxcontrib-mermaid** renders mermaid diagrams inline — one extension for flowcharts, sequence diagrams, ER diagrams.",
                  "**sphinx-gallery** auto-generates a gallery page from `.py` files in an `examples/` directory — each becomes a styled example with rendered plots and downloadable scripts.",
                  "**sphinx-copybutton** adds copy-to-clipboard buttons on code blocks — one of the highest-leverage extensions; one line of conf.",
                  "Don't install every extension that exists. Each adds build time and maintenance. Start with autodoc + napoleon + myst-parser; add others only when you hit a need."
        ],
        mistake: "Installing 20 Sphinx extensions because they look useful. Each adds build time, version-pinning maintenance, and learning curve. Start minimal: autodoc + napoleon + myst-parser. Add `sphinx-design`, `myst-nb`, etc. only when 5+ pages would benefit.",
        shorthand: {
          verbose: "# Kitchen sink — slow build, dependency hell\nextensions = [\"sphinx_design\", \"sphinx_tabs\", \"sphinx_copybutton\",\n              \"sphinxcontrib.mermaid\", \"sphinxcontrib.plantuml\",\n              \"sphinx_gallery.gen_gallery\", \"myst_nb\", \"ablog\", ...]",
          concise: "# Minimal start\nextensions = [\"sphinx.ext.autodoc\", \"sphinx.ext.napoleon\",\n              \"myst_parser\"]\n# Add others when you have 5+ pages that need them.",
        },
      },
    ],
  },

  // ── Section 3: Documentation Patterns — docstrings, doctest, mkdocs vs sphinx ─────────────────────────────────────────
  {
    id: "patterns",
    title: "Documentation Patterns — docstrings, doctest, mkdocs vs sphinx",
    entries: [
      {
        id: "docstring-styles",
        fn: "Docstring styles — Google, NumPy, RST, type-aware",
        desc: "Three styles dominate: Google (most readable), NumPy (scientific Python convention), RST (Sphinx-native, verbose). Pick one per project; all parse via `napoleon`/`mkdocstrings`. Type hints replace `:type:` annotations.",
        category: "Patterns",
        subtitle: "Google style, NumPy style, RST/Sphinx style, napoleon, mkdocstrings, type hints over :type: tags, examples sections, raises",
        signature: "def fn(x: int) -> str:\n    \"\"\"Short summary.\n\n    Args:\n        x: description.\n\n    Returns:\n        description.\n    \"\"\"",
        descLong: "Three docstring styles in the wild: **Google** (most human-readable; `Args:`, `Returns:`, `Raises:` sections), **NumPy** (popular in scientific Python; rST-style with `Parameters\\n----------`), **RST/Sphinx** (verbose; `:param x:` per arg). All three parse with the `napoleon` Sphinx extension or `mkdocstrings`. With modern type hints, the `:type:` annotations are redundant — leave types in the function signature, describe the parameter's semantics in the docstring. The three examples solve the SAME concrete task — document a `fetch_user` function with parameters, return value, exceptions, and example — at three depths: minimal Google → fully-formatted Google with examples → NumPy and RST equivalents + tooling integration.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Docstring styles — Google, NumPy, RST, type-aware — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\ndef fetch_user(user_id: int, *, include_orders: bool = False) -> dict:\n    \"\"\"Fetch a user by ID.\n    Args:\n        user_id: The user's database ID.\n        include_orders: Eager-load orders if True.\n    Returns:\n        A dict with the user's profile data.\n    Raises:\n        UserNotFound: If no user has that ID.\n    \"\"\"\n    ...\n# Why Google over RST:\n# Google:                          RST equivalent (verbose):\n# Args:                             :param user_id: ...\n#     user_id: ...                  :type user_id: int\n#     include_orders: ...           :param include_orders: ...\n# Returns:                          :type include_orders: bool\n#     A dict ...                    :returns: ...\n# Raises:                            :rtype: dict\n#     UserNotFound: ...              :raises UserNotFound: ...\n#\n# Google is ~30% fewer characters and reads as prose. Type info is in\n# the function signature; no need to repeat it as :type: tags.\n# Both autodoc-friendly when napoleon extension is enabled (Sphinx)\n# or mkdocstrings is configured (MkDocs)."
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Docstring styles — Google, NumPy, RST, type-aware — common patterns you'll see in production.\n# APPROACH  - Combine Docstring styles — Google, NumPy, RST, type-aware with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\ndef fetch_user(\n    user_id: int,\n    *,\n    include_orders: bool = False,\n    timeout: float = 10.0,\n) -> dict:\n    \"\"\"Fetch a user by ID, with optional order eager-loading.\n    Performs a single SELECT against the users table; if include_orders\n    is True, JOINs with orders. Raises if the user doesn't exist\n    (does NOT return None — see :func:`find_user` for the optional variant).\n    Args:\n        user_id: The user's database ID. Must be positive.\n        include_orders: If True, eagerly load the user's orders into\n            the returned dict. Adds one JOIN; ~2x slower.\n        timeout: Maximum seconds to wait for the database. Default 10.\n    Returns:\n        A dict with keys:\n        - `id` (int): The user's database ID.\n        - `email` (str): Primary email.\n        - `created_at` (datetime): Account creation timestamp.\n        - `orders` (list[dict]): Present only if include_orders=True.\n    Raises:\n        UserNotFound: If user_id doesn't match any row.\n        DatabaseTimeout: If the query exceeds the timeout.\n        ValueError: If user_id is not positive.\n    Example:\n        Basic usage::\n            >>> user = fetch_user(42)\n            >>> user[\"email\"]\n            'alice@example.com'\n        With orders::\n            >>> user = fetch_user(42, include_orders=True)\n            >>> len(user[\"orders\"])\n            5\n    See Also:\n        :func:`find_user`: Returns None on miss instead of raising.\n        :func:`fetch_users_batch`: Batch fetch by IDs.\n    Note:\n        This function is async-unsafe; use `afetch_user` in async code.\n    \"\"\"\n    if user_id <= 0:\n        raise ValueError(\"user_id must be positive\")\n    ...\ndef numpy_style(x: int, y: float = 1.0) -> float:\n    \"\"\"Compute x + y.\n    Parameters\n    ----------\n    x : int\n        Description of x.\n    y : float, optional\n        Description of y. Default is 1.0.\n    Returns\n    -------\n    float\n        Description of the return value.\n    Raises\n    ------\n    ValueError\n        If x is negative.\n    See Also\n    --------\n    other_function : Brief related function.\n    Notes\n    -----\n    Longer notes section.\n    Examples\n    --------\n    >>> numpy_style(5)\n    6.0\n    \"\"\"\n    return x + y\ndef rst_style(x: int, y: float = 1.0) -> float:\n    \"\"\"Compute x + y.\n    :param x: Description of x.\n    :param y: Description of y. Default 1.0.\n    :returns: Description of return.\n    :raises ValueError: If x is negative.\n    \"\"\"\n    return x + y"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Docstring styles — Google, NumPy, RST, type-aware — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             stay in sync (doctests).\n# Pydocstyle (deprecated; use ruff) checks docstring formatting.\n# pydocstyle      OR\n# ruff (recommended) — covers it via D rules:\n#\n# pyproject.toml\n# [tool.ruff.lint]\n# select = [\"E\", \"F\", \"I\", \"D\"]                          # D = docstring rules\n# [tool.ruff.lint.pydocstyle]\n# convention = \"google\"                                   # or \"numpy\" or \"pep257\"\n#\n# # ruff D rules:\n# # D100  Missing docstring in public module\n# # D101  Missing docstring in public class\n# # D102  Missing docstring in public method\n# # D103  Missing docstring in public function\n# # D200  One-line docstring should fit on one line\n# # D204  1 blank line required after class docstring\n# # ... etc.\n# BAD (legacy):\ndef add_legacy(a, b):\n    \"\"\"Add two numbers.\n    :param a: First number.\n    :type a: int\n    :param b: Second number.\n    :type b: int\n    :returns: Sum.\n    :rtype: int\n    \"\"\"\n    return a + b\n# GOOD (modern):\ndef add(a: int, b: int) -> int:\n    \"\"\"Add two numbers.\n    Args:\n        a: First number.\n        b: Second number.\n    Returns:\n        Sum of a and b.\n    \"\"\"\n    return a + b\n# Type info lives in the signature; sphinx-autodoc-typehints +\n# mkdocstrings render them in the description automatically.\nclass UserService:\n    \"\"\"Service for fetching and updating users.\n    Attributes:\n        db: Database connection.\n        cache: Optional Redis cache.\n    Example:\n        >>> svc = UserService(db=engine)\n        >>> svc.fetch(42)\n        User(id=42, email='alice@example.com')\n    \"\"\"\n    def __init__(self, db, cache=None):\n        \"\"\"Initialize the service.\n        Args:\n            db: SQLAlchemy engine or session.\n            cache: Optional Redis instance for read-through caching.\n        \"\"\"\n        self.db = db\n        self.cache = cache\n# Note: with napoleon's include_init_with_doc, you can put __init__\n# docs in the class docstring instead of a separate __init__ docstring.\n# top of myproject/users.py:\n\"\"\"User management.\nThis module provides the User model, UserService for CRUD,\nand helpers for password hashing.\nExample:\n    Basic usage::\n        from myproject.users import UserService\n        svc = UserService(db=engine)\n        user = svc.fetch(42)\nNote:\n    All operations require an active database connection.\n\"\"\"\n# Decision rule:\n#   new project                              -> Google style\n#   scientific Python                          -> NumPy style (matches numpy/scipy/pandas)\n#   existing project with one style            -> match it; consistency wins\n#   already using rST                           -> RST/Sphinx style\n#   type info                                    -> use type hints; skip :type:\n#   examples                                      -> doctest-friendly format\n#   private (_underscore) / dunder              -> can skip; ruff D rules optional\n#   docstring style enforcement                  -> ruff with [tool.ruff.lint.pydocstyle]\n#   docstring rendering                          -> napoleon (Sphinx) OR mkdocstrings (MkDocs)\n#   docstring testing                            -> doctest module + pytest --doctest-modules\n#\n# Anti-pattern: writing docstrings as :param: / :type: tags when you\n# already have type hints. The types are stated twice (signature +\n# docstring); they drift; renderers like mkdocstrings handle type\n# hints natively. Use Google/NumPy style for prose; let type hints\n# stay in the signature."
                  }
        ],
        tips: [
                  "Pick **Google style** for new projects — most human-readable, well-supported by `napoleon` (Sphinx) and `mkdocstrings` (MkDocs).",
                  "NumPy style is the convention in scientific Python — matches numpy/scipy/pandas. Pick if your project lives in that ecosystem.",
                  "NEVER use `:type:` tags when you have type hints. The types are in the signature; repeating them in the docstring just creates drift.",
                  "Use `ruff` with `[tool.ruff.lint.pydocstyle] convention = \"google\"` to lint docstring style. Replaces the older `pydocstyle` tool.",
                  "`Example:` sections in Google/NumPy format with `>>>` syntax are doctest-runnable — pair with `pytest --doctest-modules` to verify they stay accurate.",
                  "For class docstrings, put `__init__` parameter docs in the CLASS docstring (not in `__init__`) when napoleon's `include_init_with_doc = True`."
        ],
        mistake: "Using `:param x:` / `:type x:` RST tags in docstrings while ALSO using type hints. The types are stated twice (function signature AND docstring); they drift; type-hint-aware renderers (mkdocstrings, sphinx-autodoc-typehints) handle types natively. Use Google/NumPy style for prose; let type hints stay in the signature.",
        shorthand: {
          verbose: "# Types stated twice; drift inevitable\ndef fetch(uid: int) -> dict:\n    \"\"\"Fetch a user.\n\n    :param uid: ID.\n    :type uid: int\n    :returns: dict.\n    :rtype: dict\n    \"\"\"",
          concise: "# Types in signature only; docstring is prose\ndef fetch(uid: int) -> dict:\n    \"\"\"Fetch a user.\n\n    Args:\n        uid: The user's ID.\n\n    Returns:\n        The user dict.\n    \"\"\"",
        },
      },
      {
        id: "doctest-examples",
        fn: "Doctest — examples that stay in sync with code",
        desc: "`>>> example` blocks in docstrings are runnable. `pytest --doctest-modules` runs them in CI; if the code changes and the example breaks, CI fails. Documentation that can't lie about how the code works.",
        category: "Patterns",
        subtitle: "doctest module, pytest --doctest-modules, doctest_namespace fixture, sybil for richer testing, ELLIPSIS / NORMALIZE_WHITESPACE flags",
        signature: "$ pytest --doctest-modules src/   # runs every >>> example in every docstring",
        descLong: "Doctests are example code in docstrings using `>>>` prompts; the `doctest` stdlib module runs them and verifies output matches. Pair with `pytest --doctest-modules` to run them as part of the regular test suite. Three benefits: examples can't lie (CI fails if they break); examples are runnable (users can copy-paste and verify); examples become unit tests for free. The three examples solve the SAME concrete task — write executable examples for a `fetch_user` function — at three depths: basic doctest → ELLIPSIS / namespace fixture for complex output → sybil for full testing of code blocks in `.md` / `.rst` files.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Doctest — examples that stay in sync with code — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\ndef add(a: int, b: int) -> int:\n    \"\"\"Add two numbers.\n    Args:\n        a: First.\n        b: Second.\n    Returns:\n        Sum.\n    Example:\n        >>> add(1, 2)\n        3\n        >>> add(-5, 5)\n        0\n        >>> add(100, 200)\n        300\n    \"\"\"\n    return a + b\n# Run via stdlib:\n# $ python -m doctest module.py -v\n#\n# Or via pytest (recommended):\n# $ pip install pytest\n# $ pytest --doctest-modules src/\n# pytest finds every docstring with >>> blocks and runs them.\n# If the function's behavior changes, the doctest output won't\n# match — CI fails immediately.\n# Common gotchas:\n# 1. Whitespace matters by default\n#    >>> \"hello\"\n#    'hello'             # exact match required\n#\n# 2. Object reprs aren't always stable\n#    >>> User(id=42)\n#    <User object at 0x7f8b...>     # address changes; use ELLIPSIS\n#\n# 3. Set / dict ordering\n#    >>> {1, 2, 3}\n#    {1, 2, 3}                       # but Python doesn't guarantee order < 3.7\n#\n# Junior tier handles these."
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Doctest — examples that stay in sync with code — common patterns you'll see in production.\n# APPROACH  - Combine Doctest — examples that stay in sync with code with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             flexible matching, fixtures for setup.\ndef fetch_user(user_id: int) -> dict:\n    \"\"\"Fetch a user by ID.\n    Args:\n        user_id: The user's ID.\n    Returns:\n        dict with the user's profile.\n    Example:\n        >>> user = fetch_user(42)\n        >>> user[\"id\"]\n        42\n        >>> user[\"email\"]   # doctest: +ELLIPSIS\n        '...@example.com'\n        >>> \"created_at\" in user\n        True\n        For multi-line output, use NORMALIZE_WHITESPACE:\n        >>> # doctest: +NORMALIZE_WHITESPACE\n        >>> import json\n        >>> print(json.dumps({\"a\": 1, \"b\": 2}, indent=2))\n        {\n          \"a\": 1,\n          \"b\": 2\n        }\n    \"\"\"\n    return {\"id\": user_id, \"email\": f\"u{user_id}@example.com\",\n            \"created_at\": \"2024-12-01T10:00:00Z\"}\n# +ELLIPSIS:           '...' matches any substring\n# +NORMALIZE_WHITESPACE: collapse all whitespace\n# +SKIP:               skip this line (useful for env-dependent stuff)\n# +IGNORE_EXCEPTION_DETAIL: traceback message can vary\n# Set defaults for the WHOLE project in pytest config:\n# pyproject.toml\n# [tool.pytest.ini_options]\n# doctest_optionflags = \"NORMALIZE_WHITESPACE ELLIPSIS\"\n# addopts = \"--doctest-modules --doctest-glob='*.md'\"\n# Inject objects into doctest scope.\n# conftest.py:\n# import pytest\n#\n# @pytest.fixture(autouse=True)\n# def add_imports(doctest_namespace):\n#     import pandas as pd\n#     doctest_namespace[\"pd\"] = pd\n#     doctest_namespace[\"sample_df\"] = pd.DataFrame({\"x\": [1, 2, 3]})\n#\n# Now any docstring can use 'pd' or 'sample_df' without an import:\n# def transform(df):\n#     \"\"\"Transform a DataFrame.\n#\n#     Example:\n#         >>> transform(sample_df)\n#            x\n#         0  1\n#         1  2\n#         2  3\n#     \"\"\""
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Doctest — examples that stay in sync with code — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             skip-if conditions, integration with mkdocs/sphinx.\n# pip install sybil\n#\n# conftest.py:\n# from sybil import Sybil\n# from sybil.parsers.markdown import PythonCodeBlockParser, SkipParser\n#\n# pytest_collect_file = Sybil(\n#     parsers=[PythonCodeBlockParser(), SkipParser()],\n#     patterns=[\"*.md\", \"*.rst\"],\n# ).pytest()\n#\n# Now `pytest` runs every ```python code block in your .md docs.\n# Cross-references with running examples in mkdocs!\n# docs/quickstart.md:\n#\n# ## Quickstart\n#\n# Install via pip:\n#\n# ```python\n# import myproject\n# user = myproject.fetch_user(42)\n# assert user[\"id\"] == 42\n# ```\n#\n# This block runs when pytest is invoked.\n# In a docstring:\n# >>> import requests   # doctest: +SKIP\n# >>> requests.get(\"https://api.example.com\")   # doctest: +SKIP\n# In sybil-tested .md:\n# <!-- skip: requires network -->\n# ```python\n# import requests\n# requests.get(...)\n# ```\n# .github/workflows/test.yml\n# - name: Run doctests\n#   run: pytest --doctest-modules src/ docs/\n# Decision rule:\n#   simple example in docstring          -> stdlib doctest with >>>\n#   exact output matters                  -> default doctest behavior\n#   output varies (timestamps, IDs)        -> +ELLIPSIS or use placeholders\n#   complex multi-line output              -> +NORMALIZE_WHITESPACE\n#   needs setup data                        -> doctest_namespace fixture in conftest.py\n#   needs DB/network                         -> +SKIP; or pytest-vcr for replay\n#   examples in .md / .rst docs              -> sybil for cross-format testing\n#   want ALL docs tested                      -> pytest --doctest-modules + sybil\n#   examples in mkdocs / sphinx              -> sybil + mkdocs build verifies both\n#   too rigid for some examples              -> // doctest: +SKIP for env-dependent\n#   pandas DataFrame output                   -> use repr explicitly; can be brittle\n#                                                consider asserting just .shape and dtypes\n#\n# Anti-pattern: docstring examples that don't run as doctests. They\n# look authoritative (\"here's how to use it\") but drift silently —\n# the function changes, the example doesn't, users copy-paste broken\n# code. EITHER make the >>> blocks doctest-runnable AND run them in\n# CI, OR explicitly mark them as illustrative-only with a comment."
                  }
        ],
        tips: [
                  "`pytest --doctest-modules src/` runs every `>>>` block in every docstring under `src/`. Add to CI; examples can't lie.",
                  "Set `doctest_optionflags = \"NORMALIZE_WHITESPACE ELLIPSIS\"` in pyproject.toml — sane defaults for most examples.",
                  "`# doctest: +SKIP` skips an example without removing it (useful for examples that need network/DB and can't run in CI).",
                  "Use `doctest_namespace` fixture in `conftest.py` to inject common imports / sample data into all doctests. No `>>> import pandas as pd` boilerplate per docstring.",
                  "**sybil** extends this to `.md` and `.rst` files — test code blocks in your docs site so they stay accurate.",
                  "For DataFrame output, prefer asserting `df.shape` and `df.dtypes` over the full repr — repr changes break doctests on minor pandas updates."
        ],
        mistake: "Writing docstring examples that LOOK like doctests but aren't actually run. They drift silently — the function changes, the example doesn't, users copy-paste broken code. Either make the `>>>` blocks doctest-runnable AND run them in CI, OR explicitly comment them as \"illustrative only\".",
        shorthand: {
          verbose: "# Examples never tested; can drift forever\ndef fetch(uid):\n    \"\"\"Fetch a user.\n\n    Example:\n        >>> fetch(42)[\"email\"]\n        'alice@example.com'   # was true 2 years ago\n    \"\"\"",
          concise: "# Examples in CI; can't lie\n$ pytest --doctest-modules src/\n# CI fails on the day the example breaks.",
        },
      },
      {
        id: "mkdocs-vs-sphinx",
        fn: "MkDocs vs Sphinx — when each wins",
        desc: "MkDocs: Markdown-native, fast setup, modern themes via mkdocs-material, mkdocstrings for autodoc. Sphinx: RST-historical, mature autodoc, multi-format output (HTML+PDF+epub), the Python stdlib choice. The decision matrix that drives the choice.",
        category: "Patterns",
        subtitle: "MkDocs vs Sphinx feature comparison, content type, audience, hosting, autodoc differences, ecosystem",
        signature: "audience reads Markdown -> MkDocs    audience reads tech docs -> Sphinx    PDF/epub needed -> Sphinx",
        descLong: "The choice between MkDocs and Sphinx isn't about which is \"better\" — it's about which fits your content and audience. **MkDocs** wins for: Markdown-native authoring, modern theme out of the box (mkdocs-material), fast setup, simpler conf. **Sphinx** wins for: RST source, mature autodoc with autosummary, multi-format output (HTML + PDF + epub), wider extension ecosystem, ReadTheDocs paid features. The decision matrix matters more than the tool. The three examples solve the SAME concrete task — pick the right docs tool for a project — at three depths: feature comparison → audience-driven decision → migration paths between the two.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of MkDocs vs Sphinx — when each wins — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# Feature                     MkDocs                  Sphinx\n# --------------------------------------------------------------\n# Source format                Markdown (native)       RST (native), Markdown via myst-parser\n# Setup                         5 min                    10-15 min\n# Theme                         mkdocs-material          Furo, sphinx-rtd-theme, sphinx-book-theme\n# Search                        Built-in                  Built-in\n# Autodoc                        mkdocstrings (great)      sphinx.ext.autodoc (the standard)\n# PDF / epub output              No                        Yes (built-in)\n# Multi-version                  mike (separate tool)      ReadTheDocs (built-in)\n# Cross-project refs             autorefs                  intersphinx (the standard)\n# Notebook execution             mkdocs-jupyter            myst-nb\n# Extension count                ~50                        ~500\n# Build speed                    Fast                       Slower for large projects\n# Default Python project tool   Modern (FastAPI, etc.)    Stdlib, NumPy, SciPy, Django\n# Hosting                         GitHub Pages, RTD          ReadTheDocs (canonical)\n# Learning curve                  Lower                      Higher\n# Building a new public-facing Python project?\n#   -> MkDocs + mkdocs-material + mkdocstrings\n# Contributing to / forking an established project?\n#   -> Match what they use (probably Sphinx)\n# Need PDF / epub?\n#   -> Sphinx (built-in support)\n# Strict RST shop / scientific Python ecosystem?\n#   -> Sphinx\n# Want minimal config + max polish?\n#   -> MkDocs\n# Common projects using each:\n# MkDocs: FastAPI, Pydantic, Material for MkDocs, Tiangolo's stack,\n#         Litestar, Pelican, Click 8+\n# Sphinx: Python stdlib, NumPy, SciPy, Pandas, Django, Flask,\n#         scikit-learn, PyTorch, requests, urllib3"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of MkDocs vs Sphinx — when each wins — common patterns you'll see in production.\n# APPROACH  - Combine MkDocs vs Sphinx — when each wins with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#\n# Audience                              Tool             Why\n# -------------------------------------------------------------\n# General developers                    MkDocs            Modern UX; familiar Markdown\n# Scientists / researchers              Sphinx            NumPy ecosystem; PDF / epub\n# Internal teams                         MkDocs            Faster iteration; less setup\n# OSS API libraries                      Either            Both work; choose based on theme preference\n# CLI / tool docs                        Either            mkdocs-click + mkdocs-typer are great\n# Tutorials with notebooks               Either            myst-nb (Sphinx) OR mkdocs-jupyter\n# Multi-version libraries                Either            Sphinx via RTD; MkDocs via mike\n# Enterprise / regulated                  Sphinx            More mature; longer track record\n#\n# Content                              Best tool         Notes\n# ----------------------------------------------------------------\n# API reference (auto from code)        Either            mkdocstrings vs sphinx.ext.autodoc;\n#                                                          both excellent, slightly different output\n# Tutorials                              Either            Markdown tutorials work in both\n# Architecture decisions (ADRs)          MkDocs            Markdown is more readable as plain text\n# Math / scientific                      Sphinx            Better LaTeX support; MathJax in both\n# RFCs / specifications                  Sphinx            RST is purpose-built for technical specs\n# Marketing site                          Static-site gen  Either, but consider Hugo / Astro\n# Internal wiki                           Notion / Outline  Neither MkDocs nor Sphinx fit well\n#\n# Tool                MkDocs                            Sphinx\n# ----------------------------------------------------------------\n# Type hints           sphinx-autodoc-typehints         (same lib; works in both)\n# Mermaid              mkdocs-mermaid                   sphinxcontrib-mermaid\n# Tabs                 pymdownx.tabbed                  sphinx-design\n# Cards                pymdownx.blocks                  sphinx-design\n# Notebooks            mkdocs-jupyter                   myst-nb (executable)\n# Diagrams             plantuml-markdown                sphinxcontrib-plantuml\n# OpenAPI              mkdocs-render-swagger            sphinxcontrib-openapi\n# Click CLI                                              sphinx-click\n# Argparse                                                argparse helper\n# Pandas DataFrame      mkdocs-material handles         pandas2sphinx for Sphinx\n# Project size                MkDocs build       Sphinx build\n# -----------------------------------------------------------\n# 50 pages                     ~1s                ~3s\n# 500 pages                    ~5s                ~30s\n# 5000 pages                   ~30s               ~5m\n# Sphinx is slower for large projects because of the dependency\n# resolution it does per-page (cross-refs, intersphinx, etc.).\n# Most Markdown stays the same with myst-parser. RST is optional.\n# Steps:\n# 1. sphinx-quickstart docs/\n# 2. Move docs/*.md to docs/\n# 3. conf.py: extensions += [\"myst_parser\"]\n# 4. Replace mkdocstrings ::: with sphinx automodule directives.\n# Harder if you've used a lot of RST features (intersphinx, custom\n# directives). For pure-Markdown Sphinx projects: trivial."
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of MkDocs vs Sphinx — when each wins — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#\n# Just a README                            -> README.md is fine. Don't over-engineer.\n# Internal team docs (Confluence?)          -> Notion, Confluence, Outline; not MkDocs.\n# Marketing pages                            -> Hugo, Astro, Next.js, dedicated CMS.\n# Knowledge base for support                 -> Zendesk, Intercom, dedicated KB tools.\n# Specifications                              -> GitHub-flavored Markdown in repo;\n#                                                IETF RFCs use Sphinx but most don't need it.\n# A blog                                       -> Hugo / Astro / 11ty.\n# Personal site                                -> Astro / Eleventy / Jekyll.\n# Documentation tools (MkDocs/Sphinx) are for PROJECT documentation:\n#   - API reference\n#   - Tutorials\n#   - Concept explanations\n#   - Versioned per release\n#\n# 1. README + docs site\n#    - README.md: install, quickstart, link to docs\n#    - docs/: full documentation (MkDocs or Sphinx)\n#    - Both auto-deploy\n#\n# 2. Sphinx for API + MkDocs for guides\n#    Common pattern when:\n#    - The library is large with strong autodoc culture (Sphinx)\n#    - Marketing/tutorial content is Markdown-native (MkDocs)\n#    Live in different paths: docs.example.com/api (Sphinx) and\n#    docs.example.com/guides (MkDocs).\n#\n# 3. Single-source via myst-parser\n#    Use Sphinx with myst-parser; write everything in Markdown;\n#    get Sphinx's autodoc + multi-format output.\n# - Markdown is universal; everyone reads/writes it.\n# - mkdocs-material has the best out-of-the-box UX.\n# - Faster setup; faster builds.\n# - mkdocstrings is on par with Sphinx autodoc for Python.\n# - autodoc + autosummary + intersphinx are unmatched.\n# - PDF/epub built in.\n# - 15+ years of accumulated extensions.\n# - Default for the Python ecosystem (stdlib, NumPy, etc.).\n# - ReadTheDocs is purpose-built for it.\n# For NEW projects: MkDocs + mkdocs-material + mkdocstrings.\n# - Lower onboarding cost for contributors\n# - Modern UX users expect\n# - Markdown is the lingua franca\n# For EXISTING projects with Sphinx: stay on Sphinx.\n# - Migration cost is real\n# - Sphinx isn't broken; both produce great docs\n# For projects in the SCIENTIFIC PYTHON ecosystem: Sphinx.\n# - Match the convention; users have intersphinx-pre-loaded\n# For projects needing PDF/epub: Sphinx.\n# - MkDocs doesn't do this well\n# Decision rule:\n# 1. Already using one? Keep it.\n# 2. New project, no constraint? MkDocs + mkdocs-material.\n# 3. Need PDF/epub? Sphinx.\n# 4. Scientific Python? Sphinx.\n# 5. Want simplest possible setup? MkDocs.\n# 6. Want maximum flexibility / many extensions? Sphinx.\n# 7. Both work? Pick by the look of mkdocs-material vs Furo.\n#\n# Anti-pattern: switching docs tools every 2 years because \"the new\n# one is better\". Migration costs (rewriting links, retraining\n# contributors, fixing stale URLs) are real. Pick once, commit. The\n# differences are smaller than the cost of switching."
                  }
        ],
        tips: [
                  "For new projects, MkDocs + mkdocs-material + mkdocstrings is the lowest-friction path. Modern UX, fast builds, Markdown-native.",
                  "Use Sphinx if you need PDF/epub output, are in the scientific Python ecosystem, or are contributing to a project that already uses it.",
                  "Don't maintain the SAME content in both README.md and the docs site — they drift. README has install + quickstart + link to docs; docs has everything else.",
                  "Both tools support intersphinx-style cross-project links: Sphinx via `intersphinx_mapping`, MkDocs via `mkdocstrings` with `import:` URLs to `objects.inv`.",
                  "For projects with both deep API reference AND tutorials, Sphinx + myst-parser gets you both Markdown ergonomics AND `autodoc` strength.",
                  "For internal team docs (collaboration, meeting notes, project plans), use Notion/Confluence/Outline — neither MkDocs nor Sphinx fit those workflows well."
        ],
        mistake: "Switching docs tools every couple of years because \"the new one is better\". Migration costs are real — rewriting cross-references, retraining contributors, fixing every external link to a permalinked URL. Pick once, commit. The output quality difference between MkDocs and Sphinx is much smaller than the cost of migrating.",
        shorthand: {
          verbose: "# Switch every 2 years\n2022: Sphinx (RTD theme)\n2024: MkDocs (Material theme)\n2026: Switch back? URLs broken; contributors confused",
          concise: "# Pick one; commit\nFor NEW projects: MkDocs + mkdocs-material + mkdocstrings\nFor SCIENTIFIC: Sphinx (matches ecosystem)\nFor PDF/epub: Sphinx\nOtherwise: stay on whatever you have.",
        },
      },
    ],
  },
]

export default { meta, sections }

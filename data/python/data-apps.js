export const meta = {
  "id": "data-apps",
  "label": "Data Apps",
  "icon": "📊",
  "description": "Web apps for data + ML in pure Python: Streamlit (the dominant choice for internal dashboards), Dash (Plotly-based, callback-driven), Gradio (the Hugging Face standard for ML demos), plus deployment patterns (auth, multi-page, behind nginx, sticky sessions)."
}

export const sections = [

  // ── Section 1: Streamlit — dashboards, state, caching ─────────────────────────────────────────
  {
    id: "streamlit",
    title: "Streamlit — dashboards, state, caching",
    entries: [
      {
        id: "streamlit-basics",
        fn: "Streamlit basics — script-as-app, widgets, layout",
        desc: "Streamlit re-runs your Python script top-to-bottom on every widget interaction. The mental model is \"render the UI as a function of state\". Right answer for internal dashboards in <100 lines.",
        category: "Streamlit",
        subtitle: "streamlit run, st.title / write / dataframe / metric, st.selectbox / slider / file_uploader, st.sidebar, st.columns, the rerun model",
        signature: "streamlit run app.py    # the script reruns on every widget change",
        descLong: "Streamlit's model: every widget interaction (slider, button, selectbox) re-runs the entire script from top to bottom; widgets return their current value; the layout is a function of those values. This is jarring at first — there's no callback model, no event loop, no React-style component tree. The win: a 30-line script becomes a working UI; data-pipeline output becomes interactive in minutes; no JS, no HTML, no CSS for 90% of cases. The three examples solve the SAME concrete task — load a CSV, show summary stats, let the user filter by a column — at three depths: minimal `st.dataframe` + `selectbox` → sidebar layout + metrics + file uploader → production with theming, multi-column layout, performance tuning, and when to leave Streamlit.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Load a CSV; show summary stats; filter by a column.\n# APPROACH  - Read the CSV at module load; st.selectbox returns the\n#             chosen value; filter pandas DataFrame; render.\n# STRENGTHS - 15-line dashboard; live in 30 seconds via streamlit run.\n# WEAKNESSES- CSV reloads on every interaction; no caching yet.\nimport pandas as pd\nimport streamlit as st\n\nst.title(\"Sales dashboard\")\n\n# CSV load runs on EVERY rerun (every widget change).\ndf = pd.read_csv(\"sales.csv\")\n\n# Filter widget — returns the chosen value on every rerun.\nregion = st.selectbox(\"Region\", options=[\"All\"] + sorted(df[\"region\"].unique().tolist()))\n\nif region != \"All\":\n    df = df[df[\"region\"] == region]\n\nst.write(f\"**{len(df):,}** rows after filter\")\nst.dataframe(df.head(100))\nst.bar_chart(df.groupby(\"category\")[\"amount\"].sum())\n\n# Run:\n#   $ pip install streamlit pandas\n#   $ streamlit run app.py\n# Opens browser at http://localhost:8501.\n#\n# Key insight: every widget interaction reruns the WHOLE script.\n# That's why CSV reload is the next thing to fix (junior tier)."
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Streamlit basics — script-as-app, widgets, layout — common patterns you'll see in production.\n# APPROACH  - Combine Streamlit basics — script-as-app, widgets, layout with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             show metrics in columns, accept user-uploaded CSVs.\n#             control panel; st.columns for side-by-side metrics.\n#             accepts a user upload as input.\nimport pandas as pd\nimport streamlit as st\nst.set_page_config(page_title=\"Sales dashboard\", layout=\"wide\")\nst.title(\"Sales dashboard\")\n@st.cache_data\ndef load_csv(path_or_buffer) -> pd.DataFrame:\n    return pd.read_csv(path_or_buffer)\nwith st.sidebar:\n    st.header(\"Filters\")\n    uploaded = st.file_uploader(\"Upload CSV\", type=[\"csv\"])\n    region   = st.selectbox(\"Region\", [\"All\", \"us\", \"eu\", \"apac\"])\n    min_amt  = st.slider(\"Min amount ($)\", 0, 10_000, 0, step=100)\nif uploaded is None:\n    st.info(\"Upload a CSV in the sidebar to begin.\")\n    st.stop()                                          # halt rendering below\ndf = load_csv(uploaded)\nif region != \"All\":\n    df = df[df[\"region\"] == region]\ndf = df[df[\"amount\"] >= min_amt]\n# Metrics in three columns.\ncol_a, col_b, col_c = st.columns(3)\ncol_a.metric(\"Rows\",        f\"{len(df):,}\")\ncol_b.metric(\"Revenue\",     f\"${df['amount'].sum():,.0f}\")\ncol_c.metric(\"Avg order\",   f\"${df['amount'].mean():.2f}\")\n# Tabs for sections.\ntab_data, tab_chart = st.tabs([\"Data\", \"Charts\"])\nwith tab_data:\n    st.dataframe(df, use_container_width=True)\nwith tab_chart:\n    st.bar_chart(df.groupby(\"category\")[\"amount\"].sum())\n    st.line_chart(df.groupby(\"date\")[\"amount\"].sum())"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - SAME — production: theming via .streamlit/config.toml,\n#             st.fragment for partial reruns (Streamlit 1.33+),\n#             error boundaries, query-param-driven deep links, when\n#             Streamlit's model becomes a problem.\n# APPROACH  - st.fragment scopes a rerun to one section; query_params\n#             persist filter state in URL; ErrorBoundary swallows\n#             exceptions in a section without crashing the page.\n# STRENGTHS - Heavy charts don't rerun on unrelated widget changes;\n#             URLs are deep-linkable; one bad cell doesn't blank the page.\n# WEAKNESSES- Streamlit's per-session statefulness limits horizontal\n#             scaling — see deployment entry for the workarounds.\nimport pandas as pd\nimport streamlit as st\nfrom contextlib import contextmanager\n\nst.set_page_config(\n    page_title=\"Sales dashboard\",\n    page_icon=\"📊\",\n    layout=\"wide\",\n    initial_sidebar_state=\"expanded\",\n)\n\n# === Theming via .streamlit/config.toml ===\n# [theme]\n# base=\"dark\"\n# primaryColor=\"#5cf\"\n# backgroundColor=\"#0e1117\"\n\n# === Cached load ===\n@st.cache_data(ttl=300, show_spinner=\"Loading sales data...\")\ndef load_csv(path_or_buffer) -> pd.DataFrame:\n    return pd.read_csv(path_or_buffer)\n\n# === Error boundary ===\n@contextmanager\ndef error_boundary(name: str):\n    try:\n        yield\n    except Exception as e:\n        st.error(f\"**{name} failed:** {type(e).__name__}: {e}\")\n        with st.expander(\"Traceback\"):\n            import traceback\n            st.code(traceback.format_exc())\n\n# === Query-param state — URL deep-linking ===\nqp = st.query_params\ndefault_region = qp.get(\"region\", \"All\")\ndefault_min    = int(qp.get(\"min_amt\", \"0\"))\n\nwith st.sidebar:\n    st.header(\"Filters\")\n    uploaded = st.file_uploader(\"Upload CSV\", type=[\"csv\"])\n    region   = st.selectbox(\n        \"Region\", [\"All\", \"us\", \"eu\", \"apac\"],\n        index=[\"All\", \"us\", \"eu\", \"apac\"].index(default_region),\n    )\n    min_amt  = st.slider(\"Min amount ($)\", 0, 10_000, default_min, step=100)\n\n# Persist filters in URL.\nst.query_params[\"region\"]  = region\nst.query_params[\"min_amt\"] = str(min_amt)\n\nif uploaded is None:\n    st.info(\"Upload a CSV to begin.\")\n    st.stop()\n\ndf = load_csv(uploaded)\nif region != \"All\":\n    df = df[df[\"region\"] == region]\ndf = df[df[\"amount\"] >= min_amt]\n\n# === Fragments (Streamlit 1.33+): partial reruns ===\n@st.fragment\ndef chart_panel(df: pd.DataFrame):\n    \"\"\"This block reruns ONLY when widgets inside it change.\"\"\"\n    chart_type = st.radio(\"Chart type\", [\"bar\", \"line\"], horizontal=True)\n    if chart_type == \"bar\":\n        st.bar_chart(df.groupby(\"category\")[\"amount\"].sum())\n    else:\n        st.line_chart(df.groupby(\"date\")[\"amount\"].sum())\n\nwith error_boundary(\"Charts\"):\n    chart_panel(df)\n\nwith error_boundary(\"Data table\"):\n    st.dataframe(df, use_container_width=True, height=400)\n\n# Decision rule:\n#   internal dashboard, <2 weeks       -> Streamlit\n#   ML demo, share via URL              -> Gradio\n#   complex callback chains, prod UX    -> Dash (next entries)\n#   per-user persistent state           -> Streamlit OK; mind sticky sessions in deployment\n#   multi-tenant SaaS                   -> NOT Streamlit; build a real frontend\n#   need cell-level reactive logic     -> st.fragment for partial reruns\n#   share state across browser tabs    -> NOT in Streamlit; URL params + backend\n#   100k+ row tables                    -> AgGrid component; native st.dataframe is fine to ~10k\n#   real-time streaming                 -> st.write_stream + generator (LLM-style)\n#   deep links                          -> st.query_params (read + write)\n#   theming                             -> .streamlit/config.toml [theme]\n#   logged-in users                      -> reverse proxy auth (multipage-auth entry)\n#\n# Anti-pattern: doing expensive work outside @st.cache_data. EVERY\n# widget interaction reruns the script top-to-bottom; uncached\n# DB queries, file reads, or model loads run on every keystroke. Wrap\n# all expensive operations in @st.cache_data (for serializable\n# results) or @st.cache_resource (for live connections)."
                  }
        ],
        tips: [
                  "Streamlit reruns the WHOLE script on every widget change. Wrap expensive operations in `@st.cache_data` (for serializable returns) or `@st.cache_resource` (for live connections like DB pools).",
                  "`st.set_page_config(layout=\"wide\")` is essential for dashboards — default centered layout wastes ~40% of screen.",
                  "`st.fragment` (1.33+) scopes a rerun to one section. A widget inside the fragment only reruns the fragment, not the whole page — critical for heavy charts.",
                  "`st.query_params` (read + write) makes filter state deep-linkable. Users can bookmark `?region=us&min_amt=500` and land on the filtered view.",
                  "For >10k row tables, native `st.dataframe` slows down. Use `streamlit-aggrid` for pivot/group/sort UX at scale.",
                  "Use `st.stop()` to halt rendering when prerequisites are missing. Cleaner than wrapping the rest of the script in `if condition:`."
        ],
        mistake: "Doing expensive work outside `@st.cache_data`. Every widget interaction reruns the entire script top-to-bottom; uncached DB queries, large CSV reads, or model loads run on every keystroke. The UI feels broken (10s response per slider tick). Wrap all expensive ops in `@st.cache_data` or `@st.cache_resource`.",
        shorthand: {
          verbose: "// Manual / verbose approach\n# Reads CSV on every rerun (every keystroke)\ndf = pd.read_csv(\"big.csv\")\n// More explicit but longer",
          concise: "# Cached: loaded once, reused\n@st.cache_data\ndef load(): return pd.read_csv(\"big.csv\")\ndf = load()",
        },
      },
      {
        id: "streamlit-state",
        fn: "Streamlit state — session_state, callbacks, forms",
        desc: "Persist values across reruns via `st.session_state`; trigger callbacks on widget change with `on_change=`; batch widgets with `st.form` so the script doesn't rerun until submit. The state model that makes Streamlit non-trivial.",
        category: "Streamlit",
        subtitle: "st.session_state, key=, on_change / on_click callbacks, st.form / st.form_submit_button, clear_on_submit, programmatic state mutation",
        signature: "st.session_state[\"count\"] = 0\nif st.button(\"inc\", on_click=lambda: st.session_state.update(count=st.session_state.count + 1)): ...",
        descLong: "Streamlit reruns the script every interaction — but `st.session_state` is a per-browser-session dict that survives reruns. Widgets with a `key=` argument auto-bind to `session_state[key]`. Callbacks (`on_change=`, `on_click=`) fire ONCE per interaction, BEFORE the rerun, so they're where you mutate state. `st.form` batches a group of widgets — the script DOESN'T rerun on each input, only on form submission. The three examples solve the SAME concrete task — a counter with increment/reset, plus a multi-step form that doesn't fire its API call until submit — at three depths: bare `session_state` + button → callbacks + `st.form` for batching → production patterns (state versioning, programmatic reset, double-submit prevention).",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Streamlit state — session_state, callbacks, forms — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport streamlit as st\nif \"count\" not in st.session_state:\n    st.session_state[\"count\"] = 0\nst.write(f\"Count: **{st.session_state['count']}**\")\nif st.button(\"Increment\"):\n    st.session_state[\"count\"] += 1\n    # Streamlit reruns automatically after this block.\nif st.button(\"Reset\"):\n    st.session_state[\"count\"] = 0"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Streamlit state — session_state, callbacks, forms — common patterns you'll see in production.\n# APPROACH  - Combine Streamlit state — session_state, callbacks, forms with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             when SUBMIT is clicked (not every keystroke).\n#             batches inputs; clear_on_submit resets fields.\nimport streamlit as st\nimport time\ndef init_state():\n    st.session_state.setdefault(\"count\", 0)\ninit_state()\ndef inc():    st.session_state[\"count\"] += 1\ndef dec():    st.session_state[\"count\"] -= 1\ndef reset():  st.session_state[\"count\"] = 0\nst.metric(\"Count\", st.session_state[\"count\"])\nc1, c2, c3 = st.columns(3)\nc1.button(\"➕ Inc\", on_click=inc, use_container_width=True)\nc2.button(\"➖ Dec\", on_click=dec, use_container_width=True)\nc3.button(\"⟲ Reset\", on_click=reset, use_container_width=True)\nst.divider()\nst.subheader(\"Create user\")\nwith st.form(\"create_user\", clear_on_submit=True):\n    name  = st.text_input(\"Name\")\n    email = st.text_input(\"Email\")\n    role  = st.selectbox(\"Role\", [\"viewer\", \"editor\", \"admin\"])\n    submitted = st.form_submit_button(\"Create\")\nif submitted:\n    # This runs ONLY on submit — no reruns mid-typing.\n    if not name or \"@\" not in email:\n        st.error(\"Invalid input\")\n    else:\n        with st.spinner(\"Calling API...\"):\n            time.sleep(0.5)                            # imagine: requests.post(...)\n        st.success(f\"Created {name} ({role})\")\n# Why st.form matters:\n#   Without form: typing in the email field reruns the script + maybe\n#   triggers any side-effect logic. With form: NO rerun until submit."
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Streamlit state — session_state, callbacks, forms — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             prevention, state-version-aware widgets, useState-style\n#             hook helper.\n#             double-submit; key= versioning forces widget reset on\n#             logical state change.\nimport streamlit as st\nimport time\nfrom dataclasses import dataclass, field\nfrom typing import Any, Callable, TypeVar\nimport uuid\nT = TypeVar(\"T\")\n@dataclass\nclass AppState:\n    count: int = 0\n    user_form_in_flight: bool = False\n    last_created_user_id: str | None = None\n    state_version: int = 0                             # bump to reset all keyed widgets\ndef state() -> AppState:\n    if \"_state\" not in st.session_state:\n        st.session_state[\"_state\"] = AppState()\n    return st.session_state[\"_state\"]\ndef use_state(key: str, default: T) -> tuple[T, Callable[[T], None]]:\n    \"\"\"React-style: returns (value, setter).\"\"\"\n    if key not in st.session_state:\n        st.session_state[key] = default\n    def setter(v: T) -> None:\n        st.session_state[key] = v\n    return st.session_state[key], setter\ns = state()\nst.metric(\"Count\", s.count)\nc1, c2 = st.columns(2)\nif c1.button(\"➕\"):\n    s.count += 1\n    st.rerun()\nif c2.button(\"Reset\"):\n    s.count = 0\n    s.state_version += 1                               # bump → child keyed widgets reset\n    st.rerun()\nst.divider()\n# Key derived from state_version forces fresh widget on Reset.\nform_key = f\"create_user_v{s.state_version}\"\nwith st.form(form_key, clear_on_submit=False):\n    name = st.text_input(\"Name\", key=f\"name_{s.state_version}\")\n    email = st.text_input(\"Email\", key=f\"email_{s.state_version}\")\n    submitted = st.form_submit_button(\n        \"Create\",\n        disabled=s.user_form_in_flight,                # gray out during in-flight\n    )\nif submitted and not s.user_form_in_flight:\n    s.user_form_in_flight = True\n    try:\n        if not name or \"@\" not in email:\n            st.error(\"Invalid input\")\n        else:\n            with st.spinner(\"Creating...\"):\n                time.sleep(1.0)                        # imagine API call\n                user_id = str(uuid.uuid4())\n            s.last_created_user_id = user_id\n            st.success(f\"Created user {user_id}\")\n    finally:\n        s.user_form_in_flight = False\n# In a multipage app, st.session_state persists across pages of the\n# same browser tab but NOT across tabs. For cross-tab state,\n# persist to a backend (DB, Redis) keyed by user_id from auth.\n# Decision rule:\n#   widget value persists across reruns -> key= argument auto-binds to session_state\n#   reset everything                     -> bump a state_version; use it in widget keys\n#   batch inputs                         -> st.form + form_submit_button\n#   on-change callback                   -> on_change=, on_click= (fires BEFORE rerun)\n#   typed state                          -> dataclass in session_state[\"_state\"]\n#   double-submit                        -> in_flight flag + disabled= on the button\n#   share state across tabs              -> NOT session_state; DB / Redis keyed by user\n#   useState-style                        -> use_state helper returning (value, setter)\n#   programmatic refresh                  -> st.rerun() forces re-render now\n#   query-param-driven state              -> st.query_params (deep linking)\n#\n# Anti-pattern: mutating session_state INSIDE the body of the script\n# (not in a callback or button handler). The mutation happens during\n# render; downstream widgets read the new value; the next rerun\n# re-mutates; you get an infinite loop or visual flicker. Always\n# mutate state in callbacks (on_click, on_change) which fire BEFORE\n# the rerun."
                  }
        ],
        tips: [
                  "Widgets with `key=` auto-sync to `st.session_state[key]`. Reading from session_state and reading the widget return value give the same answer.",
                  "Callbacks (`on_change`, `on_click`) fire BEFORE the rerun. Mutate state there, not in the script body — body mutations cause flicker or loops.",
                  "`st.form` batches widgets — the script does NOT rerun on each input, only on `form_submit_button`. Critical for forms with many fields.",
                  "`st.rerun()` triggers an immediate rerun. Use after a state mutation that needs to take effect before the rest of the page renders.",
                  "For a \"reset everything\" button, bump a `state_version` integer and include it in widget keys (`key=f\"name_v{state_version}\"`). The new key creates a fresh widget.",
                  "`disabled=in_flight_flag` on a submit button + clearing the flag in `finally` is the simplest double-submit prevention."
        ],
        mistake: "Mutating `st.session_state` directly in the script body (not in a callback). The mutation runs during render; downstream widgets see new values; the next rerun re-mutates. Result: infinite loop or visual flicker. Always mutate in `on_change`/`on_click` callbacks, which run BEFORE the rerun.",
        shorthand: {
          verbose: "# Mutation in body — flicker / infinite rerun\nif st.session_state.count > 10:\n    st.session_state.count = 0   # mid-render mutation",
          concise: "# Mutation in callback — fires once, then rerun\ndef reset(): st.session_state.count = 0\nst.button(\"Reset\", on_click=reset)",
        },
      },
      {
        id: "streamlit-data-flow",
        fn: "Streamlit caching — cache_data, cache_resource, downloads",
        desc: "`@st.cache_data` for expensive computations that return serializable values; `@st.cache_resource` for live connections; `st.download_button` for letting users grab the result. The performance discipline that makes Streamlit usable at scale.",
        category: "Streamlit",
        subtitle: "@st.cache_data, @st.cache_resource, ttl, max_entries, hash_funcs, st.spinner, st.progress, st.download_button, async data loading",
        signature: "@st.cache_data(ttl=300, max_entries=10) def load(uid: int) -> pd.DataFrame: ...",
        descLong: "Streamlit's rerun model means EVERY interaction re-executes the script. Without caching, an expensive query runs on every keystroke. Two decorators: `@st.cache_data` for functions returning SERIALIZABLE values (DataFrames, lists, numpy arrays — Streamlit pickles + dedupes) and `@st.cache_resource` for SHARED RESOURCES (DB connections, ML models — one instance for all sessions). Choose by what the function returns: data → cache_data; live object → cache_resource. The three examples solve the SAME concrete task — compute an expensive aggregation per uploaded file; show progress; let user download — at three depths: `@st.cache_data` + `st.download_button` → TTL, `max_entries`, `st.cache_resource` for DB pools → production with Redis-backed cache, async loading, `hash_funcs` for unhashable args.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Streamlit caching — cache_data, cache_resource, downloads — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n#             progress; let user download the result.\nimport streamlit as st\nimport pandas as pd\nimport time\n@st.cache_data\ndef aggregate(df: pd.DataFrame) -> pd.DataFrame:\n    time.sleep(2)                                      # simulate slow op\n    return df.groupby(\"category\")[\"amount\"].agg([\"sum\", \"mean\", \"count\"]).reset_index()\nuploaded = st.file_uploader(\"Upload CSV\", type=[\"csv\"])\nif uploaded:\n    df = pd.read_csv(uploaded)\n    with st.spinner(\"Aggregating...\"):\n        result = aggregate(df)\n    st.dataframe(result)\n    st.download_button(\n        label=\"Download CSV\",\n        data=result.to_csv(index=False),\n        file_name=\"aggregate.csv\",\n        mime=\"text/csv\",\n    )"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Streamlit caching — cache_data, cache_resource, downloads — common patterns you'll see in production.\n# APPROACH  - Combine Streamlit caching — cache_data, cache_resource, downloads with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             cache_resource for the DB pool; progress bar for chunked\n#             work; show cache stats.\nimport streamlit as st\nimport pandas as pd\nimport sqlalchemy as sa\nimport time\nfrom io import BytesIO\n@st.cache_resource\ndef get_engine():\n    return sa.create_engine(\"postgresql+psycopg://app:secret@db/app\",\n                            pool_size=5, pool_pre_ping=True)\n@st.cache_data(ttl=300, max_entries=10, show_spinner=\"Loading...\")\ndef query_users(role: str) -> pd.DataFrame:\n    engine = get_engine()                              # cached_resource: shared\n    with engine.connect() as conn:\n        return pd.read_sql(\n            \"SELECT id, name, email, created_at FROM users WHERE role = :r\",\n            conn, params={\"r\": role},\n        )\n@st.cache_data(ttl=600, show_spinner=False)\ndef aggregate_chunked(df: pd.DataFrame) -> pd.DataFrame:\n    \"\"\"Chunked agg with progress feedback.\"\"\"\n    progress = st.progress(0, text=\"Processing...\")\n    chunks = []\n    n = max(1, len(df) // 10)\n    for i in range(0, len(df), n):\n        chunk = df.iloc[i:i + n]\n        chunks.append(chunk.groupby(\"category\")[\"amount\"].sum())\n        progress.progress(min(1.0, (i + n) / len(df)), f\"{(i+n)/len(df):.0%}\")\n        time.sleep(0.05)\n    progress.empty()\n    return pd.concat(chunks).groupby(level=0).sum().reset_index()\nst.title(\"User report\")\nrole = st.sidebar.selectbox(\"Role\", [\"viewer\", \"editor\", \"admin\"])\nusers_df = query_users(role)\nst.metric(\"Users\", len(users_df))\nst.dataframe(users_df, use_container_width=True)\n# Excel download (binary).\nbuf = BytesIO()\nwith pd.ExcelWriter(buf, engine=\"openpyxl\") as writer:\n    users_df.to_excel(writer, sheet_name=\"Users\", index=False)\nst.download_button(\n    \"Download Excel\",\n    data=buf.getvalue(),\n    file_name=f\"users_{role}.xlsx\",\n    mime=\"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet\",\n)\n# Cache management — useful for \"force refresh\" buttons.\nif st.button(\"Clear cache\"):\n    st.cache_data.clear()\n    st.rerun()"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Streamlit caching — cache_data, cache_resource, downloads — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             dedup, async data loading, hash_funcs for unhashable\n#             args, when to invalidate.\nimport streamlit as st\nimport pandas as pd\nimport asyncio, hashlib, json, time\nfrom io import BytesIO\nimport redis\n# st.cache_data is per-process; a 5-replica deployment caches 5 times.\n# For shared cache, wrap a Redis layer.\nr = redis.Redis.from_url(\"redis://localhost:6379/0\", decode_responses=False)\ndef redis_cached(*, key_prefix: str, ttl: int = 300):\n    \"\"\"Wrap a function with Redis-backed cache. Falls back to call on Redis error.\"\"\"\n    def deco(fn):\n        def wrapper(*args, **kwargs):\n            cache_key = f\"st:{key_prefix}:\" + hashlib.sha256(\n                json.dumps([args, kwargs], default=str, sort_keys=True).encode()\n            ).hexdigest()[:16]\n            try:\n                cached = r.get(cache_key)\n                if cached:\n                    import pickle\n                    return pickle.loads(cached)\n            except redis.RedisError:\n                pass\n            result = fn(*args, **kwargs)\n            try:\n                import pickle\n                r.set(cache_key, pickle.dumps(result), ex=ttl)\n            except redis.RedisError:\n                pass\n            return result\n        return wrapper\n    return deco\n@st.cache_data(ttl=300)\n@redis_cached(key_prefix=\"users\", ttl=300)\ndef load_users(role: str) -> pd.DataFrame:\n    # Two-tier cache: local @st.cache_data for in-process; Redis for cross-replica.\n    return _slow_db_query(role)\ndef _slow_db_query(role: str) -> pd.DataFrame: ...\nclass Filters:\n    def __init__(self, region: str, since: str): self.region, self.since = region, since\n@st.cache_data(hash_funcs={Filters: lambda f: (f.region, f.since)})\ndef load_filtered(filters: Filters) -> pd.DataFrame: ...\ndef run_async(coro):\n    try:\n        loop = asyncio.get_event_loop()\n        if loop.is_running():\n            return asyncio.ensure_future(coro)\n        return loop.run_until_complete(coro)\n    except RuntimeError:\n        return asyncio.run(coro)\n@st.cache_data\ndef load_from_api():\n    async def _go():\n        import httpx\n        async with httpx.AsyncClient() as client:\n            resp = await client.get(\"https://api.example.com/data\")\n            return resp.json()\n    return run_async(_go())\n# Decision rule:\n#   serializable return (DF, list, np)  -> @st.cache_data\n#   live object (DB pool, ML model)     -> @st.cache_resource\n#   per-input dedup                      -> @st.cache_data; key by args\n#   global singleton                      -> @st.cache_resource (shared across sessions)\n#   want TTL                              -> ttl=N seconds\n#   bounded memory                        -> max_entries=N\n#   force re-fetch                        -> .clear() OR caller passes a \"version\" arg\n#   unhashable args                       -> hash_funcs={MyType: lambda x: x.id}\n#   async function                        -> wrap in run_until_complete inside @st.cache_data\n#   cross-replica dedup                   -> Redis cache layer above @st.cache_data\n#   model loading                         -> @st.cache_resource (one model per process)\n#   download a DataFrame                 -> df.to_csv(index=False) -> bytes -> st.download_button\n#\n# Anti-pattern: @st.cache_data on a function returning a DB connection\n# or live model object. cache_data PICKLES the return value; pickle\n# of a connection serializes the connection state, which doesn't\n# unpickle to a working connection. Use @st.cache_resource for live\n# objects (it stores by reference, no pickle)."
                  }
        ],
        tips: [
                  "Choose decorator by return type: serializable (DataFrame, dict, list, np.ndarray) → `@st.cache_data`; live object (DB connection, ML model, file handle) → `@st.cache_resource`.",
                  "Set `ttl=N` on `@st.cache_data` for time-bound caches. Pair with `max_entries=N` so memory is bounded.",
                  "`show_spinner=\"Loading...\"` argument shows a spinner during the cached call; users get feedback for slow first-runs.",
                  "For unhashable args (custom classes), pass `hash_funcs={MyType: lambda x: x.id}` so cache_data knows how to key on them.",
                  "Streamlit doesn't natively support async. Wrap async work in `asyncio.run()` (or use a helper that handles event-loop-already-running).",
                  "For multi-replica deployments, `@st.cache_data` is per-process — your 5 replicas cache 5 times. Add a Redis layer for shared cache."
        ],
        mistake: "`@st.cache_data` on a function returning a DB connection or ML model. cache_data PICKLES the return value; pickling a connection serializes its state, which doesn't unpickle to a working connection (you'll get cryptic \"connection closed\" errors at second use). Use `@st.cache_resource` for any live object.",
        shorthand: {
          verbose: "# cache_data on a connection — pickle round-trip breaks it\n@st.cache_data\ndef get_engine(): return create_engine(URL)",
          concise: "# cache_resource — stored by reference, not pickled\n@st.cache_resource\ndef get_engine(): return create_engine(URL)",
        },
      },
    ],
  },

  // ── Section 2: Dash — Plotly callbacks, layouts ─────────────────────────────────────────
  {
    id: "dash",
    title: "Dash — Plotly callbacks, layouts",
    entries: [
      {
        id: "dash-callbacks",
        fn: "Dash callbacks — Input/Output/State, chains, pattern-matching",
        desc: "Dash uses an explicit callback model: declare which Inputs trigger which Outputs; Dash wires the reactive graph. Versus Streamlit: Dash is more like \"real React\" — explicit dependency graph, no rerun model.",
        category: "Dash",
        subtitle: "@callback Input/Output/State, prevent_initial_call, ctx.triggered_id, MATCH/ALL pattern matching, clientside_callback, background callbacks",
        signature: "@callback(Output(\"graph\", \"figure\"), Input(\"dropdown\", \"value\"))\ndef update_graph(value): return px.bar(...)",
        descLong: "Dash apps declare a layout (a tree of components) and callbacks (functions that map `Input`s to `Output`s). When an Input changes, Dash invokes the callback and updates the matching Output. No rerun-the-whole-script — only callbacks whose Inputs changed fire. The model is more verbose than Streamlit but better for complex callback chains, dynamic UIs, and apps that need to scale to many users (no per-session statefulness in the script). The three examples solve the SAME concrete task — dropdown picks a region; chart updates; selection in chart triggers a detail panel — at three depths: basic Input/Output → chains + State (read-without-trigger) + `prevent_initial_call` → pattern-matching for dynamic UIs + background callbacks for slow work.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Dash callbacks — Input/Output/State, chains, pattern-matching — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport dash\nfrom dash import dcc, html, Input, Output, callback\nimport pandas as pd\nimport plotly.express as px\ndf = pd.read_csv(\"sales.csv\")\napp = dash.Dash(__name__)\napp.layout = html.Div([\n    html.H1(\"Sales by category\"),\n    dcc.Dropdown(\n        id=\"region\",\n        options=[{\"label\": r, \"value\": r} for r in sorted(df[\"region\"].unique())],\n        value=\"us\",\n    ),\n    dcc.Graph(id=\"chart\"),\n])\n@callback(Output(\"chart\", \"figure\"), Input(\"region\", \"value\"))\ndef update_chart(region: str):\n    sub = df[df[\"region\"] == region]\n    return px.bar(sub.groupby(\"category\")[\"amount\"].sum().reset_index(),\n                  x=\"category\", y=\"amount\")\nif __name__ == \"__main__\":\n    app.run(debug=True)\n# Run: $ python app.py  → http://localhost:8050"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Dash callbacks — Input/Output/State, chains, pattern-matching — common patterns you'll see in production.\n# APPROACH  - Combine Dash callbacks — Input/Output/State, chains, pattern-matching with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             drives a detail panel), State for read-without-trigger,\n#             and prevent_initial_call for tidy startup.\nimport dash\nfrom dash import dcc, html, Input, Output, State, callback, ctx\nimport pandas as pd\nimport plotly.express as px\ndf = pd.read_csv(\"sales.csv\")\napp = dash.Dash(__name__)\napp.layout = html.Div([\n    html.H1(\"Sales explorer\"),\n    html.Div([\n        dcc.Dropdown(id=\"region\",\n                     options=[{\"label\": r, \"value\": r} for r in sorted(df[\"region\"].unique())],\n                     value=\"us\"),\n        dcc.RangeSlider(id=\"amt-range\", min=0, max=10_000, step=100, value=[0, 10_000]),\n    ], style={\"display\": \"flex\", \"gap\": \"1rem\"}),\n    dcc.Graph(id=\"chart\"),\n    html.Div(id=\"detail-panel\"),\n    dcc.Store(id=\"filters-state\"),                     # client-side store for cross-callback state\n])\n# Callback 1: filters → store + chart.\n@callback(\n    Output(\"chart\", \"figure\"),\n    Output(\"filters-state\", \"data\"),\n    Input(\"region\", \"value\"),\n    Input(\"amt-range\", \"value\"),\n)\ndef update_chart(region, amt_range):\n    lo, hi = amt_range\n    sub = df[(df[\"region\"] == region) & (df[\"amount\"].between(lo, hi))]\n    fig = px.bar(sub.groupby(\"category\")[\"amount\"].sum().reset_index(),\n                 x=\"category\", y=\"amount\")\n    fig.update_layout(clickmode=\"event+select\")\n    return fig, {\"region\": region, \"lo\": lo, \"hi\": hi}\n# Callback 2: chart click → detail panel; uses State (no trigger).\n@callback(\n    Output(\"detail-panel\", \"children\"),\n    Input(\"chart\", \"clickData\"),\n    State(\"filters-state\", \"data\"),\n    prevent_initial_call=True,                         # don't fire on page load\n)\ndef show_detail(click_data, filters):\n    if not click_data:\n        return \"Click a bar to see details.\"\n    category = click_data[\"points\"][0][\"x\"]\n    sub = df[(df[\"region\"] == filters[\"region\"]) & (df[\"category\"] == category)]\n    return html.Table([\n        html.Tr([html.Th(\"Order\"), html.Th(\"Amount\")]),\n        *[html.Tr([html.Td(r.id), html.Td(f\"${r.amount:.0f}\")]) for r in sub.head(10).itertuples()],\n    ])\nif __name__ == \"__main__\":\n    app.run(debug=True)"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Dash callbacks — Input/Output/State, chains, pattern-matching — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             N filters, background callbacks for slow queries via\n#             DiskCache, clientside_callback for snappy UI updates.\nimport dash\nfrom dash import dcc, html, Input, Output, State, callback, ALL, MATCH, ctx\nfrom dash.long_callback import DiskcacheLongCallbackManager\nimport diskcache\nimport pandas as pd\nimport plotly.express as px\nimport time\ncache = diskcache.Cache(\"./.dash-cache\")\nlong_cb_manager = DiskcacheLongCallbackManager(cache)\napp = dash.Dash(__name__, long_callback_manager=long_cb_manager)\n# Dynamically-added filter rows have IDs like {\"type\": \"filter\", \"index\": N}\napp.layout = html.Div([\n    html.H1(\"Dynamic filters\"),\n    html.Button(\"Add filter\", id=\"add-filter\"),\n    html.Div(id=\"filter-rows\", children=[]),\n    dcc.Graph(id=\"result-chart\"),\n    html.Button(\"Run report\", id=\"run-report\"),\n    html.Div(id=\"report-status\"),\n])\n# Add filter row dynamically.\n@callback(\n    Output(\"filter-rows\", \"children\"),\n    Input(\"add-filter\", \"n_clicks\"),\n    State(\"filter-rows\", \"children\"),\n    prevent_initial_call=True,\n)\ndef add_filter_row(_n, existing):\n    idx = len(existing or [])\n    return (existing or []) + [\n        html.Div([\n            dcc.Dropdown(\n                id={\"type\": \"filter-field\", \"index\": idx},\n                options=[{\"label\": c, \"value\": c} for c in [\"region\", \"category\", \"amount\"]],\n            ),\n            dcc.Input(\n                id={\"type\": \"filter-value\", \"index\": idx},\n                placeholder=\"value\",\n            ),\n        ], style={\"display\": \"flex\", \"gap\": \"1rem\"})\n    ]\n# Pattern-matching callback: gathers ALL filter rows.\n@callback(\n    Output(\"result-chart\", \"figure\"),\n    Input({\"type\": \"filter-field\", \"index\": ALL}, \"value\"),\n    Input({\"type\": \"filter-value\", \"index\": ALL}, \"value\"),\n)\ndef update_with_dynamic_filters(fields, values):\n    df = pd.read_csv(\"sales.csv\")\n    for field, value in zip(fields or [], values or []):\n        if field and value:\n            df = df[df[field].astype(str) == str(value)]\n    return px.bar(df.groupby(\"category\")[\"amount\"].sum().reset_index(),\n                  x=\"category\", y=\"amount\")\n@callback(\n    output=Output(\"report-status\", \"children\"),\n    inputs=Input(\"run-report\", \"n_clicks\"),\n    background=True,\n    manager=long_cb_manager,\n    running=[\n        (Output(\"run-report\", \"disabled\"), True, False),\n    ],\n    progress=Output(\"report-status\", \"children\"),\n    prevent_initial_call=True,\n)\ndef run_report(set_progress, _n):\n    for i in range(10):\n        time.sleep(0.5)\n        set_progress(f\"Processing... {(i+1)*10}%\")\n    return \"Report complete!\"\napp.clientside_callback(\n    \"function(value) { return value ? 'Selected: ' + value : 'Nothing selected'; }\",\n    Output(\"display-clientside\", \"children\"),\n    Input(\"dropdown-clientside\", \"value\"),\n)\n# Decision rule:\n#   simple input -> output                -> @callback Input/Output\n#   read state without triggering         -> State()\n#   chained callbacks                      -> chain Outputs to next Inputs\n#   dynamic N filters/charts              -> pattern-matching IDs + ALL/MATCH\n#   slow operation (>2s)                  -> background=True with long_callback_manager\n#   snappy UI tweaks                      -> clientside_callback (browser JS)\n#   prevent on page load                   -> prevent_initial_call=True\n#   \"which input fired?\"                   -> ctx.triggered_id\n#   \"all callbacks at once\" pattern        -> use one callback with multiple Outputs\n#   share state across callbacks          -> dcc.Store (browser-side) OR url query\n#   horizontal scaling                     -> Dash is stateless per request; scales freely\n#   multi-page                              -> dash.register_page + use_pages=True\n#\n# Anti-pattern: writing to a global Python variable from inside a\n# callback. Dash workers may serve different requests on different\n# threads/processes; global mutation = race conditions and\n# cross-user data leaks. Use dcc.Store (browser-side state),\n# pass values through the callback graph, or persist to a\n# real backend."
                  }
        ],
        tips: [
                  "`@callback(Output, Input)` is the explicit reactive graph. Inputs trigger; State reads-without-triggering; Output receives.",
                  "`prevent_initial_call=True` prevents the callback from firing on page load — useful when the callback needs an explicit user action.",
                  "`ctx.triggered_id` tells you WHICH input fired the callback (when multiple Inputs share a callback). Essential for \"which button was clicked\".",
                  "Pattern-matching IDs (`{\"type\": \"filter\", \"index\": N}` + `ALL`/`MATCH`) let you build dynamic UIs (add/remove rows). The MATCH version pairs by index across components.",
                  "`background=True` + `long_callback_manager` runs slow callbacks in a worker process so the UI stays responsive. Pair with `running=[(Output, True, False)]` to disable the trigger button during work.",
                  "`clientside_callback` runs JavaScript in the browser — zero server round-trip. Use for snappy formatting/display logic that doesn't need server data."
        ],
        mistake: "Writing to a global Python variable from inside a callback. Dash workers serve different requests on different threads/processes — global mutation causes race conditions and (worse) cross-user data leaks where user A's state appears in user B's session. Use `dcc.Store` (browser-side, per-session) or pass values explicitly through the callback graph.",
        shorthand: {
          verbose: "# Global mutation — cross-user data leak\nUSER_FILTERS = {}\n@callback(...)\ndef store(value):\n    USER_FILTERS[\"region\"] = value   # which user???",
          concise: "# dcc.Store (browser-side, per-session)\ndcc.Store(id=\"user-filters\")\n@callback(Output(\"user-filters\", \"data\"), Input(\"region\", \"value\"))\ndef store(value): return {\"region\": value}",
        },
      },
      {
        id: "dash-layouts",
        fn: "Dash layouts — bootstrap, theming, multi-page apps",
        desc: "Build responsive layouts with `dash-bootstrap-components` (`dbc`); theme via Bootswatch; multi-page apps via the `pages/` directory and `dash.register_page`. Production-grade UI without writing CSS.",
        category: "Dash",
        subtitle: "dbc.Container/Row/Col, dbc.themes, dbc.Card / Navbar / Tabs, dash.register_page, use_pages=True, query string state, dark mode toggle",
        signature: "app = Dash(use_pages=True, external_stylesheets=[dbc.themes.BOOTSTRAP])\n# pages/home.py: dash.register_page(__name__, path=\"/\")",
        descLong: "`dash-bootstrap-components` (dbc) wraps Bootstrap into Dash components — `dbc.Row`, `dbc.Col`, `dbc.Card`, `dbc.Navbar`. Bootstrap's grid system gives you responsive layouts (mobile, tablet, desktop) without writing CSS. Theming via `dbc.themes` swaps the entire color palette via Bootswatch. Multi-page apps: `pages/home.py`, `pages/users.py`; each calls `dash.register_page(__name__)`; `app = Dash(use_pages=True)` wires routing. The three examples solve the SAME concrete task — 3-column layout (sidebar, main, detail), themed, responsive on mobile, multi-page navigation — at three depths: html.Div + style → dbc.Container/Row/Col + dbc.Card → multi-page with dark-mode toggle, header navbar, deep linking via dcc.Location.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Dash layouts — bootstrap, theming, multi-page apps — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport dash\nfrom dash import dcc, html\napp = dash.Dash(__name__)\napp.layout = html.Div([\n    html.Div([html.H3(\"Filters\"), dcc.Dropdown(id=\"r\", options=[\"us\",\"eu\"])],\n             style={\"width\": \"20%\", \"padding\": \"1rem\", \"background\": \"#f0f0f0\"}),\n    html.Div([dcc.Graph(id=\"chart\")],\n             style={\"width\": \"55%\", \"padding\": \"1rem\"}),\n    html.Div([html.H3(\"Detail\"), html.Pre(id=\"detail\")],\n             style={\"width\": \"25%\", \"padding\": \"1rem\", \"background\": \"#fafafa\"}),\n], style={\"display\": \"flex\"})\n# Works, but: not responsive (3 columns at any width); inline styles;\n# no theming. Junior tier upgrades to dbc."
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Dash layouts — bootstrap, theming, multi-page apps — common patterns you'll see in production.\n# APPROACH  - Combine Dash layouts — bootstrap, theming, multi-page apps with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n# pip install dash-bootstrap-components\nimport dash\nfrom dash import dcc, html\nimport dash_bootstrap_components as dbc\napp = dash.Dash(__name__, external_stylesheets=[dbc.themes.FLATLY])\napp.layout = dbc.Container([\n    dbc.Navbar(\n        dbc.Container([\n            dbc.NavbarBrand(\"Sales Explorer\", className=\"ms-2\"),\n            dbc.Nav([\n                dbc.NavItem(dbc.NavLink(\"Home\", href=\"/\")),\n                dbc.NavItem(dbc.NavLink(\"Users\", href=\"/users\")),\n            ]),\n        ]),\n        color=\"primary\", dark=True, className=\"mb-3\",\n    ),\n    dbc.Row([\n        # Sidebar — 3 cols on lg screens, full width on mobile.\n        dbc.Col([\n            dbc.Card([\n                dbc.CardHeader(\"Filters\"),\n                dbc.CardBody([\n                    dcc.Dropdown(id=\"region\", options=[\"us\", \"eu\"]),\n                    html.Div(className=\"mt-3\"),\n                    dcc.RangeSlider(id=\"amt-range\", min=0, max=10_000),\n                ]),\n            ], className=\"mb-3\"),\n        ], lg=3, md=12),\n        # Main pane — 6 cols on lg, full on mobile.\n        dbc.Col([\n            dbc.Card([\n                dbc.CardHeader(\"Sales by category\"),\n                dbc.CardBody([dcc.Graph(id=\"chart\")]),\n            ]),\n        ], lg=6, md=12),\n        # Detail — 3 cols on lg, full on mobile.\n        dbc.Col([\n            dbc.Card([\n                dbc.CardHeader(\"Detail\"),\n                dbc.CardBody([html.Div(id=\"detail\")]),\n            ]),\n        ], lg=3, md=12),\n    ]),\n], fluid=True)\n# Available themes via dbc.themes:\n#   BOOTSTRAP, FLATLY (default-ish), DARKLY, CYBORG, SOLAR, ...\n#   Browse: https://bootswatch.com"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Dash layouts — bootstrap, theming, multi-page apps — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             query-string-driven state.\n# Project structure:\n#   app.py               <- main Dash app\n#   pages/home.py        <- registered as path=\"/\"\n#   pages/users.py       <- registered as path=\"/users\"\n#   pages/reports.py     <- registered as path=\"/reports\"\nimport dash\nfrom dash import Dash, html, dcc, Input, Output, callback, page_container\nimport dash_bootstrap_components as dbc\napp = Dash(\n    __name__,\n    use_pages=True,                                    # auto-discover pages/\n    external_stylesheets=[dbc.themes.BOOTSTRAP],\n    suppress_callback_exceptions=True,\n)\napp.layout = dbc.Container([\n    # Persistent location component (URL state).\n    dcc.Location(id=\"url\"),\n    # Theme stylesheet swap target.\n    html.Link(id=\"theme-link\", rel=\"stylesheet\", href=dbc.themes.BOOTSTRAP),\n    # Header navbar.\n    dbc.Navbar(\n        dbc.Container([\n            dbc.NavbarBrand(\"Sales Hub\", href=\"/\"),\n            dbc.Nav([\n                dbc.NavLink(p[\"name\"], href=p[\"path\"], active=\"exact\")\n                for p in dash.page_registry.values()\n            ], pills=True),\n            html.Div(className=\"ms-auto\", children=[\n                dbc.Switch(id=\"theme-toggle\", label=\"Dark\", value=False),\n            ]),\n        ], fluid=True),\n        color=\"dark\", dark=True, className=\"mb-3\",\n    ),\n    # Page content slot.\n    page_container,\n], fluid=True)\n@callback(Output(\"theme-link\", \"href\"), Input(\"theme-toggle\", \"value\"))\ndef toggle_theme(dark: bool):\n    return dbc.themes.DARKLY if dark else dbc.themes.BOOTSTRAP\nif __name__ == \"__main__\":\n    app.run(debug=True)\n# import dash, dash_bootstrap_components as dbc\n# from dash import html, dcc\n#\n# dash.register_page(__name__, path=\"/\", name=\"Home\")\n#\n# layout = dbc.Container([\n#     html.H1(\"Welcome\"),\n#     dbc.Row([\n#         dbc.Col(dbc.Card([dbc.CardBody(\"Card 1\")])),\n#         dbc.Col(dbc.Card([dbc.CardBody(\"Card 2\")])),\n#         dbc.Col(dbc.Card([dbc.CardBody(\"Card 3\")])),\n#     ]),\n# ])\n# import dash, dash_bootstrap_components as dbc\n# from dash import html, dcc, callback, Input, Output\n#\n# dash.register_page(__name__, path=\"/users\", name=\"Users\")\n#\n# def layout(role: str | None = None, **kw):\n#     # query-string param: /users?role=admin\n#     return dbc.Container([\n#         html.H1(f\"Users (role={role or 'any'})\"),\n#         html.Div(id=\"users-table\"),\n#     ])\n#\n# @callback(Output(\"users-table\", \"children\"), Input(\"role\", \"value\"))\n# def render(role): ...\n# Decision rule:\n#   responsive layout                       -> dbc.Row + dbc.Col with breakpoint args\n#   theming                                  -> dbc.themes.* + Bootswatch reference\n#   dark mode toggle                         -> swap external_stylesheets via Output\n#   multi-page                               -> use_pages=True + pages/ dir + register_page\n#   active link highlighting                  -> dbc.NavLink active=\"exact\"\n#   query-string state                        -> page-level layout(role=...) signature\n#   browser back/forward                       -> dcc.Location handles automatically\n#   complex auth                               -> reverse proxy auth; per-page check via callback\n#   custom CSS                                  -> assets/ dir; auto-loaded\n#   embedded in Flask                           -> server=Flask(__name__); Dash(server=server)\n#   alternative theming framework              -> dash-mantine-components (DMC)\n#\n# Anti-pattern: hardcoding pixel widths in inline styles for layout.\n# Looks fine on dev laptop, broken on mobile, broken on 4K monitor.\n# Use dbc.Row/Col with responsive breakpoints (lg=, md=, sm=) — they\n# adapt automatically."
                  }
        ],
        tips: [
                  "`dash-bootstrap-components` (`dbc`) wraps Bootstrap. `dbc.Row` + `dbc.Col` with breakpoint arguments (`lg=3, md=6, sm=12`) gives responsive layouts without CSS.",
                  "Theme via `dbc.themes.X` — Bootswatch themes covered: BOOTSTRAP, FLATLY, DARKLY, CYBORG, SOLAR, etc. Swap by changing `external_stylesheets`.",
                  "For multi-page: `Dash(use_pages=True)` + `pages/` directory; each page calls `dash.register_page(__name__, path=\"/...\")`. The `page_container` in your layout renders the active page.",
                  "`dcc.Location(id=\"url\")` is the source of truth for URL state. Browser back/forward works automatically; query params are passed to page `layout(**kwargs)`.",
                  "Dynamic theme switching: bind a callback to update `external_stylesheets` Output; users get dark/light mode without page reload.",
                  "For custom CSS, drop `.css` files in `assets/` — Dash auto-loads them."
        ],
        mistake: "Hardcoded pixel widths in inline styles. Looks fine on dev laptop, broken on mobile (overflow), broken on 4K (everything tiny). Use `dbc.Row` + `dbc.Col(lg=3, md=12)` — Bootstrap's grid auto-stacks on small screens.",
        shorthand: {
          verbose: "// Manual / verbose approach\n# Pixel widths — only works at one screen size\nhtml.Div([...], style={\"width\": \"300px\"})\n// More explicit but longer",
          concise: "# Responsive grid — adapts to screen size\ndbc.Col([...], lg=3, md=6, sm=12)",
        },
      },
    ],
  },

  // ── Section 3: Gradio — ML demos, Blocks, Hugging Face ─────────────────────────────────────────
  {
    id: "gradio",
    title: "Gradio — ML demos, Blocks, Hugging Face",
    entries: [
      {
        id: "gradio-interface",
        fn: "Gradio Interface — wrap a function as a UI",
        desc: "`gr.Interface(fn, inputs, outputs)` turns any Python function into a web UI. The Hugging Face standard for ML demos: image classifiers, text generators, audio transcribers — wrap and share in 5 lines.",
        category: "Gradio",
        subtitle: "gr.Interface, type-inferred widgets, examples=, queue, batch=, .launch(share=True), Hugging Face Spaces, ChatInterface",
        signature: "gr.Interface(fn=predict, inputs=gr.Image(), outputs=gr.Label()).launch(share=True)",
        descLong: "Gradio (Hugging Face) is the dominant choice for \"wrap an ML model as a web demo\". `gr.Interface(fn, inputs, outputs)` introspects type hints AND component args to build the right widgets — no layout code needed for simple cases. `share=True` creates a public tunnel URL (works through firewalls). Hugging Face Spaces hosts Gradio apps for free with GPU support. For chatbots, `gr.ChatInterface` handles multi-turn UI. The three examples solve the SAME concrete task — wrap an image classifier as a web demo with examples and a public URL — at three depths: minimal `gr.Interface` → multi-input + examples + queue for concurrent users → `gr.ChatInterface` for streaming LLM, OAuth, deployment to HF Spaces.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Gradio Interface — wrap a function as a UI — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# pip install gradio\nimport gradio as gr\ndef classify(image):\n    # Imagine: PyTorch / TF model.\n    return {\"cat\": 0.6, \"dog\": 0.3, \"bird\": 0.1}\ndemo = gr.Interface(\n    fn=classify,\n    inputs=gr.Image(type=\"pil\"),\n    outputs=gr.Label(num_top_classes=3),\n    title=\"Image classifier\",\n)\ndemo.launch(share=True)\n# share=True prints a public *.gradio.live URL (tunnels through HF servers)."
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Gradio Interface — wrap a function as a UI — common patterns you'll see in production.\n# APPROACH  - Combine Gradio Interface — wrap a function as a UI with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             examples, batched inference, queue for concurrent users.\nimport gradio as gr\nfrom PIL import Image\nimport numpy as np\nEXAMPLES = [\n    [\"examples/cat.jpg\", 0.5],\n    [\"examples/dog.jpg\", 0.5],\n    [\"examples/bird.jpg\", 0.3],\n]\ndef classify_batch(images: list, threshold: float) -> list:\n    \"\"\"Batched: receives lists when batch=True.\"\"\"\n    out = []\n    for img in images:\n        scores = {\"cat\": 0.6, \"dog\": 0.3, \"bird\": 0.1}\n        # Filter by threshold.\n        out.append({k: v for k, v in scores.items() if v >= threshold})\n    return [out]                                       # list of lists for batched\ndemo = gr.Interface(\n    fn=classify_batch,\n    inputs=[\n        gr.Image(type=\"pil\", label=\"Upload image\"),\n        gr.Slider(0.0, 1.0, value=0.5, label=\"Confidence threshold\"),\n    ],\n    outputs=gr.Label(num_top_classes=3),\n    examples=EXAMPLES,\n    cache_examples=True,                               # pre-compute on launch\n    title=\"Image classifier\",\n    description=\"Upload an image; results filtered by threshold.\",\n    batch=True, max_batch_size=4,                      # batch up to 4 concurrent requests\n)\ndemo.queue(default_concurrency_limit=10)               # max 10 concurrent\ndemo.launch()"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Gradio Interface — wrap a function as a UI — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             OAuth-gated, deployed to Hugging Face Spaces with GPU.\nimport gradio as gr\nimport os, time\nfrom typing import Iterator\ndef chat_stream(message: str, history: list[dict]) -> Iterator[str]:\n    \"\"\"Yield tokens; Gradio streams them to the UI.\"\"\"\n    full_response = \"\"\n    # Imagine: OpenAI streaming or local model with token-by-token.\n    for token in [\"Hello\", \" there\", \"!\", \" How\", \" can\", \" I\", \" help?\"]:\n        full_response += token\n        time.sleep(0.05)\n        yield full_response                            # cumulative; Gradio renders progressively\ndemo = gr.ChatInterface(\n    fn=chat_stream,\n    type=\"messages\",                                   # OpenAI message format\n    title=\"Chat demo\",\n    examples=[\"What's the weather?\", \"Tell me a joke\", \"Explain Streamlit\"],\n    multimodal=False,                                   # set True for image+text inputs\n    cache_examples=False,\n)\n# Built-in basic auth:\ndemo.launch(\n    auth=(\"user\", os.environ.get(\"GRADIO_PASSWORD\", \"secret\")),\n    auth_message=\"Internal demo\",\n    server_name=\"0.0.0.0\",\n    server_port=7860,\n)\n# Or OAuth via HF Spaces (when deployed there):\n# demo.launch()  # HF Spaces handles OAuth via Settings > Access; gr.OAuthProfile in fn signature.\n# 1. Create a Space at huggingface.co/new-space (Gradio template).\n# 2. Push your repo:\n#    $ git remote add space https://huggingface.co/spaces/USER/myapp\n#    $ git push space main\n# 3. README.md frontmatter configures the Space:\n#    ---\n#    title: My Demo\n#    emoji: 🤖\n#    colorFrom: blue\n#    colorTo: purple\n#    sdk: gradio\n#    sdk_version: 4.44.0\n#    app_file: app.py\n#    pinned: false\n#    hardware: t4-small        # GPU; or cpu-basic for free\n#    ---\n# 4. Spaces auto-builds; live at huggingface.co/spaces/USER/myapp\n# Gradio apps expose a JSON API; call from Python:\n# from gradio_client import Client\n# client = Client(\"USER/myapp\")\n# result = client.predict(\"hello\", api_name=\"/chat\")\n# Decision rule:\n#   single function → demo                  -> gr.Interface\n#   multi-turn chatbot                       -> gr.ChatInterface (built-in streaming)\n#   complex multi-input UI                   -> gr.Blocks (next entry)\n#   public share with no infra                -> .launch(share=True) tunnel\n#   permanent free hosting                   -> Hugging Face Spaces\n#   GPU                                       -> HF Spaces hardware= or self-host\n#   batched inference                         -> batch=True, max_batch_size=N\n#   queue for concurrent users               -> .queue(default_concurrency_limit=N)\n#   pre-computed examples                     -> cache_examples=True\n#   auth                                      -> auth=(user, pass) or OAuth via HF\n#   programmatic call                         -> gradio_client.Client; HTTP API auto-generated\n#   embed in another site                     -> iframe with HF Spaces URL\n#\n# Anti-pattern: launching with share=True in production. The\n# *.gradio.live URL is public AND temporary (72-hour TTL). For\n# production, deploy to HF Spaces (free for public, paid for private)\n# or self-host behind a real load balancer."
                  }
        ],
        tips: [
                  "`gr.Interface(fn, inputs, outputs)` handles 80% of demos. Components match argument types: `gr.Image` ↔ PIL Image / numpy / file path; `gr.Audio` ↔ tuple(rate, np.array); `gr.Dataframe` ↔ pandas.",
                  "Use `examples=[[...], [...]]` to pre-populate input examples. `cache_examples=True` runs them once at launch and caches outputs.",
                  "`batch=True, max_batch_size=N` accepts batched calls — your function receives lists. Critical for ML inference where batching gets 5-10× speedup.",
                  "`.queue(default_concurrency_limit=N)` controls concurrent users. Default is 1 — bump for parallel inference (GPU-bound, batch=True is even better).",
                  "For chatbots, `gr.ChatInterface` is purpose-built — multi-turn history, streaming via generator, OpenAI message format. Don't reinvent.",
                  "Deploy to Hugging Face Spaces for free permanent hosting (community plan) — GPU available paid. The `share=True` tunnel URL is dev-only."
        ],
        mistake: "Launching with `share=True` in production. The `*.gradio.live` URL is public AND temporary (72-hour TTL). Users hit a dead URL after a couple days; debugging is opaque (you didn't set up any infra). Deploy to HF Spaces (free, permanent) or self-host behind a real LB.",
        shorthand: {
          verbose: "// Manual / verbose approach\n# share=True — public, temporary, no auth\ndemo.launch(share=True)\n// More explicit but longer",
          concise: "# HF Spaces deployment — permanent, optional auth, GPU\n$ git push space main    # via huggingface.co/spaces/USER/myapp",
        },
      },
      {
        id: "gradio-blocks",
        fn: "Gradio Blocks — multi-tab UIs, events, state",
        desc: "`gr.Blocks` is Gradio's low-level layout primitive. Tabs, events (`.click`, `.change`, `.then`), conditional UI, cross-event `gr.State`. Use when `gr.Interface` is too restrictive.",
        category: "Gradio",
        subtitle: "gr.Blocks, gr.Tabs / Tab, .click() / .change() / .submit(), .then() chains, gr.State, conditional visibility, mounted in FastAPI",
        signature: "with gr.Blocks() as demo:\n    btn = gr.Button(\"Go\")\n    out = gr.Textbox()\n    btn.click(fn, inputs=[...], outputs=[out])",
        descLong: "`gr.Blocks` exposes Gradio's full layout + event system: `with gr.Blocks() as demo:` opens a context; widgets attach to events via `.click()`, `.change()`, `.submit()`. Events return `Dependency` objects you can chain with `.then(other_fn, ...)`. `gr.State` holds per-session state across events. Use Blocks when you need: tabs, sidebars, conditional UI, multi-step flows, or anything Interface can't express. The three examples solve the SAME concrete task — multi-tab UI: Tab 1 generates text, Tab 2 transforms it, results persist across tabs — at three depths: basic tabs + buttons → events with State + conditional visibility → mounted in FastAPI with auth, queue management, custom theming.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Gradio Blocks — multi-tab UIs, events, state — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nimport gradio as gr\nwith gr.Blocks() as demo:\n    gr.Markdown(\"# Multi-tab demo\")\n    with gr.Tabs():\n        with gr.Tab(\"Generate\"):\n            name = gr.Textbox(label=\"Name\")\n            greet_btn = gr.Button(\"Greet\")\n            greeting = gr.Textbox(label=\"Greeting\", interactive=False)\n            greet_btn.click(\n                fn=lambda n: f\"Hello, {n}!\",\n                inputs=name,\n                outputs=greeting,\n            )\n        with gr.Tab(\"Echo\"):\n            echo_in = gr.Textbox(label=\"Input\")\n            echo_out = gr.Textbox(label=\"Output\")\n            echo_in.change(lambda x: x.upper(), echo_in, echo_out)\ndemo.launch()"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Gradio Blocks — multi-tab UIs, events, state — common patterns you'll see in production.\n# APPROACH  - Combine Gradio Blocks — multi-tab UIs, events, state with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             (.then); conditional visibility.\nimport gradio as gr\ndef generate(prompt: str) -> str:\n    return f\"Generated: {prompt[:50]}...\"\ndef transform(text: str, op: str) -> str:\n    return text.upper() if op == \"upper\" else text.lower()\nwith gr.Blocks(title=\"Generate + Transform\") as demo:\n    gr.Markdown(\"# Pipeline demo\")\n    # gr.State holds session-scoped data across tabs.\n    last_generated = gr.State(value=\"\")\n    with gr.Tabs() as tabs:\n        with gr.Tab(\"Generate\"):\n            prompt = gr.Textbox(label=\"Prompt\", lines=3)\n            gen_btn = gr.Button(\"Generate\", variant=\"primary\")\n            gen_out = gr.Textbox(label=\"Output\", interactive=False)\n            advanced = gr.Checkbox(label=\"Show advanced\", value=False)\n            seed = gr.Number(label=\"Seed\", value=42, visible=False)\n            # Show/hide seed based on checkbox.\n            advanced.change(\n                fn=lambda v: gr.update(visible=v),\n                inputs=advanced,\n                outputs=seed,\n            )\n            # Chain: generate → save to state → switch to Transform tab.\n            gen_btn.click(\n                fn=generate, inputs=prompt, outputs=gen_out,\n            ).then(\n                fn=lambda x: x, inputs=gen_out, outputs=last_generated,\n            ).then(\n                fn=lambda: gr.Tabs(selected=1), inputs=None, outputs=tabs,\n            )\n        with gr.Tab(\"Transform\"):\n            xform_in = gr.Textbox(label=\"Input (auto-filled)\")\n            op = gr.Radio([\"upper\", \"lower\"], label=\"Op\", value=\"upper\")\n            xform_btn = gr.Button(\"Transform\")\n            xform_out = gr.Textbox(label=\"Result\")\n            # On tab change, copy state into the textbox.\n            demo.load(\n                fn=lambda x: x,\n                inputs=last_generated,\n                outputs=xform_in,\n            )\n            xform_btn.click(transform, [xform_in, op], xform_out)\ndemo.launch()"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Gradio Blocks — multi-tab UIs, events, state — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             API endpoints; custom theme; queue management for ML.\nimport gradio as gr\nfrom fastapi import FastAPI, Depends, HTTPException\nfrom fastapi.responses import RedirectResponse\nimport os\ntheme = gr.themes.Soft(\n    primary_hue=\"blue\", secondary_hue=\"indigo\",\n    font=[gr.themes.GoogleFont(\"Inter\"), \"ui-sans-serif\", \"system-ui\"],\n)\ndef generate(prompt: str, temp: float = 0.7) -> str:\n    return f\"[generated with t={temp}] {prompt[:80]}\"\ndef get_user_safe(token: str | None) -> dict | None:\n    if not token: return None\n    return {\"id\": \"u-1\", \"name\": \"Alice\"} if token == \"valid-token\" else None\nwith gr.Blocks(theme=theme, title=\"Production demo\", analytics_enabled=False) as demo:\n    user_info = gr.State(value=None)\n    with gr.Row():\n        gr.Markdown(\"# AI Studio\")\n        login_status = gr.Markdown(\"Not logged in\")\n    with gr.Tabs():\n        with gr.Tab(\"Generate\"):\n            prompt = gr.Textbox(label=\"Prompt\", lines=3)\n            with gr.Row():\n                temp = gr.Slider(0.0, 1.5, value=0.7, label=\"Temperature\")\n                go_btn = gr.Button(\"Generate\", variant=\"primary\")\n            output = gr.Textbox(label=\"Output\", interactive=False, lines=5)\n            go_btn.click(\n                fn=generate, inputs=[prompt, temp], outputs=output,\n                concurrency_limit=4,                   # max 4 concurrent gen calls\n                api_name=\"generate\",                    # exposed at /api/generate\n            )\n        with gr.Tab(\"History\"):\n            history = gr.Dataframe(headers=[\"timestamp\", \"prompt\", \"output\"], label=\"Recent\")\n            refresh_btn = gr.Button(\"Refresh\")\n            refresh_btn.click(fn=lambda: [], inputs=None, outputs=history)\n    demo.queue(default_concurrency_limit=10, max_size=50)\napp = FastAPI()\n@app.get(\"/\")\nasync def root():\n    return RedirectResponse(url=\"/gradio\")\n@app.get(\"/api/health\")\nasync def health():\n    return {\"ok\": True}\n# Mount the Gradio app at /gradio.\napp = gr.mount_gradio_app(app, demo, path=\"/gradio\")\n# Run with uvicorn:\n#   $ uvicorn app:app --host 0.0.0.0 --port 7860\n# Now you have:\n#   /                  -> redirects to /gradio\n#   /gradio            -> Gradio UI\n#   /gradio/api/...    -> Gradio's auto-generated REST API\n#   /api/health        -> custom FastAPI route\n# Decision rule:\n#   simple wrap a fn                       -> gr.Interface\n#   multi-tab / complex layout              -> gr.Blocks\n#   chatbot                                  -> gr.ChatInterface\n#   chained events                           -> .click(...).then(...).then(...)\n#   conditional UI                           -> gr.update(visible=...) on event\n#   per-session state                        -> gr.State (browser-scoped)\n#   queue / concurrency control              -> demo.queue + concurrency_limit per event\n#   shared auth / custom routes              -> mount in FastAPI via mount_gradio_app\n#   public free hosting                      -> Hugging Face Spaces\n#   programmatic access                       -> api_name=\"...\" + gradio_client.Client\n#   custom theme                              -> gr.themes.Soft/Default with overrides\n#   streaming LLM                             -> generator function in .click() + ChatInterface\n#   websocket / long-poll                     -> gr.queue handles it; don't roll your own\n#\n# Anti-pattern: storing per-session state in module-level globals.\n# Multiple users share the same Python process; one user's state\n# leaks into another's view. Use gr.State (browser-scoped) for\n# per-session data."
                  }
        ],
        tips: [
                  "`gr.Blocks` lets you express any layout. Tabs, rows, columns, accordions — all components nest naturally inside the `with gr.Blocks():` context.",
                  "Events return `Dependency` objects. Chain with `.then(fn, inputs, outputs)` — chained event runs AFTER the previous one finishes, with the previous output as input.",
                  "`gr.State(value=...)` is per-browser-session storage. Use for cross-tab data, undo history, conversation context.",
                  "`gr.update(visible=False, value=..., interactive=...)` lets one event modify multiple props of a target component. Returned by an event function; Gradio applies the update.",
                  "`mount_gradio_app(app, demo, path=\"/gradio\")` embeds Gradio inside FastAPI. Lets you share auth, add custom routes, deploy as one app.",
                  "For ML demos, `concurrency_limit` per event is the per-endpoint queue. Set it to your GPU's parallelism (e.g., batch_size for batched inference)."
        ],
        mistake: "Storing per-session state in module-level globals. Multiple users share the same Python process; one user's `LAST_PROMPT = \"...\"` leaks into another's view (cross-user data leak). Use `gr.State(value=...)` — browser-session scoped, no leakage.",
        shorthand: {
          verbose: "# Module global — shared across users\nLAST_PROMPT = \"\"\ndef go(p):\n    global LAST_PROMPT\n    LAST_PROMPT = p\n    return f\"Got: {p}\"",
          concise: "# gr.State — per-session, no leak\nlast = gr.State(\"\")\ndef go(p, prev): return f\"Got: {p} (was: {prev})\", p\nbtn.click(go, [prompt, last], [out, last])",
        },
      },
    ],
  },

  // ── Section 4: Deployment & Auth — multipage, sticky sessions, behind nginx ─────────────────────────────────────────
  {
    id: "deployment-auth",
    title: "Deployment & Auth — multipage, sticky sessions, behind nginx",
    entries: [
      {
        id: "streamlit-multipage-auth",
        fn: "Streamlit multipage + auth — pages/ folder, role-based access",
        desc: "Multi-page Streamlit apps via the `pages/` directory; auth via streamlit-authenticator OR a reverse proxy; per-page RBAC. The pattern that takes Streamlit from prototype to internal tool.",
        category: "Deployment",
        subtitle: "pages/ directory auto-discovery, st.switch_page, streamlit-authenticator, reverse-proxy auth (oauth2-proxy), role-based access, query params for deep links",
        signature: "pages/01_dashboard.py    # auto-registered\npages/02_admin.py        # auto-registered\nst.switch_page(\"pages/admin.py\")  # programmatic nav",
        descLong: "Streamlit auto-discovers `.py` files in `pages/` and renders them as separate pages with sidebar nav. Auth has two paths: (a) `streamlit-authenticator` for self-contained login (hashed creds in YAML, basic but works); (b) a reverse proxy (oauth2-proxy, Cloudflare Access, AWS ALB OIDC) that authenticates BEFORE traffic reaches Streamlit — Streamlit reads `X-Forwarded-User` headers. The reverse-proxy approach is the only sane path for SSO. Per-page RBAC: check the user's role at the top of each page, `st.stop()` if denied. The three examples solve the SAME concrete task — multi-page app with login, admin-only pages, deep links — at three depths: pages/ + streamlit-authenticator → reverse-proxy auth + RBAC + query-param deep links → production with OIDC, programmatic page nav, session-bound state.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Streamlit multipage + auth — pages/ folder, role-based access — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# Project structure:\n#   app.py\n#   pages/01_dashboard.py\n#   pages/02_users.py\nimport streamlit as st\nst.title(\"Sales Hub\")\nif \"authenticated\" not in st.session_state:\n    st.session_state[\"authenticated\"] = False\nif not st.session_state[\"authenticated\"]:\n    pw = st.text_input(\"Password\", type=\"password\")\n    if st.button(\"Login\"):\n        if pw == \"secret\":                             # IN PRODUCTION: hash + compare\n            st.session_state[\"authenticated\"] = True\n            st.rerun()\n    st.stop()\nst.success(\"Welcome! Use the sidebar to navigate.\")\n# pages/01_dashboard.py:\n#   import streamlit as st\n#   if not st.session_state.get(\"authenticated\"): st.error(\"Login first\"); st.stop()\n#   st.title(\"Dashboard\")\n# pages/02_users.py:\n#   ... (similar guard)\n# Run:\n#   $ streamlit run app.py\n# Sidebar shows: app, dashboard, users."
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Streamlit multipage + auth — pages/ folder, role-based access — common patterns you'll see in production.\n# APPROACH  - Combine Streamlit multipage + auth — pages/ folder, role-based access with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             role-based access; deep link via query params.\n# pip install streamlit-authenticator pyyaml\nimport streamlit as st\nimport streamlit_authenticator as stauth\nimport yaml\nfrom yaml.loader import SafeLoader\n# credentials:\n#   usernames:\n#     alice:\n#       email: alice@example.com\n#       name: Alice Anderson\n#       password: $2b$12$...  # bcrypt hash; generate via stauth.Hasher\n#       roles: [user, admin]\n#     bob:\n#       email: bob@example.com\n#       name: Bob Brown\n#       password: $2b$12$...\n#       roles: [user]\n# cookie:\n#   name: streamlit_auth\n#   key: random-secret-from-env\n#   expiry_days: 30\nwith open(\"credentials.yaml\") as f:\n    cfg = yaml.load(f, Loader=SafeLoader)\nauthenticator = stauth.Authenticate(\n    credentials=cfg[\"credentials\"],\n    cookie_name=cfg[\"cookie\"][\"name\"],\n    cookie_key=cfg[\"cookie\"][\"key\"],\n    cookie_expiry_days=cfg[\"cookie\"][\"expiry_days\"],\n)\nst.title(\"Sales Hub\")\nauthenticator.login(location=\"sidebar\")\nif st.session_state.get(\"authentication_status\") is None:\n    st.warning(\"Please log in\")\n    st.stop()\nelif st.session_state[\"authentication_status\"] is False:\n    st.error(\"Username/password incorrect\")\n    st.stop()\n# Logged in.\nauthenticator.logout(location=\"sidebar\")\nuser_roles = cfg[\"credentials\"][\"usernames\"][st.session_state[\"username\"]][\"roles\"]\nst.session_state[\"roles\"] = user_roles\nst.success(f\"Welcome, {st.session_state['name']}\")\n# import streamlit as st\n# if \"admin\" not in st.session_state.get(\"roles\", []):\n#     st.error(\"Admins only\")\n#     st.stop()\n# st.title(\"Admin\")\n# # ... admin content\nqp = st.query_params\ndefault_user_id = qp.get(\"user_id\")\nif default_user_id:\n    st.write(f\"Selected user: {default_user_id}\")\n    # Auto-load that user's data...\n# Generate a hash for a new user:\n# >>> stauth.Hasher.hash(\"hunter2\")\n# '$2b$12$...'"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Streamlit multipage + auth — pages/ folder, role-based access — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             page navigation, RBAC, session-bound state with backend\n#             persistence.\n# Architecture:\n#   User -> oauth2-proxy (Google/Okta OIDC) -> Streamlit\n#   oauth2-proxy adds X-Forwarded-User, X-Forwarded-Email, X-Forwarded-Groups\nimport streamlit as st\ndef get_current_user() -> dict | None:\n    \"\"\"Read auth context from reverse-proxy headers.\n    Streamlit doesn't expose request headers directly; use st.context.headers (1.36+).\"\"\"\n    headers = st.context.headers if hasattr(st, \"context\") else {}\n    user = headers.get(\"X-Forwarded-User\") or headers.get(\"Cf-Access-Authenticated-User-Email\")\n    if not user:\n        return None\n    groups = (headers.get(\"X-Forwarded-Groups\") or \"\").split(\",\")\n    return {\"email\": user, \"groups\": [g.strip() for g in groups if g.strip()]}\ndef require_role(*roles: str):\n    user = get_current_user()\n    if user is None:\n        st.error(\"Not authenticated. Refresh to log in.\")\n        st.stop()\n    if not any(r in user[\"groups\"] for r in roles):\n        st.error(f\"Permission denied. Required: {' or '.join(roles)}\")\n        st.stop()\n    return user\ndef require_login():\n    user = get_current_user()\n    if user is None:\n        st.error(\"Not authenticated.\")\n        st.stop()\n    return user\ndef home():\n    user = require_login()\n    st.title(\"Sales Hub\")\n    st.write(f\"Logged in as **{user['email']}**\")\n    st.write(f\"Groups: {', '.join(user['groups'])}\")\n    col1, col2, col3 = st.columns(3)\n    with col1:\n        if st.button(\"Dashboard\", use_container_width=True):\n            st.switch_page(\"pages/01_dashboard.py\")\n    with col2:\n        if \"viewer\" in user[\"groups\"] or \"admin\" in user[\"groups\"]:\n            if st.button(\"Reports\", use_container_width=True):\n                st.switch_page(\"pages/02_reports.py\")\n    with col3:\n        if \"admin\" in user[\"groups\"]:\n            if st.button(\"Admin\", use_container_width=True):\n                st.switch_page(\"pages/03_admin.py\")\nhome()\n# import streamlit as st\n# from auth import require_role\n# user = require_role(\"admin\")\n# st.title(\"Admin\")\n#\n# # Hide pages from sidebar based on role:\n# # In .streamlit/config.toml:\n# # [client]\n# # showSidebarNavigation = false       # hide auto sidebar; render manually\n#\n# # Then in app.py, render only the pages the user can access.\n# # config.cfg\n# provider = \"oidc\"\n# oidc_issuer_url = \"https://accounts.google.com\"\n# client_id = \"...\"\n# client_secret = \"...\"\n# email_domains = [\"example.com\"]\n# upstreams = [\"http://streamlit:8501/\"]\n# pass_authorization_header = true\n# set_xauthrequest = true\n# set_authorization_header = true\n# # Forward groups from OIDC claim to Streamlit:\n# scope = \"openid email profile groups\"\n# pass_user_headers = true\n# Decision rule:\n#   small internal tool, 1-5 users        -> streamlit-authenticator (YAML creds)\n#   SSO required (Google, Okta, AAD)     -> reverse-proxy OIDC (oauth2-proxy / ALB)\n#   public-facing                          -> NOT Streamlit; build a real app\n#   per-page access control                -> require_role() at top of each page\n#   programmatic navigation                -> st.switch_page(\"pages/X.py\")\n#   query-param deep linking               -> st.query_params (read + write)\n#   hide pages from sidebar                -> showSidebarNavigation=false; manual nav\n#   session state across pages             -> st.session_state (per-tab); DB for cross-tab\n#   mobile-friendly                          -> Streamlit's default layout; mobile-OK\n#   audit log                               -> log get_current_user() at sensitive ops\n#   logout                                   -> reverse-proxy /oauth2/sign_out endpoint\n#   role changes mid-session                 -> re-read from headers each pageview;\n#                                                no caching of group membership\n#\n# Anti-pattern: trusting a user-controlled cookie / query param for\n# auth state. \"?role=admin\" can't be trusted; X-Forwarded-User from a\n# DIRECT request can't be trusted (anyone can set it). Only trust\n# reverse-proxy headers when Streamlit is bound to a non-public\n# interface (loopback or private subnet) AND the proxy is the only\n# ingress. Otherwise users can curl --header \"X-Forwarded-User: admin\"\n# and bypass the proxy."
                  }
        ],
        tips: [
                  "Streamlit auto-discovers files in `pages/` (3.0+). Number prefix (`01_`, `02_`) controls sidebar order; the prefix is hidden from display.",
                  "For SSO, use a reverse proxy (oauth2-proxy, Cloudflare Access, AWS ALB OIDC). It authenticates BEFORE Streamlit; passes user info via `X-Forwarded-User` headers.",
                  "`st.context.headers` (Streamlit 1.36+) gives you request headers — required for reading auth headers from the proxy.",
                  "CRITICAL: bind Streamlit to a non-public interface (loopback or private subnet) when relying on proxy auth. A public Streamlit + proxy-only-set headers = trivial bypass via `curl --header`.",
                  "`st.switch_page(\"pages/X.py\")` is programmatic page navigation — useful after a successful action (\"created order; switching to Order Detail page\").",
                  "For per-page RBAC, write a `require_role(\"admin\")` helper that calls `st.stop()` on denial. Put it at the top of every protected page."
        ],
        mistake: "Trusting a user-controlled cookie or query param for auth state. `?role=admin` is set by the user; `X-Forwarded-User` from a DIRECT request is too. Only trust proxy headers when Streamlit is bound to a non-public interface AND the proxy is the only ingress. Otherwise `curl -H \"X-Forwarded-User: admin\"` bypasses everything.",
        shorthand: {
          verbose: "// Manual / verbose approach\n# Streamlit on 0.0.0.0:8501 — anyone can spoof headers\n$ streamlit run app.py --server.address 0.0.0.0\n// More explicit but longer",
          concise: "# Streamlit on 127.0.0.1; proxy is the only path in\n$ streamlit run app.py --server.address 127.0.0.1\n# nginx/oauth2-proxy in front, public traffic comes through it only",
        },
      },
      {
        id: "data-apps-deployment",
        fn: "Deployment — Streamlit / Dash / Gradio behind nginx, sticky sessions",
        desc: "Streamlit is per-session stateful → sticky sessions required. Dash is mostly stateless. Gradio queues need WebSocket. The deployment matrix that determines whether your data app survives traffic.",
        category: "Deployment",
        subtitle: "streamlit run + websockets, gunicorn for Dash, nginx reverse proxy, sticky sessions / session affinity, k8s sessionAffinity, HF Spaces, performance limits",
        signature: "streamlit run app.py --server.port 8501 --server.address 127.0.0.1\n# nginx: proxy_pass + WebSocket upgrade headers",
        descLong: "Each framework has different deployment characteristics: **Streamlit** is per-session stateful — `st.session_state` lives in the server process, so all of one user's requests must hit the SAME replica (sticky sessions). **Dash** is mostly stateless (state in `dcc.Store` browser-side or callback args) — scales horizontally by default. **Gradio** uses WebSockets for the queue — needs WebSocket-aware proxy. The three examples solve the SAME concrete task — deploy a Streamlit app behind nginx, scale to multiple replicas, survive restarts — at three depths: single instance behind nginx → sticky sessions via nginx `ip_hash` → k8s with `sessionAffinity: ClientIP`, deployment knobs, when to leave Streamlit.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Deployment — Streamlit / Dash / Gradio behind nginx, sticky sessions — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# Dockerfile (cross-references containerization sheet):\n# FROM python:3.12-slim\n# WORKDIR /app\n# COPY requirements.txt .\n# RUN pip install --no-cache-dir -r requirements.txt\n# COPY . .\n# EXPOSE 8501\n# CMD [\"streamlit\", \"run\", \"app.py\", \\\n#      \"--server.port\", \"8501\", \\\n#      \"--server.address\", \"0.0.0.0\", \\\n#      \"--server.headless\", \"true\"]\n# server {\n#     listen 80;\n#     server_name myapp.example.com;\n#\n#     location / {\n#         proxy_pass http://streamlit:8501;\n#         proxy_set_header Host $host;\n#         proxy_set_header X-Real-IP $remote_addr;\n#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;\n#         proxy_set_header X-Forwarded-Proto $scheme;\n#\n#         # WebSocket upgrade (REQUIRED for Streamlit; the UI uses WS).\n#         proxy_http_version 1.1;\n#         proxy_set_header Upgrade $http_upgrade;\n#         proxy_set_header Connection \"upgrade\";\n#\n#         # Long-lived WebSocket connections.\n#         proxy_read_timeout 86400;\n#         proxy_send_timeout 86400;\n#     }\n# }\n# Same for Dash (port 8050) and Gradio (port 7860).\n# Common gotcha: forgetting WebSocket upgrade headers. Streamlit\n# loads but every interaction freezes — the UI uses WebSockets for\n# state sync, falls back to long polling badly without proper proxy."
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Deployment — Streamlit / Dash / Gradio behind nginx, sticky sessions — common patterns you'll see in production.\n# APPROACH  - Combine Deployment — Streamlit / Dash / Gradio behind nginx, sticky sessions with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n# Streamlit: each replica has its own st.session_state; without\n# sticky sessions, request 1 hits replica A, request 2 hits replica B,\n# B has no state, app appears to \"reset\" mid-interaction.\n# upstream streamlit_backend {\n#     ip_hash;                          # all requests from same IP -> same replica\n#     server streamlit-1:8501;\n#     server streamlit-2:8501;\n#     server streamlit-3:8501;\n# }\n#\n# server {\n#     listen 80;\n#     server_name myapp.example.com;\n#\n#     location / {\n#         proxy_pass http://streamlit_backend;\n#         proxy_set_header Host $host;\n#         proxy_set_header X-Real-IP $remote_addr;\n#         proxy_http_version 1.1;\n#         proxy_set_header Upgrade $http_upgrade;\n#         proxy_set_header Connection \"upgrade\";\n#         proxy_read_timeout 86400;\n#     }\n# }\n# Better: use 'sticky cookie' (nginx Plus, or HAProxy). IP-based hashing\n# breaks behind corp proxies (everyone shares an IP).\n# In nginx Plus:\n# upstream streamlit_backend {\n#     sticky cookie srv_id expires=1h domain=.example.com path=/;\n#     server streamlit-1:8501;\n#     ...\n# }\n# Dash callbacks receive state via Inputs/State. No per-replica state\n# unless you mutate globals (which you shouldn't). Just round-robin:\n# upstream dash_backend {\n#     server dash-1:8050;\n#     server dash-2:8050;\n#     server dash-3:8050;\n# }\n# (no ip_hash)\n# Gradio's queue is per-process; sticky sessions needed:\n# upstream gradio_backend {\n#     ip_hash;\n#     server gradio-1:7860;\n#     server gradio-2:7860;\n# }"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Deployment — Streamlit / Dash / Gradio behind nginx, sticky sessions — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             health probes, autoscaling, when Streamlit hits its limits.\n# apiVersion: apps/v1\n# kind: Deployment\n# metadata: { name: streamlit, namespace: tools }\n# spec:\n#   replicas: 3\n#   selector:\n#     matchLabels: { app: streamlit }\n#   template:\n#     metadata: { labels: { app: streamlit } }\n#     spec:\n#       containers:\n#         - name: streamlit\n#           image: ghcr.io/myorg/myapp:1.4.2\n#           ports: [{ containerPort: 8501 }]\n#           env:\n#             - name: STREAMLIT_SERVER_PORT\n#               value: \"8501\"\n#             - name: STREAMLIT_SERVER_ADDRESS\n#               value: \"0.0.0.0\"\n#             - name: STREAMLIT_SERVER_ENABLE_CORS\n#               value: \"false\"\n#             - name: STREAMLIT_BROWSER_GATHER_USAGE_STATS\n#               value: \"false\"\n#           livenessProbe:\n#             httpGet: { path: /_stcore/health, port: 8501 }\n#             periodSeconds: 30\n#             timeoutSeconds: 5\n#           readinessProbe:\n#             httpGet: { path: /_stcore/health, port: 8501 }\n#             periodSeconds: 5\n#             failureThreshold: 1\n#           resources:\n#             requests: { cpu: 200m, memory: 512Mi }\n#             limits:   { cpu: 1000m, memory: 1Gi }\n# ---\n# # Service with sessionAffinity for sticky sessions\n# apiVersion: v1\n# kind: Service\n# metadata: { name: streamlit, namespace: tools }\n# spec:\n#   type: ClusterIP\n#   selector: { app: streamlit }\n#   ports:\n#     - { port: 80, targetPort: 8501, protocol: TCP }\n#   sessionAffinity: ClientIP\n#   sessionAffinityConfig:\n#     clientIP:\n#       timeoutSeconds: 10800              # 3 hours\n# apiVersion: networking.k8s.io/v1\n# kind: Ingress\n# metadata:\n#   name: streamlit\n#   namespace: tools\n#   annotations:\n#     nginx.ingress.kubernetes.io/proxy-read-timeout: \"86400\"\n#     nginx.ingress.kubernetes.io/proxy-send-timeout: \"86400\"\n#     nginx.ingress.kubernetes.io/ssl-redirect: \"true\"\n# spec:\n#   tls:\n#     - hosts: [tools.example.com]\n#       secretName: tools-tls\n#   rules:\n#     - host: tools.example.com\n#       http:\n#         paths:\n#           - path: /\n#             pathType: Prefix\n#             backend:\n#               service: { name: streamlit, port: { number: 80 } }\n# Streamlit's per-session state limits autoscaling — scaling DOWN\n# evicts active sessions. Use long pre-stop sleep + careful autoscale.\n# apiVersion: autoscaling/v2\n# kind: HorizontalPodAutoscaler\n# metadata: { name: streamlit }\n# spec:\n#   scaleTargetRef:\n#     apiVersion: apps/v1\n#     kind: Deployment\n#     name: streamlit\n#   minReplicas: 2\n#   maxReplicas: 10\n#   metrics:\n#     - type: Resource\n#       resource:\n#         name: cpu\n#         target: { type: Utilization, averageUtilization: 70 }\n#   behavior:\n#     scaleDown:\n#       stabilizationWindowSeconds: 600    # 10 min before scaling down\n#       policies:\n#         - type: Pods\n#           value: 1\n#           periodSeconds: 300\n# Decision rule:\n#   Streamlit                              -> sessionAffinity: ClientIP; can't load-balance freely\n#   Dash                                    -> stateless; round-robin works\n#   Gradio                                   -> sessionAffinity: ClientIP for queue\n#   need WebSockets through proxy           -> proxy_set_header Upgrade/Connection\n#   long-lived connections                   -> proxy_read_timeout 86400 (1 day)\n#   autoscaling                              -> Streamlit hostile to scale-down; long stabilization\n#   shared session state across replicas    -> NOT Streamlit; shared backend (Redis/DB)\n#   public app with thousands of users      -> consider real frontend; Streamlit ~100s of concurrent\n#   many ephemeral sessions                  -> add slow scale-down to preserve active users\n#   GPU-bound (Gradio)                        -> request gpu in resources; small replica count\n#   per-environment configs                   -> envFrom: configMapRef + secretRef\n#   logging                                   -> stdout JSON; same as containerization sheet\n#   tracing                                   -> install OTel SDK; auto-instrument FastAPI if mounted\n#   Streamlit Cloud                           -> free tier; managed; limited (1GB RAM, no GPU)\n#   Hugging Face Spaces                       -> free Gradio hosting; GPU paid\n#   Hard scale ceiling                         -> Streamlit ~100 concurrent / replica;\n#                                                 above that, switch to a real frontend\n#\n# Anti-pattern: deploying Streamlit at scale (1000+ concurrent users)\n# without considering its single-process stateful model. Streamlit was\n# designed for \"internal tool, 10-50 users\" — not as a public app\n# framework. At scale it becomes flaky: per-session state in process\n# memory, sticky sessions complicate everything, autoscale-down drops\n# user sessions, websocket connections accumulate. If you need that\n# scale, build a real frontend (React/Svelte) backed by FastAPI."
                  }
        ],
        tips: [
                  "Streamlit's `st.session_state` lives in the server process — sticky sessions are MANDATORY for multi-replica deployments. nginx `ip_hash` or k8s `sessionAffinity: ClientIP`.",
                  "WebSocket upgrade headers (`Upgrade`, `Connection`) are required for Streamlit AND Gradio. Without them, the UI loads but interactions freeze.",
                  "Streamlit's health endpoint is `/_stcore/health` (Streamlit 1.27+). Use it for k8s liveness/readiness probes.",
                  "Dash is mostly stateless — round-robin works fine. State lives in `dcc.Store` (browser) or callback args. Don't introduce sticky sessions unless your callbacks mutate globals (anti-pattern).",
                  "Streamlit + autoscaling is hostile — scaling down evicts active sessions. Use long `stabilizationWindowSeconds` (10+ min) and prefer scale-up-only.",
                  "For >100 concurrent Streamlit users per replica, performance degrades. That's the rough ceiling per process; horizontal scaling helps but adds sticky-session complexity. At thousands of users, build a real frontend."
        ],
        mistake: "Deploying Streamlit at scale (1000+ concurrent users) without recognizing its model. Streamlit was designed for \"internal tool, 10-50 users\" — not as a public app framework. At scale: per-session state in process memory, sticky sessions complicate everything, autoscale-down drops user sessions, websocket connections accumulate. For that scale, build a real frontend (React/Svelte) backed by FastAPI.",
        shorthand: {
          verbose: "// Manual / verbose approach\n# Streamlit + 100s of concurrent users + autoscaling\n# = sessions getting dropped, sticky sessions imbalance, ws leaks\n// More explicit but longer",
          concise: "# Real public scale: Streamlit is wrong tool\n# Build React/Svelte + FastAPI; keep Streamlit for internal tools",
        },
      },
    ],
  },
]

export default { meta, sections }

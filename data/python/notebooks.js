export const meta = {
  "id": "notebooks",
  "label": "Notebooks",
  "icon": "📒",
  "description": "Jupyter / IPython workflows that survive past prototyping: magic commands, ipywidgets and rich display, kernel management, papermill for parameterized runs, jupytext for git-friendly diffs, nbconvert for export, CI-tested notebooks, JupyterHub deployment, and the patterns that turn ad-hoc notebooks into reproducible pipelines."
}

export const sections = [

  // ── Section 1: Jupyter basics — magics, display, kernels ─────────────────────────────────────────
  {
    id: "jupyter-basics",
    title: "Jupyter basics — magics, display, kernels",
    entries: [
      {
        id: "jupyter-magic-commands",
        fn: "IPython magic commands — %time, %autoreload, %%writefile",
        desc: "Magic commands extend Python with REPL-friendly verbs: time/profile a cell, edit-and-reload modules, write a cell to disk. The \"make notebooks not annoying\" toolkit.",
        category: "Jupyter Basics",
        subtitle: "%time / %timeit, %run, %load, %%writefile, %autoreload, %matplotlib inline, %env, !shell, %%capture",
        signature: "%timeit slow_function()   # times multiple runs\n%load_ext autoreload\n%autoreload 2",
        descLong: "Magic commands are the IPython kernel's way of adding REPL verbs that aren't valid Python. `%` prefix = line magic; `%%` prefix = cell magic. Most consequential ones: `%timeit` (better than `time.time()` benchmarking), `%autoreload 2` (edit your module, re-run the cell, see changes — no kernel restart), `%%writefile` (save a cell to a file), `!cmd` (run shell commands inline). The three examples solve the SAME concrete task — work efficiently with a module you're editing during development — at three depths: basic `%time` → autoreload + cell magic for inline scripts → production patterns for reproducible notebooks (capture, env, version pinning).",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of IPython magic commands — %time, %autoreload, %%writefile — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# %time runs once, %timeit averages many runs (better signal).\n%time slow_function(big_input)\n# CPU times: user 1.23 s, sys: 23 ms, total: 1.25 s; Wall time: 1.26 s\n%timeit fast_function(big_input)\n# 12.5 µs ± 234 ns per loop (mean ± std. dev. of 7 runs, 100,000 loops each)\n# Cell magics use %%:\n%%timeit\ntotal = 0\nfor i in range(1000):\n    total += i**2\n# Common line magics:\n%pwd                                                   # current dir\n%ls                                                    # list files\n%cd ..                                                 # change dir (persists)\n%env DATABASE_URL=postgresql://...                    # set env var\n# Shell commands inline with !:\n!ls -la\n!pip install pandas\nresult = !ls *.csv                                     # capture into a Python list\n# View source / docs:\n?pd.DataFrame                                          # signature + docstring\n??pd.DataFrame                                         # full source if available"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of IPython magic commands — %time, %autoreload, %%writefile — common patterns you'll see in production.\n# APPROACH  - Combine IPython magic commands — %time, %autoreload, %%writefile with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             without restarting the kernel; capture cell output.\n# %autoreload: notebook code that imports your module gets the\n# latest changes without restarting the kernel.\n%load_ext autoreload\n%autoreload 2\n# Now editing myproj/utils.py and re-running the cell that imports\n# from it picks up the changes. Saves dozens of kernel restarts/day.\n# %%capture suppresses cell output OR captures it.\n%%capture out\nimport pandas as pd\nprint(\"loading data...\")\ndf = pd.read_csv(\"big.csv\")\n# Outside the cell:\nout.stdout                                             # 'loading data...\\n'\nout.stderr\n# %%writefile saves a cell to disk — useful for prototyping a script\n# in a notebook then exporting.\n%%writefile worker.py\nimport sys\ndef main():\n    print(sys.argv)\nif __name__ == \"__main__\":\n    main()\n# Now: !python worker.py arg1\n# %run: execute a script in the notebook's namespace.\n%run setup.py                                          # variables defined there are now in scope\n# %matplotlib inline: render plots inside the notebook.\n%matplotlib inline\nimport matplotlib.pyplot as plt\nplt.plot([1, 2, 3])\n# Other backends:\n# %matplotlib widget   - interactive zoom/pan (needs ipympl)\n# %matplotlib notebook - older interactive backend\n# %matplotlib qt       - separate window\n# %matplotlib agg      - no display (CI)\n# %config InlineBackend.figure_format = \"retina\"        # high-DPI plots"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of IPython magic commands — %time, %autoreload, %%writefile — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             enables autoreload, configures display, captures env.\n# Place this as the FIRST cell in every analysis notebook.\n%load_ext autoreload\n%autoreload 2\n# Display configuration.\n%matplotlib inline\n%config InlineBackend.figure_format = \"retina\"\nimport matplotlib.pyplot as plt\nplt.rcParams[\"figure.figsize\"] = (10, 6)\nplt.rcParams[\"figure.dpi\"] = 100\nimport pandas as pd\npd.set_option(\"display.max_columns\", 50)\npd.set_option(\"display.max_rows\", 100)\npd.set_option(\"display.float_format\", \"{:,.2f}\".format)\nimport warnings\nwarnings.filterwarnings(\"default\", category=DeprecationWarning)\nimport sys, platform, importlib.metadata as ilm\ndef _versions(*pkgs):\n    return {p: ilm.version(p) for p in pkgs}\nprint(f\"Python {sys.version.split()[0]} on {platform.platform()}\")\nfor k, v in _versions(\"pandas\", \"numpy\", \"matplotlib\", \"scikit-learn\").items():\n    print(f\"  {k}: {v}\")\nimport random, numpy as np\nSEED = 42\nrandom.seed(SEED)\nnp.random.seed(SEED)\n# torch.manual_seed(SEED) if using PyTorch\nfrom pathlib import Path\nPROJECT_ROOT = Path.cwd()\nwhile not (PROJECT_ROOT / \"pyproject.toml\").exists() and PROJECT_ROOT != PROJECT_ROOT.parent:\n    PROJECT_ROOT = PROJECT_ROOT.parent\nif str(PROJECT_ROOT) not in sys.path:\n    sys.path.insert(0, str(PROJECT_ROOT))\n# Decision rule:\n#   benchmark a function                 -> %timeit (multiple runs)\n#   one-off timing                         -> %time\n#   profile (line-by-line)                -> %lprun (line_profiler ext)\n#   memory profile                         -> %memit (memory_profiler ext)\n#   editing-and-rerunning a module        -> %autoreload 2\n#   suppress / capture output              -> %%capture\n#   shell command                           -> !cmd\n#   capture shell output to Python         -> result = !cmd\n#   set env var                             -> %env KEY=value\n#   render plots inline                     -> %matplotlib inline\n#   interactive plots                       -> %matplotlib widget (needs ipympl)\n#   debug a cell                             -> %debug after a traceback\n#   run external script                      -> %run script.py\n#   save cell to file                        -> %%writefile path.py\n#   reproducibility on share                 -> version-print header above\n#\n# Anti-pattern: starting %autoreload 2 in production notebooks. Reload\n# is slow and can cause weird state (objects with stale class\n# definitions). Fine for development; remove from notebooks intended\n# to be run end-to-end (papermill, scheduled)."
                  }
        ],
        tips: [
                  "`%autoreload 2` saves dozens of kernel restarts a day during module development. Add to a `~/.ipython/profile_default/ipython_config.py` to enable by default.",
                  "`%timeit` runs a cell multiple times and reports mean ± stddev — far better signal than `time.time()` deltas.",
                  "`!cmd` runs a shell command; `result = !cmd` captures its output as a Python list — useful for piping shell into Python loops.",
                  "For high-DPI displays, `%config InlineBackend.figure_format = \"retina\"` produces sharp matplotlib output. Default is \"png\" which looks blurry on Retina/4K screens.",
                  "Pin a `SEED` and call `random.seed(SEED) + np.random.seed(SEED)` early; reproducibility for free.",
                  "`%%writefile worker.py` is the fastest way to prototype a script — write the code in a cell, run it inline, then export when stable."
        ],
        mistake: "`%autoreload 2` in a notebook intended for unattended execution (papermill, scheduled). Reload adds startup cost and can produce stale-class-definition bugs that look mysterious. Use it during development; remove for end-to-end runs.",
        shorthand: {
          verbose: "# Production notebook with autoreload — slow + flaky\n%load_ext autoreload\n%autoreload 2\n# ... papermill executes this; reload triggers per cell ...",
          concise: "# Dev: enable autoreload. Production: don't.\nimport os\nif os.environ.get(\"NOTEBOOK_MODE\") != \"production\":\n    %load_ext autoreload\n    %autoreload 2",
        },
      },
      {
        id: "ipython-display-widgets",
        fn: "IPython.display & ipywidgets — rich output and interactivity",
        desc: "Render Markdown, HTML, images, audio, video inline; bind sliders, dropdowns, and buttons to functions via `ipywidgets`. The interactivity that makes notebooks feel alive.",
        category: "Jupyter Basics",
        subtitle: "IPython.display.display, Markdown, HTML, Image, Audio, Video, ipywidgets.interact, observe, layout, link / dlink",
        signature: "from ipywidgets import interact\n@interact(x=(0, 100))\ndef plot(x): return x**2",
        descLong: "`IPython.display` lets a notebook show more than just text — Markdown, formatted HTML, images, audio, video, even iframes. `ipywidgets` adds interactive controls: sliders, dropdowns, buttons, color pickers — bind them to functions so the output updates as the widget changes. The three examples solve the SAME concrete task — interactive parameter exploration of a function — at three depths: `display(Markdown(...))` + image → `@interact` decorator → fully composed widget layout with linked controls and observation callbacks.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of IPython.display & ipywidgets — rich output and interactivity — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\nfrom IPython.display import display, Markdown, HTML, Image, Audio, Video, IFrame\ndisplay(Markdown(\"# Section header\\n\\n**Bold** + [link](https://example.com)\"))\ndisplay(HTML(\"<div style='color:red; font-size:24px;'>Important</div>\"))\ndisplay(Image(\"logo.png\", width=200))                  # local file\ndisplay(Image(url=\"https://example.com/cat.jpg\"))      # remote\ndisplay(Image(data=png_bytes, format=\"png\"))           # bytes\ndisplay(Audio(\"clip.wav\"))                             # plays inline\ndisplay(Video(\"clip.mp4\", width=400, embed=True))\n# Embed an external page:\ndisplay(IFrame(\"https://example.com\", width=600, height=400))\n# Auto-display happens for the LAST expression of a cell:\ndf.head()                                              # equivalent to display(df.head())\n# Display a pandas DataFrame as HTML table with formatting:\ndf.style.format({\"price\": \"${:,.2f}\"}).background_gradient(subset=[\"price\"])"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of IPython.display & ipywidgets — rich output and interactivity — common patterns you'll see in production.\n# APPROACH  - Combine IPython.display & ipywidgets — rich output and interactivity with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n# pip install ipywidgets\nfrom ipywidgets import interact, interactive, fixed, IntSlider, FloatSlider, Dropdown\nimport matplotlib.pyplot as plt\nimport numpy as np\n@interact(\n    frequency=FloatSlider(min=0.1, max=5.0, step=0.1, value=1.0),\n    amplitude=FloatSlider(min=0.1, max=5.0, step=0.1, value=1.0),\n    waveform=Dropdown(options=[\"sin\", \"cos\", \"square\"], value=\"sin\"),\n)\ndef plot_wave(frequency: float, amplitude: float, waveform: str):\n    x = np.linspace(0, 4 * np.pi, 500)\n    if waveform == \"sin\":   y = amplitude * np.sin(frequency * x)\n    elif waveform == \"cos\": y = amplitude * np.cos(frequency * x)\n    else:                    y = amplitude * np.sign(np.sin(frequency * x))\n    plt.figure(figsize=(10, 4))\n    plt.plot(x, y)\n    plt.ylim(-6, 6)\n    plt.grid(True)\n    plt.show()\n# Argument shortcuts (interact infers widget type):\n#   int       -> IntSlider with default range\n#   float     -> FloatSlider\n#   str       -> Text input\n#   bool      -> Checkbox\n#   list      -> Dropdown\n#   tuple of 2 (min, max)         -> IntSlider/FloatSlider\n#   tuple of 3 (min, max, step)    -> with step\n@interact(n=10, factor=2.5, label=\"result\", show=True)\ndef compute(n: int, factor: float, label: str, show: bool):\n    if show: print(f\"{label}: {n * factor}\")\n# Use 'interactive' (returns the widget) to embed in a layout:\nw = interactive(plot_wave,\n                frequency=(0.1, 5.0, 0.1),\n                amplitude=(0.1, 5.0, 0.1),\n                waveform=[\"sin\", \"cos\", \"square\"])\ndisplay(w)"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of IPython.display & ipywidgets — rich output and interactivity — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             controls, observe callbacks, output area.\nimport ipywidgets as W\nfrom IPython.display import display, clear_output\nimport matplotlib.pyplot as plt\nimport numpy as np\nfreq = W.FloatSlider(value=1.0, min=0.1, max=5.0, step=0.1, description=\"Freq:\")\namp = W.FloatSlider(value=1.0, min=0.1, max=5.0, step=0.1, description=\"Amp:\")\nwave = W.Dropdown(options=[\"sin\", \"cos\", \"square\"], description=\"Wave:\")\npoints = W.IntSlider(value=500, min=50, max=2000, step=50, description=\"N pts:\")\nsync_amp_freq = W.Checkbox(description=\"Sync amp & freq\")\n# Output area for the plot.\nout = W.Output()\ndef render(_change=None):\n    with out:\n        clear_output(wait=True)\n        x = np.linspace(0, 4 * np.pi, points.value)\n        f = freq.value\n        a = amp.value\n        if wave.value == \"sin\":   y = a * np.sin(f * x)\n        elif wave.value == \"cos\": y = a * np.cos(f * x)\n        else:                      y = a * np.sign(np.sin(f * x))\n        plt.figure(figsize=(10, 4))\n        plt.plot(x, y)\n        plt.title(f\"{wave.value} @ f={f:.1f}, a={a:.1f}\")\n        plt.ylim(-6, 6)\n        plt.grid(True)\n        plt.show()\nfor w in (freq, amp, wave, points):\n    w.observe(render, names=\"value\")\namp_freq_link = None\ndef toggle_sync(change):\n    global amp_freq_link\n    if change[\"new\"]:\n        amp_freq_link = W.link((amp, \"value\"), (freq, \"value\"))\n    elif amp_freq_link:\n        amp_freq_link.unlink()\n        amp_freq_link = None\nsync_amp_freq.observe(toggle_sync, names=\"value\")\ncontrols = W.VBox([\n    W.HBox([freq, amp]),\n    W.HBox([wave, points]),\n    sync_amp_freq,\n])\napp = W.HBox([controls, out])\ndisplay(app)\nrender()                                               # initial render\n# Decision rule:\n#   one or two widget args                 -> @interact decorator\n#   precise control / layout                -> ipywidgets explicit + observe\n#   need the widget value, not just side-effect -> 'interactive' (returns widget object)\n#   linked widgets (one updates another)    -> W.link or W.dlink\n#   output area separated from widgets     -> W.Output context manager + clear_output\n#   want to share notebook AS an app       -> Voila (renders notebook as web app)\n#   plot updates                            -> wrap in Output; clear_output(wait=True)\n#   complex UI                               -> probably outgrown notebook; build Streamlit/Dash\n#\n# Anti-pattern: calling print() or rendering plots directly in an\n# observe callback without an Output area. Each render appends below\n# the cell; the notebook becomes infinite. Use W.Output as a target\n# and clear_output(wait=True) to render-in-place."
                  }
        ],
        tips: [
                  "`@interact` is fastest for one or two args. Reach for explicit `ipywidgets` when you need layout, linked widgets, or non-trivial callbacks.",
                  "`W.Output()` + `with out: clear_output(wait=True); plot()` is the right pattern for re-rendering plots in place — without it the cell scrolls forever.",
                  "`W.link((source, \"value\"), (target, \"value\"))` makes two widgets stay in sync. `W.dlink` for one-way (source → target).",
                  "For simple choices, pass arg shortcuts to `@interact`: `(0, 100)` → IntSlider, `(0.0, 1.0, 0.01)` → FloatSlider with step, `[\"a\", \"b\"]` → Dropdown.",
                  "`Voila` (`pip install voila; voila notebook.ipynb`) renders a notebook as a web app — strips code cells, shows widgets and outputs only.",
                  "For complex UIs (multiple tabs, auth, deployment), graduate to Streamlit or Dash — `ipywidgets` is for in-notebook interactivity."
        ],
        mistake: "Calling `plt.show()` directly in an observe callback. Each call appends a new figure below the cell; the notebook scrolls forever and the original plot stays. Wrap rendering in `W.Output()` and call `clear_output(wait=True)` first to render-in-place.",
        shorthand: {
          verbose: "// Manual / verbose approach\n# Each slider tick appends a new plot\nslider.observe(lambda c: plt.plot(...))\n// More explicit but longer",
          concise: "# Output area + clear_output → plot replaces, doesn't append\nout = W.Output()\ndef render(c):\n    with out: clear_output(wait=True); plt.plot(...)\nslider.observe(render, names=\"value\")",
        },
      },
      {
        id: "jupyter-kernel-management",
        fn: "Kernels & environments — per-project Python, JupyterLab vs notebook",
        desc: "Each project gets its own kernel pointing at its own venv. Multiple kernels in one Jupyter; switch via kernelspec. JupyterLab is the modern UI; notebook (nbclassic) and Lab differ in features and extensions.",
        category: "Jupyter Basics",
        subtitle: "ipykernel install, jupyter kernelspec list, project-specific venvs, JupyterLab vs notebook (classic), nb_conda_kernels for conda, .jupyter/jupyter_server_config.py",
        signature: "python -m ipykernel install --user --name=myproject --display-name=\"Python (myproject)\"",
        descLong: "A Jupyter kernel is a Python process started by the notebook server. By default a single kernel uses the system Python — broken once you have more than one project. The discipline: each project gets its own venv AND its own registered kernel (`python -m ipykernel install --user --name=myproject`). Switch kernels via the notebook UI or `Kernel → Change kernel`. JupyterLab is the active UI (lab >= 4.0); the old `notebook` package is now nbclassic. The three examples solve the SAME concrete task — set up a project-specific kernel, work in JupyterLab — at three depths: install ipykernel into a venv → kernelspec management + display names → production with kernelspec.json customization, env vars, resource limits.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Kernels & environments — per-project Python, JupyterLab vs notebook — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# Each project: own venv, own kernel.\n# In your project venv:\n#   $ python -m venv .venv\n#   $ source .venv/bin/activate\n#   $ pip install ipykernel pandas numpy\n#   $ python -m ipykernel install --user --name=myproject --display-name=\"Python (myproject)\"\n# Now in JupyterLab:\n#   Kernel menu → Change kernel → \"Python (myproject)\"\n# List installed kernels:\n#   $ jupyter kernelspec list\n#   Available kernels:\n#     python3        /usr/local/share/jupyter/kernels/python3      (system)\n#     myproject      /Users/me/.local/share/jupyter/kernels/myproject\n# Remove a kernel:\n#   $ jupyter kernelspec remove myproject\n# Why per-project:\n#   - One project needs pandas 1.5; another needs 2.x. Same kernel breaks one.\n#   - Conflicting deps (numpy 1 vs numpy 2 ABI) crash imports.\n#   - Reproducibility: the kernel maps to the project's pinned env.\n# JupyterLab vs notebook:\n#   $ pip install jupyterlab        # modern UI; recommended\n#   $ jupyter lab\n#   $ pip install notebook           # classic UI (nbclassic in lab>=4)\n#   $ jupyter notebook"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Kernels & environments — per-project Python, JupyterLab vs notebook — common patterns you'll see in production.\n# APPROACH  - Combine Kernels & environments — per-project Python, JupyterLab vs notebook with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n# After 'python -m ipykernel install', edit:\n#   ~/.local/share/jupyter/kernels/myproject/kernel.json\n# {\n#  \"argv\": [\n#    \"/path/to/project/.venv/bin/python\",\n#    \"-m\", \"ipykernel_launcher\",\n#    \"-f\", \"{connection_file}\"\n#  ],\n#  \"display_name\": \"Python (myproject)\",\n#  \"language\": \"python\",\n#  \"env\": {\n#    \"PYTHONPATH\": \"/path/to/project/src\",\n#    \"DATABASE_URL\": \"postgresql://...\"\n#  },\n#  \"metadata\": {\n#    \"debugger\": true\n#  }\n# }\n# pip install nb_conda_kernels\n# Now Jupyter shows ALL conda envs as kernels automatically.\n# .envrc in project root:\n#   export VIRTUAL_ENV=\"$(pwd)/.venv\"\n#   PATH_add \"$VIRTUAL_ENV/bin\"\n# Then: $ direnv allow\n# Now cd into the project automatically activates the venv.\n# Always install your project in editable mode in its venv:\n#   $ pip install -e .\n# This makes your package importable AND pickable up edits with autoreload.\n# Restart kernel:        Kernel → Restart (or 0 0)\n# Restart + run all:     Kernel → Restart and Run All\n# Interrupt:              Kernel → Interrupt (or i i)\n# Reset namespace:        %reset -f\n# Check what kernel a notebook expects:\nimport json\nwith open(\"notebook.ipynb\") as f:\n    nb = json.load(f)\nprint(nb[\"metadata\"][\"kernelspec\"])\n# {\"name\": \"myproject\", \"display_name\": \"Python (myproject)\", \"language\": \"python\"}"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Kernels & environments — per-project Python, JupyterLab vs notebook — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             resource-limited via JupyterLab Pro / KubeSpawner.\n# Each user gets their own kernel; admin manages shared images.\n# Install JupyterHub:\n#   $ pip install jupyterhub\n#   $ npm install -g configurable-http-proxy\n#   $ jupyterhub --generate-config\n# jupyterhub_config.py:\n# c.JupyterHub.spawner_class = \"dockerspawner.DockerSpawner\"\n# c.DockerSpawner.image = \"jupyter/datascience-notebook:python-3.12\"\n# c.DockerSpawner.network_name = \"jupyterhub-net\"\n# c.DockerSpawner.notebook_dir = \"/home/jovyan/work\"\n# c.DockerSpawner.volumes = {\"jupyterhub-user-{username}\": \"/home/jovyan/work\"}\n# c.DockerSpawner.mem_limit = \"4G\"\n# c.DockerSpawner.cpu_limit = 2\n# c.JupyterHub.spawner_class = \"kubespawner.KubeSpawner\"\n# c.KubeSpawner.image = \"myorg/jupyter-base:1.4.2\"\n# c.KubeSpawner.cpu_limit = 2\n# c.KubeSpawner.mem_limit = \"4G\"\n# c.KubeSpawner.profile_list = [\n#     {\"display_name\": \"Standard\", \"default\": True,\n#      \"kubespawner_override\": {\"image\": \"myorg/jupyter-base:1.4.2\"}},\n#     {\"display_name\": \"GPU (T4)\",\n#      \"kubespawner_override\": {\"image\": \"myorg/jupyter-gpu:1.4.2\",\n#                               \"extra_resource_limits\": {\"nvidia.com/gpu\": 1}}},\n# ]\n# c.JupyterHub.authenticator_class = \"oauthenticator.GitHubOAuthenticator\"\n# c.GitHubOAuthenticator.client_id = \"...\"\n# c.GitHubOAuthenticator.client_secret = \"...\"\n# Or LDAP, OIDC, dummy (dev only).\n# Decision rule:\n#   one user, one machine                 -> jupyter lab + venv + ipykernel\n#   per-project envs                        -> ipykernel install --name=PROJECT (always)\n#   conda user                              -> nb_conda_kernels for auto-discovery\n#   team of N users                         -> JupyterHub\n#   team needs GPU                          -> KubeSpawner with GPU profile\n#   need notebooks behind auth              -> JupyterHub + OAuthenticator\n#   need to scale to 100s of users          -> JupyterHub on k8s; per-pod limits\n#   notebook breaks across kernels          -> kernel.json env section; pin python path\n#   want JupyterLab, not classic            -> pip install jupyterlab; jupyter lab\n#   want classic                             -> pip install notebook; jupyter notebook\n#   want both                                -> jupyter-server hosts both\n#   notebook in CI                           -> nbval / pytest-notebook (later entry)\n#   shareable read-only                       -> nbconvert to HTML; OR Voila\n#\n# Anti-pattern: installing project deps into the system Python or the\n# default 'python3' kernel. First time you have two projects with\n# conflicting versions of pandas, both notebooks break. Always: venv\n# per project, kernel per venv, and notebooks pin the kernel name in\n# their metadata."
                  }
        ],
        tips: [
                  "Always install `ipykernel` per-venv: `python -m ipykernel install --user --name=PROJECT`. Never share kernels across projects.",
                  "`jupyter kernelspec list` shows all installed kernels and their python paths — first stop when \"wrong package version\" mysteries appear.",
                  "For shared env vars, edit `kernel.json`'s `env` field — the kernel inherits these on launch. Useful for `DATABASE_URL`, `PYTHONPATH`, etc.",
                  "JupyterLab (>= 4.0) is the modern UI; `jupyter notebook` (now nbclassic) lives on for muscle-memory users. Both share the kernel infrastructure.",
                  "For team deployments, JupyterHub + KubeSpawner gives each user a pod with their own kernel + resource limits. GPU profiles for ML teams.",
                  "Use `nb_conda_kernels` if your team uses conda — it auto-discovers conda envs as kernels (no manual install per env)."
        ],
        mistake: "Installing project dependencies into the system `python3` kernel. First time you have two projects with conflicting `pandas` versions, both notebooks break — and the fix (uninstall + reinstall) breaks the OTHER project. Always: venv per project, kernel per venv, kernel name in notebook metadata.",
        shorthand: {
          verbose: "# All projects share the system kernel — version conflicts inevitable\n$ pip install pandas numpy\n$ jupyter notebook",
          concise: "# Per-project venv + kernel\n$ python -m venv .venv && source .venv/bin/activate\n$ pip install -e . ipykernel\n$ python -m ipykernel install --user --name=myproject",
        },
      },
    ],
  },

  // ── Section 2: Workflow — papermill, jupytext, nbconvert ─────────────────────────────────────────
  {
    id: "workflow",
    title: "Workflow — papermill, jupytext, nbconvert",
    entries: [
      {
        id: "nbformat-papermill",
        fn: "papermill — parameterized notebook execution",
        desc: "Run a notebook with injected parameters from the command line; capture outputs; suitable for scheduled reports and CI. The \"treat a notebook as a data pipeline\" tool.",
        category: "Workflow",
        subtitle: "papermill execute, parameters tag, -p key=value, output notebook with results, scrapbook for outputs, integration with Airflow / Dagster",
        signature: "papermill report.ipynb out_2024-12.ipynb -p month 2024-12 -p threshold 0.5",
        descLong: "A notebook with a \"parameters\" cell tagged in nbformat metadata becomes a parameterizable pipeline. `papermill execute notebook.ipynb out.ipynb -p key value` injects parameters, runs the notebook, captures outputs into a result notebook. Good for scheduled reports (run weekly with last week's date), parameter sweeps (one notebook, N parameter sets), CI-tested analyses (run in CI; verify outputs). The three examples solve the SAME concrete task — generate a monthly sales report from a parameterized notebook — at three depths: tag a parameters cell + papermill CLI → programmatic execution + scrapbook for structured outputs → production with Airflow scheduling, S3 storage, error handling.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of papermill — parameterized notebook execution — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# In the cell containing default parameters, add the \"parameters\" tag\n# via JupyterLab: View → Cell Toolbar → Tags → add \"parameters\".\n#\n# The cell itself looks like:\n#   month = \"2024-11\"\n#   threshold = 0.5\n#   region = \"us\"\n# pip install papermill\n# $ papermill input.ipynb output_2024-12.ipynb \\\n#     -p month \"2024-12\" \\\n#     -p threshold 0.7 \\\n#     -p region \"eu\"\n#\n# papermill:\n#   1. Loads input.ipynb\n#   2. Injects a NEW cell after the \"parameters\" cell with the\n#      override values (month = \"2024-12\", threshold = 0.7, ...)\n#   3. Executes every cell top-to-bottom\n#   4. Saves the executed notebook (with all outputs) to output_*.ipynb\n#\n# If any cell raises, papermill stops and writes the partial output\n# notebook with the error visible.\nimport papermill as pm\npm.execute_notebook(\n    \"report.ipynb\",\n    \"report_executed.ipynb\",\n    parameters={\"month\": \"2024-12\", \"threshold\": 0.7, \"region\": \"eu\"},\n)\n# parameters dict:\n#   - keys must match variable names in the parameters cell\n#   - values become Python literals via repr() — strings, numbers, lists, dicts OK"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of papermill — parameterized notebook execution — common patterns you'll see in production.\n# APPROACH  - Combine papermill — parameterized notebook execution with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             cell outputs) for downstream pipelines.\n# pip install papermill scrapbook\nimport papermill as pm\nimport scrapbook as sb\n# Cell:\n#   import scrapbook as sb\n#   total_revenue = df[\"amount\"].sum()\n#   sb.glue(\"total_revenue\", float(total_revenue))\n#   sb.glue(\"by_region\", df.groupby(\"region\")[\"amount\"].sum().to_dict())\n#\n#   # Glue a plot:\n#   import matplotlib.pyplot as plt\n#   fig, ax = plt.subplots()\n#   ax.bar(by_region.keys(), by_region.values())\n#   sb.glue(\"revenue_chart\", fig, encoder=\"display\")\npm.execute_notebook(\n    \"report.ipynb\", \"report_2024-12.ipynb\",\n    parameters={\"month\": \"2024-12\"},\n)\nnb = sb.read_notebook(\"report_2024-12.ipynb\")\nrevenue = nb.scraps[\"total_revenue\"].data                # 1234567.89\nby_region = nb.scraps[\"by_region\"].data                  # {\"us\": 800000, ...}\nprint(f\"Total: ${revenue:,.0f}\")\nimport pathlib\nruns = sb.read_notebooks(pathlib.Path(\"./reports\"))\ndf = runs.scraps_report()\nprint(df)                                                 # one row per (notebook, scrap) combo\n# 1. Parameter sweep: run the same notebook with N parameter sets.\nmonths = [\"2024-09\", \"2024-10\", \"2024-11\", \"2024-12\"]\nfor m in months:\n    pm.execute_notebook(\n        \"report.ipynb\", f\"out/report_{m}.ipynb\",\n        parameters={\"month\": m},\n    )\n# 2. Error handling — papermill raises if a cell fails.\ntry:\n    pm.execute_notebook(\"report.ipynb\", \"out.ipynb\", parameters={\"bad\": \"input\"})\nexcept pm.PapermillExecutionError as e:\n    print(f\"failed at cell {e.exec_count}: {e.evalue}\")\n    # The output notebook still exists with the error visible."
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of papermill — parameterized notebook execution — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             alerts on failure.\n# pip install papermill scrapbook boto3\nimport papermill as pm\nimport scrapbook as sb\nfrom datetime import datetime, timedelta\nfrom pathlib import Path\nimport boto3, json\n# from airflow import DAG\n# from airflow.operators.python import PythonOperator\n#\n# dag = DAG(\"monthly_report\", schedule_interval=\"0 8 1 * *\",\n#           default_args={\"retries\": 2})\n#\n# def run_report(execution_date, **_ctx):\n#     month = execution_date.strftime(\"%Y-%m\")\n#     ...\ndef run_monthly_report(month: str, *, output_bucket: str = \"reports\") -> dict:\n    \"\"\"Run notebook for given month; ship to S3; return summary scraps.\"\"\"\n    out_local = Path(f\"/tmp/report_{month}.ipynb\")\n    try:\n        pm.execute_notebook(\n            \"/opt/notebooks/report.ipynb\",\n            str(out_local),\n            parameters={\"month\": month, \"production_mode\": True},\n            kernel_name=\"myproject\",\n            log_output=True,                          # stream cell outputs to stdout\n            progress_bar=False,\n        )\n    except pm.PapermillExecutionError as e:\n        # Write FAILED status; ship anyway for debugging.\n        s3 = boto3.client(\"s3\")\n        s3.upload_file(str(out_local), output_bucket,\n                       f\"failed/{month}/report.ipynb\")\n        raise\n    # Read scraps for downstream consumers.\n    nb = sb.read_notebook(str(out_local))\n    summary = {\n        k: v.data for k, v in nb.scraps.items()\n        if v.data_type in (\"application/json\", \"text/plain\")\n    }\n    # Ship the executed notebook to S3 for archival.\n    s3 = boto3.client(\"s3\")\n    s3.upload_file(str(out_local), output_bucket, f\"{month}/report.ipynb\")\n    s3.put_object(Bucket=output_bucket, Key=f\"{month}/summary.json\",\n                   Body=json.dumps(summary).encode())\n    return summary\n# subprocess.run([\n#     \"jupyter\", \"nbconvert\", \"--to\", \"html\", \"--no-input\",\n#     str(out_local), \"--output\", f\"report_{month}.html\",\n# ])\n# Decision rule:\n#   ad-hoc parameter run                  -> papermill CLI\n#   programmatic / pipeline                -> papermill Python API\n#   need structured outputs                 -> scrapbook glue + read\n#   parameter sweep                          -> for-loop over execute_notebook\n#   scheduled report                          -> Airflow / Dagster + papermill\n#   notebook is the deliverable               -> nbconvert to HTML; ship\n#   error tolerance                            -> wrap in try/except PapermillExecutionError\n#   stream cell output to logs                -> log_output=True\n#   need to test outputs                       -> nbval (next entry); OR scrapbook + assertions\n#   long-running notebook                     -> --execution-timeout=N (seconds)\n#   uses GPU                                   -> kernel that points to GPU env\n#   stop on first cell failure                 -> default behavior; raises PapermillExecutionError\n#\n# Anti-pattern: hardcoding values in a notebook intended to be run\n# scheduled. Each time the report runs you have to manually edit the\n# date cell. Tag a 'parameters' cell with sane defaults, then use\n# papermill -p to override at runtime. Same notebook, infinite parameters."
                  }
        ],
        tips: [
                  "Tag a \"parameters\" cell via JupyterLab: View → Cell Toolbar → Tags → add `parameters`. Papermill injects overrides AFTER this cell.",
                  "Use sane defaults in the parameters cell — same notebook should work both interactively (run as-is) and via papermill (with overrides).",
                  "`scrapbook.glue(\"name\", value)` captures structured outputs. Read across many runs with `scrapbook.read_notebooks(directory)` — returns a DataFrame.",
                  "`papermill.execute_notebook(..., log_output=True)` streams cell stdout/stderr to your logs — critical for debugging long-running scheduled jobs.",
                  "When a cell fails, papermill writes the OUTPUT notebook anyway with the error visible. Always upload it to S3/etc. — invaluable for debugging scheduled failures.",
                  "For Airflow, the `PapermillOperator` from `airflow-providers-papermill` wraps execute_notebook with proper task semantics."
        ],
        mistake: "Hardcoding values in a notebook that runs on a schedule. Every run requires manually editing the date cell; the schedule fails when nobody remembers; reproducing a past month's report requires version-control archaeology. Tag a `parameters` cell and use papermill -p; same notebook, infinite parameter sets.",
        shorthand: {
          verbose: "# Hardcoded date — requires manual edit per run\nmonth = \"2024-11\"\ndf = load_data(month)",
          concise: "# Tagged parameters cell — papermill -p month 2024-12 overrides\n# (cell with tag \"parameters\")\nmonth = \"2024-11\"        # default\nthreshold = 0.5\ndf = load_data(month)",
        },
      },
      {
        id: "jupytext-version-control",
        fn: "jupytext — pair .ipynb with .py for clean git diffs",
        desc: "Keep `notebook.ipynb` paired with `notebook.py`; both stay in sync; commit only the `.py` for sane diffs. Combined with `nbstripout`, your git history stays readable.",
        category: "Workflow",
        subtitle: "jupytext --set-formats ipynb,py:percent, .py percent format, --sync, nbstripout, .gitattributes filter, pre-commit hooks",
        signature: "jupytext --set-formats \"ipynb,py:percent\" notebook.ipynb",
        descLong: "Jupyter notebooks are JSON with embedded outputs and execution counts; `git diff` is unreadable, merge conflicts are nightmares, and PR reviews are slow. Two complementary fixes: (a) **jupytext** pairs each `.ipynb` with a `.py` (or `.md`) representation; both stay in sync; commit only the `.py`. (b) **nbstripout** as a git filter strips outputs from `.ipynb` before commit. The three examples solve the SAME concrete task — make notebook commits readable in PR reviews — at three depths: nbstripout for output-stripping → jupytext pairing with percent format → pre-commit hooks + .gitattributes for the team-wide setup.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of jupytext — pair .ipynb with .py for clean git diffs — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# pip install nbstripout\n# Install as a git filter for this repo:\n#   $ nbstripout --install\n# Now every `git add notebook.ipynb` strips outputs before staging.\n# Outputs still display in your local Jupyter; they're just not in git.\n# Or as a one-shot:\n#   $ nbstripout notebook.ipynb         # in-place\n#   $ nbstripout < notebook.ipynb > clean.ipynb\n# What gets stripped:\n#   - outputs (cell results, plots, error tracebacks)\n#   - execution counts (the [123] next to each cell)\n#   - metadata.kernelspec (optional; usually keep it)\n#\n# git diff now shows only the SOURCE changes, not output noise.\n# Caveat:\n#   nbstripout doesn't help with reordered cells, edited cells inside\n#   long notebooks — diff is still mostly JSON. Junior tier adds\n#   jupytext for line-by-line .py diffs."
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of jupytext — pair .ipynb with .py for clean git diffs — common patterns you'll see in production.\n# APPROACH  - Combine jupytext — pair .ipynb with .py for clean git diffs with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n#             readable line-by-line diffs.\n# pip install jupytext\n# $ jupytext --set-formats \"ipynb,py:percent\" notebook.ipynb\n# This:\n#   - Creates notebook.py alongside notebook.ipynb\n#   - Records the pairing in the .ipynb metadata\n#   - Both files are kept in sync — edit either, save, the other updates\n# Format options:\n#   py:percent  - cells delimited by '# %%' comments; most popular\n#                  Compatible with VS Code, PyCharm cell-mode runners.\n#   py:light     - lighter delimiters; less metadata\n#   md           - markdown with code blocks; good for tutorials\n# Example notebook.py (percent format):\n# ---\n# # %%\n# import pandas as pd\n# df = pd.read_csv(\"data.csv\")\n#\n# # %% [markdown]\n# # ## Section header\n# # Some prose explaining what this does.\n#\n# # %%\n# df.head()\n# ---\n# If you edit the .py file outside Jupyter (text editor, IDE):\n#   $ jupytext --sync notebook.ipynb           # propagates .py changes to .ipynb\n# .gitignore:\n#   notebook.ipynb           # don't commit the JSON\n#   *.ipynb_checkpoints/\n# Pull workflow:\n#   $ git pull               # gets updated .py\n#   $ jupyter lab            # opening the notebook recreates .ipynb from .py\n#   (jupytext_serverextension auto-creates the missing .ipynb on open)\n# Some teams want the .ipynb in git so non-developers can preview on\n# GitHub. Combine with nbstripout to keep the .ipynb minimal.\n# $ jupytext --to py notebook.ipynb            # produces notebook.py\n# $ jupytext --to ipynb notebook.py            # produces notebook.ipynb\n# $ jupytext --to md notebook.ipynb            # produces notebook.md"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of jupytext — pair .ipynb with .py for clean git diffs — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             .gitattributes for git-side filtering, CI check.\n# .pre-commit-config.yaml\n# repos:\n#   - repo: https://github.com/kynan/nbstripout\n#     rev: 0.7.1\n#     hooks:\n#       - id: nbstripout\n#\n#   - repo: https://github.com/mwouts/jupytext\n#     rev: v1.16.4\n#     hooks:\n#       - id: jupytext\n#         args: [--sync]\n#\n# Install once: $ pre-commit install\n# Now every `git commit` runs both hooks; non-jupytext'd notebooks\n# get stripped, paired notebooks get synced.\n# .gitattributes (committed)\n# *.ipynb diff=ipynb merge=ipynb\n#\n# .gitconfig (per-developer; auto-set by 'nbstripout --install --global'):\n# [diff \"ipynb\"]\n#     textconv = jupyter nbconvert --to script --stdout\n# [merge \"ipynb\"]\n#     name = Jupyter notebook merge driver\n#     driver = jupyter nbformat ...\n#\n# Result: `git diff notebook.ipynb` shows the script equivalent;\n# merge tools handle nbformat correctly.\n# .github/workflows/check-notebooks.yml\n# - name: Check jupytext sync\n#   run: |\n#     pip install jupytext\n#     for nb in $(find . -name \"*.ipynb\" -not -path \"*/.ipynb_checkpoints/*\"); do\n#       py=\"${nb%.ipynb}.py\"\n#       if [ -f \"$py\" ]; then\n#         jupytext --diff \"$nb\" \"$py\" || \\\n#           (echo \"ERROR: $nb and $py out of sync; run jupytext --sync\"; exit 1)\n#       fi\n#     done\n# Decision rule:\n#   want clean git history                 -> nbstripout for outputs\n#   want readable PR diffs                  -> jupytext pairing\n#   prefer python file as canonical          -> commit .py only; .gitignore .ipynb\n#   prefer notebook for GitHub preview        -> commit .ipynb (stripped) AND .py\n#   tutorials with prose                       -> jupytext --to md (markdown format)\n#   collaborate with non-developers           -> commit .ipynb + jupytext both\n#   one-shot conversion                        -> jupytext --to py / --to ipynb\n#   pre-commit                                  -> nbstripout + jupytext --sync hooks\n#   merge conflicts                              -> jupytext + git merge driver via .gitattributes\n#   notebook in CI testing                       -> nbval / pytest-notebook (next entry)\n#   need full output for diff (rare)            -> turn off nbstripout for that file via attribute\n#\n# Anti-pattern: committing .ipynb files with full outputs (CSVs\n# inlined as base64, plots as embedded PNGs). Repository size balloons,\n# git diff is JSON noise, PR reviews require manually opening the\n# notebook in Jupyter to see what changed. Either nbstripout (keep\n# .ipynb, strip outputs) or jupytext (commit .py companion)."
                  }
        ],
        tips: [
                  "`nbstripout --install` registers as a git filter for the current repo. Every `git add` of an `.ipynb` strips outputs before staging.",
                  "Use `py:percent` format for jupytext (cells delimited by `# %%`). VS Code, PyCharm, and other editors recognize it natively for cell-mode running.",
                  "For pure-Python projects, commit only `.py`; `.gitignore` the `.ipynb`. Jupyter regenerates the `.ipynb` on open via the jupytext extension.",
                  "For tutorials and docs, `jupytext --to md` gives Markdown output — readable on GitHub, includes prose, code blocks compile back to a notebook.",
                  "Pair with pre-commit so the team's checkout is consistent. New developers `pre-commit install` once and never think about it.",
                  "For merge conflicts in `.ipynb`, the jupytext `.py` companion makes the conflict tractable — resolve in Python, regenerate the notebook."
        ],
        mistake: "Committing `.ipynb` files with full outputs (base64 PNGs, inlined CSVs, error tracebacks). Repository size balloons, `git diff` is unreadable JSON, PR reviews require opening the notebook in Jupyter. Either install `nbstripout` (keep `.ipynb`, strip outputs) or use jupytext pairing (commit `.py` companion).",
        shorthand: {
          verbose: "# Commit notebooks with all outputs — repo bloat\n$ git add analysis.ipynb     # 5 MB of base64 PNG outputs\n$ git diff analysis.ipynb    # unreadable JSON",
          concise: "# nbstripout + jupytext pairing\n$ nbstripout --install\n$ jupytext --set-formats \"ipynb,py:percent\" analysis.ipynb\n$ git diff analysis.py       # readable diff of cells",
        },
      },
      {
        id: "nbconvert-export",
        fn: "nbconvert — export notebooks to HTML, PDF, slides",
        desc: "Convert `.ipynb` to HTML, PDF, Markdown, or RevealJS slides via `jupyter nbconvert`. Voila renders a notebook as a live web app for non-developers.",
        category: "Workflow",
        subtitle: "jupyter nbconvert --to html / pdf / markdown / slides, --no-input, custom templates, Voila for live apps, mkdocs-jupyter for docs",
        signature: "jupyter nbconvert --to html --no-input notebook.ipynb",
        descLong: "`jupyter nbconvert` converts `.ipynb` to many formats — HTML (most common), PDF (via LaTeX), Markdown, RevealJS slides. `--no-input` hides code cells; `--template` customizes output. For LIVE notebooks (interactive widgets) use Voila — renders as a web app. The three examples solve the SAME concrete task — share a notebook with non-developers — at three depths: HTML export → custom template + slide deck → Voila + mkdocs-jupyter for docs.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of nbconvert — export notebooks to HTML, PDF, slides — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# Basic HTML export — includes code + outputs.\n# $ jupyter nbconvert --to html report.ipynb\n#   produces report.html\n# Hide code cells; show only outputs (Markdown, plots, tables):\n# $ jupyter nbconvert --to html --no-input report.ipynb\n# Other formats:\n# $ jupyter nbconvert --to markdown report.ipynb       # report.md\n# $ jupyter nbconvert --to pdf      report.ipynb       # needs xelatex / TeX\n# $ jupyter nbconvert --to slides   report.ipynb       # RevealJS slide deck\n# $ jupyter nbconvert --to script   report.ipynb       # report.py (just the code)\n# Programmatically:\nimport subprocess\nsubprocess.run([\n    \"jupyter\", \"nbconvert\",\n    \"--to\", \"html\", \"--no-input\",\n    \"--output-dir\", \"out\",\n    \"report.ipynb\",\n])\n# Or via Python API:\nfrom nbconvert import HTMLExporter\nimport nbformat\nnb = nbformat.read(\"report.ipynb\", as_version=4)\nexporter = HTMLExporter(template_name=\"lab\", exclude_input=True)\nbody, resources = exporter.from_notebook_node(nb)\nwith open(\"report.html\", \"w\") as f:\n    f.write(body)"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of nbconvert — export notebooks to HTML, PDF, slides — common patterns you'll see in production.\n# APPROACH  - Combine nbconvert — export notebooks to HTML, PDF, slides with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n# Built-in templates:\n#   classic      - default, simple\n#   lab          - JupyterLab styling (modern)\n#   reveal       - reveal.js slides\n#   webpdf       - PDF via headless Chrome (no LaTeX needed)\n#\n# $ jupyter nbconvert --to html --template lab report.ipynb\n# 1. Tag cells with \"Slide Type\" via View → Cell Toolbar → Slideshow:\n#    - Slide        : new slide\n#    - Sub-slide    : sub-slide (down arrow)\n#    - Fragment     : reveal piece-by-piece\n#    - Skip         : exclude\n#    - Notes        : speaker notes\n# 2. Convert:\n#    $ jupyter nbconvert --to slides report.ipynb \\\n#        --SlidesExporter.reveal_theme=simple \\\n#        --SlidesExporter.reveal_scroll=true\n# 3. Open report.slides.html or serve with reveal.js.\n# pip install nbconvert[webpdf] playwright\n# python -m playwright install chromium\n# $ jupyter nbconvert --to webpdf --no-input report.ipynb\n#   Cleaner than LaTeX-PDF for non-mathy reports.\n# $ jupyter nbconvert --to html \\\n#     --template-path=./templates \\\n#     --template my_template \\\n#     report.ipynb\n#\n# templates/my_template/conf.json:\n#   { \"base_template\": \"lab\", \"mimetypes\": { \"text/html\": true } }\n# templates/my_template/index.html.j2:\n#   {%- extends 'lab/index.html.j2' -%}\n#   {%- block body_header -%}\n#   <header>Internal — {{ resources.metadata.name }}</header>\n#   {%- endblock body_header -%}\n# Tag with \"remove_cell\" to drop entirely; \"remove_input\" to hide code;\n# \"remove_output\" to hide output.\n# $ jupyter nbconvert --to html \\\n#     --TagRemovePreprocessor.remove_cell_tags=remove_cell \\\n#     --TagRemovePreprocessor.remove_input_tags=remove_input \\\n#     report.ipynb\n# nbconvert --to html-with-resources writes a directory with assets.\n# nbconvert --to html (default) inlines small assets as base64."
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of nbconvert — export notebooks to HTML, PDF, slides — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             mkdocs-jupyter for docs site.\n# pip install voila\n# $ voila notebook.ipynb\n# Opens browser at http://localhost:8866 — strips code cells, shows\n# only widgets and outputs. Each visitor gets a fresh kernel.\n# Customize:\n# $ voila --template=material report.ipynb\n# $ voila --no-browser --port=8866 --Voila.tornado_settings='{\"trust_xheaders\": True}' \\\n#         --base_url=/voila/ report.ipynb\n# Docker deploy:\n# FROM jupyter/scipy-notebook\n# RUN pip install voila\n# COPY report.ipynb /app/\n# WORKDIR /app\n# EXPOSE 8866\n# CMD [\"voila\", \"--no-browser\", \"--port=8866\", \"--Voila.ip=0.0.0.0\", \"report.ipynb\"]\n# pip install mkdocs mkdocs-material mkdocs-jupyter\n# mkdocs.yml:\n#   nav:\n#     - Home: index.md\n#     - Tutorials:\n#       - notebooks/tutorial1.ipynb\n#       - notebooks/tutorial2.ipynb\n#   plugins:\n#     - mkdocs-jupyter:\n#         execute: false           # use existing outputs (faster)\n#         include_source: true     # include the .ipynb in the site\n#         theme: dark\n# $ mkdocs serve\n# .github/workflows/docs.yml\n# - name: Build site\n#   run: |\n#     pip install mkdocs mkdocs-material mkdocs-jupyter jupytext\n#     mkdocs build\n# - name: Deploy\n#   uses: peaceiris/actions-gh-pages@v3\n#   with:\n#     publish_dir: ./site\n# Decision rule:\n#   one-off share with stakeholders        -> nbconvert --to html --no-input\n#   slide deck                              -> nbconvert --to slides + reveal\n#   PDF for archival                          -> --to webpdf (no LaTeX)\n#   docs site / tutorial                     -> mkdocs-jupyter\n#   live interactive demo                    -> Voila (renders widgets)\n#   email a static report                     -> --to html with embedded assets\n#   ML model card                              -> nbconvert --to html; check into git\n#   company-branded report                    -> custom Jinja template\n#   exclude scratch cells                      -> tag \"remove_cell\"; nbconvert preprocessor\n#   need to schedule + email                   -> papermill + nbconvert + sendgrid\n#   need to publish to confluence/wiki        -> --to markdown; paste/import\n#\n# Anti-pattern: shipping a notebook .ipynb directly to non-developers\n# and asking them to \"open it in Jupyter\". They don't have Jupyter\n# installed; opening on GitHub shows raw outputs but no interactivity;\n# attaching to email leaves stale outputs forever. ALWAYS export to\n# HTML (static reports), PDF (formal docs), or Voila (live) before\n# sharing."
                  }
        ],
        tips: [
                  "`--no-input` hides code cells — turns a development notebook into a clean stakeholder-friendly report.",
                  "Use `--to webpdf` for PDFs (needs `playwright`). Cleaner than `--to pdf` (LaTeX) for non-mathematical content; doesn't require a TeX install.",
                  "Tag cells with `remove_cell`, `remove_input`, or `remove_output` to control export. Useful for hiding scratch cells / debugging while keeping the analysis cell.",
                  "`Voila` renders a notebook as a live web app — strips code, runs widgets. The non-developer-friendly way to share interactive analyses.",
                  "`mkdocs-jupyter` plugin lets you embed notebooks in a docs site. Set `execute: false` to use cached outputs (faster); `execute: true` for always-fresh.",
                  "For corporate-branded reports, build a custom Jinja template extending `lab/index.html.j2` — adds header/footer/CSS to all exports."
        ],
        mistake: "Emailing a notebook `.ipynb` file to non-developers. They don't have Jupyter; GitHub view shows stale outputs without interactivity; the `.ipynb` is huge with embedded base64. Export to HTML (static), PDF (formal), or Voila (live) before sharing.",
        shorthand: {
          verbose: "// Manual / verbose approach\n# Email the .ipynb — recipient can't open it\n$ email -a notebook.ipynb stakeholder@example.com\n// More explicit but longer",
          concise: "# Export first, then share the HTML\n$ jupyter nbconvert --to html --no-input notebook.ipynb\n$ email -a notebook.html stakeholder@example.com",
        },
      },
    ],
  },

  // ── Section 3: Production — CI testing, JupyterHub, anti-patterns ─────────────────────────────────────────
  {
    id: "production",
    title: "Production — CI testing, JupyterHub, anti-patterns",
    entries: [
      {
        id: "notebook-ci-testing",
        fn: "Notebook CI testing — nbval, pytest-notebook, papermill assertions",
        desc: "Run notebooks in CI to catch regressions; assert outputs match expectations; ensure reproducibility. Three approaches: `nbval` validates outputs match, `pytest-notebook` allows pytest-style asserts, papermill + scrapbook for output checking.",
        category: "Production",
        subtitle: "nbval --nbval-lax, pytest-notebook, papermill execute + scrapbook scraps, fixtures via tmp_path, kernel selection in CI",
        signature: "pytest --nbval notebooks/   # validates each cell's outputs match the saved version",
        descLong: "Notebooks rot — a pandas update changes a column name, your data source moves, the model retrains. CI catches these. Three patterns: **nbval** runs each cell and verifies the new output matches the saved output (good for analyses where output should be stable); **pytest-notebook** lets you write pytest tests that assert on notebook outputs; **papermill + scrapbook** runs the notebook and asserts on glued values. The three examples solve the SAME concrete task — verify the monthly report notebook still produces correct outputs — at three depths: nbval validation → papermill + scrapbook + pytest assertions → production with fixture-based test data, multi-version matrix, alert on drift.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Notebook CI testing — nbval, pytest-notebook, papermill assertions — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# pip install pytest nbval\n# Runs every cell of every notebook in the directory; FAILS if a cell\n# raises OR if the output differs from the saved version.\n# Run all notebooks:\n# $ pytest --nbval notebooks/\n# pass = cells run cleanly AND outputs match\n# fail = cell raises OR output differs from what's saved\n# Useful when outputs vary (random seeds, timestamps, IDs).\n# $ pytest --nbval-lax notebooks/\n# Tag a cell with \"nbval-ignore-output\" to skip output comparison.\n# (View → Cell Toolbar → Tags → add nbval-ignore-output)\n# Cell still executes; just doesn't have to match saved output.\n# pytest.ini\n# [pytest]\n# addopts = --nbval-lax\n# testpaths = notebooks/\n# Or run a specific subset:\n# $ pytest --nbval-lax notebooks/critical/ -v"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Notebook CI testing — nbval, pytest-notebook, papermill assertions — common patterns you'll see in production.\n# APPROACH  - Combine Notebook CI testing — nbval, pytest-notebook, papermill assertions with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n# pip install pytest papermill scrapbook\nimport pytest, papermill as pm, scrapbook as sb\nfrom pathlib import Path\ndef test_monthly_report(tmp_path: Path):\n    \"\"\"Run the monthly report; assert key outputs are sane.\"\"\"\n    out = tmp_path / \"result.ipynb\"\n    # Execute with test parameters.\n    pm.execute_notebook(\n        \"notebooks/monthly_report.ipynb\",\n        str(out),\n        parameters={\n            \"month\": \"2024-11\",\n            \"test_mode\": True,             # use a small CSV instead of full DB\n        },\n    )\n    # Read scraps and assert.\n    nb = sb.read_notebook(str(out))\n    revenue = nb.scraps[\"total_revenue\"].data\n    by_region = nb.scraps[\"by_region\"].data\n    assert revenue > 0,                       \"revenue must be positive\"\n    assert revenue < 10_000_000,               \"revenue suspiciously large\"\n    assert \"us\" in by_region,                  \"missing US data\"\n    assert sum(by_region.values()) == pytest.approx(revenue, rel=0.01)\n@pytest.mark.parametrize(\"month\", [\"2024-09\", \"2024-10\", \"2024-11\"])\ndef test_report_for_each_month(tmp_path: Path, month: str):\n    out = tmp_path / f\"out_{month}.ipynb\"\n    pm.execute_notebook(\n        \"notebooks/monthly_report.ipynb\", str(out),\n        parameters={\"month\": month, \"test_mode\": True},\n    )\n    nb = sb.read_notebook(str(out))\n    assert nb.scraps[\"total_revenue\"].data > 0\n# .github/workflows/test-notebooks.yml\n# - run: pytest --nbval-lax notebooks/exploratory/\n# - run: pytest tests/test_reports.py     # papermill-based tests"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Notebook CI testing — nbval, pytest-notebook, papermill assertions — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             multi-version matrix, alert on output drift.\nimport pytest\nimport papermill as pm\nimport scrapbook as sb\nimport pandas as pd\nimport json\nfrom pathlib import Path\n@pytest.fixture(scope=\"session\")\ndef test_data_dir(tmp_path_factory):\n    \"\"\"Set up test fixtures once; reuse across tests.\"\"\"\n    d = tmp_path_factory.mktemp(\"test_data\")\n    pd.DataFrame({\n        \"month\": [\"2024-11\", \"2024-11\", \"2024-11\"],\n        \"region\": [\"us\", \"eu\", \"apac\"],\n        \"amount\": [1000, 800, 500],\n    }).to_csv(d / \"sales.csv\", index=False)\n    return d\n@pytest.fixture(scope=\"session\")\ndef snapshot_dir() -> Path:\n    return Path(\"tests/snapshots\")\ndef test_report_against_snapshot(test_data_dir, tmp_path, snapshot_dir):\n    \"\"\"Run the notebook; compare scraps to saved snapshot.\"\"\"\n    out = tmp_path / \"result.ipynb\"\n    pm.execute_notebook(\n        \"notebooks/monthly_report.ipynb\",\n        str(out),\n        parameters={\"data_dir\": str(test_data_dir), \"month\": \"2024-11\"},\n    )\n    nb = sb.read_notebook(str(out))\n    actual = {k: v.data for k, v in nb.scraps.items()}\n    snapshot_path = snapshot_dir / \"monthly_2024-11.json\"\n    if not snapshot_path.exists():\n        # First run: save the snapshot.\n        snapshot_dir.mkdir(parents=True, exist_ok=True)\n        snapshot_path.write_text(json.dumps(actual, sort_keys=True, indent=2))\n        pytest.skip(\"snapshot created; rerun to compare\")\n    expected = json.loads(snapshot_path.read_text())\n    assert actual == expected, f\"output drift detected; review {snapshot_path}\"\n# To regenerate snapshots after a deliberate change:\n# $ rm tests/snapshots/*.json && pytest tests/test_reports.py\n# .github/workflows/notebooks.yml\n# strategy:\n#   matrix:\n#     python-version: [\"3.11\", \"3.12\"]\n#     pandas-version: [\"1.5\", \"2.2\"]\n# steps:\n#   - uses: actions/setup-python@v5\n#     with:\n#       python-version: ${{ matrix.python-version }}\n#   - run: |\n#       pip install pandas==${{ matrix.pandas-version }} \\\n#                   pytest papermill scrapbook nbval\n#   - run: pytest tests/test_reports.py\ndef test_notebook_runtime(test_data_dir, tmp_path):\n    \"\"\"Notebook should execute within budget.\"\"\"\n    import time\n    started = time.monotonic()\n    pm.execute_notebook(\n        \"notebooks/monthly_report.ipynb\",\n        str(tmp_path / \"out.ipynb\"),\n        parameters={\"data_dir\": str(test_data_dir)},\n    )\n    elapsed = time.monotonic() - started\n    assert elapsed < 60.0, f\"notebook took {elapsed:.1f}s (budget: 60s)\"\n# Decision rule:\n#   exploratory notebook                  -> nbval-lax (only check it runs)\n#   stable analysis with known outputs    -> nbval (full output match)\n#   programmatic outputs                    -> papermill + scrapbook + asserts\n#   snapshot test against baseline          -> snapshot file + diff in CI\n#   multi-version compatibility            -> matrix in CI; varied pandas/python versions\n#   long-running notebook                   -> tag for nightly only; not on every PR\n#   GPU-required                              -> separate job with GPU runner\n#   notebook in deployed pipeline           -> CI is the regression test; PR-gated\n#   parameter sweeps                          -> pytest.mark.parametrize for known params\n#   data drift detection                      -> fixture mocks data; assert on summary stats\n#\n# Anti-pattern: never running notebooks in CI. Notebooks rot silently\n# — pandas changes, data formats shift, libraries deprecate. Six\n# months later you reopen \"the report notebook\" and 40% of cells\n# error. CI catches these the day they break, not when you next look\n# at the notebook."
                  }
        ],
        tips: [
                  "`pytest --nbval-lax notebooks/` is the \"minimum viable\" notebook CI — only checks cells run without errors. Add explicit value asserts for stronger checks.",
                  "Tag volatile cells (timestamps, random seeds, generated IDs) with `nbval-ignore-output` so the full match doesn't break on irrelevant changes.",
                  "For programmatic outputs, papermill + scrapbook is the right tool. Glue values; assert on them in pytest. Better than parsing notebook JSON.",
                  "Snapshot tests (`expected.json`) + JSON diff catch subtle output drift. Regenerate via `rm snapshots/*.json && pytest`.",
                  "For long-running notebooks (>1 min), tag them as `@pytest.mark.slow` and run nightly only. PR builds run the fast subset.",
                  "For multi-environment tests (Python versions, library versions), use GitHub Actions matrix. Catches \"works on my machine\" issues."
        ],
        mistake: "Never running notebooks in CI. Six months later you open \"the monthly report\" and half the cells error — pandas changed, the data source moved, a library was deprecated. CI catches these the day they break. nbval-lax + papermill + scrapbook covers most needs.",
        shorthand: {
          verbose: "// Manual / verbose approach\n# No CI for notebooks\n# Six months later: \"why did the report break?\"\n// More explicit but longer",
          concise: "# CI runs notebooks every PR\n$ pytest --nbval-lax notebooks/         # cells must execute\n$ pytest tests/test_reports.py          # outputs must match expectations",
        },
      },
      {
        id: "jupyterhub-deployment",
        fn: "JupyterHub — multi-user notebook server with auth, resources",
        desc: "JupyterHub spawns a per-user notebook server (process or container or pod). OAuth via OIDC; resource limits per user; GPU profiles. The team-deployment story for Jupyter.",
        category: "Production",
        subtitle: "JupyterHub config, KubeSpawner, OAuth (GitHub/Google/OIDC), resource limits, profile_list, persistent storage, image curation",
        signature: "jupyterhub --config /etc/jupyterhub_config.py",
        descLong: "JupyterHub is the \"many-users-one-Jupyter\" deployment: each user logs in (OAuth), gets their own kernel (in a process, container, or k8s pod), with their own home directory persisted across sessions. Spawners: `LocalProcessSpawner` (one host, multiple users — small teams); `DockerSpawner` (container per user); `KubeSpawner` (pod per user, the standard for any team >5). The three examples solve the SAME concrete task — deploy JupyterHub for a team of 20 — at three depths: LocalProcessSpawner + dummy auth → DockerSpawner + GitHub OAuth → KubeSpawner + GPU profiles + persistent volumes.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of JupyterHub — multi-user notebook server with auth, resources — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# pip install jupyterhub\n# npm install -g configurable-http-proxy\n# jupyterhub --generate-config\n# c = get_config()\n#\n# # Listen on all interfaces.\n# c.JupyterHub.bind_url = \"http://0.0.0.0:8000\"\n#\n# # Use system PAM (Linux user accounts) for auth.\n# c.JupyterHub.authenticator_class = \"pam\"\n# c.PAMAuthenticator.create_system_users = False\n#\n# # Each user gets their own subprocess.\n# c.JupyterHub.spawner_class = \"jupyterhub.spawner.LocalProcessSpawner\"\n#\n# # Default kernel image.\n# c.Spawner.notebook_dir = \"~/notebooks\"\n# c.Spawner.default_url = \"/lab\"                       # open JupyterLab by default\n# Run:\n#   $ sudo jupyterhub\n# Open http://localhost:8000; log in with a Linux account.\n# This works for ~5-20 users on a single beefy server. Beyond that,\n# move to DockerSpawner or KubeSpawner.\n# c.JupyterHub.services = [\n#     {\n#         \"name\": \"idle-culler\",\n#         \"admin\": True,\n#         \"command\": [\n#             \"python\", \"-m\", \"jupyterhub_idle_culler\",\n#             \"--timeout=3600\", \"--cull-every=600\",\n#         ],\n#     },\n# ]"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of JupyterHub — multi-user notebook server with auth, resources — common patterns you'll see in production.\n# APPROACH  - Combine JupyterHub — multi-user notebook server with auth, resources with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n# pip install jupyterhub dockerspawner oauthenticator\n# c = get_config()\n#\n# # GitHub OAuth.\n# c.JupyterHub.authenticator_class = \"oauthenticator.GitHubOAuthenticator\"\n# c.GitHubOAuthenticator.client_id = \"...\"\n# c.GitHubOAuthenticator.client_secret = \"...\"\n# c.GitHubOAuthenticator.oauth_callback_url = \"https://hub.example.com/hub/oauth_callback\"\n# c.GitHubOAuthenticator.allowed_organizations = [\"myorg\"]\n# # Optional team restriction:\n# c.GitHubOAuthenticator.allowed_teams = [\"myorg:data-science\"]\n#\n# # Docker container per user.\n# c.JupyterHub.spawner_class = \"dockerspawner.DockerSpawner\"\n# c.DockerSpawner.image = \"jupyter/datascience-notebook:python-3.12\"\n# c.DockerSpawner.network_name = \"jupyterhub-net\"\n# c.DockerSpawner.notebook_dir = \"/home/jovyan/work\"\n#\n# # Per-user persistent volume (named volume).\n# c.DockerSpawner.volumes = {\n#     \"jupyterhub-user-{username}\": \"/home/jovyan/work\",\n# }\n#\n# # Resource limits.\n# c.DockerSpawner.mem_limit = \"4G\"\n# c.DockerSpawner.cpu_limit = 2\n#\n# # Idle timeout.\n# c.Spawner.timeout = 60                                 # spawn timeout\n# c.Spawner.http_timeout = 120                           # request timeout\n# version: '3'\n# services:\n#   hub:\n#     build: .\n#     image: jupyterhub:custom\n#     container_name: jupyterhub\n#     networks: [jupyterhub-net]\n#     volumes:\n#       - \"/var/run/docker.sock:/var/run/docker.sock:rw\"\n#       - \"./data:/data\"\n#     ports: [\"8000:8000\"]\n#     environment:\n#       - DOCKER_NETWORK_NAME=jupyterhub-net\n# networks:\n#   jupyterhub-net:\n#     name: jupyterhub-net\n# Dockerfile.user\n# FROM jupyter/datascience-notebook:python-3.12\n# USER root\n# RUN apt-get update && apt-get install -y --no-install-recommends git \\\n#     && rm -rf /var/lib/apt/lists/*\n# USER ${NB_UID}\n# COPY --chown=${NB_UID}:${NB_GID} requirements.txt .\n# RUN pip install --no-cache-dir -r requirements.txt"
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of JupyterHub — multi-user notebook server with auth, resources — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n#             persistent volumes, autoscaling.\n# pip install jupyterhub kubespawner oauthenticator\n# c = get_config()\n#\n# # OIDC auth (Okta / Auth0 / Google Workspace).\n# c.JupyterHub.authenticator_class = \"oauthenticator.generic.GenericOAuthenticator\"\n# c.GenericOAuthenticator.client_id = os.environ[\"OIDC_CLIENT_ID\"]\n# c.GenericOAuthenticator.client_secret = os.environ[\"OIDC_CLIENT_SECRET\"]\n# c.GenericOAuthenticator.token_url = \"https://accounts.example.com/oauth2/token\"\n# c.GenericOAuthenticator.userdata_url = \"https://accounts.example.com/oauth2/userinfo\"\n# c.GenericOAuthenticator.username_claim = \"email\"\n# c.GenericOAuthenticator.scope = [\"openid\", \"email\", \"profile\", \"groups\"]\n#\n# # KubeSpawner: each user gets a pod.\n# c.JupyterHub.spawner_class = \"kubespawner.KubeSpawner\"\n# c.KubeSpawner.image = \"myorg/jupyter-base:1.4.2\"\n# c.KubeSpawner.cpu_limit = 4\n# c.KubeSpawner.mem_limit = \"8G\"\n# c.KubeSpawner.cmd = [\"jupyter\", \"labhub\"]\n#\n# # Persistent volume per user (PVC).\n# c.KubeSpawner.pvc_name_template = \"claim-{username}\"\n# c.KubeSpawner.storage_pvc_ensure = True\n# c.KubeSpawner.storage_capacity = \"10Gi\"\n# c.KubeSpawner.volumes = [\n#     {\"name\": \"home\", \"persistentVolumeClaim\": {\"claimName\": \"claim-{username}\"}},\n# ]\n# c.KubeSpawner.volume_mounts = [\n#     {\"name\": \"home\", \"mountPath\": \"/home/jovyan/work\"},\n# ]\n#\n# # Profile picker on spawn — user chooses CPU vs GPU.\n# c.KubeSpawner.profile_list = [\n#     {\n#         \"display_name\": \"Standard (4 CPU, 8 GB)\",\n#         \"default\": True,\n#         \"kubespawner_override\": {\n#             \"image\": \"myorg/jupyter-base:1.4.2\",\n#             \"cpu_limit\": 4,\n#             \"mem_limit\": \"8G\",\n#         },\n#     },\n#     {\n#         \"display_name\": \"Large (16 CPU, 32 GB)\",\n#         \"kubespawner_override\": {\n#             \"image\": \"myorg/jupyter-base:1.4.2\",\n#             \"cpu_limit\": 16,\n#             \"mem_limit\": \"32G\",\n#         },\n#     },\n#     {\n#         \"display_name\": \"GPU T4 (1 GPU, 8 CPU, 32 GB)\",\n#         \"kubespawner_override\": {\n#             \"image\": \"myorg/jupyter-gpu:1.4.2\",\n#             \"cpu_limit\": 8,\n#             \"mem_limit\": \"32G\",\n#             \"extra_resource_limits\": {\"nvidia.com/gpu\": 1},\n#             \"node_selector\": {\"cloud.google.com/gke-accelerator\": \"nvidia-tesla-t4\"},\n#         },\n#     },\n# ]\n#\n# # Idle culling.\n# c.JupyterHub.services = [{\n#     \"name\": \"idle-culler\", \"admin\": True,\n#     \"command\": [\"python\", \"-m\", \"jupyterhub_idle_culler\",\n#                  \"--timeout=7200\", \"--cull-every=600\"],\n# }]\n#\n# # Cluster-autoscale-friendly: configure HPA on JupyterHub deployment;\n# # cluster autoscaler grows nodes for spawned pods.\n# Use the official Zero to JupyterHub on Kubernetes (Z2JH) helm chart;\n# captures all of the above plus more.\n# $ helm repo add jupyterhub https://hub.jupyter.org/helm-chart/\n# $ helm install hub jupyterhub/jupyterhub --version 4.0.0 -f values.yaml\n# values.yaml (excerpt):\n#   hub:\n#     config:\n#       GenericOAuthenticator:\n#         client_id: \"...\"\n#         token_url: \"...\"\n#       JupyterHub:\n#         authenticator_class: generic-oauth\n#   singleuser:\n#     image:\n#       name: myorg/jupyter-base\n#       tag: \"1.4.2\"\n#     storage:\n#       capacity: 10Gi\n#     profileList:\n#       - display_name: \"Standard\"\n#         default: true\n#         kubespawner_override:\n#           cpu_limit: 4\n#           mem_limit: 8G\n# Decision rule:\n#   1-5 users, one machine                 -> LocalProcessSpawner\n#   5-50 users, isolation needed            -> DockerSpawner\n#   any team on k8s                          -> KubeSpawner\n#   GPU access                                -> profile_list with GPU node selector\n#   per-user persistent storage              -> PVC per user (KubeSpawner)\n#   shared datasets                            -> read-only volume mounted on all spawns\n#   need full Z2JH features                   -> use the official Helm chart\n#   simple LDAP                                 -> ldapauthenticator\n#   GitHub-only org                             -> GitHubOAuthenticator\n#   multi-tenant SaaS                          -> NOT JupyterHub; build your own\n#   want managed service                       -> Vertex AI Workbench, SageMaker, Hex, Deepnote\n#   need to scale to 1000+ users               -> z2jh on big k8s; or managed service\n#   read-only shared notebooks                 -> nbgallery; voila\n#\n# Anti-pattern: running JupyterHub with PAM auth on a public-facing\n# server. PAM = system Linux users; everyone you give a notebook to\n# has shell access to the host. Use OAuth (GitHub, OIDC) and Docker/\n# KubeSpawner for proper isolation."
                  }
        ],
        tips: [
                  "For >5 users, ALWAYS use container isolation (DockerSpawner or KubeSpawner). LocalProcessSpawner gives users full host access — fine for solo, dangerous for teams.",
                  "OAuth via OIDC (Okta, Auth0, Google) is the production auth choice. GitHub auth is fine for engineering teams; PAM (Linux users) is dev-only.",
                  "`profile_list` lets users pick resource size at spawn — Standard / Large / GPU. KubeSpawner reads the choice and configures the pod accordingly.",
                  "Persistent per-user storage via PVC (`pvc_name_template = \"claim-{username}\"`). Without it, users lose their files on every container restart.",
                  "Always run idle-culler — kills servers idle for N seconds. Without it, abandoned sessions hold resources forever.",
                  "Use the official Z2JH Helm chart (Zero to JupyterHub) for k8s deployments. Captures all best practices; well-tested by the community."
        ],
        mistake: "Running JupyterHub with PAM auth on a public-facing server. PAM = system Linux users; users you grant notebook access to also have shell on the host. Use OAuth (GitHub/Google/OIDC) and DockerSpawner or KubeSpawner so users are isolated to a container, not the host.",
        shorthand: {
          verbose: "# PAM + LocalProcessSpawner — users have host shell access\nc.JupyterHub.authenticator_class = \"pam\"\nc.JupyterHub.spawner_class = \"LocalProcessSpawner\"",
          concise: "# OAuth + KubeSpawner — proper isolation\nc.JupyterHub.authenticator_class = \"oauthenticator.generic.GenericOAuthenticator\"\nc.JupyterHub.spawner_class = \"kubespawner.KubeSpawner\"\nc.KubeSpawner.profile_list = [{\"display_name\": \"...\", \"kubespawner_override\": {...}}]",
        },
      },
      {
        id: "notebook-anti-patterns",
        fn: "Notebook anti-patterns — when to graduate to .py",
        desc: "Notebooks are great for exploration; bad for production code. The traps: cell-order dependencies, hidden state, no tests, copy-paste reuse. The discipline: refactor reusable code into modules, keep notebooks for analysis + visualization.",
        category: "Production",
        subtitle: "cell-order dependency, hidden mutable state, \"Restart and Run All\" test, refactoring functions into modules, when to leave Jupyter, the \"module + thin notebook\" pattern",
        signature: "rule: if Restart-and-Run-All breaks, the notebook has hidden state",
        descLong: "Notebooks are interactive — you run cells out of order, retry with mutations, leave dead code. That's fine for exploration; it's a disaster when shipping. Three traps: (1) **cell-order dependency** — only works in the order you happened to run them; \"Restart and Run All\" breaks; (2) **hidden mutable state** — earlier cells mutate variables you forgot about; (3) **copy-paste reuse** — the same function exists in 5 notebooks, each with subtle differences. The discipline: extract reusable code into a `src/` module, install it (`pip install -e .`), import in notebooks for analysis. The three examples solve the SAME concrete task — turn a working exploratory notebook into reproducible code — at three depths: identify the smells → extract functions to a module → production with thin notebook + tested module.",
        examples: [
                  {
                            "tier": "intro",
                            "code": "# === ENTRY-LEVEL EXAMPLE ===\n# TASK      - Basic usage of Notebook anti-patterns — when to graduate to .py — understand the core syntax and behavior.\n# APPROACH  - Simple example with minimal parameters; no edge cases.\n# STRENGTHS - Clear, readable; shows the fundamental pattern.\n# WEAKNESSES- Not production-ready; no error handling or complex scenarios.\n# Smell 1: cell-order dependency.\n#   Cell 1: df = pd.read_csv(\"data.csv\")\n#   Cell 2: df[\"clean_amount\"] = df[\"amount\"].str.replace(\"$\", \"\").astype(float)\n#   Cell 3: <... more analysis ...>\n#   Cell 4: df = df[df[\"clean_amount\"] > 100]                # MUTATES df\n#   Cell 5: <re-running cell 2 now FAILS — clean_amount is float, not str>\n# Test: Kernel → Restart and Run All. If that breaks, you have order dependency.\n# Smell 2: hidden state in mutable globals.\n#   Cell 1: df = load_data()\n#   Cell 10 (3 days later): df.dropna(inplace=True)         # changes 'df' globally\n#   Cell 11: <results that look fine but used the dropna'd df>\n# Reader of cell 11 has no idea df was mutated.\n# Smell 3: copy-paste reuse.\n#   notebook_a.ipynb:  def parse_log(line): ...             # version 1\n#   notebook_b.ipynb:  def parse_log(line): ...             # version 1.1, slightly different\n#   notebook_c.ipynb:  def parse_log(line): ...             # version 2\n# Three subtly different implementations; bug fixed in C never reaches A and B.\n# Smell 4: secrets in cells.\n#   Cell 3: API_KEY = \"sk-abc123...\"                         # committed to git\n# nbstripout doesn't help with code cells; only outputs.\n# Use os.environ + .env file (or notebook env via kernelspec).\n# Smell 5: ad-hoc tests in cells.\n#   Cell 7: assert df[\"amount\"].min() > 0                    # passes today\n#   Cell 12 (next week): df gets new rows; assertion fails silently\n#                         (notebook still runs but assertion in middle never fires)"
                  },
                  {
                            "tier": "junior",
                            "code": "# === JUNIOR EXAMPLE ===\n# TASK      - Real-world usage of Notebook anti-patterns — when to graduate to .py — common patterns you'll see in production.\n# APPROACH  - Combine Notebook anti-patterns — when to graduate to .py with related functions; handle common edge cases.\n# STRENGTHS - Practical; covers the 80% use cases encountered on the job.\n# WEAKNESSES- May need optimization for large datasets or complex scenarios.\n# Project layout:\n#   myproj/\n#     pyproject.toml\n#     src/myproj/\n#       __init__.py\n#       data.py          <- functions extracted from notebook\n#       analysis.py\n#     tests/\n#       test_data.py\n#     notebooks/\n#       exploration.ipynb   <- thin; imports from myproj\n#       monthly_report.ipynb\n# import pandas as pd\n# from pathlib import Path\n#\n# def load_sales(path: Path) -> pd.DataFrame:\n#     \"\"\"Read sales CSV; clean amount column.\"\"\"\n#     df = pd.read_csv(path)\n#     df[\"clean_amount\"] = df[\"amount\"].str.replace(\"$\", \"\", regex=False).astype(float)\n#     return df\n#\n# def filter_by_threshold(df: pd.DataFrame, *, min_amount: float) -> pd.DataFrame:\n#     \"\"\"Filter; returns NEW DataFrame (no mutation).\"\"\"\n#     return df[df[\"clean_amount\"] >= min_amount].copy()\n# from myproj.data import load_sales, filter_by_threshold\n#\n# def test_filter_returns_new_frame(tmp_path):\n#     # ... create test CSV ...\n#     df = load_sales(tmp_path / \"test.csv\")\n#     filtered = filter_by_threshold(df, min_amount=100)\n#     assert len(filtered) <= len(df)\n#     assert df.equals(load_sales(tmp_path / \"test.csv\"))    # original unchanged\n# $ pip install -e .\n# Now from any notebook:\n# import pandas as pd\n# from myproj.data import load_sales, filter_by_threshold\n#\n# df = load_sales(\"data/sales.csv\")\n# filtered = filter_by_threshold(df, min_amount=100)\n# filtered.head()\n#\n# # Notebook is now THIN: just imports, calls, and visualization.\n# # Refactor and test happens in src/, not in the notebook.\n# BAD:\n#   df = pd.read_csv(\"data.csv\")\n#   df[\"clean\"] = df[\"amount\"].str.replace(\"$\", \"\")\n#   df = df[df[\"clean\"] > 100]                              # mutate\n#\n# GOOD:\n#   def clean_and_filter(path, *, min_amount):\n#       df = pd.read_csv(path)\n#       df[\"clean\"] = df[\"amount\"].str.replace(\"$\", \"\")\n#       return df[df[\"clean\"] > min_amount]\n#   filtered = clean_and_filter(\"data.csv\", min_amount=100)\n# Functions are testable, importable, and don't mutate globals."
                  },
                  {
                            "tier": "senior",
                            "code": "# === SENIOR EXAMPLE ===\n# TASK      - Advanced usage of Notebook anti-patterns — when to graduate to .py — performance, edge cases, and expert patterns.\n# APPROACH  - Production-grade patterns; optimization; handling complex scenarios.\n# STRENGTHS - Complete; handles edge cases, performance, and maintainability.\n# WEAKNESSES- Complex; requires deep knowledge to understand and maintain.\n# Goal: notebooks are 80% imports + visualization, 20% one-off exploration.\n# All reusable code lives in src/ with tests.\n# notebooks/sales_report.ipynb:\n#\n# # %% [markdown]\n# # # Sales Report — Q4 2024\n# # *Generated: <timestamp>*\n#\n# # %%\n# # parameters cell (papermill-tagged)\n# month = \"2024-12\"\n# threshold = 100\n#\n# # %%\n# from myproj.data import load_sales, filter_by_threshold\n# from myproj.analysis import summarize_by_region, plot_revenue_trend\n# import scrapbook as sb\n#\n# # %%\n# df = load_sales(f\"data/sales_{month}.csv\")\n# filtered = filter_by_threshold(df, min_amount=threshold)\n#\n# # %%\n# summary = summarize_by_region(filtered)\n# summary\n#\n# # %%\n# plot_revenue_trend(filtered)\n#\n# # %%\n# # Glue summary for downstream pipelines (papermill).\n# sb.glue(\"revenue_total\", float(summary[\"amount\"].sum()))\n# sb.glue(\"by_region\",      summary.set_index(\"region\")[\"amount\"].to_dict())\n#\n# Stay in notebook when:\n#   - Output IS the deliverable (analysis, report, viz)\n#   - Iterating on visualization / EDA\n#   - Audience is humans reading top-to-bottom\n#   - Computation is the explanation\n#\n# Leave for .py when:\n#   - Code is reused in 3+ places (extract to module)\n#   - Code is run unattended (script + scheduler)\n#   - You have unit tests (notebooks aren't great test hosts)\n#   - It's a service / API / pipeline (notebooks aren't long-running)\n#   - State management matters (notebooks have hidden state)\n#   - Multiple developers contribute (.py + git is much better)\n#\n# Hybrid:\n#   - Module (.py) holds reusable functions; notebook (.ipynb) drives + visualizes\n#   - papermill executes the notebook as a pipeline step\n#   - jupytext keeps a .py companion for git/code review\n#   - nbval/papermill+scrapbook test outputs in CI\n# Decision rule:\n#   exploration / EDA                       -> notebook\n#   analysis report (one-time)               -> notebook + nbconvert HTML\n#   recurring report                          -> notebook + papermill scheduled\n#   reusable code                              -> .py module; import from notebooks\n#   long-running job                            -> .py script; not a notebook\n#   service / API                              -> .py; FastAPI or similar\n#   ML training pipeline                       -> .py orchestrator + notebook for analysis\n#   shared library                             -> .py with tests; never a notebook\n#   prototype to production                    -> notebook → extract to .py incrementally\n#   stuck in cell-order hell                  -> \"Restart and Run All\"; if breaks, refactor\n#   hidden state bug                            -> use copy() not inplace; immutable patterns\n#\n# Anti-pattern: shipping a notebook AS the production code. Notebook\n# scheduled with papermill, fine. Notebook IS the API server, not\n# fine — Jupyter wasn't built for that. The discipline: notebook is\n# DEVELOPMENT FLOW; .py module is PRODUCTION CODE; papermill bridges\n# the two when \"scheduled notebook output\" is the deliverable."
                  }
        ],
        tips: [
                  "The \"Restart and Run All\" test is the single best smell detector. If a notebook only works in the order you happened to run it, refactor.",
                  "Extract any function used twice into a `.py` module under `src/`. `pip install -e .` makes it importable from notebooks.",
                  "Avoid `df.x.inplace = True` and `df.method(inplace=True)`. Returning new DataFrames is the same memory cost in modern pandas and removes hidden mutation.",
                  "For monthly/scheduled reports, the right pattern is a thin notebook (imports + viz) + a tested `.py` module + papermill for scheduling.",
                  "Use `jupytext` paired notebooks so the `.py` companion is the git-tracked source. Notebook for run/preview; `.py` for review.",
                  "Notebook is for INTERACTIVE EXPLORATION and ONE-TIME REPORTS. For services, APIs, batch jobs, scheduled work — write `.py`."
        ],
        mistake: "Shipping a notebook AS production code (web service, batch job, recurring pipeline). Notebooks have hidden state, no tests, fragile cell-order dependencies, and no proper concurrency story. The right pattern: tested `.py` module for the logic; thin notebook for analysis + viz; papermill for scheduling notebooks that produce reports.",
        shorthand: {
          verbose: "# Notebook IS the recurring job — fragile + un-debuggable\n# notebook.ipynb scheduled directly via cron, runs all 50 cells\n# Some cell mutates global state; next run sees stale data",
          concise: "# Module + thin notebook + papermill\n# src/myproj/report.py — tested business logic\n# notebooks/report.ipynb — calls the module, glues outputs\n# Airflow papermill task: pm.execute_notebook(report.ipynb, ...)",
        },
      },
    ],
  },
]

export default { meta, sections }
